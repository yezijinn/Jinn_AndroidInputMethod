# 更新日志

> 按时间顺序记录。

## 2026-09-15 首批修复（基线 e4ce2fd）

语音模块（MicRecorder / AsrClient / Protocol / MicButton 及录音、WebSocket、识别链路）全程未动。

最重的两个 P0。按压反馈一直不生效：OnTouchListener 把事件吞了，pressed 恒为 false，改成 setPressedVisual + handleKeyTouch 三分支驱动。粘贴状态机同病：广播不回来 pasting 永 true，暂存文本无时效会误粘，加 resetPasteState() + PENDING_PASTE_TTL_MS=10s。

其余修复：

- 候选刷新重复 queryWithCompletion() → 复用结果、日志降 v
- Root 审计：su() 不排空 stderr、waitFor 无超时会挂死、checkBackupConfig 硬编码 → 加超时 + 解析 backup_rules.xml
- 删库用 readableDatabase 写 → writableDatabase
- showClipboardPanel 注释与实现相反；.gitignore 忽略云凭证 ses\_*.json
- 构建：阿里云镜像（直连 Google/Maven 失败）；Gradle 8.9 手动放置（Wrapper SSL 失败）
- 语音模块已知未动：refreshConfig 死代码、AsrClient/MicRecorder 零测试、VAD 120s 偏长

当日补充批次：

- 字母键抬起 isInsideKey() 重判（滑出键位外扩 8dp）；下推 SQL 先过滤再解密；面板 onPanelShown 清 revealedIds
- 抽出 ClipboardFilter + 测试；搜索面板明文直显改掩码 + 二次确认（隐私功能，09-16 已移除）
- 设置页四处：上限失焦不保存、Root 开关递归（suppressRootSwitchCallback）、导出回调未判销毁、Spinner 早于 loadPrefs 改写默认模式（defaultModeSpinnerReady 闸门）
- 退格双击态永不超时 → clearGestureWindowMs=500；面板异常 26 键消失 → restoreLettersLayout；切搜索不清拼音
- V 级日志只走 logcat；isTruePrefixOfSyllable O(n)→O(1) 预建音节前缀集合；保活缓存永不刷新 → 移除
- 真机：签名包 10.6MB 装机；内存 165/384MB；前台保活 SecurityException → START_NOT_STICKY

## 2026-09-16 词库 / 生僻字 / 分层

「企鹅」打不出，排查下来是零声母连写歧义：qi+e 会被切给「切」等字，全表共 144 个歧义键。写了 detect_ambiguous_keys.py 扫描，add_words.py 补 11 词，dict_builder.py 的 compact 重复键一并修掉，PhraseDictIntegrityTest 防回归。

- 生僻字开关（默认关）：依《通用规范汉字表》6500 常用，BooleanArray 加载期过滤；键 104.8 万→89.3 万
- APK 10.6→8.4MB：词库 xz 流式解压（org.tukaani:xz:1.9 + noCompress），加载独立线程不阻塞 UI
- 词库分层：基础包（≤3 字）进 APK、扩展包（>3 字）下载；loadExtensionDict + 同键合并
- 分发走 Releases 附件（Gitee raw 大文件 404 须走附件）；多源回退 + 下载后自动重启 IME
- 四字成语入基础包：基础 628.8k→1,006.3k 词、扩展 601.3k→223.8k；APK 6.2MB

## 2026-09-16 移除保活 + 符号重构

保活整套删除：KeepAliveService / JinnAccessibilityService / RootShizuku / BootReceiver + accessibility.xml + 5 权限 + shizuku 依赖，APK 6.2MB 不变。

符号层重构：方案按钮提示目标方案；「编程」分组（后扩 7 页：Python/Java/C#/JS/C++）；符号键「返回」替代「ABC」；键面居中 centeredStyle。平/片假名合并「日本」组，「特殊」前移 6 页，最终 10 组（常用→编程→标点→特殊→序号→数学→单位→日本→拉丁→注音）。

- 中英切换键上下两行「中文/英文」，当前行 aurora_purple 加粗（SpannableString，12sp / 行距 0.9）
- 序号组 4→7 页；修数据错：第 2 页 z/n 同值 Ⅷ、v/m 同值 Ⅻ → 改 ⅰ ⅱ 补位
- 日本组补末页 26 键（拗音 + 长音符浊点，片 ヷヸヹヺ）→ 8→10 页
- 剪贴板搜索页「退出搜索」占两行 → 13→12sp

## 2026-09-16 build_apk.py 健壮性

实质缺陷是缺 zipalign：Python zipfile 重打包会打乱 AGP 产物的 4 字节对齐，apksigner 不负责对齐。流程固定为 AGP → remove_meta_inf → zipalign -p -f 4 → apksigner sign，补事后复核（apksigner verify + zipalign -c 4）。

其余 8 处小修：.idsig 清理列表、docstring raw 串、find_build_tool 按 os.name 选后缀、签名方案校验改正则、临时文件 try/finally、remove_meta_inf 流式 256KB 块、verify 列表传参、密钥缺失补 JINN_KEYSTORE_ROOT 示例。端到端跑 3 次全过，V1=false / V2=V3=true。

## 2026-09-16 回车键输出原始按键

双拼下输 but 回车，拼音串原样留着——既不提交候选也不输出英文，performEnter() 原本没这个能力。新增 takeRawComposing() 取原始按键（composing 存原始按键、不做双拼转换），拼音串非空时 commitText 原文并 return。双拼按 budv 输出 budv，不会变 budui。

## 2026-09-16 Lint 扫描

修 3 处：手写版本注册广播 → ContextCompat.registerReceiver；requestShowSelf() 是 API28 方法而 minSdk=26 → 加 SDK_INT >= P；SDK_INT < N 在 minSdk26 下恒 false → 删。2 error 清零。

确认无问题：全项目 !! 0 处、rawQuery 全 use{}、广播三处配对、PinyinEngine @Volatile + synchronized、空 catch 仅 1 处有注释。未修：MicButton.kt:98 每 onDraw 新建 RadialGradient（语音模块，不动）、onTouch 未调 performClick 4 处、UnusedResources 49 处。

onTouch 后补 performClick（5→2）：字母键 ACTION_UP + 水平位移 < touchSlop（避滑动翻页误报）、退格、麦克风；MicButton 重写未动。switchToVoiceKeyboard() 在 asr.state != ONLINE 时 Toast 拦在入口。清理保活遗留文案 19 条；其余 UnusedResources 不动（isShrinkResources 已裁，清源码不减小 APK 且有 getIdentifier 动态引用风险）。

## 2026-09-16 Binder 崩溃 + 词库压缩

复制长文即崩：粘贴广播携全文，超 Binder 上限（≈1MB）抛 TransactionTooLargeException。改广播只传 id，接收方 contentById(id) 读库解密。

词库压缩调参：preset=7+lc=4+pb=0 再省 1.59% 采用；xz 9e（+48MiB 解压内存）、brotli（需引库）、二进制格式（工程量大）、砍低频词（有损）都否了。重压后逐字节解压校验无损，真机加载 8550ms，APK 6.2→6.1MB。

## 2026-09-16 词库整体换 rime-ice

原词库三个问题：无词频字段（候选没法按常用度排）、16.2% 行 GBK 乱码（瀵勮繙 类永远打不出）、连写歧义词靠手工维护。rime-ice 全解决。

convert_rime_ice.py 读 cn_dicts/{base,ext}.dict.yaml，解析「词 拼音 词频」按词频降序聚合，≤4 字 / >4 字切基础包与扩展包。APK 6.1→4.5MB；加载 8550→6602ms；拼音键 826,897→787,991；词条 100.6 万→88.2 万（多的本是损坏词）。真机：qie→[企鹅,且,切]、xian→[西安,西岸,锡安]、jie→[饥饿,极恶,极饿]。

游戏词库（docs/games_ime_dict.txt 1361 条 + klbq_ime_dict.txt 329 条）清洗合并：删 145 条垃圾留 1545 条，字母数字必删——拼音引擎按合法音节切、字母数字切不开，当键永远打不出。扩展包 183,919→185,408 键；Release 附件同步，两源 Content-Length 均 1,901,048。

## 2026-09-16 重复候选修复

loadPhrasesReader() merge 直接拼接不去重，同键同词双候选（aersasi 出两个「阿尔萨斯」，共 877 词条 / 404 键）。根因两层：游戏词合并只查扩展包 → 合并前载入基础包全词排除（跳过 415 条）；rime-ice base/ext 自带重叠 → 代码层先建「已见」集合，扩展包只追加未出现词（数据修完仍剩 613 键由代码消除）。修复后全库无重复。

顺排核对：基础 604,077 + 扩展 185,408 键切分失败 0；常用词覆盖抽查 20/20；单字表 17,117 字符全覆盖 rime-ice 8,182 且更全；音节 414 非问题（单字表实际覆盖数，缺的 8 个本无对应单字）。

## 2026-09-16 可选词库延迟加载 + 词库页

可选包（长词包 / 腾讯大词库）原本和基础包一起加载，首次候选就绪从 6.6s 拖到 25.4s。改 loadOptionalAsync 后台补齐；加载后须重 finalizeLoad()——sortedPhraseKeys 是加载时快照，不重排就查不到新词，这个坑差点漏掉。

新增 OptionalDicts.kt（清单）+ DictManagerActivity.kt；设置页旧扩展词库卡片换单按钮。容器线程安全化：Map → ConcurrentHashMap、有序表 @Volatile 快照整体替换（防扩容桶成环死循环 / CME）。其余：候选截断渲染 24 条、加载失败 runCatching 留栈、下载失败 Toast、OptionalDictMergeTest。

## 2026-09-16 剪贴板 + 键区圆角

- 删不可达 ClipboardHistoryActivity（719 行，exported=false 无调用点）+ Manifest + 22 字符串；入口收敛为面板两处
- 容量裁剪三级优先：普通 → 隐私（最后才动）→ 收藏永不参与
- 拖选状态 selectionActive 两处各存一份 → 复位对齐
- 布局数据抽离 KeyboardLayouts.kt（455 行），视图 2029→1574 行；新旧 1263 键值对逐项比对一致

键区圆角与间隙可调：原三行字母键两绘制路径圆角不一、大写/删除贴满高出一圈 → 统一参数。KeyAppearance.kt（圆角 0~~24dp 步进 1、间隙 0~~8dp 步进 0.5，NaN/Infinity 退回默认）；间隙语义是相邻键各内缩 gap/2、相加等于 gap。第四行不跟随（只圈字母键区）。发布 v20260916：d5be2da，APK 4,737,805B。

复核 6 项：删死代码 onBackspacePressed()（真路径 handleBackspaceTouch→deleteOne() 是其超集）和 formatSize()；剪贴板上限越界不回显 → 存完统一回写生效值；5 个 EditText 关自动填充（importantForAutofill=no，防密码管理器把 host/port 当账号弹保存）；33 处 Color.parseColor → R.color 资源色；PinyinKey 绘制分配移 setter 缓存、字号下限改键高比（MIN_LONG_TEXT_RATIO=0.06f）。覆盖发布 6103814，sha256 f45bf09d。

## 2026-09-17 删废弃词库源 + 文档对齐

删 tools/dict_builder/source/pinyin_phrases.txt（~28MB，被 .gitignore 忽略从未入库）：构建链不读、全仓无引用、含 573 行 GBK 乱码易误导。dict_builder README 补流水线图；工程约定新增「词库来源与生成」；DATA_SOURCES.md 按实测重写；订正 pinyin_chars/syllables.txt 实为 422 键/行。无代码变更，不重编 APK。

## 2026-09-17 双拼 7 套方案

键位改为从 schema 生成：gen_shuangpin_tables.py 解析 rime-ice double_pinyin*.schema.yaml 的 speller/algebra，按 librime 代数语义（xform/derive/erase/xlit）施加到 422 合法音节，产出 ShuangpinSchemes.kt（730 行生成文件）。7 套码数：自然码 434 / 小鹤 424 / 搜狗 429 / 微软 435 / 紫光 417 / 智能ABC 413 / 加加 429（搜狗/微软/紫光需分号）。

verify_shuangpin_migration.py 676 两键码对拍：3 差异全是旧 BUG（rw→rua 等，旧 242 条独有码产物全非音节），换表零功能损失。键面提示由码表反推（finalHint 从 codes 反推韵母、initialOf 给 zh/ch/sh 红字），顺带修了旧 shuangpinHint('o') 手写成 ou 而引擎是 o/uo 的不一致。分号键：搜狗/微软/紫光的 ing 落 ; → 第三行 m 与删除键之间加键，仅需要时显示。

状态拆三键：useShuangpin（面板）/ shuangpinScheme（设置页下拉，只增不改）/ effectiveShuangpinScheme（派生），读取带迁移。编不出的音节 hng/hm/m/n/junding 直接放弃（词库 0 条 / 脏数据）；不做 rime 多拼写容错（与两键一音节的键位冲突）。ShuangpinSchemesTest 16 例。发布 v20260917：cff70c7，APK 4,774,669B，sha256 5a172f74。

两轮后续：面板「拼音/双拼」按钮恢复原样（文本不改），删八态循环改 toggle，方案只在设置页改——顺带修两个真机暴露的缺陷：面板切回双拼丢方案（改读 shuangpinScheme 恒定 1..7）；设置页下拉静默改写方案（onRestoreInstanceState 迟来的 onItemSelected 穿透闸门 → 只有触摸过 Spinner 才写；「默认键盘模式」下拉同类隐患一并修）。下拉再去掉全拼（只 7 套，选中即记方案 + 启双拼）。覆盖发布 9b31731、4a0b0ea。

## 2026-09-17 双拼性能 + 冷启动

双拼热路径无问题：toQuanpin 每键 133~~368ns、表常驻 0.254MB。唯一开销是 7 套表首次访问 57~~79ms → 惰性构建 + warmUpAll() 服务创建后后台预热。

冷启动诊断出真问题：基础词库 601,139 键 10,672ms、可选包 34,435ms；就绪前 query() 静默返空、加载期无提示、可选包固定 5s 撞打字、PSS 305MB 易被杀。

高频子集先行：gen_hot_dict.py 产出 assets/hot_phrases.txt.xz（4 万词 / 34,515 键 / 220KB，与全量同源同序、同键同词取最大词频）。一致性关键在子集必须是全量词表前缀，否则并入后候选序漂移——生成器「取权重≥阈值全部词」并自校验前缀。load() 改两段：先载子集 + 单字表 + 音节表 → loaded=true 可输入，再载全量合并。加载期提示内联候选栏。真机首次可用 243~~314ms（此前 6~~10.7s），全量补全 6885ms。代价 APK +220KB；改词库须重跑 gen_hot_dict.py（否则测试失败），这条写进了工程约定。

可选包空闲加载：息屏 / 键盘收起后闲置 20s / 兜底 180s，先到先得，加载线程 THREAD_PRIORITY_BACKGROUND。

## 2026-09-17 词库二进制索引 Stage 1-2

Stage 1 构建期产 assets/pinyin_index.bin.xz（magic/version/摘要 + keysBlob + keyOffsets + wordsBlob + wordOffsets，同源同序）；运行时 PhraseIndex 二分 + 按需解码、共享字节数组。统一 phrasesFor(key)；生僻字过滤移查询期（索引与开关无关可复用）；删 69 万条反向索引，predict 改依赖「候选→键」短期映射（上限 4096）。真机：首次可用 238ms、全量就绪 6~10.7s→2.9s、PSS 90MB；APK 4.30→5.04MB。实现期修两个问题：头部 24/30 字节不一致致偏移全错；删反向索引致 predict 失效。护栏 IndexParityTest（文本 vs 索引 300 键逐词一致）、IndexFeatureRegressionTest。

Stage 2 可选包索引化：PhraseIndex.build(Sequence, stamp) 单趟流式构建同套格式；设备端缓存 filesDir/index/<源>.idx（源 length:mtime 不符才重建）。查询改逐段查找（运行时子集 → 基础索引 → 各可选索引）LinkedHashSet 去重，mergedCache(4096) 避免重复合并。真机：首载 21~~34s→8.7s、后续 59ms、PSS 305→110~~113MB、磁盘 ~40MB。IndexBuilderParityTest（Kotlin 构建器 vs Python 脚本逐字节一致）。修 3 个问题：mergedCache 未失效、单例污染、fixture 键序。

索引 v2：两张 u32 偏移表（4.83MB）换成 keyLengths(u8) + wordLengths(u16)，读前缀和还原，Python 与 Kotlin 同步升级。APK 5,745,490→5,184,338B；基础索引加载 2289→1700ms；可选包复用 59→44ms。v1 旧缓存自动重建；构建脚本加长度边界自检（键 ≤255B / 词表 ≤64KB）。

当日专项审查修 6 缺陷：PhraseIndex.parse 段长校验 Int 溢出 → Long 累加 + 精确总长比对；mergedCache 失效点收敛 finalizeLoad()；裸 HashMap → ConcurrentHashMap；gen_hot_dict.py 读已删文本资产静默废闸门 → 改从源现算；verify_shuangpin_migration.py 判据定义未调用 → 真正接入 valid_syllables()，回归 0；引用腐化脚本改读留档文本 + 兼容 xz + 显式警告。

## 2026-09-17 键面韵母提示规则

四条：绝不显示与该键字母相同的韵母；ü 两写法只留 v；总行数 ≤2（含红字行）；同行长者在前、同长按规范序。写入生成器模板重生成 ShuangpinSchemes.kt。顺序必须先去重 ü 再剔自身字母，否则 v 键的去重条件永不命中。生成器词典源改读流水线留档。HintRuleTest 7 套 × 26 键全量 + 点名项核对。

## 2026-09-17 深度路径审查

predict 的成本悬崖：keysWithPrefix("ni") 6,185 键 / 2.18ms，实例化全部 String——predict 加「收满 MAX_PREDICTIONS 即停」安全阀。单字候选无预测是既有行为（单字源与词库两份数据），非回归。拖选状态机发散修复：showDirectionPanel 补 onSelectionModeChanged(false)；onStartInputView 切输入框未复位 → clearSelectionState()；三处重复逻辑收敛。

## 2026-09-17 librime 对照与取舍

代数语义对照 librime 1.17.0 calculus 逐条核对，修 4 个潜伏差异 + 1 个解析加固：xform/derive 空结果丢弃；xlit 不等长报错（原 zip 截断）；正则统一 ASCII；parse_algebra 按 YAML 缩进收紧 + 未知 op 报错。重生成 ShuangpinSchemes.kt 逐字节不变（git diff 空）——对现行 7 套零语义变更，价值在上游 schema 调整不会静默算错。未发版。

设计取舍：吸收了内存映射索引和前缀枚举不物化字符串；音节图 + DP 分词试过实测变差——key 恒等于输入使 DP 退化成只比音节数，真实词库切出 ng 鼻音碎片（11 语料 5 变坏）；双数组 Trie 不值得（二分已够快、加载被解压支配）；引擎流水线不现实（XL 规模、与语音链路交错）。结论连同反例写进 dictionarySegmentation/dfsSegment 的 KDoc。

落地 mmap：PhraseIndex 读 ByteArray 改 ByteBuffer（堆内 wrap / 映射 map 共用逻辑）；ofMapped(file) 失败返 null 回退；可选包缓存改「临时文件 + 原子改名」——覆盖被 mmap 的文件会 SIGBUS。新增 forEachRangeWithPrefix（只回调区间不建 String）+ collectLongerSuffixes（字节级比对），删死代码 keysStartingWith。真机：基础索引加载 1798→48ms（37×）、两段式 2689→949ms、PSS 77.7MB。IndexMappedParityTest 3 例；改了生产代码 → 覆盖发布，sha256 051953d7。

## 2026-09-17 用户词频学习

语义对齐 librime：点候选 / 空格取首候选 / 点预测词时学；dee_new=commits+dee_old*exp((tick_old-tick_now)/200)（半衰期≈139 天）；权重 <0.15 丢弃、上限 3000 条。UserFrequency.kt 内存 ConcurrentHashMap + 文件「词 权重 天」原子写 + 防抖 2s；排序注入 query 收尾；设置页开关默认开。真机 A/B：预置「拟好 9.000」→ 候选提前、空格上屏后落盘。UserFrequencyTest 9 例。


词频缺陷排查 5 项：UserFrequency.file 缺 @Volatile；累加未钳「未来 day」（系统回拨放大 22 万倍霸榜）→ coerceAtMost(0)；flush 异步可能丢尾批 → 同步写（≤3000 行 1~3ms）；学习词无行格式防御 → 入口拒收；加载时序移到高频子集就绪后。

索引缓存生命周期：可选包加载把基础索引缓存当失效删掉——判据是「文件名去 .idx 是否在词库包列表」，基础缓存源 APK 资产永不入列 → staleOptionalCacheNames() 显式排除 base. 前缀 + IndexCacheLifecycleTest 钉住。writeIndexCacheAtomically 失败无日志且调用方误报成功 → 补日志 + 按返回值区分。

真机验证矩阵 5 项一致（冷启动 442ms + 97ms mmap / 预置学习词 / 词库下载落盘重启 / 失效缓存只清失效 / 文案核对）。存疑未真机：腾讯词库下载、设置页开关点击、拖选中断、剪贴板全链路、语音链路（代码 + 单测覆盖）。

文案订正：dict_startup_cost_line1~3 数字与机制描述不符 → 改「空闲后台加载、首次构建索引约 N 秒不影响打字、之后 mmap 复用≈0.05s」。设置页 69 字符串核对（settings_subtitle「26键双拼(自然码)」→「7 套双拼方案」；生僻字说明去「不加载/更省内存」；「点剪贴板」→「点历史」）；词库页 28 条数字均实测一致（长词包 1.81MB、腾讯包 6.36MB / 971,257 词条）。

## 2026-09-18

发布 v20260918：工程约定修性能数字（0.3s 可打字 / mmap 复用 0.05s / 首次 1.8s / PSS 77.7MB）；README 中英同步（双拼 7 套 / 分类词库空闲加载 / 用户词频）。APK 5,188,434B，sha256 f3337cd6，双端新建 Release。

注释与文档订正：隐私功能移除后一批注释仍按隐私写 → 改「只提收藏」；PinyinEngine 类头数据源按 assets 实际重写；术语统一（剪贴板面板 / 候选 / 落盘 / 内存映射 / 双拼方案）；长词包与扩展包两叫法在定义处加对照；README_EN APK 4.5→4.9MB。

按键响应取证：真机逐键 p50 7ms / p90 12ms / p99 48ms；janky 6.94%；ANR / Davey 0。两个假象排除：初测「键盘不弹渲染 0 帧」实为息屏；uiautomator 抓不到 Canvas 自绘键文本，坐标按代码布局推算。唯一受限窗口是首切未预热双拼方案同步建表 0.21~1.13ms 量级，warmUpAll 已覆盖。

「拼音打错/太长时字母键卡住」修复：根因是 dfsSegment「16 条完整路径」上限只数已完成路径，切不通的输入前缀组合爆炸——实测 31 字符 36.8ms，每 +6 字符 ×4~5，更长秒级。新增 suffixParseable() 自后向前 DP 剪枝，切分结果完全一致，25 字符 8.61→0.73ms、31 字符 36.8→0.43ms。SegmentCliffTest 2 例。

冷启动窗口打字卡顿：删 base 缓存弹键盘连打 15 键，p99 14→300ms、janky 0.58%→12.07%。根因是基础索引重建 1.8s 连续解压 + 14.7MB 大分配挤主线程。修法分片解压每 1MB 让出 CPU、原子写缓存每 256KB 让出，总耗时只多几十毫秒。复现标准：删 filesDir/index/base.*.idx → force-stop → 启动连打 → dumpsys gfxinfo。

JVM 压力 + 模糊测试：KeyboardStressTest 复刻触发逻辑，7 场景 1514 键 p50 ≤0.156ms、max ≤0.83ms；7 套首用含建表 0.21~~1.13ms——顺带纠正了「7 套表一次全建几十毫秒」的旧判断。模糊测试 589 例（随机字母 400 / 真实拼音打乱 60 / 合法音节拼坏尾 120 / 超长 60~~240 字符）max 3.437ms（a×240）。

长按退格 ≥1.2s 且拼音串 ≥12 字符 → 清空拼音串不动已上屏（与「双击 + 长按清空输入框」成两级）；onTripleBackspace 改 clearComposingState()（原漏清 lastCommittedWord）。门槛降 0.6s/8 字符、候选栏加「清空拼音」按钮、输入长度硬上限三项评估后未实施（硬上限会静默丢键，240 字符才 3.4ms，不值得）。

三项待验补测：adb install -r 直接 Success（-99 拦截可重试非永久拒绝）；冷启动窗口 p99 300→101ms、janky 12.07%→6.78%；高频压测 Davey 1229→0ms、p99 ≤32ms。长按退格 1.26s 触发清空 69 字符。坐标留档：1080×2280 屏，行1 y=1680 / 行2 y=1840 / 行3 y=2020 / 空格(540,2200) / 回车(1015,2200)。

候选预测词开关：Prefs.predictEnabled（默认开）+ 设置页开关（紧跟「用户词频学习」）；产出点加闸门；refreshCandidateBar() 开头自愈；即时生效免重启。关掉只影响「选词后预测」，不影响候选 / 补全 / 词频排序。

双拼下按 g/h 拼音行不更新：拼音行显示的是 queryInput（转换后全拼），双拼下与输入常不同（jg→全拼被吞成 j）→ 两处改显示 input（实际按键）。真机双拼自然码逐帧 j→g→h 拼音行逐字更新、候选保持[级 集 几 及 机…]。模式敏感的功能，验证前先确认当前模式；「不更新」类问题必须核对渲染结果（截图 / UI dump），只看日志会整类漏掉。

## 2026-09-19

工程约定更新（双拼 7 套 / 测试规模 / 隐私移除事实 / 「按键与模式排查方法」章节）；README 键盘特性补三条。推送修复：git push … | tail -1 && break 以管道退出码当结果，origin/main 曾静默落后 9 提交 → 改真实退出码重试补齐三处。发布 v20260919（sha256 30f288ec）+ 覆盖（0df86371）。

文档与文案订正：README / 工程约定隐私残留（分类仅 全部/网址/数字/收藏，长按仅 收藏/删除）；词条数 18.5 万 / 95.5 万（OptionalDicts.kt 同步）；APK 4.95MB；THUOCL README 按事实改写；两 README 补 26 键圆角/间隙可调；退格手势四级写入工程约定；订正 ClipboardPrefs.maxItems KDoc 默认 200 实为 500。

缺陷修复批次：su() 先读流后 waitFor 致超时形同虚设 → 双路后台排空；host 校验 trim 不对称主线程崩 → normalizeHost；剪贴板延迟重试堵 BackgroundIo → 主线程 Handler；TextSelection.lineStart 行首边界；UserFrequency 落盘时序；复审修 3 处新引入缺陷；SymbolLayoutTest 数据护栏。

剪贴板容量上限：单条 256KB 超出不入库（不截断，先字符数快筛再 UTF-8 精确）；总量 100MB 按最旧非收藏淘汰（收藏永不删）；单条按明文字节、总量按库内密文（base64）。ClipboardLimitsTest 15 例。

搜索节流：分块 300→50（解密峰值 ≈196MB→≈33MB）+ decryptWindowPeakBytes() 预算护栏（放大系数实测 2.33 取 2.5）；逐块发布加 120ms 节流合并；附带修好空库不更新。密钥外移：keystore 归档仓外凭据目录，发布经 JINN_KEYSTORE_ROOT 传入，未提供时产未签名包。

源码安全审查：P1 剪贴板粘贴广播对外可达（任意应用 am broadcast 可注入解密后历史）→ 删接收器与 ClipboardDb.contentById；用户输入正文 I 级落盘 → 统一降 v。P2 词频未排除备份 → 补；词库下载无上限 → copyCapped() 64MB。P3 索引偏移前缀和 Int 溢出 → Long 累加越界返 null。判定非缺陷：SQL 注入（? 占位）、更新跳转 URL、剪贴板加密（AES-256-GCM 随机 IV）、su 拼接。遗留：语音正文 i 级日志（语音模块）、词库下载 sha256（后补）。

## 2026-09-20

四轮安全审查。P1：密码框内容被学进用户词频 → InputFieldPrivacy.suppressLearning(inputType) 按 EditorInfo 设闸门（TYPE_NULL=0 缺省须 null，这个坑排查了小半天）；拼音串每键 I 级落盘 → 降 v；词库下载无完整性校验 → checksum 字段 + 落盘前校验 + 硬编码摘要；暂存粘贴丢输入框身份 → 一并恢复 pendingPasteFieldKey。

P2：搜索态五个入口落宿主（回车 = 退出搜索、逗号句号 = 追加搜索、清空手势 = 清搜索、commitComposing 搜索态丢弃）；长按连删拼音删空后继续删已上屏 → 删空即停闸门；自身剪贴板标记误置致真实复制被丢 → 删整套机制；imeOptions「不要个性化学习」纳入敏感判定；搜索内存预算改 UTF-8 字节计；备份排除 SharedPreferences 两文件；防火墙审计按 SDK_INT 挑生效文件；更新检查终态 URL 断言 https；后台 IO 队列 runCatching；整窗解密失败「游标没前进才停」；词库删除/下载互斥；data_extraction_rules 补 SQLite 侧车；长按退格判据用按下时长度。

P3 若干：崩溃快照滤 V 级正文；content_hash 唯一索引；防火墙空集合误判；面板点击按稳定 id 取条目；maxItems/defaultKeyboardMode 读侧钳位；数字分类下界 4 位；su() 超时 destroy + 排空；诊断导出原子写 + 清理；搜索/剪贴板面板互斥补齐；合并缓存失效；候选→键映射最新写入者胜；ACTION_POINTER_UP 不复位；连删延时任务 onDetachedFromWindow 收尾；音节前缀集合整体替换；补全缓存并发容器；dumpLogcat suffix 白名单；NaN/Infinity 权重过滤；特殊组天气页错字；UpdateChecker.readCapped 常量名错。

发版 20260920：产物 5,196,626B / sha256 4d1b886d（后同轮覆盖 3914cd8e）；提交 4e44b5f。发布前全量单测 + lint + 打包链 + 证书同签名；移除 5 条指向已删类的 R8 规则；修 gitee_release.py 的 resolve_release（tag 无 Release 时 Gitee 返 null 按 dict 解引用崩）。真机覆盖安装数据保留，启动 / 输入 / 拖选 / 复制粘贴正常。

批次 A（纯逻辑）：PhraseIndex.build 同键多行静默少词（二分查找永久跳过第二词表）→ 逐键写出 + 同键连续行合并；UserFrequency.flush 写失败仍清 dirty → 锁内成功才清；setThreadPriority 在 runCatching 外 → try/finally；Thread.start() 失败复位；onReady 仅成功才回调；检查更新 3s 闸门失效 → 15s 看门狗 + UpdateState 收敛两态；filterOwnVerboseLines 跨年时间戳 → 正则加可选年份前缀。

批次 B（IME 交互）：搜索态功能面板动作落宿主 → 搜索态候选栏只渲染「退出搜索」；双击 + 长按清空输入框 AOSP 失效 → setSelection(0,0)+deleteSurroundingText(0,Int.MAX_VALUE)；搜索态跨会话残留 → 两处补 hideSearchPanel()；copySelection 未窗口换算；分号键 ACTION_UP 缺命中判定。

批次 C/D：dumpLogcat 失败路径残留含正文的快照 → produced 标志统一收口；su 三管道排空 + 超时 destroy；崩溃处理同步等 logcat 最长 10s → 独立线程 + 2s waitMs；剪贴板监听主线程 coerceToText（URI 型同步读整流）→ 统一 BackgroundIo；loadOptionalAsync 与 load() 争用 this 锁 → optionalLock 专锁；面板分页状态位异常不复位 → runCatching 复位；收藏切换截回第 1 页 → refresh(resetScroll=true)；预测绕过生僻字过滤 → predict 出口统一过滤。

搜索面板布局精简：删输入框右侧「退出搜索」按钮，输入框独占一行（退出入口仍完备）。输入方案全局化：ShuangpinScheme.ALL（全拼 + 7 套）；设置页下拉选中即全局生效；键盘功能面板删首个按钮。候选栏右端新增 ✕（默认 GONE，有候选/预测/拼音串时显示）；移除「长按退格整串清空拼音」（删除键长按恢复标准逐字连删语义），保留「双击 + 长按 >800ms 清空输入框」。

当日全量审查：PhraseIndex.build 合并期 O(n²) 重建 → LinkedHashSet 保序累积；合并未去重 → 与文本路径对齐；dumpLogcat 误删同名旧快照 → 进 try 先删；settings_usage 手势文案过期。deleteAllText 的 Int.MAX_VALUE 经 AOSP BaseInputConnection 核对无溢出（numDeleteAfter>0 守卫挡负区间）。既有不改：搜索新查询不回顶、空查询显「未找到」、粘贴失败无 Toast、输入方案切换后键盘侧无即时同步（无发送方）。覆盖发版 ec6f705（三次独立构建一致，发布规范写入工程约定：main 主分支 + 覆盖式 + 双端三方校验）。

## 2026-09-21

符号面板「常用」拆「全角 / 半角」：全角组只放全角符号（26 键满，8 个 ASCII 换全角变体）；半角组 2 页；分组 10→11。两组禁数字 / 字母 / 汉字（半角 32 个 ASCII 重排两页，数字走数字层）；编程组删第 1 页纯符号页 7→6。排版规范定为非末页恒 26 键先放满再翻页（全表核对仅半角违规），护栏升级全表规则。符号层返回键红色粗体（btnSymbol SpannableString，退出自动还原）；shift 符号层禁用（isEnabled=false + alpha 0.4 + 点击守卫）。全角组扩 68 项三页（26+26+16）；半角序与全角逐项对应（31 个有对应的按其全角出现次序排，$ 无对应放末尾）。

默认值调整（仅未写入时取值）：词频学习开→关、圆角 0→1dp、间隙 0→0.5dp，其余不变。

主题批次 1：values/colors.xml 重写亮白 + values-night 暗黑，两套 40 令牌逐名对齐；5 处 drawable 硬编码色收敛；对比度全 ≥4.5:1（实测）；真机 root 切 uimode night 浅深同步转无闪烁。

主题批次 2：ThemeManager（isDarkNow / minutesUntilSwitch 纯函数 + wrapContext 只覆盖 uiMode）；Prefs 三键；设置页「主题」卡片；两 Activity attachBaseContext、IME onCreateInputView / onStartInputView 应用；默认跟随系统。审查修复：键盘可见时改主题不换肤 → notifyThemeChanged() + applyThemeIfNeeded() 共用；定时跨切换点不换肤 → scheduleThemeTick()；跨零点 floorMod 归一、恰在切换点返 1440、windowLightNavigationBar API27+ 兼容核对。ThemeColorParityTest 2 例。

服务层取色用错 Context（renderLink 走系统配置）→ 新增 keyboardThemeCtx；主题时刻 setter 未归一 → floorMod；StaticFieldLeak → WeakReference + onDestroy 置空；Prefs.themeMode 非法值统一落「跟随系统」；换肤静默丢未上屏内容（定时到点重建丢拼音串/预测词）→ hasPendingInput 有则延后到下次弹出。

符号分组排序页：移除面板长按拖拽整套交互（分组按钮恢复纯点击）；新增 SymbolOrderActivity（每行分组 + ↑↓ + 恢复默认，即时落盘）；onPause → JinnIme.onSymbolOrderChanged() 重建键盘。

「收藏」分组：默认第三位（全角/半角/收藏）可排序；组内自由增删、满 26 自动翻页、出厂预置 D I Y、禁止重复。FavoriteSymbols.kt 存 Prefs JSON；KeyboardLayouts.favoriteGroup(pages)；SymbolOrder.DEFAULT 插第三 + normalize 兼容旧串；FavoriteSymbolsActivity 按页网格 + 移除角标 + 添加输入框。FavoriteSymbolsTest 7 例。

审查修复：FavoriteSymbols.parse 归一（输出恒规范结构：保序去重 + chunked(26) 重分页、空页消解；原缺陷 favoriteGroup 只铺 26 键位、超界静默丢——编辑页见第 27 个、键盘不见）；重建守卫 hasActiveOverlay（剪贴板/搜索面板）与 hasPendingInput 合并为换肤延后判据；编辑页/排序页仅实际写入后才通知重建；删两个 0 调用 companion fun start；Prefs KDoc 归位。发布前探针实证：100 项正确分页 [26,26,26,22]、删下标两种算法相等、脏 JSON 回退不崩、归一幂等。

发版 20260921（4f3d050，APK 5,200,722B / sha256 c1db99af）+ 同日覆盖（0e8dcdd，5,217,218B / caadaae9）。

设置页改版：主页两列网格，新顺序：标题 → 点这里唤起键盘 → 检查软件更新｜打开下载页面 → 启用本输入法｜改默认输入法 → 符号分组顺序｜收藏的自定义 → 拼音输入方案｜键盘主题配色 → 自动唤起键盘｜加更多生僻字 → 用户词频学习｜追加候选词语 → 其余原样；44 控件 id 逐一核对。

## 2026-09-22

半透明键盘修复 D1~D7：剪贴板/搜索面板不参与透明 → alphaFaces 双档色集（背板 plate / 内容面 surface）；候选栏已构建面不即时刷新 → 末尾补 refreshCandidateBar()；语音面板覆盖不全 → applyTransparencyToVoicePanel()（MicButton 不动）；候选栏档位按内容状态选；死代码 + 诊断无条件执行 → 移入档位变化分支。

产品改名：显示文本 Jinn安卓 → Jinn拼音（strings.xml / README 中英 / KDoc）；包名 / 签名 / 数据 / DB / URL 不变；仓库标识与日志 TAG 非显示文本不改。

Android 16 实测有个插曲：透明度「消失」，查下来设备装的是当日早期构建——UI dump 文案还是旧版，而 versionCode 只到日期粒度，同日多次构建从版本号分不出来，白排查一场。覆盖安装后三滑杆 / 61% 透明度 / 无 FATAL 全过。附注：A16 收紧 shell，am start 无法启动 exported=false 页面；wm size Override 与 Physical 不一时以 dump bounds 为准；输入框首次聚焦可能弹系统 Autofill。

覆盖发版 269276e（gradlew clean 全量重编译，sha256 39e08ff2）。Gitee API 三个坑：更新正文 access_token 须放 body；正文 JSON 手工构造（PS ConvertTo-Json 把中文转 \uXXXX 令 payload 膨胀被拒）；PS 5.1 读返回中文乱码，校验以 curl.exe 为准。

入口行 + 键盘外观独立页：符号布局行下一行两按钮——「补充短语词库」（打开既有 DictManagerActivity）、「按钮圆角间隙」（新 KeyAppearanceActivity：标题 + ✕ + 圆角/间隙滑杆，定义域与换算一律走 KeyAppearance 与绘制逻辑共用一套边界；拖动落盘，键盘下次弹出套用）。设置页移除旧「键盘外观」「分类词库」两卡片；Manifest 注册新页。输入法测试简化：设置页首行改纯编辑框（actionDone），删发送链路；键盘外观页加同款编辑框便于对照调参。

复查：残留引用清零；DictManagerActivity 入口唯一；新页滑杆逻辑与旧实现逐行对照一致；lint 30→22（DisableBaselineAlignment×3 移到父容器、RtlSymmetry×3 补 paddingEnd、UnusedResources 删 theme_mode_label、顺带清历史 1 项）；注释订正（默认值 1dp/0.5dp；「设置页」→ 键盘外观页；工程约定补登 5 文件、入口段落更新）。

延后重建补重试：收藏页打一半拼音按 Home，再拉键盘收藏组还是旧数据而 prefs 已新——框架跨会话复用同一键盘视图（onCreateInputView 2 次 vs 弹出 9 次），「延后到下次创建自然生效」不发生。修复：pendingSymbolLayoutRebuild 延后置位 + recreateKeyboardView()（换肤/符号重建共用）+ onStartInputView 在 applyThemeIfNeeded() 后补一次重建（仍看 hasPendingInput）。外观页外层包 ScrollView（横屏键盘遮滑杆，scrollable 0→1）。

Release 旧 APK 清理：按「只留最新一版」，删双端 6 个历史 Release 的 APK 附件；词库包 3 个保留（分类词库下载源）。Gitee 批量连续 DELETE 被拦成 HTML 404，单条重试返 204；逐个删、间隔 ~0.8s 稳定，这条写进了工程约定。

界面文案修订 8 处：词库页、Root 增强、用户词频、双拼说明、收藏编辑、OptionalDicts 说明改写、ClipboardFirewall 去掉 Markdown 加粗、README 断句。

## 2026-09-22 ~ 09-23 编译警告 / lint 全量审查

全量 clean --rerun-tasks --no-build-cache 逐条回源码核实 51 条 Kotlin 警告（去重 26 处）后修到 0：PinyinKeyboardView 20 处 + SearchPanelView 3 处 lateinit → val（构造期必初始化）+ 删随之 isInitialized 恒真守卫；两处多余 ?.；TextSelectionTest.range(focus) 删未用参（12 调用同步）；SimpleDateFormat 静态缓存 → formatTime() 即时构造（语言切换不沿用旧 Locale）；删无引用 key_bg_rect.xml；ic_launcher 补 monochrome 层（麦克风剪影单 path，13+ 主题化图标复用 alpha）；rootProject.name 订正；删未用 import 5、重复 proguard 规则、6 失效 .gitignore 规则。lint 23→19。


保留不改（有意设计 / 规则约束）：ObsoleteSdkInt（改名 AAPT 报 resource not found 已回退）；Overdraw（半透明背板载体）；DrawAllocation（MicButton 语音模块）；GradleDependency（锁版本守 <5MB，当前 4.98MiB）；SetTextI18n×6（不动 strings.xml）等。

## 2026-09-23 发版 20260923

产物 5,225,466B / sha256 39e08ff2 / versionCode 20260923；提交 cf10f1b（23 文件）推双端。build_apk.py 改「时间戳包 + jinn-release.apk 同源双写」——发布靠手工复制，漏做会取错包；重构建覆盖重发（GitHub --clobber、Gitee 删旧传新），复核双端 71a5e12b / 5,221,370B 三方一致。GitHub CDN 首次拉取可能命中缓存，需加 query 复核。

键盘皮肤第一期：KeyboardSkin.kt（参数 + 清单 + byId/visualFor/issues 纯函数，自实现 HSV→RGB 便 JVM 单测）；PinyinKey 增皮肤绘制路径（渐变 shader 带缓存 / 描边 / 键帽厚度 / 字色提示色）；固定顺序 syncKeyboardSkin→applyKeyTransparency→applySkinToKeys→applyKeyAppearance；默认皮肤全空覆盖，与半透明历史观感逐像素一致；零位图零新增依赖。交互坑（写入工程约定）：PinyinKey.onTouchEvent 恒 true，预览块吃点击，须显式转回选项容器。

皮肤实测缺陷修复：磨砂/极光下功能按钮文字不可读（背景深而文字仍亮白主题色，暗黑主题下不暴露）→ KeyboardSkin 新增 functionGlyph（功能键/候选区主文字图标色）与 functionHint（次要：空格小字/页码），applySkinToTexts() 统一套用 8 处动态构建点。剪贴板/搜索面板 + 次级功能键覆盖：applySkin()（面板底 plate、条目卡 functionFill、文字两档）；rebuildFunctionKeyBackgrounds 次级键填充；顺带修「拖透明度后分类选中标记丢失」。

外观页版式精简：只留大标题、测试编辑框、三行滑杆（标签与拖动条同行）、皮肤行；字符串审计 150 string + 6 string-array 全有引用，删失引用 3 条 + 两套色板。

皮肤扩到二十四套：标签统一两字（复古机械→复古；守卫：新增皮肤三字以上即失败）；暗黑/霓虹/樱花/海洋/薄荷 → 石墨/日落/岩浆/墨玉/皮革 → 钛金/夜蓝/夜空/紫晶/雪原/青瓷/蜜桃/抹茶/珊瑚，纯色值参数零图片。选择器一行 8 个三行、皮肤名入按钮（fitTextSize 自适应）；排序按 faceBrightness()（BT.601 感知亮度）从亮到暗，默认固定首位不参与排序（键面色随主题漂移）；守卫钉住。定义域校验（填充不透明 / 渐变不退化 / 描边与宽度成对 / 功能键字色齐备）在接入即拦下一处错误（霓虹底边厚度带 alpha → 改不透明）。

皮肤审查修复 6 项：彩虹按压态闪主题深块（visualFor 判据 keyFill==null → isDefault）；皮肤+透明度时候选栏被压成背板档（plateRgb 移除 skin.candidateBar，档位交 updateCandidateBarBackground）；方向面板 ◉ 激活态字色未随 accent；中英切换键高亮写死 aurora_purple → skinToken；皮肤绘制每帧分配（PinyinKey 复用 faceRect、shader 判据改 5 数值字段）；plateFaceAlpha 声明晚于 init（赋值被属性初始化器重置 → 上移）。刷新链补 2 项：中英键高亮 span 不更新（buildLangLabel() 末尾重建 btnLang.text）；方向面板 9 键不更新（applySkinToDirectionPanel()，showDirectionPanel() 每次打开重刷）。刷新时机修正：applySkinToDirectionPanel 移到状态归零后（原插在 selectionActive=false 前，进入面板先按进入前拖选态刷中心键再被回正，多一次反向重绘）。守卫补 2 条：候选栏色不得与背板或功能键面撞色（遍历全皮肤）；未声明按压色时默认皮肤走令牌、皮肤按首色推导。边界核对：darken 内 factor.coerceIn(0,1) + 逐通道钳位；默认皮肤全空覆盖（含 accent）→ 中英键 span 回退 aurora_purple 历史观感不变。

主题与皮肤解耦：文案「键盘主题配色」→「界面明暗切换」、「键盘主题皮肤」→「键盘配色皮肤」（原均以「键盘主题」开头重叠）；涟漪随皮肤明暗（rippleOn(base)：亮底 10% 黑 / 暗底 30% 白）三个构建器统一走推导，语音面板不传参保持原样。

移除「Root 增强模式」：该开关仅做数据目录安全审计，与日常使用无关 → 删开关 + ClipboardFirewall.kt 整文件 + 键 + 5 条文案；加密 / 历史上限不变；旧 root_enhance 偏好键留本机不再读。

存储权限清理：移除 MANAGE_EXTERNAL_STORAGE（高危且与输入法职责不匹配，原仅「写日志到共享存储方便取出」，有零权限等价方案）：日志统一写应用专属目录 Android/data/<pkg>/files/logs/；导出改「缓存目录打包 → 系统 SAF 文件选择器（ACTION_CREATE_DOCUMENT）用户自选 → 写完删临时包」；提示显示文件名（部分 provider lastPathSegment 是文档 ID 无意义）。权限 4→3 条（INTERNET / ACCESS_NETWORK_STATE / RECORD_AUDIO），不申请任何存储权限。

导出中断修复：选择器打开期间进程被回收 → 回保存时临时包引用已丢、回调提前返回 → 目标留 0 字节空文件、无提示、缓存包残留，真机复现到的。修复：Diagnostics.latestBundle()（按最近生成时间在缓存目录兜底定位）；SAF 回调重建后仍继续写入；取消则删包恢复提示；包不存在明确提示「导出被中断，请重试」。杀进程后保存目标 113,789B（修复前 0B）、缓存 total 0；取消同样干净。tools/bug_capture.ps1 / bug_clear.ps1 写死旧目录 → 改多候选探测。

覆盖发版（8661f68，APK 5,229,506B / sha256 2a2b239e，双端覆盖 + 三方校验）。

## 2026-09-23 用户配置导出与导入

备份包格式：zip = manifest.json（格式标识 / 版本 / 来源 / 时间 / 设备 + 各节 sha256，置首条目免每次扫 manifest 全解压）+ prefs.json（两 SharedPreferences 白名单 26+2 键带类型标记）+ user_freq.txt + 可选 clipboard.jsonl（明文）+ 可选 dicts/*.xz。范围：Prefs 全 26 键 + 剪贴板 2 键 + 用户词频 + 剪贴板历史 + 可选词库；索引缓存（派生）、诊断日志（非配置）不迁移。PrefsBackupCoverageTest 源码对拍每个 KEY\_* 必进导出 + 导入白名单、键名不重、类型键与取值工具配对（KEY_PORT 写成 asString 会红）。剪贴板为何明文：密文本机 Keystore 保护、跨设备无法解密 → 只能明文迁移；导出默认不勾、导入显式勾选；单节导出预算 12MB / 读入 16MB。

加密：ConfigCrypto 用 PBKDF2-HMAC-SHA256 600,000 次派生 AES-256-GCM，每包独立 16B salt + 12B IV、GCM tag 128 位；头部（magic JINNENC1 / 版本 / 迭代数 / salt / IV）只含公开参数，迭代数入头部 → 将来调高强度仍能解旧包。迭代数补下限 10 万（iterations=1 曾可解）。密码仅 Unicode 汉字、4~64 字、不许同字重复；本机不存密码；输入框为明文输入框（NO_SUGGESTIONS + IME_FLAG_NO_PERSONALIZED_LEARNING）——密码型会在部分 ROM 唤起系统安全键盘打不出汉字。错误提示恒「密码错误或文件已损坏」；解密用 Cipher.update/doFinal 手写循环不用 CipherInputStream（后者可能吞 AEAD 异常放出短一截的包）；明文中间件用完即删。ConfigCryptoTest 15 例。

导入两种模式：覆盖还原（全量写回设置）/ 仅并入数据（只合并词频与剪贴板不动设置）。词频「同词取较大权重 + 较新天、不叠加」（两侧先按天衰减再合并免翻倍）；剪贴板按 content_hash 跳过本机已有、批内去重、按原时间落库；导入按阶段 runCatching 返 failedStage 部分完成如实报告（顺序写入不回滚、重试幂等）；导入前逐节 sha256 校验，不符整包拒绝；词库先提取到临时名校验通过才改名；聚合摘要仅勾选导入词库时校验，白名单内单文件内容不符按跳过（条目名含分隔符 / 条目数>16 / 总量>64MB / 读取异常仍整包失败）。

加固：返回键取消导入 → 明文 zip 残留（补 setOnCancelListener + onDestroy 清理）；导入词频被运行期写盘覆盖（UserFrequency.replaceFromBackup 写盘锁内落盘 + 换内存态、load(enabled=false) 清内存态与脏标记）；设置页三处后台线程无兜底（整体 runCatching）；剪贴板导出分页漏导（nextOffset 是否前进判结束）；readEntry 改三态 SectionRead 超限拒整包；词频天差用 Long（防 Int.MIN_VALUE 回绕永久霸榜）；写盘 renameTo 直接覆盖（免文件消失窗口）；dirty 加版本号仅版本未变才清；导入前 Prefs.flush()。攻击面：词库白名单（清单外不落盘）+ 内容校验（与 OptionalDicts.checksum 一致不符跳过）+ 总量闸 64MB（旧 16×64MB 先落盘再校验）；zipEntryNames 512 上限 + hasDictEntry 短路；favorite_symbols 导入归一 + 限长（主线程解析防大 JSON ANR）；manifest.device 截断压平；解锁读流 90s 预算 + 防 read 返回 0 空转；importInFlight 防 onDestroy 删正在读的明文 zip；三对话框存字段 + onDestroy dismiss 防 WindowLeaked；重启前双 flush；pendingNotice 承接重建期间后台结果；tempInFlight 集合 + 60s 最小年龄（防崩溃残留明文 zip 活过会话）。

交互反馈：模态进度框（派生 ~~1~~2s 不可取消、页面销毁兜底关）；结果弹框（成功 / 失败 / 解密失败带「重新输入密码」）；密码规则错误红字气泡 + Toast 双重；取消也有提示。

默认值调整：圆角 1dp→5dp、间隙 0.5dp→2dp、透明 0% 不变、皮肤→紫晶（KeyboardSkins.INITIAL_ID 与 DEFAULT_ID 分开，坏值回退不冲突）；主题跟随系统。

构建陷阱两条（写入工程约定）：assembleRelease 可能报 compileReleaseKotlin UP-TO-DATE 交付旧包；判据不要只看字节数（R8 吸收改动连续多轮 size 相同），读 APK 内 classes.dex 文本搜本次新增字符串常量（方法名会被混淆假阴性），确认 UP-TO-DATE 而源码已改时 gradlew clean 重建；注释里避免 /* 序列（Kotlin 块注释嵌套会吞后续代码编译失败）。

测试：ConfigBackupZipTest 8 例（zip 主路径 JVM 守卫：打包→读回 / Missing / TooLarge / Failed 三态 / 损坏 / 摘要绑定 / 条目上限 / 分类归一）；变异验证：临时改 restoreDicts 为 (0,0) → 6 条词库测试全红、删具体逻辑只对应 2 条红（细粒度定位）。门禁 45 类 / 472 例全绿、Kotlin 警告 0、lint 0 error / 23 warning；包 5,213,029B（4.97 MiB）；真机逐项：导出 / 导入 / 两种模式、清理窗、词库残留清理、密码框调起本输入法（非安全键盘）、dex 判据命中；ADB 无法输中文，「中文密码导出→导入」由真实算法单测覆盖。

## 2026-09-24 发布 20260924

提交 e581676（31 文件 +4987/−88）推双端 main；APK versionCode 20260924、5,213,029B、sha256 05CF1476、V1=false / V2=V3=true、4 字节对齐；全量 build_apk.py --clean + dex 判据复核（HHmmss-SSS / textLen= 命中）。双端新建 Release 20260924；正文 ≤10 行指路本文件；删双端 20260923 旧 APK 附件（词库包不受影响）。校验双端下载 HTTP 200、size 5,213,029、sha256 三方一致（以服务端 Content-Length + 重下 sha256 为准）。

## 2026-09-24 模糊音容错

11 组等价类：平翘舌 zh⇄z / ch⇄c / sh⇄s、鼻边音 n⇄l、f⇄h、r⇄l、r⇄y、k⇄g、前后鼻音 en⇄eng / in⇄ing / an⇄ang。规则全在 FuzzyPinyin.kt（纯函数、零数据文件，APK 体积零增长）。开关是位掩码 fuzzy_pinyin，**默认 0 = 关闭**：关着时查询路径不派生任何变体，结果与历史逐候选一致。接入点唯一 —— PinyinEngine.query() 收到的已是全拼串（双拼在键盘侧先 Shuangpin.toQuanpin 转换），7 套双拼自动同享；setFuzzyMask 实时生效，不需要重启输入法。

顺序契约：精确词 → 变体词 → 精确单字 → 变体单字。变体只补不抢；变体词必须排在精确单字之前，否则会被几十条单字挤到看不见的位置。三条上限：单音节变体 ≤3、每个前缀层级的变体键 ≤8（逐级查询各自计数，预算不跨级共享）、变体单字 ≤20；只做「替换其中一个音节」的派生，不做多音节同时模糊（后者随音节数指数增长）。变体必须仍是合法音节（过 validSyllables）：r/y、r/l 会派生 yen、ring 这类词库不存在的键，靠词库撞空不如在源头挡掉。

设置页新增「模糊音容错」入口（按钮带当前状态：关闭 / N 组），自定义勾选列表对话框 + 全开 / 全关，勾选即落盘即时生效。不用框架的 setMultiChoiceItems —— 实测在本项目对话框主题下一个条目都不显示（只剩标题、说明与按钮）。

测试：FuzzyPinyinTest 10 例（组位唯一且被 MASK_ALL 覆盖、双向派生、长声母优先、非法音节丢弃、变体与键数上限、掩码 0 为空）+ FuzzyQueryTest 4 例（关闭时零变体；精确/变体的顺序契约）。三处变异验证（去掉合法音节过滤 / 去掉掩码判据 / 调换顺序）分别只让对应用例变红。门禁 47 类 / 486 例全绿、Kotlin 警告 0、lint 0 error / 23 warning；包 5,213,029 B（4.97 MiB）。

真机（PACM00，ROOT 覆盖安装）：入口与对话框 11 组可勾选、「全关」落盘 fuzzy_pinyin=0；双拼输入 zsgo（全拼 zongguo）在开启 zh⇄z 时候选首位即「中国」，关闭后同一输入只剩「总/宗/纵…」——开关与实时生效双向验证。备份白名单同步（主配置 27 键），PrefsBackupCoverageTest 的计数与类型配对已随之更新（上一版日志里「三处变异验证分别只让对应用例变红」的计数不准，实数见下节）。

## 2026-09-24 模糊音容错（审查修订）

三视角独立审查（引擎逻辑 / 设置与持久化 / 测试与性能）后逐条回源码复核，修 2 处真缺陷：① **变体单字选中后消费区间退化为「消费全部」**——`consumption()` 只在精确音节表里找归属，变体字（输入 zang 时的「张」）找不到就落兜底、把整段残码清掉，而精确字能保留残码；改为精确音节找不到时再看启用组的变体音节（两遍扫描，精确优先与历史行为不变）。② 模糊音对话框未在 `onDestroy` 关闭（旋转 / 重建会 WindowLeaked），补字段与 dismiss，与既有三个配置对话框同型。

补 3 条守卫，均做变异验证：消费区间（去掉变体归属查找 → 该条红）、`MAX_FUZZY_CHARS` 裁剪（变体音节 25 字 → 只取 20）、`MAX_VARIANTS_PER_SYLLABLE` 能容纳理论最大值（`rang` = lang/yang/ran，上限调小会吃掉第三个变体）。`FuzzyQueryTest` 补 `UserFrequency.resetForTest()` 防跨类污染（掩码 / 词库 / 词频三个单例都要复位）。

变异验证实数（上一版记为「分别只让对应用例变红」，实际是逐条打中对应用例）：去掉合法音节过滤 → 1 条红；去掉逐组掩码判据 → 6 条红；调换变体词位置 → 1 条红。

文档订正：「整串变体键 ≤8」实为「**每个前缀层级** ≤8（`query()` 逐个 k 调用，预算不跨 k 共享）」；掩码 0 判据的说明改为「`variantsOf` 里逐组那一处是语义判据，`keyVariants` 与 `query()` 的 `mask == NONE` 只是省一次调用的短路」；新增「已知边界」三条（见下）。

真机复核（PACM00，ROOT 覆盖安装新包；`classes.dex` 810,968 → 811,396 B 证明构建确实重编）：全开分组后 ① 双拼 `ui`（全拼 shi，该音节精确单字 152 个）候选栏只见 shi 系字，变体 si 的字落在候选栏 24 条渲染上限之外；② `zsgu`（全拼 zonggu，末音节未完成）候选只有精确补全词，敲成 `zsguo` 才出现「中国 / 种过」。两条都是「不挤占精确候选」的取舍（分别记入已知边界②①），不是缺陷；设备已还原为默认关闭、临时文件已清理。

测试 47 类 / 489 例全绿、Kotlin 警告 0、lint 0 error / 23 warning；包 5,213,029 B（4.97 MiB）。

## 2026-09-24 模糊音容错（第二轮审查修订）

第二轮三视角审查（今日 diff 全量）后修 1 处真缺陷 + 3 处文案失准。① **模糊音命中的词只记「输入的键」，预测因此静默扫空**：预测要按该词在词库里的真实键扫延续词（输入 `zangguo` 选到「张国」，延续词在 `zhangguo` 键区下），而消费区间必须按输入键算 ⇒ 改为**额外记一份真实键**（`candidateTruePinyin`，只对模糊音命中的词有值；两个映射分工不同，不可合并）。② 前两节写的「整串变体键 ≤8」实为「每个前缀层级 ≤8」。③ `zsgu` 的双拼展开应为 zonggu 而非 zanggu。④ README / README_EN / 对话框说明的「补在原有候选之后」不准确（变体词排在精确单字之前）⇒ 改为「作为补充候选加上，精确候选一个不动、不被替换」。

补 3 条守卫，均做变异验证：预测按真实键扫（去掉真实键查找 → 该条红）、变体单字按所在音节定界（只看首音节的实现 → 该条红；用例特意在末音节缀一段未完成音节，使「消费到该音节」与「全清兜底」可区分）、变体归属优先于末尾残码的全清兜底（顺序反转 → 2 条红）。

判为非缺陷并逐条证伪（不改动）：`clampMask(-1) = MASK_ALL`（位语义，有测试钉住）、RESTORE 对包里缺失键保留本机现值（既有语义）、`load()` 取掩码与设置页写入之间的亚微秒竞态（不可复现）、`MAX_VARIANTS_PER_SYLLABLE` 的截断不可达、`keyVariants` 的 buffer 无别名、设置页对话框无重入守卫（模态窗口遮挡，路径不可达）、导出侧类型无守卫（既有测试设计，非本次引入）。

门禁 47 类 / 492 例全绿、Kotlin 警告 0、lint 0 error / 23 warning。包 5,213,029 B（与上一版字节数巧合相同：`classes.dex` 811,396 → 811,516 B —— 判据用 dex 字符串：新文案 `作为补充候选加上` 命中、旧文案 `补在原有候选之后` 已消失）。真机 ROOT 覆盖安装，对话框正常、`fuzzy_pinyin` 还原为默认 0、logcat 无 FATAL。

## 2026-09-24 皮肤双档（32 套）+ 明暗联动

皮肤清单 25 → 32 套：原「默认」（全空覆盖、跟随主题）拆成两套固定令牌皮肤**原黑 / 原白**，另补 6 套亮色（奶油 / 柠檬 / 天青 / 藕荷 / 雾灰 / 沙丘）与 1 套暗色（午夜），亮色 16 / 暗色 16。亮暗归属改由 `KeyboardSkin.tone` 显式声明 —— 彩虹首键亮度 0.543 低于 0.55 阈值，按键面亮度推导会把它判成暗色。

「界面明暗切换」重新设计为**亮色 / 暗色两档各带一套键盘皮肤**：`skin_light`（缺省雪原）/ `skin_dark`（缺省紫晶），生效皮肤 = `ThemeManager.keyboardSkin(prefs, isDark)`；槽位与档位强绑定（跨档脏值回退本档令牌基线），页面色板与键盘皮肤同源，不再出现「浅色页面 + 深色键盘」。旧键 `keyboard_skin` 退役并一次性迁移：旧皮肤按**自己的档位**入槽、另一槽取该档令牌基线（旧「默认」双槽都落令牌基线 ⇒ 升级观感不变）；迁移在 getter 里触发，覆盖「备份导入 / 回滚旧版再升级」这类晚于进程启动的写入。

外观页改**单列表双选中态**：32 项（每行 8 个，恰四整行），原黑 / 原白固定头两位、不参与亮度排序；点某套按其 `tone` 写入对应槽；黄框 = 当前档正在生效、蓝框 = 另一档已选、未选降到 55%；令牌皮肤预览固定吃自己那一档色板（`ThemeManager.wrapContext`），否则在亮色页面上「原黑」会画成白键面。

主配置 27 → 29 键（新增 `skin_light` / `skin_dark`；`keyboard_skin` 计入退役键，仍保留常量供守卫清点、仍只在迁移里读），备份覆盖面与类型配对守卫同步。

测试 47 类 / **499 例**全绿（新增令牌色板 / 分组规模 / 分组与亮度同向 / 跨档回退 / 迁移规则 / 档位映射 5 例 + 2 条源码对拍守卫；5 处变异验证逐条打中对应用例）。Kotlin 警告 0、lint 0 error / 23 warning（基线持平）。包 5,217,125 B（4.98 MiB，余量 25.8KB），dex 内 `午夜` / `原黑` / `skin_light` 等新字符串命中。真机 PACM00（ROOT 覆盖安装）：32 项列表与原黑原白置顶、双选中框、迁移落盘 `skin_light=legacy_light` + `skin_dark=amethyst`、切「暗色」后生效框由原白移到紫晶、原白预览在暗色页上仍是白键面、键盘（原白）观感与历史一致；验证后主题模式已还原为「亮色」。`values/strings.xml` 仅在本次授权范围内改动（亮白 → 亮色、暗黑 → 暗色，卡片说明改为两档皮肤）。

## 2026-09-24 皮肤列表固定顺序 + 外观页分组说明

皮肤展示顺序改回**逐项固定**（用户指定，不再按键面亮度重排）：亮色 16 = 原白 柠檬 奶油 雪原 樱花 蜜桃 青瓷 雾灰 · 天青 抹茶 沙丘 薄荷 藕荷 珊瑚 复古 彩虹；暗色 16 = 原黑 钛金 日落 皮革 磨砂 石墨 紫晶 海洋 · 岩浆 夜蓝 墨玉 极光 夜空 暗黑 午夜 霓虹。`KeyboardSkins.ALL` 的索引顺序即展示顺序（删掉 `orderedForDisplay()`，改 `ofTone(tone)` 分段取）；新增皮肤要显式插进指定位置，期望顺序由 `KeyboardSkinTest.展示顺序两段固定` 逐项对拍 label。

外观页拆成两段、各带一行标题，删掉原「皮肤」标题与「点亮色/暗色皮肤分别设定；黄框＝当前生效，蓝框＝另一档已选」图例：标题括号随当前档位实时变化 —— 生效那段写「现为亮色/现为暗色,唤起键盘可直接预览」，另一段写「未激活,不可预览」（用户设计的提示，说明哪一段点了能立刻看到）；框色保留（黄 = 生效段选中的那套，蓝 = 另一段已选的那套）。标题是长句，`maxLines=1` + `autoSizeTextType="uniform"`（9~13sp）自动缩字号，不换行。

设置页明暗卡片的说明改为「亮色:%1$s 暗色:%2$s」（原「亮色皮肤 %1$s ｜ 暗色皮肤 %2$s」）。默认两档仍是雪原 / 紫晶，未改。

门禁 47 类 / 499 例全绿、Kotlin 警告 0、lint 0 error / 23 warning。包 5,217,125 B（4.98 MiB，余量 25.8KB）—— 本日第三次与前版字节数完全相同，判据只能看内容：dex 字符串 `界面明暗切换:现为` / `未激活,不可预览` 命中、旧文案 `黄框` / `点亮色 / 暗色皮肤分别设定` 消失，资源串 `亮色:%1$s 暗色:%2$s` 用 `aapt2 dump strings` 核对。真机 ROOT 覆盖安装：设置页说明显示「亮色:青瓷 暗色:极光」（用户自己的两档选择），外观页两段顺序与标题、黄 / 蓝框均与设计一致（亮色段首原白、暗色段首原黑，各 8×2）。

## 2026-09-24 皮肤双档（独立复核修订）

独立视角复核后修 2 处真缺陷：① **延后重建窗口里皮肤与色板会错档** —— 换主题的重建被「未上屏输入 / 面板打开」延后时（`JinnIme.applyThemeIfNeeded` 只退出自己），`onStartInputView` 仍会走 `configure()` → `syncKeyboardSkin()`，按此刻现算的决策取皮肤，而键面令牌还是视图创建时的旧档 ⇒ 暗档的原黑被画成白键面（日志却写着「原黑」）。改为按**视图自己的色板快照**取档（新增 `ThemeManager.paletteIsDark`，`isSystemDark` 改为它的别名），皮肤与色板永远同档，换肤只在重建后发生。② **导入旧版备份会丢皮肤** —— 旧键被退役后导入侧不再认它，「新机器导入旧版备份」会静默落回出厂皮肤；补 `KEY_KEYBOARD_SKIN` 兼容分支（仅当包内没有新键时生效，走同一套迁移语义）。另补 2 条源码对拍守卫：选皮肤必须按色板快照（变异改回现算决策 → 红）、退役键只在导入侧出现（加回导出或删掉兼容分支 → 红）。同时修掉一处脆弱：外观页末行补位改用展示清单的长度算余数（原先用 `ALL.size`，两者不同量时末行会多塞权重格而宽度不齐）。

## 2026-09-24 变量组（17 项，按清单重排）

「变量」符号组按清单重排，键面文本与顺序都照清单：第 1 排 `星期 农历 季度 财年 生肖 天数 周数 分辨率 时间戳 毫秒戳`（10 键），第 2 排 `年日中 年日数 年日符 时秒中 时秒数 时秒符 长时中 长时数 长时符`（9 键，正好排满），共 19 键；不做 IPV4、IPV6。键面短名与上屏内容依旧分离：键面显示短名，**点击那一刻**才展开；短名 ≤4 字且组内唯一，顺序由 `SymbolLayoutTest` 的期望表钉住。旧短名（定长码 / 连字符 / 日期 / 日期码 / 日期连字 / 年月 / 时分秒，以及同日上一版的年月日码 / 时分秒码 / 长时间 / 长时间码 / 长时间线）全部保留为别名 —— 用户收藏里存的是旧取值，删掉分支会把「定长码」这种字面量打进输入框；同名冲突以已发版语义为准。

命名后缀即格式：**中 = 中文式、数 = 纯数字、符 = 带分隔符**。日期 `2026年9月24日` / `20260924` / `2026-09-24`；时刻 `13点50分28秒` / `135028` / `13:50:28`；长时间 = 日期与时刻拼接，另有 `20260924135028` 与 `2026-09-24-13:50:28`；季度 `2026年第3季度 (Q3)`；财年 `FY2026-Q2`（**从 4 月起算**，换口径只改一个常量）；天数 `已过267天/剩98天`；周数 `已过39周/剩14周`（按日历年：1 月 1 日起每 7 天一段、末段不足 7 天也算一段）；分辨率 `1080×2280`。

农历是唯一带数据的新增项：`LunarDate` + 2000–2050 的 51 个 Int 表（**204 字节**）。表由 `tools/dict_builder/gen_lunar_table.py` 从万象拼音的 `LUNAR_DATA` 裁出并重编码（26 位/年），脚本内自校验春节 / 中秋 / 闰月锚点；运行期锚点由 `LunarDateTest` 钉住（春节 7 例、闰月 14 例、中秋 3 例，外加表内逐日连续性遍历）。超出范围（如 `2000-01-01`，早于 2000 年春节）返回 null，`expand` 回退为公历日期、生肖回退为公历年粗算。

分辨率不用 `Resources.getSystem()`：真机按应用区域报（PACM00 实测 1080×2200，少 80px 系统栏），改由 `JinnIme.onCreate` 注入真实物理尺寸（`getRealMetrics`，捕获 applicationContext）。单测把该取值器替换为固定串，好让「变量组全部可展开」的护栏也覆盖它。

体积：**dex 的增量约 5 KB（原值）**，但发布包体积未变（5,221,277 B）—— APK 签名块的对齐填充正好吃掉了这点增量，属巧合而非余量；评估功能涨多少要看 dex。门禁 49 类 / 522 例全绿、Kotlin 警告 0、lint 0 error / 23 warning。真机（PACM00，ROOT 覆盖安装）按位置逐位验证 19 项：`星期四 / 农历八月十四 / 2026年第3季度 (Q3) / FY2026-Q2 / 马年 / 已过267天/剩98天 / 已过39周/剩14周 / 1080×2280 / 时间戳 / 毫秒戳 / 2026年9月24日 / 20260924 / 2026-09-24 / 16点06分37秒 / 160702 / 16:07:27 / … / 2026-09-24-16:08:43` 全部符合清单。

## 2026-09-24 审查缺陷修复（5 项）

深度审查（静态路径追踪 + 对照实验 + 独立复算 + 真机像素实测）确认 5 个真实缺陷，全部修复：

**① 模糊音历史污染预测**：`candidateTruePinyin`（词库真实键）只在模糊分支写、精确路径不清，于是「开着模糊音命中过某词 → 关掉模糊音 → 用另一个精确键打到同一个词」时，`predict` 仍按上一次的变体键扫延续词。修法：`noteCandidateKeys` 内同步 `candidateTruePinyin.remove(w)`（模糊分支在其后写入，顺序天然正确）。复现/回归：造一份「朝阳」同时挂在 `chaoyang` / `zhaoyang` 下、两键各配不同延续词的词表 —— 修复前得到「区」（旧键 chaoyang*），修复后是「门」（当前键 zhaoyang*），对照组（从未用过模糊音）两者一致。

**② 符号层统一字号基准被低估导致裁切**：宽度估算原为「长度 + 全角标点额外记 1」，把 `\`（2 个 ASCII≈1em）与 `……`（2 个全角≈2em）判成同宽，基准落到窄的一条 ⇒ 全角/标点页的 `……`、`——` 被键面裁掉两端（真机实测：这两键墨迹 115px / 键面 117px、左右余量各 1px，同页其余键余量 33~45px）。改为 **ASCII≈0.5em、其余≈1em** 的估算，并给 `SymbolLayoutTest` 加逐页属性断言（基准不得比本页任何标签窄）。真机复验：走遍符号分组，触边键 **0** 个。代价：全角/标点页整页字号由最宽标签决定，比只含单字符的页小一档（这是「页内同号且不裁切」的必然结果）。

**③ 周数按 ISO 周年显示**：元旦会显示成「已过53周/剩0周」（2027-01-01 属 2026 年第 53 周），与「今年已过周数」语义矛盾。改为**日历年**口径（1 月 1 日起每 7 天一段、末段不足 7 天也算一段）：2027-01-01 = `已过1周/剩52周`、2026-12-31 = `已过53周/剩0周`；2026-09-24 仍是 `已过39周/剩14周`。

**④ 键盘外观页缺定时刷新钩子**：色板来自 `attachBaseContext`、段标题在 `initSkinSelector` 写死，停在页面上跨过定时切换点时标题与黄框停在旧档（键盘已换肤）。补上与设置页同机制的 `themeTickRunnable` + `scheduleThemeTick()` + `onDestroy` 撤销延时任务。

**⑤ 备份守卫测试注释计数**（写「26 个普通键」、实际 27 项）改为自描述注释，避免按错的数量维护。

**验证**：变异验证 —— 把 ① 的清理行、② 的宽度估算分别改回旧实现，对应用例各自变红（守卫有效）；无变异残留。门禁 **49 类 / 523 例全绿**、Kotlin 警告 0、lint 0 error / 23 warning。包 **5,225,373 B**（dex +908 B；签名块对齐填充此次耗尽，总体积较上一版 +4,096 B），仍在 5 MiB 内（余量 17.5 KB）。

## 2026-09-24 发版（皮肤分档与变量组）

提交 c754ddc（39 文件 +2331/−446）推双端 main；APK versionCode 20260924、5,225,373 B、sha256 C12F10E82E6380D16D547266CCA706B23882CD5337B57D2AD896396D7572D7AD、V1=false / V2=V3=true、4 字节对齐通过；全量 build_apk.py --clean + dex 判据复核（长时符 / 农历 / 皮肤 均命中）。**lint 基线收口**：新增 `app/lint.xml` 按文件精确写下存量 23 条例外（每条写明理由，新文件同类问题仍会报），lint 由 0 error / 23 warning 变为 **0 error / 0 warning**；Kotlin 警告 0；门禁 49 类 / 523 例全绿；真机（PACM00，ROOT 覆盖安装）冒烟通过、无 FATAL。同日覆盖 Release 20260924：双端删旧 APK 附件 → 上传新包 → 正文改为 ≤10 行；校验双端下载 HTTP 200、size 5,225,373、**sha256 三方一致**（本地 / GitHub / Gitee）。

## 2026-09-24 导入链路加固

复核确认两处真实缺陷，都在配置导入的按名查找上：

**① 跳过的 zip 条目没有解压量预算**。`ZipInputStream.nextEntry` 会先把当前条目读完（`closeEntry` 解压丢弃）才走到下一条，于是按名查找的代价按**解压后**体积计，而包体上限管的是压缩后的 256MB。实测：前一条目解压后 256MB 时单遍扫描 847ms，对照组（1MB）8ms；而一次导入要顺序扫约 11 遍（检查 5 遍 + 导入 6 遍），几百 KB 的对抗包就能让导入卡到只能杀进程。修法：新增 `ScanBudget`，跳读逐字节记账、超限立即停止遍历；检查与导入各建一份、内部逐节共用（每个节都要从包首重扫，分开记账等于把上限乘以节数），上限 `MAX_SCAN_INFLATED_BYTES = 512MB`（合法包一次约需 300MB）。预算耗尽一律整包拒收：`readSection` 回 `Failed`、`zipEntryNames` 停在预算处、`hasDictEntry` 回 false，由调用方按 `ScanBudget.exhausted` 在写入之前拒绝。

**② 导入缺进程级互斥**。导出早有 `exportInFlight`，导入只有设置页的实例字段，页面重建后归零 —— 旋转屏幕再点一次导入就会两路并发写 prefs / 词频 / 剪贴板（各自有锁不会损坏，但报数失真，来自不同包时各阶段还会各留一份）。修法：`beginImport()` / `endImport()` 进程级闸门 + 对外只读 `importing`，设置页发起前先查，失败语义不变（返回 null）。

**验证**：`ConfigBackupZipTest` 新增 4 例（超预算判 `Failed` 且同一包在默认预算下仍可读、条目名收集与词库判定超限留痕、词库恢复超限不留临时件、互斥门不可重入且释放后可再进）；门禁 50 类 / 529 例 0 失败、lint 无问题；`build_apk.py --install` 出包 5,225,373 B（dex 命中「包内解压量超过预算」与「已有导入在进行中」，体积与上一版相同属签名填充的巧合），真机覆盖安装后设置页 453ms 启动、无 FATAL。

## 2026-09-24 粘贴上限与收藏上限

**粘贴路径补长度闸门**（`JinnIme.pasteClipboardText`）：采集侧的单条上限（256KB）只拦新写入，库里上限生效前留下的旧行会被整条 `commitText` 发给宿主 —— 量级大时提交失败或让宿主卡顿。改为按同一上限拒收（`ClipboardStore.exceedsItemLimit`），记 W 日志并提示「内容过大，未粘贴」。这个出口是两处剪贴板面板与暂存粘贴的唯一入口，一处拦住即可。选择拒收而不是截断：截断会静默改掉用户要粘的内容。顺带把剪贴板面板的失败提示从「请确认输入框可用」改为只报结果 —— 原措辞把失败原因一律归到输入框，会把用户引偏。

**收藏导入上限放宽**（`Prefs.MAX_FAVORITE_SYMBOLS_CHARS`）：由 4096 字符调到 32K。按单项最长 8 字符、序列化后每项约 12 字符算，原上限约三百项即触顶，收藏更多的用户自己的备份会因该键被整条拒收而导不回来（界面只显示「忽略 N 项」）。上限本身要保留：解析发生在键盘视图构造的主线程上，超长 JSON 会让每次重建键盘都做百万级解析；32K 下仍是毫秒级。新增单测钉住「20 页 × 26 项、每项顶到 8 字符」仍在上限内，上限再被调小会直接失败。

**验证**：门禁 50 类 / 530 例 0 失败、lint 无问题；`build_apk.py --install` 出包 5,225,373 B（dex 命中「内容过大，未粘贴」），真机覆盖安装后设置页 708ms 启动、无 FATAL。

## 2026-09-24 导入链路收尾

**多节一次遍历**（`ConfigBackupManager.readSections`）：按名查找必须从包首顺序扫（`ZipInputStream` 不能跳转），而检查与导入读的正是同一批节 —— 此前一次导入合计约 11 遍（检查 5 + 导入 6），同一个 16MB 的节被反复解压。检查改为一遍读完 manifest / prefs / 词频 / 剪贴板；导入改为「manifest 单独先读（版本不兼容的包要在读完前拒掉）+ 三个节一遍」。两处合计降到 4~6 遍。

严格性与分开调用一致：读不出来的节记 `TooLarge`；遍历中途出错或预算耗尽时，**没走到的节一律记 `Failed`**，调用方据此整包拒收，而不是把「没走到」当成「包里没有」。超限节读满上限后的剩余部分仍走解压预算，不会白送。

**词库临时件注销**（`ConfigBackupManager.restoreDicts`）：`*.restore` 登记进 `tempInFlight` 后从不注销（注销只清导出/解密两个前缀），集合每导入一次最多涨 16 项，清扫器还会一直跳过同名残留。改成包装 + 实现体两层，出口的 `finally` 按后缀统一注销；后缀提成常量，登记、清理、注销三处共用。

**验证**：新增 2 例单测（多节遍历的缺失 / 超限 / 预算耗尽三种语义）；门禁 50 类 / 532 例 0 失败、lint 无问题；出包 5,225,373 B（dex 命中「读取节失败」），真机覆盖安装后设置页 730ms 启动、无 FATAL。

## 2026-09-24 低风险项收口

**API 26 导航栏对比度**：亮色主题给浅底导航栏配 `windowLightNavigationBar`，而那是 API 27+ 属性，`minSdk 26` 上标志被忽略 ⇒ 浅底配浅色图标。修法不必复制整套主题：导航栏底色改用 `@color/nav_bar`，`values/colors.xml` 给深底作 26 的回退，`values-v27/colors.xml` 覆盖成 `@color/app_bg`，27+ 仍与页面同色（观感不变）；夜间档不引用它、保持 `@color/app_bg`，两套色板都写同名令牌维持逐名对齐守卫。

**词频落盘时间戳**（`UserFrequency.saveNow`）：防抖尾沿补写不刷新 `lastSaveAt`，紧接着的下一次学习会立刻再同步写一次。改为在 `saveNow` 里刷新（立即写分支本来就在调用前设，语义一致），字段补 `@Volatile` —— 调度线程写、主线程读。

**诊断快照与导出包互斥**（`Diagnostics`）：快照是就地写的，与导出包共用固定文件名且无锁，并发时会互删产物或把半截快照打进包里。两者改走同一把 `snapshotLock`（包装 + 实现体，`synchronized` 可重入，导出内部再抓快照不会自锁）。代价是崩溃路径要多等一次进行中的导出，秒级、且两者都由用户主动触发。

**同日复核结论**：旧滑杆值被新步进吸附一条不成立 —— `Prefs.keyCornerDp` / `keyGapDp` 读写两端都过钳位，显示、绘制、导出用的是同一份归一值。「诊断正文防护」属调用约定，没有机械校验手段，保留在缺陷台账里备查。

**验证**：门禁 50 类 / 532 例 0 失败、lint 无问题；出包 5,225,373 B（`resources.arsc` 命中 `nav_bar`），真机覆盖安装后设置页 719ms 启动、无 FATAL。API 26 档位没有真机，未做实机观感验证。

## 2026-09-24 备份包同名节的处理

**同名条目改回首条定案**（`ConfigBackupManager.readSections`）：一次遍历多节的首版实现用 `found[name] = …` 覆盖，同名重复时取**末条**，而逐节读取（`readSection` 命中即返回）取**首条** —— 同一个包在两条路径下结论不同：「首条有效、末条与 manifest 摘要不符」的包会被判校验失败、整包拒收，用户看到导入失败，而按逐节读取它会正常导入。改成首条定案，重复的那条按跳读记账（不再解压进内存）。

触发面限定在非本程序生成的包：导出侧 `putBytes` 用四个不同的节常量、`putFile` 用词库目录内的唯一文件名，且 `ZipOutputStream` 直接拒绝重名条目；第三方工具可以 —— python `zipfile` 只警告 `Duplicate name` 仍会写入，程序所用的 `ZipInputStream` 按局部头顺序能看到全部条目（已实测，含 `{"first":1}` / `{"second":2}` 两条同名节）。内容仍由 manifest 摘要把关，无安全影响。

**验证**：新增用例「同名条目重复时取第一条_与逐节读取一致」。造包方式要绕两道坎：`ZipOutputStream` 会拒绝重名、把两段 zip 拼接起来读侧只认第一段，所以用例自己拼 stored 局部头（`ZipInputStream` 不需要中央目录）。变异验证 —— 把首条改回末条覆盖，用例变红（`expected:<{"first":1}> but was:<{"second":2}>`）。门禁 50 类 / 533 例 0 失败、lint 无问题。

## 2026-09-24 导入后状态同步与稳定性修复

**词库预加载失败后不补试**（`JinnIme`）：加载失败只在日志里留痕，整个会话就打不出拼音。补 `retryPinyinLoadIfNeeded`，上限 3 次（含启动那次）、走独立后台线程，已就绪时开销是一次 volatile 读；触发点收敛在键盘弹出入口。

**模糊音掩码导入后不同步**（`ConfigBackupManager`）：导入改了偏好里的掩码，而引擎里另有一份运行期副本，两者不一致时设置页显示与候选行为对不上，直到 IME 进程重建。导入落地后补一次同步，`RuntimeStateSyncTest` 守住它与导入的先后顺序。

**检查更新的看门狗没随页面撤销**（`SettingsActivity`）：页面销毁后延时任务仍持有视图。`onDestroy` 补 `removeCallbacks`，与同页主题定时任务的处理对齐。

**剪贴板入库失败被日志伪装成成功**（`ClipboardController`）：日志打在 `upsert` 之前、返回值又被丢掉，加密或写库失败时静默丢条。日志移到 `upsert` 之后，按返回 id 分开记 i / w。

**验证**：门禁 49 类 / 523 例全绿；出包装机后设置页启动正常、无 FATAL。

## 2026-09-25 候选词两行模式（可选）

**双行档**（新键 `candidate_rows`，设置页「候选词行数」= 1 行 / 2 行，默认 1 行）：下排显示第 1/3/5… 个候选、上排显示第 2/4/6… 个，上下对齐成列；两排共用外层那一个 `HorizontalScrollView`，横滑天然同步，不存在某一排单独滚动导致错位。候选栏 48dp → 72dp（每排 36dp、字号 21 → 20sp）。

**排版规则**收敛在 `CandidateRows` 纯函数（渲染与单测同一来源）。缺项列用零宽等高占位 —— 裸 `View` 配 `WRAP_CONTENT` 时会在 AT_MOST 下吃掉整列剩余宽度，把奇数条时候选顶到栏中间。行高按系统字体缩放取「36dp 与文字行高」的较大者，字体放大时候选栏整体长高（1.6 倍档实测 946px，与算式逐项吻合），候选文字不裁。单行档路径与历史逐项一致（层级、字号、顺序均未变）。

**复用块高度**：功能面板 6 按钮、符号分组标签、词库内联提示维持单行，高度取「内容与 48dp 的较大者」——用 `MATCH_PARENT` 会被 72dp 拉成瘦高长条，固定 48dp 又会在系统字体放大时裁掉第二行小字（实测 1.5 倍即已裁）。

**验证**：门禁 51 类 / 543 例 0 失败、lint 无问题；真机覆盖安装后核对：上排「农哝脓耨」/ 下排「弄浓依秾」上下成列、拼音串跨两行垂直居中、空格上屏首候选与下排首列一致、切回 1 行档候选栏即回 48dp（无需重启输入法）；字体 1.6 倍档下按钮两行文字完整显示。

## 2026-09-25 备份包按名查找补条目数闸

**问题**：`readSections` / `hasDictEntry` 一直遍历到「找齐节」或包尾，而 `ScanBudget` 只按**解压后字节**记账 —— 零字节条目一次 `read` 即 EOF、不消耗预算。zip 本地头只有几十字节，256MB 的包能塞数百万条，于是按名查找可被拖成分钟级空转，而导入跑在不可取消的进度框后面。

**改动**：两处各加一道**遍历条目数**闸（`MAX_SCAN_ENTRIES = 4096`）。它与 `zipEntryNames` 的 512 **收集**上限不是一回事：收集上限防内存（只留前 N 个名字），遍历上限防空转，取值必须容得下「词库条目排在收集上限之后」的形态，否则会把「有词库条目」漏判成「没有词库」（既有用例 `条目名收集有上限且词库条目判定不受上限影响` 钉住该语义）。超限走与预算耗尽同一出口（`ScanBudget.overflow()` 置 `exhausted`），检查与导入都按整包不可用拒收。

**测试**：新增「条目数超限时停止遍历并判包不可用」（摘掉闸即变红，已做变异验证）；补强两条原本恒真的断言 —— 「词库恢复在预算耗尽时中止且不留临时件」把在册词库排到巨型条目之前，让 `*.restore` 真的落盘后再验证清理；「多节一次遍历_超限节记TooLarge」改用 4MB 的节，使「超限节的剩余部分读空并记账」真正执行（预算被耗尽 ⇒ 后面的节记 Failed）。

**验证**：门禁 51 类 / 544 例 0 失败、lint 无问题；签名包 5,225,373 B（dex 内含本次新增日志串）真机覆盖安装后键盘与候选栏回归正常。导入端到端仍受「密码只用汉字、ADB 打不出中文」限制，由 JVM 层守卫覆盖。

## 2026-09-25 备份包遍历补齐条目数闸（其余两处）

**问题**：条目数闸首版只落到 `readSections` / `hasDictEntry`，同类遍历还有两处没闸 —— `readSection`（导入第一步读 manifest 就走它）与 `restoreDictsLocked`（词库恢复段）。零字节条目一次 `read` 即 EOF、不吃 `ScanBudget`，这两处仍可被数百万条目的包拖成分钟级空转，而导入跑在不可取消的进度框后面。

**改动**：两处各加同款遍历条目数闸（`MAX_SCAN_ENTRIES`，默认 4096，可注入以便测试），超限走 `budget.overflow()` 置 `exhausted`、调用方整包拒收；失败位分别是 `SectionRead.Failed` 与 `return@zipStream false`。`readSection` 的命中判定仍在计数之前；`restoreDicts` 的 `maxEntries` 放在 `checksumOf` **之前** —— 后者必须留在参数表末尾，因为既有调用点把它写成尾随 lambda，追加其后的参数会让尾随 lambda 绑错目标。

**测试**：新增「单节读取在条目数超限时停止遍历」与「词库恢复在条目数超限时中止」，两条都做了变异验证（把闸条件改成永不成立，对应用例变红，恢复后绿）。

**验证**：门禁 51 类 / 546 例 0 失败、lint 无问题；签名包 5,225,373 B（dex 内含 `词库恢复: 条目数超过`）真机覆盖安装后键盘与候选栏回归正常。

## 2026-09-25 条目时间显示固定 Locale

**改动**：剪贴板历史面板与搜索面板的条目时间（`ClipboardPanelView.formatTime` / `SearchPanelView.formatTime`）由 `Locale.getDefault()` 改为 `Locale.US`，与设置页里备份包时间的写法一致。默认历法地区（如泰国）下 `SimpleDateFormat` 会走佛历，年份从 2025 变成 2568；固定 US 后是公历与阿拉伯数字。pattern（`yyyy-MM-dd HH:mm`）与「即时构造、不做静态缓存」的做法不变 —— 后者是为了让语言切换即时生效（lint `ConstantLocale`）。

**验证**：门禁 51 类 / 546 例 0 失败、lint 无问题（`Locale.US` 不触发 `ConstantLocale`）；签名包 5,225,373 B 真机覆盖安装后剪贴板面板可正常打开（该机剪贴板历史为空，时间列未能在真机端到端看到，面板渲染与空状态文案正常）。

## 2026-09-25 候选栏高度常量命名整理

**改动**：`CandidateRows` 的三个高度常量改名，让「整栏」与「每排」一眼可分 —— `SINGLE_ROW_HEIGHT_DP` → `SINGLE_BAR_HEIGHT_DP`（整栏 48dp）、`ROW_HEIGHT_DP` → `DOUBLE_ROW_EACH_DP`（每排 36dp）、`DOUBLE_ROW_HEIGHT_DP` → `DOUBLE_BAR_HEIGHT_DP`（整栏 72dp）。同时把 `PinyinKeyboardView` 里复用块（功能按钮 / 符号分组标签）的最小高度抽成 `REUSE_BLOCK_MIN_DP`，两处构建点共用同一常量，不再各写一次。纯结构整理、无行为变化：旧名零残留（编译器兜底），取值与原先相同。

**验证**：门禁 51 类 / 546 例 0 失败、lint 无问题；签名包 5,225,373 B 真机覆盖安装后功能面板 6 个按钮文字完整、48dp 高并在候选栏内居中（与整理前一致）。

## 2026-09-25 面板时间工具收敛与预算参数整理

**改动**：① 剪贴板面板与搜索面板的条目时间收敛到新工具 `PanelTimes.entryStamp` —— 原先两处逐字重复（各带一份实现与注释），改格式要改两遍；② 单行档候选栏高度改用与 XML 同口径的换算（`TypedValue.applyDimension`，资源值走四舍五入而 `dp()` 是截断，非整数密度设备上原先会差 1px 并多一次 `requestLayout`）；③ 条目数闸收进 `ScanBudget.stepEntry()`，四处调用点（`readSections` / `readSection` / `hasDictEntry` / `restoreDictsLocked`）不再各写一遍计数与 `overflow()`，上限改由预算构造参数指定（`ScanBudget(maxEntries = …)`），四个函数签名里随之删掉该参数。

**验证**：门禁 51 类 / 546 例 0 失败、lint 无问题；变异验证 —— 把 `stepEntry` 的上限判断改成永不成立，三条守卫用例（覆盖四处闸）同时变红，恢复后绿；签名包 5,225,373 B 真机覆盖安装后两行候选、拼音串居中、✕ 居中，外观与整理前一致。

## 2026-09-25 导入侧补齐「声明了却缺节」的判定

**改动**：新增 `ConfigBackupManager.firstMissingDeclaredSection` —— manifest 登记了某节摘要、包里却没有该节（或该节读不出来）时返回节名；导入流程在**写入任何数据之前**调用它，命中即整包拒绝。此前只挡了反方向（节存在但 manifest 未登记摘要），缺这一半时「删掉整节、manifest 留着摘要」的包会被当成「没有这一节」静默跳过：用户看到「导入完成」，整节数据其实没进来。

**范围**：覆盖 prefs / user_freq / clipboard 三节；词库节本就对称 —— manifest 有摘要而包内无条目时聚合摘要必然不符，早已拒绝。

**验证**：门禁 51 类 / 547 例 0 失败、lint 无问题；新增用例「manifest 声明了摘要却没有对应节时判包不完整」（缺失 / 齐全 / 读不出来三种形态），变异验证 —— 把默认的节对列表置空即变红、恢复后绿；签名包 5,225,373 B 真机覆盖安装后键盘与候选栏正常。

## 2026-09-25 双行档无障碍顺序与语义对齐

**改动**：双行档每列原先用垂直 `LinearLayout`（只能先加上排、后下排），而无障碍遍历默认按视图树顺序 ⇒ TalkBack 朗读 / 焦点顺序成 2、1、4、3…，与「第 1 个候选在下排首列（空格取它）」相反。列改用 `FrameLayout` + 子项 `gravity`（上排 `TOP`、下排 `BOTTOM`）定视觉位置，添加顺序改为「先下排（第 1/3/5…）后上排」⇒ 遍历顺序 1、2、3、4…。视觉位置不再由添加顺序决定，外观与触摸坐标不变；缺项零宽等高占位的规则未动。

**验证**：真机覆盖安装后 —— 候选满 24 条时上排「气奇七骑弃企旗岂棋泣漆契」/ 下排「起其期器齐妻启欺汽琪凄戚」上下成列（与改前一致），点击下排首条「嗯」上屏（= 第 1 个候选，与空格同行为）。顺序部分依据平台语义（默认遍历即视图树先序）与代码级核对；未做 TalkBack 端到端 —— IME 窗口不在 `uiautomator dump` 范围（实测 dump 内无候选文本），也不为验证改动设备无障碍设置。

## 2026-09-25 候选栏底色覆盖到键盘顶

**现象**：候选栏（工具栏）底色上方露出约 4dp 的键盘背板 —— 来源是根布局 `keyboard_pinyin.xml` 的 `android:paddingTop="4dp"`（项目早期 `968dcf9a`，2026-08-15 引入）。功能面板 / 符号层态下候选栏与背板同档（plate）看不出；一旦有候选切到更实的 surface 档，那条 12px（density 3）色差就显形。

**改动**：删掉根布局的 `paddingTop`，候选栏底色自键盘最顶铺满；键盘总高相应少 4dp（双行档 894 → 882px @ density 3），候选栏内容随之上移 4dp。字母区自身的 `paddingTop="4dp"`（第一行键上边距）、底部功能行、剪贴板 / 搜索面板高度均未动。

**验证**：真机逐像素扫描（x=120 / 540 / 900 三列）—— 双行档键盘顶 1398、单行档 1470 起直接是候选栏色 `250,252,255`，此前的背板条 `220,231,245`（12px）消失，与算式逐项吻合。

**同日续**：字母区自身的 `paddingTop="4dp"` 也一并收掉 —— `keyboard_letters` 不再留上边距，第一行字母键的顶边直接接候选栏底边。字母区高度 166 → 162dp，键盘总高再 -4dp（字母模式 870px @ density 3）；只影响字母模式（剪贴板 / 搜索面板的 `contentArea` 是固定高度、符号层与底部功能行不在该容器内），键盘底部固定 ⇒ 键面绝对位置不变，收掉的正是候选栏与键面之间那 12px。

**验证（同日续）**：真机扫描（单行档）—— 键盘顶 1482（= 2280 − 798，候选栏 144 + 字母区 486 + 底行 168，证实字母区已无上边距），候选栏底色一直铺到 1626，1626 起即第一行键的顶部描边，中间无背板带。

## 2026-09-25 键面韵母提示改为可选开关

**背景**：有用户反馈不喜欢 26 键键面显示韵母。

**改动**：新增设置项「键面韵母提示」（`show_key_hint`，默认开），与「候选词行数」同卡片、位于其下方 —— 控制双拼字母键与分号键下方的小字（韵母 + 红色 zh·ch·sh）。关闭后不画提示，且字母改按**铺满居中**绘制（只清空提示会让字母挤在上半、下方留一块空白，看起来像渲染缺失）。规则收敛在 `KeyHint` 纯函数（`visible` / `fillLetter`），渲染与单测同一来源；全拼、英文态、大写锁定不受影响，键面与历史逐项一致。开关一帧只读一次并向下传给分号键，两处不会出现两档错配；键盘下次弹出即生效，无需重启输入法；`show_key_hint` 已进备份导出 / 导入白名单。

**验证**：门禁 52 类 / 553 例 0 失败（新增 `KeyHintTest` 6 例，含「画提示的档位绝不铺满」全组合对拍）、lint 无问题；`PrefsBackupCoverageTest` 精确键数同步为 31；签名包 5,229,469 B 经 `adb install -r` 覆盖安装成功；真机双拼自然码键面截图对照 —— 开 = 韵母提示、关 = 纯字母铺满，开关落盘 true/false 核对通过；验证后设备已还原（26 键全拼 + 开关开）。

## 2026-09-25 双拼候选栏拼音行可切换为声韵显示

**背景**：用户希望候选栏拼音行除「按下的字母」外，还能切换成按声母 / 韵母显示。

**改动**：新增设置项「拼音显示为声韵」（`show_quanpin`，默认关，与「键面韵母提示」同卡片）—— 打开后候选栏拼音行按声母 / 韵母展开成全拼（`vsgo` → `zhongguo`）。转换走新增的 `Shuangpin.displayQuanpin`：合法输入与查询用的 `toQuanpin` 逐字节一致；残码 / 非法组合逐键展开、**绝不丢键**（直接复用查询串会在残码处截断，重现 2026-09-18「按键没反应」的观感）。只影响候选栏那一行文本，候选 / 上屏 / 消费逻辑与输入完全不变；键盘下次弹出即生效；已进备份导出 / 导入白名单。

**验证**：门禁 53 类 / 560 例 0 失败（新增 `PinyinDisplayTest` 7 例：七套方案全码表与 `toQuanpin` 对拍、残码不丢键、逐键推进显示必变、分号键、全拼方案与空串）、lint 无问题；签名包 5,229,469 B 覆盖安装后真机（PACM00，设备当前即自然码）输入 `vsgo` 截图对照 —— 关闭档显示 `vsgo`、打开档显示 `zhongguo`，候选不变；验证后开关已关回原值。

## 2026-09-25 设置页收紧间距与高度

**背景**：用户要求首页控件「减少一点高度、行间距改为 0、上下之间足够紧凑」。

**改动**（只调 `activity_settings.xml` 的间距与高度属性，不动结构与文案）：
- 卡片间距 `layout_marginBottom` 10 → **0dp**（18 处，行间距归零）；
- 卡片内边距 `padding` 12 → **8dp**（15 处），根内容容器 14 → 12dp；
- 按钮 / 下拉 / 输入框高度 44 → **40dp**（22 处）；开关 `minHeight` 40 → **36dp**（6 处）；
- 卡片内控件间距 `layout_marginTop` 各降一档（10→8、8→6、6→4dp）。
每张卡片因此矮约 12dp，一屏可见项明显增多；顺序与文案完全保持上一轮定稿。

**验证**：lint 0 项；签名包 `jinn-release.apk` 覆盖安装后真机（PACM00）截图（顶 / 中两屏）——
卡片 0 间距紧贴、控件高度收紧、顺序与文案逐项核对无变化。

## 2026-09-25 诊断导出收敛为单按钮

**背景**：「导出诊断包 / 发给作者」并排两个按钮是理解偏差 —— 用户要的是一个按钮，文本「导出诊断数据」。

**改动**：删除「发给作者」（`btn_share_diag`），保留单个全宽按钮，文案改为「导出诊断数据」（`settings_export_diag`）。
UI 入口消失后分享通道一并移除：`shareDiagnostics()`、AndroidManifest 的 FileProvider provider、
`res/xml/file_paths.xml`、字符串 `settings_share_diag`。导出逻辑（SAF 存到用户自选位置）与日志目录提示不变。

**验证**：门禁 53 类 / 562 例 0 失败、lint 0 项；签名包覆盖安装后真机（PACM00）dump + 截图核对 ——
底部只剩一个全宽按钮「导出诊断数据」（`[60,1670][1020,1790]`，无 `btn_share_diag`）；APK 内 dex 已无
`settings_share_diag`、manifest 已无 `fileprovider`。

## 2026-09-25 剪贴板面板退出口移到键盘「历史」键

**背景**：用户要求 —— 进入剪贴板页面后，键盘上打开它的「历史」按钮变为红色加粗「返回」、点击回键盘；
面板内部那个「返回」按钮删除。

**改动**：
- `PinyinKeyboardView.renderFunctionPanel`：功能面板第一键按 `clipboardActive` 渲染 —— 关闭态「历史 / 剪贴板」
  （点击开面板），打开态**红色加粗**「返回 / 剪贴板」（点击 `hideClipboardPanel()`）；`buildFunctionButton`
  新增 `red` 参数（走 `skin.hintRed` 皮肤色，与「方向」键进入面板后的红色「返回」同款，不新增颜色来源）。
- `showClipboardPanel` / `hideClipboardPanel` 各自补一次 `refreshCandidateBar()`：可见性变化后候选栏必须重刷，
  否则键面停在旧档（仍显示「历史」= 找不到退出口，或残留红色「返回」）。
- `ClipboardPanelView` 删除内部「返回」：顶栏由 7 键减为 6 键（全部/网址/数字/收藏/搜索/清空）仍均分宽度；
  粘贴成功自动关面板仍走 `Listener.onClose`（接口保留）。

**验证**：门禁 53 类 / 562 例 0 失败、lint 0 项；签名包覆盖安装后真机（PACM00，自然码双拼）截图三连 ——
① 功能面板「历史」正常；② 点开后候选栏该键变红色粗体「返回」、面板顶栏 6 键均无内部「返回」；
③ 点红色「返回」→ 面板关闭、26 键恢复、该键还原为白色「历史」。

## 2026-09-25 剪贴板分组改为「提取干净片段」

**背景**：用户希望网址 / 数字分组从内容里**提取**该类片段并只显示它 ——
「我的电话是123456,你记住了吗?记得访问www.baidu.com」在数字组显示 `123456`、网址组显示 `www.baidu.com`。
确认的三个决策：点击粘贴**提取出的片段**（所见即所得）、多个匹配**只取第一个**、存量条目**重算一次**。

**改动**：
- `ClipboardClassifier`：分类改**多标签**（一条内容可同时进两组）；新增 `firstUrl` / `firstNumber`
  （按「最先出现」提取并归一：网址剥尾随标点、手机号去分隔符与 `+86`、金额去货币符号、关键词后只取号码）
  与 `pieceFor` / `labelText` / `normalizeLabels`；`classify` = 「两个提取函数是否为 null」，
  于是「进组 ⇒ 能提取到片段」是代码级不变式。
- `ClipboardDb`：筛选由 `category = ?` 改 `LIKE '%TAG%'`（旧单值天然兼容，无 schema 迁移）；
  新增 `reclassifyAll()`（分页解密 → 重算 → 有变化才 UPDATE）。
- `ClipboardController.reclassifyIfNeeded()` + `ClipboardPrefs.reclassified`：一次性、后台、幂等；
  备份导入剪贴板后清标记（包里的分类来自导出设备当时的规则）。
- 面板：网址 / 数字组显示并粘贴提取片段（全部 / 收藏组保持原文）；meta 分类标签改中文（`网址·数字`）；
  搜索面板是全库结果、无分组上下文，保持原文，仅 meta 同步中文。
- `ConfigBackup` 导入分类归一改走 `normalizeLabels`（`URL,NUMBER` 不再被压成 `OTHER`）。
- ⚠ **踩坑与修复（只有真机能暴露）**：首版沿用 `\b` 边界，单测（JVM）全绿，但真机对用户例句只判出 `URL`
  —— Android 的 `Pattern` 走 ICU，`\b` 是 Unicode 单词边界（汉字算单词字符），`是|1` 处不成立、数字提取失败
  （URL 是裸域名 `baidu.com` 命中的，与汉字边界无关，所以现象有迷惑性）。改显式前后瞻后真机通过，
  并加源码守卫测试禁止 `\b` / `\B` 回归。

**验证**：门禁 54 类 / 578 例 0 失败、lint 0 项；签名包覆盖安装后真机（PACM00）用**用户自己复制的两条例句**
实测 —— 重算日志「改动 2 条」、库内 `category` 由 `URL` 变 `URL,NUMBER`；面板「全部」显示原文 + meta
`网址·数字`、「网址」只显示 `www.baidu.com`、「数字」只显示 `123456`；点数字组条目粘贴结果为 `123456`
（干净片段，替换了当时选中的文本）。

## 2026-09-25 候选栏重设计：拼音条 + 候选行

**背景**：用户要求重排候选栏里「拼音」与「候选」的分布（原先拼音挤在左侧 120dp 内、候选占右侧一行）。

**改动**：候选栏改为「拼音条（横条）+ 候选行」：单行档 48 → **52dp**（拼音条 20dp 贴顶 + 候选一排 32dp）；
双行档 72 → **84dp**（上排候选 32dp + 拼音条 20dp **垂直居中、叠在两排之间的中缝** + 下排候选 32dp）。
字号两档统一（候选 20sp / 拼音 14sp，用户指定）；拼音条**全宽**（取消 120dp 上限，长输入不再提前截断）；
✕ 清空按钮改叠在整栏右侧（触摸目标 40dp × 整栏高）。拼音条是叠放层：无拼音时（功能面板 / 符号分组 /
预测 / 内联提示）隐藏、候选区占满整栏；候选区三边内边距（左 8dp / 上让位 / 右 ✕ 宽度）集中在
`syncCandidatePadding()`（分开 setPadding 会互相覆盖）。高度唯一来源 `CandidateRows.barHeightPx`：
紧凑值只是下限，系统字体放大时栏高 / 拼音条 / 行高一起长高，不裁字。

**验证**：门禁 53 类 / 561 例 0 失败（`CandidateRowsTest` 同步新规则 + 栏高恒等式断言）、lint 无问题；
签名包 5,229,469 B 覆盖安装后真机（PACM00）两档截图核对 —— 单行档「拼音在上、候选在下」；双行档
「上排候选 / 拼音 vsgo / 下排候选」（首候选仍在下排首列）；清空后功能面板占满整栏；验证用的 2 行档
已还原为用户当前的单行档。

## 2026-09-25 单行档拉开拼音与候选的间距

**现象**：单行档里拼音条与候选行直接相邻，两条文字只隔约 2dp，看起来像一行（用户反馈「行间距不明显」）。

**改动**：新增 `CandidateRows.SINGLE_PINYIN_GAP_DP = 6`，单行档栏高 52 → **58dp**（拼音条 20 + 间隔 6 +
候选 32）；双行档按用户要求维持原样（其间隔由两排之间的中缝充当，84dp / 拼音 14sp 不变）。让位高度改用
`CandidateRows.pinyinAreaHeightPx`（拼音条 + 单行档间隔），与栏高同源。

**验证**：门禁 53 类 / 562 例 0 失败（`CandidateRowsTest` 同步 58dp 与恒等式）、lint 无问题；
真机（PACM00）单行档 `vsgo` 截图对照 —— 拼音与候选之间出现清晰空隙；双行档截图确认维持原样
（顺带核对了设备当时已开启的声韵显示 `zhongguo`）；验证用的 1 行档已还原为设备当前的 2 行档。

## 2026-09-25 候选栏拼音与候选紧贴（间距归零，上一条方向修正）

**背景**：上一条把「行间距不明显」当成「间距太小、要加大」，方向反了 —— 用户澄清真意是
「拼音与文字的上下行间距缩小为 0」。

**改动**：候选栏不再留人为间隔（回退 `SINGLE_PINYIN_GAP_DP`）——
- 候选行高 32 → **28dp**（= 20sp 的行框本身，去掉上下各 2dp 留白）；
- 拼音条 20 → **16dp**：只按**字形**给（14sp × 1.15），字体行框的多余余量溢出条外
  （`pinyin_bar` 设 `clipChildren="false"`，裁的只是行框空白、不是笔画）；此前沿用候选行的 1.4
  行框口径，会让拼音行「比自己的字高出一圈」；
- 栏高：单行 52 → **44dp**、双行 84 → **72dp**（两档都更紧凑，键盘相应变矮）。

**验证**：门禁 53 类 / 562 例 0 失败（`CandidateRowsTest` 同步 44/72dp、两条口径与不裁字断言）、
lint 无问题；真机（PACM00）单行 / 双行两档 `vsgo` 截图对照 —— 拼音与上下候选均紧贴
（只剩字体行框的固有间隙）；验证用的档位已还原为设备当时的单行档。

## 2026-09-25 ✕ 清空按钮移入拼音条（不再遮挡候选）

**背景**：✕ 原先叠在整栏右侧、高等于整栏，单行档时压在候选行上、双行档时横跨上下两排（用户要求
「放在拼音同一行的最右端，完全不遮挡候选字」）。

**改动**：✕ 移入 `pinyin_bar`，**高度 = 拼音条高**（16dp）、贴条右端 —— 单行档落在顶部拼音行、
双行档落在中间拼音行，两档都不与候选重叠。随之取消候选区的右内边距让位（`syncCandidatePadding`
不再有 right 项，候选可顶到栏右缘），候选行宽也因此恢复；拼音文字仍按 `CLEAR_BUTTON_WIDTH_DP`
（40dp）让出右端。✕ 字号 16 → 14sp 以放进 16dp 高的条内。代价：✕ 触摸高度由整栏高降为 16dp
（「不遮候选」的必然结果，已记台账）。

**验证**：门禁 53 类 / 562 例 0 失败（布局尺寸对拍守卫覆盖 ✕ 宽度与拼音条 `paddingEnd`）、lint 无问题；
真机（PACM00）单行 / 双行两档 `vsgo` 截图对照 —— ✕ 与拼音同行同高、候选（两档）都排到屏幕右缘
未被遮挡；验证用的档位已还原为设备当时的单行档。

## 2026-09-25 修复 ✕ 点不动：拼音条须排在候选滚动区之后

**现象**（用户报告）：✕ 按钮点不了 —— 上一条把 ✕ 移进 `pinyin_bar` 时，为「无障碍先读拼音」把
`pinyin_bar` 排在了 `candidate_scroll` **之前**。FrameLayout 里后加的子项绘制与触摸都在上层，
而候选滚动区虽然透明却铺满整栏 ⇒ 触摸全被它接走，✕ 收不到点击。

**改动**：`pinyin_bar` 移回 `candidate_scroll` **之后**（触摸可达）；无障碍遍历顺序改由
`candidate_scroll` 上的 `android:accessibilityTraversalAfter="@id/pinyin_bar"` 拉正（语义不变：
仍先读拼音串再读候选）。新增源码对拍守卫：`CandidateRowsTest` 断言布局里 `pinyin_bar` 必须出现在
`candidate_scroll` 之后 —— 今后再为遍历顺序调回去会直接测试变红。

**验证**：门禁 53 类 / 562 例 0 失败、lint 无问题；真机（PACM00）**实测点击 ✕**：
单行 / 双行两档输入 `vsgo` 后点击均清空候选并回到功能面板（此前仅看截图未点实，才漏掉这个 BUG —
后续凡「按钮类」改动一律加一次实点验证）。

## 2026-09-25 设置页控件重排（用户指定顺序）

**改动**：按用户给定的首页顺序重排 `activity_settings.xml`——
「候选词行数 | 历史数量上限」并排（剪贴板的数量自剪贴板卡上移，该卡只留说明文字）；
「键面韵母提示 | 拼音显示为声韵」两个显示开关并排（原为纵向排列）；
「语音输入」总开关自页面底部上移到显示开关之后；其余未提及的控件保持原有相对顺序。
只调布局块位置与分组：控件 id、代码逻辑与文案下发方式均未变。

**验证**：编译 + lint 0 项（id 唯一性核对通过：`edit_clipboard_max` 在旧处的重复已删）；
真机（PACM00）`uiautomator dump` 逐项核对 —— 纵向 y 单调递增、并排项同行 y 相等：
检查更新/下载 → 启用/改默认 → 符号顺序/自定义 → 补充词库/键盘皮肤 → 增加模糊拼音 →
候选词行数/历史数量上限 → 拼音输入方案/界面明暗切换 → 键面韵母提示/拼音显示为声韵 → 语音输入 ✓；
截图确认并排卡片的文字与控件无裁切。

## 2026-09-25 设置页按用户清单定稿（顺序 + 文案 + 新按钮）

**改动**：按用户逐项给定的清单重排设置页并统一文案（顺序与文本都严格照排）——
- 顺序：拼音输入方案 / 界面明暗切换 移到 候选词的行数 / 剪贴板的数量 **之前**；语音输入上移到
  「用户词频学习 / 追加候选词语」之后（授权麦克风、NAS 语音紧随其后，仍随开关显隐）；
  配置备份（导出 / 导入配置文件）移到诊断卡之前；
- 文案：「候选词的行数」「剪贴板的数量」「键盘内嵌韵母」「双拼显示声韵」「导出配置文件」「导入配置文件」；
- 删掉纯说明卡（「剪贴板 / 剪贴板内容加密保存在本机…」），`settings_clipboard` 与
  `settings_clipboard_desc` 两条字符串一并移除（否则 lint 报未使用资源）；
- 新增「发给作者」：生成诊断包后经系统分享面板发出（FileProvider 只暴露 `cacheDir/JinnIme/`、
  无存储权限）；「导出诊断包」逻辑不变（SAF 存到用户自选位置），两者共用 `Diagnostics.exportBundle`。

**验证**：门禁 53 类 / 562 例 0 失败、lint 0 项；真机（PACM00）`uiautomator dump` 逐项核对顺序
（y 递增、并排同行）与文案（逐字一致）、剪贴板说明卡已消失；**实点**「发给作者」→ 系统分享面板弹出 ✓。

