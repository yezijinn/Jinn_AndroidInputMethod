# 更新日志

> 本文件按次序记录改动（自下而上积累），最新发布见文末「发版：20260920」。
> 早期次序中提到的「剪贴板隐私分类 / 掩码」功能已于 2026-09-16 整体移除，当前版本不含该功能。

> 本次为接手后的第一批修复，严格避开语音模块（MicRecorder / AsrClient / Protocol /
> MicButton 以及 JinnIme 中的录音、WebSocket、识别结果处理链路均未改动）。
>
> 日期：2026-09-15 ｜ 基线提交：`941ca68` ｜ 验证：`testDebugUnitTest` BUILD SUCCESSFUL

## 总览

| 类别 | 数量 |
|---|---|
| 修复 P0（严重）问题 | 3 |
| 修复 P1（中等）问题 | 3 |
| 修复 P2（轻微）问题 | 3 |
| 改动文件 | 9 个源码 + 2 个配置 |
| 新增文档 | `ONBOARDING.md`、`计划任务.md`、`更新日志.md` |

所有改动仅保证编译通过 + 单元测试通过，UI 手感类改动需真机复验（见文末）。

---

## P0-1 修复拼音键盘按键按压反馈失效

问题：`PinyinKeyboardView.bindLetterKeys()` 给每个字母键设置 `OnTouchListener`，
而 `handleKeyTouch()` 所有分支一律 `return true`。Android 事件分发中监听器返回 true 即消费事件，
`PinyinKey.onTouchEvent()` 永不执行 → `pressed` 恒为 false、按压高亮是死代码、
`performClick()` 不触发。用户按字母键看不到任何视觉反馈。

改动：
- `PinyinKey.kt`：新增公开方法 `setPressedVisual(Boolean)`，内部比较后 `invalidate()`。
  命名刻意避开 `View.setPressed`，不复用系统按压态（会被父容器重置）。
- `PinyinKeyboardView.kt`：`handleKeyTouch()` 在 `ACTION_DOWN` / `ACTION_UP` / `ACTION_CANCEL`
  三个分支显式调用 `keyViews[c]?.setPressedVisual(...)` 驱动按压态。

为什么这样修：退格键用的是 `btnBackspace.isPressed = true`（系统按压态 + 选择器），
不受影响；只有字母键走了自定义触摸监听，所以只改字母键路径，不改动事件分发语义，风险最低。

---

## P0-2 补齐剪贴板隐私分类功能

问题：`is_private` 在 `ClipboardDb` 中已完整实现（建表、upsert、去重合并标记、
`deleteAll` 豁免），但 UI 层完全没有入口，分类栏只有 4 个 Tab、长按菜单只有收藏/删除、
没有任何明文掩码。而 `AGENTS.md` 明确承诺「隐私分类」与「隐私内容默认隐藏明文」。

改动：

数据层 `ClipboardDb.kt`
- `whereClause()` 增加 `privateOnly` 参数；`count()` / `recentPage()` 增加同名默认参数
  （默认 false，现有调用点零改动）。
- 新增 `setPrivate(id, Boolean)`：隐私只能由用户主动标记，数据层不提供任何自动判定入口。

独立页 `ClipboardHistoryActivity.kt`
- 新增「隐私」分类 Tab 与 `CATEGORY_PRIVATE = "PRIVATE"` 伪分类常量（与 `FAVORITE` 同构，
  是独立标签列而非 `category` 取值，传 SQL 前会转成 `privateOnly`）。
- 列表渲染：隐私条目默认显示掩码文案，点击一次展开明文（`revealedIds`），再点一次才粘贴。
- 长按菜单新增「标记为隐私 / 取消隐私标记」，走 `BackgroundIo` 后台线程。
- `runChunkedSearch` 的分类判定增加隐私分支。

内嵌面板 `ClipboardPanelView.kt`
- 同样新增「隐私」Tab、掩码与展开逻辑、长按操作条新增「隐私」按钮（`togglePrivate()`）。
- 分页查询同步支持 `privateOnly`。

资源 `strings.xml`：新增 `clipboard_cat_private` / `clipboard_mark_private` /
`clipboard_unmark_private` / `clipboard_private_masked`。

注意：隐私条目在列表里的 meta 行会显示「· 隐私」标记；`deleteAll` 原本就已豁免隐私条目，
现在这条规则真正可生效了。

> 注：隐私分类与掩码功能已于 2026-09-16 整体移除（见下文「剪贴板模块关键约束」）；本条目为当时
> 的临时实现，当前版本不再包含任何隐私功能。

---

## P0-3 修复粘贴状态机卡死与跨输入框误粘贴

问题：
1. `ClipboardHistoryActivity` 的 `pasting` / `pendingPasteItemId` 一旦 IME 不回广播
   （进程被杀、未处于输入态）就永久为 true，之后点任何条目都无响应，粘贴功能彻底卡死。
2. `JinnIme.pendingPasteText` 没有时效，`flushPendingPaste()` 在下一次 `onStartInputView`
   无条件提交，用户切换输入框或搁置很久后，旧文本会被粘到完全无关的位置。

改动：
- `ClipboardHistoryActivity.kt`：新增 `pasteTimeoutRunnable`（3s 超时）与统一的
  `resetPasteState()`；`requestPaste()` 发出广播后挂上超时，结果接收器与失败分支都走统一复位，
  `onDestroy` 一并移除回调。
- `JinnIme.kt`：新增 `pendingPasteAt` 时间戳与常量 `PENDING_PASTE_TTL_MS = 10s`；
  `flushPendingPaste()` 先判时效，过期则丢弃并记日志（连接仍无效时放回暂存但保留原时刻，
  不会因反复重试而无限续期）。

---

## P1-4 去掉每次按键的一倍冗余查询

问题：候选刷新时额外调用了一次 `PinyinEngine.queryWithCompletion()`，
而 `PinyinEngine.query()` 内部已经跑过补全召回，这次重复调用的结果只用于打一行诊断日志，
等于每次按键双倍查询开销。

改动：`PinyinKeyboardView.kt` 删除该重复调用，诊断日志改为复用 `query()` 已有的结果
（`partialSyllable` + 前 3 个候选），并降级为 `Diagnostics.v`（原来是 I 级，会刷屏）。

---

## P1-5 修复 Root 审计命令阻塞与假检查

问题：
- `su()` 只 `readText()` stdout，不排空 stderr。子进程 stderr 管道写满会阻塞，
  `find` 这类可能大量输出的命令会直接挂死；且 `waitFor()` 无超时。
- `checkBackupConfig()` 硬编码返回 ✓，无论 `backup_rules.xml` 被改成什么样都显示「安全」。

改动：`ClipboardFirewall.kt`
- `su()`：后台 daemon 线程排空 stderr；`waitFor(10s)` 超时后销毁进程并返回 null。
- `isRootAvailable()`：同样加超时（用户未在 su 弹窗授权时会一直挂着）。
- `checkBackupConfig(context)`：运行时用 `resources.getXml(R.xml.backup_rules)` 解析实际生效的
  规则文件，确认 `jinn_clipboard.db` 被排除，未排除则报 ✗。
- 顺带删除 `checkExternalLeak(pkg)` 与 `audit()` 中未使用的参数（消除编译警告，不是未跟踪文件）。

---

## P1-6 删除操作改用可写数据库句柄

问题：`ClipboardDb.delete()` / `deleteAll()` 用 `readableDatabase` 执行写操作。
正常情况下 `getReadableDatabase()` 返回的就是可写实例所以没暴露问题，
但磁盘满等异常场景会退回只读句柄，删除会静默失败。

改动：两处均改为 `writableDatabase`。

---

## P2-7 修正与实现完全相反的注释

问题：`showClipboardPanel()` 的 KDoc 与初始化处注释都写「viewLetters 用 INVISIBLE、
零 layoutParams 修改、绝不 relayout」，而实际实现是「viewLetters 置 GONE、
contentArea 改成固定 162dp×2 高度、会修改 layoutParams」。注释与实现完全相反，极易误导。

改动：`PinyinKeyboardView.kt` 两处注释改为描述真实实现，并保留「layoutParams 类型必须匹配
父容器，用错会 ClassCastException」这条真正有价值的经验。

---

## P2-8 忽略疑似云凭证文件

问题：根目录 `ses_fb2d904cbffewGCRN4iuzDTDSg.json`（3.6MB）未被 `.gitignore` 忽略，
有误提交风险。

改动：`.gitignore` 新增 `ses_*.json`、`*service-account*.json`、`*-credentials.json`。
（文件本身未删除，请自行确认后处理。）

---

## P2-9 搜索防抖的滚动残留

改动：`ClipboardHistoryActivity` 新增 `scrollToken`，「滚回顶部」的 `post` 任务带上令牌，
快速切分类或连续输入时旧任务自动失效（对齐内嵌面板 `ClipboardPanelView` 已有的做法）。

---

## 构建环境改动（非功能）

| 文件 | 改动 | 原因 |
|---|---|---|
| `settings.gradle.kts` | 启用注释里预留的阿里云镜像（pluginManagement + dependencyResolutionManagement） | 本机直连 `dl.google.com` / `repo.maven.apache.org` 解析失败（guava、asm-util 等依赖找不到），镜像可正常拉取 |
| Gradle 8.9 | 手动安装到 `~/.gradle/wrapper/dists/` | Wrapper 下载 gradle-8.9 时 SSL 握手失败，改用腾讯云镜像下载后手动放置 |

> 镜像对国内网络是加速，且 Google/MavenCentral 仍在列表中作为兜底；
> 若你的网络可直连，把镜像行注释掉即可，不影响构建结果。

---

## 新增文档

| 文件 | 内容 |
|---|---|
| `ONBOARDING.md` | 接手指南：架构、目录、技术栈、IME 生命周期、核心逻辑、已知问题、风险、维护约束 |
| `计划任务.md` | 按严重度排序的任务清单（已修/待办/不修及原因） |
| `更新日志.md` | 本文件 |

---

## 明确未修（受「禁止修改语音」约束）

| 问题 | 原因 |
|---|---|
| `refreshConfig()` / `ACTION_CONFIG_UPDATED` 是死代码 | 唯一调用目标是 `asr.connect()` 重连，属语音链路。已知影响：设置页改服务端地址后 IME 不热更新，靠 `killProcess` 重启生效 |
| `AsrClient` / `MicRecorder` 零测试覆盖 | 语音模块，解禁后建议优先补 |
| VAD 静音阈值 120s 偏长 | 语音模块 |

---

## 验证与真机复验清单

```bash
.\gradlew.bat testDebugUnitTest   # BUILD SUCCESSFUL，7 个测试类 ~100 用例全过
```

以下改动必须真机复验（编译通过 ≠ 体验正确）：

1. 按键按压反馈：按任意字母键应出现高亮，松手恢复；符号层横滑翻页仍正常。
2. 隐私功能：长按条目 → 标记隐私 → 切到「隐私」Tab 能看到该条 → 列表显示掩码 →
   点一次展开明文 → 再点一次才粘贴；「删除全部」不应删除隐私条目。
3. 粘贴超时：正常粘贴仍应成功并关闭页面；断网/杀 IME 进程后 3s 内应恢复可点击状态。
4. 剪贴板面板：顶栏新增「隐私」按钮后共 8 个格子，需确认在窄屏上文字不被挤掉
   （若过挤，可考虑把「返回」改图标或缩短标签）。

---

# 补充：键命中 / 搜索下推 / 掩码重置 / 内存采样

- **字母键抬起无命中判定**：从 Q 滑到 W 抬起仍回调 Q 的监听器 → `ACTION_UP` 增加 `isInsideKey()`
  （raw 坐标比对、边界外扩 8dp；取不到布局信息返回 true 保留旧行为）
- **剪贴板搜索减少解密**：分类 / 收藏 / 隐私下推到 SQL（`db.recentPage(..., scope, ...)`）先过滤再解密；
  序号基准改为分类内总数 `db.count(scope, ...)`，与搜索态 `categoryTotal` 语义一致
- **隐私掩码未重置**：`onPanelShown()` 清空 `revealedIds`，重开面板须再点一次
- **词库内存与耗时采样**：`PinyinEngine.load()` 前后各采样一次进程内存并记录耗时写入日志

**不修（已登记）**：粘贴广播携带明文（#10，现有 `setPackage` + `RECEIVER_NOT_EXPORTED` 已阻断跨应用接收，
改 itemId 异步查库会动只有真机可验的核心链路）；候选栏重建 TextView（#14，复用省不掉 measure / layout，
应整体换 RecyclerView 而非打补丁）。

验证：单测通过。真机复验：Q 按下滑到 W 抬起应上屏 W（不应上屏 Q）；切分类后搜索序号从该分类总数往下数。

---

# 改动自查：掩码文案与粘贴复位

- 掩码文案「内容已隐藏，点击显示后再点一次粘贴」过长，单行末尾省略号会截断 → 缩短「已隐藏 · 点击显示」（资源与面板硬编码同步）
- `pasteTimeoutRunnable` 手写复位绕过 `resetPasteState()` → 改为先取 id 打日志再走统一复位口

**复查无问题（记录免重复怀疑）**：属性初始化顺序（lambda 3 秒后执行，无 NPE）；SAM lambda 的 `this` 指向宿主；
`whereClause` 参数兼容（既有调用点行为不变）；`isInsideKey` 保守分支；`handleItemClick` 隐私分支在 `if (pasting) return` 之后。

验证：单测通过；已修 14 项。

---

# 推进 #9：抽取 ClipboardFilter

- 分类栏 5 个 Tab 里收藏 / 隐私是独立标签列，当 category 传给 SQL 恒不成立（列表恒空）。新增
  `ClipboardFilter(category, favoritesOnly, privateOnly)` + `of(raw)`，5 处重复翻译统一改为调用它；
  UI 的 `CATEGORY_FAVORITE` / `CATEGORY_PRIVATE` 改为引用 `PSEUDO_*`
- 新增 `ClipboardFilterTest`（7 例）：钉死「收藏 / 隐私的 category 必须为 null」
- 渲染层重复仍在（需真机回归），本轮只做纯函数部分

验证：单测 8 类 / 161 例全绿。

---

# 搜索面板隐私泄露（P0）

- `SearchPanelView.getView()` 直接显示明文且点一次就粘贴 → 补齐 `revealedIds`、掩码「已隐藏 · 点击显示」、
  meta 补「· 隐私」、首次点击只展开二次才粘贴，`onShown` / `onHidden` 清空
- 删掉空态文案「找到 N 条」的无效赋值（同函数下一行即设 GONE）

**复查未改**：搜索全库扫描是设计如此；进搜索不跟随当前分类（需产品确认）。

验证：单测通过。
# 设置页：保存遗漏 / 开关递归 / 回调越界 / Spinner 改写配置

- **剪贴板上限不生效**（P1）：`saveAndRestart()` 未保存上限，而它依赖的失焦回调时序不保证早于保存
  → 显式调用 `saveMaxItems()`（幂等）
- **Root 开关递归**（P2）：程序化拨回开关再次触发监听器，把「不可用」提示覆盖成功能描述
  → 新增 `suppressRootSwitchCallback` 抑制回调
- **导出回调未判销毁**（P2）：后台打包后直接操作 view → 加 `isFinishing || isDestroyed` 返回
- **Spinner 改写默认键盘模式**（P2）：`onItemSelected` 早于 `loadPrefs()` 注册，`setSelection` 触发它；
  值不在数组时 `indexOf` 返回 -1 → 退回第 0 项（语音）→ 新增 `defaultModeSpinnerReady`，加载完成才置位

验证：单测通过。真机：改上限后保存重启应保持；未 Root 开启应提示「不可用」；反复切默认模式重启后保持。
累计已修 21 项，8 类 / 161 例。

---

# 拼音键盘：退格双击态 / 面板回滚 / 搜索残留拼音

- **退格双击态永不超时**（P0）：双击后 `backspaceTapCount=2` 长期挂着，几分钟后长按仍触发 `onDeleteAll()`
  → 双击成立时记录第二击时刻，DOWN 分支加 `clearGestureWindowMs`（500ms）校验，超窗即失效
- **剪贴板面板异常后 26 键永久消失**（P1）：`onPanelShown()` 抛异常时 catch 只置 `clipboardActive=false` 不恢复视图，
  而 `hideClipboardPanel()` 开头即 return → 抽出 `restoreLettersLayout()`，catch 完整回滚并通知 IME
- **切搜索模式不清拼音缓冲**（P1）：残留拼音不可见但生效（退格先删它、空格把候选塞进搜索框）
  → 新增 `clearComposingState()`，进出都调

**未修（待真机核实）**：`onPredictionSelected` 漏搜索分支；切层 / 切大写不清拼音串；方向面板与剪贴板互斥只做一半；
拖选态行首 / 行末跳全文首尾；`capsMode` 生命周期不复位（「清空后连删不停」已顺带修：`removeCallbacks`）。

验证：单测通过。真机：点两下退格后等几秒长按应正常连删；0.5s 内长按应触发清空。累计已修 24 项，8 类 / 161 例。

---

# 待核实清单：修 4 项、1 项维持现状

- **搜索态点预测词串进宿主输入框**（P1）：`onPredictionSelected` 缺 `isPanelSearch()` 分支 → 补上
- **大写锁定切换不清拼音串**（P2）：切换时清 composing 与预测（同切中英文）
- **方向面板与剪贴板互斥只做一半**（P2）：`showDirectionPanel()` 开头先关剪贴板面板
- **拖选态行首 / 行末跳全文首尾**（P2）：`nextFocus` 改为按当前行计算（与光标态统一）+ 补多行用例

**维持现状**：`capsMode` 不在 `configure()` 复位（大写锁定是用户显式持久态，重置会让「锁定」失去意义）。

验证：单测 8 类 / 162 例全绿。累计已修 28 项。

---
# 性能瓶颈与边界排查

- **V 级日志全量落盘**：`Diagnostics.log()` 每级都写文件（无持久流，每条 open → write → close），
  V 级记录音频包收发（每秒可达 10 条）→ 新增 `VERBOSE_TO_FILE`（默认 false），V 级只走 logcat
- **音节前缀判断 O(n) → O(1)**：`isTruePrefixOfSyllable()` 每次遍历 400+ 音节做 `startsWith`，
  且在每次按键路径上多次调用 → 加载时预建 `syllablePrefixes` 改哈希查找
  （`PinyinEngineTest` / `PinyinCompletionTest` 通过确认语义不变）
- **保活开关缓存为 false 后永不刷新**：`JinnAccessibilityService` 的 `onAccessibilityEvent` 开头
  `if (!keepAliveCached) return` 把重读挡在返回后 → 廉价过滤提到最前，节流窗口外无条件重读，移除缓存字段
- **白名单命令首条失败则后续全不执行**：`commands.all { runSu(it) }` 遇 false 短路，而 4 条命令彼此独立
  → 改为逐条执行再汇总，日志输出「成功 N/4 条」
- **文档订正**（`AGENTS.md`）：测试命令改为 `.\gradlew.bat testDebugUnitTest` + 镜像回退；补 3 个模块；
  「保存并测试连接」改为「保存后杀进程重启 IME」；写明语音模块禁改；补伪分类陷阱与 `ClipboardFilterTest` 守卫

验证：单测通过。累计已修 33 项，8 类 / 162 例。
性能量化（待真机）：对比日志体积确认写入下降；词库加载耗时日志确认未变慢。

---
# 真机实测：构建 / 签名 / 部署 / 验证跑通

真机可用（PACM00），首次完成完整闭环。

- **构建与签名**：密钥库 `com.jinn.inputmethod/release.jks`，V1=false / V2=V3=true；产物 `jinn-release.apk`
  10.6MB；`adb install -r` Success（`versionCode=20260915`）。密钥根由 `JINN_KEYSTORE_ROOT` 环境变量指定
- **词库内存结案**（#7）：加载前 9MB → 后 165MB（堆上限 384MB），净占 156MB / 41%；加载 8424ms；
  词语键 1,048,342、反向索引 1,162,486、音节 422。
  预分配无空间（`HashMap(1_500_000)` 经 `tableSizeFor` 得 2^21，不触发扩容）；唯一方向是省
  `wordToPinyin`（约 45MB），但它是 `consumption()` 残码处理与 `predict()` 必需，改动覆盖候选生成 /
  消费 / 预测 / 测试全链路 → **判定不做**（收益 29% 内存，当前 165 / 384MB 无 OOM 压力；将来堆上限
  192 / 256MB 的设备再按「候选携带拼音」重构）
- **退格 P0 双向验证**：`adb shell input` 单次 200~400ms 无法凑进 280ms 双击窗口 → 临时放宽到 3000ms，验证后还原。
  A：双击 → 等 3s → 长按 1.2s 应不清空 ✅；B：双击 → 立刻长按应清空 ✅（旧代码下 A 必清空，用例能有效区分）
- **保活前台提升失败不再崩溃**：`startForeground` 无异常保护，部分 ROM 抛 `SecurityException` /
  `ForegroundServiceStartNotAllowedException` → 抽出 `promoteToForeground()`，失败记日志并 `stopSelf()`，
  `onStartCommand` 返回 `START_NOT_STICKY`
- 词库诊断日志补反向索引规模

验证全通过：release 签名、覆盖安装保留数据、IME 重启加载 8424ms（422 音节 / 104.8 万键）、键盘 UI 截图完整、
退格双向、内存采样 165 / 384MB。累计已修 36 项，8 类 / 162 例。

---

# 修复「企鹅」打不出来（连写歧义类漏词）

用户报告：双拼 `qiee` 打不出「企鹅」（qi + ee）。

- **定位**：词库里「企鹅」0 条，而含「企鹅」的 23 个词拼音都标 `qie`，`qie` 键只有 14 个单字；
  双拼转换正确（`ZERO_TABLE` 有 `ee → e`，`toQuanpin("qiee")` = `qie`）→ 问题在词库。
  根因是一整类：拼音 `S1 + Z`（Z 为零声母音节 a/ai/an/ang/ao/e/ei/en/eng/er/o/ou）连写后恰等于另一合法
  单音节 → 同键（企鹅 qi+e→qie 撞「切」、西安 xi+an→xian 撞「先」、饥饿 ji+e→jie 撞「姐」、
  提案 ti+an→tian 撞「天」、彼岸 bi+an→bian 撞「边」），实测这批词全不在词库
- **检测工具**：新增 `detect_ambiguous_keys.py` 枚举歧义键 → 歧义键 144 个，其中可疑（只有单字）112 个，
  作后续人工核对面
- **修复**：新增 `add_words.py` + 补词表 `fix_ambiguous_words.txt`，补入 11 个缺失词并插入第 4 位
  （前三位留给最高频单字，不抢首候选也不用翻页）：企鹅 / 西安 / 饥饿 / 提案 / 奇案 / 彼岸 / 立案 /
  堤岸 / 皮袄 / 集安 / 西澳。修复后 `qie` 键 = `且|切|窃|企鹅|怯|…`，键数 1,048,342 不变
- **顺带修 `dict_builder.py`**：`write_outputs` 对带空格拼音各写一行，「qi e」与「qie」compact 相同 →
  重复键；app 侧 `phrasesByPinyin[key] = ...` 后写覆盖前写，词条静默消失 → 改为先按 compact 聚合再输出
- **防回归**：新增 `PhraseDictIntegrityTest`，流式扫描 assets 词库断言这些词存在（读到所需键即停，不加载整份）

验证：单测 9 类 / 163 例全绿；真机重启 IME 后点按输入 `q i e` 与 `q i e e`，候选第 4 位均出现「企鹅」。
累计已修 39 项。

**未修（需确认）**：疑似编码损坏词条「涓嶅」出现在 603 个词的拼音键下（UTF-8 字节被按 GBK 解码的产物，
原文可能是「不」），属词库源数据问题，已登记；若确认是坏数据，建议在生成流程加编码校验。

---

# 新增「显示生僻字」开关（默认关闭）

开关只在设置页（键盘无入口），默认关。

- **判定标准**：依《通用规范汉字表》(2013)：一级 3500 + 二级 3000 = 6500 字为常用（保留）；三级 1605 与表外字为
  生僻（默认不加载）。字表写入 asset `common_chars.txt`（6500 字 / 19.8KB）
- **过滤粒度**：整词丢弃（词中任一字生僻即丢）——用不到生僻字也就用不到含生僻字的词
- **加载时过滤**（非加载后）：解析词库即跳过，被跳过的词条从未进 HashMap，才能真正降内存
- **数据结构**：`BooleanArray(0x9FFF + 1)` ≈ 64KB 按码点索引 O(1)；不用 `HashSet<Char>`（需判 123 万词条 /
  450 万字符）。规则：`< 0x4E00` 一律放行；`0x4E00~0x9FFF` 查位图；其余（含扩展区代理对）视为生僻
- **开关位置**：设置页「自动唤起键盘」之后；切换即写配置并 Toast「重启输入法后生效」

**实测收益**：单字表 17,863 → 7,038（−60.6%）；词组键 1,048,342 → 892,539（−15.1%）；词条 1,230,120 →
1,017,136（−17.3%）；字符总量 −15.9%。真机：词语键 892,714、反向索引 953,915、音节 422 → 414、过滤 223,358 条；
`dumpsys meminfo` Java Heap Alloc 131MB 与降幅吻合（`Runtime.totalMemory() - freeMemory()` 含未 GC 垃圾，
加载刚结束读会偏高）。

**测试**：新增 `RareCharsFilterTest`（3 例：「饕餮」丢弃而同键「涛涛」保留、生僻单字「饕」过滤）；加
`PinyinEngine.resetForTest()` 避免单例污染。单测 10 类 / 166 例全绿。

---

# APK 体积瘦身（10.6MB → 8.4MB，−20.8%）

- **诊断**：APK 10.6MB 中 `pinyin_phrases.txt` 压后 10.7MB（占 95%），其余全部 < 0.6MB → 体积即词库体积
- **排除的手段**（实测）：裁生僻字（字节仅 −7.9%，拼音键占词库 44% 且不含汉字，过滤不掉）；
  只留一级 3500 字（−10.8%）；限词长 ≤3（−37.2%，但会砍四字常用词）；提高 deflate 等级（持平）；
  R8 / shrinkResources（早已启用）
- **采用**：词库改 xz + 流式解压。原始 28.01MB → deflate 10.22MB → xz preset=6 8.00MB（再省 21.7%）。
  `pinyin_phrases.txt.xz` + 依赖 `org.tukaani:xz:1.9`（dex 后约 80KB）+ `XZInputStream` 流式解压
  （不落地磁盘、不构造超大 String）+ `noCompress += "xz"` + `PhraseDictIntegrityTest` 改为解压校验
- **代价**：加载 8424ms → 10640ms（+2.2s，xz 纯 Java 比原生 zlib 慢）。加载在 `JinnIme.onCreate` 独立线程，
  键盘视图创建在其之后 → 不阻塞 UI，代价仅是「弹出后约 2.2s 内候选未就绪」；真机候选验证正常。
  回退：删 `xz` 依赖与 `noCompress`，源文本放回 assets，`loadPhrases()` 换回 `assets.open`

累计已修 43 项；单测 10 类 / 166 例；APK 10.6 → 8.4MB（−20.8%），签名方案不变。

---

# 词库分层：APK 只内置基础包，长词改为可选下载

需求：少用 / 专业词库做成可下载扩展项，不进 APK。

- **切分依据**：只能按词长（格式无词频字段）。实测 1–2 字 xz 0.64M / 1–3 字 3.16M / 1–4 字 5.74M
  → 选 **1–3 字**：三字词覆盖日常（天安门 / 计算机 / 办公室），四字及以上以成语与长尾专有名词为主。
  代价：装扩展包前四字成语打不出来
- **切分**：`split_dict.py` 输出两个同格式包（运行时同一解析器合并）
  - 基础包（≤3 字）：462,189 键 / 628,800 词，9.92MB → xz 3.16MB ← 进 APK
  - 扩展包（>3 字）：586,190 键 / 601,320 词，17.09MB → xz 4.73MB ← 按需下载
  - 合计 1,230,120 词条，与源零丢失
- **引擎**：新增 `loadExtensionDict()`（读 `filesDir/dict_ext.xz`，不存在静默跳过）；
  `loadPhrasesReader(reader, merge)` 新增合并模式 —— **关键正确性点**：两包存在相同拼音键
  （如 `qie`），直接赋值会把基础候选整体挤掉，必须 `existing + kept` 合并且基础在前；
  预分配容量按基础规模下调（60 万 / 70 万）
- **设置页**：新增「扩展词库（长词包）」卡片（状态 / 地址输入 / 下载安装 / 删除）。下载先写 `.tmp`
  再改名（避免半截文件被引擎当有效词库加载）；回调切主线程并判 `isFinishing / isDestroyed`

结果：APK 10.6MB → **3.6MB（−66%）**；扩展包 `release/dict_ext.txt.xz` 4.73MB，直链填入设置页即可安装。

验证：编译 + 单测 10 类 / 166 例通过（含完整性测试，基础包仍有「企鹅」「西安」）；APK 实测 3.6MB；
签名 V1=false / V2=V3=true；真机安装 Success；设置页 UI 截图确认。
未验证：基础包真机加载数据（adb 未聚焦输入框，未采到日志；逻辑与上一版一致、仅词条减少，风险低）；
扩展包下载与同键合并（需可访问的下载地址）。

累计：已修 47 项。

---

# 词库分发上线 + 源码同步双仓库

- **分发位置**：托管为 Releases 附件（非仓库文件）
  - Gitee（主源）：`.../releases/download/dict-ext-20260915/dict_ext.txt.xz`
  - GitHub（备用）：`.../releases/download/dict-ext-20260915/dict_ext.txt.xz`
  - 两源实测：302 重定向后 200，`Content-Length = 4956768`；OkHttp 默认跟随重定向
  - **坑**：最初把扩展包提交进仓库用 raw 直链 → Gitee 对大文件 raw 返回 404（文件确实存在、API 可查），
    小文件（README.md）raw 正常 → **大文件必须走 Releases 附件**
- **下载体验**：设置页预填主源直链；多源回退（用户填写 → Gitee → GitHub）；下载成功后自动重启 IME
  （词库只在启动时加载，省掉用户再点重启）；先写 `.tmp` 再改名
- **源码同步**：GitHub `perf/paging-pinyin-rematch` 与 Gitee `master` 均推到 `12dc44b`
  （`6226554` 首批修复 + 优化 + 新功能 39 文件；`12dc44b` 下载源修正）。`.gitignore` 新增：
  本地工作区目录、`docs/*.zip`（内容已提取为 `assets/common_chars.txt`）、
  `tools/dict_builder/source/`（28MB 未压缩源，可由基础包 + 扩展包还原）
  - 推送 GitHub 遇两次 `TLS connect error`（国内网络波动），第三次成功 —— 重试即可，非配置问题

累计：已修 48 项；APK 3.6MB（含基础词库），扩展包 4.7MB 按需下载；单测 10 类 / 166 例。

---

# 基础包纳入四字成语 + 界面调整 + 同步 main

- **切分点 3 字 → 4 字**：四字成语属高频用词，必须随 APK 内置
  - 基础包：462,189 键 / 628,800 词 → **826,897 键 / 1,006,326 词**（3.16MB → 5.74MB）
  - 扩展包：586,190 键 / 601,320 词 → **221,510 键 / 223,794 词**（4.73MB → 2.19MB）
  - APK 3.6 → 6.2MB（仍比最初 10.6MB 小 42%）；扩展包反而更小、下载更快；词条合计 1,230,120 零丢失
- **扩展词库 v2 直链**（已重传实测，两源 HTTP 200 / `Content-Length = 2297184`，设置页预填 Gitee 主源）：
  `.../releases/download/dict-ext-20260915-v2/dict_ext.txt.xz`（Gitee 主源、GitHub 备用）
- **界面**：「点这里唤起键盘打字」移到「保存配置并立即重启生效」下方（保存完即可验证）；扩展词库两个按钮
  改用 `@style/SettingsButton`（与电池白名单等一致，此前是裸 Button、与深色主题不搭）；地址输入框补
  `field_aurora` 背景与内边距；状态文案改为「未安装，五字及以上长词暂时打不出来（四字成语已内置）」
- **同步 main**：main（`ab7e6e8`）是本地 HEAD 祖先 → 快进推送，无需强推：
  GitHub `main` `ab7e6e8..c8b93e9`、GitHub `perf/paging-pinyin-rematch` `ae5f376..c8b93e9`、
  Gitee `master` `ae5f376..c8b93e9` —— 三个引用均指向 `c8b93e9`

累计：已修 52 项；APK 6.2MB（含四字成语，原 10.6MB，−42%）；扩展包 2.19MB；单测 10 类 / 166 例。

---

# 移除全部保活机制

承接上一轮审查结论：保活三件套属过度设计。IME 是系统绑定服务、用户一输入即被拉起，真正需要常驻的
只有那条 WebSocket 长连接；换来的"进程常驻"对一个自用输入法不值得，代价是 5 项权限、一个无障碍服务、
一个前台服务与一个 ROOT 依赖。

- **删除**：源文件 `KeepAliveService.kt` / `JinnAccessibilityService.kt` / `RootShizuku.kt` /
  `BootReceiver.kt`（4 个）+ `res/xml/accessibility.xml`；权限 `FOREGROUND_SERVICE`、
  `FOREGROUND_SERVICE_SPECIAL_USE`、`POST_NOTIFICATIONS`、`RECEIVE_BOOT_COMPLETED`、
  `REQUEST_IGNORE_BATTERY_OPTIMIZATIONS`（5 项）；依赖 `dev.rikka.shizuku:api`；设置页整张「后台保活」
  卡片（3 开关 + 3 按钮 + 状态文本）、`notifyPermissionLauncher`、`toggleKeepAlive` /
  `ensureNotifyPermission` / `runRootKeepAlive`；配置 `Prefs.keepAlive` / `useRootShizuku` /
  `notifyHighPriority` 及 KEY 常量
- **刻意保留**：剪贴板卡片的「Root 增强模式」——它做的是剪贴板数据目录安全审计（防第三方读取），
  与"保活防杀"是两件事，只是恰好都用 root，不能顺手删
- **按需连接**：`asr.connect()` 时机未改（属语音模块，按禁改约束不动）。保活移除后实际已接近按需：
  IME 不再被强制常驻，用户输入时系统拉起即建连、后台闲置随进程回收。真正做到「按下录音键才连接」
  需改 `AsrClient` 的连接时机，要先解除语音约束

验证（真机）：编译 + 单测通过，引用清理干净；APK 6.2MB，签名 V1=false / V2=V3=true；安装 Success；
设置页截图确认保活卡片消失；语音链路日志 `renderLink: CONNECTING → ONLINE` 正常。

**工程教训（本次踩两次）**：改 `strings.xml` / `findViewById` 序列这类每行结构高度相似的文件时，
`old_string` 必须只包住目标行，别带相邻行当锚点 —— 因此两次误伤（删掉相邻的
`ext_dict_status_installed`、往 `findViewById` 序列插入重复赋值），均靠随后的 `grep -c` 发现并修。
结论：这类文件改完必须 grep 验证关键 key 的存在性与唯一性。

累计：已修 56 项；已移除 4 源文件 / 5 权限 / 1 依赖 / 整张保活卡片。

---

# 方案按钮提示目标方案 + 符号菜单新增「编程」分组

- **方案按钮小字**：原来固定「换拼音」，看不出点下去会变成什么 → 按当前方案提示目标
  （双拼时「换全拼」、全拼时「换双拼」）。真机截图确认功能栏首键显示「全拼 / 换双拼」
- **符号菜单新增「编程」分组**（第 11 组，3 页）：键位映射值本就是 `String`（不限于单字符），
  关键字可整词上键
  - 1：英文符号 `( ) { } [ ] < > = !` / `; : ' " \` ~ & | ^ _` / `+ - * / %`
  - 2：常用关键字 `return print main if else for while class def func` / `import from const let var new public private void` / `true false null None this static async`
  - 3：类型与语句 `int float double string bool char long struct enum interface` / `try catch finally throw break continue switch case default` / `print() main() println printf scanf lambda yield`
- **键面长文本自适应（关键改动）**：原 `onDraw` 字号固定 `TEXT_RATIO = 0.4`，`return`（6 字符）、
  `private`（7 字符）会左右溢出 → label >2 字符即按长文本处理，`fitTextSize()` 按可用宽度等比收缩
  （下限 `MIN_LONG_TEXT_SIZE = 9sp`）并垂直居中（不再用小字顶置）
  - 真机：`if`（2 字符）走原样式字号较大；`return` / `import` / `private` 自动收缩且完整显示，
    同页不同长度关键字字号各异

累计：已修 59 项；符号分组 10 → 11 组；APK 6.2MB；单测 10 类 / 166 例。

---

# 符号键文案居中 + 编程分组扩至 7 页

- **符号键文案改「返回」**（新增 `key_back`）：进入符号层后左下角原显示「ABC」，该键此刻的作用就是
  回字母页 → 文案更直白；数字层的「ABC」保持不变
- **符号层键面一律水平 + 垂直居中**：新增 `PinyinKey.centeredStyle`
  （`centeredStyle = layer == LAYER_SYMBOL`），不再沿用字母层小字顶置 → 修掉短符号偏上
  （`if` 是 2 字符，原走顶置分支、视觉上不在键中央）。居中分支仍调 `fitTextSize()`，长关键字照旧收缩
- **分组顺序**：常用 → **编程** → 标点 → 序号 → 数学 → 单位 → 平假名 → 片假名 → 拉丁 → 特殊 → 注音
  （编程组从末位提到第 2 位，日常高频之后即可够到）
- **编程分组 3 页 → 7 页**：1 英文符号；2 通用关键字；3 类型与语句；4 Python（`self None elif lambda
  yield global assert raise with as pass del not and or is True False __init__ __name__` 与 `print( input(
  range( len( str( dict(`）；5 Java / C#（`System String Integer Boolean List Map HashMap ArrayList package
  extends implements abstract final override namespace using foreach params synchronized new get; set; await
  Task var public static`）；6 JS / TypeScript（`function => console.log document window export default
  interface type enum undefined NaN typeof instanceof Promise async await require module forEach map( filter(
  reduce( JSON. Object. Array.`）；7 C / C++（`#include #define stdio.h stdlib.h iostream cout cin endl std::
  vector template typename nullptr sizeof malloc free( printf( scanf( struct -> :: int main return 0 unsigned
  const static`）
  - 所有页面受字号自适应保护，最长的 `public static`（13 字符）也能完整显示

真机：符号键显示「返回」✅；分组顺序「常用 / 编程 / 标点…」✅；编程组标签 1/7 ✅；键面居中 ✅。

累计：已修 62 项；符号分组 11 组（编程组 7 页）；APK 6.2MB；单测 10 类 / 166 例。

---

# 符号分组重构 + 剪贴板搜索页退出按钮两行显示

- **剪贴板搜索页「退出搜索」改两行**：用户要求严格两行（`退出` / `搜索`），原单行在窄按钮里挤压 →
  `text = "退出\n搜索"`、字号 13f → 12f、`setLineSpacing(0f, 0.95f)`（避免 46dp 高度里两行贴边）
- **平假名 + 片假名合并为「日本」组**：同一语言两套字母合并，共 8 页（前 4 页平假名、后 4 页片假名），
  翻页在同一组内滑动
- **「特殊」组前移并扩到 6 页**：位置从末位移到「序号」之前；1–3 原内容（牌面 / 星形 / 音符 / 表情、
  天气 / 星座 / 杂项、常用 emoji）；4 箭头 `→ ← ↑ ↓ ⇒ ⇐ ⇑ ⇓ ↔ ↕ ➤ ➔ ↗ ↘ ↙ ↖ ⤴ ⤵ ↩ ↪ ⇄ ⇅ ⇆ ⇧ ⇩ ⏎`；
  5 货币与金融 `¥ $ € £ ₩ ₽ ¢ ₪ ₹ ₫ ₴ ₦ ₱ ﷼ ₨ ₡ ₭ ₮ ₲ ₵ ₸ ₺ ₼ ₾ ¤ ₿`；
  6 数理与几何 `≠ ≈ ≤ ≥ ± × ÷ ∞ ∵ ∴ ∠ ⊥ ∥ △ ▲ ▽ ▼ ◇ ◆ □ ■ ○ ● ◎ ⊙ ⌒`
- **最终顺序（10 组）**：常用 → 编程 → 标点 → 特殊 → 序号 → 数学 → 单位 → 日本 → 拉丁 → 注音
  （原 11 组，因平假名 + 片假名合并减 1）

真机：分组顺序「常用 / 编程 / 标点 / 特殊 / 序号 / 数学 / 单位 / 日…」✅；特殊组 1/6 ✅；
编程组 3/7 且键面全部居中 ✅；符号键「返回」✅。
未取到截图：退出搜索两行排版 —— adb 操作时候选栏状态残留（`input keyevent` 清空输入框后 IME 拼音串
未同步清除，功能面板不显示），非真实用户场景；改动本身简单明确，已编译验证。

累计：已修 66 项；符号分组 10 组（编程 7 页、特殊 6 页、日本 8 页）；APK 6.2MB；单测 10 类 / 166 例。

---

# 中英键两行富文本、序号组扩充、日本组补齐空位

- **中英切换键改上下两行富文本**：原单个「中」/「EN」随模式切换 → 同一控件显示两行「中文 / 英文」，
  当前生效那行染主题紫 `aurora_purple` 并加粗
  - 必须用 `SpannableString` 做部分着色。试过「中文/英文」单行斜杠写法，按钮仅约 132px 宽，13sp 会
    折成「中文/英」+「文」；改上下两行后字号放回 12sp、行距 0.9，在 144px 键高里排布均匀
  - 不能拆成两个 TextView：两段各自居中，视觉上像并排两个按钮而非一个切换键
- **序号组 4 页 → 7 页**：5 圈字母收尾 `Ⓥ Ⓦ Ⓧ Ⓨ Ⓩ` + 带括号数字 `⑴`–`⒇`；6 带括号小写字母 `⒜`–`⒵`；
  7 大数圈号 `㉑`–`㉟` + 中文大写数字 `壹 贰 叁 肆 伍 陆 柒 捌 玖 拾`
  - 顺带修数据错误：原第 2 页 `'z' to "Ⅷ"` 与 `'n' to "Ⅷ"` 重复、`'v' to "Ⅻ"` 与 `'m' to "Ⅻ"` 重复
    → 改为 `ⅰ ⅱ` 补位
- **日本组补齐末页空位**：合并平假名 / 片假名时两组各自第 4 页只有 10 键（q–p）、余 16 键空着 → 补为
  完整 26 键：拗音 `きゃ きゅ きょ しゃ しゅ しょ ちゃ ちゅ ちょ にゃ にゅ にょ ひゃ ひゅ ひょ みゃ
  みゅ みょ りゃ りゅ りょ ぎゃ ぎゅ ぎょ じゃ じゅ じょ びゃ びゅ びょ ぴゃ ぴゅ ぴょ`（片假名同构）+
  长音符与浊点 `ー ゛ ゜ ・ 〜 ゔ ゠ ゟ`（片假名用 `ヷ ヸ ヹ ヺ`）→ 日本组 8 页增至 10 页

真机：中英键两行「中文 / 英文」且「中文」紫色加粗 ✅；序号组 1/7 ✅；特殊组 1/6 ✅；
分组顺序「常用 / 编程 / 标点 / 特殊 / 序号 / 数学 / 单位 / 日…」✅。

累计：已修 70 项；符号分组 10 组（编程 7 页、特殊 6 页、序号 7 页、日本 10 页）；APK 6.2MB；
单测 10 类 / 166 例。

---

# build_apk.py 健壮性审查与修复

审查构建脚本，修 9 处，其中 1 处实质性缺陷。

- **缺 zipalign（实质性）**：脚本用 Python `zipfile` 重打包去 `META-INF`（`remove_meta_inf`）会打乱 AGP
  产物的 4 字节对齐，而 apksigner 不负责对齐 → 之前产物的对齐状态只是巧合。未对齐会让设备读取 APK 内
  资源时先解压，影响启动速度与内存占用（尤其 `resources.arsc` 与大量 `res/*.xml`）。流程固定为
  `AGP 产物 → remove_meta_inf → zipalign -p -f 4 → apksigner sign`，并补事后复核
  （`apksigner verify` 查签名方案 + `zipalign -c 4` 查对齐）
- **其余 8 处**：① `.idsig`（v4 签名产物）未清理 → 加入清理列表；② docstring 的 `<密钥目录>` 是无效
  转义，Python 3.12+ 报 `SyntaxWarning` → 改 raw 字符串；③ `find_build_tool("apksigner.bat")` 硬编码
  Windows 扩展名 → 按 `os.name` 选后缀；④ 签名方案校验用精确英文串匹配 → 改正则
  `v2\s+scheme[^:]*:\s*true`（避免 build-tools 升级后措辞变化误报）；⑤ 临时文件只在成功路径清理 →
  改 `try/finally`；⑥ `remove_meta_inf` 用 `src.read()` 整块读入内存（词库包数 MB）→ 改
  `shutil.copyfileobj` 流式（256KB 块）并保留原 `compress_type`；⑦ `verify` 用 `shell=True` 拼路径 →
  改列表传参；⑧ 密钥找不到只报路径 → 补 `set JINN_KEYSTORE_ROOT=...` 示例

验证：`python -W error::SyntaxWarning -m py_compile` 无警告 ✅；`--help` 正常 ✅；端到端实跑 3 次全通过 ✅；
签名 `V1=false V2=true V3=true` ✅；`zipalign -c 4` 通过（脚本内 + 外部复核各一次）✅；
产物目录只剩 `app-release.apk` 与 AGP 原生产物 ✅。

累计：已修 79 项；APK 6.2MB（对齐状态有保证，不再依赖巧合）。

---

# 回车键输出「原始按键」英文（双拼不做转换）

- **纠正前提**：需求说「全拼输入 but 后回车输出英文 but」，实测并非如此 —— 双拼模式输入 `but` 后回车，
  候选栏显示「不他 / 不太 / 步态」，回车后拼音串 `but` 原样留着，既不提交候选也不输出英文。原因是
  `performEnter()` 当时只做「有 action 则 `performEditorAction`、否则 `sendDownUpKeyEvents(ENTER)`」，
  完全没碰拼音串 → **该能力此前不存在，本次是新实现**
- **实现**：新增 `PinyinKeyboardView.takeRawComposing()` 取出用户实际按下的键（`composing` 存的就是原始
  按键、不做双拼转换），取出后清空 `composing` 与 `lastCandidates` / `lastPredictions`（避免下一轮
  串味）；`JinnIme.performEnter()` 在拼音串非空时 `commitText(rawComposing, 1)` 并 return
  （不再执行输入框动作 / 换行），为空时保持原逻辑
  - 关键：`composing` 本就是原始按键序列，无需转换 → 双拼按 `budv` 输出 `budv`，不会变成 `budui`

真机（PACM00，双拼）：按 `udv` → 候选「双闸 / 双寨 / 双栈」，回车输出 `udv` ✅；按 `vbudv` → 候选
「周 / 州 / 皱」，回车输出 `vbudv` ✅。两处日志均记「回车输出英文原文: xxx」，确认输出按键原文而非
候选或转换后拼音；回车后候选栏回到功能面板（输入状态已正确清空）。

累计：已修 81 项；APK 6.2MB。

---

# 深度缺陷扫描（Lint + 高危模式排查）

用 Android Lint 系统扫描 + 对六类高危模式（API 版本 / 并发 / 资源泄漏 / 异常处理 / 索引边界 / 性能）
定向排查。

**修的 3 处**

| # | 位置 | 缺陷 | 级别 | 修法 |
|---|---|---|---|---|
| 1 | `ClipboardHistoryActivity` | 手写 `if (SDK_INT >= TIRAMISU)` 注册广播，Lint 判「未声明导出标志」Error | P1 | 改 `ContextCompat.registerReceiver`（androidx 按版本自动适配） |
| 2 | `JinnIme:251` | `requestShowSelf()` 是 API 28 方法而 minSdk = 26，低版本抛 `NoSuchMethodError` | P1 | 加 `SDK_INT >= P` 判断；空 `onFailure {}` 补日志 |
| 3 | `JinnIme:966` | `if (SDK_INT < N) return` 在 minSdk 26 下恒为 false（`ObsoleteSdkInt`） | P3 | 删冗余判断 |

- ① 原版本分支逻辑本身正确（API 33+ 传 flag、以下不传）不会崩溃，但 Lint 仍报 Error —— 手写分支
  无法静态证明完备、日后改动易漏 → `ContextCompat` 是正解：既消告警又把版本适配交给 androidx
- ② `runCatching` 兜住异常所以不崩溃，但功能在 Android 8.0/8.1 上静默失效，且空 `onFailure {}`
  违反项目「绝不静默吞异常」规则 → 两处都补

**确认无问题（排查过、留证）**：`!!` 强制解包全项目 0 处；`ClipboardDb` 六处 `rawQuery` 全部 `use {}`
关闭；广播注册 / 注销三处全部配对；`PinyinEngine` 用 `@Volatile` + `synchronized` 保护共享词库；
`candidates[0]` 有 `isNotEmpty()` 守卫、`EXT_DICT_URLS.first()` 是常量非空列表；仅 1 处空 catch
（`BackgroundIo` 的 `RejectedExecutionException`），有意设计且有注释。

**登记未修**

- ① `MicButton.kt:98`（P2，属语音部分受禁改保护）：每次 `onDraw` 新建 `RadialGradient`，重绘频繁时
  持续 GC 压力（`DrawAllocation`）。建议用 `Matrix` + `setLocalMatrix()` 移动已有 shader，或在
  `onSizeChanged` 缓存 shader —— 不动语音代码
- ② `onTouch` 未调 `performClick`（`JinnIme:495/1078`、`PinyinKeyboardView:726/896`）：影响 TalkBack。
  标准修法是判为点击时补 `performClick()`，但键盘手势判定敏感、改动有触碰长按 / 滑动手势风险，待确认
- ③ 49 处 `UnusedResources`：纯体积优化，无功能影响

验证：Lint 2 error → **0 error**（88 warnings 保留）；单测 10 类 / 166 例；APK 6.2MB、
签名 V1=false / V2=V3=true；4 字节对齐通过；真机安装 Success（PACM00）。

累计：已修 84 项；Lint 0 error。

---

# 无障碍 performClick、语音入口连通性、保活文案清理

- **无障碍：onTouch 补 performClick（Lint 5 → 2）**：Lint `ClickableViewAccessibility` 要求自定义触摸
  处理在识别到点击时通知无障碍服务。原则：只在确认是「点击」时补，且不改动原手势返回值
  - `PinyinKeyboardView` 字母键：仅 `ACTION_UP` 且水平位移 < `touchSlop` 时补（该键有「符号层左右滑动
    翻页」手势，滑动结束时发点击事件会误导 TalkBack）
  - `PinyinKeyboardView` 退格键：`ACTION_UP` 时补（只有点击 / 长按连删，无滑动）
  - `JinnIme` 退格键：`ACTION_UP` 时补（`CANCEL` 不补）
  - `JinnIme` 麦克风按钮：`ACTION_UP` 时补（该按钮无 OnClickListener，`performClick()` 只向无障碍服务
    补发一次事件，不影响手势判定与录音）
  - 保留 1 项：Lint 另要求 `MicButton` 自身重写 `performClick()`，属语音部分受禁改保护，未动
- **语音入口增加服务连通性判断（核心）**：`JinnIme.switchToVoiceKeyboard()` 在
  `asr.state != LinkState.ONLINE` 时记日志 + Toast `voice_offline_blocked` 并 return（不切换键盘模式）。
  拦在入口的价值：否则用户进了语音面板才发现连不上，会白等一次 WebSocket 握手并连带触发录音权限检查、
  录音器初始化等整条链路负担
  - 新增文案「语音服务未连接（%1$s:%2$d），暂不能使用语音输入」。属入口控制，未改 `AsrClient` /
    录音链路，符合禁改约束
- **清理保活遗留文案 19 条**：`keepalive_*`、`accessibility_*`、`settings_keepalive*`、
  `switch_keepalive / root / notify_high`、`btn_accessibility / battery / root`、`battery_open`、
  `root_done / fail / need`
  - 其余 30 项 `UnusedResources` 不动：已启用 `isShrinkResources = true`，release 构建已自动裁掉
    （APK 里本就没有），清源码定义不减小 APK；且存在被动态引用（如 `getIdentifier`）的可能。
    收益 0、风险不为 0

验证：Lint 0 error / 88 → 64 warnings（`ClickableViewAccessibility` 5 → 2）；单测 10 类 / 166 例；
APK 6.2MB、签名与对齐均通过；真机 —— 字母键 `qie` → 且 / 切 / 窃（点击未被 performClick 破坏）、
符号层滑动翻页未被误判为点击、长按空格在服务在线时正常进入语音键盘未被误拦。

累计：已修 90 项；Lint 0 error / 64 warnings。

---

# Binder 超限崩溃修复 + 词库压缩调优（APK 6.2 → 6.1MB）

- **缺陷：粘贴广播携带全文 → `TransactionTooLargeException` 崩溃**
  （`ClipboardHistoryActivity.requestPaste()` 把 `item.content` 整条正文塞进广播）
  - 必然出事：广播经 Binder 传递、事务上限约 1MB，而剪贴板单条正文无长度上限
    （`ClipboardPrefs.maxItems` 只限条数）→ 复制长文章 / 日志 / 大段代码后点「粘贴」即崩溃。
    这正是「文本类应用 + Android 14+」的典型闪退源
  - 修法：广播只传 id，正文由接收方按 id 读库 —— `ClipboardDb` 新增 `contentById(id)`（含读库 +
    AES 解密，必须走 `BackgroundIo`）；`ClipboardHistoryActivity` 只带
    `EXTRA_CLIPBOARD_PASTE_ITEM_ID`；`JinnIme` 接收器按 id 读库后回主线程 `pasteClipboardText`；
    删除已无引用的 `EXTRA_CLIPBOARD_PASTE_TEXT`
  - 三处剪贴板入口核对（隐私 / 安全改动必须同步）：只有 `ClipboardHistoryActivity` 走广播；
    `ClipboardPanelView`（键盘内嵌）与 `SearchPanelView`（顶部搜索）是直接回调、不经过 Binder
  - 注：崩溃需 >1MB 剪贴板内容才能复现，无法用 adb 便捷构造 → 只有代码逻辑与编译保证，无真机复现证据
- **APK 体积分析**：`pinyin_phrases.txt.xz` 5.74MB 占 93.4%，其余（`classes.dex` 0.26MB、
  `pinyin_chars.txt` 0.04MB、okhttp `publicsuffixes.gz` 0.04MB）合计 <7% → **APK 就是词库**，
  其他已优化到位（R8 + `shrinkResources` 生效）
  - 试过的方案：xz `preset=7 + lc=4 + pb=0` 5.743 → 5.652MB（−1.59%，解压字典 8→16MiB）✅ 采用；
    xz `9e`（64MiB 字典）再省 0.14% 但要 +48MiB 解压内存 ❌；brotli `-q11 -w24` 5.653MB ❌
    （Android 无内置解码，引库反增体积）；二进制词库格式预估 −5~10% ❌（工程量大、风险高）；
    删低频词 / 更多词移到扩展包可达 −10~40% ❌（有损、牺牲输入体验，需用户决定）；
    扩展包同步重压只省 5KB ❌（要重传两个仓库附件）
  - 关键洞察：词库是「用体积换输入体验」的资产，无损压缩已到极限（再挤只剩 1.5%）；
    要显著变小只能砍词，那是体验取舍，不该替用户决定

**验证**：重压基础包并逐字节解压校验（无损）✓；`PhraseDictIntegrityTest`（直接校验 asset 词库）通过 ✓；
真机词库加载 8550ms（优化前 9514ms，反而快约 1 秒）、输入 `qie` → 且 / 切 / 窃 正常 ✓；
APK 6.2 → 6.1MB，签名与对齐通过。

累计：已修 92 项；APK 6.1MB；Lint 0 error；单测 166 例全绿。

---

# 词库整体替换为 rime-ice（APK 6.1 → 4.5MB）

- **为什么换**：原词库三个结构性问题 —— 无词频字段（候选无法按常用度排序、"低频"无从定义）、
  16.2% 的行有 GBK 误解码残留（`瀵勮繙` / `浣犲ソ` 永远打不出来，纯废条目）、连写歧义词需手工维护
  （企鹅 / 西安 / 饥饿靠人肉补，易漏）。rime-ice（雾凇拼音）三个都解决：100% 带词频、已校对无损坏、
  原生含歧义词
- **转换**：新脚本 `tools/dict_builder/convert_rime_ice.py`（可重跑，纯读取 + 统计，不碰 docs）——
  读 `docs/rime-ice/cn_dicts/{base,ext}.dict.yaml`，解析 `词<TAB>拼音(空格分隔无调)<TAB>词频`，
  拼音去空格即本项目的键，按词频降序聚合输出 `键<TAB>词1|词2|…`，再按 ≤4 字 / >4 字切分为基础包与扩展包

| 指标 | 原词库 | rime-ice | 变化 |
|---|---|---|---|
| APK | 6.1MB | 4.5MB | −26% |
| 基础包 xz | 5.65MB | 4.10MB | −27.4% |
| 扩展包 xz | 2.19MB | 1.80MB | −17.8% |
| 词库加载 | 8550ms | 6602ms | −1.9s |
| 拼音键 | 826,897 | 787,991 | −4.7% |
| 词条 | 1,006,326 | 882,180 | −12.3%（多为损坏词） |

真机：`qie` → [企鹅, 且, 切]、`xian` → [西安, 西岸, 锡安]、`jie` → [饥饿, 极恶, 极饿] —— 原先手工维护的
歧义词 rime-ice 全都原生支持，且靠词频自然排在首位。

---

# 清洗游戏词库并合并进扩展包

- **来源**：`docs/games_ime_dict.txt`（1361 条）+ `docs/klbq_ime_dict.txt`（329 条），格式与 rime-ice
  一致（`词 拼音(空格分隔无调)`）；docs/ 只读
- **清洗：删 145 条垃圾词，保留 1545 条**：含数字或字母 139（`CS2` `CSGO` `DOTA2` `DNF` `刀塔2`
  `奥卡努斯2` `2号仓库2`）、超长 >8 字 5（`前面的区域以后再来探索吧`、`行踪不定,暂无固定的居所`，是句子）、
  含标点 1（`千代(ちよ)`）；保留纯中文，允许间隔号 `·`（`奥黛丽·格罗夫`）
  - 字母数字必须删：拼音引擎按合法音节切分，字母 / 数字切不开（单字母合法音节只有 `a e m n o`），
    当键永远打不出来，与网络词 `yyds` 同理
- **合并（merge 模式）**：扩展包 183,919 → 185,408 键（新增 1,489 键、追加 1,506 词）；词条总数
  186,073；体积 1.80 → 1.81MB。同键已有词保留在前、游戏词追加在后，不覆盖基础候选

验证：词条入库（解压校验）—— `阿拉德/alade`、`崩坏星穹铁道/benghuaixingqiongtiedao`、
`艾泽拉斯/aizelasi`、`奥黛丽·格罗夫/aodailigeluofu`、`艾卡/aika`、`白墨/baimo` 全部在位；
真机日志确认「扩展词库: 已加载（长词可用）」；Release 附件同步（关键，否则用户下载到旧包）：
GitHub 覆盖上传 1,901,048 字节 ✅、Gitee 删旧附件（id 3201963）→ 传新（id 3204221）1,901,048 字节 ✅，
两源实测 `Content-Length` 均 1,901,048 ✅。
方法记录：Gitee 的 `releases/tags/{tag}` 返回的 assets 没有 id，必须用
`releases/{release_id}/attach_files` 才能拿到附件 id 来删除。

**新增工具**：`convert_rime_ice.py`（rime-ice 转换 + 对比报告）、`merge_game_dicts.py`
（游戏词库清洗 + 合并，支持 `--dry-run`）、`词库重构-提示词与公开词库参考.md`（格式规范、拼音约束、
网络词收集提示词）。

累计：已修 96 项；APK 4.5MB（自 10.6MB 累计 −58%）；Lint 0 error；单测 166 例全绿。

---

# 深挖词库 BUG：修复扩展包合并产生重复候选

- **缺陷**：`PinyinEngine.loadPhrasesReader()` 的 merge 分支是
  `if (existing != null) existing + kept else kept.toTypedArray()`，直接拼接不去重 → 扩展包与基础包
  收录同一个词时候选栏出现两个完全相同的候选：`aersasi` [阿尔萨斯, 阿尔萨斯]、
  `aizelasi` [艾泽拉斯, 艾泽拉斯]、`airen` [爱人, 矮人, 矮人]、`aixi` [爱惜, 艾希, 艾希]。
  影响面实测：877 个词条会产生重复候选，其中 404 个键完全重复
- **根因两层，代码层才是根治**
  - 数据层：合并游戏词时只查扩展包、没查基础包 → `merge_game_dicts.py` 合并前载入基础包全部词并排除
    （本次跳过 415 条）
  - 代码层：rime-ice 自身的 base/ext 就有重叠（如 `aersasi` 两边都收）→ merge 时先建「已见」集合，
    扩展包只追加未出现过的词
  - 关键：数据层只能治「自己引入的重复」，第三方词库自带的重叠必须靠代码层兜住 —— 数据修完仍剩
    613 个重叠键 / 467 个重复词条，全部由代码层消除
- **修复后**：`aersasi` [阿尔萨斯]、`aizelasi` [艾泽拉斯]、`airen` [爱人, 矮人]、`aixi` [爱惜, 艾希]。
  去重开销只落在真有重叠的键上（现 613 个），对 60 万键的加载耗时可忽略

**顺带排查（确认无问题，留证）**：键能否被合法音节切分 —— 基础包 604,077 键 + 扩展包 185,408 键、
切分失败 0 ✅；常用词覆盖抽查 20/20 命中 ✅；单字表不需替换（`pinyin_chars.txt` 17,117 字符完全覆盖
rime-ice 的 8,182 且更全）✅；单字表编码损坏仅 1 处（`覅`），属检测方法误报 ✅。
另注：日志里 `音节=414`（非合法音节表的 422）不是 BUG —— 414 是单字表实际覆盖到的音节数，
缺的 8 个（`biang` / `fiao` / `junding` 等）本就没有对应单字。

**验证与同步**：`lintDebug` + `testDebugUnitTest` 通过；APK 4.5MB、签名与对齐通过；真机安装 Success；
扩展包已变更 → Release 附件同步（GitHub `--clobber` + Gitee 删旧 id 3204221 → 传新 id 3204300），
两源实测 `Content-Length` 均 1,897,940 ✓。

累计：已修 98 项；APK 4.5MB。

---

# 可选词库延迟加载 + 分类词库页（词库链路专项）

背景：基础包换 rime-ice 后，可选包（长词包 1.8MB / 腾讯大词库 6.4MB）与基础包在 `onCreate` 一起加载，
使「开机后首次输入的候选就绪」从 6.6s 拖到 25.4s。

- **可选词库改延迟加载**：`PinyinEngine.load()` 只加载基础包；新增 `loadOptionalAsync(delayMs = 5000)`
  在基础包就绪 5 秒后于后台补齐可选包。IME 加载时机实测：弹 / 收键盘走 `onStartInputView` /
  `onFinishInputView`，不碰词库 → 加载耗时只影响「IME 服务重建后的首次输入」，日常打字无影响
  - 实测：基础包 6.4s 就绪（与不装可选包一致），可选包 26s 后后台就绪（172 万键）
  - 关键细节：可选包加载后必须重新 `finalizeLoad()` —— `sortedPhraseKeys` 是加载时快照，
    不重建则新词不参与前缀补全
- **新增分类词库页**：`OptionalDicts.kt`（词库清单：文件名 / 说明 / 体积 / 启动耗时 / 下载源，新增词库
  只需加一条记录）+ `DictManagerActivity.kt`（列表 + 下载 / 删除 + 顶栏关闭；视觉沿用剪贴板页配色：
  背景 `#0B1020`、顶栏 `#141C33`、卡片 `#1C1F26`、强调 `#4C8DFF`）；设置页旧扩展词库卡片
  （URL 输入框 + 下载 / 删除按钮）换成单按钮「打开分类词库」
- **词库容器线程安全化（并发缺陷修复）**：延迟加载在后台线程 merge、而用户此刻正在打字、主线程
  `query()` 并发读同一批容器。原实现全是普通 `HashMap` / `HashSet` / `ArrayList`：扩容可能让桶链表成环
  （读方死循环、IME 卡死）或抛 `ConcurrentModificationException`；有序表用 `clear()+addAll()` 会让读方
  看到中间态 → Map 改 `ConcurrentHashMap`（容量在声明处预分配）、`validSyllables` / `syllablePrefixes`
  改 `ConcurrentHashMap.newKeySet()`、三个有序表改 `@Volatile` 不可变快照 + 整体替换引用、
  `loadOptionalAsync` 的「检查-置位」纳入同一把锁。真机：基础包就绪后立刻连续打字（触发原竞态窗口），
  全程 0 异常
- **其余修复**：候选可达 60 条且每次按键全量重建 View（`yi` 有 326 字、93 个音节超 `MAX_CHARS(60)`）
  → 截断渲染为 24 条（上屏逻辑不受影响，`lastCandidates` 仍存完整列表）；加载失败静默无痕（裸线程 +
  `load()` 无 try/catch，异常穿透后线程死亡）→ `JinnIme` 侧 `runCatching` + `Diagnostics.e(msg, tr)`
  保留堆栈；下载失败用户看不到提示（回调写进已重建 Activity 的旧 View）→ 额外弹 Toast；
  merge 去重无测试守护（测试入口 `loadFromTexts` 固定 `merge=false`、分支不可达）→ 补
  `loadPhrasesText(merge)` + `mergePhrasesForTest`，新增 `OptionalDictMergeTest` 7 用例

验证：`compileDebugKotlin` + `testDebugUnitTest`（183 用例 / 14 类）+ `lintDebug` 全通过；APK 4.5MB、
对齐与签名通过；真机 —— 基础包 6409ms 就绪、可选词库后台就绪、候选栏输入 `yi` 命中
「易 / 翼 / 一 / 以 / 已…」、0 崩溃。

---

# 剪贴板模块专项（死代码清理 + 语义修正 + 状态同步）

- **删除已下线的剪贴板历史页**：实跑发现 `ClipboardHistoryActivity`（719 行完整实现）不可达
  （`exported=false` + 全项目无 `startActivity` 调用点），确认有意下线后清理 —— 删
  `ClipboardHistoryActivity.kt` 与 Manifest 声明、删 22 个历史页专用字符串资源（保留 `clipboard_root_*`
  等设置页仍在用的）；`AGENTS.md` 的「三处入口」修正为「两处入口」并删 4 条描述 ListView /
  Activity 生命周期 / FLAG_SECURE 的失效约束。现状：剪贴板只有 `ClipboardPanelView`（键盘内嵌）+
  `SearchPanelView`（顶部搜索）两个入口，改动需两处同步
- **容量裁剪改三级优先级**：原 `trimTo` 只豁免收藏（`WHERE is_favorite = 0`），隐私记录会随「最旧非收藏」
  被删除；虽代码 / 注释 / 规则三者一致（属有意设计），但隐私是用户刻意主动标记的、语义上更该保留 →
  新增 `TRIM_PRIORITY` 按顺序分轮取证、凑够即停：① `is_favorite = 0 AND is_private = 0`（普通）→
  ② `is_favorite = 0 AND is_private = 1`（隐私，最后才动）→ ③ 收藏永不参与裁剪。每轮只取「还需删的
  条数」，普通记录够删时完全不触碰隐私，也不会因「隐私太多」而无法裁剪
- **修复拖选状态未同步到键盘侧**：`selectionActive` 在 `JinnIme` 与 `PinyinKeyboardView` 各存一份、
  靠显式调用同步；两处「复位」逻辑分别实现，其中 `onClipboardStateChanged` 漏了同步调用
  （`onSelectionModeChanged` 复位后有 `setSelectionActive(active)`，它没有）。根因是同一语义的复位逻辑
  在两处重复实现、各自演化 —— 典型的复制粘贴式重复导致的不一致，已让两处对齐
- **布局数据抽离（顺带架构改进）**：`PinyinKeyboardView` 内嵌 455 行静态配置数据（QWERTY 行定义 + 数字层
  映射 + 符号分组含日文假名表），与视图逻辑混杂 → 抽出为 `KeyboardLayouts.kt`，视图类 2029 → 1574 行。
  验收用逐项比对：提取全部 `'x' to "Y"` 键值对，旧 1263 / 新 1263 序列完全一致 —— 编译与常规测试都
  发现不了数据丢失 / 串位，只有用户按到那个键才会暴露

验证：`compileDebugKotlin` + `testDebugUnitTest`（183 用例）+ `lintDebug` 全通过；真机 —— 设置页剪贴板
卡片与「历史数量上限」正常渲染、键盘面板可开、符号层正常、0 崩溃；新增
`ClipboardClassifierBoundaryTest`（6 用例：空白包裹、长数字 + 换行含 CRLF、长度边界、含数字负例、
空白输入、URL 优先级）。

累计：本次净减代码约 1800 行（含删 719 行死代码、抽离 455 行数据）；测试用例 166 → 183（+17）。

---

# 26 键区圆角与按键间隙改为设置页可调

日期 2026-09-16 ｜ 验证：`testDebugUnitTest`（184 用例）+ `lintDebug`（0 error）+ 真机

- **需求与隐藏不一致**：要求把 26 键区（布局上整整三行、共 28 键：10 字母 + 9 字母 +「大写 + 7 字母 +
  删除」）的按键圆角与左右 / 上下间隙做成设置页可调、定义域合理。现状是这 28 键视觉连成一片但绘制路径
  两条：26 个字母键走 `PinyinKey.onDraw` 自绘（`KEY_CORNER_DP = 0f` / `KEY_MARGIN_DP = 0f` 硬编码），
  大写 / 删除键走 XML `ImageButton` + `key_bg.xml`（圆角 10dp 且贴满单元格）→ 同一行两种圆角、且大写 /
  删除键比字母键高一圈。做成「统一参数」正好一并修掉
- **实现**
  1. 新增 `KeyAppearance.kt`（纯 JVM 可单测），定义域 / 默认值 / 进度换算收敛一处：圆角 0~24dp（步进 1dp，
     0 为直角，>24 会削成胶囊失去按键辨识度）；间隙 0~8dp（步进 0.5dp，0 为无缝，8dp 时最窄字母键仍剩
     23dp 可点）；默认 0 / 0dp 与加入参数前外观完全一致（增量修改，不偷偷改观感）；NaN / Infinity 一律
     退回默认值 —— `coerceIn` 对 NaN 的比较恒为 false，不拦住会一路传到 `drawRoundRect`（整个键面画不出来）
  2. `Prefs` 新增 `keyCornerDp` / `keyGapDp`，读写都钳位（历史配置或外部写入可能越界）
  3. `PinyinKey` 删两个硬编码常量，改 `setKeyAppearance(cornerPx, insetPx)`：四边等量内缩（上下也缩，
     原来只有左右缩 + 顶部固定 8px），并加「内缩不超过键尺寸一半」保护
  4. `PinyinKeyboardView.applyKeyAppearance()` 把同一组参数推给三类键：字母键 → `setKeyAppearance`；
     大写 / 删除键 → `GradientDrawable` + `RippleDrawable` 动态重建同圆角背景（保留按压水波纹，mask 必须
     单独造不透明同圆角矩形否则波纹被裁没），并把内缩等效成四周 `LayoutParams` 外边距（否则普通 View
     贴满单元格、与字母键错位）；大写锁定切换改走 `refreshShiftBackground()`（原来直接
     `setBackgroundResource`，圆角写死）
  5. 生效时机：挂在 `init` 与 `configure()`，而 `configure()` 由 `onStartInputView` 每次聚焦时调用 →
     设置页改完收起键盘再弹出即生效，无需重启进程
  6. 设置页新增「键盘外观」卡片（两个 SeekBar + 当前值 + 生效说明），拖动即时落盘
- **间隙语义（写进 KDoc 避免误解）**：「间隙」= 相邻两键之间的空隙 —— 每个键四边各内缩 `gap / 2`，
  相邻两键相加正好等于 gap，左右与上下一致；不是「每边留多少」（旧常量 `KEY_MARGIN_DP` 是每边内缩
  语义，已随常量删除）

验证：184 用例全绿（新增 `KeyAppearanceTest` 11 例：钳位 / NaN / 步进 / 全进度往返 / 定义域 /
上界可点性）；lint 0 error / 34 warning（既有基线）；真机 PACM00 —— 设置页卡片渲染正常、滑杆数值联动
（拖到 24dp / 4dp 标签同步），收起再弹出后 28 键（含大写 / 删除）圆角与间隙同步生效、行高完全对齐，
日志侧证 `PinyinKeyboard: 键盘外观: 圆角=24 dp 间隙=4 dp`，复位 0 / 0dp 后恢复「连成一片的直角键区」。
勘误：用例数此前记的 183 与实际不符，实测本次前 173、本次后 184（以实测为准）。

**明确未纳入**：第四行（符号 / 数字 / 逗号 / 空格 / 中英文 / 回车）仍用 `key_bg.xml` 的 10dp 圆角、
不跟随本参数 —— 需求只圈定 28 键的三行区域；要整块统一再加一次即可。

**发布（覆盖式）**：提交 `08a9f31`（10 文件，+678 / −15）快进推送三处引用（GitHub `main`、
GitHub `perf/paging-pinyin-rematch`、Gitee `master`，均 `d730404` → `08a9f31`）；APK 重构建
`versionCode=20260916` / `versionName=jinn`，4,737,805 字节，V1=false / V2=V3=true、对齐通过；
两仓库 `v20260916` Release 的 `jinn-release.apk` 覆盖（GitHub `gh release upload --clobber`、
Gitee 取附件 id → DELETE 旧 → POST 新）；正文各补一条本功能说明（变更 + 验证各一行），其余逐字保持；
校验两个下载 URL 均 HTTP 200、`size_download=4,737,805`、sha256 与本地一致。

---

# 独立复核后的 6 项修复（死代码 / 回显 / 自动填充 / 颜色收敛 / 绘制分配）

日期 2026-09-16 ｜ 验证：`testDebugUnitTest`（184 用例）+ `lintDebug`（0 error）+ 真机逐项实测

- **来源与范围**：独立复核（`docs/代码审查-20260916-独立复核.md`）十七项候选里只采纳 6 项，其余九项取证后
  否定（外部清单裁定见 `docs/chatGLM建20260916232359.md` 对照）。采纳：死代码两处、设置页回显、自动填充、
  颜色收敛、绘制分配与单位。**明确不做**：第四行（符号 / 数字 / 逗号 / 空格 / 中英文 / 回车）圆角不跟随
  28 键参数 —— 用户要求保持现状，此后不得再动
- **修复明细**
  1. 死代码 `onBackspacePressed()`（`PinyinKeyboardView`）：全文件只出现一次；删除键真实路径是
     `setOnTouchListener → handleBackspaceTouch → deleteOne()`，且 `deleteOne()` 是它的严格超集
     （多一个预测态分支 + V 日志）。删 19 行 —— 它就是外部审查被带偏的源头，对方把这段死代码当活代码逐行分析
  2. 死代码 `formatSize()`（`SettingsActivity`）：本文件内无调用；`DictManagerActivity` 有独立副本且在用。删 6 行
  3. 剪贴板上限越界后界面不回显真值：输入 `0` / `99999` 会被 `Prefs` 钳到 `1` / `9999`，但原实现只在解析
     失败时才回写输入框 → 界面显示与生效值不一致。改为存完统一回写生效值（仅在显示不同时 `setText`）
  4. 设置页 5 个 `EditText` 关闭自动填充（`android:importantForAutofill="no"`）：避免密码管理器把
     host / port / 提示词 / 上限当账号字段弹「保存密码」。Lint `Autofill` 5 条清零
  5. 硬编码颜色收敛：`ClipboardPanelView` / `SearchPanelView` / `DictManagerActivity` 共 33 处
     `Color.parseColor` → `R.color.*`；`colors.xml` 新增 `danger` / `ok` / `warn` / `btn_disabled` 四个色值
     （其余复用既有同名同值项：`app_bg` / `card_bg` / `surface_hi` / `text_primary` / `text_secondary` /
     `accent` / `text_on_accent`），逐值等价、零行为变化；`DictManagerActivity` 里 9 个 `COLOR_*` 常量位于
     `companion object`（取不到 Context），整块删除、调用点改用 `getColor(R.color.x)`
  6. `PinyinKey` 绘制分配与字号下限单位：`subLabel` / `subLabelRed` 的 `split + filter + trim` 从 `onDraw`
     移到 setter 缓存（绘制路径零分配，行为不变：空行丢弃、去首尾空白）；`MIN_LONG_TEXT_SIZE = 9f`（像素）→
     `MIN_LONG_TEXT_RATIO = 0.06f`（占键高比例）—— 旧值在 mdpi 上等于 9dp，比按宽度算出的适配字号还大，
     会把 `synchronized` 这类长标签顶出按键左右边界；改比例后任何密度都自洽，本机 2.75x 上等效 ≈ 8.9px，
     与旧观感一致

**真机逐项实测**：上限回显 —— 输入 `0` → 失焦后日志 `剪贴板历史数量上限: 1`、输入框回显 1（改前恒显示
`0`），测完已把用户原值 `9999` 复原并复核 ✅；删除键（死代码删除后）—— 单击产生 `退格删拼音: b`、按住
1.5s 连续删除生效、无崩溃 ✅；双拼键面多行提示 `uai/ing`、`ong/iong`、`iang/uang` 正常渲染 ✅；符号层编程
分组长文本按宽度自适应、无溢出无裁切 ✅；颜色与改前一致 ✅；28 键区仍为默认 0dp / 0dp 连片直角块 ✅。

门禁：184 用例全绿；lint 0 error / 34 → 29 warning（Autofill 5 条清零）。用例数不变 —— 本次无新增可测纯逻辑。

**发布（覆盖式）**：提交 `798b874`（9 文件，+157 / −91）快进推送三处引用（GitHub `main`、
`perf/paging-pinyin-rematch`、Gitee `master`，均 `68e671c` → `798b874`）；APK `versionCode=20260916` /
`versionName=jinn`，4,737,805 字节，`sha256=f45bf09d…`，V1=false / V2=V3=true、对齐通过；两仓库
`v20260916` Release 覆盖（GitHub `--clobber`；Gitee 删旧附件 id `3212831` → 重传新 id `3212991`）；
正文各补本次条目（变更 + 验证各一条），其余逐字保留；两支下载 URL 均 HTTP 200、
`size_download=4,737,805`、sha256 与本地一致。
过程记录：GitHub 侧当晚多次瞬时抖动（`git ls-remote` / `api.github.com` 报 TLS unexpected eof），
push 与 upload 重试一次即成功，非权限问题。

---

# 删除废弃词库源文件 + 文档全量对齐

日期 2026-09-17 ｜ 无代码变更，不重编 APK（`tools/` 与 `docs/` 不参与构建）

- **删除 `tools/dict_builder/source/pinyin_phrases.txt`**（顺带移除删空后的 `source/` 目录，回收约 28MB）。
  删除前指纹留档：29,370,289 字节 / 1,048,342 行，sha256
  `b3519a87176b22e45101fa418a9c859576834423e3efc7f77bd6c15a64500129`；该文件被 `.gitignore` 忽略、
  从未入库，删后不可恢复
  - 删除前三项核实：① 构建链（`build.gradle.kts` / `app/build.gradle.kts` / `build_apk.py` /
    `settings.gradle.kts`）与现行词库脚本（`convert_rime_ice.py`、`convert_rime_tencent.py`）均不读它；
    ② 全仓已无 `source/pinyin_phrases` 硬编码引用；③ 现行资产完好（`assets/pinyin_phrases.txt.xz`、
    `release/dict_ext.txt.xz`、`release/opt_tencent.txt.xz`）
  - 删除理由（每条独立成立）：第 28 轮起词库整体换成 rime-ice、流水线改读 `docs/rime-ice/`；文件含 573 行
    GBK 乱码（如 `鍙樺姩涓嶅眳` 应为「变动不居」、`b煤d脿o` 应为 `bùdào`，连拼音键都坏了）；留着会让人
    误以为「改词库＝改这个文件」
- **文档对齐到最新版**：`tools/dict_builder/README.md` 新增现行流水线图（源 → 脚本 → 资产 → 发布要求）、
  脚本一览（在用 / 备用 / 评估 / 历史）、已删文件说明与指纹；`AGENTS.md` 新增「词库来源与生成（重要）」
  （现行源 `docs/rime-ice/`、改词库必做三件事、旧目录不得再放源文件）；`docs/DATA_SOURCES.md`
  「当前状态」按实测重写（基础包 604,077 行 / 4.3MB、扩展包 185,005 行、可选包 955,032 行、单字表
  422 键、音节表 422 行、常用字 6,528 字，标注雾凇拼音已正式采用，补 GPL-3.0 说明与旧文件删除记录）；
  `docs/词库重构-提示词与公开词库参考.md` 提示词模板源码路径改 `docs/rime-ice/cn_dicts/`、第五节生效流程
  改写为现行流水线（旧步骤划掉留历史）；`docs/计划任务.md` #10.10 标为「✅ 已于 2026-09-17 直接删除」；
  `docs/代码审查-20260916-独立复核.md` 废料隔离项标完成
  - 顺带修正两处历史错数：`pinyin_chars.txt` 与 `pinyin_syllables.txt` 实测均为 422（旧文档写 416）

**刻意未动**：`docs/HANDOFF.md`、`docs/ONBOARDING.md` 等带日期的历史快照保持原样（`AGENTS.md` 规则 ——
`docs/` 是本地历史资料目录、只读，且它们自带「文档生成时间 / 对应提交」标注）；不重编 APK、不动 Release
（本次无代码变更，第 34 轮产物 4,737,805 字节 / sha256 `f45bf09d…` 与当前代码完全对应，附件保持有效）。

---

# 按 rime-ice 标准复刻 7 套双拼方案（含「26 键键面随方案变化」）

日期 2026-09-17 ｜ 验证：`testDebugUnitTest`（200 用例）+ `lintDebug`（0 error）+ 真机逐方案实测

需求原文：按 rime-ice 标准复刻；一次不只做小鹤（搜狗 / 微软 / 紫光 / ABC / 加加一并做）；双拼时 26 键键面
必须随方案变化（韵母提示必做）；「输入方案」必须在「显示生僻字」上方。

- **核心做法：键位不手抄，从 schema 生成**。手抄 7 套方案（约 3000 条键位）必然出错且无法自证 → 新增生成器
  `tools/dict_builder/gen_shuangpin_tables.py`：① 解析 `docs/rime-ice/double_pinyin*.schema.yaml` 的
  `speller/algebra`（权威定义）；② 按 librime 代数语义（`xform` 替换 / `derive` 追加 / `erase` /
  `xlit` 逐字符映射）逐条施加到 `assets/pinyin_syllables.txt` 的 422 个合法音节上算出每套方案的码；
  ③ 产出 `app/.../ShuangpinSchemes.kt`（生成文件，勿手改，730 行）：`initials`（声母键）+ `codes`
  （码 → 音节，扁平表）

| 方案 | 声母特例 | 码数 | 需分号键 |
|---|---|---|---|
| 自然码 | zh→v / ch→i / sh→u | 434 | 否 |
| 小鹤 | 同上 | 424 | 否 |
| 搜狗 | 同上 | 429 | 是（`ing` 在 `;`） |
| 微软 | 同上 | 435 | 是 |
| 紫光 | zh→u / ch→a / sh→i | 417 | 是 |
| 智能 ABC | zh→a / ch→e / sh→v | 413 | 否 |
| 拼音加加 | zh→v / ch→u / sh→i | 429 | 否 |

- **换表安全性：与旧实现逐码对拍**。`verify_shuangpin_migration.py` 把旧手写自然码实现照抄成 Python，
  在 676 个两键码上与新表逐一对拍：3 处差异全是旧实现的 BUG（新表正确）—— `rw` 旧产出 `ria`（非音节）
  → 应 `rua`（挼）；`yo` 旧 `yuo` → 应 `yo`（哟）；`hm` 旧 `hian` → 应 `hm`（叹词）。旧实现另有 242 条
  独有码、产物全是非音节（垃圾）→ 换表零功能损失
- **键面提示由码表反推（「随方案变化」的正确做法）**：`ShuangpinTable.finalHint(key)` 在运行期从 `codes`
  反推每个键的韵母、`initialOf(key)` 给出 zh/ch/sh 红字 → 7 套方案键面提示各不相同且永远与引擎一致
  （同一份数据，不可能漂移）。顺带修历史缺陷：旧 `shuangpinHint('o')` 手写成 `"ou"`，而引擎里 `o` 是
  `o/uo`（`ou` 在 `b` 键），键面在骗用户 → 现自动派生为 `uo/o` ✓（`ShuangpinSchemesTest` 用回归用例钉死）。
  特例自动跟随：加加的 `sh/ch/zh` 是 `i/u/v`、ABC 是 `v/e/a`、紫光是 `i/a/u`，红字提示自己就对了
- **分号键：三套方案缺它就打不出 ing**。键盘只有 26 字母键，而搜狗 / 微软 / 紫光的 `ing` 落在分号上
  （三个 schema 的 `alphabet` 都显式含 `;`）→ 应 / 听 / 明 / 定 这类音节完全打不出来。做法：第三行 `m`
  与删除键之间加一个 `PinyinKey`（`key_semicolon`），只在当前方案需要时显示，其余方案与符号层 / 数字层
  一律 `GONE`（不参与测量 → 26 键布局与已调好的圆角 / 间隙零影响）；键面与大写字母键同款：主文本 `;`、
  下方韵母提示 `ing`（同样取自方案表）
- **状态与入口**：`Prefs.useShuangpin: Boolean` → `Prefs.shuangpinScheme: Int`（0 全拼 … 7 加加，取值只增
  不改），读取时带迁移（新键不存在而老键为 `true` → 自然码）；真机实测设备老配置 `shuangpin=true` →
  升级后显示「自然码双拼」，用户方案不跳变（老键保留不删，利于回滚）。`ShuangpinScheme` 枚举
  （`PinyinEngine.kt`）：方案身份 + 持久化取值 + 展示名 / 短名 + `cycle()`，设置页下拉与键盘循环共用同一份
  列表不会脱节。功能面板按钮改为方案循环（主文本＝当前方案名、副文本＝下一个方案，实测「自然码 / 换小鹤」
  →「小鹤 / 换搜狗」…）；空格键小字改「中文 + 方案名」。设置页新增「输入方案」下拉（8 项），按要求放在
  「显示生僻字」上方（首版放错位置已修正并复验）

**诚实的边界（写清不做的部分）**

1. 4 个音节任何双拼都编不出：`hng` / `hm` / `m` / `n`（鼻音叹词，词库 0 条）与 `junding`
   （`pinyin_syllables.txt` 里的脏数据：7 个字母不是音节，顺带登记，本次不动词库）
2. 「两键一音节」模型下同码写法必须让位（一码只能映射一个音节）：`lo` → 让给 `luo`（咯 / 啰 双拼打不出，
   全拼仍可）；`lve` / `nve` → 让给 `lue` / `nue`（但词库里 lve/nve 词条仍能命中，查询链路有 ue↔ve
   变体回退，无功能损失）；`ng` → 让给 `neng` / `nang` / `niang`（词库 0 条）。覆盖测试对「让位集合」
   做了显式豁免，其它任何缺口都算失败
3. 不实现 rime 的多拼写容错（如 `aai` / `oan` 这类 3 字母派生形式）：与「两键一音节」键盘模型冲突；
   好处是每个音节固定两次按键

**真机逐项实测**：设置页 `spinner_shuangpin` y=121 在 `switch_show_rare_chars` y=440 上方 ✓；配置迁移
老键 `shuangpin=true` → 显示「自然码双拼」、改选小鹤后写 `shuangpin_scheme=1/=2` 可复现 ✓；键面提示逐方案
截图核对 —— 小鹤 `d=ai / k=ing·uai / l=iang·uang`，搜狗 / 微软 `d=iang·uang / k=ao / l=ai`，紫光
`q=ao / w=en / b=iao / n=ui·ue / m=un`，ABC 红字 `a=zh / e=ch`，加加红字 `i=sh / u=ch / v=zh`，全部
符合各自标准 ✓；分号键仅搜狗 / 微软 / 紫光出现、切到小鹤 / ABC / 加加 / 全拼自动消失 ✓；紫光方案下
`t` + `;` → 日志 `双拼[t;]→ting → [听, 停, 厅]` 端到端打通 ✓；小鹤 `x n h e` → `双拼[xnhe]→xiaohe →
[小河, 小盒, 小鹤]` ✓（「小鹤」第 3 位，前两位是同音更高频词，属正常排序）；自然码回归键面 `o` 显示
`uo/o`（修掉旧 `ou` 错误）、其余与旧版一致 ✓。实测后已把方案改回用户原值「自然码双拼」
（`shuangpin_scheme=1`）。

门禁：200 用例全绿（新增 `ShuangpinSchemesTest` 16 例）；lint 0 error / 29 warning（新加下拉补了
`paddingEnd`，未引入新告警）。

**新增文件**：`app/.../ShuangpinSchemes.kt`（生成数据：7 套方案声母键 + 码表 + 提示派生，730 行）、
`ShuangpinSchemesTest.kt`（16 例：各方案键位、分号键、提示、全表往返自洽、覆盖完整性）、
`gen_shuangpin_tables.py`（生成器）、`verify_shuangpin_migration.py`（换表对拍，676 码逐一）。

**发布（新建 v20260917，非覆盖）**：提交 `9e9e8e3` 推送三处引用（`bbe6cbf` → `9e9e8e3`：GitHub `main`、
`perf/paging-pinyin-rematch`、Gitee `master`）；APK `versionCode=20260917`（构建日期跨天故新 tag，
与 v20260916 并存）、`versionName=jinn`、4,774,669 字节、`sha256=5a172f74…`、V1=false / V2=V3=true、
4 字节对齐；两端新建 Release `v20260917`（GitHub `gh release create`，首次因 `api.github.com` EOF 失败、
重试成功，`--latest` 另一次重试成功；Gitee `POST /releases` id `1149106` `target_commitish=master` →
`POST attach_files` 附件 id `3215230`）；校验两支下载 URL 均 HTTP 200、`size_download=4,774,669`、
sha256 与本地一致 —— `v20260917` 标签在两仓库都存在，应用内「检查更新」不会再误报网络异常。

---

# 面板按钮恢复原样 + 方案选择只在设置页（含两个真机发现的修复）

日期 2026-09-17 ｜ 验证：`testDebugUnitTest`（201 用例）+ `lintDebug`（0 error）+ 真机实测

需求原文：「面板导航栏的原来的 拼音和双拼 切换的按钮，禁止修改，必须改回原样（文本都不准改，用回原来的）。
因为输入方案在 APP 首页设置里改就行了，禁止放在输入法面板里修改。」

- **恢复原样（一字未改）**：功能面板按钮主文本 `全拼` / `双拼`（不再显示方案名）、副文本 `换双拼` /
  `换全拼`、空格键顶部小字 `中文全拼` / `中文双拼`。原二态交互也一并恢复：面板只切「用不用双拼」，
  `ShuangpinScheme.cycle()`（八态循环）删除、改为 `ShuangpinScheme.toggle(current, configured)`；
  具体方案只在设置页「双拼的输入方案」下拉里改，面板无选择方案入口
- **状态拆分为三键（老配置零迁移）**

| 键 | 类型 | 谁写 | 含义 |
|---|---|---|---|
| `Prefs.useShuangpin` | Boolean | 键盘面板按钮 | 用不用双拼（老版本即此键，语义不变 → 无需迁移） |
| `Prefs.shuangpinScheme` | Int 1..7 | 设置页下拉 | 选定哪套双拼（只增不改） |
| `Prefs.effectiveShuangpinScheme` | 派生 | IME 读取 | 生效方案：没启用双拼就是全拼 |

设置页下拉语义：选「XX 双拼」＝启用双拼并记住该方案；选「26 键全拼」＝关闭双拼（方案记忆保留，下次面板
切回双拼仍是这套）。

- **真机发现并修掉的两个缺陷（本次重点）**
  1. 面板切回双拼会丢用户选的方案：原实现读 `effectiveShuangpinScheme`，而把双拼关掉后它就等于全拼 →
     兜底成自然码，用户在设置里选的小鹤被丢掉。改为读 `Prefs.shuangpinScheme`（恒定 1..7）。
     实测：设置选小鹤 → 面板切到全拼 → 再切回小鹤 ✓，且 `shuangpin_scheme=2` 全程未被面板改动
  2. 设置页下拉会静默改写用户的方案：原「初始化完成」闸门挡不住 `onRestoreInstanceState` 触发的迟来
     `onItemSelected`（实测设备上 `shuangpin_scheme` 被改成 2）。改为只有用户触摸过 Spinner 才允许写配置
     （`setOnTouchListener` 置位）；「默认键盘模式」下拉同类隐患一并修掉（同一个坑）

**验证**：门禁 201 用例全绿（新增面板二态切换用例）；lint 0 error / 29 warning（给两个 Spinner 的触摸监听
补 `performClick`，消掉新引入的 2 条 `ClickableViewAccessibility`）；真机 PACM00 —— 面板按钮显示原文
「双拼 / 换全拼」、小鹤方案下键面提示确为小鹤（`w=ei`、`y=un`…）、面板二态切换不动方案值、设置页下拉选
自然码 / 小鹤分别写入 `1` / `2`，实测后已还原为自然码；用户自行编辑的文案原样保留未动（「双拼的输入方案」、
「26 键按钮 会自动显示韵母…」、「检查 APP 更新」、键盘外观两条说明）。

**发布（同日覆盖 v20260917）**：提交 `adf2429`（9 文件，+168 / −51）推送三处引用（`9f532f0` → `adf2429`：
GitHub `main`、`perf/paging-pinyin-rematch`、Gitee `master`；用户自行编辑的 `strings.xml` 文案随本次一并
提交，内容未做改动）；APK `versionCode=20260917`、4,774,669 字节、`sha256=12360062…`、V1=false /
V2=V3=true、对齐通过；两仓库 `v20260917` 同日覆盖（GitHub `--clobber`；Gitee 旧附件 `3215230` →
DELETE → 重传新 `3215384`）；Release 正文各补本次条目，并修正上一轮正文中已过时的「面板按钮改为方案循环」
描述 —— 同一个 tag 的正文必须与 APK 的实际行为一致；校验两支下载 URL 均 HTTP 200、
`size_download=4,774,669`、sha256 与本地一致。

---

# 双拼方案下拉去掉「全拼」（2026-09-17）

- 需求：「输入方案的下拉列表只列双拼，不放全拼」
- 改动：下拉改用 `ShuangpinScheme.SHUANGPIN_ONLY`（只 7 套双拼，不含「26 键全拼」）；语义简化——选中即「记该方案+启双拼」，关双拼改回键盘面板「全拼/双拼」按钮；初始选中显示**已记住的双拼**（列表无全拼，用生效方案会错位）；说明文案追加「此处只选哪套双拼；全拼/双拼切换用面板按钮」
- 验证：门禁 202 例全绿（新增「双拼列表_共七套且不含全拼」校验每套有键位表）；真机下拉 7 项、无「全拼」、选小鹤→`shuangpin_scheme=2`+`shuangpin=true`、面板切全拼→`false` 而 `scheme` 仍 2、双拼关时下拉仍显「小鹤双拼」、选自然码恢复 `scheme=1`
- 发布（覆盖 v20260917）：提交 `505f755`（5 文件 +71/−18）推三处引用；APK `versionCode=20260917`/4,774,669B/sha256 `34979c13…`/V1=false V2=V3=true 4字节对齐；两仓库同日覆盖（GitHub `--clobber`；Gitee 删旧重传），正文补本次条目

---

# 双拼性能 / 健壮性审查 + 冷启动卡顿诊断（含 3 处低风险优化，2026-09-17）

- 双拼审查：热路径无问题——`toQuanpin` 每键 133~368ns（12键445ns/22键978ns）、26 键提示派生 3.2µs、表常驻 0.254MB（PSS 0.08%）；全同步、无阻塞、无每键监听、表不可变无竞态。真实开销仅一处：7 套键位表首次访问 57~79ms（ART 同量级），键盘视图恰在 `onCreateInputView`（首弹）构造→拖慢首屏；大头是 730 行/3000 字面量生成类的类加载与静态初始化，优化针对「何时做」
  - 已修（行为不变）：① `ShuangpinSchemes.kt` 改惰性构建（`Map<String,Lazy<ShuangpinTable>>`，生成器同步）；② 新增 `warmUpAll()`，`onCreate` 后台优先级守护线程预热（日志 `双拼键位表预热完成: 7 套 / Xms`），最坏退化只建当前一套（1/7）
  - 证伪猜想：输入 `;` 后切方案导致 composing 残留打不出字→不可达（面板按钮仅 composing 空时显、设置页切换清 composing），不加防护
  - 判不划算不改：`finalHint` 每次排序(3.2µs)/残码重复 `toQuanpin`(~0.5µs)/每键日志串/`Prefs(context)` 复用/`of()` 线性扫描
- 冷启动诊断：真机时间线 基础词库 601,139 键/反向 692,183/10,672ms → 可选包 1,721,350 键/34,435ms，波动近 2×⇒非算力不足是启动期争抢。根因：① `query()` `loaded==false` 静默返空→基础就绪前 6~10.7s 打不出字；② 加载期无提示；③ 可选包固定 5s 延迟开跑正撞用户打字（21~34s 抢 CPU/带宽）；④ PSS 305MB 易 LMK 杀。骁龙 8 至尊版非瓶颈。耗时构成 xz 解压 732ms(41%)/解析+建表 971ms(54%)，真机 4~6×。优化方案：P0-1 高频子集先行(<1s)/P0-2 加载期提示/P1 持久化索引(6~10.7s→0.3~1s,290MB→60~100MB)/P1-2 可选包空闲/P2 反向索引懒建/P3 启动错峰
- 本次改动 3 处低风险：键位表惰性 + `warmUpAll`；服务创建后台预热；可选包加载线程降后台优先级
- 验证：202 例全绿；lint 0/29；探针实测预热 34ms 后台完成不占首弹路径；审查覆盖 676 码对拍/全表往返/7 套同音节集/边界

---

# P0-1 高频子集先行 + P0-2 加载期提示（冷启动可输入 243~314ms，2026-09-17）

- P0-1 高频子集先行（两段式）：新增生成器 `gen_hot_dict.py`，从与全量同源同序的 `base/ext.dict.yaml` 按词频取一批词产出 `assets/hot_phrases.txt.xz`（4 万词/34,515 键/220KB，词长≤4 同基础包、同键同词取最大词频）。`load()` 改两段：① 先载子集+单字表+音节表→`loaded=true`（可输入）；② 再载全量基础包 merge（已有在前+去重追加）
  - 一致性保证：子集须在每个键下是全量词表**前缀**否则并入后候选序与全量单载不一致→生成器改「取权重≥阈值 W 全部词含并列」并自校验前缀；Kotlin 加 `HotDictMergeTest`(子集先行+全量并入 vs 全量单载候选一致)/`HotDictAssetTest`(抽样346键确认子集全量前缀+体积/键数/必含词)。测试期真抓 BUG：第一版子集只从 base 取词、全量是 base+ext 合并→抽样键前缀破坏→同源同序重写后过
- P0-2 加载期提示：未就绪显「词库加载中…」、有输入但候选空且全量未并入显「词库补全中…」（候选栏内联文本，IME 内不弹窗；文案进 strings.xml）。单字表随子集先行→任何合法音节至少出单字，第二条很少触发
- 真机：首次可用 243/288/314ms（此前 6~10.7s，25~40×）；全量并入 `高频子集 288ms + 全量补全 6885ms`；1.1s 后 `nihk→nihao→[你好,拟好,你]` ✓；预热 `7 套 / 33ms` 后台
- 代价：APK +220KB（4.8MB）；改词库须重跑 `gen_hot_dict.py`（否则 `HotDictAssetTest` 失败）→ 写入 AGENTS/工具 README

---

# P1-2 可选包空闲加载（+ P1 持久化索引设计，2026-09-17）

- 可选包（ext+腾讯 1.14M 词条、真机解析 21~34s）原「基础就绪后固定 5s」开跑正撞用户打字→后台重活抢 CPU/带宽。改 `maybeLoadOptionalDict`（单次触发、先到先得）：息屏(`ACTION_SCREEN_OFF`)/键盘收起后闲置 20s(`OPTIONAL_IDLE_DELAY_MS=20_000`)/兜底 服务创建后 180s(`OPTIONAL_FALLBACK_DELAY_MS=180_000`)；加载线程仍 `THREAD_PRIORITY_BACKGROUND`，`loadOptionalAsync` 内先 `load()`(幂等持锁)故与基础加载天然串行不重复解析。真机：`开始后台加载（触发: 息屏）`/`（触发: 键盘收起后闲置）`、重活在息屏完成 `已加载 2 个包 8365KB`
- P1 持久化二进制索引：设计完成待开工，产出 `docs/词库持久化索引-设计与分期.md`（格式 keysBlob+keyOffsets+wordsBlob+wordOffsets 带 version+源摘要、运行时二分、可选包各自索引缓存 `filesDir/index/`、索引异常回退文本、`IndexParityTest` 文本 vs 索引逐条一致）；改动半径 `phrasesByPinyin` 6 读+3 写/`wordToPinyin` 7/`charsBySyllable` 不动→独立一轮中等重构，分三期（Stage1 索引+只读/Stage2 可选包独立索引+缓存/Stage3 反向索引懒建）；预期基础包 6~10.7s→0.3~1s、常驻 290MB→20~30MB；开工前确认①生僻字过滤移查询期②是否只留索引资产
- 验证：208 例全绿；lint 0/29；真机两信号各自实测命中

---

# P1 Stage 1 落地：词库改二进制索引（加载 6~10.7s → 2.9s，基础包内存 −70%，2026-09-17）

- 用户确认：接受「生僻字过滤移到查询期」+「APK 只留索引」
- 改动：① 构建期 `build_dict_index.py` 写 `assets/pinyin_index.bin.xz`（magic/version/摘要+keysBlob+keyOffsets+wordsBlob+wordOffsets，同源同序），文本资产留档 `out/rime_ice/`；② 运行时只读索引新 `PhraseIndex`（二分+按需解码、共享字节数组不复制）；`load()` 第二段改「解压索引+读偏移数组」；③ 统一 `phrasesFor(key)`（运行时并入词优先否则查索引；生僻字过滤移到查询期，索引与开关无关可复用）；④ 移除 69 万条反向索引：`consumption`(残码保留)/`predict` 改依赖 `candidatePinyin`（查询记「候选→键」上限 4096 清空，省 50MB 语义更准）；⑤ 索引就绪后摘与索引重复的 subset 条目
- 真机对比：首次可用 243~314→238ms；全量基础就绪 6~10.7s→2.9s（索引 2.29s）；基础包内存 ~145MB→PSS 90/Private 77MB（−70%）；含可选 305/290→245/232MB；可选 21~34s→17s；APK 4.30MB 文本→5.04MB 索引(+0.74MB)
- 与预估差距：没到 0.3~1s（瓶颈变解压、索引原始 17.3MB 偏移表 4.8MB）；含可选包内存只降 20%（可选包仍 HashMap，Stage2）
- 护栏 + 实现期抓 2 问题：新增 `IndexParityTest`(文本 vs 索引 300键/80部分输入/逐键词表全一致)+`IndexFeatureRegressionTest`(残码消费长==键长；预测与文本路径逐条一致)；`PhraseDictIntegrityTest`/`HotDictAssetTest` 改读索引。问题1 头部解析 24 字节/构建写 30→偏移全错，统一 `HEADER_SIZE=30`；问题2 删反向索引致 `predict` 失效→改「候选→键」短期映射；测试暴露 fixture 缺「键是另键前缀」组合→生成器补扩展键
- 下一步 Stage 2：可选包索引化 → 内存 245→40~60MB、加载 17→~1s；另两条可选优化：偏移表改长度数组(省3.6MB)/更快 xz preset

---

# 键面韵母提示硬性规则（去自身字母 / ü 去重 / 行数 ≤2，2026-09-17）

- 用户明示四条规则：① 绝不显示与该键字母相同的韵母（e 键不显 e、v 键不显 v…纯冗余）；② ü 两写法只留 `v`（`u`(jqxy后) 与 `v`(l/n后) 同现时只留 v）；③ 总行数强制 ≤2（含红色 zh/ch/sh 行，挤成一行红色占第2行）；④ 同行内顺序确定（长者在前、同长按规范序 `ui` 在 `ue` 前）
- 用户点名项逐条断言：自然码/小鹤/加加 `v`=`ui`+红`zh`；微软 `v`=一行`ui ue`+红`zh`；搜狗/微软 `y`=`uai`/`v`
- 规则写入生成器模板 `gen_shuangpin_tables.py` 的 `ShuangpinTable.finalHint`，重生成 `ShuangpinSchemes.kt`；顺序踩坑：须先去重 ü 写法、再剔除自身字母（否则 v 键 `{ui,u}` 去重条件永不命中）
- 另修：生成器词典源改读流水线留档 `tools/dict_builder/out/rime_ice/pinyin_phrases.txt`（原 `assets/pinyin_phrases.txt.xz` Stage1 已删，否则崩）
- 新增 `HintRuleTest`：7 套×26 键全量（无自身字母/行数≤2）+ 点名项核对；真机截图自然码 e/a/i/u/o/v 键均符合

---

# P1 Stage 2：可选词库包索引化（复用 59ms，内存 −63%，2026-09-17）

- 改动：① `PhraseIndex.build(Sequence<String>, stamp)` 单趟流式构建同套索引格式（自增偏移数组+两个字节缓冲，峰值≈索引体积），键序异常抛错回退旧「运行时并入」路径；② 设备端缓存：可选包首载构建索引落盘 `filesDir/index/<源>.idx`，源 `length:mtime` 不符才重建，删源则清缓存；③ 查询改逐段查找（运行时高频子集→基础索引→各可选索引）`LinkedHashSet` 去重合并，结果与旧一致；加 `mergedCache`(4096) 避免同键重复合并；④ `wordToPinyin` 不再因可选包膨胀
- 真机实测：首载 21~34s→8.7s（ext 1.8s/腾讯 6.9s）、后续 21~34s→59ms、内存 PSS 305→110~113MB（−63%）、磁盘 ~40MB 索引缓存、输入正常（`nihk→nihao→[你好,拟好,你]`）
- 护栏 + 实现期修 3 问题：新增 `IndexBuilderParityTest`（Kotlin 构建器 vs Python 脚本逐字节一致/两端互读/可选索引旧语义合并）；问题1 `mergedCache` 未在词库变更失效→`loadPhrasesReader` 汇合点 `clear()`；问题2 新测试类污染单例→统一加 `@After resetForTest()`；问题3 fixture 打乱键序→流式构建报错→生成器改排序输出

---

# 索引格式 v2（偏移表 → 长度数组，APK −561KB、加载 −26%，2026-09-17）

- v1 两张 u32 偏移表（各 604,078×4B=2.42MB 共 4.83MB）替换为 `keyLengths`(u8 0.60MB)+`wordLengths`(u16 1.21MB)；读取前缀和还原 IntArray，运行时结构与 v1 一致；版本升 v2，Python 脚本与 Kotlin 构建器同步升级，`IndexBuilderParityTest` 逐字节对拍继续通过
- 真机对比（`nihk→nihao→[你好,拟好,你]`）：索引原始 17.3→14.4MB；xz(APK内) 5.04→4.48MB；APK 5,745,490→5,184,338；基础索引加载 2289→1700ms；两段式 2942→2314ms；可选包 `.idx` 39.6→34MB；可选包复用 59→44ms
- 兼容性：v2 被 `parse` 拒的旧缓存自动重建（真机连续两次启动 `已构建…`→`复用索引缓存…`）；构建脚本加长度数组边界自检（键≤255B/词表≤64KB，超界即失败）

---

# 今日改动专项审查（6 个缺陷，全部修复并回归，2026-09-17）

- 范围：今日 8 提交（`4452d2e..ddd6193`）+ 未提交补丁（12 文件）全部差异；验证 221 例 + lint 0 error + 脚本冒烟
- 6 修复：① `PhraseIndex.parse` 段长校验 Int 溢出→改 Long 累加+精确总长比对（含回归，保「不自洽返 null」）；② `mergedCache` 未在基础索引到位时失效→失效点收敛 `finalizeLoad()`；③ `mergedCache` 裸 HashMap（主线程查询 vs 加载线程 `clear` 丢更新/错值/环死）→`ConcurrentHashMap`；④ `gen_hot_dict.py` 读已删文本资产→`FileNotFoundError` 静默废闸门→改从源现算全量词表；⑤ `verify_shuangpin_migration.py` 找旧格式表 + 判据定义了没调用→适配新格式+真正接入 `valid_syllables()`，修正后真回归 0（3 差异均为「旧非法→新合法」修正）；⑥ 引用腐化脚本（`convert_rime_tencent`/`merge_game_dicts`(静默退化空集)/`split_dict`/流水线 README 指向已删资产）→全改读留档文本+兼容 xz+显式警告，README 补索引步骤
- 额外核对：`Prefs.shuangpinScheme` 归一 ✓；生成器幂等 ✓；`build_dict_index.py --fixture` 逐字节一致 ✓；`convert_rime_ice.py` 对删资产有 `isfile` 兜底 ✓

---

# 深度路径审查（predict 成本悬崖 + 拖选状态机发散，2026-09-17）

- predict 成本悬崖（唯一可控风险）：探针（真索引 604,077 键）`keysWithPrefix("ni")` 6,185 键/2.18ms 实例化 6185 String；当前不可达（predict 前缀是整词拼音≥4字、键数几十）；处置 `predict` 加「收满 `MAX_PREDICTIONS` 即停」安全阀（结果同旧「扫完 take(N)」），`keysWithPrefix` 标成本悬崖与整改方向（区间回调 API 不建 String）
- 单字候选无预测（既有非回归）：`wordsFor("ni")=null`（单字源 `pinyin_chars.txt` 与词库两份数据），旧全量反向索引同样没单字；要「选你→预测你们」前置先解成本悬崖
- 拖选状态机发散（真缺陷已修）：`selectionActive` 两处各存一份（跨类同步是项目标记坑）→ ① `showDirectionPanel` 只重置视图侧未回传 IME→补 `onSelectionModeChanged(false)` 与 hide 对称；② `onStartInputView` 切输入框 anchor/focus 未复位→`clearSelectionState()`；三处重复逻辑收敛为单 `clearSelectionState()` 避免「只改一侧」
- 核实无缺陷：候选→键 `phraseKeysOf`（ue↔ve）在所有出候选路径生效、记录的键恒命中键故预测前缀规范无变体缺口；分词打分两查找用原始键（既有非本次）仅登记

---

# 双拼代数语义与 librime C++ 逐条核对（2026-09-17）

- 核对源 `docs/librime-master/src/rime/algo/{calculus.h,calculus.cc,algebra.cc}`(librime 1.17.0)
- 逐条对照（C++ 权威 → 生成器）：`xform`(`Transformation` 默认 addition+deletion=替换/`boost::regex_replace` 全局)一致，补空结果丢弃；`derive`(`Derivation` deletion=false 保留+追加)一致，补空不追加；`erase`(`Erasion` addition=false+整串 `regex_match`)→`re.fullmatch` 一致；`xlit` 逐字符、Parse 要求两侧等长得加载失败→`zip` 静默截断，本次修不等长即报错；`fuzz/abbrev/correction` 为 `Derivation` 子类差异只 credibility/type→忽略 tag 一致；集合语义 `Projection::Apply` 每 op 链式+map 去重→`cur→nxt→去重` 一致；未匹配原样保留一致；正则 `\w\d\b` librime 按 ASCII、Python `re` 按 Unicode→本次修统一 `re.ASCII`（0 处受影响）；解析分隔符=首个非小写字母、多余 tag 忽略，本次加固段结束按 YAML 缩进收紧+未知 op 响亮失败
- 修正 4 潜伏差异+1 解析加固：① xform 空串 librime 丢、旧留集合；② derive 同样不追空；③ xlit 不等长 librime 拒、旧 zip 截断；④ \w ASCII/Unicode 预防性对齐；⑤ `parse_algebra` 旧 `^\S` 在 algebra 非末尾吞后续段→改 YAML 缩紧+遇不识规则报错
- 决定性验证：重生成 `ShuangpinSchemes.kt` 逐字节不变（git diff 空）→ 差异对现行 7 套零语义变更（原有实现本正确），价值为「上游 schema 调整/新增 op 不会静默算错」；迁移闸门仍真回归 0
- 仅改构建期脚本+文档，APK 产物未变→沿用 sha256 `f1f45f44…`，不发版

---

# 从 librime 可吸收的设计（取舍清单，2026-09-17）

- 值得吸收：`P0-1 Prism:MappedFile` 内存映射索引（设备端 34MB `.idx` 由 `readBytes` 进堆→省 ~34MB 堆+免大拷贝，待办风险低）；`P0-2 Prism::ExpandSearch` 的 `Match(value,length)` 不物化枚举（`keysWithPrefix` 实例化全部命中键 `"ni"→6185 String/2.18ms`，单字预测需求时才必改，现已「收满即停」安全阀兜住），按需
- 明确不该吸收（有实测）：音节图+DP 分词看似最该抄、实测变坏——① `dictionarySegmentation` `key=path.joinToString("")` 恒等于输入⇒`hit` 对所有路径同⇒只比音节数最少（DFS 最长音节优先）；② 按 librime DP 形式「Σ 各前缀命中数」退化（每音节边界加分→切越碎分越高），真实词库 `tianqi→ti-a-n-qi`/`beijingdaxue→bei-ji-ng-da-xue`/`wangshang→wang-sha-ng`（11 语料 5 变坏、切出 `ng` 鼻音碎片）；③ 根因 librime 解拼写歧义+可信度排序（同音节多拼写/模糊音），本 26 键全拼固定音节表目标即「最长音节优先」用不着图与 DP；同样不荐双数组 Trie（二分 ~0.5µs、加载 100% 被 xz 解压支配）、引擎流水线（XL 规模+与「禁改语音链路」交错，长期方向）
- 本次落地：① 修正 `dictionarySegmentation`/`dfsSegment` KDoc（原称「返回最多词库短语切分」不符、反例 `xuni→[xu,ni]` 不成立因 `xun-i` 的 `i` 非合法音节）、举真实语义+反例+「别照那思路改」告诫；② 说明 `MAX_SEGMENT_PATHS=16` 仅防御（14 常见语料仅 `jintiantianqi` 18 种切分超限），非「截断致选错」
- 纯注释改动（零行为变更）→ 门禁 221 例全绿、Lint 0 error，不发版

---

# 吸收 librime 的两项设计（内存映射索引 + 不物化字符串枚举，2026-09-17）

- P0-1 `Prism:MappedFile`，索引全改内存映射：`PhraseIndex` 读取由 `ByteArray` 改 `ByteBuffer`（堆内 `wrap`/映射 `map` 共用偏移/二分/解码逻辑），新增 `ofMapped(file)` 失败返 null 回退；基础索引新增磁盘缓存（xz 先解压→原子落盘→映射，缓存名用 APK mtime 有效性键、旧版缓存启动清）；可选包缓存改 `ofMapped`、写入改「临时文件+原子改名」（覆盖被 mmap 文件会 SIGBUS 截断映射页），顺解「`.idx` 非原子写」遗留风险
- 真机（PACM00）：基础索引加载 1798→48ms（37×）、两段式 2689→949ms、PSS 110~113→77.7MB（−33MB）、Private_Dirty ~100→51.5MB（−49MB）、磁盘 34→48.8MB
- P0-2 `Prism::ExpandSearch` 前缀枚举不物化字符串：新增 `forEachRangeWithPrefix`（只回调键字节长+词区区间、不建 String）+ `collectLongerSuffixes`（字节级 `regionEquals`、未命中零分配、命中只解码后缀），`predict` 改用、扫描顺序严格保旧（基础→可选→运行时），死代码 `keysStartingWith` 删
- 护栏 `IndexMappedParityTest` 3 例（映射 vs 堆内逐键逐词表等价/区间枚举+字节后缀收集 vs 旧物化写法一致/缺文件截断 magic 不符 `ofMapped` 返 null 不抛）；用例 221→224
- 改了生产代码 → 重建覆盖发布（APK 5,184,338B / sha256 `051953d7…`），两端校验一致

---

# 用户词频学习（吸收 librime UserDictionary，2026-09-17）

- 语义（对齐 librime）：仅点候选/空格取首候选/点预测词时学；累加 `dee_new=commits+dee_old*exp((tick_old-tick_now)/200)`（tick 天计数、半衰期≈139天、当天重复不衰减）；稳定排序权重降序同权保词库序；权重<0.15 丢弃、上限 3000 条
- 实现：新 `UserFrequency.kt`（内存 `ConcurrentHashMap` + 文件 `user_freq.txt`「词<TAB>权重<TAB>天」，原子写临时文件+改名、防抖2s、`flushUserFrequency` 兜底；加载挂词库加载线程降优先级）；排序注入 `query` 收尾；设置页「用户词频学习」开关（`Prefs.userLearning` 默认开、立即生效）
- 真机 A/B：预置 `拟好 9.000`→`已载入 1 条`；`nihk` 候选 `[你好,拟好,你]`→`[拟好,你好,你]`；空格上屏→`拟好 10.000` 落盘 ✓
- 护栏 `UserFrequencyTest` 9 例（排序稳定/累加/衰减/序列化/脏数据/开关/原子写）；用例 224→234

---

# 近期功能改动全面缺陷排查（5 问题全修，2026-09-17）

- 排查范围：用户词频学习/内存映射索引/区间枚举预测/拖选状态机修复及相互作用
- 5 修复：① `UserFrequency.file` 缺 `@Volatile`（加载线程赋值主线程读可能永见 null→首批学习不落盘）→ 补；② `remember` 累加未钳「未来 day」（系统回拨 `exp(正)` 放大≈22万倍霸榜）→ 对齐 `parse` 的 `coerceAtMost(0)` + 回归；③ `flush` 走 BackgroundIo 异步（`onDestroy` 后进程回收→最后学习丢失）→ 改同步写（≤3000行/主线程1~3ms）；④ 学习词无行格式防御（制表符/换行写坏行→`parse` 整行跳过静默丢失）→ 入口拒收 + 用例；⑤ 加载时序回归（`UserFrequency.load` 排高频子集前占首字延迟预算 254~332→337ms）→ 移到高频子集就绪后
- 真机复验：高频子库 280ms→用户词频加载其后→基础索引重建 1834ms（两段式 2716ms）；重装首日重建、日常 mmap 48ms
- 核实无缺陷：搜索面板隔离/候选缓存交互/`rank` 稳定排序/`parse` 容错/开关语义/文件有界/原子写；已知边界（学习仅同拼音候选列表内提名次、预测不参与排序）非缺陷

---

# 索引缓存生命周期缺陷（内存映射真实使用失效）+ 修复（2026-09-17）

- D1（严重）：可选包加载会把基础索引缓存删掉——`loadExtensionDict` 扫地判据「文件名去 `.idx` 后是否在词库包列表」，基础缓存 `base.<APK mtime>.idx` 源 APK 资产永不入列 → 被当失效删；后果可选包每次加载即删基础缓存、mmap 永不命中、冷启动退回 1.8s 解压。修复：`staleOptionalCacheNames()` 纯函数显式排除 `base.` 前缀 + `IndexCacheLifecycleTest` 3 例钉住；真机全流程 base 缓存存活、重启 mmap 48ms/两段式 924ms
- D2（可观测性）：`writeIndexCacheAtomically` 两处 `return false` 无日志、调用方无条件打「已写入磁盘缓存」误导排查 → 两处失败打 W、调用方按 `written && mapped!=null` 区分
- 同轮核实无缺陷：拖选状态机复位/候选→键映射不失效/区间枚举预测集合顺序一致/原子写不截断映射

---

# 近期改动验证矩阵（+1 用户文案过期缺陷，2026-09-17）

- 真机逐点实测 5 项全一致（全新安装冷启动 442ms+97ms mmap/预置学习词提前/分类词库下载落盘+自动重启/失效缓存只清失效基础存活/文案核对）
- F1（文案过期）：`dict_startup_cost_line1~3`+`startupSec` 写「首次就绪等 7/19 秒」与现状不符（加载只在空闲时、首次是建索引、数值错、未提 mmap 复用）→ 改「空闲后台加载、首次构建索引约 N 秒不影响打字、之后 mmap 复用≈0.05s」、`startupSec` 改 2/7
- 存疑未验证：腾讯大词库下载/设置页开关真机点击/拖选中断复现/剪贴板全链路/语音链路（均代码+单测覆盖，未真机跑）

---

# 用户可见文案全量审计（纯源码核对，改 6 处，2026-09-17）

- 首页（设置页，69 字符串）：`settings_subtitle`「26键双拼(自然码)」→「7 套双拼方案」；`settings_usage` 同步；`switch_show_rare_chars_desc`/`rare_chars_on`/`rare_chars_off` 去「不加载/更省内存」（索引化后查询期过滤，词库不裁剪）；`settings_clipboard_desc`「点剪贴板」→「点历史」
- 分类词库页+检查更新（28 字符串）：`dict_manager_hint_line2`「下载后需重启」→「下载后会自动重启」；数字均实测一致（长词包 1.81MB/1,897,940B、腾讯包 6.36MB/6,668,612B/971,257词条）

---

# 文档同步到最新内容 + 发布 v20260918（2026-09-18）

- 文档：`AGENTS.md` 修正性能数字（基础包 0.3s 可打字、全量索引 0.05s 复用/首次 1.8s、索引加载 2.9s、PSS 77.7MB）+ 内存映射索引设计与踩坑；`README` 双拼7套/分类词库空闲加载/用户词频/APK 4.9MB；`README_EN` 同步；`tools/dict_builder/README` 真机验证标准；`词库持久化索引-设计与分期.md` 定稿
- 发布 v20260918：`versionCode=20260918`、APK 5,188,434B/sha256 `f3337cd6…`；两端新建 Release `v20260918`（GitHub `gh release create`、Gitee POST+手写 multipart）；下载 sha256 三方一致；代码 main/perf/paging-pinyin-rematch/Gitee master 三处快进同提交

---

# 文档与注释措辞平实化（不改功能代码，2026-09-18）

- 用户可见文案：生僻字说明去「构建索引/复用缓存」内部词；用户词频说明精简；词库加载代价 1~3 行改「空闲加载/首次约 N 秒照样打字/之后几乎瞬间」；原则：用户句不出现内部实现词
- 源码注释：圈号改项目「 - 」列举；删写给评审的元话语（「2026-09-17 复核…已改正」直接说清做什么）；收回单 `` 
- 未改：`更新日志.md`（一贯 `## 一、`+表格+加粗）；`AGENTS.md`「必须是并发容器」类项目原本写法

---

# 源码注释全面审读，去除模板化措辞（只动注释，2026-09-18）

- 做法：机械扫特征→逐处改→复查清零；共 12 处/10 文件（含 4 未碰测试文件）
- 改掉：设问式「为什么 xxx：」去设问留理由；文档式「背景：」直接去；KDoc 下划线小标题删（标题留）；理由列表①②③改顺口两句
- 判误报不动：`KeyboardLayouts` emoji 是表情数据；`ClipboardDb` 裁剪优先级 ①②③ 真实顺序；「不拦截本次复制」正常中文
- 复查：设问/文档标签/下划线/套话/元话语五类复扫命中 0

---

# 全部源码注释再扫一遍（补 4 处，只动注释，2026-09-18）

- 67 源文件全扫，补正则漏掉两类（加粗里的设问/句中的文档标签）共 4 处：`PinyinEngine` 两处「为什么/原因：」去引导词；`build_dict_index.py`「# 原因：」→「# 」；`PhraseIndex` 删悬空标题合句（10行压4行）
- 判定不改：「注意：」12 处（项目原有）；加粗不成对 0；套话/括号堆叠 0；注释最厚 5 文件逐块读

---

# 全身审注释前述（按模块逐块读，改 8 处，2026-09-18）

- 删引用仓库外文档的章节号（7 处）：注释写「（文档第十五/十六节）」等读者无文档等于没写（`ClipboardClassifier`/`Controller`/`Crypto`/`Firewall`/`Prefs`）；保留指向本机真实文件的 `docs/从 librime 可吸收的设计.md 第二节`
- 修正过期注释（1）：`Prefs.showRareChars` 仍写「关闭不加载生僻字降常驻占用」，实际查询期过滤 → 补上
- 判定不改：「一行一句话」「职责：」「注意：」项目原有；真实历史坑保留
- 覆盖：67 源文件全纳入，61 个按块逐块读

---

# 注释过期审查（机器可判三类核查，改 19 处，2026-09-18）

- 已移除功能残留词汇（17 处）：v5 迁移（2026-09-16）已整体移除隐私、`is_private` 列删/`TRIM_PRIORITY` 仅 `is_favorite`、`PSEUDO_PRIVATE` 常量不存在，但一批注释仍按隐私写 → 全部改「只提收藏」；保留 v3/v4/v5 迁移历史（记录当初为何改）
- 数据源列表过期（1）：`PinyinEngine` 类头仍列 `pinyin_phrases.txt`（文本），全量词库早已换 `pinyin_index.bin.xz` → 按 `assets/` 实际重写
- 文档数字过期（1）：`AGENTS.md`「20类/221例」实 23类/239例 → 改
- 核查：12 处「疑似不存在符号」全误报（Android API/标准库/局部参数）；28 处路径「找不到」仅 2 真问题，余为 assets 拼接/测试资源/运行时路径

---

# 术语统一 + 文档事实核对（改 21 处，只动注释与文档，2026-09-18）

- 术语统一（19）：「剪贴板页」→剪贴板面板；「候选项」→候选；「写盘」→落盘；「mmap」→内存映射；「输入方案」→双拼方案（对齐设置页下拉、界面字符串不动）；长词包/扩展包同一包 `dict_ext` 两叫法在定义处加对照
- 段落级重复：仅 2 条该留（三索引测试「收尾复位」×3、TextSelectionTest「行结构」×3，各测试须独立跑）
- 文档事实（2 过期）：`README_EN.md` APK 仍 4.5MB→4.9MB；`AGENTS.md` 高频子集耗时 0.25~0.31s 改 0.28~0.44s 注原因；其余声明（60.4万键/4.48MB·14.4MB/可选包8.7s→44ms/7套双拼/圆角0~24dp）一致

---

# 打字时按键响应专项取证（无卡死/ANR，1 处受限卡顿窗口，2026-09-18）

- 真机实测（PACM00，IME 进程）：逐键 50th 7ms/90th 12ms/99th 48ms（50/90 在 16.6ms 预算内）；janky 6.94%（小卡顿非卡死）；ANR/Davey 0/0；按键往返 450~532ms 无尖峰
- 假 BUG：① 初测「键盘不弹渲染0帧」实息屏；② uiautomator 抓不到按键文本（`PinyinKey` Canvas 自绘无文本，坐标用代码布局推算）
- 静态扫描：① 曾疑每键写日志文件——不成立，候选日志走 `v` 不落盘；② 剪贴板/搜索 DB 查询解密全走 BackgroundIo 不在主线程；③ 索引首次缺页一次性成本几十毫秒
- 唯一受限卡顿窗口：首切未预热双拼方案可能主线程同步建表（实测 0.21~1.13ms 量级）→ 处置不改代码（`warmUpAll` 已覆盖，改同步建表更糟）

---

# 修复「拼音打错/太长时字母键卡住」（用户报告，已定位根因，2026-09-18）

- 根因：`dfsSegment`「16 条完整路径」上限只数已完成路径，对切不通输入永不触发；前缀合法切分组合爆炸。JVM 实测 13→0.72ms、19→1.48ms、25→8.61ms、31→36.8ms，每+6字符×4~5，30+字符每键几百毫秒/更长秒级
- 修法：新增 `suffixParseable()` 自后向前 DP 判「后缀能否切通」，`dfsSegment` 进分支前剪枝；切分结果完全一致（候选数 110/369 未变），代价 O(n·6)。实测 25字符 8.61→0.73ms、31字符 36.8→0.43ms（85×）
- 护栏 `SegmentCliffTest` 2 例（43 字符切不通远低于 200ms、剪枝不改结果）；用例 239→241
- 编译器坑：KDoc 下标 `` `input[i..]` `` 被当 KDoc 链接报 `Closing bracket expected` → 改文字描述

---

# 26 键打字模拟测试（复现并修复「冷启动窗口内打字卡顿」，2026-09-18）

- 稳态干净：79 次真实用键 p50 7ms/p90 10ms/p99 14ms、janky 0.58%、高延迟/掉帧/ANR 0/0/0 → 稳态无问题
- 非稳态复现：刚装 APK 删 `base.*.idx` 弹键盘狂打 15 键，p99 14→300ms、janky 0.58%→12.07%、高延迟 0→15、掉帧 0→3；日志 `基础索引: 已解压写入磁盘缓存（604077键/14767KB/1846ms）` 与打字重叠
- 根因：基础索引重建 `XZInputStream.readBytes()` 一次 1.8s 连续 CPU 冲击 + 14.7MB 大分配，单核占满挤主线程
- 修法：分片解压 `readWithYields` 每 1MB `Thread.sleep(1)` 让出 CPU；原子写缓存每 256KB 让出；总耗时只多几十毫秒，前台不再被挤；结果功能不变
- 复现标准：`su -c 'rm -f filesDir/index/base.*.idx'`→force-stop→启动→弹键盘连打→`dumpsys gfxinfo` 看 p99/janky

---

# JVM 按键压力测试（不依赖真机，量化触发耗时，2026-09-18）

- `KeyboardStressTest`：JVM 模拟键盘复刻触发逻辑（字母走 `Shuangpin.toQuanpin`+`query`、空格/候选走 `consumption` 后裁 composing、全消费走 `predict`），每键计时+最慢现场
- 实测 7 场景/1514 键：p50 ≤0.156ms、max ≤0.83ms（缓存涨到 112/191 字符仍 <1ms）；7 套方案首用含建表 0.21~1.13ms
- 结论：按键触发逻辑非瓶颈（真机 1.2s 卡顿不来自引擎，嫌疑 UI 路径/GC）；纠正旧判断「7 套表一次全建几十毫秒」夸大、撤回「首切未预热方案可能卡」登记
- 护栏：用例 241→243，阈值 500ms/键只守灾难退化

---

# 乱序 / 超长拼音的模糊测试（589 例，最慢 3.4ms，2026-09-18）

- `KeyboardStressTest` 加模糊场景：固定种子 20260918，纯随机字母（400/1~40字符）、真实拼音打乱（60）、合法音节拼坏尾（120）、60/120/240 字符超长（随机/音节流/同键重复）
- 实测 589 例 p50 0.037/p99 0.453/max 3.437ms；最慢 5 例全为超长同键 a×240（3.437ms,len=240）/a×120/a×60 与随机/音节流
- 护栏：用例 243→244

---

# 打错的拼音怎么快速清掉（新增「长按退格清拼音串」，2026-09-18）

- 纠正上条：原「清空手势不清 IME 拼音缓存」是读漏——`onTripleBackspace()` 本就有 `composing.clear()`，清空手势干净
- 真缺口：「双击→长按退格」能清干净但连已上屏正文一起删（打错一串拼音会误删前文）；只剩逐字退格（78 字符×55ms≈6~8s）
- 新增：**按住退格 ≥1.2s → 清空拼音串（不动已上屏）**：连删 runnable 加判定，按住够久且拼音串 ≥12 字符→清空并停连删；与「双击+长按」成两级（轻量清拼音/彻底清输入框）；`onTripleBackspace` 改用 `clearComposingState()`（原漏清 `lastCommittedWord`）
- 可选项未实施：门槛降到 0.6s/8 字符、候选栏加「清空拼音」按钮、「给输入长度设硬上限」不建议（会静默丢键，240 字符仅 3.4ms）；`settings_usage` 补手势提示

---

# 安装成功 + 三项待验全部通过（1.2s 卡顿消失，2026-09-18）

- 订正上条：原写「ColorOS 一律拦截 adb install（-99）」不对——本次 `adb install -r` 直接 Success、`lastUpdateTime` 更新；`-99` 可重试非永久拒绝，先重试再走 push+安装界面
- ① 冷启动窗口（删 base 缓存→启动→连打 15 键）：p99 300→101ms、janky 12.07%→6.78%、高延迟帧 15→8、p50/p90 11/20→9/11ms；分片让出 CPU 生效，剩 101ms 为解压冲击残余（再压需改解压/延后空闲）
- ② 高频压测（160 键/34 次·秒⁻¹/缓存 78+ 字符）：Davey 1229/1198→0ms、p99 30~300→32ms、janky 8.11~13.14%→1.82%、高延迟帧 4~20→4；1.2s 卡顿消失，分词成本悬崖修复真机确认
- ③ 长按退格清拼音串真机：拼音串 84→长按 1260ms 清空（69 字符）→候选栏回功能面板；1.26s 触发（阈值 1.2s+55ms 轮询），触发前已逐字删 15 字符
- 真机坐标留档：1080×2280 屏 行1 y=1680 / 行2 y=1840 / 行3 y=2020（退格行3末非行2）/ 空格(540,2200) / 回车(1015,2200)

---

# 新增「候选预测词」开关（设置页可关，2026-09-18）

- 需求：预测候选允许用户自由开/关，置于设置页；关闭后选完文字不再出预测
- 实现：`Prefs.predictEnabled`（默认开）+ 设置页开关（紧跟「用户词频学习」下）；产出点加闸门（预测只从空格取首候选后/点候选后两处产生，关掉置空，「用预测」两处也校验）；`refreshCandidateBar()` 开头自愈（开关已关仍留预测就地清空）；开关走 `Prefs(context)` 立即生效免重启
- 真机：开→选词上屏 `空格取首候选:"您好"` + 1 条预测日志；关→同款上屏 + 0 条预测日志；切换即时落 `候选预测词: 关闭/开启（立即生效）`
- 说明：默认开（与现状一致）；关掉只影响「选词后预测」，不影响候选查询/补全/词频排序

---

# 修复「双拼下按 g/h 候选栏拼音行不更新」（2026-09-18）

- 用户确认根因：非卡顿——候选栏完全不显示 g 之后字母，实际是候选栏没更新
- 根因：拼音行显示 `queryInput`（转换后全拼），但注释声明「双拼先转全拼再查、显示保留双拼原文」，代码与意图不符。双拼下二者常不同：`jg`→全拼被吞成 `j`（日志 `双拼[j]→j`/`[jg]→j`），按 g/h 拼音行一字不动（底栏有反馈仅拼音行未变）
- 修法：两处 `viewCandidatePinyin.text` 由 `queryInput` 改 `input`（用户实际按键）；全拼下 `input==queryInput` 不变
- 真机（双拼·自然码）逐帧：j→`j`、g→`jg`、h→`jgh`，候选保持 `[级 集 几 及 机…]`（全拼仍 `j`）符合预期
- 教训：① 模式敏感功能验证前必确认当前模式（首次在全拼验证被用户指出，切回自然码重测）；②「看不见/不更新」类须核对渲染结果（截图/UiAutomator），只看日志整类漏掉
- 7 套双拼覆盖：修复在显示层（`input=composing.toString()`，与方案无关），6 套（自然码/小鹤/搜狗/微软/智能ABC/加加）这组键序都会「看起来卡住」（紫光恰不碰撞），真机只验自然码，其余依据「显示代码与方案无关」+ 对照表

---

# 文档同步到最新 + 同步推送（2026-09-19）

- 文档更新：`AGENTS.md` 首行「自然码双拼」→「双拼（7 套方案，默认自然码）」、测试规模「23类/239例」→「26/247」+列新增三支测试、订正事实错误（原写 `is_private` 列与掩码保留，实 v5 迁移已删该列、掩码失效、隐私整体移除，目录与 `ClipboardFilter` 的 `is_private`/`PRIVATE` 一并清）、新增「按键/模式排查方法」章节（日志 `拼音输入[双拼·自然码]: jgh` 自带模式；切换步骤；默认值 词频学习开/预测关/剪贴板 500）；README/README_EN 键盘特性补三条（预测默认关可开、按住退格清拼音、候选栏显实际按键）
- 同步推送：上轮 `git push … | tail -1 && break` 以管道退出码当结果（推失败也 break），`origin/main` 静默停 `6380ae0`、落后 9 提交（Gitee 反成功看似都推上）；改真实退出码重试补齐三处均 `d3e0366`：`origin/main`/`origin/perf/paging-pinyin-rematch`/Gitee `master`（本地 `main` 快进对齐）
- 发布：跨日 `versionCode=20260919`，两端新建 Release `v20260919`（保留 v20260917/18）；两端下载 sha256 一致 `30f288ec36caf9320ddb44db2aa86deb5559fceaae9bdfc92006fe695c56f827`

---

# 补齐推送 + 补发 v20260919 + 订正入库文档（2026-09-19）

- 补齐上轮没兑现两件事：① 推送失败（同管道退出码 bug，`origin/main` 停 `6380ae0` 落后 9 提交；Gitee 反而成功）→ 真实退出码重试补齐三处，本地 `main` 快进对齐（落后 130）；② Release 没发（`v20260918` 后累计 29 提交未发布，含用户可见修复：双拼拼音行/字母键失活隐患/分词卡顿/冷启动卡顿/新增开关）→ 补发 `v20260919`：两端新建 Release+传 APK+sha256 一致
- 订正不符处：① `ClipboardPrefs.maxItems` KDoc 仍写「默认 200」实 `DEFAULT_MAX_ITEMS=500`（2c0dcbe 改常量同步 strings.xml 漏 KDoc）；② `AGENTS.md` 双拼段写「7 套」却列 8 项（含全拼）→ 改「7 套双拼方案（全拼不属于双拼）」；③ `AGENTS.md` 点名 `ClipboardDedupeTest` 已删（d0f8fe3 隐私移除删 75 行）→ 去掉，补清单漏写的 `IndexMappedParityTest`；④ 前述一节记本机文件（不入库）订正且剪贴板上限已过期→从日志移除

---

# 入库文档措辞平实化 + 清掉与实际不符表述（2026-09-19）

- 去模板化（对外文档）：README 中英各 7 处章节 emoji（`## ✨ 核心特性`→`## 核心特性`）全去；删自问自答与过渡句（`In other words:`/`也就是说：`→结论直说）；加密描述说两遍→合并；英文 README 夹中文「双拼」→`Shuangpin`；`tools/dict_builder/README.md`「已按作者要求删除」元话语→「已于 2026-09-17 删除」；`注音规范-执行版.md`「三个必须记住的事实」→「三条」
- 订正不符：① 隐私残留（2026-09-16 整体移除、`is_private` 已删）→ README/AGENTS 分类/长按/模块职责按现状改写（分类仅 全部/网址/数字/收藏，长按仅 收藏/删除）；② 词条数实测 长词包 184,995/腾讯包 955,031 → README 18.6万/97万→18.5万/95.5万，`OptionalDicts.kt` 97万一并改；③ APK 实测 5,188,434B=4.95MB → README 4.9MB 改；④ THUOCL README 写「自动注音」而 dict_builder README 已标「评估·未采纳」→按事实改写并说明未采用原因
- 补漏：① 两 README 漏写 26 键圆角/间隙可调（2026-09-16 上线）；② `AGENTS.md`「按键/模式排查」补退格手势四级（单击/按住连删/≥1.2s清拼音/双击+长按清输入框）含判据常量；③ `ClipboardPanelView` 顶栏注释去隐私
- 代码改动：`OptionalDicts.kt` 腾讯包「约97万」→「约95.5万」；`ClipboardPanelView.kt` 顶栏注释清隐私残留
- 发布：重建 versionCode `20260919`（V1=false/V2=V3=true 4字节对齐），APK sha256 `0df8637124e0ee227419cc3d42a4b1fa7ac7a6b31c5b4b965a8fcdca8291feb2`；GitHub+Gitee 覆盖 `v20260919`（Gitee 先删附件再传），下载 sha256 与本地一致

---

# 入库文档模板化措辞二次清理（2026-09-19）

- 改了什么：① 语音说明铺垫太厚（中「本项目还内置…但不是开箱即用」/英同样绕圈）→ 直说「语音听写是可选功能，不开箱即用：识别完全在服务端完成」；② 删除键四条手势挤一句→拆四条并给英文版同样处理；③ 重复修饰（用户词频「记」连说两遍、同义反复）→合并；④ 两处一并去掉（`pypinyin` 保留，`extend_dict.py` 确实用过）
- 没改什么：章节标题/层级/顺序/表格一处未动；`AGENTS.md` 正文（执行标准，事实密度高，润色只稀释信息）；`tools/dict_builder/README.md` 除上一轮一处元话语无可精简；历史更新日志不动
- 验证：只改文档未改 Kotlin，`git diff --stat` 复核仅 5 个 md

---

# 缺陷修复、剪贴板容量上限与文档标准同步（2026-09-19）

- 缺陷修复（提交 `e00fd37`/`ae5ab1c`/`0b4864d`/`8b08c80`）：`su()` 先读流后 `waitFor` 致超时形同虚设→双路后台排空+先 `waitFor` 再取文本；host 校验 trim 不对称主线程崩（`ws:// 1.2.3.4 :6016`）→取值侧统一 `normalizeHost`；剪贴板延迟重试堵 BackgroundIo→改主线程 `Handler`；`TextSelection.lineStart` 行首边界；`PinyinEngine` 两处静态化与未用参数；`UserFrequency` 落盘时序（尾沿补写+`setEnabled` 补载）；复审自改代码修 3 处新引入缺陷+落盘时序窗口；host 校验补不变式用例（放行取值须能被 OkHttp 接受）；补符号表数据护栏 `SymbolLayoutTest`（分组非空/标签唯一/取值不含换行与制表符+标点组第2页 g 键录入回归）
- 剪贴板容量两条上限（`c9c2eae`）：① 单条 256KB 超出直接不入库（不截断），先字符数快筛再 UTF-8 精确定字节；② 总量 100MB 超出按最旧非收藏淘汰（复用 `TRIM_PRIORITY`「收藏永不删」，按字节累计），体积取 `LENGTH(encrypted_content)`（base64 纯 ASCII）；③ 抽纯函数 `overflowIdsForByteBudget`/`deleteByIds`，新增 `ClipboardLimitsTest`（15 例）；④ 标准：单条按明文字节、总量按库内密文（100MB≈73MB 明文），单条上限仅采集侧生效，库内超限行只由总量预算淘汰
- 搜索解密窗口与发布节流（`5e16c76`/`6d0cfec`/`7899185`）：① 分块 300→50（`SEARCH_WINDOW_ITEMS`，面板每页一致提文件级 `PANEL_PAGE_ITEMS`），最坏单次明文峰值 ≈196MB→≈33MB；② 新增纯函数 `ClipboardStore.decryptWindowPeakBytes()`+预算 `DECRYPT_WINDOW_BUDGET_BYTES`，配「窗口×单条上限×放大系数≤预算」护栏（含「300 条被拦」反向验证）；③ 窗口细化后逐块发布会把大库搜索主线程布局抬到数百次→补 `PUBLISH_MIN_INTERVAL_MS=120ms` 节流合并，循环结束无条件补发（防吞最后一块）；④ 放大系数按实测修正：单条解密常驻=2.33×明文（密文串 4/3+明文串 1.0），原按 UTF-16 估 3.5 高估 50%，取 2.5、预算 48MB；⑤ 附带修好：库空/无命中时搜索面板不再「静默不更新」
- 密钥与构建：仓库内不留密钥——`keystore/jinn-release.jks` 与 `keystore.properties` 移出（移动非删；两份 .jks sha256 比对非同一份属旧遗留）→归档 `GLOBAL/credentials/JinnKeyStores/legacy-in-repo/`；发布用现役密钥经 `JINN_KEYSTORE_ROOT` 传入；README/README_EN 密钥指引改为外部凭据目录+环境变量，写未提供时产未签名包
- 文档同步：`AGENTS.md` 测试规模 283→285 例、解密窗口标准 ×3.5 订正 ×2.5（实测 2.33）、真机安装补 ColorOS 拦新装可用通道（`adb push→/data/local/tmp→su -c 'pm install -r'`；旧「-99 可重试」「VIEW file:// 兜底」失效）+记「卸载即销毁 Keystore 密钥」；README/README_EN `assembleRelease` 注释与「未提供配置产未签名包」对齐
- 验证：`testDebugUnitTest` 29类/285例全绿、`lintDebug` 0/29；真机（PACM00/Android10/root，debug）`su()` 审计 7/7 无超时、词频文件即时生成（kill -9 后重启 `已载入`）、复制 3 次（含1重复）库内 2 条（去重生效）、体积淘汰 8KB 预算降尺度跑通、搜索节流 A/B（14分块→节流10次/未节流14次，收尾含完整结果）
- 剩余待办：① 发布 tag 纯数字化（远端 6 个 `v*` 重建+附着包重传）属发布动作未执行；② `ACTION_CLIPBOARD_PASTE` 动态广播 ≤12 对外可达→已删除该接收器；③ 日志内容最小化/`MANAGE_EXTERNAL_STORAGE` 去留/`Diagnostics.init` 移出主线程（前两项属用户决策）

---

# 源码安全审查与修复（2026-09-19）

- 缺陷：①【P1】剪贴板粘贴广播对外可达→删 `clipboardPasteReceiver`/`clipboardPasteFilter`/`notifyPasteResult` 与三常量及仅供其用的 `ClipboardDb.contentById`（任意应用 `am broadcast` 即把解密后剪贴板历史注入当前输入框）；②【P1】用户输入正文写入共享存储日志→`PinyinKeyboardView` 五处 `Diagnostics.i` 候选上屏/空格取首候选/搜索候选/无候选丢弃拼音 + `SettingsActivity.sendImeTest` 照常落盘（与「候选类走 v」相悖）→ 统一降 `v`；③【P2】明文用户词频未排除备份→两份规则补 `user_freq.txt` 与 `.tmp` 排除；④【P2】词库下载无体积上限→`copyCapped()` 超 `MAX_DOWNLOAD_BYTES`(64MB) 中止；⑤【P3】索引偏移前缀和 Int 溢出→`PhraseIndex.expandLengths` 改 `Long` 累加，越界返 null（不自洽返 null 绝不抛）
- 判定非缺陷：SQL 注入（`?` 占位/`Int` 字面量/`$where` 仅内部布尔）、更新跳转 URL（整型/内部字面量/固定 https/远端不进 Intent）、剪贴板加密（Keystore AES-256-GCM 随机 IV 篡改即失败）、su 拼接（仅 dataDir/uid/包名/固定命令）
- 验证：`compileDebugKotlin` 通过（20 条既有 Lateinit 告警）；`testDebugUnitTest` 29类/285例全绿；`lintDebug` 0/29；`AGENTS.md`「点击粘贴」同步为同进程回调标准
- 遗留：语音结果 `text.take(60)` i 级落盘（禁改）、词库下载无 sha256（需写哈希进 OptionalDicts）、`MANAGE_EXTERNAL_STORAGE` 去留/`Diagnostics.init` 移出主线程（用户决策）

---
---
# 源码安全审查（前述）与修复（2026-09-20，四轮累计）

- 范围：全量 Kotlin + Manifest + 备份规则 XML + 构建/仓库敏感文件 + 符号表数据。语音/NAS 链路按既定未审查未改
- **P1**
  - 密码框内容被学进用户词频：全仓无 `inputType` 甄别，三处 `rememberChoice` 无条件→新增 `InputFieldPrivacy.suppressLearning(inputType)`、`onStartInputView` 每次聚焦按 EditorInfo 设闸门、`learnChoice()` 统一停落盘（坑：`TYPE_NULL`=0 缺省须 `null`）
  - 拼音串每键 I 级落盘（隐私+主线程 IO）：`query` 补全分支 `Diagnostics.i` → 降 `v`；`takeRawComposing` 回车输英文原文、`prompt.take(30)` 改记 `promptLen`
  - 可选词库下载无完整性校验（可注入任意候选词/解压炸弹）：新增 `checksum` 字段 + `OptionalDicts.sha256Of`/`matchesChecksum`，`fetchToFile` rename 前校验，`OptionalDictChecksumTest` 7 例；硬编码摘要 `ext.xz`/`opt_tencent.xz`
  - 暂存粘贴重试丢失输入框身份（跨字段注入）：`flushPendingPaste()` 放回暂存一并恢复 `pendingPasteFieldKey`
- **P2**
  - 搜索态下五个入口仍落宿主（回车/逗号/句号/`onTripleBackspace`/`commitComposing`）：回车=退出搜索、逗号句号=`appendSearch()`、清空手势=`clearSearch()`、`commitComposing` 搜索态丢弃
  - 长按连删在「拼音被自己删空」后仍继续删已上屏：循环内补「删空即停」闸门（判据用按下时长度）
  - 自身剪贴板标记被三处「粘贴」误置→真实复制静默丢弃：删三处误置调用与整套机制
  - `imeOptions`「不要个性化学习」未参与敏感判定：新增 `suppressLearning` 形参 + `onStartInputView` 传入
  - 搜索驻留内存预算按字符数（中文低估≈3 倍）：改用 `ClipboardStore.utf8ByteSize()` 字节计，`ClipboardLimitsTest`+3
  - 备份未排除 SharedPreferences：`jinn_inputmethod.xml`/`jinn_clipboard.xml` 各补两条 `sharedpref` 排除
  - 防火墙备份审计查已失效文件（`backup_rules` vs `dataExtractionRules`）：按 `SDK_INT` 挑生效文件，判据升「库+词频两者都排除」
  - 更新检查可重定向降级明文 HTTP：终态 URL 协议断言，非 https 即失败
  - 后台 IO 队列静默吞异常：任务外层 `runCatching`+`Diagnostics.e`
  - 整窗解密失败让分页/搜索提前终止：一律「游标没前进才停」+无限循环保护
  - 词库「删除」与「下载」可并发：`remove()` 开头判 `downloading!=null` Toast 后返回
  - `data_extraction_rules.xml` 漏 SQLite 侧车（防火墙审计假 ✓）：两段各补三条侧车 exclude；`checkBackupConfig` 改分段解析，必需清单升 `REQUIRED_EXCLUDES`（库+三侧车+`user_freq.txt`）
  - 长按退格「整串清拼音」判据用错基准（12~27 字符继续删已上屏）：`ACTION_DOWN` 记录按下长度、循环内改用它判据（≈28 字符）
- **P3（同批 8 项 + 零散）**
  - 崩溃快照落 V 级正文绕开「V 默认不落盘」→ `filterOwnVerboseLines` + `DiagnosticsLogcatFilterTest` 4
  - `ClipboardDb.onCreate` 缺 `content_hash` 唯一索引→补建
  - 防火墙审计假 ✓（空集合 `any{}` 恒 false）→空集合改判「全部缺失」
  - 面板点击按下标取条目（声明稳定 itemId 却 `getOrNull(pos)`）→改「点中行稳定 id→按 id 找→退回下标」
  - `ClipboardPrefs.maxItems` 读侧不钳位→`coerceIn(1,9999)`；`Prefs.defaultKeyboardMode` 读侧不兜底→兜底 `PINYIN_CN`+写侧钳
  - 数字分类下界 `\d{6,}`→4 位；`completeSyllablePrefix` KDoc 如实描述（按字典序截断 32）
  - `su()` 超时后不 `destroy` 也没排空三管道→超时即 `destroy()`+记日志
  - 诊断导出包非原子写 + 设备信息文件永久残留→临时文件（纳秒名）+原子改名、`finally` 清理
  - 备份规则审计「整段缺失」仍给 ✓→先判段齐备性
  - 搜索面板与剪贴板面板可同屏（互斥不对称）→补 `hideSearchPanel()`
  - 剪贴板面板确认条与操作条不互斥/切分类不清操作条→两处各补一行
  - 合并结果缓存被「变更前旧结果」永久粘住→`@Volatile dataGeneration`+`invalidateMergedCache()`
  - 候选→拼音键映射「首写锁定」→改最新写入者胜
  - 单字表长度判据只在生僻字分支→改无分支生效（挡下假单字表 `字1`…`字70`）
  - 键盘触摸 `ACTION_POINTER_UP` 复位「已消费」→改只在 `ACTION_DOWN/UP/CANCEL` 复位
  - 连删延时任务无生命周期收尾（视图 detach 后循环永久运行）→`onDetachedFromWindow` `removeCallbacksAndMessages(null)`+复位
  - 音节前缀集合「就地 clear+add」与热路径并发→先建 `HashSet` 再整体替换 `@Volatile` 引用
  - 补全缓存裸 `HashMap`→声明处对齐并发容器
  - `dumpLogcat(suffix)`：suffix 进文件名又进 shell→改 `ProcessBuilder.redirectOutput(dest)`+suffix 白名单
  - `user_freq.txt` NaN/Infinity 权重未过滤→补 `!weight.isFinite()` + `UserFrequencyTest` 用例
  - 特殊组天气页混入汉字「由」「白」→`k→♃`(木星)/`l→♅`(天王星)，`SymbolLayoutTest` 用例
  - `UpdateChecker.readCapped` 引用不存在常量 `MAX_BODY_BYTES`→`MAX_BODY_CHARS`+补「截断按行」
- 判定非缺陷：SQL 注入、更新跳转 URL、剪贴板加密、su 注入/路径穿越、导出组件、`PhraseIndex.parse` 越界、并发可见性、仓库敏感文件（均逐项核对）
- 验证：门禁 33 类/323 例全绿、`lintDebug` 0/29；多方审查（全量通读+三路分域+候选逐条回源码+数据表脚本扫描）
- 遗留（登记）：语音识别正文 i 级日志（禁改）、引擎加载期不校验存量词库、诊断日志落共享存储、junding 伪音节、common_chars 非 BMP 字、`content_hash` 明文 SHA-256、`Diagnostics.init` 主线程探测、收藏数≥maxItems 新复制被裁、`MicRecorder.stop` join 超时（禁改）

---

# 发版：20260920（2026-09-20）

- 产物 `jinn-release.apk` 5,196,626 B / sha256 `4d1b886d…`（后同轮覆盖为 `3914cd8e…`）；代码 `27621ae`（39 文件 +2341/−340）
- 发布前验证：`compileDebugKotlin` 0 error（21 告警均既有）；`testDebugUnitTest` 33类/323例（`clean`+`--no-build-cache`）；`lintDebug` 0/29；打包 AGP→去 META-INF→zipalign -p 4→apksigner（V1=false/V2=V3=true 4字节对齐）；证书与旧版同签名可覆盖升级；manifest 3 组件对应、`res/xml` 仅 method/backup_rules/data_extraction_rules；仓库无密钥/硬编码凭据；可选词库两端 HTTP 200 且 sha256 与内置摘要一致；移除 5 条指向已删类（KeepAlive/Accessibility/BootReceiver/shizuku）的 R8 规则
- 真机（PACM00/Android10/KernelSU）：`adb push`+`su -c 'pm install -r'`（versionCode 19→20 数据保留）；启动/输入/方向面板拖选/剪贴板复制粘贴全链路正常，无 FATAL/ANR/E
- 双端发布：GitHub/Gitee tag `20260920`+Release（Latest 未覆盖历史），附件同名 5,196,626B、sha256 三方一致；`git ls-remote` 均 `27621ae`
- 工具缺陷修掉：`scripts/gitee_release.py` 的 `resolve_release` 在 tag 尚无 Release 时 Gitee 返 200+`null` 按 dict 解引用崩→已修 `resolve_release` 与 `--dry-run`
- 遗留：① 历史 6 个 `v*` tag 纯数字化重建未执行；② 真机未覆盖符号层/搜索框/多指手势/语音链路/可选词库实际下载

---

# 审查 · 批次 A 修复（2026-09-20）

- 范围：全量审查后第一批低风险纯逻辑修复，JVM 单测内跑通；IME 交互（B）/诊断隐私（C）/引擎剪贴板状态机（D）于后续批次
- 修复 8 项：① `PhraseIndex.build` 同键多行「静默少词」（二分查找永久跳过第二词表）→逐键写出+同键连续行合并（`|` 拼接受 64KB 上限），无重复键输入输出逐字节不变（parity 钉）；② `UserFrequency.flush` 写失败仍清 `dirty`（下次 `!dirty` 直接 return 无重试）→锁内 `if(writeAtomically) dirty=false`；③ `loadOptionalAsync` 的 `setThreadPriority` 在 `runCatching` 外（抛异常 `optionalLoading` 永 true）→加载线程套 `try/finally`；④ `Thread.start()` 失败不复位→包 try/catch 复位；⑤ `onReady` 失败路径也回调→仅 `isSuccess` 时回调；⑥ 检查更新 3s 闸门失效（网络超时 10s 可并发弹窗）→15s 看门狗（仅仍 Checking 兜底解锁）+删被覆盖死赋值、`UpdateState` 收敛两态；⑦ `filterOwnVerboseLines` 不认跨年时间戳（logcat 跨年加 `yyyy-` 旧正则失配）→正则加可选年份前缀（捕获组编号不变）；⑧ 三未用 import/`build.gradle.kts` `FileInputStream`→`inputStream().use{}`/Manifest 注释 `JinnIME`→`JinnIme`
- 验证：`testDebugUnitTest` 34类/330例全绿（`--rerun-tasks`）；`lintDebug` 0/28；parity/UserFrequency/DiagnosticsLogcatFilter 全绿

---

# 审查 · 批次 B 修复（2026-09-20，IME 交互类+真机）

- 修复 5 项：① M1 搜索态功能面板动作落宿主（`renderFunctionPanel`+`show/hideSearchPanel`+`JinnIme` 四回调）→搜索态候选栏只渲染「退出搜索」、显示/隐藏与方向面板互斥且可见性变化后重刷候选栏、`rejectedBySearchPanel()` 纵深防御；② M2 双击+长按清空输入框标准宿主失效（`deleteAllText` 旧 `Ctrl+A`+`deleteSurroundingText(MAX,0)` AOSP 只作用选区删不掉）→`setSelection(0,0)+deleteSurroundingText(0,Int.MAX_VALUE)`；③ M7 搜索态跨模式/会话残留（`switchToVoiceKeyboard`/`onFinishInputView`）→两处各补 `hideSearchPanel()`；④ L20 `copySelection` 未窗口换算（长文档 `startOffset>0` 把绝对下标当窗口下标）→复用 `currentSelectionRange`+`toWindowOffset`；⑤ L21 分号键 `ACTION_UP` 缺命中判定→补 `isInsideKey(keySemicolon,event)`
- 验证：`testDebugUnitTest` 33类/326例全绿；`lintDebug` 0/29；正式签名构建+覆盖安装；真机（PACM00 `-s BIYLBAFQQSS8DA69`）M1 日志 `功能面板(搜索态):退出`、退出后候选栏恢复；M2 `deleteAllText:已发送清空` 33 字符全清；M7 `onFinishInputView→顶部搜索面板:隐藏`；未覆盖 L20/L21
- 遗留（C/D）：M5 快照失败路径删除/L23 su 管道排空/L24 event 双时间戳/L25 崩溃同步等待；M3 主线程 `coerceToText`/M4 加载锁/L3 分页状态位/L4 收藏后刷新/L11 预测过滤

---

# 审查 · 批次 C/D 修复（2026-09-20，诊断隐私 C + 引擎剪贴板状态机 D）

- **批次 C（4 项）**：① M5 `dumpLogcat` 失败/异常路径残留未过滤快照（仅「子进程失败」一种删文件，readText/过滤/writeText 抛异常或进程落盘后被强杀留下含本进程 V 级正文——搜索关键词/拼音串/测试文本——滞留 7 天被 `exportBundle` 打包）→`try/catch/finally`+`produced` 标志统一收口，仅「过滤后成功写回」算产出；② L23 `su` 三管道既不排空也不关闭（输出写满 64KB 子进程阻塞、父进程 `waitFor` 必 5s 超时）→套 `ClipboardFirewall.su` 写法（stdout/stderr 各 daemon 排空、超时 destroy 后 join）；③ L24 事件日志双时间戳 + 两个死方法（`Diagnostics.event` 正文自带时间戳又被 `log()` 补一次；`eventSnapshot`/`dumpEventRing` 声称「崩溃落盘事件序列」从未接线、500 条环形缓冲从未消费）→正文不带时间戳，删环形缓冲与两死方法；④ L25 崩溃处理同步等 logcat 最长 10s（崩溃线程常主线程长时间阻塞推迟 ANR/tombstone）→快照放独立线程、`dumpLogcat` 增 `waitMs`（崩溃路径 2s），崩溃线程最多等 2.5s
- **批次 D（5 项）**：⑤ M3 剪贴板监听主线程 `coerceToText`（URI 型条目同步读整个 content:// 流、AOSP 无长度上限 → 键盘卡顿/ANR）→回调只取 ClipData 快照、文本提取统一进 `BackgroundIo`（新增 `extractAndSave`，正常与重试共用）；顺带修 L2：重试路径缺 `itemCount==0` 守卫集中进 `extractAndSave`；⑥ M4 `loadOptionalAsync` 与 `load()` 争用 `this` 锁（`load()` 全程 `synchronized(this)` 含秒级索引解压，调用点全主线程 → 首次加载期间空闲信号阻塞主线程数秒）→新增 `optionalLock` 专用于「检查-置位」原子性（两标志仍 `@Volatile`）；⑦ L3 面板分页状态位异常后不复位（查询抛异常 `loadingPage` 永 true、`loadNextPage` 首闸永久拦住后续分页静默「没有更多」）→两处查询包 `runCatching`、失败也复位记 E；⑧ L4 收藏切换后列表截回第 1 页（`refresh()` 只取首页不重置滚动）→与删除一致改 `refresh(resetScroll=true)`；⑨ L11 预测候选绕过生僻字过滤（索引路径 `collectLongerSuffixes` 直接解码后缀不过滤，与运行时 `phrasesFor`/`query` 不一致）→`predict` 出口统一过滤（`commonChars==null` 原样返回、零开销）
- 验证：`testDebugUnitTest` 33类/326例全绿；`lintDebug` 0/29；正式签名构建+覆盖安装；真机 L24 单时间戳格式、M3 取文本已不在主线程、M4 专用锁检查-置位正常无重复加载、冷启动两段式 3166ms
- 遗留：L20/L21 真机；junding 伪音节/common_chars 非 BMP/引擎加载期校验/content_hash 明文/语音正文日志/`Diagnostics.init` 主线程/备份规则漏 external 域 logs/L9 面板 OFFSET 分页/`MicRecorder.stop` join（禁改）

---

# 搜索面板布局精简（2026-09-20）

- 删输入框右侧 `btnExit`（「退出\n搜索」两行）与外层 `searchRow` 包装，输入框改独占一行（`MATCH_PARENT×46dp`，IME 总高不变）；删随失引用 `WRAP_EXIT_DP`；`Listener.onClose` 保留（粘贴搜索结果成功仍自动关闭面板）
- 退出搜索入口仍完备：候选栏「退出」/搜索态回车/BACK 收起/剪贴板面板互斥接管
- 验证：`testDebugUnitTest` 331 例全绿；`lintDebug` 0/28；真机截图输入框独占一行、候选栏「退出/搜索」两行；点候选栏「退出」→`功能面板:退出搜索`→`搜索面板:隐藏`跑通；附带 `onFinishInputView`→`顶部搜索面板:隐藏`（M7 又一处证据）

---

# 输入方案全局化 + 面板按钮移除（2026-09-20）

- 改动：① `ShuangpinScheme` 新增 `ALL = listOf(QUANPIN) + SHUANGPIN_ONLY`（8 项、全拼最前），删 `toggle`（面板二态切换已无调用方）；② 设置页下拉数据源改 `ALL`、选中即全局生效（全拼=`useShuangpin=false`、双拼=记 `shuangpinScheme`+启用）；③ 键盘 `renderFunctionPanel` 删首个按钮（含 `togglePinyinScheme` 死代码），功能面板 6→7 按钮格式（原 7 按钮格式真机验证时修正）；④ 文案 `strings.xml` 标题→「输入方案」、说明同步；⑤ `ShuangpinSchemesTest` 删 `toggle` 用例、列表断言改「全拼加七套双拼共八项」；⑥ `AGENTS.md` 更新「输入方案/持久化/切换排查/项目结构」，旧「面板按钮禁止改文案/下拉只列 7 套」作废
- 验证：`testDebugUnitTest` 34类/330例全绿；`lintDebug` 0/28；真机设置页标题/说明为「输入方案」「全拼+7 套双拼；选中即全局生效（语音键盘除外）」、下拉 8 项（全拼最前）、选「26 键全拼」→日志 `输入方案: 26键全拼（双拼=false）`、键盘功能面板 6 按钮、键面全拼样式、空格提示「中文全拼」

---

# 候选栏 ✕ 清空 + 移除长按清空手势（2026-09-20）

- 改动：① 候选栏 ✕（`keyboard_pinyin.xml`+`PinyinKeyboardView`）：`candidate_bar` 右端新增 `btn_clear_candidates`（40dp、默认 GONE），`refreshCandidateBar` 在候选/预测/拼音串展示时 VISIBLE、符号层与功能面板/搜索态退出 GONE；点击=`clearComposingState()`（回 6 按钮面板）；② 移除「长按退格整串清空拼音」（机制 B）：删 `shouldClearComposingOnHold`/`shouldStopRepeatOnExhaustedComposing`/`HOLD_TO_CLEAR_COMPOSING_MS`/`LONG_COMPOSING_TO_CLEAR`/`composingLenAtBackspaceDown` 及连删循环两处判据，删除键长按恢复标准语义（逐字连删，拼音删空后继续删已上屏）；③ 保留「双击+长按 >800ms 清空输入框」（机制 A）；④ `RecentChangesParityTest` 删随机制 B 失效 3 用例+常量（330→327）；⑤ `Prefs.KEY_SHUANGPIN` 注释不提面板按钮、`AGENTS.md` 退格手势排查同步
- 需求①核实：下拉=`ShuangpinScheme.ALL`（8 种、全拼最前）；全新安装默认「26 键全拼」
- 验证：`testDebugUnitTest` 34类/327例全绿（`--rerun-tasks`）；`lintDebug` 0/28；真机 `jk`→候选栏最右出 ✕，点 ✕→`候选栏 ✕：清空候选`→回 6 按钮面板且 ✕ 消失；输入 12 字符长按退格 1.5s 无「清空拼音串」日志、改逐字连删（机制 B 确移除）
- 订正：原记「批次 B/C/D 未实施」有误——已随 `a5b9d8c` 实施

---

# 当日改动全量审查与修复（2026-09-20 晚）

- 范围：`a5b9d8c`（批次 A~D + 输入方案 + 搜索面板）+ 未提交（候选栏 ✕、移除长按清空、README/注释）；三路独立审查（输入方案/搜索面板剪贴板/引擎诊断）+主审逐条回源码+AOSP 判定，按「是否本次引入」分档
- 修复 8 项：① `PhraseIndex.build` 合并期 O(n²) 重建→`LinkedHashSet` 保序累积；② 合并未去重→与文本路径 `existing + kept.filter{seen.add(it)}` 对齐、重复词只留首；③ `loadOptionalAsync` 的 `Thread.start()` 失败不复位→包 try/catch 复位；④ `onReady` 失败路径也回调→仅 `isSuccess` 时；⑤ `dumpLogcat` 误删同名旧快照（固定名 `dest` 启动即失败则旧产物被 finally 清）→进 try 先 `dest.delete()`；⑥ `settings_usage` 文案过期（手势移除遗漏）→「点候选栏右侧 ✕ 整串清」；⑦ `UserFrequency.saveNow` 陈旧任务防御（写盘前校验 `file !== f`）；⑧ `resetForTest` 补 `extensionLoaded` 复位+删 `labelColorRes` 死参/过期注释/`deleteAllText` 日志术语
- 复核撤销误判 1：`deleteAllText` 的 `Int.MAX_VALUE` 溢出——回 AOSP `BaseInputConnection.deleteSurroundingText`：`if(numDeleteAfter>0)` 守卫挡负区间、`b=0` 时 `0+MAX_VALUE` 不溢出 → 无崩溃，M2 修复正确
- 登记不改（非本次/既有）：搜索面板结果列表新查询不回顶/空查询也显「未找到」/粘贴失败无 Toast；剪贴板面板切分类只收操作条不收清空确认条；输入方案切换后键盘侧无即时同步（`ACTION_CONFIG_UPDATED` 无发送方）；引擎 `dirty` 置位锁外/`isOptionalReady`/`isExtensionLoaded` 无生产调用方
- 验证：`testDebugUnitTest` 34类/328例全绿（`--rerun-tasks`，+1 去重用例）；`lintDebug` 0/28；真机词库加载 `音节=414 基础键=604077` 逐项一致，候选栏 ✕ 正常（`jk`+12 候选+最右 ✕）

---

# 发版记录：同日覆盖 20260920（2026-09-20 晚）

- 提交 `ec6f705`（16 文件 +296/−222）推送 main：GitHub `main`+Gitee `main`（新规则不再推 Gitee `master`；`master` 已同步同提交）
- 发布包 5,196,626 B / sha256 `3914cd8e…` / `versionCode=20260920` / V1=false V2=V3=true / 4 字节对齐；三次独立构建 sha256 一致（确定性构建）
- 覆盖两端 `20260920` Release 附件：GitHub `--clobber`；Gitee 旧 `3229178`→DELETE→新 `3235668`
- Release 正文双端同步：新增候选栏 ✕ + 移除长按退格整串清空、补当日前述审查批次修复、修正过时描述；校验数字 34类/328例
- 校验：双端下载 HTTP 200、`size=5,196,626`、sha256 三方一致；发布前强制全量 `testDebugUnitTest` 34类/328例 + `lintDebug` 0/28 + 真机覆盖安装全过
- 发布规范写进 `AGENTS.md`「发布与同步」（main 主分支 + 覆盖式 + 双端三方校验）

---

# 半透明键盘审查修复（D1~D7，2026-09-22）

- 背景：透明度 WIP 经多轮审查收敛 6 条真缺陷（D1~D4 中等 + D6/D7 轻微；D5「窗口级透明无条件」/D8「shift 双淡」判为「待确认/设计意图」移出）
- 修复：
  - D1 剪贴板/搜索面板整块不参与透明（识别只认 `kb_bg` 一种色）→ `alphaFaces` 升级双档色集：背板类（`kb_bg`/`app_bg`）走 plate、内容面类（`card_bg`/`surface_hi`）走 surface；面板按钮由 `ClipboardPanelView.applySurfaceAlpha` 按档重建（共享构建器 `buildKeyFaceBackground`，`SearchPanelView` 留空对齐）
  - D2 候选栏内已构建面不跟随即时刷新→`refreshAppearance()` 末尾补 `refreshCandidateBar()`（方向面板展开态跳过）
  - D3 语音面板覆盖不全+不即时→`onKeyAppearanceChanged` 补 `applyTransparencyToVoicePanel()`（即时）；`kb_divider` 走 plate、麦克风底盘与 6 编辑键按 surface 重建（`MicButton` 禁改域不动）
  - D4 候选栏底档位与设计矛盾（有候选全透→文字叠宿主）→`updateCandidateBarBackground()` 按内容状态选档（有拼音/候选/预测→surface、空白→plate），带缓存免重绘
  - D6 `lastTransparencyDesc` 死代码+诊断无条件执行→日志与 `logBackgrounds` 移入「档位变化」分支（弹键盘不再刷日志/不再 500ms 扫全树）
  - D7 轻量项（整体覆盖安装真机验证：透明度全链路在 A16 正常，见下条 A16 实测）
- 验证（见 A16 条目）：覆盖安装后设置页标题/入口/三滑杆/61% 透明度/半透明效果/无 FATAL 全过

---

# 产品改名：Jinn安卓 → Jinn拼音（2026-09-22）

- 核对（仅显示文本，包名/签名/数据/DB/URL 全不变）：`strings.xml` `app_name`/`settings_title` Jinn安卓输入法→Jinn拼音输入法；`README.md`/`README_EN.md` 标题同步；`JinnIme.kt` 类 KDoc 同步
- 刻意不改（`AGENTS.md`「命名规则」）：`Jinn_AndroidInputMethod`（GitHub 仓库标识，被 `UpdateChecker.REPO`/下载 URL/`gh release --repo` 引用）、`JinnIme`（日志 TAG 与 `Diagnostics.DIR_NAME`）——皆非显示文本
- 验证：`aapt dump badging`→`application-label:'Jinn拼音输入法'`；真机设置页标题/冷启动词库加载正常（音节 414/基础键 604077 逐项一致）；`testDebugUnitTest` 40类/382例全绿、`lintDebug` 0/23
- 兼容性：包名/签名/数据/Prefs/DB/远程 URL 全不变，仅桌面图标名与设置页标题变化

---

# A16（Android 16）实测与「透明度功能消失」根因（2026-09-22 晚）

- 根因：设备装的是当日早期构建，非 A16 兼容问题。① 代码侧核查——透明度区块在 `activity_key_appearance.xml`/`KeyAppearanceActivity`/`Prefs.keyTransparencyPercent` 全链路无任何系统版本条件分支（布局无 visibility 条件、外层 ScrollView 不裁剪）→不存在「A16 不显示」路径；② 实测证据——装包前 UI dump 文案仍是旧版（标题「Jinn安卓输入法」、入口「按钮圆角间隙」，新版应为「Jinn拼音输入法」/「键盘按钮调整」）→该包构建于「透明度+新文案」合入之前；③ 教训：`versionCode` 只到「日期」粒度，同日多次构建无法从版本号区分，装包以最新构建 APK 为准
- 解决与实测（覆盖安装 `com.jinn.inputmethod.20260922180459.APK` 后）：设置页标题/入口「Jinn拼音输入法」/「键盘按钮调整」✓；外观页三滑杆（圆角1dp/间隙0.5dp/透明度0%）全在✓；拖至 61%→日志`键盘透明度: 61%`✓落盘即时生效；半透明效果明显透出背景、文字清晰✓；无 FATAL/ANR✓
- 结论：半透明键盘/改名/候选栏 ✕ 在 Android 16（API 36）全部正常；此前「功能消失」为设备装了当日早期构建，覆盖安装最新包即恢复
- 排查附注：A16 收紧 shell——`am start` 无法启动 `exported=false` 页面（走设置页入口）；该机 `wm size` Override 1080x2400（Physical 1440x3200）→`uiautomator dump` 坐标与截图均 1080x2400 空间以 dump bounds 为准；输入框首次聚焦弹系统 Autofill（Google 服务），再点一次才出键盘

---

# 发版 · 20260922 前述覆盖发布（全量重编译）

- 全量重编译：`gradlew clean`+`build_apk.py`→`com.jinn.inputmethod.20260922185834.APK`（5.0MB；V1=false/V2=V3=true 4字节对齐）；复制 `jinn-release.apk` 覆盖双端 Release `20260922` 附件（删旧传新）
- 三方 sha256 一致：本地=GitHub=Gitee=`39e08ff2…b7645aa`
- 双端 Release 正文更新：补本次条目（半透明/候选栏 ✕/移除长按清空/改名/A16 实测），修正过时描述（三滑杆+松手即时生效、入口「键盘按钮调整」、测试 40类/382例、lint 0/23）
- 代码提交 `999d5d8`（副标题去「语音识别 +」+A16 记录）→双端 main 同步
- Gitee API 坑（已登记）：① 更新正文 `access_token` 必须放 body（放 query 报「登录失效」）；② 正文 JSON 须手工构造（PS `ConvertTo-Json` 把中文转 `\uXXXX` 令 payload 膨胀 ~8 倍被拒）；排查附注：`Invoke-RestMethod` 读 Gitee 返回中文乱码（PS 5.1 解码），校验以 `curl.exe` 拉原始 JSON 为准

---

# 符号面板：「常用」拆为「全角 / 半角」（2026-09-21）

日期 2026-09-21 ｜ 诉求（用户）：「把『常用』改为『全角』，只放全角符号；在右侧新增『半角』，只放半角符号」，顺序为「全角 半角 编程 ……」

- **改动（`KeyboardLayouts.kt`）**：原「常用」组拆成两组（分组总数 10 → 11，「全角」仍第 1 组、进入符号层默认组）：
  ① `全角`（1 页，26 键满）：中文全角标点原样，8 个 ASCII 符号换全角变体（`~ @ # $ % ^ & *` → `～ ＠ ＃ ＄ ％ ＾ ＆ ＊`）；
  ② `半角`（2 页）：第 1 页 = 原「常用」第 2 页（数字 + `- / : ; ( ) ' " \ = + _ | \` [ ]`，布局未动），第 2 页 = 补齐常用半角标点与从全角组移出的半角符号（`. , ? ! ~ @ # $ % ^ & * < > { }`，16 键，余位留空）。
  符号层渲染与手势逻辑零改动（分组标签、组内翻页均数据驱动）
- **测试护栏（`SymbolLayoutTest`，5 → 8 例）**：`全角半角紧随编程之前`（钉用户要求组顺序）；`全角组不得混入半角字符` / `半角组不得混入全角字符`（按「是否 ASCII（<0x80）」判宽度归属，旧「常用」正是两种宽度混装；`·—…` 非 ASCII 计入全角组）
- **验证**：`testDebugUnitTest` 35 类/336 例全绿（+3）；`lintDebug` 0/28（无新增）；真机（PACM00，`-s BIYLBAFQQSS8DA69`，release 覆盖安装）分组标签实测 `全角(1/11)/半角/编程/标点/特殊/序号/数学/…`，全角页 26 键全为全角符号（截图 `！？。，；：""（）`/`【】《》·—…、～`/`＠＃＄％＾＆＊`）、半角页 `1/2` 数字+半角、`2/2` 补齐标点（日志 `符号组内翻页: 半角 第 2/2 页`，该状态已同日修订覆盖：半角组数字已删、两页按纯符号重排见下节）、端到端 q 上屏半角 `.` / 切回全角 z 上屏全角 `＠`（`uiautomator` 读到 `.＠` ✓，误触 shift 已复位 `大写锁定: false`）；APK `com.jinn.inputmethod.20260921151413.APK` 5.0 MB（<8MB 上限）、`adb install -r` 覆盖成功；说明：本次尚未提交/发版（工作区同时含上一轮「设置页自动检查更新」未提交改动）

---

# 符号面板修订：全角/半角纯度与编程组去符号页（2026-09-21）

日期 2026-09-21 ｜ 诉求（用户）：① 全角组每字符须为全角符号、禁数字 0-9；② 半角组同理（禁数字）；③ 删编程组第 1 页全部符号

- **改动**：① 半角组删数字（上节引入时第 1 页带 `1~0`）：32 个可打印 ASCII 重排两页各 16 键（第 1 页 `- / : ; ( ) ' " \ = + _ | \` [ ]`、第 2 页 `. , ? ! ~ @ # $ % ^ & * < > { }`），两页排满无空行，数字走数字层；② 全角组核对为纯全角（`！？。，；：""（）【】《》·—…、～＠＃＄％＾＆＊` 26 键），无 ASCII/数字/字母/汉字，数据未动由护栏钉；③ 编程组删第 1 页（原「编程常用英文符号」页），页数 7→6，第 1 页直接关键字页（`return/print/main/if/else…`），需符号用半角组（互补）
- **测试护栏（`SymbolLayoutTest`，8 → 11）**：`全角半角两组不得含数字`（`isDigit`，含全角 ０-９）、`全角半角两组不得混入字母或汉字`（`isLetter`，全角 Ａ/汉字都算）、`编程组第一页从关键字开始`（每条含字母，钉「纯符号页不得加回」）
- **验证**：`testDebugUnitTest` 35 类/339 例全绿（+3）；`lintDebug` 0/28；真机（PACM00，`-s BIYLBAFQQSS8DA69`，release 覆盖）全角页 26 键全全角、半角 `1/2`/`2/2` 各 16 键无数字、编程标签 `1/6` 第 1 页全关键字无纯符号、点 `if`→`if`、切半角翻 2 页点 `!`→`if!`（`uiautomator` 读、`符号分组切换: 编程 (3/11)`/`符号组内翻页: 半角 第 2/2 页`）；APK `com.jinn.inputmethod.20260921152006.APK` 5.0MB、`adb install -r` 成功
- **排版修订（同日）：一页 26 键，先放满再翻页**：上节把半角排成「16+16」两页、第 1 页只 16 就翻页留 10 空键，违反规范 → 32 符号按码点升序铺排（第 1 页 26 满 `! " # $ % & ' ( ) * + , - . / : ; < = > ? @ [ \ ] ^`、第 2 页余 6 `` _ ` { | } ~ ``）；全表核对其余 10 组（日 10 页/序号 7 页）本就「非末页满 26 键」，仅半角违规，护栏升级为全表规则（非末页恒 26 键、任何页不得超 26，`SymbolLayoutTest.符号页必须先放满26键再翻页`，类 11→12）；真机半角 `1/2` 26 满/`2/2` 6 键，点第 2 页 `|`→`|` ✓；门禁 35 类/340 例；APK `com.jinn.inputmethod.20260921154028.APK` 5.0MB、`adb install -r` 成功

---

# 符号层：返回键红色粗体 + 大写键无效（2026-09-21）

- 改动（`PinyinKeyboardView.kt`）：`btnSymbol` 文本改用 `SpannableString` 上 `StyleSpan(BOLD)`+`ForegroundColorSpan(kb_key_hint_red)`，退出符号层换回普通串、样式自动还原（无状态残留）；`btnShift` 符号层三层兜底不可用——`isEnabled=false`（无障碍报 disabled）+`alpha=0.4` 置灰+点击守卫（`layer==LAYER_SYMBOL` 直接 return），字母层不受影响
- 验证：门禁 35 类/340 例、`lintDebug` 0/28；真机符号层「返回」红色粗体、shift 明显置灰，点 shift 无反应/无 `大写锁定` 日志；回字母层键面恢复、shift 正常锁定（日志 `大写锁定: true` 后复位 `false`）；APK `com.jinn.inputmethod.20260921154631.APK` 5.0MB、`adb install -r` 成功（未提交/发版）

---

# 全角组按用户清单扩充为 68 项三页（2026-09-21）

- 清单核对：全角 69 项（`,` 重 1→去重 68，全非 ASCII）；半角 70 项（10 组重复 + 21 项非 ASCII 混入）→ 按此排违反护栏；两清单 17 项重合，经确认半角只留 ASCII、通用符号归全角
- 改动：「全角」26→68 三页（26+26+16）按用户清单顺序（去重）；「半角」不变（32 ASCII，26+6）；临时脚本对拍 68 项、页 26/26/16、顺序一致 ✓
- 护栏（`SymbolLayoutTest` 12→13）：`全角与半角两组取值零重合`
- 验证：门禁 35 类/341 例、`lintDebug` 0/28；真机全角三页截图（1/3 26、2/3 26、3/3 16，顺序与清单一致，日志 `符号组内翻页: 全角 第 2/3 页`/`第 3/3 页`），点 `℃`→输入框 `℃` ✓；APK `com.jinn.inputmethod.20260921155712.APK` 5.0MB、`adb install -r` 成功（未提交/发版）

---

# 半角组顺序改为「与全角逐项对应」（2026-09-21）

- 改动：「半角」由 ASCII 码点升序改为按全角对应序：取 32 个 ASCII 中有全角对应的 31 个按其在「全角」组出现次序排，`$` 无对应放末尾；页次不变（26+6）
- 护栏（`SymbolLayoutTest` 13→14）：`半角组顺序与全角组逐项对应`（半角每项全角对应在半角序列下标单调递增，无对应者只许末尾）
- 验证：门禁 35 类/342 例、`lintDebug` 0/28；真机半角 `1/2` 首位 `,`（与全角首位 `，` 对应）、末位 `|`，`2/2` 为 `{ } \` _ ^ $`（截图），点 `{`→`{` ✓；APK `com.jinn.inputmethod.20260921160541.APK` 5.0MB、`adb install -r` 成功（未提交/发版）

---

# 默认值调整：词频/预测/生僻字关、圆角 1dp、间隙 0.5dp（2026-09-21）

- 改动（仅改「未写入过」时的取值，不动已保存配置）：生僻字关（不变）、词频学习 开→关、预测词关（不变）、圆角 0→1dp（`KeyAppearance.DEFAULT_CORNER_DP`）、间隙 0→0.5dp（`DEFAULT_GAP_DP`）、剪贴板上限 500（不变）；圆角/间隙默认同时是 NaN/Infinity 兜底（`snap` fallback），仍落定义域与步进网格
- 护栏（`KeyAppearanceTest`）：`defaults_areInsideDomainAndOnStepGrid` 断言 1dp/0.5dp 落定义域且钳位原样保留
- 验证：门禁 35 类/342 例、`lintDebug` 0/28；真机设置页读数与设备已存配置一致（生僻字关/词频关/预测关/圆角 3dp/间隙 0.5dp/上限 500）——`key_corner_dp=3.0` 是用户手动存值不被覆盖；APK `com.jinn.inputmethod.20260921162814.APK` 5.0MB、`adb install -r` 成功（未提交/发版）

---

# 亮白/暗黑主题 · 批次 1：双色板落地（2026-09-21）

- 范围：主题方案第 1 批（色板基础设施）；批次 2（手动/定时切换+设置页入口）未实施
- 改动：`values/colors.xml` 重写亮白色板（默认目录=跟随系统浅色），新增 `values-night/colors.xml`（暗黑=原值），两套逐名对齐（40 令牌+5 新收敛令牌）；5 处 drawable 硬编码色收敛为令牌（`btn_aurora_primary`/`btn_aurora_secondary`/`field_aurora`/`grip` 等）；`themes.xml` 两套：默认父 `Theme.Material.Light.NoActionBar`+`windowLightStatusBar=true`，新增 `values-night/themes.xml`，顺带解决亮色 AlertDialog 深色框架问题；对比度不足两处微调（`danger` `#E5484D`→`#FF5A5F`、`text_on_accent`→`#0B1020`）
- 验证（对比度脚本实测）：亮色正文/次文字 15.36/5.58:1、accent 4.88、danger 5.62、键盘正文 16.47、候选选中字 4.63，全部 ≥4.5:1（`card_stroke` 1.31:1 属装饰豁免）；门禁 35 类/342 例、`lintDebug` 0/28；真机（root 切 `uimode night`）：浅/深色设置页与键盘同步转、切换无白屏黑屏闪烁；APK `com.jinn.inputmethod.20260921165859.APK` 5.0MB、`adb install -r` 成功；批次 1 后 App 主题=跟随系统，批次 2 待办已登记 `AGENTS.md`

---

# 主题 · 批次 2：手动 / 定时切换（2026-09-21）

- 改动：新增 `ThemeManager`（`isDarkNow`/`minutesUntilSwitch` 纯函数 + `wrapContext` 只覆盖 uiMode 两位）；`Prefs` 新增 `theme_mode`（默认 2=暗黑）/`theme_light_at`/`theme_dark_at`（07:00/19:00）；设置页「主题」卡片（模式下拉+定时两个 TimePicker+说明），改值即 `recreate()` 换色，页面内按 `minutesUntilSwitch` 排到点刷新；应用点：两 Activity `attachBaseContext`、IME `onCreateInputView`（`themedContext` 建键盘）与 `onStartInputView`（决策变化才 `setInputView` 重建）
- 验证：门禁 35 类/353 例（`ThemeManagerTest` 11 例）；真机默认暗黑（系统亮色下仍暗→强制生效）、选「亮白」（日志 `主题模式: 1`）立即变亮、选「定时」改 18:00 立即变暗（日志 `定时切换: 亮起 18:00`、键盘 `主题变更: 重建键盘（暗黑）`）；APK `com.jinn.inputmethod.20260921171056.APK` 5.0MB 覆盖（未提交/发版）
- 默认值改为跟随系统（同日晚）：`Prefs.themeMode` 默认 2→0（越界兜底同步），设置页下拉同步；真机选「跟随系统」后系统切亮色→设置页立即变亮白、切回暗色复原（`Night mode: yes`）；门禁 35 类/353 例、`lintDebug` 0/28；APK `com.jinn.inputmethod.20260921173523.APK` 5.0MB 覆盖

---

# 当日改动全量审查与修复（2026-09-21）

- 范围：`ea40239`（符号面板/主题批次1-2/默认值/自动检查更新）+ 审查后新增修复
- 修复 2 项：① 键盘可见时改主题不换肤（`JinnIme`）：原只在 `onStartInputView` 判定，设页改主题要等下次弹出→新增 `JinnIme.notifyThemeChanged()`（设页三处改动点调用）、判定抽成 `applyThemeIfNeeded()` 共用、`instance` `@Volatile` 可空、`onDestroy` 置空；② 定时模式跨切换点不换肤：新增 `scheduleThemeTick()`（`postDelayed` 到下一切换点+2s、`onFinishInputView` 取消），非定时无操作
- 护栏（`ThemeColorParityTest` 2 例）：`亮白与暗黑色板逐名对齐`（缺名不报错只在真机表现为颜色不对）、`两套色板都非空`
- 逐项审查：功能/边界（跨零点、脏时刻 `floorMod` 归一、`minutesUntilSwitch` 恰在切换点返 1440 避免 0 误判）/异常（`wrapContext` 只改 uiMode 两位、`notifyThemeChanged` 无实例静默返回）/并发（`@Volatile`+主线程 Handler）/性能（仅决策变化重建）/规范（判定全纯函数、drawable 无硬编码色）/兼容（`windowLightNavigationBar` API27+，API26 亮色导航栏图标仍浅色不崩）/文档（`AGENTS.md`「主题」同步修复与默认跟随系统）
- 验证：门禁 35 类/355 例、`lintDebug` 0/28；APK `com.jinn.inputmethod.20260921175813.APK`（5.0MB）覆盖，现场还原（`theme_mode=0`、暗色、定时 07:00/19:00）

---

# 符号分组排序：拖拽反馈 + 独立排序页（2026-09-21）

- 九、用户实测「长按拖拽无反应」根因与强化：根因为①进入拖拽视觉反馈过弱（alpha0.85+elevation6dp 浅色上几乎不可见）；②拖动中其他分组不让位、无落点指示；③真指长按超阈值抖动（>24px）会取消计时（与横向滚动冲突，保留取消；`input swipe` 匀速无法注入「先按2s再移」，自动化难覆盖）。强化（阈值保持2秒）：进入拖拽被拖项 `scale1.15+elevation8dp`、其余分组变暗（alpha0.45）；拖动中被拖项跟手、落点槽位提亮（0.85）；松手复位、顺序即时落盘（`SymbolOrder.move` 覆盖）；落点换算 `位移/宽度` 四舍五入、越界钳两端
- 十、排序改版：移除面板长按拖拽整套交互（`bindGroupTouch`/`dropGroup`/`highlightDropSlot` 等），分组按钮恢复纯点击切组；设置主页该区改一个按钮（「符号分组顺序」）打开排序页；新增 `SymbolOrderActivity`（独立排序页，每行分组+↑↓调整+「恢复默认顺序」，即时落盘）；离开页 `onPause`→`JinnIme.onSymbolOrderChanged()`→键盘已建则重建（日志 `符号分组顺序变更: 重建键盘视图`）
- 十一、排序页视觉微调：↑↓ 按钮换主题配色（`btn_aurora_secondary` 半透明蓝底圆角白字，禁用态仍半透明，与「恢复默认顺序」一致）；右上角新增 ✕ 关闭按钮
- 验证：门禁 35 类/357 例、`lintDebug` 0/27（少1：消 `DisableBaselineAlignment`）；真机 root 拉起排序页（11 行+↑↓+恢复默认、首末行箭头正确禁用）、设置主页收为单按钮；长按 `符号分组: 长按 2000ms 进入拖拽排序（起点 2）` 由 `SymbolOrderTest` 覆盖；APK `com.jinn.inputmethod.20260921205628.APK` 5.0MB

---

# 新增「收藏」分组：用户自由 DIY 符号（2026-09-21）

- 诉求：符号面板新增「收藏」分组（默认第三位：全角/半角/收藏），可排序；组内符号用户自由增删、满26个自动新页；出厂预置 D I Y、添加仅输入框、禁止重复
- 改动：`FavoriteSymbols.kt`（新，存 `Prefs.favoriteSymbols` JSON二维数组）：出厂 `D I Y`；`null`→预置、`"[]"`→空组不回退、损坏→回退；追加全局去重（≤8字符）、末页满26开新页；删除=前移补位+空页收起（天然满足「非末页恒26」）；`KeyboardLayouts.kt` 新增 `favoriteGroup(pages)`（页内按 QWERTY 行序铺26键）；`SymbolOrder.kt` `DEFAULT` 插「收藏」到第3、`normalize` 兼容旧串（无收藏→插回半角后）、`groupsInOrder` 收动态收藏组；`PinyinKeyboardView` 初始化读排序+收藏组装，切组/翻页/上屏零改动；`FavoriteSymbolsActivity`（新）+布局：按页网格、每格红「移除」角标、底部「＋添加符号」弹输入框（剥换行制表符、空/超长/重复提示）、右上 ✕；设置页「编辑收藏符号」按钮；排序页/编辑页共用 `JinnIme.onSymbolLayoutChanged()`（离页重建键盘）
- 护栏（370例+13）：`FavoriteSymbolsTest` 7例（预置/空态/序列化/去重长度/满26翻页/删补位收页/默认第三位+旧串兼容+页内铺键位）；`SymbolOrderTest` 3例适配；`SymbolLayoutTest` 照旧（收藏不在常量）
- 验证：门禁 370例、`lintDebug` 0/27；真机编辑页 root 拉起（标题+✕+「第1页（3个）」+`D I Y`三格红角标+「＋添加符号」）；APK `com.jinn.inputmethod.20260921221613.APK` 5.0MB、`adb install -r` 成功（未提交/发版；待用户实测添加/移除/排序）

---

# 收藏与主题审查（同日，4 轮，5+3+2+0 项修复）

- 五、3 项修复：①【中】服务层取色用错 Context（`JinnIme.renderLink` 走系统配置，强制亮暗拿错色板）→ 新增 `keyboardThemeCtx`（键盘创建时主题覆盖 Context）服务层取色一律走它；②【低】主题时刻 setter 未归一→`Math.floorMod` 归一；③【低】`StaticFieldLeak`（静态强引用 Service 触发 lint 29 条）→改 `WeakReference`+`onDestroy` 置空、lint 回 0/28；护栏补 `ThemeColorParityTest.两套主题定义的item逐名对齐`；验证 35类/356例、覆盖安装无回归
- 六、修复：①【低】`Prefs.themeMode` setter `coerceIn` 与读取端/`isDarkNow` else 分支解释冲突（非法值如9落成「定时」vs「跟随系统」）→ 统一落「跟随系统」；②【护栏】主题下拉 `theme_mode_values` 裸数字改错不报错→新增 `主题下拉取值与常量一一对应`；③【规范】`assertEquals(true,cond)`→`assertTrue(cond,msg)`；非缺陷（记录标准）：全角68项码点核对无误（PowerShell `Group-Object` 会把 `《`U+300A 与 `〈`U+3008 折叠成同键产生假重复，须走码点）；`SettingsActivity` 定时 tick `onDestroy` 已 `removeCallbacks` 无泄漏；验证 35类/357例、0/28
- 七、复核前几轮结论（真缺陷保留/非用户可达防御性保留/护栏防未来改错），新发现2修复：①【中】换肤静默丢未上屏内容（上轮定时 tick 引入：到点重建丢拼音串/预测词）→ 新增 `PinyinKeyboardView.hasPendingInput`，`applyThemeIfNeeded` 先判、有则延后到下次弹出（日志 `主题变更: 键盘有未上屏内容，延后到下次弹出换肤`）；②【文档】`minutesUntilSwitch` 注释错（写「站切换点给1440」实为「到另一端距离」）→ 订正（测试期望本正确）；验证 35类/357例、0/28
- 八、多角度核实、零代码改动：14组配色对比度全达标（最差4.84:1暗色红字、状态点4.97:1）；`DictManagerActivity` 已有 `attachBaseContext`→`themedContext`；`Prefs` 构造开销仅 `getSharedPreferences`（系统缓存同实例）可忽略；`wrapContext` 无既定异常路径、为假想问题加 try/catch 属增码不增健壮，不改；语音键盘路径守卫 `?.hasPendingInput==true` 行为正确；安全面无新增权限/导出/外部输入、日志只记模式时刻无敏感；真机补验系统暗→强制亮白双向生效（此前只验反向）

---

# 发版：`20260921`（新 tag）

- 提交 `ea40239` 推送 main：GitHub+Gitee 双端 `0f658bb..ea40239`
- 产物 `jinn-release.apk` 5,200,722 B / `sha256=c1db99af…` / `versionCode=20260921`
- GitHub `gh release create 20260921`+附件；Gitee 新建 Release（id `1157859`）+附件（id `3240334`）
- 校验：双端下载 `size=5,200,722`、`sha256` 与本地三方一致 ✓

---

# 审查修复轮：parse 归一 + 重建守卫扩展 + dirty 标记 + 注释/死代码（2026-09-21）

- 背景：「收藏」（`2f3b297`/`0728ee5`）三轮审查后只实施低成本正向修复；「多点触控并发删除」「renderPages 卡片化」过度项明确不做
- `FavoriteSymbols.parse` 归一：输出恒规范结构（`LinkedHashSet` 保序去重 + `chunked(PER_PAGE)` 重分页、空页消解）；原缺陷 `favoriteGroup` 只铺26键位、超界静默丢→「编辑页见第27个、键盘不见」；页边界非规范会被合并（内容/顺序不变）、规范结构下重分页恒等；测试改「内容/顺序稳定+页结构归一」+新增「超页重排不丢+去重/空页清理」「满页26铺满26键位」
- 重建守卫扩展：新增 `hasActiveOverlay`（剪贴板/搜索面板）与 `hasPendingInput` 合并为换肤延后判据（修「定时到点换肤关掉用户开着的面板」）；`rebuildInputViewForSymbolLayout` 补 `hasPendingInput` 守卫
- dirty 标记：`FavoriteSymbolsActivity`/`SymbolOrderActivity` 仅实际写入后 `onPause` 才通知重建（进来看看就退不再整块重建），标记不复位（守卫可能延后、重建幂等）
- 注释/死代码：删两个0调用 `companion fun start`（`SymbolOrder/FavoriteSymbols Activity`，`exported=false` 理由不成立）；`Prefs` KDoc 归位（`themeLightAtMinutes` 孤儿、「排序页写入」标注、「仅供诊断日志」）；`SymbolOrderTest` 去「长按拖拽」过期表述
- 验证：门禁 39类/373例(+3)、`lintDebug` 0/27；真机 root 逐项：预置 `D I Y` 正常、添加 `Z` 即时生效(第1页4个)、关页日志 `重建键盘视图`(+1)、未改进出计数不变、键盘「收藏」第3位键面 `D I Y Z`、删 `Z` 回3个+计数再+1+落盘 `[["D","I","Y"]]`；APK `com.jinn.inputmethod.20260921231707.APK` 5.0MB ROOT 装；发布前探针实证（100项正确分页 `[26,26,26,22]`、删下标两种算法相等、脏JSON回退不崩、归一幂等）结论无缺陷

---

# 发版：`20260921`（同日覆盖，tag 不变）

- 提交 `6b1abd8` 推送 main：GitHub+Gitee `0728ee5..6b1abd8`
- 产物 `jinn-release.apk` 5,217,218 B / `sha256=caadaae9…`；ROOT 装 Success
- 覆盖附件：GitHub `gh release upload --clobber`；Gitee 删旧(id `3240334`)→传新(id `3242216`)
- 正文双端追加本次条目（原文逐字）；Gitee `PATCH` 带 `tag_name`+`name`
- 校验：双端 `size=5,217,218`、`sha256` 三方一致 ✓

---

# 设置页「符号布局」入口改版：分组顺序 + 收藏编辑并排（2026-09-21）

- 两按钮（「编辑收藏符号」上、「符号分组顺序」下）改为同张圆角卡片内并排（`card_aurora`+`padding=12dp`+两个 `weight=1` 按钮、`marginStart=8dp`）
- 顺序：左「符号分组顺序」、右「编辑收藏符号」；文案沿用既有字符串资源、id 未变、`SettingsActivity` 绑定零改动
- 验证：门禁 39类/373例；APK `com.jinn.inputmethod.20260921232129.APK` 5.0MB ROOT 装；设置页截图（同行两按钮+圆角外框，与相邻组一致）；未发版

---

# 设置页布局与文案改版：两列网格（2026-09-21）

- 需求：主页面排序/文本按指定改、能同排的两两一行、未提到的保持原样
- 新顺序（自上而下）：1 标题/保存配置重启（原样）；2 点这里唤起键盘（由第3提最前）；3 检查软件更新｜打开下载页面；4 启用本输入法｜改默认输入法；5 符号分组顺序｜收藏的自定义；6 拼音输入方案｜键盘主题配色（定时两时刻按钮及说明保留同卡下方、非定时整体隐藏）；7 自动唤起键盘｜加更多生僻字；8 用户词频学习｜追加候选词语；9 键盘外观/分类词库/剪贴板/授权麦克风/NAS语音/语音开关/导出诊断/使用说明（原样）
- 文案（`strings.xml`）：检查APP更新→检查软件更新；启用输入法→启用本输入法；切换输入法→改默认输入法；显示生僻字→加更多生僻字；候选预测词→追加候选词语；输入方案→拼音输入方案；主题→键盘主题配色；「点这里 唤起键盘」→「点这里唤起键盘」（去空格）；`favorite_title` 用户手改保留、设置页按钮改 `favorite_entry_title`=收藏的自定义；`symbol_order_title`=符号分组顺序；开关说明小字未改
- 验证：44 控件 id 逐一核对齐全；门禁 39类/373例；APK `com.jinn.inputmethod.20260921233800.APK` 5.0MB ROOT 装；真机截图 1-8 行顺序/文案/两列并排/圆角外框一致、键盘外观及之后原样；未发版

---

# 新增「补充短语词库 / 按钮圆角间隙」入口行 + 键盘外观独立页（2026-09-22）

需求（用户）：在「符号分组顺序 ｜ 收藏的自定义」的下一行放置两个按钮（补充短语词库 / 按钮圆角间隙），
两者都打开独立页面设置。

## 改动
- 新入口行（符号布局行正下方，两按钮并排）：
  - 「补充短语词库」→ 打开既有的 `DictManagerActivity`（长词包 / 腾讯大词库按需下载）
  - 「按钮圆角间隙」→ 打开新建的 `KeyAppearanceActivity`
- 新增页面：`KeyAppearanceActivity` + `activity_key_appearance.xml`，标题「键盘外观」+ ✕ 关闭 +
  圆角 / 间隙两个滑杆（定义域与换算一律走 `KeyAppearance`，与绘制逻辑共用一套边界）；
  拖动即落盘，键盘下次弹出套用，不重启进程
- 设置页移除两个旧卡片（内容已迁出，避免重复入口）：「键盘外观」卡片（滑杆迁入新页）、
  「分类词库」卡片（入口改为上方按钮）
- 清理：`SettingsActivity` 删除 `btnDictManager` 与四个滑杆的字段 / 绑定 / 两个 init 方法及 `SeekBar` 导入；
  删除不再引用的字符串 `dict_entry_title` / `dict_entry_desc` / `dict_entry_open`；
  Manifest 注册新页（`exported=false`）
- 新增字符串：`dict_entry_button` =「补充短语词库」、`key_appearance_entry` =「按钮圆角间隙」
- 注：本次同时入库用户手改的 6 处说明文案（自动唤起键盘 / 加更多生僻字 / 用户词频学习 /
  追加候选词语 / 拼音输入方案 / 键盘主题配色 的说明小字）

## 验证
- `testDebugUnitTest` 39 类 / 373 例全绿；编译 + 签名 + 对齐 → `com.jinn.inputmethod.20260922000933.APK`（5.0 MB）
- ROOT 通道（`adb push` + `su -c 'pm install -r'`）覆盖安装 `Success`
- 真机逐项核对：设置页新增行两按钮就位；点「按钮圆角间隙」→ 新页正常（标题 / 说明 / 两滑杆 / 数值）；
  滑杆 tap → 落盘 `key_corner_dp=23.0` + 日志 `KeyAppearance: 键盘圆角: 23 dp`，
  再点回 → 精确恢复 1.0 dp（用户原值）；点「补充短语词库」→ 日志 `DictManagerActivity: 打开分类词库页`，页面正常
- 说明：本次未发版（如需发布可覆盖 `20260921`）。

---

# 输入法测试简化为纯编辑框 + 键盘外观页新增测试编辑框（2026-09-22）

需求（用户）：设置页首行的「点这里唤起键盘 + 发送 +（尚未收到任何文本）」太复杂，只保留编辑框；
键盘外观新页面同样加一个（复刻）编辑框，方便随时唤起 / 收起键盘对照调参。

## 改动
- 设置页：`edit_ime_test` 卡片简化为单一编辑框（`match_parent`；`imeOptions` 由 `actionSend` 改为 `actionDone`）；
  删除 `btn_ime_send` / `text_ime_received` 两个控件
- `SettingsActivity`：删除 `editImeTest` / `btnImeSend` / `textImeReceived` 字段与绑定、`sendImeTest()` 方法
  （含本地回显逻辑）、按钮监听与编辑框回车发送监听（`IME_ACTION_SEND`）
- 字符串清理：删除不再引用的 `settings_ime_send` / `settings_ime_received` / `settings_ime_received_empty`
- 键盘外观页：说明下方新增测试编辑框 `edit_key_appearance_test`（样式复刻设置页编辑框），
  点击即唤起 / 收起键盘；键盘弹出时滑杆仍同屏可见，可以对着键盘调
- 注：本次同时入库用户手改的 `favorite_entry_title`（收藏的自定义 → 符号的自定义）

## 验证
- `testDebugUnitTest` 39 类 / 373 例全绿；编译 + 签名 + 对齐 → `com.jinn.inputmethod.20260922001803.APK`（5.0 MB）
- ROOT 通道（`adb push` + `su -c 'pm install -r'`）覆盖安装 `Success`
- 真机截图核对：设置页首行只剩一个编辑框；键盘外观页编辑框就位，点它 → JinnIme 键盘弹出且滑杆同屏可见 ✅
- 过程记录：首次编译失败（漏删编辑框的 `IME_ACTION_SEND` 回车监听 → `Unresolved reference: editImeTest / sendImeTest`），
  已补删并复编译通过
- 说明：本次未发版（如需发布可覆盖 `20260921`）。

---

# 今日改动复查：lint 收敛 + 过期注释/文档订正（2026-09-22）

范围：`5ee6c82`（设置页两列网格）、`21ed71d`（词库/外观入口行 + 键盘外观独立页）、
`a117724`（输入法测试简化 + 新页编辑框）的累计影响。

## 一、功能与回归核查（未发现功能缺陷）
- 没有残留引用：`imeTest` / `edit_ime_test` / `settings_ime_send` / `settings_ime_received` /
  `btn_ime_send` / `text_ime_received` / `dict_entry_open` 全项目搜不到一处（发送链路清理干净）
- 入口只有一处：`DictManagerActivity` 只从新按钮「补充短语词库」进；
  `seek_key_corner` / `seek_key_gap` 现在只属于 `KeyAppearanceActivity`
- 与旧实现逐行对照一致：新页滑杆逻辑取自 `SettingsActivity.initKeyAppearanceCard`，搬迁未改动
  （定义域 / 换算 / 落盘 / 日志，连「先写初值再挂监听」的顺序也照旧）
- 真机实测：日志里能看到「新页调参 → 键盘下次弹出读到新值」（圆角 10dp → 3dp 两次变更都被键盘读到）
- 边界：钳位与换算仍只在 `KeyAppearance` 一处；新页不注册监听、不用 Handler，没有需要清理的状态

## 二、lint：30 → 22（优于 27 旧基线，净 −5）
- 修掉今天引入的 7 项：3×`DisableBaselineAlignment`（属性加错层级 → 移到父横排容器上）、
  3×`RtlSymmetry`（说明文字补 `paddingEnd`）、1×`UnusedResources`（删除已无引用的 `theme_mode_label`）
- 顺带清掉历史 1 项（NAS 卡片权重列的基线对齐）

## 三、注释与文档订正（全部为过期描述，非功能改动）
- `Prefs`：`keyCornerDp`「默认 0dp 直角」→ 1dp；`keyGapDp`「默认 0dp 无缝」→ 0.5dp
- `KeyAppearance`（3 处）与 `PinyinKeyboardView`（1 处）：「设置页」→ 键盘外观页（独立页标准）
- `AGENTS.md`：项目结构补登 5 个文件（`KeyAppearanceActivity` / `SymbolOrder(Activity)` /
  `FavoriteSymbols(Activity)`）；键盘外观入口段落改为「设置页 →『按钮圆角间隙』」；词库入口改「补充短语词库」

## 四、验证
- `testDebugUnitTest` 39 类 / 373 例全绿；`lintDebug` 0 error / 22 warning（较 27 基线 −5）
- `com.jinn.inputmethod.20260922002901.APK`（5.0 MB）ROOT 通道覆盖安装 `Success`
- 真机截图复核两列下拉 / 两组开关的排版与说明对齐，无视觉回归
- 说明：本次未发版（如需发布可覆盖 `20260921`）。

---

# 文档措辞平实化 + 发版 `20260922`（新 tag）

- 文档修订（只动措辞与过期事实）：`README.md`/`README_EN.md` 入口名同步（「输入方案」→「拼音输入方案」、圆角/间隙→设置页「按钮圆角间隙」、分类词库→「补充短语词库」）、APK 体积改约 5.0MB、模块表补 `KeyAppearanceActivity`/`SymbolOrderActivity`/`FavoriteSymbolsActivity`；`更新日志.md` 术语腔改直述、补「点这里唤起键盘」卡片已简化为纯编辑框注、去「彻底审查轮」式口号；仅处理新写段落与事实过期处，未大改历史
- 发版 `20260922`（跨天新建 tag）：提交 `0750ca2` 推双端 main；产物 `jinn-release.apk` 5,217,274 B / `sha256=64169420…` / `versionCode=20260922`；GitHub `gh release create 20260922`+附件、Gitee 新建 Release（id `1158697`）+附件（id `3242417`），正文取自 `.relnotes.md`；双端下载 `size=5,217,274`、`sha256` 三方一致 ✓

---

# 符号布局重建「延后」补重试 + 键盘外观页加 ScrollView（2026-09-22）

- 背景：上轮审查发现两项低危缺陷，先实机确证再修
- 确证：D1 延后无重试——收藏编辑页打一半拼音按 Home 出现「延后到下次创建视图」，再拉键盘收藏组仍是旧 `D I Y` 而 prefs 已新；同日证实框架跨会话复用同一键盘视图（`onCreateInputView` 2 次 vs 弹出 9 次），故「延后到下次创建自然生效」不发生；D2 外观页无滚动——横屏键盘遮滑杆，`uiautomator` `scrollable=0`
- 修复：`JinnIme` 新增 `pendingSymbolLayoutRebuild`（延后置位）+ 抽出 `recreateKeyboardView()`（换肤/符号重建共用、重建即清标记）+ `onStartInputView` 在 `applyThemeIfNeeded()` 后补一次重建（仍看 `hasPendingInput`）；注释场景改为「对话框打一半按 Home/锁屏」；`activity_key_appearance.xml` 外层包 `ScrollView`
- 验证：延后日志变「延后到下次弹键盘重建」、下次拉键盘即「补重建（上次延后）」、收藏组=`D I Y` ✓；横屏 `scrollable=1`、滑杆滚入可视区 ✓；门禁 39类/373例、`lintDebug` 0/22；APK `com.jinn.inputmethod.20260922101030.APK` 5.0MB ROOT 装；设备复原（旋转默认、临时符号删除、`favorite_symbols` 还原 `[["D","I","Y"]]`）；未发版

---

# Release 旧 APK 清理（2026-09-22）

- 按「Release 只留最新一版 APK」，删双端 6 个历史 Release 的 APK 附件：tag = 20260921/20260920/v20260919/v20260918/v20260917/v20260916
- 清理后双端一致：APK 仅 20260922（`jinn-release.apk` 5225466 B）；词库包 3 个（`dict_ext.txt.xz`×2、`opt_tencent.txt.xz`）保留（分类词库下载源、被 `OptionalDicts` 引用）
- 只删附件，Release 本体与 tag 保留；GitHub `gh release delete-asset` / Gitee `DELETE .../attach_files/{id}`
- 坑：Gitee 批量连续 DELETE 被拦成 HTML 404，单条重试返 204；逐个删、间隔 ~0.8s 稳定（已写入 `AGENTS.md` 发布规范）

---

# 界面文案平实化（2026-09-22）

- 逐条审查面向用户中文文本（`strings.xml` 全部 + 各 Activity 硬编码 + README），改写 8 处、修 3 处标点/格式：`strings.xml` 词库页（「按需下载…扩充输入覆盖范围」→「需要哪个下哪个，能打的词更多」、去「后自动重启…以生效」冗余）、Root 增强（「可选扩展：使用 Root 权限审计…」→「用 Root 权限检查数据目录是否安全（可选）」）、用户词频（「记住常选词,自动放前排」→「记住常选的词，下次放前排」+修半角逗号）、双拼说明（「双拼26键显示韵母」→「双拼时键面显示韵母」）、收藏编辑口语化、`settings_usage` 引号/全角逗号统一；`OptionalDicts` 去「含…等」模板、去「建议」；`ClipboardFirewall` 去误入 UI 的 Markdown 加粗；README 断句 + 修引用块双空格
- 真机：设置页/词库页（4 条新文案）/符号自定义页正常显示无溢出；门禁 40类/382例、`lint 0 error`；提交 `6a268d1`

---

# 编译警告 / lint 全量审查修复（2026-09-22 ~ 09-23）

- 范围：全量 `clean … --rerun-tasks --no-build-cache` 逐条回源码核实 **51 条 Kotlin 警告**（去重 26 源位置）/ **23 条 lint** 后修或不修
- 已修（Kotlin 警告 51→0）：`PinyinKeyboardView` 20 处+`SearchPanelView` 3 处 `lateinit`→`val`（构造期必初始化）、删随之 `isInitialized` 恒真守卫；两处多余 `?.`（lateinit 不提供保护）；`TextSelectionTest.range(focus)` 删未用参（12 调用同步）；`Clipboard/SearchPanelView` 的 `SimpleDateFormat` 静态缓存→`formatTime()` 即时构造（语言切换不沿用旧 Locale）；删无引用 `key_bg_rect.xml`+订正 3 注释
- 已修（2026-09-23）：`mipmap-anydpi-v26/ic_launcher.xml` 补 `<monochrome>` 层（麦克风剪影单 path，13+ 主题化图标复用 alpha），lint 20→19；删未用 id ×2（`edit_ime_test`/`edit_key_appearance_test`）；`settings.gradle.kts` `rootProject.name` `JinnVoiceIME`→`com.jinn.inputmethod`（`.idea/.name` 同步）；`build.gradle.kts` 注释 `<密钥目录>`→`JINN_KEYSTORE_ROOT`；删未用 import 5（`ViewGroup`/`LinearLayout`/`assertEquals`/`assertFalse`/`FileInputStream`）；`proguard-rules.pro` 删与 `-keep JinnIme` 重复规则；`.gitignore` 删 6 失效规则；`gradle.properties` `tooling.parallel` 注释改中文注「Gradle 9.4+ 才识别」
- 保留不改（有意设计/规则约束，列依据）：`ObsoleteSdkInt`（改名 AAPT 报 `resource mipmap/ic_launcher not found`、编译失败已回退）；`Overdraw`（半透明背板 plate 档载体）；`DrawAllocation`（`MicButton` 禁改域）；`GradleDependency`（锁版本守<5MB，当前 4.98MiB）；`SetTextI18n`×6/`TypographyDashes`（strings.xml 默认禁改）；`ScrollViewSize`/`SmallSp`/`UselessParent`/`InflateParams`/`ClickableViewAccessibility`/`MonochromeLauncherIcon`（既定布局/图标，后者已在 `ACTION_UP` 补 `performClick`）；informational 79/81 条（`SyntheticAccessor`38/`LogConditional`10/`SelectableText`7/`DuplicateStrings`2/`NewerVersionAvailable`2/`NegativeMargin`1，只作审查手段不写入仓库）
- 门禁：全量 100/100、Kotlin 警告 0、单测 40类/382例、lint 19 warning/0 error；release 5,216,890 B（4.98MiB）；真机装机 Success、桌面图标无回归、`FATAL`=0

---

# 发版 `20260923`（跨天新建 tag）+ 同日覆盖重发

- 产物 `jinn-release.apk` 5,225,466 B（4.98MiB）/ `sha256=39e08ff2…` / `versionCode=20260923`；门禁全量 100/100、Kotlin 0/错误 0、单测 40类/382例、lint 0/19；真机 ROOT 覆盖（`lastUpdateTime=2026-09-23 00:32`）、`FATAL`=0
- 双端发布：GitHub Release `20260923`、Gitee Release id `1160874`（附件 `3249560`）；删旧 `20260922` 附件（Gitee `3247655` HTTP 204）；校验双端 `size=5,225,466`、`sha256=39e08ff2…` 三方一致 ✓；提交 `e863516`（告警清理与订正，23 文件）推双端 main（`0c7ca31..e863516`）
- 发布修正（事故）：首次上传误用根目录遗留历史 `jinn-release.apk`（5,225,466B/`39e08ff2…`）而非本次产物（5,221,370B/`71a5e12b…`）；根因 `build_apk.py` 只产时间戳包、发布靠手工复制漏做；修正→`build_apk.py` 改「时间戳包+`jinn-release.apk` 同源双写」、重构建（`com.jinn.inputmethod.20260923095511.APK` ROOT 装）覆盖重发（GitHub `--clobber`、Gitee 删 `3249560`→新 `3252256`）；复核双端 `sha256=71a5e12b…`/`size=5,221,370B` 三方一致 ✓（GitHub CDN 首次拉取可能命中缓存，需加 query 复核）

---

# 键盘皮肤第一期（默认/磨砂/彩虹/极光/复古机械，2026-09-23）

- 需求：用户可选美化配色皮肤，APK<5MB；第一期 4 套+默认：磨砂/彩虹/极光/复古机械；磨砂玻璃（API31+ 真模糊）与霓虹留二期
- 实现：新增 `KeyboardSkin.kt`（参数+清单+`byId`/`visualFor`/`issues` 纯函数，自实现 HSV→RGB 便 JVM 单测）；`PinyinKey` 增皮肤绘制路径（渐变 shader 带缓存/描边/键帽厚度/字色提示色）；`PinyinKeyboardView` 固定顺序 `syncKeyboardSkin`→`applyKeyTransparency`→`applySkinToKeys`→`applyKeyAppearance`；功能/大写/删除/候选栏/背板统一 `skinToken()`、皮肤色并入 `alphaFaces`；`Prefs.keyboardSkinId`（未知归一默认）；外观页新增横向皮肤选择器（预览块 `PinyinKey` 自绘、零图片）
- 兼容：默认皮肤全空覆盖 ⇒ 与半透明键盘历史观感逐像素一致；体积 release 5,223,046 B（较上+1,676B，余量~19.8KB）、零位图零新增依赖；门禁 100/100、Kotlin 0、单测 41类/395例（`KeyboardSkinTest` 13）、lint 0/19；真机 ROOT 装、三态截图（彩虹/复古机械/默认）核对、`keyboard_skin` 落盘即生效
- 交互坑（登记代码注释+`AGENTS.md`）：`PinyinKey.onTouchEvent` 恒 true，预览块吃点击，须显式转回选项容器

---

# 皮肤实测缺陷修复（同日）

- 真机反馈：磨砂/极光下候选栏功能按钮/大写/删除/空格/回车文字图标不可读（背景改深色而文字仍亮白主题 `text_primary`/`text_secondary`，暗黑主题下不暴露）
- 修复：`KeyboardSkin` 新增 `functionGlyph`（功能键/候选区主文字图标色）与 `functionHint`（次要：空格小字/页码/候选提示）、4 套非默认补齐、`issues()` 纳入「填充类必须不透明」校验；`PinyinKeyboardView` 新增 `applySkinToTexts()` 统一套用 8 处动态构建点（候选词/预测词/符号标签/页码/候选提示/6按钮 label/hint/方向键/方向按钮非激活）；默认皮肤两项覆盖 null 回落历史；单测加「非默认皮肤必须给功能键文字色」
- 门禁 100/100、Kotlin 0、单测 41类/396例、lint 0/19；release 5,223,382 B；真机磨砂/极光下按钮文字图标均清晰 ✓、默认回归；误碰透明度滑杆（100%→0%）已精确还原 100%

---

# 皮肤覆盖面补齐：剪贴板面板 + 搜索面板 + 次级功能键（同日）

- 反馈：换皮肤后剪贴板历史面板没跟着变（仍是主题亮色）、底部次级功能键（符号/数字/逗号/句号/中英）深色皮肤下白底刺眼
- 修复：`ClipboardPanelView`/`SearchPanelView` 新增 `applySkin()`（面板底/列表底=`plate`、条目卡=`functionFill`、主/次文字=`functionGlyph`/`functionHint`、序号/分类选中=`accent`；条目卡在 adapter `getView` 逐次读皮肤色）；分类栏按钮面改运行时构建（选中=accent 实心、`applySurfaceAlpha` 顺带修「拖透明度后选中标记丢失」老问题）；`rebuildFunctionKeyBackgrounds` 次级键填充改 `skin.functionFill`、`applySkinToTexts` 同步 5 键文字；下发时机定在 `syncKeyboardSkin()`；`buildKeyFaceBackground` 加 `fillColor`、新增文件级 `skinColor()`；删失去引用 `key_bg_active.xml`、lint 回 19
- 门禁 100/100、Kotlin 0、lint 0/19、单测 41类/396例、release 5,223,570 B；真机极光/磨砂下键盘（含次级键深色面）与剪贴板面板观感统一、默认回归浅底白卡深字

---

# 键盘外观页极简紧凑版式 + 字符串审计（同日）

- 版式：只留大标题「键盘外观」、测试输入框、三行滑杆（标签「圆角/间隙/透明」与拖动条同行）、「皮肤」标题+皮肤一行五个；删副标题与全部说明（含「松手即时生效」）、三行由「标题独占+滑杆+数值」改「两字标签+拖动条+数值」同行；皮肤行改按权重等分（预览 56×44→42×34、名 11→10sp）；`KeyAppearanceActivity` 皮肤标题改「皮肤」
- 字符串审计：150 `string`+6 `string-array` 全部有引用（含 `R.array.*`，初版漏查已复核）；删因改版失引用 `key_appearance_desc`/`key_transparency_desc`/`key_appearance_apply_hint` 三字符串及两套色板 `key_appearance_fg_dim`；`ThemeColorParityTest` 原「透明度标题与定义域一致」前提随版式作废→改守「三滑杆标题只两字不带范围」（`KeyTransparencyTest`/`KeyAppearanceTest` 定义域守卫仍在）
- 门禁 100/100、Kotlin 0、lint 0/19、单测 41类/396例、release 5,222,790 B（-780B）；真机截图确认版式生效

---

# 皮肤标签统一两字

- 「复古机械」→「复古」，五套（默认/磨砂/彩虹/极光/复古）全两字、等分一行不挤压
- 守卫 `KeyboardSkinTest.皮肤标签一律两字`（新增皮肤三字以上即失败）；门禁 100/100、Kotlin 0、lint 19、单测 41类/397例

---
# 新增五套皮肤（共十套，2026-09-23）

- 标签统一两字后追加五套纯参数皮肤（零图片，+524B）：暗黑（近黑键面+冷白字）/ 霓虹（黑底+洋红发光描边、功能键青色描边+洋红底边厚度）/ 樱花（浅粉渐变+深枣字）/ 海洋（深青蓝渐变+浅水蓝字）/ 薄荷（浅薄荷渐变+墨绿字）
- 外观页选择器改多行：每行五个（`SKINS_PER_ROW`），十个排两行，末行不足补空占位；容器由横向改竖向不再横向滚动
- 守卫：`KeyboardSkinTest` 定义域校验（填充不透明/渐变不退化/描边与宽度成对/功能键字色齐备）在接入即拦下一处错误（霓虹底边厚度带 alpha→改不透明）
- 门禁 100/100、Kotlin 0、lint 0/19、单测 41类/397例、release 5,223,314 B；真机十套两行五列正常、霓虹/樱花切换观感与可读性通过

---

# 再增五套皮肤（共十五套）

- 新增：石墨（中性深灰+银灰蓝强调）/ 日落（深紫→暖橙渐变+暖白字）/ 岩浆（深红渐变+暖黄字+橙底边厚度；卷舌提示亮黄）/ 墨玉（墨绿渐变+玉白字+玉色微光描边）/ 皮革（棕调键帽+暖色缝线描边+2dp底边厚度+米白字）
- 清单按「深色/中性→深色彩色→浅色/暖色」重排三行五个
- 门禁 100/100、Kotlin 0、lint 0/19、单测 41类/397例、release 5,223,794 B（+480B）；真机十五套三行五列正常、日落/皮革观感通过

---

# 皮肤增至二十四套：选择器一行八个、名称入按钮

- 用户要求：按钮缩小、一行 8 个、行距尽量小、皮肤名从按钮下方移入按钮内（替「A」占位），三行共 24
- 新增九套（纯色值）：钛金/夜蓝/夜空/紫晶/雪原/青瓷/蜜桃/抹茶/珊瑚
- 选择器：`SKINS_PER_ROW` 5→8（屏宽÷8 等分）、行距 6→2dp；预览键 42×34→等分宽×30dp、圆角 8→6；去「占位A+下方名称」，名（两字）直接写进预览键（`centeredStyle`+`fitTextSize` 自适应字号）
- 门禁 100/100、Kotlin 0、lint 0/19、单测 41类/397例、release 5,224,394 B（+600B）；真机三行八列 24 按钮、名称在按钮内清晰

---

# 皮肤选择器按键面亮度排序（从亮到暗）

- 排序改按 `faceBrightness()`（BT.601 感知亮度，键面首/次色均值）降序；默认皮肤不覆盖键面色→由调用方传当前主题 `R.color.kb_key` 兜底；彩虹按起始色相实际生成色计算；纯函数+稳定排序，新增皮肤自动落位
- 守卫 `KeyboardSkinTest.展示顺序按键面亮度从亮到暗`（相邻非递增即失败）
- 顺序（三行八列）：行1 默认·雪原·樱花·蜜桃·青瓷·抹茶·薄荷·珊瑚；行2 复古·彩虹·钛金·日落·皮革·磨砂·石墨·紫晶；行3 海洋·岩浆·夜蓝·墨玉·极光·夜空·暗黑·霓虹
- 门禁 100/100、Kotlin 0、lint 0/19、单测 41类/398例、release 5,224,726 B

---

# 皮肤代码审查与修复（D1–D4 复核 + 6 项新缺陷）

- 范围：今日皮肤系列提交（`313d442..HEAD`）+ 未提交 D1–D4 修复
- D1–D4 复核通过：`ClipboardPanelView.surfaceAlpha` 上移 `init` 前、`applyTabFaces()` 遍历全部 `tabButtons`；候选栏「拼音回显/✕」走 `skin.functionHint`；红色语义走 `skin.hintRed`；大写锁定 shift 图标用 `accentOnColor`
- 6 项新缺陷修复：① 彩虹按压态闪成主题深块（`visualFor` 判据 `keyFill==null`→`isDefault`）；② 皮肤+透明度时候选栏被压成背板档（`plateRgb` 移除 `skin.candidateBar`，档位交 `updateCandidateBarBackground`）；③ 方向面板 ◉ 激活态字色未随 accent（`setSelectionActive` 用 `accentOnColor`）；④ 中英切换键高亮写死 `aurora_purple`→`skinToken(skin.accent,...)`；⑤ 皮肤绘制每帧分配（`PinyinKey` 复用 `faceRect`、shader 判据改 5 数值字段）；⑥ `plateFaceAlpha` 声明晚于 `init`（赋值被属性初始化器重置→上移）
- 守卫 `KeyboardSkinTest.按压色分支不因缺keyFill而回落主题色`；门禁 100/100、Kotlin 0、单测 41类/400例、lint 19、release 5,224,730 B；真机极光+透明度100%：无内容走背板档、有候选走内容面档且清晰（修复前全透）；设备复原 `amethyst`/0

---

# 皮肤刷新链二次复核与补齐

- 上轮 6 项（F1–F6）逐条证伪后全维持真实缺陷；新发现并修复 2 项（同属换肤/改透明度后未重刷）：
  - 中英键高亮 span 不更新：`applySkinToTexts` 只改 base color，`buildLangLabel()` 的 `ForegroundColorSpan` 仍旧→该方法末尾重建 `btnLang.text`
  - 方向面板 9 键不更新：`ensureDirectionPanel` 复用面板对象键面/字色停留构建期→新增 `applySkinToDirectionPanel()`，`showDirectionPanel()` 每次打开重刷
- 排除非缺陷：`gradientShader` 判据不含位置（26 键会话内位置固定）
- 门禁 100/100、Kotlin 0、单测 41类/400例、lint 19、release 5,224,890 B；真机方向面板 9 键随紫晶皮肤、中心键 ● 可见、候选栏「方向」显红「返回」；装机 Success

---

# 自身改动自查（其一）：刷新时机修正

- 复核 8 处改动（F1–F6 + 2 处刷新补齐）：全部正确；发现 1 处刷新时机不当：`applySkinToDirectionPanel` 原插在 `panel.tag` 后（`selectionActive=false` 前）→进入面板先按进入前拖选态刷中心键再被回正，多一次反向重绘；已移到状态归零后（紧邻 `refreshDirectionButton`）
- 非缺陷确证：`gradientShader` 判据不含键位置（`faceRect` 本地坐标，尺寸判据完备）；进入方向面板直赋 `selectionActive=false` 由 IME `clearSelectionState()` 回传 `setSelectionActive(false)` 跑通
- 核实：`refreshKeyLabels` 不设 base color（与 span 重建无冲突）、`refreshAppearance` 换肤链不含 `refreshKeyLabels`；24 套皮肤 `candidateBar` 与 `plate`/`functionFill`/默认令牌无撞色
- 验证：单测 41类/400例、Kotlin 0、装机 Success；真机方向面板中心键 ● 与状态一致

---

# 改动自查（其二）：测试补齐与风险清单

- 补 2 条守卫（`KeyboardSkinTest`，402例）：`候选栏色不得与背板或功能键面撞色`（遍历全皮肤断言 `candidateBar ∉ {plate,functionFill}`）；`未声明按压色时默认皮肤走令牌_皮肤按首色推导`（默认令牌/`darken(首色,0.88)`）
- 边界/异常核对（无需改动）：`darken` 内 `factor.coerceIn(0,1)`+逐通道钳位；`plateAlpha=1−p/100`、`surfaceAlpha=1−p/100·(1−MIN)`（0% 两档同为1f、100% 有区分）；按压推导变更只影响 rainbow 一套；`applySkinToTexts` 调用点唯一不在 init 路径；`applySkinToDirectionPanel` 全程 `as?`+`?: continue`
- 兼容：默认皮肤全空覆盖（含 accent）→中英键 span 回退 `aurora_purple`，历史观感不变
- 回滚：`git reset --hard 066d466~1` 可整体回到今日改动前（三提交均未推送）；单独回退需手工

---

# 主题与皮肤解耦 + 涟漪随皮肤

- 文案：`theme_card_title`「键盘主题配色」→「界面明暗切换」；`key_appearance_entry`「键盘主题皮肤」→「键盘配色皮肤」（原均以「键盘主题」开头造成重叠）
- 涟漪随皮肤明暗：新增 `KeyboardSkins.rippleOn(base)`（亮底10%黑/暗底30%白）；`buildKeyBackground`/`buildButtonBackground`/`buildKeyFaceBackground` 全改走推导，默认皮肤仍走令牌（随主题），语音面板（禁改域）不传参保持原样
- 皮肤排序：`orderedForDisplay` 改默认固定首位不参与排序，其余仍按亮度从亮到暗（默认不覆盖键面色，亮度随主题漂移）
- 守卫 `展示顺序默认固定首位_其余按键面亮度从亮到暗`、`涟漪色按底面明暗取相反侧`；单测 403例、lint 19；真机设置页显示「界面明暗切换」「键盘配色皮肤」、外观页「默认」首位

---

# 设置页移除「Root 增强模式」

- 背景：该开关仅做数据目录安全审计（Root 权限体检），与日常使用无关
- 移除：剪贴板卡片开关+状态文本（布局+逻辑）、`ClipboardFirewall.kt`（整文件）、`ClipboardPrefs.rootEnhanceEnabled`+`root_enhance` 键、5 条文案、`Diagnostics` 对已删代码注释引用
- 保留：`MANAGE_EXTERNAL_STORAGE`（诊断日志直写仍在用）；加密/历史上限等不变；旧 `root_enhance` 偏好键留本机不再读
- 门禁 Kotlin 0、单测 41类/403例、lint 19；真机设置页剪贴板卡片只余「历史数量上限 500」；装机 Success

---

# 存储权限清理：日志不落共享存储，诊断包改走系统文件选择器

- 背景：`MANAGE_EXTERNAL_STORAGE` 高危且与输入法职责不匹配，原仅「写日志到共享存储方便取出」，有零权限等价方案
- 移除：Manifest 声明+`xmlns:tools`；`Diagnostics` 共享存储直写分支与 root `appops set … allow` 授权（`makeWritable`/`grantPublicDirViaRoot`）；文案「导出诊断包到内部存储」→「导出诊断包」
- 改造：日志统一写应用专属目录 `Android/data/<pkg>/files/logs/`（零权限）；导出改「缓存目录打包→系统文件选择器（SAF，`ACTION_CREATE_DOCUMENT`）用户自选位置→写完删临时包」；提示显示文件名（部分 provider `lastPathSegment` 是文档ID无意义）
- 结果：权限由 4 条降至 3 条（INTERNET/ACCESS_NETWORK_STATE/RECORD_AUDIO），不申请任何存储权限
- 门禁 Kotlin 0、单测 41类/403例、lint 19；真机点导出→系统「保存到」页→下载目录存 `jinn-diagnostics-20260923-143247.zip`（112,630 B）；提示「已导出：<完整文件名>」

---

# 修复导出中断与排障脚本路径

- 导出进程回收后静默中断（实测复现）：选择器打开期间进程被回收→回选择器保存时临时包引用已丢、回调提前返回→目标留 **0 字节空文件**、无提示、缓存包残留。修复：新增 `Diagnostics.latestBundle()`（按最近生成时间在缓存目录兜底定位）；SAF 回调改「重建后仍继续写入；取消则删包恢复提示；包不存在明确提示『导出被中断，请重试』」
- 排障脚本路径失效（存量）：`tools/bug_capture.ps1`/`bug_clear.ps1` 写死旧目录 `/storage/emulated/0/JinnIme/logs`（实测空，日志早已在应用专属目录）。修复：两脚本改多候选探测（应用专属优先、旧共享兜底）
- 验证：杀进程后保存→目标 **113,789 B**（修复前 0 B）、缓存 `total 0`（修复前残留 113,301 B）；取消→「用户取消导出（清理临时包）」+缓存 `total 0`；脚本命中新目录、UTF-8 解析通过；门禁 Kotlin 0、单测 41类/403例、lint 19；装机 Success

---

# 发版记录：20260923 覆盖轮

- 推送双端 `main` 至 `59a5a73`（`e2dc888..59a5a73`）三方一致
- 发布包 `jinn-release.apk` 5,229,506 B（4.99 MiB）/ `sha256=2a2b239efb7412f5996fbe017ed345e5338033805ca618c297b5cfa6e71b520b`；V1=false/V2=V3=true、4 字节对齐
- Release 附件覆盖：GitHub `--clobber`；Gitee 删旧 `3253629`→新 `3254175`
- 校验：本地/GitHub/Gitee 三方 sha256 一致；正文改「本版（覆盖发布）→ 覆盖轮变更 → 覆盖轮验证 → 首轮记录（逐字保留）」

---

# 2026-09-23：用户配置导出与导入（跨设备迁移）

### 备份包格式与范围
- 包格式（`ConfigBackup`）：zip = `manifest.json`（格式标识/版本/来源/时间/设备 + 各节 sha256）+ `prefs.json`（两 SharedPreferences 白名单键带类型标记）+ `user_freq.txt` + 可选 `clipboard.jsonl`（明文）+ 可选 `dicts/*.xz`
- 范围：`Prefs` 全 26 键 + 剪贴板 2 键 + 用户词频 + 剪贴板历史 + 可选词库；索引缓存（派生）、诊断日志（非配置）不迁移；`update_last_check_at` 后经用户澄清也纳入（全 26 键）。`PrefsBackupCoverageTest` 源码对拍每个 `KEY_*` 必进导出+导入白名单、键名不重
- 剪贴板为何明文：密文本机 Keystore 保护、跨设备无法解密 → 只能明文迁移；导出默认不勾、导入需显式勾选；单节导出预算 12MB / 读入 16MB（UTF-8 明文字节）

### 加密与密码（整包加密、中文密码）
- 算法 `ConfigCrypto`：PBKDF2-HMAC-SHA256 **600,000 次**派生 AES-256-GCM，每包独立 16B salt+12B IV、GCM tag 128 位；头部（magic `JINNENC1`/版本/迭代数/salt/IV）只含公开参数，迭代数入头部 ⇒ 将来调高强度仍能解旧包
- 密码规则：**仅 Unicode HAN 汉字、4~64 字、不许同字重复**；本机不存密码。输入框为**明文输入框**（`NO_SUGGESTIONS`+`IME_FLAG_NO_PERSONALIZED_LEARNING`）—— 密码型会在部分 ROM 唤起系统安全键盘打不出汉字
- 防破解/误判：错误提示恒「密码错误或文件已损坏」；解密用 `Cipher.update/doFinal` 手写循环**不用 `CipherInputStream`**（后者可能吞 AEAD 异常放出短一截的包）；明文中间件用完即删。迭代数补**下限 10 万**（`iterations=1` 曾可解）；`ConfigCryptoTest` 15 例

### 导入模式与合并语义
- 两种模式：**覆盖还原**（全量写回设置）/ **仅并入数据**（只合并词频与剪贴板不动设置）
- 词频「同词取较大权重 + 较新天、不叠加」（两侧先按天衰减再合并免翻倍）；剪贴板按 `content_hash` 跳过本机已有、批内去重、按原时间落库；导入按阶段 `runCatching` 返 `failedStage`**部分完成如实报告**（顺序写入不回滚、重试幂等）
- 完整性：导入前逐节 sha256 校验，不符即整包拒绝；词库先提取到临时名校验通过才改名；聚合摘要**仅勾选导入词库时校验**，白名单内单文件内容不符按**跳过**（条目名含分隔符/条目数>16/总量>64MB/读取异常仍整包失败）

### 加固与修复（多轮审查，重点消除静默失败）
- 修复真缺陷：① 返回键取消导入 → 明文 zip 残留（补 `setOnCancelListener`+`onDestroy` 清理）；② 导入词频被运行期写盘覆盖（新增 `UserFrequency.replaceFromBackup` 写盘锁内落盘+换内存态、`load(enabled=false)` 清内存态与脏标记）；③ 设置页三处后台线程无兜底（整体 `runCatching`）；④ 剪贴板导出分页漏导（`nextOffset` 是否前进判结束）
- 静默失败全消除：`readEntry` 改三态 `SectionRead` 超限拒整包；结果展示忽略/跳过数；剪贴板超限中止导出给原因；词频/prefs 字节闸补齐；manifest 未登记摘要 ≠ 静默跳过
- 数据正确性：词频天差用 Long（防 `Int.MIN_VALUE` 回绕永久霸榜）；写盘 `renameTo` 直接覆盖（免文件消失窗口）；`dirty` 加版本号仅版本未变才清；导入前 `Prefs.flush()`
- 攻击面（红队）：词库**白名单**（清单外不落盘）+ **内容校验**（与 `OptionalDicts.checksum` 一致不符跳过）+ **总量闸 64MB**（旧 16×64MB 先落盘再校验）；`zipEntryNames` 512 上限 + `hasDictEntry` 短路；`favorite_symbols` 导入归一+限长（主线程解析防大 JSON ANR）；`manifest.device` 截断压平；解锁读流 90s 预算 + 防 read 返回 0 空转
- 生命周期：导入中 `importInFlight` 防 `onDestroy` 删正在读的明文 zip；三对话框存字段 + `onDestroy` dismiss 防 WindowLeaked；重启前 `Prefs.flush()`+`ClipboardPrefs.flush()`；`pendingNotice` 承接重建期间后台结果
- 性能：manifest 置 zip**首条目**（免每次扫 manifest 全解压）；导出三节各一次 `toByteArray`；`tempInFlight` 集合 + 60s 最小年龄（替原 10 分钟窗，防崩溃残留明文 zip 活过会话）；`.restore` 纳入清理且失败留日志

### 测试与可测性
- `ConfigBackupZipTest` 8 例（zip 主路径 JVM 守卫：打包→读回/Missing/TooLarge/Failed 三态/损坏/摘要绑定/条目上限/分类归一）；变异验证：临时改 `restoreDicts` 为 `(0,0)` → 6 条词库测试全红、删具体逻辑只对应 2 条红（细粒度定位）
- `PrefsBackupCoverageTest` 键名正则放宽 + 精确 26、类型键与取值工具配对（`KEY_PORT` 写成 `asString` 会红）；`FAST_ITERATIONS` 提 10 万对齐下限；新增双 prefs 往返/极值/emoji/畸形输入/剪贴板上限/迭代数下限/原子写覆盖等

### 默认界面值调整（用户指定）
- 圆角 1dp→**5dp**、间隙 0.5dp→**2dp**、透明 0%（不变）、皮肤→**紫晶**（新增 `KeyboardSkins.INITIAL_ID` 与 `DEFAULT_ID` 分开，坏值回退不冲突）；主题默认仍跟随系统

### 交互反馈
- 模态进度框（派生 ~1~2s 不可取消、页面销毁兜底关防 WindowLeaked）；结果弹框（成功/失败/解密失败带「重新输入密码」）；密码规则错误红字气泡+Toast 双重；取消也有提示

### 交付级工程陷阱（写入 AGENTS）
- `assembleRelease` 可能报 `compileReleaseKotlin UP-TO-DATE` 交付旧包；判据**不要只看字节数**（R8 吸收改动连续多轮 size 相同），读 APK 内 `classes.dex` 文本搜本次**新增字符串常量**（方法名会被混淆假阴性），确认 UP-TO-DATE 而源码已改时 `gradlew clean` 重建
- 注释里避免 `/*` 序列（Kotlin 块注释嵌套会吞后续代码编译失败）；改完必须跑门禁

### 验证
- 门禁 **45 类 / 472 例全绿**、Kotlin 警告 0、lint **0 error / 23 warning**；包 5,213,029 B（4.97 MiB）；真机（PACM00）逐项：导出/导入/MERGE/RESTORE、清理窗、词库残留清理、密码框调起本输入法（非安全键盘）、dex 判据命中（`HHmmss-SSS`/`textLen=`）；ADB 无法输中文故「中文密码导出→导入」由真实算法单测覆盖

---

# 发布记录 20260924（跨天新建 tag）

- 提交 `311aecb`（31 文件 +4987/−88）推双端 main（`0c617e3`→`311aecb`）；首提交误带临时信息文件已 `git reset --soft` 修正重提
- APK `versionCode=20260924`、5,213,029 B、`sha256=05CF1476…2A7F`、V1=false/V2=V3=true、4 字节对齐；全量 `build_apk.py --clean` + dex 判据复核（`HHmmss-SSS`/`textLen=` 命中）规避 UP-TO-DATE 陷阱
- 双端**新建** Release `20260924`：GitHub `gh release create … --latest`、Gitee `POST /releases`（id `1163187`）+ 附件（id `3258954`，5,213,029 B）；正文 **≤10 行**（初版误写 130+ 行已重写为 5 行指路本文件）
- 旧版清理：删双端 `20260923` 的 `jinn-release.apk`（Gitee `3254175` 返回 204）；词库包不受影响
- 校验：双端下载 `HTTP=200`、`size=5213029`、sha256 三方一致（`05CF1476…2A7F`），以服务端 `Content-Length` + 重下 sha256 为准（GitHub 当晚 API 抖动重试 + 续传）

---

# 文案清理与 Release 正文精简 20260924

- 全仓 35 文件去掉自述工作过程的话（章节标题去次序编号、正文直接叙述，纯文字替换）；Release 正文统一「本版新增/修复/验证 + 详细变更见更新日志」，双端 12 个 Release 均 ≤7 行（最长曾 179 行）
- `AGENTS.md` 补「正文不超过 10 行」；保留「提示词」（是设置页功能名 `settings_prompt` 非自指）
- 门禁 45 类 / 472 例全绿；lint 0 error / 23 warning

---
