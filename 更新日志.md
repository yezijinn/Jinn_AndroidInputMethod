# 更新日志

> 本轮为**接手后的第一批修复**，严格避开语音模块（MicRecorder / AsrClient / Protocol /
> MicButton 以及 JinnIme 中的录音、WebSocket、识别结果处理链路均未改动）。
>
> 日期：2026-09-15 ｜ 基线提交：`941ca68` ｜ 验证：`testDebugUnitTest` BUILD SUCCESSFUL

## 总览

| 类别 | 数量 |
|---|---|
| 修复 P0（严重）问题 | 3 |
| 修复 P1（中等）问题 | 3 |
| 修复 P2（轻微）问题 | 3 |
| 改动文件 | 9 个源码 + 2 个配置 |
| 新增文档 | `ONBOARDING.md`、`计划任务.md`、`更新日志.md` |

所有改动**仅保证编译通过 + 单元测试通过**，UI 手感类改动需真机复验（见文末）。

---

## P0-1 修复拼音键盘按键按压反馈失效

**问题**：`PinyinKeyboardView.bindLetterKeys()` 给每个字母键设置 `OnTouchListener`，
而 `handleKeyTouch()` 所有分支一律 `return true`。Android 事件分发中监听器返回 true 即消费事件，
`PinyinKey.onTouchEvent()` 永不执行 → `pressed` 恒为 false、按压高亮是死代码、
`performClick()` 不触发。用户按字母键看不到任何视觉反馈。

**改动**：
- `PinyinKey.kt`：新增公开方法 `setPressedVisual(Boolean)`，内部比较后 `invalidate()`。
  命名刻意避开 `View.setPressed`，不复用系统按压态（会被父容器重置）。
- `PinyinKeyboardView.kt`：`handleKeyTouch()` 在 `ACTION_DOWN` / `ACTION_UP` / `ACTION_CANCEL`
  三个分支显式调用 `keyViews[c]?.setPressedVisual(...)` 驱动按压态。

**为什么这样修**：退格键用的是 `btnBackspace.isPressed = true`（系统按压态 + 选择器），
不受影响；只有字母键走了自定义触摸监听，所以只改字母键路径，不改动事件分发语义，风险最低。

---

## P0-2 补齐剪贴板隐私分类功能

**问题**：`is_private` 在 `ClipboardDb` 中已完整实现（建表、upsert、去重合并标记、
`deleteAll` 豁免），但 UI 层**完全没有入口**——分类栏只有 4 个 Tab、长按菜单只有收藏/删除、
没有任何明文掩码。而 `AGENTS.md` 明确承诺「隐私分类」与「隐私内容默认隐藏明文」。

**改动**：

数据层 `ClipboardDb.kt`
- `whereClause()` 增加 `privateOnly` 参数；`count()` / `recentPage()` 增加同名默认参数
  （默认 false，现有调用点零改动）。
- 新增 `setPrivate(id, Boolean)`：**隐私只能由用户主动标记**，数据层不提供任何自动判定入口。

独立页 `ClipboardHistoryActivity.kt`
- 新增「隐私」分类 Tab 与 `CATEGORY_PRIVATE = "PRIVATE"` 伪分类常量（与 `FAVORITE` 同构，
  是独立标签列而非 `category` 取值，传 SQL 前会转成 `privateOnly`）。
- 列表渲染：隐私条目默认显示掩码文案，点击一次展开明文（`revealedIds`），再点一次才粘贴。
- 长按菜单新增「标记为隐私 / 取消隐私标记」，走 `BackgroundIo` 后台线程。
- `runChunkedSearch` 的分类判定增加隐私分支。

内嵌面板 `ClipboardPanelView.kt`
- 同样新增「隐私」Tab、掩码与展开逻辑、长按操作条新增「隐私」按钮（`togglePrivate()`）。
- 分页查询同步支持 `privateOnly`。

资源 `strings.xml`：新增 `clipboard_cat_private` / `clipboard_mark_private` /
`clipboard_unmark_private` / `clipboard_private_masked`。

**注意**：隐私条目在列表里的 meta 行会显示「· 隐私」标记；`deleteAll` 原本就已豁免隐私条目，
现在这条规则真正可生效了。

---

## P0-3 修复粘贴状态机卡死与跨输入框误粘贴

**问题**：
1. `ClipboardHistoryActivity` 的 `pasting` / `pendingPasteItemId` 一旦 IME 不回广播
   （进程被杀、未处于输入态）就永久为 true，之后点任何条目都无响应——粘贴功能彻底卡死。
2. `JinnIme.pendingPasteText` 没有时效，`flushPendingPaste()` 在下一次 `onStartInputView`
   无条件提交，用户切换输入框或搁置很久后，旧文本会被粘到完全无关的位置。

**改动**：
- `ClipboardHistoryActivity.kt`：新增 `pasteTimeoutRunnable`（3s 超时）与统一的
  `resetPasteState()`；`requestPaste()` 发出广播后挂上超时，结果接收器与失败分支都走统一复位，
  `onDestroy` 一并移除回调。
- `JinnIme.kt`：新增 `pendingPasteAt` 时间戳与常量 `PENDING_PASTE_TTL_MS = 10s`；
  `flushPendingPaste()` 先判时效，过期则丢弃并记日志（连接仍无效时放回暂存但**保留原时刻**，
  不会因反复重试而无限续期）。

---

## P1-4 去掉每次按键的一倍冗余查询

**问题**：候选刷新时额外调用了一次 `PinyinEngine.queryWithCompletion()`，
而 `PinyinEngine.query()` 内部已经跑过补全召回，这次重复调用的结果只用于打一行诊断日志——
等于每次按键双倍查询开销。

**改动**：`PinyinKeyboardView.kt` 删除该重复调用，诊断日志改为复用 `query()` 已有的结果
（`partialSyllable` + 前 3 个候选），并降级为 `Diagnostics.v`（原来是 I 级，会刷屏）。

---

## P1-5 修复 Root 审计命令阻塞与假检查

**问题**：
- `su()` 只 `readText()` stdout，不排空 stderr。子进程 stderr 管道写满会阻塞，
  `find` 这类可能大量输出的命令会直接挂死；且 `waitFor()` 无超时。
- `checkBackupConfig()` **硬编码返回 ✓**，无论 `backup_rules.xml` 被改成什么样都显示「安全」。

**改动**：`ClipboardFirewall.kt`
- `su()`：后台 daemon 线程排空 stderr；`waitFor(10s)` 超时后销毁进程并返回 null。
- `isRootAvailable()`：同样加超时（用户未在 su 弹窗授权时会一直挂着）。
- `checkBackupConfig(context)`：运行时用 `resources.getXml(R.xml.backup_rules)` 解析实际生效的
  规则文件，确认 `jinn_clipboard.db` 被排除，未排除则报 ✗。
- 顺带删除 `checkExternalLeak(pkg)` 与 `audit()` 中未使用的参数（消除编译警告，不是未跟踪文件）。

---

## P1-6 删除操作改用可写数据库句柄

**问题**：`ClipboardDb.delete()` / `deleteAll()` 用 `readableDatabase` 执行写操作。
正常情况下 `getReadableDatabase()` 返回的就是可写实例所以没暴露问题，
但磁盘满等异常场景会退回只读句柄，删除会静默失败。

**改动**：两处均改为 `writableDatabase`。

---

## P2-7 修正与实现完全相反的注释

**问题**：`showClipboardPanel()` 的 KDoc 与初始化处注释都写「viewLetters 用 INVISIBLE、
零 layoutParams 修改、绝不 relayout」，而实际实现是「viewLetters 置 GONE、
contentArea 改成固定 162dp×2 高度、会修改 layoutParams」。注释与实现完全相反，极易误导。

**改动**：`PinyinKeyboardView.kt` 两处注释改为描述真实实现，并保留「layoutParams 类型必须匹配
父容器，用错会 ClassCastException」这条真正有价值的经验。

---

## P2-8 忽略疑似云凭证文件

**问题**：根目录 `ses_fb2d904cbffewGCRN4iuzDTDSg.json`（3.6MB）未被 `.gitignore` 忽略，
有误提交风险。

**改动**：`.gitignore` 新增 `ses_*.json`、`*service-account*.json`、`*-credentials.json`。
（文件本身未删除，请自行确认后处理。）

---

## P2-9 搜索防抖的滚动残留

**改动**：`ClipboardHistoryActivity` 新增 `scrollToken`，「滚回顶部」的 `post` 任务带上令牌，
快速切分类或连续输入时旧任务自动失效（对齐内嵌面板 `ClipboardPanelView` 已有的做法）。

---

## 构建环境改动（非功能）

| 文件 | 改动 | 原因 |
|---|---|---|
| `settings.gradle.kts` | 启用注释里预留的阿里云镜像（pluginManagement + dependencyResolutionManagement） | 本机直连 `dl.google.com` / `repo.maven.apache.org` 解析失败（guava、asm-util 等依赖找不到），镜像可正常拉取 |
| Gradle 8.9 | 手动安装到 `~/.gradle/wrapper/dists/` | Wrapper 下载 gradle-8.9 时 SSL 握手失败，改用腾讯云镜像下载后手动放置 |

> 镜像对国内网络是加速，且 Google/MavenCentral 仍在列表中作为兜底；
> 若你的网络可直连，把镜像行注释掉即可，不影响构建结果。

---

## 新增文档

| 文件 | 内容 |
|---|---|
| `ONBOARDING.md` | 接手指南：架构、目录、技术栈、IME 生命周期、核心逻辑、已知问题、风险、维护约束 |
| `计划任务.md` | 按严重度排序的任务清单（已修/待办/不修及原因） |
| `更新日志.md` | 本文件 |

---

## 明确未修（受「禁止修改语音」约束）

| 问题 | 原因 |
|---|---|
| `refreshConfig()` / `ACTION_CONFIG_UPDATED` 是死代码 | 唯一调用目标是 `asr.connect()` 重连，属语音链路。**已知影响**：设置页改服务端地址后 IME 不热更新，靠 `killProcess` 重启生效 |
| `AsrClient` / `MicRecorder` 零测试覆盖 | 语音模块，解禁后建议优先补 |
| VAD 静音阈值 120s 偏长 | 语音模块 |

---

## 验证与真机复验清单

```bash
.\gradlew.bat testDebugUnitTest   # BUILD SUCCESSFUL，7 个测试类 ~100 用例全过
```

以下改动**必须真机复验**（编译通过 ≠ 体验正确）：

1. **按键按压反馈**：按任意字母键应出现高亮，松手恢复；符号层横滑翻页仍正常。
2. **隐私功能**：长按条目 → 标记隐私 → 切到「隐私」Tab 能看到该条 → 列表显示掩码 →
   点一次展开明文 → 再点一次才粘贴；「删除全部」不应删除隐私条目。
3. **粘贴超时**：正常粘贴仍应成功并关闭页面；断网/杀 IME 进程后 3s 内应恢复可点击状态。
4. **剪贴板面板**：顶栏新增「隐私」按钮后共 8 个格子，需确认在窄屏上文字不被挤掉
   （若过挤，可考虑把「返回」改图标或缩短标签）。

---

# 第二轮补充（同日凌晨，继续按待办推进）

上一批任务完成后，继续处理待办清单里风险可控的项。同样未触碰语音模块。

## 补充修复 1：字母键抬起无命中判定（原 #13，体验问题）

**问题**：手指从 Q 滑到 W 抬起时，`ACTION_UP` 依然回调到 Q 的触摸监听器，
代码不做任何位置判断就直接 `onLetterPressed('q')`——用户看到的是自己按了 W，上屏的却是 Q。

**改动**：`PinyinKeyboardView.kt`
- `handleKeyTouch()` 的 `ACTION_UP` 分支增加 `isInsideKey()` 命中判定。
- 新增 `isInsideKey(key, event)`：用 raw 坐标（与 DOWN 记录的 `rawX` 同源）比对按键
  屏幕位置，边界外扩 8dp 容忍边缘抖动；拿不到布局信息时返回 true 保守退回原行为
  （宁可保留旧行为，也不能因为取不到坐标让用户按不出字）。

## 补充修复 2：剪贴板搜索减少无谓解密（原 #8）

**问题**：搜索时先把整库分块读出来（含解密），再在内存里判断分类。
被分类筛掉的条目同样付了解密成本，而解密是这条路径上最贵的操作。

**改动**：`ClipboardHistoryActivity.kt` 的 `runChunkedSearch()`
把分类 / 收藏 / 隐私下推到 SQL（`db.recentPage(offset, CHUNK, scope, favoritesOnly, privateOnly)`），
先过滤再解密。

**连带修正（易踩的坑，已处理）**：SQL 过滤后 `globalIndex` 变成「分类内偏移」而不再是全库偏移，
若序号基准仍用全库总数 `db.count()`，搜索结果的序号会整体算大。已把基准改为
**分类内总数** `db.count(scope, favoritesOnly, privateOnly)`，这也让搜索态与非搜索态
（用 `categoryTotal`）的序号语义终于一致。

## 补充修复 3：隐私掩码重新打开面板后未重置

**问题**：`ClipboardPanelView.revealedIds` 是内存态，但只在条目取消隐私标记时清理；
用户在面板里展开过某条隐私内容后关闭面板再打开，明文仍是展开状态——掩码形同虚设。

**改动**：`onPanelShown()` 中清空 `revealedIds`，每次重新打开都必须再点一次才能看到内容。

## 补充改进 4：词库内存与加载耗时采样

**问题**：29MB 词库常驻 IME 进程是低端机被 LMK 杀的最大嫌疑，但项目此前**没有任何实测数据**，
无法判断该不该优化、优化到什么程度。

**改动**：`PinyinEngine.load()` 前后各采样一次进程内存并写入诊断日志，同时记录加载耗时：

```
内存采样[词库加载前]: 已用=xxMB 堆上限=xxMB
词库加载完成: 音节=N 词语键=N
词库加载耗时: xxxms
内存采样[词库加载后]: 已用=xxMB 堆上限=xxMB
```

只读一次 `Runtime`（不触发 GC、不阻塞）。真机上 `cat` 当天日志即可拿到基线，
据此再决定要不要动 HashMap 预分配容量或改数据结构。**先量化再优化，不凭猜测改。**

## 本轮决定不修的两项（已记入计划任务）

| 项 | 理由 |
|---|---|
| 粘贴广播携带明文（#10） | 现有 `setPackage` + `RECEIVER_NOT_EXPORTED` 已阻断跨应用接收，明文只在同进程内传递。改成传 itemId + IME 侧异步查库会改动「粘贴」这条只有真机才能验证的核心链路，当前无法回归，风险收益不对等 |
| 候选栏每次重建 TextView（#14） | 重建的是 `new TextView`（无 inflate），内容变化必然重排，复用省不掉 measure/layout；而候选数量每次波动大、命中率有限，却要处理「普通/预测/符号组三种渲染分支样式不同」的复用污染问题。真要优化应整体换 RecyclerView，而非打补丁 |

## 验证

```
.\gradlew.bat testDebugUnitTest   →  BUILD SUCCESSFUL
```

本轮新增的真机复验项：
1. **按键命中判定**：Q 上按下、滑到 W 抬起 → 应上屏 W（或都不上屏），不应上屏 Q；
   正常原地点击不受影响（含贴边点击）。
2. **搜索序号**：切到「网址」分类后搜索，结果序号应从该分类总数往下数，而不是全库总数。

---

# 第三轮：改动自查（逐行复查 diff）

改了十几个文件却只有编译验证，不足以放心。逐行复查了全部 diff，修正自查发现的问题。

## 自查修正 1：隐私掩码文案过长

掩码文案原为「内容已隐藏，点击显示后再点一次粘贴」（18 字），而列表内容区是
**单行 + 末尾省略号**，会直接被截断成一截没说完的话。缩短为「已隐藏 · 点击显示」，
独立页（string 资源）与内嵌面板（硬编码文案）同步改一致。

## 自查修正 2：粘贴超时回调未走统一复位

`pasteTimeoutRunnable` 里是手写 `pasting = false; pendingPasteItemId = -1L`，
虽然结果相同，但绕过了 `resetPasteState()` 这个唯一复位口（将来往里加清理逻辑会漏）。
改为先取 id 打日志、再 `resetPasteState()`。

## 复查确认无问题的点（记录以免后人重复怀疑）

- **属性初始化顺序**：`pasteTimeoutRunnable` 声明位置早于 `pasting` / `pendingPasteItemId`，
  但 lambda 只在 3 秒后执行，届时属性早已初始化完毕，不存在 NPE。
- **Runnable 内的 `this`**：SAM 转换的 lambda 中 `this` 指向宿主 Activity，`Toast` 用法正确。
- **`whereClause` 参数兼容**：`privateOnly` 带默认值，`count()` / `recentPage()` 所有既有
  调用点（含 `count()` 无参重载）行为不变。
- **`isInsideKey` 的保守分支**：取不到布局信息（未测量/已分离）时返回 true 保留旧行为，
  不会因为拿不到坐标导致用户按不出字。
- **`handleItemClick` 的隐私分支**：在 `if (pasting) return` 之后执行，不会污染粘贴状态机。

## 当前状态

```
.\gradlew.bat testDebugUnitTest   →  BUILD SUCCESSFUL
```

**已修复 14 项**（含本轮 2 项自查修正），剩余待办均已在 `计划任务.md` 中登记，
其中需要真机才能安全推进的：词库内存实测后优化（#7）、三份重复列表 UI 抽取（#9）。

---

# 第四轮：推进 #9（抽取剪贴板筛选逻辑 + 补单测）

待办里剩下两项都标注着"需要真机"，其中 #9（三份重复列表 UI）一直没动。
本轮先把它里面**风险最低、收益最确定**的一块做掉，而不是整体重构。

## 抽取 ClipboardFilter：消除 5 处重复的「伪分类翻译」

**问题**：分类栏有 5 个 Tab（全部/网址/数字/收藏/隐私），但只有网址、数字是真正的
`category` 列取值，**收藏和隐私是独立的标签列**（`is_favorite` / `is_private`）。
把 `"FAVORITE"` 当 category 传给 SQL，条件就变成 `WHERE category = 'FAVORITE'`——
恒不成立，列表永远是空的。这个翻译原本在 5 处各写一遍：

```
Activity.loadFirstPage / Activity.loadNextPage / Activity.runChunkedSearch
Panel.refresh / Panel.loadNextPage
```

**改动**：
- `ClipboardDb.kt` 新增顶层 `data class ClipboardFilter(category, favoritesOnly, privateOnly)`
  与 `ClipboardFilter.of(raw)`，负责伪分类 → SQL 参数的翻译；同时定义
  `PSEUDO_FAVORITE` / `PSEUDO_PRIVATE` 常量。
- 上述 5 处全部改为 `ClipboardFilter.of(category)`，不再各写三行。
- 两个 UI 里的 `CATEGORY_FAVORITE` / `CATEGORY_PRIVATE` 常量改为引用
  `ClipboardFilter.PSEUDO_*`，消除 UI 与数据层各定义一份导致的漂移风险。

**为什么只做这一块**：adapter / 分页 / Holder 的渲染层重复确实还在，但那部分
改一处必漏两处、且没有真机无法回归；而筛选翻译是纯函数、零 UI 影响、还能写单测——
先做低风险的，把剩下的留给有真机的时候。

## 新增 ClipboardFilterTest（7 个用例）

覆盖：全部(null) / 网址 / 数字 / 收藏 / 隐私 / 两标记互斥 / 未知取值透传。
重点是钉死「收藏、隐私的 `category` 必须为 null」这条规则——将来谁改错了测试会立刻失败。

这也是本项目**唯一能对剪贴板数据层做的 JVM 测试**（其余依赖 Android SQLite），
补上了一小块此前为零的覆盖。

## 验证

```
.\gradlew.bat testDebugUnitTest   →  BUILD SUCCESSFUL
测试类 8 个，用例 161 个，失败 0，错误 0
```

## 累计

- 已修复 / 改进：**15 项**
- 单元测试：8 个类、161 个用例全绿（新增 1 个类 7 个用例）
- 剩余待办：#7 词库内存（需真机实测后再决定优化方案）、#9 渲染层重复（需真机回归）

---

# 第五轮：审查此前未细看的模块 —— 发现并修复一处隐私泄露（P0）

前几轮修的都是已登记问题。这轮主动去读还没逐行看过的模块，在 `SearchPanelView`
（键盘内嵌的剪贴板搜索面板）里发现一个**比之前所有问题都严重的隐私漏洞**。

## P0：搜索面板完全不处理隐私条目，明文直显且点一次就粘贴

**问题**：上一轮给历史页和内嵌面板补齐了隐私掩码，但**搜索面板漏了**。
它是剪贴板内容的第三个展示入口，而它的 `getView()` 是 `holder.content.text = item.content`
——被标记为隐私的内容在这里**明文直接显示**；`handleItemClick()` 更是点一次就
`onPaste(item.content)` 粘贴出去（正确行为应是点一次展开、再点一次才粘贴）。

后果很直接：用户在剪贴板面板里把某条标记为隐私 → 切到搜索面板 → 内容明文可见、
且一点就粘贴。隐私标记在这个入口形同虚设。

**改动**（`SearchPanelView.kt`，与历史页 / 剪贴板面板完全对齐）：
- 新增 `revealedIds`（内存态已展开集合）。
- 列表渲染：隐私条目默认显示「已隐藏 · 点击显示」掩码；meta 行补「· 隐私」标记。
- `handleItemClick()`：隐私条目第一次点击只展开明文，第二次才粘贴。
- `onShown()` / `onHidden()` 都清空 `revealedIds`——每次打开都必须重新点一次才能看到内容。

## P2：空态文案的无效赋值

`updateEmpty()` 里给空态文本赋了「找到 N 条」，但同一函数下一行就把空态设为 `GONE`
（列表非空时），导致这个值永远显示不出来。删掉无效分支，只保留空态文案。

## 复查确认（未改动，符合预期）

- 搜索面板**没有分类栏**，所以 `runSearch()` 全库扫描是设计如此，不是漏过滤。
- 剪贴板面板选了某个分类后再进搜索，搜索仍查全库——这与「搜索是全局入口」的
  定位一致，未擅自改成按当前分类过滤（若产品上希望跟随分类，需另行确认）。

## 验证

```
.\gradlew.bat testDebugUnitTest   →  BUILD SUCCESSFUL
```

**真机复验重点**：标记一条隐私 → 在搜索面板里搜到它 → 应显示掩码而不是明文，
且第一次点击只展开、第二次才粘贴。

## 累计

- 已修复 / 改进：**17 项**（本轮新增 P0 一项、P2 一项）
- 单元测试：8 个类、161 个用例全绿

---

# 第六轮：审查设置页（SettingsActivity），修复 4 处

上轮靠"读没细看过的模块"挖出了 P0，这轮继续审查最后一个没细看的大文件：设置页。

## P1：点「保存并重启」时，剪贴板数量上限可能不生效

`saveAndRestart()` 保存了 host / port / 语言 / 提示词 / 两个复选项，
**唯独没有保存剪贴板数量上限**——它平时依赖 `editClipboardMax` 的失焦回调保存，
而失焦回调的时序不保证早于本次保存（输入框仍有焦点时压根不触发）。
用户改完上限点保存，很可能发现没生效。

**改动**：`saveAndRestart()` 里显式调用一次 `saveMaxItems()`（幂等，重复保存无副作用）。

## P2：Root 增强开关递归触发，把「Root 不可用」提示覆盖掉

Root 不可用时，代码需要在回调里把开关拨回关闭；但改 `isChecked` **本身又会触发同一个
监听器**，递归进入 else 分支，把刚设置的「Root 不可用」提示覆盖成功能描述文案。
结果用户开启 Root 增强后，看到的不是失败原因，而是一段功能说明。

**改动**：新增 `suppressRootSwitchCallback` 标志，程序化拨动开关期间抑制回调；
监听器开头判断该标志直接返回。

## P2：导出诊断包回调未检查 Activity 已销毁

`exportDiagnostics()` 在后台线程打包，回调里直接操作 `btnExportDiag` / `textDiagDir`；
若期间用户退出页面，会操作已 detach 的 view。

**改动**：回调开头加 `if (isFinishing || isDestroyed) return@runOnUiThread`
（与同文件 `runRootKeepAlive` 里已有的防护保持一致）。

## P2：Spinner 初始化回调可能静默改写「默认键盘模式」

`spinnerDefaultMode` 的 `onItemSelected` 在 `loadPrefs()` 之前就注册了，
而 `loadPrefs()` 里的 `setSelection()` 会触发它。若 prefs 里的值不在 `values` 数组中
（配置损坏、或将来新增了模式），`indexOf` 返回 -1 → 退回第 0 项 →
**把用户的默认键盘静默改成第 0 项**（语音）。

**改动**：新增 `defaultModeSpinnerReady` 标志，`loadPrefs()` 完成后才置位，
回调在置位前直接返回。这是 Android Spinner 的标准坑，同类改动要一并注意。

## 验证

```
.\gradlew.bat testDebugUnitTest   →  BUILD SUCCESSFUL
```

**真机复验重点**：
1. 改剪贴板数量上限后直接点「保存并重启」，重启后上限应保持。
2. 未 Root 的设备上开启「Root 增强」，应看到「不可用」提示（而不是功能描述文案）。
3. 设置页反复切换「默认键盘模式」，杀进程重启后应保持最后一次选择。

## 累计

- 已修复 / 改进：**21 项**
- 单元测试：8 个类、161 个用例全绿
- 剩余待办（均需真机）：#7 词库内存实测后优化、#9 三份 UI 渲染层抽取

---

# 第七轮：审查拼音键盘剩余部分 —— 修掉一个数据丢失级 BUG

上两轮"读没细看过的模块"连续挖出真问题，这轮继续审 `PinyinKeyboardView`（1718 行）
里此前没看过的退格状态机、面板切换、搜索路由。三个问题都**逐行核实过代码**才动手。

## P0：退格「双击态」永不超时 —— 点两下退格后，任何长按都会清空整个输入框

**触发路径**：快速点两下退格（删两个字，极高频操作）→ 此后哪怕过了几分钟，
只要长按退格超过 800ms，就触发 `onDeleteAll()` **清空整个输入框，且不可撤销**。

**根因**：`recordBackspaceTap()` 在形成双击态（`backspaceTapCount = 2`）时
**没有更新 `backspaceLastTapAt`**，而 `handleBackspaceTouch()` 的 DOWN 分支
只判断 `backspaceTapCount == 2`、完全不校验时间。于是双击态会一直挂着，
只有"再短按第三下"或"长按一次（此时已经清空了）"才会解除。

**改动**：
- 双击成立时同步记录第二击时刻；
- DOWN 分支新增时间校验：只有双击后 `clearGestureWindowMs`（500ms）内再次按下才挂清空检测；
- 超过窗口则立即把双击态失效，杜绝长期挂着。

## P1：剪贴板面板异常后，26 键永久消失

`showClipboardPanel()` 先把 `viewLetters` 置 GONE、改了 contentArea 高度、显示了面板，
**之后**才调 `onPanelShown()`（内部走 DB 查询）。若这一步抛异常，`catch` 只置
`clipboardActive = false`，**不恢复视图**；而 `hideClipboardPanel()` 开头就是
`if (!clipboardActive) return` —— 于是再也恢复不了，每次点剪贴板都重复失败，
键盘只能靠重启 IME 救回。

**改动**：抽出 `restoreLettersLayout()`（关闭与回滚共用），catch 里做完整回滚
并通知 IME 面板已关闭。

## P1：切换搜索模式不清拼音缓冲，残留拼音隐性生效

`showSearchPanel()` / `hideSearchPanel()` 都不清理 `composing`。
拼音未上屏时打开搜索面板：残留拼音不可见但仍生效——按退格先删这些看不见的拼音
（要按 N 次才轮到搜索框），按空格则把上一次的候选词直接塞进搜索框。

**改动**：新增 `clearComposingState()`（清 composing / lastCandidates /
lastPredictions / lastCommittedWord），进出搜索模式都调用。

## 已发现但本轮未修（登记备查，需真机确认后再动）

由子代理报出、我尚未逐行核实或改动风险较高的项，先登记不盲修：

| 项 | 现象 | 状态 |
|---|---|---|
| 清空后连删不停 | `clearOnLongPressRunnable` 触发全清后未取消 55ms 连删 | ✅ 本轮已顺带修（加了 removeCallbacks） |
| `onPredictionSelected` 漏搜索模式判断 | 预测态点击可能把文本提交到宿主输入框而非搜索框 | 待核实 |
| 切层/切大写不清拼音串 | 输入拼音后切符号层上屏，再按退格删的是看不见的拼音 | 待核实 |
| 方向面板与剪贴板面板互斥只做一半 | 剪贴板打开时点「方向」，面板加进 GONE 容器导致按钮无反应 | 待核实 |
| 拖选态行首/行末跳全文首尾 | `TextSelection.nextFocus` 用 0/textLength，与光标态的 lineStart/lineEnd 不一致 | 待核实（涉及 JinnIme，需谨慎） |
| `capsMode` 生命周期不复位 | `configure()` 重置了其它状态唯独漏它，且大写锁定时中英切换被禁 | 待核实 |

## 验证

```
.\gradlew.bat testDebugUnitTest   →  BUILD SUCCESSFUL
```

**真机复验重点（本轮最该测的一条）**：
1. 快速点两下退格删掉两个字 → 等几秒 → 长按退格：应正常连续删除，**不能清空输入框**。
2. 真的想清空时：双击退格后**立刻**（0.5s 内）长按，仍应触发清空。

## 累计

- 已修复 / 改进：**24 项**
- 单元测试：8 个类、161 个用例全绿

---

# 第八轮：逐条核实并修复「待核实」清单

上一轮子代理报了 9 项，我修了 3 项、把另外 5 项列为待核实。这轮逐条读代码核实，
修掉 4 项，1 项判定为设计权衡维持现状。**仍然坚持先核实再动手**。

## P1：搜索态点「预测词」会把文本串进宿主输入框

`onCandidateSelected` 有 `isPanelSearch()` 分支（候选路由到搜索框），
但 `onPredictionSelected` **完全没有**——搜索模式下点预测词会直接
`listener.onCommitText(pred)`，文本被提交到正在聊天的输入框里，而不是搜索框。

**改动**：补上与候选一致的搜索分支。

## P2：大写锁定切换不清拼音串

`btnShift` 只切标志位。大写锁定实际是「字母直通」模式，切回小写后按退格会先删
切换前残留的拼音（用户想删的是刚打的大写字母）。

**改动**：切换时清 composing 与预测（与切中英文的处理一致），并刷新候选栏。

**未改（设计权衡）**：符号层 / 数字层切换**不清**拼音。因为符号层常用于输入中文标点，
之后还要继续拼音输入，清空反而会丢用户的输入。这与主流输入法行为一致。

## P2：方向面板与剪贴板面板互斥只做了一半

`showClipboardPanel()` 里有关方向面板，反向却没有：剪贴板打开时 `viewLetters` 整体 GONE，
此时调 `showDirectionPanel()` 会把面板加进一个不可见的容器，面板不显示、
但 `directionPanelVisible` 已置 true —— 用户点方向键毫无反应。

**改动**：`showDirectionPanel()` 开头若剪贴板面板打开则先关闭它。

## P2：拖选态「行首 / 行末」跳到全文首尾

`TextSelection.nextFocus` 的 LINE_START / LINE_END 直接返回 `0` / `textLength`，
而**光标态**（JinnIme）用的是 `TextSelection.lineStart / lineEnd`。
同一对按钮两种行为：多行文本里拖选时按「行首」会选到全文开头，
而用户预期是选到本行行首（对齐编辑器的 Shift+Home）。

**改动**：`nextFocus` 改为按当前行计算，与光标态统一。
同时新增多行测试用例锁定该行为（原有两个用例用的是单行文本，改后仍通过）。

## 判定为设计权衡、维持现状

| 项 | 理由 |
|---|---|
| `capsMode` 不在 `configure()` 中复位 | 大写锁定是用户显式设置的持久态；每次聚焦输入框都重置会让「锁定」失去意义。用户点一下 shift 即可退出，不构成困扰 |

## 验证

```
.\gradlew.bat testDebugUnitTest   →  BUILD SUCCESSFUL
测试类 8 个，用例 162 个，失败 0，错误 0（本轮新增 1 个用例）
```

## 累计

- 已修复 / 改进：**28 项**
- 单元测试：8 个类、162 个用例全绿
- 仍待真机：#7 词库内存实测后优化、#9 三份 UI 渲染层抽取

---

# 第九轮：性能瓶颈与边界排查

前几轮找的是功能缺陷，这轮专门查性能与边界。

## 性能 1：V 级日志全量落盘 —— 每秒最多 10 次小文件 IO

`Diagnostics.log()` 对所有级别一视同仁地写文件，而本类**不持有持久输出流**，
每条日志都是「open → write → close」。V 级恰好记录音频包收发这类
**每秒可达 10 条**的高频噪音，等于打字/说话期间持续做无收益的小文件 IO。

**改动**：新增 `VERBOSE_TO_FILE` 常量（默认 false），V 级只走 logcat、不落盘；
其它级别不受影响。需要完整落盘时把常量改成 true 即可。
logcat 侧始终保留（`adb logcat -s MicRecorder AsrClient`）。

**预期效果**：写文件次数大幅下降（音频包是最主要的高频来源），
日志文件体积也随之减小，排查时有效信息密度更高。

## 性能 2：音节前缀判断从 O(音节数) 降到 O(1)

`PinyinEngine.isTruePrefixOfSyllable()` 原实现**每次调用都遍历整个合法音节表**
（400+ 条）做 `startsWith`。而它在每次按键的查询路径上会被调用若干次
（分词、伪完整音节判定、补全召回）。

**改动**：加载时预建 `syllablePrefixes`（所有音节的 1..len-1 前缀），
判断改为一次哈希查找。行为完全等价（最长音节 6 字符 → 最长真前缀 5 字符，
原实现里 `s.length >= 6 → false` 的特判由集合天然覆盖）。

**预期效果**：单次按键省下数百次字符串比较，按键响应更轻。
`PinyinEngineTest` / `PinyinCompletionTest` 全部通过，确认语义未变。

## 缺陷 1：保活开关缓存为 false 后，再也感知不到用户重新开启

`JinnAccessibilityService` 用一个 `keepAliveCached` 缓存开关（避免高频事件反复读配置），
但 `onAccessibilityEvent` 开头就 `if (!keepAliveCached) return` —— **重读被挡在返回之后**。
于是缓存一旦为 false 就永远不会刷新：用户后来在设置页开启保活并成功启动服务，
可一旦该服务被杀，无障碍再也不会把它拉起来，**互保彻底失效**。
（原注释只考虑了 true → false 的方向。）

**改动**：把廉价过滤（事件类型、控件类型）提到最前，节流窗口外**无条件重读**开关
再决定是否拉起。缓存字段与 `prefsLoaded` 一并移除——节流已保证每 5s 最多读一次配置。

**预期效果**：开启/关闭保活两个方向都能在一个节流周期内被感知。

## 缺陷 2：白名单命令第一条失败，后面全部不执行

`RootShizuku.applyKeepAlive()` 用 `commands.all { runSu(it) }`，
而 `all` 遇到第一个 false 就**短路**。这几条命令（Doze 白名单、两个 appops、
`am set-inactive`）彼此独立，第一条失败（如 `dumpsys` 繁忙超时）会导致后三条一条都不跑。

**改动**：改为逐条执行完再汇总，日志输出成功条数（`成功 N/4 条`）。

**预期效果**：部分成功也生效，且失败原因在日志里可量化。

## 可维护性：修正 AGENTS.md 中与代码不符的内容

这份文件每次会话都会注入，错误会持续误导后续维护。已修正：

| 原文 | 修正 |
|---|---|
| 测试命令写死 `$env:LOCALAPPDATA\Temp\opencode\gradle-8.10.2` | 改为 `.\gradlew.bat testDebugUnitTest`，并补充镜像回退办法 |
| 项目结构缺 3 个模块 | 补上 `BackgroundIo` / `ClipboardPanelView` / `SearchPanelView` |
| 「设置页……保存并测试连接」 | 实际没有测试连接，改为「保存后杀进程重启 IME」 |
| 未记录语音模块禁改约束 | 在「约定」首条写明**禁止修改语音部分**及原因 |
| 未记录伪分类陷阱 | 补充 `ClipboardFilter` 说明与 `ClipboardFilterTest` 守卫 |

## 验证

```
.\gradlew.bat testDebugUnitTest   →  BUILD SUCCESSFUL
```

## 累计

- 已修复 / 改进：**33 项**
- 单元测试：8 个类、162 个用例全绿
- 仍待真机：#7 词库内存实测后优化、#9 三份 UI 渲染层抽取

## 性能优化效果（需真机确认的量化项）

| 优化 | 预期 | 怎么验证 |
|---|---|---|
| V 级不落盘 | 日志写入次数显著下降 | 对比优化前后 `jinn-YYYY-MM-dd.log` 同一使用时长的文件体积 |
| 音节前缀 O(1) | 按键响应更轻 | 用 `Diagnostics` 已有的词库加载耗时日志确认加载未变慢；按键延迟需真机体感 |

---

# 第十轮：真机实测与验证（构建 / 签名 / 部署 / 验证闭环）

本轮起真机可用（PACM00，设备号 `BIYLBAFQQSS8DA69`），把此前"只能编译验证"的项目
第一次跑成了完整闭环。

## 一、APK 构建与签名（按指定密钥库）

```
密钥库: C:\AI_WORKSPACE\GLOBAL\credentials\JinnKeyStores\com.jinn.inputmethod\release.jks
签名方案: V1=false V2=true V3=true（与项目约定一致）
产物: jinn-release.apk (10.6 MB)
安装: adb install -r → Success（覆盖安装，签名一致，无需卸载、不丢数据）
已装版本: versionCode=20260915
```

> `build_apk.py` 的密钥根默认是 `E:\JinnKeyStores`，本机实际在
> `C:\AI_WORKSPACE\GLOBAL\credentials\JinnKeyStores`，需用环境变量指定：
> `JINN_KEYSTORE_ROOT='C:\AI_WORKSPACE\GLOBAL\credentials\JinnKeyStores' python build_apk.py`

## 二、#7 词库内存 —— 实测结案（不必再猜）

**实测数据（真机 logcat）**：

| 指标 | 数值 |
|---|---|
| 词库加载前 | 已用 9 MB |
| 词库加载后 | 已用 **165 MB**（堆上限 384 MB） |
| 词库净占用 | **约 156 MB**，占堆上限 **41%** |
| 加载耗时 | **8424 ms**（后台线程预加载） |
| 词语键 | 1,048,342 |
| 反向索引 wordToPinyin | 1,162,486 |
| 合法音节 | 422 |

**分析结论**：

1. **原预分配是合适的**：`wordToPinyin = HashMap(1_500_000)` 经 `tableSizeFor` 得容量
   2^21 = 2,097,152，扩容阈值 1,572,864 > 实际 1,162,486 → **不触发扩容**。
   `phrasesByPinyin = HashMap(2_000_000)` 同理。**调参没有空间**。
2. 内存大头是百万级 HashMap 的 Node 对象 + 词条 String，属数据结构固有开销。
3. **唯一有效方向**是省掉 `wordToPinyin`（约 45 MB，占词库 29%）。但它是功能必需的：
   - `consumption()` 用它反查候选词拼音，计算应消费的输入区间（残码处理，核心逻辑）
   - `predict()` 用它把上个词映射回拼音再做联想
   要省掉必须让候选在生成时就**携带来源拼音**，即修改 `query()` 返回类型与
   `consumption()` 入参——**改动核心候选链路**。
4. **判定：不做**。风险收益不匹配：改动覆盖候选生成 / 消费 / 预测 / 测试全链路，
   收益 29% 内存；而当前设备堆上限 384 MB、占用 165 MB 仍有充足余量，无 OOM 压力。
   若将来换到堆上限 192 / 256 MB 的设备，再按「候选携带拼音」方案重构。

> 本轮最大价值是把结论**从猜测变成数据**：以前只知道"29MB 词库可能占内存多"，
> 现在是"156MB / 41% / 预分配无空间 / 唯一方向风险高"。后续不必重复调研。

## 三、退格 P0 修复的双向真机验证（本轮重点）

`adb shell input` 每次调用都是一次独立的 `app_process` 启动（200~400ms），
物理上无法凑进 280ms 双击窗口，因此**临时把窗口放宽到 3000ms 验证逻辑，验证后还原**。

| 测试 | 操作 | 期望 | 实测 |
|---|---|---|---|
| A（修复有效性） | 双击退格 → **等 3 秒** → 长按 1.2s | 不应清空 | ✅ 双击成立（日志 1 次），**清空 0 次** |
| B（功能未破坏） | 双击退格 → **立刻**长按 1.2s | 应清空 | ✅ 双击成立（1 次），清空触发（PinyinKeyboard + JinnIme 两条日志） |

测试 A 在旧代码下**必然清空**（`count==2` 时无条件挂清空检测），因此这条用例
能有效区分修复前后。验证完成后 `doubleTapWindowMs` 已还原为 280L 并重新构建安装。

## 四、其余两项改动

**KeepAliveService：前台服务提升失败不再崩溃。**
`onCreate` / `onStartCommand` 里的 `startForeground` 原本没有异常保护。
部分 ROM 在通知权限被关闭或后台启动受限时会抛 `SecurityException` /
`ForegroundServiceStartNotAllowedException`——服务崩溃等于应用崩溃。
现抽出 `promoteToForeground()` 统一捕获：失败则记日志并 `stopSelf()` 优雅退出
（保活失效，但应用可用），`onStartCommand` 相应返回 `START_NOT_STICKY`。

**词库诊断日志补充反向索引规模**——正是靠它才拿到上面的 116 万条数据。

## 五、验证清单（本轮全部通过）

| 项 | 结果 |
|---|---|
| release 构建（指定密钥库签名） | ✅ V1=false V2=true V3=true |
| 真机覆盖安装 | ✅ Success，数据保留 |
| IME 进程重启 / 词库加载 | ✅ 8424ms，422 音节 / 104.8 万词语键 |
| 键盘 UI 渲染（26 键 + 功能栏） | ✅ 截图确认完整 |
| 退格双击修复（双向） | ✅ 见上表 |
| 内存采样点产出数据 | ✅ 165MB / 384MB |

## 累计

- 已修复 / 改进：**36 项**
- 单元测试：8 个类、162 个用例全绿
- 真机：已构建签名 APK 并部署验证
- 剩余待办：#9 三份 UI 渲染层抽取（真机条件已具备，可在下次迭代推进）

---

# 第十一轮：修复「企鹅」打不出来 —— 连写歧义类漏词

用户报告：**双拼输入 qiee 打不出「企鹅」**（qǐ + é，双拼按 qi + ee）。

## 一、定位过程

**第一步，确认词库现状**：在 `pinyin_phrases.txt` 里精确匹配「企鹅」这个词条——
**0 条**。而含"企鹅"的 23 个词（帝企鹅、北斗企鹅、帝王企鹅…）拼音都写作 `qie`，
说明词库的拼音标注把「企鹅」记作 `qie`，**这个词条本该存在于 `qie` 键下，但它不在**。
`qie` 键只有 `且|切|窃|怯|妾|茄|惬|趄|锲|伽|挈|箧|慊|郄` 14 个单字。

**第二步，确认双拼转换没问题**：`Shuangpin.ZERO_TABLE` 里有 `ee → e`，
`toQuanpin("qiee")` = `qie` ✓ 转换正确。所以问题不在双拼，在词库。

**第三步，找到这是一整类问题**：这类词的特征是——词的拼音是 `S1 + Z`
（Z 为零声母音节 a/ai/an/ang/ao/e/ei/en/eng/er/o/ou），连写后 `S1+Z` 恰好等于
**另一个合法单音节**，于是与那个单音节同键：

| 词 | 拼音 | 连写 | 撞上的单音节 |
|---|---|---|---|
| 企鹅 | qi e | qie | 切 |
| 西安 | xi an | xian | 先 |
| 饥饿 | ji e | jie | 姐 |
| 提案 | ti an | tian | 天 |
| 彼岸 | bi an | bian | 边 |

实测这一批词**全部不在词库里**。

## 二、检测工具（新增）

**`tools/dict_builder/detect_ambiguous_keys.py`** —— 枚举所有"连写后仍是合法音节"的
歧义键，并挑出"该键存在但里面只有单字、一个多字词都没有"的键：

```
合法音节 422 个｜词库键 1,048,342 个
歧义键共 144 个｜其中可疑（只有单字）112 个
```

112 个是本类问题的排查面（含假阳性，见文档内说明），供后续人工核对。

## 三、修复

**新增 `tools/dict_builder/add_words.py`** + 补词表 `fix_ambiguous_words.txt`：
只做数据合并，不改动已有词条顺序；支持指定插入位置。

补入 11 个确实缺失的词，插入位置取第 4 位（落在最常用三个单字之后，
既不抢第一候选，也不用翻到几十个候选末尾）：

```
企鹅 qi e ｜ 西安 xi an ｜ 饥饿 ji e ｜ 提案 ti an ｜ 奇案 qi an
彼岸 bi an ｜ 立案 li an ｜ 堤岸 di an ｜ 皮袄 pi ao ｜ 集安 ji an ｜ 西澳 xi ao
```

修复后 `qie` 键：`且|切|窃|企鹅|怯|妾|茄|惬|趄|锲|…`
词库键数 1,048,342 保持不变（只改词条内容，不动结构）。

**同时修掉 `dict_builder.py` 的一个隐患**：原 `write_outputs` 对每个带空格拼音
各写一行（`compact = pinyin.replace(" ", "")`），而「qi e」和「qie」的 compact 相同
→ 产生重复键；app 侧加载是 `phrasesByPinyin[key] = ...`，**后写的那行会整体覆盖前一行**，
词条静默消失。现改为先按 compact 聚合再输出，从根本上杜绝这类丢词。

## 四、防回归测试（新增）

`PhraseDictIntegrityTest` —— 直接流式扫描 assets 词库文件，断言这些"连写歧义"词
确实存在于对应键下。任何一次词库重新生成/替换若丢掉它们，测试立刻失败。

不加载整份词库（29MB / 约 8s），只读到所需键即停，测试开销可忽略。

```
测试类 9 个、用例 163 个、失败 0、错误 0
```

## 五、累计

- 已修复 / 改进：**39 项**
- 单元测试：9 个类、163 个用例全绿

## 六、顺带发现的另一个数据问题（未修，需你确认）

用脚本扫描词库时发现**疑似编码损坏的词条**：字符串 `涓嶅` 出现在 **603 个**词的拼音键下
（`涓嶅` 是 UTF-8 字节被按 GBK 解码的典型产物，原文很可能是「不」）。
这属于词库源数据的编码问题，不在本次修复范围，**已登记待确认**——
如果确实是坏数据，建议在词库生成流程里加一步编码校验。

## 七、真机验证（通过）

构建签名 APK 并安装到真机（PACM00）后，重启 IME 加载新词库，用触摸点按键盘字母键
输入（`adb shell input text` 会绕过拼音引擎，必须用真实点击）：

```
输入 3 键 q i e   → 双拼[qie]→qie  → 候选栏：且 切 窃 [企鹅] 怯 妾 茄 惬 趄 锲 …
输入 4 键 q i e e → 双拼[qiee]→qie → 候选栏：且 切 窃 [企鹅] 怯 妾 茄 惬 趄 锲 …
```

用户报告的精确场景（**双拼 qiee**）已能打出「企鹅」，位于候选栏第 4 位——
正好是设计的位置：前三位留给最高频的「且/切/窃」，第 4 位既不用翻页也不抢首候选。
截图已确认候选栏渲染正常。

**结论：问题修复并真机验证通过。**

## 八、累计

- 已修复 / 改进：**39 项**
- 单元测试：9 个类、163 个用例全绿（新增词库防回归测试）
- 真机：已构建签名 APK、部署并验证修复效果

---

# 第十二轮：新增「显示生僻字」开关（默认关闭）

用户需求：**默认不显示生僻字**——绝大多数情况下用不到，全部放出来只是徒增内存与性能负担。
开关要放在 **APP 首页控件**上，键盘上不放。

## 一、判定标准：怎么区分「生僻字」和「常用字」

依据用户提供的《通用规范汉字表》(2013)（`docs/common-standard-chinese-characters-table-master.zip`）：

| 层级 | 字数 | 定位（官方说明） | 本项目处理 |
|---|---|---|---|
| 一级 | 3500 | 常用字集 | **常用** → 保留 |
| 二级 | 3000 | 满足出版印刷、辞书编纂、信息处理的一般用字需要 | **常用** → 保留 |
| 三级 | 1605 | 姓氏人名、地名、科技术语、中小学文言文用字 | **生僻** → 默认不加载 |
| 表外 | — | 扩展 A/B/C… 区汉字 | **生僻** → 默认不加载 |

**为什么选一级+二级（6500 字）作为界线**：官方对一级二级的定义正是"社会一般应用领域"，
而三级字表是为**专门领域**准备的——这与用户"绝大多数情况用不到生僻字"的判断完全吻合。
另一份《常用 3500 / 7000 字》（语料库在线、邢红兵）作为交叉参考，但最终采用国家标准，
因为它的三级别划分天然给出了"常用/生僻"的界线，无需自行推断阈值。

字表已固化为 asset `common_chars.txt`（6500 字，19.8KB，含来源说明注释）。

## 二、过滤粒度：整词丢弃

- **单字**：生僻字不进入单字表；
- **词**：词中**任一**字生僻 → 整条丢弃。

理由：用户既然用不到生僻字，也就不会去用含生僻字的词（如「饕餮」）。

## 三、关键设计：加载时过滤，而不是加载后过滤

在**解析词库的过程中**就跳过生僻内容，被跳过的词条**从未进入 HashMap**——
这才是真正降低内存占用的做法。若是"先全量加载再过滤"，内存峰值和常驻占用都不会变，
只是候选列表变干净了而已，达不到用户要的省内存效果。

## 四、数据结构：位图判定

`BooleanArray(0x9FFF + 1)` ≈ 64KB，按 Char 码点直接索引，O(1) 判定。

为什么不用 `HashSet<Char>`：词库加载要判定 **123 万词条 / 450 万字符**，
位图是纯数组下标访问，比哈希查找快得多；而 64KB 相对它省下的内存可以忽略。

判定规则（`isLoadableChar`）：
- `< 0x4E00`（ASCII / 数字 / 标点）：不参与判定，一律放行；
- `0x4E00~0x9FFF`（基本区汉字）：查常用字位图；
- 其它（含 BMP 外扩展区汉字的代理对）：视为生僻。

## 五、开关位置：APP 首页（设置页），键盘无入口

按用户要求，开关只放在**设置页**（APP 从桌面启动的首页），位置紧随「自动唤起键盘」卡片，
键盘的功能面板与剪贴板面板**都没有**该入口。

交互：切换即写入配置并 Toast 提示「重启输入法后生效」——词库在 IME 进程启动时加载，
设置页顶部已有的「保存配置并立即重启生效」按钮可立即应用。

## 六、实测收益（过滤前后对比）

用真实词库统计（`common_chars.txt` + `pinyin_chars.txt` + `pinyin_phrases.txt`）：

| 项 | 过滤前 | 过滤后 | 降幅 |
|---|---|---|---|
| 单字表 | 17,863 字 | 7,038 字 | **−60.6%** |
| 词组键 | 1,048,342 | 892,539 | −15.1% |
| 词条 | 1,230,120 | 1,017,136 | −17.3% |
| 字符总量 | 4,530,572 | 3,811,853 | −15.9% |

单字表降幅尤其明显（60.6%）——生僻字在候选里既没人选，又实实在在占着内存和候选位。

## 七、测试

新增 `RareCharsFilterTest`（3 个用例）：
- 未启用过滤时，生僻字与含生僻字的词都可用；
- 启用过滤后，含生僻字的词「饕餮」被丢弃，而同键的常用词「涛涛」保留；
- 启用过滤后，生僻单字「饕」被过滤，常用字「涛」保留。

另加 `PinyinEngine.resetForTest()`：引擎是单例且数据累加，过滤开关又是全局状态，
多个测试类相继注入会互相污染，测试需要干净起点。

```
测试类 10 个、用例 166 个、失败 0、错误 0
```

## 八、真机实测（生僻字过滤生效）

```
词库加载完成: 音节=414 词语键=892714 反向索引=953915 合法音节=422 生僻字=隐藏(已过滤 223358 条)
```

| 指标 | 过滤前 | 过滤后 | 变化 |
|---|---|---|---|
| 词语键 | 1,048,342 | 892,714 | −14.8% |
| 反向索引 | 1,162,486 | 953,915 | −17.9% |
| 合法音节 | 422 | 414 | −8（该音节下全是生僻字的被清空） |
| 被过滤词条 | — | 223,358 条 | — |

**内存核对**：`dumpsys meminfo` 显示 Java Heap **Alloc 131MB**（实际使用），
与"词库数据减少 14.8%"后的量级吻合——内存确实下降了。
（注意别被 `Runtime.totalMemory() - freeMemory()` 的采样误导：它包含尚未 GC 的垃圾，
加载刚结束时读会明显偏高。）

**开关位置**：截图确认在设置页第 3 个卡片（「自动唤起键盘」之后），默认关闭，
键盘功能面板与剪贴板面板均无该入口。

---

# 第十三轮：APK 体积瘦身（10.6MB → 8.4MB，−20.8%）

用户要求：尽可能缩减 APK 体积，但**不能过于影响运行速度与词库加载性能**。

## 一、先诊断：钱花在哪了

```
APK 总计 10.6MB，其中：
  assets/pinyin_phrases.txt   29.4MB → 10.7MB（deflate 压 64%）← 占 APK 的 95%
  classes.dex                559KB → 267KB
  assets/pinyin_chars.txt     73KB →  47KB
  其余全部加起来                < 0.6MB
```

**结论：APK 体积 = 词库体积**，其它一切都已很小（R8 与 shrinkResources 早已启用）。

**词库内部词长分布**（这是后面判断「裁什么最有效」的依据）：

| 词长 | 词条数 | 占比 | 字符数 | 字符占比 |
|---|---|---|---|---|
| 1 字 | 8,777 | 0.7% | 8,777 | 0.2% |
| 2 字 | 164,446 | 13.4% | 328,892 | 7.3% |
| 3 字 | 455,577 | 37.0% | 1,366,731 | 30.2% |
| 4 字 | 377,526 | 30.7% | 1,510,104 | 33.3% |
| 5 字 | 106,676 | 8.7% | 533,380 | 11.8% |
| 6 字 | 75,247 | 6.1% | 451,482 | 10.0% |
| 7 字及以上 | 39,861 | 3.2% | 331,206 | 7.3% |
| **1–2 字合计** | **173,223** | **14.1%** | **337,669** | **7.5%** |
| **3 字及以上合计** | **1,056,897** | **85.9%** | **4,192,903** | **92.5%** |

即：日常输入真正用到的 1–2 字词只占词条的 14%、字符的 7.5%；其余 85.9% 的词条
（92.5% 的字符）是长尾专有名词——动植物名、地名、人名、作品名、游戏装备名等，
例如「阿巴敦胫甲」「阿坝大头花天牛」「阿不思·邓布利多」「阿坝藏族羌族自治州」。

## 二、排除掉的手段（用数据说话，避免瞎试）

| 手段 | 实测收益 | 结论 |
|---|---|---|
| 裁掉生僻字 | 词库字节仅 −7.9% | 收益小，且会让「显示生僻字」开关失效 |
| 只保留常用字（一级 3500） | −10.8% | 同上 |
| 限制词长 ≤3 字 | −37.2% | 会砍掉「中华人民共和国」这类常用四字词，**有损，不做** |
| 提高 deflate 等级 / gzip -9 | 与现状持平（36.5%） | 无收益 |
| R8 / shrinkResources | 早已启用 | 无空间 |

> 注意一个反直觉点：裁生僻字对**词条数**能减 17%，但对**字节数**只减 7.9%——
> 因为拼音键里没有汉字，过滤不掉，而键本身就占词库的 44%。

## 三、采用方案：词库改用 xz 存储 + 流式解压

```
原始           28.01MB
deflate（现状） 10.22MB（36.5%）
xz preset=6     8.00MB（28.6%）  ← 比 deflate 再省 21.7%
```

**实现**：
1. `pinyin_phrases.txt` → `pinyin_phrases.txt.xz`（preset 6），源文件移到
   `tools/dict_builder/source/` 供构建工具使用；
2. 依赖 `org.tukaani:xz:1.9`（纯 Java 实现，dex 后约 80KB）；
3. `PinyinEngine.loadPhrases()` 改用 `XZInputStream` **流式解压**——不落地磁盘、
   不构造超大 String，仍是逐行解析；
4. `androidResources { noCompress += "xz" }`：xz 已是压缩格式，避免被 deflate 再压一遍；
5. `PhraseDictIntegrityTest` 同步改为解压校验，确保测的是**真正打进 APK 的那份词库**。

**结果**：

| 项 | 优化前 | 优化后 |
|---|---|---|
| APK 体积 | 10.6MB | **8.4MB（−20.8%）** |

## 四、代价与验证（诚实记账）

| 指标 | 优化前 | 优化后 | 说明 |
|---|---|---|---|
| 词库加载耗时 | 8424ms | **10640ms（+2.2s）** | xz 是纯 Java 解压，比原生 zlib 慢 |
| 加载所在线程 | 后台 | 后台 | **不阻塞 UI**；键盘显示由 `onCreateInputView` 驱动，不受影响 |
| 候选功能 | 正常 | 正常 | 真机验证 `qie → 且 切 窃 企鹅 怯…`（企鹅仍在第 4 位） |

**为什么接受这 2.2s**：加载发生在 `JinnIme.onCreate` 的独立线程上，日志显示键盘视图创建
（`onCreateInputView`）在词库加载完成之后才发生，用户看到键盘的时机不受影响；
代价仅是"键盘弹出后约 2.2 秒内拼音候选还没就绪"。

**若你更在意加载速度**，回退方法很简单：删除 `build.gradle.kts` 里的 `xz` 依赖与
`noCompress`，把 `tools/dict_builder/source/pinyin_phrases.txt` 放回 assets，
并将 `loadPhrases()` 换回 `context.assets.open("pinyin_phrases.txt")`。

## 五、累计

- 已修复 / 改进：**43 项**
- 单元测试：10 个类、166 个用例全绿
- APK：**10.6MB → 8.4MB（−20.8%）**，签名方案 V1=false/V2=V3=true 未变

---

# 第十四轮：词库分层 —— APK 只内置基础包，长词改为可选下载

用户需求：把少用/专业词库做成**可下载的扩展项**，不进 APK；APK 只打包基础词库。

## 一、先算账：切在哪一刀

切分依据只能是**词长**——词库格式里没有词频字段，无法按频率切。实测各档：

| 基础包保留 | 基础包 xz | 扩展包（需下载） | APK | 丢失内容 |
|---|---|---|---|---|
| 1–2 字 | 0.64M | 7.52M | 1.6M | 三字词（「天安门」「计算机」）——过于激进 |
| **1–3 字** | **3.16M** | **4.73M** | **3.6M** | 四字及以上（成语、专有名词） |
| 1–4 字 | 5.74M | 2.56M | 6.7M | 五字及以上（含「中华人民共和国」） |
| 完整（上轮） | 8.00M | — | 8.4M | 无 |

**选 1–3 字**：三字词覆盖日常表达（天安门、计算机、办公室、差不多），
而四字及以上以成语与长尾专有名词为主，适合做成扩展包。**代价是四字成语暂时打不出来**，
装上扩展包即恢复。

## 二、实现

**切分工具 `tools/dict_builder/split_dict.py`**：输出两个格式完全相同的包
（`拼音<TAB>词1|词2`），运行时用同一套解析器合并加载。

```
基础包（≤3 字）: 462,189 键 / 628,800 词条   9.92MB → xz 3.16MB   ← 进 APK
扩展包（>3 字）: 586,190 键 / 601,320 词条  17.09MB → xz 4.73MB   ← 按需下载
合计 1,230,120 词条（与源词库一致，零丢失）
```

**引擎改造**（`PinyinEngine`）：
- 新增 `loadExtensionDict()`：从 `filesDir/dict_ext.xz` 加载扩展包，**不存在时静默跳过**
  （基础包已覆盖日常输入，未装扩展不算错误）；
- `loadPhrasesReader(reader, merge)` 新增**合并模式**——这是本次最关键的正确性点：
  扩展包与基础包存在**相同的拼音键**（`qie` 两边都有词），若直接赋值会把基础候选整体挤掉，
  必须 `existing + kept` 合并，且基础词条在前（优先级更高）；
- 预分配容量按基础包规模下调（60 万 / 70 万），避免按已不存在的 104 万键浪费。

**设置页新增「扩展词库（长词包）」卡片**：状态显示（已安装 + 体积 / 未安装）、
下载地址输入框、「下载并安装」「删除扩展词库」按钮。
下载先写 `.tmp` 再改名，**避免下载中断留下半个文件被引擎当成有效词库加载**；
回调切主线程并检查 `isFinishing/isDestroyed`。

## 三、结果

| 项 | 原始 | 上一版 | 本版 |
|---|---|---|---|
| APK 体积 | 10.6MB | 8.4MB | **3.6MB（−66%）** |

## 四、扩展包如何部署与安装

扩展包已生成在 `release/dict_ext.txt.xz`（4.73MB）。放到任意可访问位置即可：

```
# 例：放到 NAS 的 HTTP 目录，然后在设置页填入该地址
http://192.168.1.3:8080/dict_ext.txt.xz
```

设置页「扩展词库」卡片 → 填地址 → 点「下载并安装」→ 用顶部「保存配置并立即重启生效」重启输入法。

## 五、验证范围（如实说明）

| 项 | 状态 |
|---|---|
| 编译 + 单元测试（10 类 166 用例） | ✅ 通过（含词库完整性测试，校验基础包仍有「企鹅」「西安」等词） |
| APK 体积 | ✅ 实测 3.6MB |
| 签名方案 | ✅ V1=false / V2=true / V3=true |
| 真机安装 | ✅ Success |
| 设置页 UI（扩展词库卡片、状态「未安装」） | ✅ 截图确认 |
| xz 流式加载可用性 | ✅ 上一版真机已验证（本次基础包格式完全相同） |
| **基础包真机加载数据（键数/内存/耗时）** | ⚠️ **未取到**：本次 adb 点击始终没能聚焦设置页输入框（键盘未弹出、`uiautomator dump` 也不可用），故未采到加载日志。功能逻辑与上一版一致、仅词条减少，风险低，但**这一项确实没验证**，留待下次或你手动点一下键盘确认 |
| 扩展包下载与合并加载（同键 merge） | ⚠️ 未经真机验证（需先有可访问的下载地址） |

## 六、累计

- 已修复 / 改进：**47 项**
- 单元测试：10 个类、166 个用例全绿
- APK：**10.6MB → 3.6MB（−66%）**

---

# 第十五轮：词库分发上线 + 源码同步到两个仓库

## 一、扩展词库的分发位置

两个源都托管为 **Releases 附件**（不是仓库文件）：

| 源 | 直链 |
|---|---|
| **Gitee**（国内快，主源） | `https://gitee.com/yezijinn/com.jinn.inputmethod/releases/download/dict-ext-20260915/dict_ext.txt.xz` |
| **GitHub**（备用） | `https://github.com/yezijinn/Jinn_AndroidInputMethod/releases/download/dict-ext-20260915/dict_ext.txt.xz` |

两个源均已实测：请求 302 重定向后返回 200，`Content-Length = 4956768`。
OkHttp 默认跟随重定向，无需额外配置。

**踩到的坑（重要）**：最初把扩展包提交进仓库、打算用 raw 直链，结果
**Gitee 对较大文件的 raw 访问返回 404**——仓库里文件确实存在（API 可查到），
但 4.7MB 的文件 raw 取不到；同一个仓库的 README.md（小文件）raw 正常 302→200。
所以**必须走 Releases 附件**，raw 路径对小文件才可靠。

## 二、设置页的下载体验

- **预填直链**：进入设置页自动填入主源地址，开箱即可点「下载并安装」；
- **多源自动回退**：按「用户填写 → Gitee → GitHub」依次尝试，任一成功即停；
- **一键生效**：下载成功后**自动重启输入法进程**（词库只在 IME 启动时加载），
  用户点一次按钮即完成「下载 → 安装 → 合并生效」，不用再去点顶部的重启按钮；
- 先写 `.tmp` 再改名，避免下载中断留下半个文件被引擎当成有效词库加载。

## 三、源码同步

| 仓库 | 分支 | 最新提交 |
|---|---|---|
| GitHub `yezijinn/Jinn_AndroidInputMethod` | `perf/paging-pinyin-rematch` | `12dc44b` |
| Gitee `yezijinn/com.jinn.inputmethod` | `master` | `12dc44b` |

提交内容：`6226554`（首批修复 + 优化 + 新功能，39 文件）、`12dc44b`（下载源修正）。

**入库范围**：源码、资源、构建配置、测试、工具脚本、三份文档、扩展包。
以下已加入 `.gitignore` 不入库：

- `.workbuddy/`（AI 工具工作区状态）
- `docs/*.zip`（参考字表原始压缩包，内容已提取为 `assets/common_chars.txt`）
- `tools/dict_builder/source/`（28MB 未压缩词库源文件，可由基础包 + 扩展包还原）

> 推送 GitHub 时遇到两次 `TLS connect error`（国内网络波动），第三次成功——
> 若以后推送失败，直接重试即可，不是配置问题。

## 四、累计

- 已修复 / 改进：**48 项**
- APK：**3.6MB**（含基础词库），扩展包 4.7MB 按需下载
- 测试：10 个类、166 个用例全绿

---

# 第十六轮：基础包纳入四字成语 + 界面调整 + 同步 main 分支

## 一、切分点从 3 字改为 4 字

**理由**：四字成语属于高频用词，必须随 APK 内置，不能塞进扩展包。

| | 旧（≤3 字） | 新（≤4 字） |
|---|---|---|
| 基础包 | 462,189 键 / 628,800 词条 → 3.16MB | **826,897 键 / 1,006,326 词条 → 5.74MB** |
| 扩展包 | 586,190 键 / 601,320 词条 → 4.73MB | **221,510 键 / 223,794 词条 → 2.19MB** |
| APK | 3.6MB | **6.2MB** |
| 词条合计 | 1,230,120（零丢失） | 1,230,120（零丢失） |

APK 由 3.6MB 增到 6.2MB，但仍比最初的 10.6MB 小 42%；扩展包反而从 4.73MB 降到
2.19MB（只需补五字及以上），下载更快。

## 二、扩展词库 v2 直链（已重新上传并实测）

```
Gitee  （主源）https://gitee.com/yezijinn/com.jinn.inputmethod/releases/download/dict-ext-20260915-v2/dict_ext.txt.xz
GitHub （备用）https://github.com/yezijinn/Jinn_AndroidInputMethod/releases/download/dict-ext-20260915-v2/dict_ext.txt.xz
```

两个源均实测 **HTTP 200，Content-Length = 2297184**。设置页已预填 Gitee 主源。

## 三、界面调整

1. **「点这里唤起键盘打字」整块移到「保存配置并立即重启生效」下方** —— 保存完即可立刻验证；
2. **扩展词库的两个按钮改用 `@style/SettingsButton`**，与「电池白名单 / 无障碍保活 / ROOT 防杀」
   以及「发送」按钮完全一致（此前是无样式裸 Button，与深色主题不搭）；
3. 地址输入框补上 `field_aurora` 背景与内边距，与「唤起键盘」输入框统一；
4. 状态文案更新为「未安装 —— 五字及以上长词暂时打不出来（四字成语已内置）」，
   避免仍说"四字成语打不出来"造成误解。

## 四、GitHub main 主分支同步

检查后发现 **main（`ab7e6e8`）是本地 HEAD 的祖先**，因此是**快进推送**，无需强推、不丢历史：

| 远端 | 分支 | 结果 |
|---|---|---|
| GitHub | `main` | `ab7e6e8..c8b93e9` ✅ |
| GitHub | `perf/paging-pinyin-rematch` | `ae5f376..c8b93e9` ✅ |
| Gitee | `master` | `ae5f376..c8b93e9` ✅ |

三个引用确认均指向 `c8b93e9`。

## 五、累计

- 已修复 / 改进：**52 项**
- APK：**6.2MB**（含四字成语；原 10.6MB，−42%）
- 扩展包：2.19MB 按需下载
- 测试：10 个类、166 个用例全绿

---

# 第十八轮：移除全部保活机制

承接上一轮的审查结论落地：**保活三件套属于过度设计**。IME 本身是系统绑定服务、
用户一输入就会被拉起，真正需要常驻的只是那条 WebSocket 长连接；而这套保活换来的
"进程常驻"对一个自用输入法并不值得——代价是 5 项权限、一个无障碍服务、
一个前台服务与一个 ROOT 依赖。

## 一、删除内容

| 类别 | 内容 |
|---|---|
| 源文件 | `KeepAliveService.kt`、`JinnAccessibilityService.kt`、`RootShizuku.kt`、`BootReceiver.kt`（4 个）+ `res/xml/accessibility.xml` |
| 权限 | `FOREGROUND_SERVICE`、`FOREGROUND_SERVICE_SPECIAL_USE`、`POST_NOTIFICATIONS`、`RECEIVE_BOOT_COMPLETED`、`REQUEST_IGNORE_BATTERY_OPTIMIZATIONS`（5 项） |
| 依赖 | `dev.rikka.shizuku:api`（Shizuku） |
| 设置页 | 整张「后台保活」卡片（3 开关 + 3 按钮 + 状态文本）、`notifyPermissionLauncher`、`toggleKeepAlive` / `ensureNotifyPermission` / `runRootKeepAlive` |
| 配置 | `Prefs.keepAlive` / `useRootShizuku` / `notifyHighPriority` 及 KEY 常量 |

## 二、刻意保留

**剪贴板卡片里的「Root 增强模式」没有删** —— 它做的是剪贴板数据目录的安全审计
（防第三方读取），与"保活防杀"是两件事，只是恰好都用到 root。删保活不该顺手删它。

## 三、关于「按需连接」

**`asr.connect()` 的调用时机我没有改** —— 那属于语音模块（`AsrClient` / 录音链路），
按"禁止修改语音部分"的硬性约束不动它。

不过保活移除后**实际效果已接近按需连接**：IME 进程不再被强制常驻，
用户输入时系统拉起 IME 即建立连接、后台闲置则随进程回收，不再有前台服务/无障碍互保兜着。
若要真正做到"只有按下录音键那一刻才连接"，需要改 `AsrClient` 的连接时机——那要先解除语音约束。

## 四、验证（真机）

| 项 | 结果 |
|---|---|
| 编译 + 单元测试 | ✅ 通过，引用清理干净无遗留 |
| APK | ✅ 6.2MB，签名 V1=false / V2=true / V3=true |
| 真机安装 | ✅ Success |
| 设置页 | ✅ 截图确认保活卡片消失，「扩展词库」直接接「剪贴板」 |
| **语音链路** | ✅ 日志确认 `renderLink: CONNECTING → ONLINE`，ASR 连接正常 |

## 五、工程教训（本轮踩到两次）

改 `strings.xml`、`findViewById` 序列这类**每行结构高度相似**的文件时，`Edit` 的
`old_string` 必须**只包住目标行**，别顺手带相邻行当锚点——我因此两次误伤：
一次删掉了相邻的 `ext_dict_status_installed`，一次往 `findViewById` 序列里插了重复赋值。
两次都靠随后的 `grep -c` 校验发现并修复。
**结论：这类文件改完必须 grep 验证关键 key 的存在性与唯一性。**

## 六、累计

- 已修复 / 改进：**56 项**
- APK：6.2MB｜测试：10 个类 166 用例全绿
- 已移除：4 个源文件、5 项权限、1 个第三方依赖、整张保活设置卡片

---

# 第十九轮：方案按钮提示目标方案 + 符号菜单新增「编程」分组

## 一、功能栏方案按钮的小字

原来小字固定为「换拼音」，看不出点下去会变成什么。改为**按当前方案提示目标方案**：

| 当前方案 | 主文字 | 底部小字 |
|---|---|---|
| 双拼 | 双拼 | **换全拼** |
| 全拼 | 全拼 | **换双拼** |

真机截图确认：功能栏首键显示「全拼 / 换双拼」。

## 二、符号菜单新增「编程」分组（第 11 组，共 3 页）

键位映射值本来就是 `String`（不限于单字符），所以关键字可以整词上键：

| 页 | 内容 |
|---|---|
| 1 | 编程常用英文符号：`( ) { } [ ] < > = !` / `; : ' " \` ~ & \| \` / `+ - * / % ^ _` |
| 2 | 常用关键字：`return print main if else for while class def func` / `import from const let var new public private void` / `true false null None this static async` |
| 3 | 类型与语句：`int float double string bool char long struct enum interface` / `try catch finally throw break continue switch case default` / `print() main() println printf scanf lambda yield` |

## 三、键面长文本自适应（本轮的关键改动）

原 `PinyinKey.onDraw` 的字号是**固定比例**（`TEXT_RATIO = 0.4`），
`return`（6 字符）、`private`（7 字符）这类关键字会左右溢出、显示不全。

新增规则：**label 超过 2 字符即按长文本处理** ——
`fitTextSize()` 按可用宽度等比收缩字号（下限 `MIN_LONG_TEXT_SIZE = 9sp`），并**垂直居中**
（而非沿用小字顶置样式）：

```
基准字号 h * LONG_TEXT_RATIO(0.30) → 若测宽 > 键内可用宽度，则按比例收缩到刚好放下
```

真机截图确认：`if`（2 字符）走原样式、字号较大；`return`/`import`/`private` 自动收缩
且完整显示，同一页内不同长度关键字的字号各不相同，这就是自适应的直接体现。

## 四、累计

- 已修复 / 改进：**59 项**
- APK：6.2MB｜测试：10 个类 166 用例全绿
- 符号分组：10 组 → **11 组**

---

# 第二十轮：符号键文案、键面一律居中、编程分组扩至 7 页

## 一、符号键文案改为「返回」

进入符号层后，左下角那个键原来显示「ABC」，现在显示 **「返回」**
（新增 `key_back` 字符串）——它此刻的作用就是回到字母页，这样更直白。
数字层的「ABC」保持不变。

## 二、符号层键面一律水平 + 垂直居中

新增 `PinyinKey.centeredStyle`，符号层（`LAYER_SYMBOL`）**不再沿用字母层的小字顶置样式**：

```kotlin
key.centeredStyle = layer == LAYER_SYMBOL
```

修复了短符号看起来偏上的问题（例如 `if` 是 2 字符，原来走顶置分支，视觉上不在键中央）。
居中分支里仍然调用 `fitTextSize()`，长关键字照旧按宽度收缩字号。

## 三、分组顺序调整

**常用 → 编程 → 标点** → 序号 → 数学 → 单位 → 平假名 → 片假名 → 拉丁 → 特殊 → 注音

编程组从末位提到第 2 位，日常高频的「常用」之后就能直接够到。

## 四、编程分组 3 页 → 7 页

| 页 | 内容 |
|---|---|
| 1 | 编程常用英文符号 `( ) { } [ ] < > = !` … |
| 2 | 通用关键字 `return print main if else for while class def func` … |
| 3 | 类型与语句 `int float double string bool` / `try catch finally throw` … |
| **4** | **Python**：`self None elif lambda yield global assert raise with as pass del not and or is True False __init__ __name__ print( input( range( len( str( dict(` |
| **5** | **Java / C#**：`System String Integer Boolean List Map HashMap ArrayList package extends implements abstract final override namespace using foreach params synchronized new get; set; await Task var public static` |
| **6** | **JS / TypeScript**：`function => console.log document window export default interface type enum undefined NaN typeof instanceof Promise async await require module forEach map( filter( reduce( JSON. Object. Array.` |
| **7** | **C / C++**：`#include #define stdio.h stdlib.h iostream cout cin endl std:: vector template typename nullptr sizeof malloc free( printf( scanf( struct -> :: int main return 0 unsigned const static` |

所有页面都受本轮的字号自适应保护，最长的 `public static`（13 字符）也能完整显示。

## 五、真机验证

| 项 | 结果 |
|---|---|
| 符号键文案 | ✅ 显示「返回」 |
| 分组顺序 | ✅ 候选栏显示 常用 / **编程** / 标点 … |
| 编程组页数 | ✅ 标签右下角显示 **1/7** |
| 键面居中 | ✅ 符号与关键字均在键中央，长文本自动收缩 |

## 六、累计

- 已修复 / 改进：**62 项**
- APK：6.2MB｜测试：10 个类 166 用例全绿
- 符号分组：11 组，其中编程组 **7 页**

---

# 第二十一轮：符号分组重构 + 剪贴板搜索页退出按钮两行显示

## 一、剪贴板搜索页的「退出搜索」改为两行

用户要求**严格两行**排版（`退出` / `搜索`）。原来单行「退出搜索」在窄按钮里会挤压：

```kotlin
text = "退出\n搜索"
textSize = 12f                    // 13f → 12f，给两行留出空间
setLineSpacing(0f, 0.95f)         // 行距略收紧，避免 46dp 高度里两行贴边
```

## 二、平假名 + 片假名合并为「日本」组

两个分组本来就是同一语言的两套字母，合并为一组、内容二合一，**共 8 页**
（前 4 页平假名、后 4 页片假名），翻页时在同一组内滑动即可。

## 三、「特殊」组前移并扩展到 6 页

- **位置**：从末位（拉丁之后）移到**「序号」之前**；
- **页数**：3 页 → **6 页**，新增三页：

| 页 | 内容 |
|---|---|
| 1–3 | 原内容：牌面/星形/音符/表情、天气/星座/杂项、常用 emoji |
| **4** | **箭头**：`→ ← ↑ ↓ ⇒ ⇐ ⇑ ⇓ ↔ ↕ ➤ ➔ ↗ ↘ ↙ ↖ ⤴ ⤵ ↩ ↪ ⇄ ⇅ ⇆ ⇧ ⇩ ⏎` |
| **5** | **货币与金融**：`¥ $ € £ ₩ ₽ ¢ ₪ ₹ ₫ ₴ ₦ ₱ ﷼ ₨ ₡ ₭ ₮ ₲ ₵ ₸ ₺ ₼ ₾ ¤ ₿` |
| **6** | **数理与几何**：`≠ ≈ ≤ ≥ ± × ÷ ∞ ∵ ∴ ∠ ⊥ ∥ △ ▲ ▽ ▼ ◇ ◆ □ ■ ○ ● ◎ ⊙ ⌒` |

## 四、最终分组顺序（10 组）

```
常用 → 编程 → 标点 → 特殊 → 序号 → 数学 → 单位 → 日本 → 拉丁 → 注音
```

（原 11 组，因平假名+片假名合并减少 1 组）

## 五、真机验证

| 项 | 结果 |
|---|---|
| 分组顺序 | ✅ 截图确认：常用 / 编程 / 标点 / **特殊** / 序号 / 数学 / 单位 / 日… |
| 特殊组页数 | ✅ 标签右下角显示 **1/6** |
| 编程组 | ✅ 显示 3/7，键面 `int float double …` 全部居中 |
| 符号键「返回」 | ✅ |
| 键面居中 | ✅ |
| **退出搜索两行排版** | ⚠️ **未取到真机截图**：需要经功能面板打开剪贴板搜索页，但通过 adb 操作时候选栏出现状态残留（`input keyevent` 清空输入框后 IME 的拼音串未同步清除，导致功能面板不显示），非真实用户场景。改动本身简单明确（`text = "退出\n搜索"` + 字号/行距），已编译验证 |

## 六、累计

- 已修复 / 改进：**66 项**
- APK：6.2MB｜测试：10 个类 166 用例全绿
- 符号分组：10 组；编程 7 页、特殊 6 页、日本 8 页

---

# 第二十二轮：中英键两行富文本、序号组扩充、日本组补齐空位

## 一、中英切换键改为上下两行富文本

原来是单个「中」/「EN」随模式切换文本，现在**同一控件显示两行**「中文 / 英文」，
把**当前生效的那一行**染成主题紫 `aurora_purple` 并加粗：

```
中文   ← 中文模式下：紫色 + 加粗
英文   ← 英文模式下：紫色 + 加粗（另一行保持灰色）
```

**实现要点**：必须用 `SpannableString` 做**部分着色**。
一开始试过「中文/英文」单行斜杠写法，但按钮只有约 132px 宽，13sp 会折成
「中文/英」+「文」两行；改成上下两行后字号可以放回 12sp，行距 0.9，在 144px 键高里排布均匀。

> 也不能拆成两个 TextView——两段会各自居中，视觉上像并排两个按钮，而不是一个可点击的切换键。

## 二、序号组 4 页 → 7 页

新增三页：

| 页 | 内容 |
|---|---|
| **5** | 圈字母收尾 `Ⓥ Ⓦ Ⓧ Ⓨ Ⓩ` + 带括号数字 `⑴`–`⒇` |
| **6** | 带括号小写字母 `⒜`–`⒵` |
| **7** | 大数圈号 `㉑`–`㉟` + 中文大写数字 `壹 贰 叁 肆 伍 陆 柒 捌 玖 拾` |

**顺带修了一处数据错误**：原第 2 页里 `'z' to "Ⅷ"` 与 `'n' to "Ⅷ"` 重复、
`'v' to "Ⅻ"` 与 `'m' to "Ⅻ"` 重复，已改为 `ⅰ ⅱ` 补位。

## 三、日本组补齐末页空位

合并平假名/片假名时，两组各自的**第 4 页只有 10 个键**（q–p），其余 16 键空着。
现在补齐为完整的 26 键：

- **拗音**：`きゃ きゅ きょ しゃ しゅ しょ ちゃ ちゅ ちょ にゃ にゅ にょ ひゃ ひゅ ひょ みゃ みゅ みょ りゃ りゅ りょ ぎゃ ぎゅ ぎょ じゃ じゅ じょ びゃ びゅ びょ ぴゃ ぴゅ ぴょ`（片假名同构）
- **长音符与浊点**：`ー ゛ ゜ ・ 〜 ゔ ゠ ゟ`（片假名用 `ヷ ヸ ヹ ヺ`）

日本组因此由 8 页增至 **10 页**（平假名 6 页 + 片假名 6 页中的 5+5，末页各含拗音与长音符号）。

## 四、真机验证

| 项 | 结果 |
|---|---|
| 中英键 | ✅ 截图确认两行「中文 / 英文」，「中文」紫色加粗 |
| 序号组 | ✅ 标签显示 **1/7** |
| 特殊组 | ✅ 标签显示 1/6 |
| 分组顺序 | ✅ 常用 / 编程 / 标点 / 特殊 / 序号 / 数学 / 单位 / 日… |

## 五、累计

- 已修复 / 改进：**70 项**
- APK：6.2MB｜测试：10 个类 166 用例全绿
- 符号分组：10 组；编程 7 页、特殊 6 页、序号 7 页、日本 10 页

---

# 第二十三轮：build_apk.py 健壮性审查与修复

按要求全面审查构建脚本，发现并修复 **9 处问题**，其中 1 处是实质性缺陷。

## 一、实质性缺陷：缺少 zipalign

**问题**：脚本用 Python `zipfile` 重打包去掉 `META-INF`（`remove_meta_inf`），
这会打乱 AGP 产物原有的 4 字节对齐；而 **apksigner 不负责对齐**，
之前直接 `apksigner sign` 输出，产物的对齐状态只是巧合，没有保证。

**未对齐的后果**：设备读取 APK 内资源时需先解压，影响启动速度与内存占用
（尤其带有 `resources.arsc` 与大量 `res/*.xml` 的场景）。

**修复**：把流程固定为标准顺序，并补上事后复核：

```
AGP 产物 → remove_meta_inf → zipalign -p -f 4 → apksigner sign → 校验
                                                              ├─ apksigner verify（签名方案）
                                                              └─ zipalign -c 4（对齐复核）
```

## 二、其余 8 处问题

| # | 问题 | 修复 |
|---|---|---|
| 2 | apksigner 会生成 `.idsig`（v4 签名），未被清理，产物目录残留中间文件 | 清理列表加入 `app-release-signed.apk.idsig` |
| 3 | docstring 里 `E:\JinnKeyStores` 是**无效转义序列**，Python 3.12+ 报 `SyntaxWarning`（未来版本将报错） | 改 `r"""..."""` raw 字符串 |
| 4 | `find_build_tool("apksigner.bat")` **硬编码 Windows 扩展名**，Linux/macOS 上找不到 | 按 `os.name` 选择 `.bat`/`.exe`/无后缀 |
| 5 | 签名方案校验用**精确英文串**匹配 apksigner 输出，升级 build-tools 后措辞变化会误报「未启用 v2/v3」 | 改用正则 `v2\s+scheme[^:]*:\s*true` |
| 6 | 临时文件只在成功路径清理，**中途失败会残留**并误导后续排查 | 改 `try/finally` 统一清理 |
| 7 | `remove_meta_inf` 用 `src.read()` **整块读入内存**（词库压缩包就有数 MB） | 改 `shutil.copyfileobj` 流式复制（256KB 块），并保留原 `compress_type` |
| 8 | `verify` 用 `shell=True` + 字符串拼路径，路径含特殊字符时可能被 shell 解释 | 改列表传参 |
| 9 | 密钥文件找不到时报错只说路径，不告诉用户怎么办 | 补上 `set JINN_KEYSTORE_ROOT=...` 的示例 |

## 三、验证

| 项 | 结果 |
|---|---|
| 严格模式语法检查 | ✅ `python -W error::SyntaxWarning -m py_compile` 无警告 |
| `--help` | ✅ 正常输出用法 |
| **端到端实跑（3 次）** | ✅ 编译 → 去 META-INF → 对齐 → 签名 → 校验 全通过 |
| 签名校验 | ✅ `V1=false V2=true V3=true` |
| **对齐校验** | ✅ `zipalign -c 4` 通过（脚本内 + 外部复核各一次） |
| 临时文件清理 | ✅ 产物目录只剩 `app-release.apk` 与 AGP 原生产物 |

## 四、累计

- 已修复 / 改进：**79 项**
- APK：6.2MB（对齐状态有保证，不再依赖巧合）

---

# 第二十四轮：回车键输出「原始按键」英文（双拼不做转换）

## 一、先纠正一个前提

需求描述里说「全拼输入 but 后回车会输出英文 but」——**实测并非如此**。
真机测试（双拼模式，输入 `but` 后按回车）：候选栏显示「不他/不太/步态」，
按回车后**拼音串 `but` 原样留着**，既不提交候选也没有输出英文。

原因在代码：`performEnter()` 当时只做两件事——

```kotlin
// 修改前
if (hasAction) connection.performEditorAction(action)   // 执行输入框动作
else sendDownUpKeyEvents(KeyEvent.KEYCODE_ENTER)        // 否则换行
```

它**完全没碰拼音串**。所以这个能力此前并不存在，本轮是新实现的。

## 二、实现

**`PinyinKeyboardView.takeRawComposing()`**（新增）——取出**用户实际按下的键**：

```kotlin
fun takeRawComposing(): String {
    if (composing.isEmpty()) return ""
    val raw = composing.toString()      // composing 存的就是原始按键，不做双拼转换
    composing.clear()
    lastCandidates = emptyList()        // 清掉陈旧候选，避免下一轮串味
    lastPredictions = emptyList()
    refreshCandidateBar()
    return raw
}
```

**`JinnIme.performEnter()`**——拼音串非空时优先输出按键原文：

```kotlin
val rawComposing = pinyinKeyboard?.takeRawComposing().orEmpty()
if (rawComposing.isNotEmpty()) {
    connection.commitText(rawComposing, 1)   // 打了几个键就输出几个字母
    return                                   // 不再执行输入框动作/换行
}
// 拼音串为空：保持原有「输入框动作 / 换行」逻辑
```

**关键点**：`composing` 里存的本来就是原始按键序列，所以**不需要任何转换逻辑**——
直接取出来提交即可。双拼按 `budv` 得到的就是 `budv`，不会变成 `budui`。

## 三、真机验证（PACM00，双拼模式）

| 操作 | 候选栏（双拼转换结果） | 回车后输入框 |
|---|---|---|
| 按 `udv` | `双拼[udv]→shuangzh` → 双闸/双寨/双栈 | **`udv`** |
| 按 `vbudv` | `双拼[vbudv]→zhoushuangzh` → 周/州/皱 | **`vbudv`** |

两处日志均记录「回车输出英文原文: xxx」，确认输出的是**按键原文**而非候选或转换后的拼音。
回车后候选栏回到功能面板（说明输入状态已正确清空）。

## 四、累计

- 已修复 / 改进：**81 项**
- APK：6.2MB

---

# 第二十五轮：深度缺陷扫描（Android Lint + 高危模式排查）

按「深度挖掘 BUG 并修复」要求，用 Android Lint 做系统扫描 + 对六类高危模式
（API 版本 / 并发 / 资源泄漏 / 异常处理 / 索引边界 / 性能）做定向排查。

## 一、修复的 3 处缺陷

| # | 位置 | 缺陷 | 级别 | 修法 |
|---|---|---|---|---|
| 1 | `ClipboardHistoryActivity` | 手写 `if (SDK_INT >= TIRAMISU)` 注册广播，Lint 判为「未声明导出标志」**Error** | P1 | 改用 `ContextCompat.registerReceiver`，由 androidx 按版本自动适配 |
| 2 | `JinnIme:251` | `requestShowSelf()` 是 **API 28** 方法而 minSdk = **26**，低版本抛 `NoSuchMethodError` | P1 | 加 `SDK_INT >= P` 判断；并把空的 `onFailure {}` 补上日志 |
| 3 | `JinnIme:966` | `if (SDK_INT < N) return` 在 minSdk 26 下**恒为 false**（Lint `ObsoleteSdkInt`） | P3 | 删除冗余判断 |

**第 1 项要说明**：原代码的版本分支**逻辑本身是对的**（API 33+ 传 flag、以下不传），
实际不会崩溃。但 Lint 仍报 Error——手写分支无法静态证明完备，日后改动也容易漏。
`ContextCompat.registerReceiver` 是正解：既消除告警，又把版本适配交给 androidx。

**第 2 项**：`runCatching` 兜住了异常所以**不会崩溃**，但功能在 Android 8.0/8.1 上
静默失效，且空 `onFailure {}` 违反项目「绝不静默吞异常」的约定——两处都补上了。

## 二、确认无问题的项（排查过、留证）

| 检查项 | 结论 |
|---|---|
| `!!` 强制解包 | 全项目 **0 处** |
| Cursor 资源泄漏 | `ClipboardDb` 六处 `rawQuery` **全部 `use {}` 关闭** |
| 监听器泄漏 | `registerReceiver`/`unregisterReceiver` 三处**全部配对**（JinnIme 配置与粘贴广播、ClipboardHistoryActivity、网络回调） |
| 并发安全 | `PinyinEngine` 用 `@Volatile` + `synchronized` 保护共享词库 |
| 索引越界 | `candidates[0]` 有 `isNotEmpty()` 守卫；`EXT_DICT_URLS.first()` 是常量非空列表 |
| 异常吞噬 | 仅 1 处空 catch（`BackgroundIo` 的 `RejectedExecutionException`），**有意设计且有注释** |

## 三、登记未修

**① `MicButton.kt:98`（P2，属语音部分 → 受「禁止修改语音」硬约束保护，未改动）**

```kotlin
// 每次 onDraw 都新建 RadialGradient
fillPaint.shader = RadialGradient(centerX, centerY - radius * 0.25f, radius * 1.15f, ...)
```

麦克风按钮重绘频繁时会产生持续的 GC 压力（Lint `DrawAllocation`）。
**建议修法**（供你决定，我不动语音代码）：
- 用 `Matrix` + `Shader.setLocalMatrix()` 移动已有 shader，避免重建；
- 或在 `onSizeChanged` 时缓存 shader，`onDraw` 只做赋值。

**② 5 处 `onTouch` 未调用 `performClick`**（`ClickableViewAccessibility`）

位于 `JinnIme:495/1078`、`PinyinKeyboardView:726/896`。影响 TalkBack 等无障碍操作。
标准修法是在判定为点击时补一次 `performClick()`，但**键盘手势判定逻辑敏感**，
改动有触碰长按/滑动手势的风险，需你确认后再做。

**③ 49 处 `UnusedResources`**：可清理若干 KB，纯体积优化，无功能影响。

## 四、验证

| 项 | 结果 |
|---|---|
| Android Lint | ✅ **2 error → 0 error**（88 warnings 保留） |
| 单元测试 | ✅ 10 个类 166 用例全绿 |
| APK | ✅ 6.2MB，签名 V1=false/V2=true/V3=true |
| 对齐 | ✅ 4 字节对齐校验通过 |
| 真机安装 | ✅ Success（PACM00） |

## 五、累计

- 已修复 / 改进：**84 项**
- Lint：0 error

---

# 第二十六轮：无障碍 performClick、语音入口连通性、保活文案清理

## 一、无障碍：onTouch 补 performClick（Lint 5 → 2）

Lint `ClickableViewAccessibility` 要求自定义触摸处理在识别到点击时通知无障碍服务。
**原则：只在确认是「点击」时补，且不改动原手势返回值**。

| 位置 | 处理 |
|---|---|
| `PinyinKeyboardView` 字母键 | **仅当 `ACTION_UP` 且水平位移 < `touchSlop`** 时补——该键有「符号层左右滑动翻页」手势，滑动结束时发点击事件会误导 TalkBack |
| `PinyinKeyboardView` 退格键 | `ACTION_UP` 时补（只有点击/长按连删，无滑动） |
| `JinnIme` 退格键 | `ACTION_UP` 时补（`CANCEL` 不补） |
| `JinnIme` 麦克风按钮 | `ACTION_UP` 时补。该按钮**没有 OnClickListener**，`performClick()` 只向无障碍服务补发一次事件，不影响手势判定与录音 |

**剩余 1 项保留**：Lint 另要求 `MicButton` 自身重写 `performClick()`，
但 `MicButton` 属语音部分（受「禁止修改语音」硬约束保护），未改动。

## 二、语音入口增加服务连通性判断（本轮核心）

**需求**：长按空格进语音前先判断服务器连通性，未连通就直接不给用，
避免语音功能带来任何额外负担。

**实现**（`JinnIme.switchToVoiceKeyboard()`）：

```kotlin
if (asr.state != LinkState.ONLINE) {
    Diagnostics.i(TAG, "语音服务未连通(${asr.state})，拒绝进入语音功能")
    Toast.makeText(this, getString(R.string.voice_offline_blocked, prefs.host, prefs.port), LENGTH_SHORT).show()
    return                       // 不切换键盘模式，停在拼音键盘
}
// 在线才继续原有切换逻辑
```

**拦在这里的价值**：否则用户进了语音面板才发现连不上，会白等一次 WebSocket 握手，
并连带触发录音权限检查、录音器初始化等一整套语音链路负担——对一个用不了的面板而言全是浪费。

新增文案：`voice_offline_blocked` = 「语音服务未连接（%1$s:%2$d），暂不能使用语音输入」。

> 说明：这属于**入口控制**，没有改动 `AsrClient`/录音链路本身，符合「禁止修改语音链路」的约束。

## 三、清理保活遗留文案 19 条

第十八轮移除保活机制时漏删的字符串，本轮一并清掉：

```
keepalive_channel / keepalive_notice / keepalive_on / keepalive_off
accessibility_desc / accessibility_summary / accessibility_open
settings_keepalive / settings_keepalive_desc
switch_keepalive / switch_root / switch_notify_high
btn_accessibility / btn_battery / btn_root
battery_open / root_done / root_fail / root_need
```

**关于其余 30 项 `UnusedResources` 保持不动的原因**：
项目已启用 `isShrinkResources = true`，这些资源在 **release 构建中已被自动裁掉**
（APK 里本来就没有），清理源码定义**不会减小 APK**，只影响 warning 数量；
而它们存在被动态引用的可能（如 `getIdentifier`），删除有风险。**收益为 0、风险不为 0，故不动。**

## 四、验证

| 项 | 结果 |
|---|---|
| Android Lint | ✅ **0 error，88 → 64 warnings**（`ClickableViewAccessibility` 5 → 2） |
| 单元测试 | ✅ 10 类 166 用例全绿 |
| APK | ✅ 6.2MB，签名与 4 字节对齐校验均通过 |
| 真机：字母键点击 | ✅ `qie` → 且/切/窃（点击未被 performClick 改动破坏） |
| 真机：符号层滑动 | ✅ 翻页到「常用 第 2/2 页」——**滑动未被误判为点击** |
| 真机：长按空格 | ✅ 服务在线（顶部「已连接 192.168.1.3:6016」）时正常进入语音键盘，未被误拦 |

## 五、累计

- 已修复 / 改进：**90 项**
- Lint：0 error / 64 warnings

---

# 第二十七轮：Binder 超限崩溃修复 + 词库压缩调优（APK 6.2 → 6.1MB）

## 一、缺陷：粘贴广播携带全文 → `TransactionTooLargeException` 崩溃

**定位**（`ClipboardHistoryActivity.requestPaste()`）：

```kotlin
sendBroadcast(Intent(ACTION_CLIPBOARD_PASTE)
    .putExtra(EXTRA_CLIPBOARD_PASTE_ITEM_ID, item.id)
    .putExtra(EXTRA_CLIPBOARD_PASTE_TEXT, item.content))   // ← 整条正文塞进广播
```

**为什么必然出事**：
- 广播经 **Binder** 传递，事务上限约 **1MB**；
- 而剪贴板**单条正文没有长度上限**（`ClipboardPrefs.maxItems` 只限制条数）；
- 用户复制长文章 / 日志 / 大段代码后点「粘贴」→ `sendBroadcast` 抛
  `TransactionTooLargeException` → **直接崩溃**。

这正是「文本类应用 + Android 14+」的典型闪退源（skill 里点名的高频元凶）。

**修法：广播只传 id，正文由接收方按 id 读库**

| 位置 | 改动 |
|---|---|
| `ClipboardDb` | 新增 `contentById(id)`（含读库 + AES 解密，标注必须走 `BackgroundIo`） |
| `ClipboardHistoryActivity` | 广播只带 `EXTRA_CLIPBOARD_PASTE_ITEM_ID`，去掉正文 |
| `JinnIme` 接收器 | 按 id 读库（`BackgroundIo`）→ 回主线程 `pasteClipboardText` |
| 常量 | 删除已无引用的 `EXTRA_CLIPBOARD_PASTE_TEXT` |

**三处剪贴板入口核对**（项目约定：隐私/安全改动必须三处同步）：
只有 `ClipboardHistoryActivity` 走广播；`ClipboardPanelView`（键盘内嵌）与
`SearchPanelView`（顶部搜索）是**直接回调**，不经过 Binder，无需同步改动。

> 注：崩溃场景需要 >1MB 的剪贴板内容才能复现，无法用 adb 便捷构造，
> 因此**这条只有代码逻辑与编译保证，没有真机崩溃复现证据**。

## 二、APK 体积分析：还有空间吗

### 体积构成（实测）

| 部件 | 压缩后 | 占比 |
|---|---|---|
| **`assets/pinyin_phrases.txt.xz`** | **5.74 MB** | **93.4%** |
| `classes.dex` | 0.26 MB | 4.2% |
| `pinyin_chars.txt` | 0.04 MB | 0.7% |
| okhttp `publicsuffixes.gz` | 0.04 MB | 0.6% |
| 其余（arsc/res/manifest…） | ~0.07 MB | ~1% |

**结论：APK 就是词库，其他都已优化到位**（R8 + `shrinkResources` 生效）。

### 能压的都试了

| 方案 | 结果 | 决策 |
|---|---|---|
| **xz 调参** `preset=7 + lc=4 + pb=0` | 5.743 → **5.652 MB（-1.59%）**，解压字典 8→16MiB | ✅ **采用** |
| xz `9e`（64MiB 字典） | 再省 0.14%（-5KB）但要 **+48MiB 解压内存** | ❌ 不值 |
| **brotli** `-q11 -w24` | 5.653 MB（-1.57%） | ❌ Android 无内置解码，引库反增体积 |
| 二进制词库格式 | 预估 -5~10% | ⚠️ 工程量大、风险高，未做 |
| 删低频词 / 更多词移到扩展包 | 可达 -10~40% | ❌ **有损**，会牺牲输入体验，需你决定 |
| 扩展包同步重压 | 只省 5KB | ❌ 要重传两个仓库 Release 附件，不值 |

**关键洞察**：词库是「用体积换输入体验」的资产。当前 6.1MB 里 93% 是词库，
**无损压缩已到极限（再挤只剩 1.5%）**；若还要显著变小，只能走「砍词」这条路，
那是体验取舍，不该由我替你决定。

### 实施与验证

- 重压基础包并**逐字节解压校验**（无损）✓
- `PhraseDictIntegrityTest`（直接校验 asset 词库）通过 ✓
- **真机**：词库加载 **8550ms**（优化前 9514ms，**反而快了 ~1 秒**）；
  输入 `qie` → 且/切/窃 正常 ✓
- APK：**6.2 → 6.1 MB**，签名与 4 字节对齐校验通过

## 三、累计

- 已修复 / 改进：**92 项**
- APK：**6.1MB**｜Lint 0 error｜单测 166 用例全绿

---

# 第二十八轮：词库整体替换为 rime-ice（APK 6.1MB → 4.5MB）

## 一、为什么换

前几轮排查发现原词库有三个结构性问题：

| 问题 | 影响 |
|---|---|
| **无词频字段** | 候选无法按常用度排序，"低频"也无从定义 |
| **16.2% 的行有 GBK 误解码残留** | `瀵勮繙`/`浣犲ソ` 这类**永远打不出来**，纯废条目 |
| **连写歧义词需手工维护** | 企鹅/西安/饥饿 等靠人肉补，易漏 |

rime-ice（雾凇拼音）三个都解决：**100% 带词频**、已校对无损坏、原生含歧义词。

## 二、转换（纯读取 + 统计，不碰 docs）

新脚本 `tools/dict_builder/convert_rime_ice.py`（可重跑）：

```
docs/rime-ice/cn_dicts/{base,ext}.dict.yaml
  → 解析 `词<TAB>拼音(空格分隔无调)<TAB>词频`
  → 拼音去空格即本项目的键
  → 按词频降序聚合 → 输出 `键<TAB>词1|词2|…`
  → 按 ≤4字/>4字 切分成基础包与扩展包
```

## 三、结果

| 指标 | 原词库 | rime-ice | 变化 |
|---|---|---|---|
| **APK** | 6.1 MB | **4.5 MB** | **-26%** |
| 基础包 xz | 5.65 MB | 4.10 MB | -27.4% |
| 扩展包 xz | 2.19 MB | 1.80 MB | -17.8% |
| 词库加载 | 8550 ms | **6602 ms** | **-1.9s** |
| 拼音键 | 826,897 | 787,991 | -4.7% |
| 词条 | 1,006,326 | 882,180 | -12.3%（多为损坏词） |

## 四、真机实测

| 拼音 | 候选 | 说明 |
|---|---|---|
| `qie` | **[企鹅**, 且, 切] | 手工补过的歧义词，排第一 |
| `xian` | **[西安**, 西岸, 锡安] | 同上 |
| `jie` | **[饥饿**, 极恶, 极饿] | 同上 |

**原先手工维护的歧义词 rime-ice 全都原生支持，且靠词频自然排在首位。**

---

# 第二十九轮：清洗游戏词库并合并进扩展包

## 一、来源与格式

`docs/games_ime_dict.txt`（1361 条）+ `docs/klbq_ime_dict.txt`（329 条），
格式与 rime-ice 一致：`词 拼音(空格分隔无调)`。docs/ 只读，脚本仅读取。

## 二、清洗：删 145 条垃圾词，保留 1545 条

| 类别 | 条数 | 例子 |
|---|---|---|
| **含数字或字母** | 139 | `CS2` `CSGO` `DOTA2` `DNF` `刀塔2` `奥卡努斯2` `2号仓库2` |
| 超长(>8字) | 5 | `前面的区域以后再来探索吧`、`行踪不定,暂无固定的居所`（句子） |
| 含标点 | 1 | `千代(ちよ)` |
| **保留** | 1545 | 纯中文，允许间隔号 `·`（`奥黛丽·格罗夫`） |

**为什么字母数字必须删**：拼音引擎按**合法音节**切分，字母/数字切不开
（单字母合法音节只有 `a e m n o`），当键**永远打不出来**——与网络词 `yyds` 同理，是纯废条目。

## 三、合并（merge 模式）

- 扩展包 183,919 → **185,408 键**（新增 1,489 键、追加 1,506 词）
- 词条总数 **186,073**；体积 1.80 → 1.81 MB（+0.01MB）
- 同键已有词保留在前、游戏词追加在后，**不覆盖基础候选**

## 四、验证

**词条入库**（解压校验）：`阿拉德/alade`、`崩坏星穹铁道/benghuaixingqiongtiedao`、
`艾泽拉斯/aizelasi`、`奥黛丽·格罗夫/aodailigeluofu`、`艾卡/aika`、`白墨/baimo` 全部在位

**真机**：日志确认「扩展词库: 已加载（长词可用）」

**Release 附件同步**（关键动作，否则用户下载到旧包）：

| 源 | 结果 |
|---|---|
| GitHub | ✅ 覆盖上传，1,901,048 字节 |
| Gitee | ✅ 删旧附件(id 3201963) → 传新(id 3204221)，1,901,048 字节 |
| 两个下载链接实测 | ✅ Gitee 302→200、GitHub 200，Content-Length 均 1,901,048 |

> 方法记录：Gitee 的 `releases/tags/{tag}` 接口返回的 assets **没有 id**，
> 必须用 `releases/{release_id}/attach_files` 才能拿到附件 id 来删除。

## 五、新增工具

- `tools/dict_builder/convert_rime_ice.py` —— rime-ice 词库转换 + 对比报告
- `tools/dict_builder/merge_game_dicts.py` —— 游戏词库清洗 + 合并（`--dry-run` 可先看报告）
- `词库重构-AI提示词与公开词库参考.md` —— 词库格式规范、拼音硬约束、网络词收集提示词

## 六、累计

- 已修复 / 改进：**96 项**
- **APK：4.5MB**（从 10.6MB 一路优化，累计 -58%）｜Lint 0 error｜单测 166 用例全绿

---

# 第三十轮：深挖词库 BUG —— 修复扩展包合并产生重复候选

## 一、缺陷：merge 时直接拼接、不去重

`PinyinEngine.loadPhrasesReader()` 的 merge 分支原本是：

```kotlin
val existing = phrasesByPinyin[pinyin]
phrasesByPinyin[pinyin] = if (existing != null) existing + kept else kept.toTypedArray()
```

**`existing + kept` 直接拼接，没有去重**。而扩展包与基础包**会收录同一个词**，合并后候选栏出现两个完全相同的候选：

| 键 | 合并后的候选 |
|---|---|
| `aersasi` | [**阿尔萨斯**, **阿尔萨斯**] |
| `aizelasi` | [**艾泽拉斯**, **艾泽拉斯**] |
| `airen` | [爱人, **矮人**, **矮人**] |
| `aixi` | [爱惜, **艾希**, **艾希**] |

**影响面实测**：**877 个词条**会产生重复候选，其中 **404 个键完全重复**。

## 二、根因有两层，代码层才是根治

| 来源 | 归属 | 修法 |
|---|---|---|
| **我合并游戏词时**只查扩展包、没查基础包 | 数据层 | `merge_game_dicts.py` 合并前载入基础包全部词并排除（本次跳过 415 条） |
| **rime-ice 自身的 base/ext 就有重叠**（如 `aersasi` 两边都收） | **代码层** | merge 时先建「已见」集合，扩展包只追加未出现过的词 |

**关键认知**：数据层只能治「我自己引入的重复」，**第三方词库自带的重叠必须靠代码层兜住**——
本次数据修完仍剩 613 个重叠键、467 个重复词条，全部由代码层消除。

## 三、修复后的效果

| 键 | 修复前 | 修复后 |
|---|---|---|
| `aersasi` | [阿尔萨斯, 阿尔萨斯] | **[阿尔萨斯]** ✓ |
| `aizelasi` | [艾泽拉斯, 艾泽拉斯] | **[艾泽拉斯]** ✓ |
| `airen` | [爱人, 矮人, 矮人] | **[爱人, 矮人]** ✓ |
| `aixi` | [爱惜, 艾希, 艾希] | **[爱惜, 艾希]** ✓ |

去重开销只落在**真有重叠的键**上（现 613 个），对 60 万键的加载耗时可忽略。

## 四、顺带排查（确认无问题，留证）

| 检查项 | 结果 |
|---|---|
| **键能否被合法音节切分** | ✅ 基础包 604,077 键 + 扩展包 185,408 键，**切分失败 0** |
| **常用词覆盖** | ✅ 抽查 20/20 命中（你好/中国/北京/手机/电脑/朋友/什么/怎么/知道…）|
| **单字表是否需替换** | ❌ **不需要**：`pinyin_chars.txt` 有 17,117 字符，**完全覆盖** rime-ice 的 8105 字表（8,182）且更全 |
| 单字表编码损坏 | ✅ 仅 1 处（`覅`），且属检测方法误报 |

> 另注：日志里的 `音节=414`（而非合法音节表的 422）**不是 BUG**——
> 414 是**单字表实际覆盖到的音节数**，缺的 8 个（`biang`/`fiao`/`junding` 等）本就没有对应单字。

## 五、验证与同步

- `lintDebug` + `testDebugUnitTest` 通过；APK 4.5MB，签名与 4 字节对齐校验通过；真机安装 Success
- 扩展包已变更 → **Release 附件同步**：GitHub（`--clobber`）+ Gitee（删旧 id 3204221 → 传新 id 3204300）
- 两个下载链接实测 `Content-Length` 均为 **1,897,940** ✓

## 六、累计

- 已修复 / 改进：**98 项**｜APK 4.5MB

---

# 第三十一轮：可选词库延迟加载 + 分类词库页（词库链路专项）

**背景**：基础包换 rime-ice 后，可选包（长词包 1.8MB / 腾讯大词库 6.4MB）与基础包在
`onCreate` 一起加载，使「开机后首次输入的候选就绪」从 6.6s 拖到 25.4s。

## 一、核心改动

### 1. 可选词库改为延迟加载

`PinyinEngine.load()` 只加载基础包；新增 `loadOptionalAsync(delayMs = 5000)`，
在基础包就绪 5 秒后于后台补齐可选包。IME 加载时机实测确认：弹/收键盘走
`onStartInputView/onFinishInputView`，**不碰词库**，所以「加载耗时」只影响
「IME 服务重建后的首次输入」，日常打字无影响。

**实测**：基础包 6.4s 就绪（与不装可选包一致），可选包 26s 后后台就绪（172 万键）。

**关键细节**：可选包加载后**必须重新 `finalizeLoad()`** —— `sortedPhraseKeys` 是加载时
快照，不重建则新词不参与前缀补全。

### 2. 新增分类词库页

- `OptionalDicts.kt`：词库清单（文件名/说明/体积/启动耗时/下载源），新增词库只需加一条记录
- `DictManagerActivity.kt`：列表 + 下载 / 删除 + 顶栏关闭按钮；视觉沿用剪贴板页配色
  （背景 `#0B1020`｜顶栏 `#141C33`｜卡片 `#1C1F26`｜强调 `#4C8DFF`）
- 设置页的旧扩展词库卡片（URL 输入框 + 下载/删除按钮）换成单按钮「打开分类词库」

### 3. 词库容器线程安全化（并发缺陷修复）

延迟加载在**后台线程** merge 词库，而用户此刻正在打字、主线程在 `query()` 并发读同一批容器。
原实现全是普通 `HashMap`/`HashSet`/`ArrayList`：扩容可能让桶链表成环（读方死循环、IME 卡死），
或翻抛 `ConcurrentModificationException`；有序表用 `clear()+addAll()` 会让读方看到中间态。

- Map 改 `ConcurrentHashMap`（容量在声明处预分配）
- `validSyllables`/`syllablePrefixes` 改 `ConcurrentHashMap.newKeySet()`
- 三个有序表改 `@Volatile` 不可变快照 + **整体替换引用**
- `loadOptionalAsync` 的「检查-置位」纳入同一把锁

**真机验证**：基础包就绪后立刻连续打字（触发原先的竞态窗口），全程 0 异常。

### 4. 其余修复

| 问题 | 根因 | 修复 |
|---|---|---|
| 候选可达 60 条且每次按键全量重建 View | `yi` 有 326 字、93 个音节超 `MAX_CHARS(60)`，渲染无上限 | 截断渲染为 24 条（上屏逻辑不受影响，`lastCandidates` 仍存完整列表）|
| 加载失败静默无痕 | 裸线程 + `load()` 无 try/catch，异常穿透后线程死亡 | `JinnIme` 侧 `runCatching` + `Diagnostics.e(msg, tr)` 保留堆栈 |
| 下载失败用户看不到提示 | 回调写进已重建 Activity 的旧 View | 失败时额外弹 Toast（不受 Activity 生命周期影响）|
| merge 去重无测试守护 | 测试入口 `loadFromTexts` 固定 `merge=false`，分支不可达 | 补 `loadPhrasesText(merge)` + `mergePhrasesForTest`，新增 `OptionalDictMergeTest` 7 用例 |
| 候选栏渲染 60 View | 见上 | 见上 |

## 二、验证

- `compileDebugKotlin` + `testDebugUnitTest`（183 用例 / 14 类）+ `lintDebug` 全通过
- APK 4.5MB，对齐与签名校验通过
- 真机：基础包 6409ms 就绪、可选词库后台就绪、候选栏输入 `yi` 命中「易/翼/一/以/已…」、0 崩溃

---

# 第三十二轮：剪贴板模块专项（死代码清理 + 语义修正 + 状态同步）

## 一、删除已下线的剪贴板历史页

实跑发现 `ClipboardHistoryActivity`（719 行完整实现）**不可达**：
`exported=false` + 全项目无任何 `startActivity` 调用点。用户确认为**有意下线**后清理：

- 删除 `ClipboardHistoryActivity.kt`（719 行）与 Manifest 声明
- 删除 22 个历史页专用字符串资源（**保留** `clipboard_root_*` 等设置页仍在用的）
- `AGENTS.md`：「三处入口」修正为「**两处入口**」；删除 4 条描述 ListView /
  Activity 生命周期 / FLAG_SECURE 的失效约束

**现状**：剪贴板只有两个展示入口 —— `ClipboardPanelView`（键盘内嵌面板）+
`SearchPanelView`（顶部搜索面板），改动需**两处同步**。

## 二、容量裁剪改为三级优先级

原 `trimTo` 只豁免收藏（`WHERE is_favorite = 0`），隐私记录会随「最旧非收藏」被删除。
虽代码/注释/约定三者一致（属有意设计），但隐私是用户刻意主动标记的，语义上更该保留。

新增 `TRIM_PRIORITY` 按顺序分轮取证、凑够即停：

1. `is_favorite = 0 AND is_private = 0`（普通记录）
2. `is_favorite = 0 AND is_private = 1`（隐私，最后才动）
3. **收藏永不参与裁剪**

每轮只取「还需删的条数」，因此普通记录够删时完全不触碰隐私 —— 不会因「隐私太多」而无法裁剪。

## 三、修复拖选状态未同步到键盘侧

`selectionActive` 在 `JinnIme` 与 `PinyinKeyboardView` 各存一份，靠显式调用同步。
两处「复位」逻辑分别实现，其中 `onClipboardStateChanged` 漏了同步调用：

```
onSelectionModeChanged  复位后 **有** pinyinKeyboard?.setSelectionActive(active)
onClipboardStateChanged 复位后 **没有** 同步调用    ← 缺陷
```

**根本原因**：同一语义的复位逻辑在两处重复实现，各自演化 —— 典型的「复制粘贴式重复」
导致的不一致。已让两处对齐。

## 四、剪辑板布局数据抽离（顺带的架构改进）

`PinyinKeyboardView` 内嵌 455 行静态配置数据（QWERTY 行定义 + 数字层映射 +
符号分组含日文假名表），与视图逻辑混杂。抽出为 `KeyboardLayouts.kt`：

**视图类 2029 → 1574 行**。验收采用**逐项比对**：提取全部 `'x' to "Y"` 键值对，
旧 1263 / 新 1263 **序列完全一致** —— 编译与常规测试都发现不了数据丢失/串位，
只有用户按到那个键才会暴露。

## 五、验证

- `compileDebugKotlin` + `testDebugUnitTest`（183 用例）+ `lintDebug` 全通过
- 真机：设置页剪贴板卡片与「历史数量上限」正常渲染、键盘面板可开、符号层正常、0 崩溃
- 新增 `ClipboardClassifierBoundaryTest`（6 用例）：空白包裹、长数字+换行（含 CRLF）、
  长度边界、含数字负例、空白输入、URL 优先级

## 六、累计

- 本轮净减代码约 **1800 行**（含删除 719 行死代码、抽离 455 行数据）
- 测试用例 **166 → 183**（+17）

---

# 第三十三轮：26 键区圆角与按键间隙改为设置页可调

**日期**：2026-09-16 ｜ 验证：`testDebugUnitTest`（184 用例）+ `lintDebug`（0 error）+ 真机

## 一、需求与现状

要求：把 26 键区（布局上整整三行、共 **28 个按键** —— 10 字母 + 9 字母 + 「大写 + 7 字母 + 删除」）
的**按键圆角**与**按键左右/上下间隙**做成设置页可调项，定义域合理。

现状有个隐藏的不一致：这 28 键虽然视觉上连成一片，绘制路径却是两条：

| 键 | 绘制方式 | 原参数 |
|---|---|---|
| 26 个字母键 | `PinyinKey.onDraw` 自绘圆角矩形 | `KEY_CORNER_DP = 0f` / `KEY_MARGIN_DP = 0f`（硬编码常量） |
| 大写键 / 删除键 | XML `ImageButton` + `key_bg.xml` 背景 | 圆角 **10dp**，且贴满单元格（无内缩） |

也就是说同一行里两种圆角，且大写/删除键比字母键高一圈。做成「统一参数」正好一并修掉。

## 二、实现

1. **新增 `KeyAppearance.kt`（纯 JVM，可单测）**：定义域、默认值、进度换算收敛到一处。
   - 圆角 **0 ~ 24dp**（步进 1dp，0 为直角，>24 会削成胶囊失去按键辨识度）
   - 间隙 **0 ~ 8dp**（步进 0.5dp，0 为无缝；8dp 时最窄的字母键仍剩 23dp 可点）
   - 默认 **0dp / 0dp** —— 与加入参数之前的外观完全一致（增量修改，不偷偷改观感）
   - NaN / Infinity 一律退回默认值：`coerceIn` 对 NaN 的比较恒为 false，不拦住会一路传到
     `drawRoundRect`（整个键面画不出来）
2. **`Prefs`**：新增 `keyCornerDp` / `keyGapDp`，**读写都钳位**（历史配置或外部写入可能越界）。
3. **`PinyinKey`**：删掉 `KEY_CORNER_DP` / `KEY_MARGIN_DP` 两个硬编码常量，改为
   `setKeyAppearance(cornerPx, insetPx)`。四边**等量**内缩（上下也缩，原来只有左右缩 +
   顶部固定 8px），并加「内缩不超过键尺寸一半」的保护。
4. **`PinyinKeyboardView`**：新增 `applyKeyAppearance()`，把同一组参数推给三类键：
   - 26 个字母键 → `setKeyAppearance`
   - 大写/删除键 → 用 `GradientDrawable` + `RippleDrawable` 动态重建**同圆角**背景
     （保留按压水波纹；mask 必须单独造不透明同圆角矩形，否则波纹被裁没）
   - 并把内缩等效成四周 `LayoutParams` 外边距，否则普通 View 会贴满单元格、与字母键错位
   - 大写锁定切换改走 `refreshShiftBackground()`（原来直接 `setBackgroundResource`，圆角写死）
5. **生效时机**：`applyKeyAppearance()` 挂在 `init` 与 `configure()`；而 `configure()` 由
   `onStartInputView` 每次聚焦时调用 —— 所以**设置页改完，收起键盘再弹出即生效，无需重启进程**。
6. **设置页**：新增「键盘外观」卡片（两个 SeekBar + 当前值 + 生效说明），拖动即时落盘。

## 三、间隙语义（写进 KDoc，避免后人误解）

「间隙」= **相邻两键之间的空隙**：每个键四边各内缩 `gap / 2`，相邻两键相加正好等于 gap，
左右与上下一致。不是「每边留多少」（旧常量 `KEY_MARGIN_DP` 是每边内缩语义，已随常量删除）。

## 四、验证

- `testDebugUnitTest`：**184 用例全绿**（新增 `KeyAppearanceTest` 11 例：钳位 / NaN /
  步进 / 全进度往返 / 定义域 / 上界可点性）
- `lintDebug`：**0 error / 34 warning**（warning 为既有基线）
- 真机（PACM00，1080×2280）：`build_apk.py` 产物 `adb install -r` 覆盖安装 →
  - 设置页「键盘外观」卡片渲染正常，滑杆 → 数值联动（实测拖到 24dp / 4dp，标签同步显示）
  - 收起键盘再弹出：**28 键（含大写/删除）圆角与间隙同步生效、行高完全对齐**（截图放大复核）
  - 日志侧证：`PinyinKeyboard: 键盘外观: 圆角=24 dp 间隙=4 dp`
  - 复位 0dp / 0dp 后恢复「连成一片的直角键区」
- 测试用例数勘误：第三十二轮记的 183 与实际不符，**实测本轮前 173、本轮后 184**（以实测为准）。

## 五、明确未纳入

第四行（符号 / 数字 / 逗号 / 空格 / 中英文 / 回车）仍用 `key_bg.xml` 的 10dp 圆角，
**不跟随**本参数 —— 需求只圈定了 28 键的三行区域。若要整块键盘统一，再加一次即可。

## 六、发布（覆盖式）

- 提交 `08a9f31`（10 文件，+678 / −15）推送到三处引用，全部为快进：
  GitHub `main`、GitHub `perf/paging-pinyin-rematch`、Gitee `master`（均 `d730404` → `08a9f31`）。
- APK 重新构建：`versionCode=20260916` / `versionName=jinn`，4,737,805 字节，
  `apksigner` 校验 V1=false / V2=true / V3=true，4 字节对齐通过。
- 两仓库 `v20260916` Release 的 `jinn-release.apk` **覆盖**：
  - GitHub：`gh release upload v20260916 jinn-release.apk --clobber`
  - Gitee：`GET /releases/{id}/attach_files` 取附件 id → `DELETE` 旧附件 → `POST attach_files` 上传新包
- Release 正文各补一条本条功能说明（变更内容 + 验证各一行），其余文字逐字保持原样。
- 校验：两个下载 URL 均 HTTP 200，`size_download=4,737,805`，**sha256 与本地 APK 完全一致**。

---

# 第三十四轮：独立复核后的 6 项修复（死代码 / 回显 / 无障碍 / 颜色收敛 / 绘制分配）

**日期**：2026-09-16 ｜ 验证：`testDebugUnitTest`（184 用例）+ `lintDebug`（0 error）+ 真机逐项实测

## 一、来源与范围

本轮来源是**自己的独立复核**（`docs/代码审查-20260916-独立复核.md`），十七项候选里只采纳 6 项，
其余九项在取证后被否（外部清单的裁定见 `docs/chatGLM建20260916232359.md` 对照）。

- **采纳**：死代码两处、设置页回显、设置页自动填充、颜色收敛、绘制分配与单位。
- **明确不做**：第四行（符号 / 数字 / 逗号 / 空格 / 中英文 / 回车）圆角不跟随 28 键参数——
  **用户要求保持现状，禁止改动**，此后不得再动。

## 二、修复明细

1. **死代码 `onBackspacePressed()`（`PinyinKeyboardView`）**：全文件只出现一次；删除键真实路径是
   `setOnTouchListener → handleBackspaceTouch → deleteOne()`，且 `deleteOne()` 是它的**严格超集**
   （多一个预测态分支 + V 日志）。删 19 行。
   > 顺带说明：**它就是外部审查被带偏的源头**——对方把这段死代码当活代码逐行分析。
2. **死代码 `formatSize()`（`SettingsActivity`）**：本文件内无调用；`DictManagerActivity` 有独立副本且在用。删 6 行。
3. **剪贴板上限越界后界面不回显真值**：输入 `0` / `99999` 会被 `Prefs` 钳到 `1` / `9999`，但原实现只在
   解析失败时才回写输入框 → 界面显示与生效值不一致。改为**存完统一回写生效值**（仅在显示不同时 `setText`）。
4. **设置页 5 个 `EditText` 关闭自动填充**（`android:importantForAutofill="no"`）：避免密码管理器把
   host / port / 提示词 / 上限当账号字段弹「保存密码」。Lint `Autofill` 5 条清零。
5. **硬编码颜色收敛**：`ClipboardPanelView` / `SearchPanelView` / `DictManagerActivity` 共 **33 处**
   `Color.parseColor` → `R.color.*`。`colors.xml` 新增 `danger` / `ok` / `warn` / `btn_disabled` 四个
   色值（其余复用既有同名同值项：`app_bg` / `card_bg` / `surface_hi` / `text_primary` /
   `text_secondary` / `accent` / `text_on_accent`）。**逐值等价、零行为变化**。
   `DictManagerActivity` 里 9 个 `COLOR_*` 常量位于 `companion object`（取不到 Context），整块删除，
   调用点改用 `getColor(R.color.x)`。
6. **`PinyinKey` 绘制分配与字号下限单位**：
   - `subLabel` / `subLabelRed` 的 `split + filter + trim` 从 `onDraw` 移到 setter 缓存
     （绘制路径零分配，行为不变：空行丢弃、去首尾空白）；
   - `MIN_LONG_TEXT_SIZE = 9f`（像素）→ **`MIN_LONG_TEXT_RATIO = 0.06f`**（占键高比例）。
     旧值在 mdpi 上等于 9dp，比按宽度算出的适配字号还大，会把 `synchronized` 这类长标签顶出按键
     左右边界；改比例后任何密度都自洽，本机 2.75x 上等效 ≈ 8.9px，与旧观感一致。

## 三、验证

**门禁**：`testDebugUnitTest` **184 用例全绿**；`lintDebug` **0 error / 34 → 29 warning**（Autofill 5 条清零）。

**真机（PACM00，覆盖安装后逐项实测）**：

| 项 | 证据 |
|---|---|
| 上限回显 | 输入 `0` → 失焦后日志 `剪贴板历史数量上限: 1`，**输入框回显 1**（改前恒显示 `0`）；测完已把用户原值 `9999` 复原并复核 |
| 删除键（死代码删除后） | 单击 → `退格删拼音: b`；按住 1.5s → `退格删拼音:`（连续删除生效），无崩溃 |
| 双拼键面提示 | 多行提示 `uai/ing`、`ong/iong`、`iang/uang` 全部正常渲染（验证缓存改造没丢内容） |
| 符号层长文本 | 编程分组 `self/None/elif/lambda/global/assert/__name__` 按宽度自适应，无溢出、无裁切 |
| 颜色 | 剪贴板面板空态、分类词库页（顶栏 / 卡片 / 次文字 / warn 橙 / ok 绿 / danger 红 / accent 蓝）与改前一致 |
| 28 键区外观 | 仍为默认 0dp / 0dp 连片直角块（本轮未触碰外观参数） |

## 四、累计

- 测试用例 **184**（不变，本轮无新增可测纯逻辑）；Lint **0 error / 29 warning**

## 五、发布（覆盖式）

- 提交 `798b874`（9 文件，+157 / −91）推送到三处引用，全部快进：GitHub `main`、
  GitHub `perf/paging-pinyin-rematch`、Gitee `master`（均 `68e671c` → `798b874`）。
- APK：`versionCode=20260916` / `versionName=jinn`，**4,737,805 字节**，
  `sha256=f45bf09d…`，`apksigner` V1=false / V2=true / V3=true，4 字节对齐通过。
- 两仓库 `v20260916` Release 的 `jinn-release.apk` 覆盖：GitHub `gh release upload --clobber`；
  Gitee 取旧附件 id `3212831` → DELETE → 重新 `attach_files`（新 id `3212991`）。
  Release 正文各补本轮条目（变更内容 + 验证各一条），其余文字逐字保留。
- 校验：两支下载 URL 均 HTTP 200、`size_download=4,737,805`、**sha256 与本地 APK 完全一致**。
- 过程记录：GitHub 侧当晚出现多次瞬时抖动（`git ls-remote` / `api.github.com` 报
  TLS unexpected eof），push 与 upload **重试一次即成功**，非权限问题。

---

# 第三十五轮：删除废弃词库源文件 + 文档全量对齐

**日期**：2026-09-17 ｜ 影响面：**无代码变更，不重编 APK**（`tools/` 与 `docs/` 不参与构建）

## 一、删除

按要求删除 `tools/dict_builder/source/pinyin_phrases.txt`，顺带移除删空后的
`tools/dict_builder/source/` 目录（约 28MB 磁盘回收）。

**删除前指纹（留档备查，文件已不可恢复）**

| 项 | 值 |
|---|---|
| 大小 / 行数 | 29,370,289 字节 / 1,048,342 行 |
| sha256 | `b3519a87176b22e45101fa418a9c859576834423e3efc7f77bd6c15a64500129` |
| 版本库状态 | 被 `.gitignore`（`tools/dict_builder/source/`）忽略，**从未入库，删后无法从 git 恢复** |

**删除前确认（三条都已核实）**

1. 构建链（`build.gradle.kts` / `app/build.gradle.kts` / `build_apk.py` / `settings.gradle.kts`）
   与现行词库脚本（`convert_rime_ice.py`、`convert_rime_tencent.py`）**均不读取它**；
2. 全仓已无 `source/pinyin_phrases` 硬编码引用；
3. 现行资产完好（`assets/pinyin_phrases.txt.xz`、`release/dict_ext.txt.xz`、`release/opt_tencent.txt.xz`）。

**删除理由（每条独立成立）**：第 28 轮起词库整体换成 rime-ice，流水线改读 `docs/rime-ice/`；
文件含 **573 行 GBK 乱码**（如 `鍙樺姩涓嶅眳` 应为「变动不居」、`b煤d脿o` 应为 `bùdào`，连拼音键都坏了）；
留着它只会让人误以为「改词库＝改这个文件」。

## 二、文档对齐到最新版

| 文档 | 改动 |
|---|---|
| `tools/dict_builder/README.md` | **新增**：现行流水线图（源 → 脚本 → 资产 → 发布要求）、脚本一览（在用 / 备用 / 评估 / 历史）、已删文件说明与指纹 |
| `AGENTS.md` | 新增「**词库来源与生成（重要）**」：现行源 `docs/rime-ice/`、改词库必做三件事、旧目录不得再放源文件 |
| `docs/DATA_SOURCES.md` | 「当前状态」整段按实测重写：基础包 604,077 行 / 4.3MB、扩展包 185,005 行、可选包 955,032 行、单字表 422 键、音节表 422 行、常用字 6,528 字；标注雾凇拼音已正式采用；补 GPL-3.0 说明与旧文件删除记录 |
| `docs/词库重构-AI提示词与公开词库参考.md` | 提示词模板里的源码路径改为 `docs/rime-ice/cn_dicts/`；第五节生效流程改写为现行流水线（旧步骤划掉留作历史） |
| `docs/计划任务.md` | #10.10 由「建议改名 `_DEPRECATED_` 或删除」→「✅ 已于 2026-09-17 直接删除」 |
| `docs/代码审查-20260916-独立复核.md` | 废料隔离项标记完成 |

顺带修正两处历史错数：`pinyin_chars.txt` 与 `pinyin_syllables.txt` 实测均为 **422**（旧文档写 416）。

## 三、刻意未动

- `docs/HANDOFF.md`、`docs/ONBOARDING.md` 等**带日期的历史快照**保持原样
  （按 `AGENTS.md` 约定 `docs/` 是本地历史资料目录、只读；且它们自带「文档生成时间 / 对应提交」标注）。
- **不重编 APK、不动 Release**：本轮无代码变更，第 34 轮产物（4,737,805 字节 / sha256 `f45bf09d…`）
  与当前代码完全对应，Release 附件保持有效。

---

# 第三十六轮：按 rime-ice 标准复刻 7 套双拼方案（含「26 键键面随方案变化」）

**日期**：2026-09-17 ｜ 验证：`testDebugUnitTest`（200 用例）+ `lintDebug`（0 error）+ 真机逐方案实测

> 需求原文：**按 rime-ice 的标准复刻**；一次不只做小鹤（搜狗/微软/紫光/ABC/加加一并做）；
> **双拼时 26 键键面必须随方案变化**（韵母提示是必须做的事）；**「输入方案」必须在「显示生僻字」上方**。

## 一、核心做法：键位不手抄，从 schema 生成

手抄 7 套方案（共约 3000 条键位）必然出错且无法自证，因此新增生成器
`tools/dict_builder/gen_shuangpin_tables.py`：

1. 解析 `docs/rime-ice/double_pinyin*.schema.yaml` 的 `speller/algebra`（**权威定义**）；
2. 按 **librime 的代数语义**（`xform` 替换 / `derive` 追加 / `erase` / `xlit` 逐字符映射）
   逐条施加到项目 `assets/pinyin_syllables.txt` 的 **422 个合法音节**上，算出每套方案的码；
3. 产出 `app/.../ShuangpinSchemes.kt`（**生成文件，勿手改**，730 行）：
   `initials`（声母键）+ `codes`（码 → 音节，扁平表）。

| 方案 | 声母特例 | 码数 | 需要分号键 |
|---|---|---|---|
| 自然码 | zh→v / ch→i / sh→u | 434 | 否 |
| 小鹤 | 同上 | 424 | 否 |
| 搜狗 | 同上 | 429 | **是**（`ing` 在 `;`） |
| 微软 | 同上 | 435 | **是** |
| 紫光 | zh→u / ch→a / sh→i | 417 | **是** |
| 智能 ABC | zh→a / ch→e / sh→v | 413 | 否 |
| 拼音加加 | zh→v / ch→u / sh→i | 429 | 否 |

## 二、换表安全性：与旧实现逐码对拍

`tools/dict_builder/verify_shuangpin_migration.py` 把旧的手写自然码实现照抄成 Python，
在 **676 个两键码**上与新生成的表逐一对拍：

- **3 处差异，全部是旧实现的 BUG**（新表正确）：`rw` 旧产出 `ria`（非音节）→ 应为 **`rua`（挼）**；
  `yo` 旧 `yuo` → 应为 **`yo`（哟）**；`hm` 旧 `hian` → 应为 **`hm`（叹词）**；
- 旧实现另有 **242 条独有码，产物全是非音节（垃圾）** → 换表**零功能损失**。

## 三、键面提示：由码表反推（这才是「随方案变化」的正确做法）

`ShuangpinTable.finalHint(key)` 在运行期从 `codes` 反推每个键的韵母，
`initialOf(key)` 给出 zh/ch/sh 红字。于是：

- 7 套方案的键面提示**各不相同且永远与引擎一致**（同一份数据，不可能漂移）；
- **顺带修掉一个历史缺陷**：旧 `shuangpinHint('o')` 手写成 `"ou"`，而引擎里 `o` 是 `o/uo`（`ou` 在 `b` 键）
  —— 键面在骗用户。现在自动派生为 `uo/o` ✓（`ShuangpinSchemesTest` 用回归用例钉死）；
- 特例自动跟随：加加的 `sh/ch/zh` 是 `i/u/v`、ABC 是 `v/e/a`、紫光是 `i/a/u`，红字提示自己就对了。

## 四、分号键：三套方案缺它就打不出 ing

键盘只有 26 个字母键，而**搜狗 / 微软 / 紫光的 `ing` 落在分号上**
（三个 schema 的 `alphabet` 都显式含 `;`）。没有它，应/听/明/定 这类音节完全打不出来。

做法：第三行 `m` 与删除键之间加一个 `PinyinKey`（`key_semicolon`），**只在当前方案需要时显示**，
其余方案与符号层/数字层一律 `GONE`（不参与测量 → 26 键布局与已调好的圆角/间隙零影响）。
键面与大写字母键同款：主文本 `;`、下方韵母提示 `ing`（提示同样取自方案表）。

## 五、状态与入口

- `Prefs.useShuangpin: Boolean` → **`Prefs.shuangpinScheme: Int`**（0 全拼 … 7 加加，取值只增不改），
  读取时**带迁移**：新键不存在而老键为 `true` → 自然码。**真机实测**：设备老配置 `shuangpin=true`
  → 升级后设置页显示「自然码双拼」，用户方案不跳变（老键保留不删，利于回滚）。
- `ShuangpinScheme` 枚举（`PinyinEngine.kt`）：方案身份 + 持久化取值 + 展示名/短名 + `cycle()`；
  设置页下拉与键盘循环**共用同一份列表**，不会脱节。
- 键盘功能面板：按钮改为**方案循环**，主文本＝当前方案名、副文本＝下一个方案（真机实测
  「自然码 / 换小鹤」→「小鹤 / 换搜狗」…）；空格键小字改为「中文+方案名」。
- 设置页新增「输入方案」下拉（8 项），**按要求放在「显示生僻字」上方**（首版放错了位置，已修正并复验）。

## 六、诚实的边界（写清不做的部分）

1. **4 个音节任何双拼都编不出**：`hng` / `hm` / `m` / `n`（鼻音叹词，词库 0 条）与 `junding`
   （`pinyin_syllables.txt` 里的**脏数据**：7 个字母，不是音节——顺带登记，本轮不动词库）。
2. **「两键一音节」模型下同码写法必须让位**（一码只能映射一个音节）：
   `lo`→让给 `luo`（咯/啰 双拼打不出，全拼仍可）；`lve`/`nve`→让给 `lue`/`nue`
   （**但词库里 lve/nve 的词条仍能命中**，因为查询链路有 ue↔ve 变体回退，无功能损失）；
   `ng`→让给 `neng`/`nang`/`niang`（词库 0 条）。覆盖测试对「让位集合」做了显式豁免，其它任何缺口都算失败。
3. **不实现 rime 的多拼写容错**（如 `aai`/`oan` 这类 3 字母派生形式）：键盘模型是两键一音节，
   与其冲突；好处是每个音节固定两次按键。

## 七、验证

**门禁**：`testDebugUnitTest` **200 用例全绿**（新增 `ShuangpinSchemesTest` 16 例）；
`lintDebug` **0 error / 29 warning**（新加的下拉补了 `paddingEnd`，未引入新告警）。

**真机（PACM00，覆盖安装后逐方案实测）**：

| 项 | 证据 |
|---|---|
| 设置页位置 | `spinner_shuangpin` y=121 在 `switch_show_rare_chars` y=440 **上方** ✓（截图复验） |
| 显示生僻字下方 | 「输入方案」卡片与其说明完整渲染，位于生僻字卡片正上方 |
| 配置迁移 | 老键 `shuangpin=true` → 设置页显示「自然码双拼」；改选小鹤后写 `shuangpin_scheme=1`/`=2` 可复现 |
| 键面提示随方案 | 逐方案截图核对：小鹤 `d=ai / k=ing·uai / l=iang·uang`；搜狗/微软 `d=iang·uang / k=ao / l=ai`；紫光 `q=ao / w=en / b=iao / n=ui·ue / m=un`；ABC 红字 `a=zh / e=ch`；加加红字 `i=sh / u=ch / v=zh` ✓ 全部符合各自标准 |
| 分号键 | 仅搜狗/微软/紫光出现，键面显示 `;` + `ing`；切到小鹤/ABC/加加/全拼后自动消失 ✓ |
| 分号键输入 | 紫光方案下 `t` + `;` → 日志 `双拼[t;]→ting → [听, 停, 厅]` ✓ 端到端打通 |
| 小鹤输入 | `x n h e` → 日志 `双拼[xnhe]→xiaohe → [小河, 小盒, 小鹤]` ✓（「小鹤」在第 3 位，前两位是同音更高频词，属正常排序） |
| 自然码回归 | 键面 `o` 键显示 `uo/o`（修掉旧 `ou` 错误），其余键位与旧版一致 ✓ |
| 现场还原 | 实测后已把方案改回用户原值「自然码双拼」（`shuangpin_scheme=1`） |

## 八、新增文件

| 文件 | 说明 |
|---|---|
| `app/.../ShuangpinSchemes.kt` | **生成数据**：7 套方案的声母键 + 码表 + 提示派生（730 行） |
| `app/src/test/.../ShuangpinSchemesTest.kt` | 16 例：各方案键位、分号键、提示、全表往返自洽、覆盖完整性 |
| `tools/dict_builder/gen_shuangpin_tables.py` | 生成器（解析 schema → 施加代数 → 出 Kotlin） |
| `tools/dict_builder/verify_shuangpin_migration.py` | 换表对拍脚本（676 码逐一对拍） |

## 九、发布（**新建 v20260917**，非覆盖）

- 提交 `9e9e8e3` 推送到三处引用（`bbe6cbf` → `9e9e8e3`）：GitHub `main`、
  GitHub `perf/paging-pinyin-rematch`、Gitee `master`。
- APK：`versionCode=20260917`（构建日期已跨天，故是**新 tag**，与 v20260916 并存）、
  `versionName=jinn`、**4,774,669 字节**、`sha256=5a172f74…`、`apksigner` V1=false / V2=true / V3=true、4 字节对齐。
- 两端新建 Release `v20260917`：
  - GitHub：`gh release create`（首次因 `api.github.com` EOF 失败，重试成功；`--latest` 另一次重试成功）
  - Gitee：`POST /releases`（id `1149106`，`target_commitish=master`）→ `POST /releases/{id}/attach_files`（附件 id `3215230`）
- 校验：两支下载 URL 均 HTTP 200、`size_download=4,774,669`、**sha256 与本地 APK 完全一致**。
  至此 `v20260917` 标签在两仓库都存在，应用内「检查更新」不会再误报网络异常。
