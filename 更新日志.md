# 更新日志

> 本文件按次序记录改动（自下而上积累），最新发布见文末「发版：20260920」。
> 早期次序中提到的「剪贴板隐私分类 / 掩码」功能已于 2026-09-16 整体移除，当前版本不含该功能。

> 本次为接手后的第一批修复，严格避开语音模块（MicRecorder / AsrClient / Protocol /
> MicButton 以及 JinnIme 中的录音、WebSocket、识别结果处理链路均未改动）。
>
> 日期：2026-09-15 ｜ 基线提交：`941ca68` ｜ 验证：`testDebugUnitTest` BUILD SUCCESSFUL

## 总览

| 类别 | 数量 |
|---|---|
| 修复 P0（严重）问题 | 3 |
| 修复 P1（中等）问题 | 3 |
| 修复 P2（轻微）问题 | 3 |
| 改动文件 | 9 个源码 + 2 个配置 |
| 新增文档 | `ONBOARDING.md`、`计划任务.md`、`更新日志.md` |

所有改动仅保证编译通过 + 单元测试通过，UI 手感类改动需真机复验（见文末）。

---

## P0-1 修复拼音键盘按键按压反馈失效

问题：`PinyinKeyboardView.bindLetterKeys()` 给每个字母键设置 `OnTouchListener`，
而 `handleKeyTouch()` 所有分支一律 `return true`。Android 事件分发中监听器返回 true 即消费事件，
`PinyinKey.onTouchEvent()` 永不执行 → `pressed` 恒为 false、按压高亮是死代码、
`performClick()` 不触发。用户按字母键看不到任何视觉反馈。

改动：
- `PinyinKey.kt`：新增公开方法 `setPressedVisual(Boolean)`，内部比较后 `invalidate()`。
  命名刻意避开 `View.setPressed`，不复用系统按压态（会被父容器重置）。
- `PinyinKeyboardView.kt`：`handleKeyTouch()` 在 `ACTION_DOWN` / `ACTION_UP` / `ACTION_CANCEL`
  三个分支显式调用 `keyViews[c]?.setPressedVisual(...)` 驱动按压态。

为什么这样修：退格键用的是 `btnBackspace.isPressed = true`（系统按压态 + 选择器），
不受影响；只有字母键走了自定义触摸监听，所以只改字母键路径，不改动事件分发语义，风险最低。

---

## P0-2 补齐剪贴板隐私分类功能

问题：`is_private` 在 `ClipboardDb` 中已完整实现（建表、upsert、去重合并标记、
`deleteAll` 豁免），但 UI 层完全没有入口，分类栏只有 4 个 Tab、长按菜单只有收藏/删除、
没有任何明文掩码。而 `AGENTS.md` 明确承诺「隐私分类」与「隐私内容默认隐藏明文」。

改动：

数据层 `ClipboardDb.kt`
- `whereClause()` 增加 `privateOnly` 参数；`count()` / `recentPage()` 增加同名默认参数
  （默认 false，现有调用点零改动）。
- 新增 `setPrivate(id, Boolean)`：隐私只能由用户主动标记，数据层不提供任何自动判定入口。

独立页 `ClipboardHistoryActivity.kt`
- 新增「隐私」分类 Tab 与 `CATEGORY_PRIVATE = "PRIVATE"` 伪分类常量（与 `FAVORITE` 同构，
  是独立标签列而非 `category` 取值，传 SQL 前会转成 `privateOnly`）。
- 列表渲染：隐私条目默认显示掩码文案，点击一次展开明文（`revealedIds`），再点一次才粘贴。
- 长按菜单新增「标记为隐私 / 取消隐私标记」，走 `BackgroundIo` 后台线程。
- `runChunkedSearch` 的分类判定增加隐私分支。

内嵌面板 `ClipboardPanelView.kt`
- 同样新增「隐私」Tab、掩码与展开逻辑、长按操作条新增「隐私」按钮（`togglePrivate()`）。
- 分页查询同步支持 `privateOnly`。

资源 `strings.xml`：新增 `clipboard_cat_private` / `clipboard_mark_private` /
`clipboard_unmark_private` / `clipboard_private_masked`。

注意：隐私条目在列表里的 meta 行会显示「· 隐私」标记；`deleteAll` 原本就已豁免隐私条目，
现在这条规则真正可生效了。

> 注：隐私分类与掩码功能已于 2026-09-16 整体移除（见下文「剪贴板模块关键约束」）；本条目为当时
> 的临时实现，当前版本不再包含任何隐私功能。

---

## P0-3 修复粘贴状态机卡死与跨输入框误粘贴

问题：
1. `ClipboardHistoryActivity` 的 `pasting` / `pendingPasteItemId` 一旦 IME 不回广播
   （进程被杀、未处于输入态）就永久为 true，之后点任何条目都无响应，粘贴功能彻底卡死。
2. `JinnIme.pendingPasteText` 没有时效，`flushPendingPaste()` 在下一次 `onStartInputView`
   无条件提交，用户切换输入框或搁置很久后，旧文本会被粘到完全无关的位置。

改动：
- `ClipboardHistoryActivity.kt`：新增 `pasteTimeoutRunnable`（3s 超时）与统一的
  `resetPasteState()`；`requestPaste()` 发出广播后挂上超时，结果接收器与失败分支都走统一复位，
  `onDestroy` 一并移除回调。
- `JinnIme.kt`：新增 `pendingPasteAt` 时间戳与常量 `PENDING_PASTE_TTL_MS = 10s`；
  `flushPendingPaste()` 先判时效，过期则丢弃并记日志（连接仍无效时放回暂存但保留原时刻，
  不会因反复重试而无限续期）。

---

## P1-4 去掉每次按键的一倍冗余查询

问题：候选刷新时额外调用了一次 `PinyinEngine.queryWithCompletion()`，
而 `PinyinEngine.query()` 内部已经跑过补全召回，这次重复调用的结果只用于打一行诊断日志，
等于每次按键双倍查询开销。

改动：`PinyinKeyboardView.kt` 删除该重复调用，诊断日志改为复用 `query()` 已有的结果
（`partialSyllable` + 前 3 个候选），并降级为 `Diagnostics.v`（原来是 I 级，会刷屏）。

---

## P1-5 修复 Root 审计命令阻塞与假检查

问题：
- `su()` 只 `readText()` stdout，不排空 stderr。子进程 stderr 管道写满会阻塞，
  `find` 这类可能大量输出的命令会直接挂死；且 `waitFor()` 无超时。
- `checkBackupConfig()` 硬编码返回 ✓，无论 `backup_rules.xml` 被改成什么样都显示「安全」。

改动：`ClipboardFirewall.kt`
- `su()`：后台 daemon 线程排空 stderr；`waitFor(10s)` 超时后销毁进程并返回 null。
- `isRootAvailable()`：同样加超时（用户未在 su 弹窗授权时会一直挂着）。
- `checkBackupConfig(context)`：运行时用 `resources.getXml(R.xml.backup_rules)` 解析实际生效的
  规则文件，确认 `jinn_clipboard.db` 被排除，未排除则报 ✗。
- 顺带删除 `checkExternalLeak(pkg)` 与 `audit()` 中未使用的参数（消除编译警告，不是未跟踪文件）。

---

## P1-6 删除操作改用可写数据库句柄

问题：`ClipboardDb.delete()` / `deleteAll()` 用 `readableDatabase` 执行写操作。
正常情况下 `getReadableDatabase()` 返回的就是可写实例所以没暴露问题，
但磁盘满等异常场景会退回只读句柄，删除会静默失败。

改动：两处均改为 `writableDatabase`。

---

## P2-7 修正与实现完全相反的注释

问题：`showClipboardPanel()` 的 KDoc 与初始化处注释都写「viewLetters 用 INVISIBLE、
零 layoutParams 修改、绝不 relayout」，而实际实现是「viewLetters 置 GONE、
contentArea 改成固定 162dp×2 高度、会修改 layoutParams」。注释与实现完全相反，极易误导。

改动：`PinyinKeyboardView.kt` 两处注释改为描述真实实现，并保留「layoutParams 类型必须匹配
父容器，用错会 ClassCastException」这条真正有价值的经验。

---

## P2-8 忽略疑似云凭证文件

问题：根目录 `ses_fb2d904cbffewGCRN4iuzDTDSg.json`（3.6MB）未被 `.gitignore` 忽略，
有误提交风险。

改动：`.gitignore` 新增 `ses_*.json`、`*service-account*.json`、`*-credentials.json`。
（文件本身未删除，请自行确认后处理。）

---

## P2-9 搜索防抖的滚动残留

改动：`ClipboardHistoryActivity` 新增 `scrollToken`，「滚回顶部」的 `post` 任务带上令牌，
快速切分类或连续输入时旧任务自动失效（对齐内嵌面板 `ClipboardPanelView` 已有的做法）。

---

## 构建环境改动（非功能）

| 文件 | 改动 | 原因 |
|---|---|---|
| `settings.gradle.kts` | 启用注释里预留的阿里云镜像（pluginManagement + dependencyResolutionManagement） | 本机直连 `dl.google.com` / `repo.maven.apache.org` 解析失败（guava、asm-util 等依赖找不到），镜像可正常拉取 |
| Gradle 8.9 | 手动安装到 `~/.gradle/wrapper/dists/` | Wrapper 下载 gradle-8.9 时 SSL 握手失败，改用腾讯云镜像下载后手动放置 |

> 镜像对国内网络是加速，且 Google/MavenCentral 仍在列表中作为兜底；
> 若你的网络可直连，把镜像行注释掉即可，不影响构建结果。

---

## 新增文档

| 文件 | 内容 |
|---|---|
| `ONBOARDING.md` | 接手指南：架构、目录、技术栈、IME 生命周期、核心逻辑、已知问题、风险、维护约束 |
| `计划任务.md` | 按严重度排序的任务清单（已修/待办/不修及原因） |
| `更新日志.md` | 本文件 |

---

## 明确未修（受「禁止修改语音」约束）

| 问题 | 原因 |
|---|---|
| `refreshConfig()` / `ACTION_CONFIG_UPDATED` 是死代码 | 唯一调用目标是 `asr.connect()` 重连，属语音链路。已知影响：设置页改服务端地址后 IME 不热更新，靠 `killProcess` 重启生效 |
| `AsrClient` / `MicRecorder` 零测试覆盖 | 语音模块，解禁后建议优先补 |
| VAD 静音阈值 120s 偏长 | 语音模块 |

---

## 验证与真机复验清单

```bash
.\gradlew.bat testDebugUnitTest   # BUILD SUCCESSFUL，7 个测试类 ~100 用例全过
```

以下改动必须真机复验（编译通过 ≠ 体验正确）：

1. 按键按压反馈：按任意字母键应出现高亮，松手恢复；符号层横滑翻页仍正常。
2. 隐私功能：长按条目 → 标记隐私 → 切到「隐私」Tab 能看到该条 → 列表显示掩码 →
   点一次展开明文 → 再点一次才粘贴；「删除全部」不应删除隐私条目。
3. 粘贴超时：正常粘贴仍应成功并关闭页面；断网/杀 IME 进程后 3s 内应恢复可点击状态。
4. 剪贴板面板：顶栏新增「隐私」按钮后共 8 个格子，需确认在窄屏上文字不被挤掉
   （若过挤，可考虑把「返回」改图标或缩短标签）。

---

# 补充：键命中 / 搜索下推 / 掩码重置 / 内存采样

- **字母键抬起无命中判定**：从 Q 滑到 W 抬起仍回调 Q 的监听器 → `ACTION_UP` 增加 `isInsideKey()`
  （raw 坐标比对、边界外扩 8dp；取不到布局信息返回 true 保留旧行为）
- **剪贴板搜索减少解密**：分类 / 收藏 / 隐私下推到 SQL（`db.recentPage(..., scope, ...)`）先过滤再解密；
  序号基准改为分类内总数 `db.count(scope, ...)`，与搜索态 `categoryTotal` 语义一致
- **隐私掩码未重置**：`onPanelShown()` 清空 `revealedIds`，重开面板须再点一次
- **词库内存与耗时采样**：`PinyinEngine.load()` 前后各采样一次进程内存并记录耗时写入日志

**不修（已登记）**：粘贴广播携带明文（#10，现有 `setPackage` + `RECEIVER_NOT_EXPORTED` 已阻断跨应用接收，
改 itemId 异步查库会动只有真机可验的核心链路）；候选栏重建 TextView（#14，复用省不掉 measure / layout，
应整体换 RecyclerView 而非打补丁）。

验证：单测通过。真机复验：Q 按下滑到 W 抬起应上屏 W（不应上屏 Q）；切分类后搜索序号从该分类总数往下数。

---

# 改动自查：掩码文案与粘贴复位

- 掩码文案「内容已隐藏，点击显示后再点一次粘贴」过长，单行末尾省略号会截断 → 缩短「已隐藏 · 点击显示」（资源与面板硬编码同步）
- `pasteTimeoutRunnable` 手写复位绕过 `resetPasteState()` → 改为先取 id 打日志再走统一复位口

**复查无问题（记录免重复怀疑）**：属性初始化顺序（lambda 3 秒后执行，无 NPE）；SAM lambda 的 `this` 指向宿主；
`whereClause` 参数兼容（既有调用点行为不变）；`isInsideKey` 保守分支；`handleItemClick` 隐私分支在 `if (pasting) return` 之后。

验证：单测通过；已修 14 项。

---

# 推进 #9：抽取 ClipboardFilter

- 分类栏 5 个 Tab 里收藏 / 隐私是独立标签列，当 category 传给 SQL 恒不成立（列表恒空）。新增
  `ClipboardFilter(category, favoritesOnly, privateOnly)` + `of(raw)`，5 处重复翻译统一改为调用它；
  UI 的 `CATEGORY_FAVORITE` / `CATEGORY_PRIVATE` 改为引用 `PSEUDO_*`
- 新增 `ClipboardFilterTest`（7 例）：钉死「收藏 / 隐私的 category 必须为 null」
- 渲染层重复仍在（需真机回归），本轮只做纯函数部分

验证：单测 8 类 / 161 例全绿。

---

# 搜索面板隐私泄露（P0）

- `SearchPanelView.getView()` 直接显示明文且点一次就粘贴 → 补齐 `revealedIds`、掩码「已隐藏 · 点击显示」、
  meta 补「· 隐私」、首次点击只展开二次才粘贴，`onShown` / `onHidden` 清空
- 删掉空态文案「找到 N 条」的无效赋值（同函数下一行即设 GONE）

**复查未改**：搜索全库扫描是设计如此；进搜索不跟随当前分类（需产品确认）。

验证：单测通过。
# 设置页：保存遗漏 / 开关递归 / 回调越界 / Spinner 改写配置

- **剪贴板上限不生效**（P1）：`saveAndRestart()` 未保存上限，而它依赖的失焦回调时序不保证早于保存
  → 显式调用 `saveMaxItems()`（幂等）
- **Root 开关递归**（P2）：程序化拨回开关再次触发监听器，把「不可用」提示覆盖成功能描述
  → 新增 `suppressRootSwitchCallback` 抑制回调
- **导出回调未判销毁**（P2）：后台打包后直接操作 view → 加 `isFinishing || isDestroyed` 返回
- **Spinner 改写默认键盘模式**（P2）：`onItemSelected` 早于 `loadPrefs()` 注册，`setSelection` 触发它；
  值不在数组时 `indexOf` 返回 -1 → 退回第 0 项（语音）→ 新增 `defaultModeSpinnerReady`，加载完成才置位

验证：单测通过。真机：改上限后保存重启应保持；未 Root 开启应提示「不可用」；反复切默认模式重启后保持。
累计已修 21 项，8 类 / 161 例。

---

# 拼音键盘：退格双击态 / 面板回滚 / 搜索残留拼音

- **退格双击态永不超时**（P0）：双击后 `backspaceTapCount=2` 长期挂着，几分钟后长按仍触发 `onDeleteAll()`
  → 双击成立时记录第二击时刻，DOWN 分支加 `clearGestureWindowMs`（500ms）校验，超窗即失效
- **剪贴板面板异常后 26 键永久消失**（P1）：`onPanelShown()` 抛异常时 catch 只置 `clipboardActive=false` 不恢复视图，
  而 `hideClipboardPanel()` 开头即 return → 抽出 `restoreLettersLayout()`，catch 完整回滚并通知 IME
- **切搜索模式不清拼音缓冲**（P1）：残留拼音不可见但生效（退格先删它、空格把候选塞进搜索框）
  → 新增 `clearComposingState()`，进出都调

**未修（待真机核实）**：`onPredictionSelected` 漏搜索分支；切层 / 切大写不清拼音串；方向面板与剪贴板互斥只做一半；
拖选态行首 / 行末跳全文首尾；`capsMode` 生命周期不复位（「清空后连删不停」已顺带修：`removeCallbacks`）。

验证：单测通过。真机：点两下退格后等几秒长按应正常连删；0.5s 内长按应触发清空。累计已修 24 项，8 类 / 161 例。

---

# 待核实清单：修 4 项、1 项维持现状

- **搜索态点预测词串进宿主输入框**（P1）：`onPredictionSelected` 缺 `isPanelSearch()` 分支 → 补上
- **大写锁定切换不清拼音串**（P2）：切换时清 composing 与预测（同切中英文）
- **方向面板与剪贴板互斥只做一半**（P2）：`showDirectionPanel()` 开头先关剪贴板面板
- **拖选态行首 / 行末跳全文首尾**（P2）：`nextFocus` 改为按当前行计算（与光标态统一）+ 补多行用例

**维持现状**：`capsMode` 不在 `configure()` 复位（大写锁定是用户显式持久态，重置会让「锁定」失去意义）。

验证：单测 8 类 / 162 例全绿。累计已修 28 项。

---
# 性能瓶颈与边界排查

- **V 级日志全量落盘**：`Diagnostics.log()` 每级都写文件（无持久流，每条 open → write → close），
  V 级记录音频包收发（每秒可达 10 条）→ 新增 `VERBOSE_TO_FILE`（默认 false），V 级只走 logcat
- **音节前缀判断 O(n) → O(1)**：`isTruePrefixOfSyllable()` 每次遍历 400+ 音节做 `startsWith`，
  且在每次按键路径上多次调用 → 加载时预建 `syllablePrefixes` 改哈希查找
  （`PinyinEngineTest` / `PinyinCompletionTest` 通过确认语义不变）
- **保活开关缓存为 false 后永不刷新**：`JinnAccessibilityService` 的 `onAccessibilityEvent` 开头
  `if (!keepAliveCached) return` 把重读挡在返回后 → 廉价过滤提到最前，节流窗口外无条件重读，移除缓存字段
- **白名单命令首条失败则后续全不执行**：`commands.all { runSu(it) }` 遇 false 短路，而 4 条命令彼此独立
  → 改为逐条执行再汇总，日志输出「成功 N/4 条」
- **文档订正**（`AGENTS.md`）：测试命令改为 `.\gradlew.bat testDebugUnitTest` + 镜像回退；补 3 个模块；
  「保存并测试连接」改为「保存后杀进程重启 IME」；写明语音模块禁改；补伪分类陷阱与 `ClipboardFilterTest` 守卫

验证：单测通过。累计已修 33 项，8 类 / 162 例。
性能量化（待真机）：对比日志体积确认写入下降；词库加载耗时日志确认未变慢。

---
# 真机实测：构建 / 签名 / 部署 / 验证跑通

真机可用（PACM00），首次完成完整闭环。

- **构建与签名**：密钥库 `com.jinn.inputmethod/release.jks`，V1=false / V2=V3=true；产物 `jinn-release.apk`
  10.6MB；`adb install -r` Success（`versionCode=20260915`）。密钥根由 `JINN_KEYSTORE_ROOT` 环境变量指定
- **词库内存结案**（#7）：加载前 9MB → 后 165MB（堆上限 384MB），净占 156MB / 41%；加载 8424ms；
  词语键 1,048,342、反向索引 1,162,486、音节 422。
  预分配无空间（`HashMap(1_500_000)` 经 `tableSizeFor` 得 2^21，不触发扩容）；唯一方向是省
  `wordToPinyin`（约 45MB），但它是 `consumption()` 残码处理与 `predict()` 必需，改动覆盖候选生成 /
  消费 / 预测 / 测试全链路 → **判定不做**（收益 29% 内存，当前 165 / 384MB 无 OOM 压力；将来堆上限
  192 / 256MB 的设备再按「候选携带拼音」重构）
- **退格 P0 双向验证**：`adb shell input` 单次 200~400ms 无法凑进 280ms 双击窗口 → 临时放宽到 3000ms，验证后还原。
  A：双击 → 等 3s → 长按 1.2s 应不清空 ✅；B：双击 → 立刻长按应清空 ✅（旧代码下 A 必清空，用例能有效区分）
- **保活前台提升失败不再崩溃**：`startForeground` 无异常保护，部分 ROM 抛 `SecurityException` /
  `ForegroundServiceStartNotAllowedException` → 抽出 `promoteToForeground()`，失败记日志并 `stopSelf()`，
  `onStartCommand` 返回 `START_NOT_STICKY`
- 词库诊断日志补反向索引规模

验证全通过：release 签名、覆盖安装保留数据、IME 重启加载 8424ms（422 音节 / 104.8 万键）、键盘 UI 截图完整、
退格双向、内存采样 165 / 384MB。累计已修 36 项，8 类 / 162 例。

---

# 修复「企鹅」打不出来（连写歧义类漏词）

用户报告：双拼 `qiee` 打不出「企鹅」（qi + ee）。

- **定位**：词库里「企鹅」0 条，而含「企鹅」的 23 个词拼音都标 `qie`，`qie` 键只有 14 个单字；
  双拼转换正确（`ZERO_TABLE` 有 `ee → e`，`toQuanpin("qiee")` = `qie`）→ 问题在词库。
  根因是一整类：拼音 `S1 + Z`（Z 为零声母音节 a/ai/an/ang/ao/e/ei/en/eng/er/o/ou）连写后恰等于另一合法
  单音节 → 同键（企鹅 qi+e→qie 撞「切」、西安 xi+an→xian 撞「先」、饥饿 ji+e→jie 撞「姐」、
  提案 ti+an→tian 撞「天」、彼岸 bi+an→bian 撞「边」），实测这批词全不在词库
- **检测工具**：新增 `detect_ambiguous_keys.py` 枚举歧义键 → 歧义键 144 个，其中可疑（只有单字）112 个，
  作后续人工核对面
- **修复**：新增 `add_words.py` + 补词表 `fix_ambiguous_words.txt`，补入 11 个缺失词并插入第 4 位
  （前三位留给最高频单字，不抢首候选也不用翻页）：企鹅 / 西安 / 饥饿 / 提案 / 奇案 / 彼岸 / 立案 /
  堤岸 / 皮袄 / 集安 / 西澳。修复后 `qie` 键 = `且|切|窃|企鹅|怯|…`，键数 1,048,342 不变
- **顺带修 `dict_builder.py`**：`write_outputs` 对带空格拼音各写一行，「qi e」与「qie」compact 相同 →
  重复键；app 侧 `phrasesByPinyin[key] = ...` 后写覆盖前写，词条静默消失 → 改为先按 compact 聚合再输出
- **防回归**：新增 `PhraseDictIntegrityTest`，流式扫描 assets 词库断言这些词存在（读到所需键即停，不加载整份）

验证：单测 9 类 / 163 例全绿；真机重启 IME 后点按输入 `q i e` 与 `q i e e`，候选第 4 位均出现「企鹅」。
累计已修 39 项。

**未修（需确认）**：疑似编码损坏词条「涓嶅」出现在 603 个词的拼音键下（UTF-8 字节被按 GBK 解码的产物，
原文可能是「不」），属词库源数据问题，已登记；若确认是坏数据，建议在生成流程加编码校验。

---

# 新增「显示生僻字」开关（默认关闭）

开关只在设置页（键盘无入口），默认关。

- **判定标准**：依《通用规范汉字表》(2013)：一级 3500 + 二级 3000 = 6500 字为常用（保留）；三级 1605 与表外字为
  生僻（默认不加载）。字表写入 asset `common_chars.txt`（6500 字 / 19.8KB）
- **过滤粒度**：整词丢弃（词中任一字生僻即丢）——用不到生僻字也就用不到含生僻字的词
- **加载时过滤**（非加载后）：解析词库即跳过，被跳过的词条从未进 HashMap，才能真正降内存
- **数据结构**：`BooleanArray(0x9FFF + 1)` ≈ 64KB 按码点索引 O(1)；不用 `HashSet<Char>`（需判 123 万词条 /
  450 万字符）。规则：`< 0x4E00` 一律放行；`0x4E00~0x9FFF` 查位图；其余（含扩展区代理对）视为生僻
- **开关位置**：设置页「自动唤起键盘」之后；切换即写配置并 Toast「重启输入法后生效」

**实测收益**：单字表 17,863 → 7,038（−60.6%）；词组键 1,048,342 → 892,539（−15.1%）；词条 1,230,120 →
1,017,136（−17.3%）；字符总量 −15.9%。真机：词语键 892,714、反向索引 953,915、音节 422 → 414、过滤 223,358 条；
`dumpsys meminfo` Java Heap Alloc 131MB 与降幅吻合（`Runtime.totalMemory() - freeMemory()` 含未 GC 垃圾，
加载刚结束读会偏高）。

**测试**：新增 `RareCharsFilterTest`（3 例：「饕餮」丢弃而同键「涛涛」保留、生僻单字「饕」过滤）；加
`PinyinEngine.resetForTest()` 避免单例污染。单测 10 类 / 166 例全绿。

---

# APK 体积瘦身（10.6MB → 8.4MB，−20.8%）

- **诊断**：APK 10.6MB 中 `pinyin_phrases.txt` 压后 10.7MB（占 95%），其余全部 < 0.6MB → 体积即词库体积
- **排除的手段**（实测）：裁生僻字（字节仅 −7.9%，拼音键占词库 44% 且不含汉字，过滤不掉）；
  只留一级 3500 字（−10.8%）；限词长 ≤3（−37.2%，但会砍四字常用词）；提高 deflate 等级（持平）；
  R8 / shrinkResources（早已启用）
- **采用**：词库改 xz + 流式解压。原始 28.01MB → deflate 10.22MB → xz preset=6 8.00MB（再省 21.7%）。
  `pinyin_phrases.txt.xz` + 依赖 `org.tukaani:xz:1.9`（dex 后约 80KB）+ `XZInputStream` 流式解压
  （不落地磁盘、不构造超大 String）+ `noCompress += "xz"` + `PhraseDictIntegrityTest` 改为解压校验
- **代价**：加载 8424ms → 10640ms（+2.2s，xz 纯 Java 比原生 zlib 慢）。加载在 `JinnIme.onCreate` 独立线程，
  键盘视图创建在其之后 → 不阻塞 UI，代价仅是「弹出后约 2.2s 内候选未就绪」；真机候选验证正常。
  回退：删 `xz` 依赖与 `noCompress`，源文本放回 assets，`loadPhrases()` 换回 `assets.open`

累计已修 43 项；单测 10 类 / 166 例；APK 10.6 → 8.4MB（−20.8%），签名方案不变。

---

# 词库分层：APK 只内置基础包，长词改为可选下载

需求：少用 / 专业词库做成可下载扩展项，不进 APK。

- **切分依据**：只能按词长（格式无词频字段）。实测 1–2 字 xz 0.64M / 1–3 字 3.16M / 1–4 字 5.74M
  → 选 **1–3 字**：三字词覆盖日常（天安门 / 计算机 / 办公室），四字及以上以成语与长尾专有名词为主。
  代价：装扩展包前四字成语打不出来
- **切分**：`split_dict.py` 输出两个同格式包（运行时同一解析器合并）
  - 基础包（≤3 字）：462,189 键 / 628,800 词，9.92MB → xz 3.16MB ← 进 APK
  - 扩展包（>3 字）：586,190 键 / 601,320 词，17.09MB → xz 4.73MB ← 按需下载
  - 合计 1,230,120 词条，与源零丢失
- **引擎**：新增 `loadExtensionDict()`（读 `filesDir/dict_ext.xz`，不存在静默跳过）；
  `loadPhrasesReader(reader, merge)` 新增合并模式 —— **关键正确性点**：两包存在相同拼音键
  （如 `qie`），直接赋值会把基础候选整体挤掉，必须 `existing + kept` 合并且基础在前；
  预分配容量按基础规模下调（60 万 / 70 万）
- **设置页**：新增「扩展词库（长词包）」卡片（状态 / 地址输入 / 下载安装 / 删除）。下载先写 `.tmp`
  再改名（避免半截文件被引擎当有效词库加载）；回调切主线程并判 `isFinishing / isDestroyed`

结果：APK 10.6MB → **3.6MB（−66%）**；扩展包 `release/dict_ext.txt.xz` 4.73MB，直链填入设置页即可安装。

验证：编译 + 单测 10 类 / 166 例通过（含完整性测试，基础包仍有「企鹅」「西安」）；APK 实测 3.6MB；
签名 V1=false / V2=V3=true；真机安装 Success；设置页 UI 截图确认。
未验证：基础包真机加载数据（adb 未聚焦输入框，未采到日志；逻辑与上一版一致、仅词条减少，风险低）；
扩展包下载与同键合并（需可访问的下载地址）。

累计：已修 47 项。

---

# 词库分发上线 + 源码同步双仓库

- **分发位置**：托管为 Releases 附件（非仓库文件）
  - Gitee（主源）：`.../releases/download/dict-ext-20260915/dict_ext.txt.xz`
  - GitHub（备用）：`.../releases/download/dict-ext-20260915/dict_ext.txt.xz`
  - 两源实测：302 重定向后 200，`Content-Length = 4956768`；OkHttp 默认跟随重定向
  - **坑**：最初把扩展包提交进仓库用 raw 直链 → Gitee 对大文件 raw 返回 404（文件确实存在、API 可查），
    小文件（README.md）raw 正常 → **大文件必须走 Releases 附件**
- **下载体验**：设置页预填主源直链；多源回退（用户填写 → Gitee → GitHub）；下载成功后自动重启 IME
  （词库只在启动时加载，省掉用户再点重启）；先写 `.tmp` 再改名
- **源码同步**：GitHub `perf/paging-pinyin-rematch` 与 Gitee `master` 均推到 `12dc44b`
  （`6226554` 首批修复 + 优化 + 新功能 39 文件；`12dc44b` 下载源修正）。`.gitignore` 新增：
  本地工作区目录、`docs/*.zip`（内容已提取为 `assets/common_chars.txt`）、
  `tools/dict_builder/source/`（28MB 未压缩源，可由基础包 + 扩展包还原）
  - 推送 GitHub 遇两次 `TLS connect error`（国内网络波动），第三次成功 —— 重试即可，非配置问题

累计：已修 48 项；APK 3.6MB（含基础词库），扩展包 4.7MB 按需下载；单测 10 类 / 166 例。

---

# 基础包纳入四字成语 + 界面调整 + 同步 main

- **切分点 3 字 → 4 字**：四字成语属高频用词，必须随 APK 内置
  - 基础包：462,189 键 / 628,800 词 → **826,897 键 / 1,006,326 词**（3.16MB → 5.74MB）
  - 扩展包：586,190 键 / 601,320 词 → **221,510 键 / 223,794 词**（4.73MB → 2.19MB）
  - APK 3.6 → 6.2MB（仍比最初 10.6MB 小 42%）；扩展包反而更小、下载更快；词条合计 1,230,120 零丢失
- **扩展词库 v2 直链**（已重传实测，两源 HTTP 200 / `Content-Length = 2297184`，设置页预填 Gitee 主源）：
  `.../releases/download/dict-ext-20260915-v2/dict_ext.txt.xz`（Gitee 主源、GitHub 备用）
- **界面**：「点这里唤起键盘打字」移到「保存配置并立即重启生效」下方（保存完即可验证）；扩展词库两个按钮
  改用 `@style/SettingsButton`（与电池白名单等一致，此前是裸 Button、与深色主题不搭）；地址输入框补
  `field_aurora` 背景与内边距；状态文案改为「未安装，五字及以上长词暂时打不出来（四字成语已内置）」
- **同步 main**：main（`ab7e6e8`）是本地 HEAD 祖先 → 快进推送，无需强推：
  GitHub `main` `ab7e6e8..c8b93e9`、GitHub `perf/paging-pinyin-rematch` `ae5f376..c8b93e9`、
  Gitee `master` `ae5f376..c8b93e9` —— 三个引用均指向 `c8b93e9`

累计：已修 52 项；APK 6.2MB（含四字成语，原 10.6MB，−42%）；扩展包 2.19MB；单测 10 类 / 166 例。

---

# 移除全部保活机制

承接上一轮审查结论：保活三件套属过度设计。IME 是系统绑定服务、用户一输入即被拉起，真正需要常驻的
只有那条 WebSocket 长连接；换来的"进程常驻"对一个自用输入法不值得，代价是 5 项权限、一个无障碍服务、
一个前台服务与一个 ROOT 依赖。

- **删除**：源文件 `KeepAliveService.kt` / `JinnAccessibilityService.kt` / `RootShizuku.kt` /
  `BootReceiver.kt`（4 个）+ `res/xml/accessibility.xml`；权限 `FOREGROUND_SERVICE`、
  `FOREGROUND_SERVICE_SPECIAL_USE`、`POST_NOTIFICATIONS`、`RECEIVE_BOOT_COMPLETED`、
  `REQUEST_IGNORE_BATTERY_OPTIMIZATIONS`（5 项）；依赖 `dev.rikka.shizuku:api`；设置页整张「后台保活」
  卡片（3 开关 + 3 按钮 + 状态文本）、`notifyPermissionLauncher`、`toggleKeepAlive` /
  `ensureNotifyPermission` / `runRootKeepAlive`；配置 `Prefs.keepAlive` / `useRootShizuku` /
  `notifyHighPriority` 及 KEY 常量
- **刻意保留**：剪贴板卡片的「Root 增强模式」——它做的是剪贴板数据目录安全审计（防第三方读取），
  与"保活防杀"是两件事，只是恰好都用 root，不能顺手删
- **按需连接**：`asr.connect()` 时机未改（属语音模块，按禁改约束不动）。保活移除后实际已接近按需：
  IME 不再被强制常驻，用户输入时系统拉起即建连、后台闲置随进程回收。真正做到「按下录音键才连接」
  需改 `AsrClient` 的连接时机，要先解除语音约束

验证（真机）：编译 + 单测通过，引用清理干净；APK 6.2MB，签名 V1=false / V2=V3=true；安装 Success；
设置页截图确认保活卡片消失；语音链路日志 `renderLink: CONNECTING → ONLINE` 正常。

**工程教训（本次踩两次）**：改 `strings.xml` / `findViewById` 序列这类每行结构高度相似的文件时，
`old_string` 必须只包住目标行，别带相邻行当锚点 —— 因此两次误伤（删掉相邻的
`ext_dict_status_installed`、往 `findViewById` 序列插入重复赋值），均靠随后的 `grep -c` 发现并修。
结论：这类文件改完必须 grep 验证关键 key 的存在性与唯一性。

累计：已修 56 项；已移除 4 源文件 / 5 权限 / 1 依赖 / 整张保活卡片。

---

# 方案按钮提示目标方案 + 符号菜单新增「编程」分组

- **方案按钮小字**：原来固定「换拼音」，看不出点下去会变成什么 → 按当前方案提示目标
  （双拼时「换全拼」、全拼时「换双拼」）。真机截图确认功能栏首键显示「全拼 / 换双拼」
- **符号菜单新增「编程」分组**（第 11 组，3 页）：键位映射值本就是 `String`（不限于单字符），
  关键字可整词上键
  - 1：英文符号 `( ) { } [ ] < > = !` / `; : ' " \` ~ & | ^ _` / `+ - * / %`
  - 2：常用关键字 `return print main if else for while class def func` / `import from const let var new public private void` / `true false null None this static async`
  - 3：类型与语句 `int float double string bool char long struct enum interface` / `try catch finally throw break continue switch case default` / `print() main() println printf scanf lambda yield`
- **键面长文本自适应（关键改动）**：原 `onDraw` 字号固定 `TEXT_RATIO = 0.4`，`return`（6 字符）、
  `private`（7 字符）会左右溢出 → label >2 字符即按长文本处理，`fitTextSize()` 按可用宽度等比收缩
  （下限 `MIN_LONG_TEXT_SIZE = 9sp`）并垂直居中（不再用小字顶置）
  - 真机：`if`（2 字符）走原样式字号较大；`return` / `import` / `private` 自动收缩且完整显示，
    同页不同长度关键字字号各异

累计：已修 59 项；符号分组 10 → 11 组；APK 6.2MB；单测 10 类 / 166 例。

---

# 符号键文案居中 + 编程分组扩至 7 页

- **符号键文案改「返回」**（新增 `key_back`）：进入符号层后左下角原显示「ABC」，该键此刻的作用就是
  回字母页 → 文案更直白；数字层的「ABC」保持不变
- **符号层键面一律水平 + 垂直居中**：新增 `PinyinKey.centeredStyle`
  （`centeredStyle = layer == LAYER_SYMBOL`），不再沿用字母层小字顶置 → 修掉短符号偏上
  （`if` 是 2 字符，原走顶置分支、视觉上不在键中央）。居中分支仍调 `fitTextSize()`，长关键字照旧收缩
- **分组顺序**：常用 → **编程** → 标点 → 序号 → 数学 → 单位 → 平假名 → 片假名 → 拉丁 → 特殊 → 注音
  （编程组从末位提到第 2 位，日常高频之后即可够到）
- **编程分组 3 页 → 7 页**：1 英文符号；2 通用关键字；3 类型与语句；4 Python（`self None elif lambda
  yield global assert raise with as pass del not and or is True False __init__ __name__` 与 `print( input(
  range( len( str( dict(`）；5 Java / C#（`System String Integer Boolean List Map HashMap ArrayList package
  extends implements abstract final override namespace using foreach params synchronized new get; set; await
  Task var public static`）；6 JS / TypeScript（`function => console.log document window export default
  interface type enum undefined NaN typeof instanceof Promise async await require module forEach map( filter(
  reduce( JSON. Object. Array.`）；7 C / C++（`#include #define stdio.h stdlib.h iostream cout cin endl std::
  vector template typename nullptr sizeof malloc free( printf( scanf( struct -> :: int main return 0 unsigned
  const static`）
  - 所有页面受字号自适应保护，最长的 `public static`（13 字符）也能完整显示

真机：符号键显示「返回」✅；分组顺序「常用 / 编程 / 标点…」✅；编程组标签 1/7 ✅；键面居中 ✅。

累计：已修 62 项；符号分组 11 组（编程组 7 页）；APK 6.2MB；单测 10 类 / 166 例。

---

# 符号分组重构 + 剪贴板搜索页退出按钮两行显示

- **剪贴板搜索页「退出搜索」改两行**：用户要求严格两行（`退出` / `搜索`），原单行在窄按钮里挤压 →
  `text = "退出\n搜索"`、字号 13f → 12f、`setLineSpacing(0f, 0.95f)`（避免 46dp 高度里两行贴边）
- **平假名 + 片假名合并为「日本」组**：同一语言两套字母合并，共 8 页（前 4 页平假名、后 4 页片假名），
  翻页在同一组内滑动
- **「特殊」组前移并扩到 6 页**：位置从末位移到「序号」之前；1–3 原内容（牌面 / 星形 / 音符 / 表情、
  天气 / 星座 / 杂项、常用 emoji）；4 箭头 `→ ← ↑ ↓ ⇒ ⇐ ⇑ ⇓ ↔ ↕ ➤ ➔ ↗ ↘ ↙ ↖ ⤴ ⤵ ↩ ↪ ⇄ ⇅ ⇆ ⇧ ⇩ ⏎`；
  5 货币与金融 `¥ $ € £ ₩ ₽ ¢ ₪ ₹ ₫ ₴ ₦ ₱ ﷼ ₨ ₡ ₭ ₮ ₲ ₵ ₸ ₺ ₼ ₾ ¤ ₿`；
  6 数理与几何 `≠ ≈ ≤ ≥ ± × ÷ ∞ ∵ ∴ ∠ ⊥ ∥ △ ▲ ▽ ▼ ◇ ◆ □ ■ ○ ● ◎ ⊙ ⌒`
- **最终顺序（10 组）**：常用 → 编程 → 标点 → 特殊 → 序号 → 数学 → 单位 → 日本 → 拉丁 → 注音
  （原 11 组，因平假名 + 片假名合并减 1）

真机：分组顺序「常用 / 编程 / 标点 / 特殊 / 序号 / 数学 / 单位 / 日…」✅；特殊组 1/6 ✅；
编程组 3/7 且键面全部居中 ✅；符号键「返回」✅。
未取到截图：退出搜索两行排版 —— adb 操作时候选栏状态残留（`input keyevent` 清空输入框后 IME 拼音串
未同步清除，功能面板不显示），非真实用户场景；改动本身简单明确，已编译验证。

累计：已修 66 项；符号分组 10 组（编程 7 页、特殊 6 页、日本 8 页）；APK 6.2MB；单测 10 类 / 166 例。

---

# 中英键两行富文本、序号组扩充、日本组补齐空位

- **中英切换键改上下两行富文本**：原单个「中」/「EN」随模式切换 → 同一控件显示两行「中文 / 英文」，
  当前生效那行染主题紫 `aurora_purple` 并加粗
  - 必须用 `SpannableString` 做部分着色。试过「中文/英文」单行斜杠写法，按钮仅约 132px 宽，13sp 会
    折成「中文/英」+「文」；改上下两行后字号放回 12sp、行距 0.9，在 144px 键高里排布均匀
  - 不能拆成两个 TextView：两段各自居中，视觉上像并排两个按钮而非一个切换键
- **序号组 4 页 → 7 页**：5 圈字母收尾 `Ⓥ Ⓦ Ⓧ Ⓨ Ⓩ` + 带括号数字 `⑴`–`⒇`；6 带括号小写字母 `⒜`–`⒵`；
  7 大数圈号 `㉑`–`㉟` + 中文大写数字 `壹 贰 叁 肆 伍 陆 柒 捌 玖 拾`
  - 顺带修数据错误：原第 2 页 `'z' to "Ⅷ"` 与 `'n' to "Ⅷ"` 重复、`'v' to "Ⅻ"` 与 `'m' to "Ⅻ"` 重复
    → 改为 `ⅰ ⅱ` 补位
- **日本组补齐末页空位**：合并平假名 / 片假名时两组各自第 4 页只有 10 键（q–p）、余 16 键空着 → 补为
  完整 26 键：拗音 `きゃ きゅ きょ しゃ しゅ しょ ちゃ ちゅ ちょ にゃ にゅ にょ ひゃ ひゅ ひょ みゃ
  みゅ みょ りゃ りゅ りょ ぎゃ ぎゅ ぎょ じゃ じゅ じょ びゃ びゅ びょ ぴゃ ぴゅ ぴょ`（片假名同构）+
  长音符与浊点 `ー ゛ ゜ ・ 〜 ゔ ゠ ゟ`（片假名用 `ヷ ヸ ヹ ヺ`）→ 日本组 8 页增至 10 页

真机：中英键两行「中文 / 英文」且「中文」紫色加粗 ✅；序号组 1/7 ✅；特殊组 1/6 ✅；
分组顺序「常用 / 编程 / 标点 / 特殊 / 序号 / 数学 / 单位 / 日…」✅。

累计：已修 70 项；符号分组 10 组（编程 7 页、特殊 6 页、序号 7 页、日本 10 页）；APK 6.2MB；
单测 10 类 / 166 例。

---

# build_apk.py 健壮性审查与修复

审查构建脚本，修 9 处，其中 1 处实质性缺陷。

- **缺 zipalign（实质性）**：脚本用 Python `zipfile` 重打包去 `META-INF`（`remove_meta_inf`）会打乱 AGP
  产物的 4 字节对齐，而 apksigner 不负责对齐 → 之前产物的对齐状态只是巧合。未对齐会让设备读取 APK 内
  资源时先解压，影响启动速度与内存占用（尤其 `resources.arsc` 与大量 `res/*.xml`）。流程固定为
  `AGP 产物 → remove_meta_inf → zipalign -p -f 4 → apksigner sign`，并补事后复核
  （`apksigner verify` 查签名方案 + `zipalign -c 4` 查对齐）
- **其余 8 处**：① `.idsig`（v4 签名产物）未清理 → 加入清理列表；② docstring 的 `<密钥目录>` 是无效
  转义，Python 3.12+ 报 `SyntaxWarning` → 改 raw 字符串；③ `find_build_tool("apksigner.bat")` 硬编码
  Windows 扩展名 → 按 `os.name` 选后缀；④ 签名方案校验用精确英文串匹配 → 改正则
  `v2\s+scheme[^:]*:\s*true`（避免 build-tools 升级后措辞变化误报）；⑤ 临时文件只在成功路径清理 →
  改 `try/finally`；⑥ `remove_meta_inf` 用 `src.read()` 整块读入内存（词库包数 MB）→ 改
  `shutil.copyfileobj` 流式（256KB 块）并保留原 `compress_type`；⑦ `verify` 用 `shell=True` 拼路径 →
  改列表传参；⑧ 密钥找不到只报路径 → 补 `set JINN_KEYSTORE_ROOT=...` 示例

验证：`python -W error::SyntaxWarning -m py_compile` 无警告 ✅；`--help` 正常 ✅；端到端实跑 3 次全通过 ✅；
签名 `V1=false V2=true V3=true` ✅；`zipalign -c 4` 通过（脚本内 + 外部复核各一次）✅；
产物目录只剩 `app-release.apk` 与 AGP 原生产物 ✅。

累计：已修 79 项；APK 6.2MB（对齐状态有保证，不再依赖巧合）。

---

# 回车键输出「原始按键」英文（双拼不做转换）

- **纠正前提**：需求说「全拼输入 but 后回车输出英文 but」，实测并非如此 —— 双拼模式输入 `but` 后回车，
  候选栏显示「不他 / 不太 / 步态」，回车后拼音串 `but` 原样留着，既不提交候选也不输出英文。原因是
  `performEnter()` 当时只做「有 action 则 `performEditorAction`、否则 `sendDownUpKeyEvents(ENTER)`」，
  完全没碰拼音串 → **该能力此前不存在，本次是新实现**
- **实现**：新增 `PinyinKeyboardView.takeRawComposing()` 取出用户实际按下的键（`composing` 存的就是原始
  按键、不做双拼转换），取出后清空 `composing` 与 `lastCandidates` / `lastPredictions`（避免下一轮
  串味）；`JinnIme.performEnter()` 在拼音串非空时 `commitText(rawComposing, 1)` 并 return
  （不再执行输入框动作 / 换行），为空时保持原逻辑
  - 关键：`composing` 本就是原始按键序列，无需转换 → 双拼按 `budv` 输出 `budv`，不会变成 `budui`

真机（PACM00，双拼）：按 `udv` → 候选「双闸 / 双寨 / 双栈」，回车输出 `udv` ✅；按 `vbudv` → 候选
「周 / 州 / 皱」，回车输出 `vbudv` ✅。两处日志均记「回车输出英文原文: xxx」，确认输出按键原文而非
候选或转换后拼音；回车后候选栏回到功能面板（输入状态已正确清空）。

累计：已修 81 项；APK 6.2MB。

---

# 深度缺陷扫描（Lint + 高危模式排查）

用 Android Lint 系统扫描 + 对六类高危模式（API 版本 / 并发 / 资源泄漏 / 异常处理 / 索引边界 / 性能）
定向排查。

**修的 3 处**

| # | 位置 | 缺陷 | 级别 | 修法 |
|---|---|---|---|---|
| 1 | `ClipboardHistoryActivity` | 手写 `if (SDK_INT >= TIRAMISU)` 注册广播，Lint 判「未声明导出标志」Error | P1 | 改 `ContextCompat.registerReceiver`（androidx 按版本自动适配） |
| 2 | `JinnIme:251` | `requestShowSelf()` 是 API 28 方法而 minSdk = 26，低版本抛 `NoSuchMethodError` | P1 | 加 `SDK_INT >= P` 判断；空 `onFailure {}` 补日志 |
| 3 | `JinnIme:966` | `if (SDK_INT < N) return` 在 minSdk 26 下恒为 false（`ObsoleteSdkInt`） | P3 | 删冗余判断 |

- ① 原版本分支逻辑本身正确（API 33+ 传 flag、以下不传）不会崩溃，但 Lint 仍报 Error —— 手写分支
  无法静态证明完备、日后改动易漏 → `ContextCompat` 是正解：既消告警又把版本适配交给 androidx
- ② `runCatching` 兜住异常所以不崩溃，但功能在 Android 8.0/8.1 上静默失效，且空 `onFailure {}`
  违反项目「绝不静默吞异常」规则 → 两处都补

**确认无问题（排查过、留证）**：`!!` 强制解包全项目 0 处；`ClipboardDb` 六处 `rawQuery` 全部 `use {}`
关闭；广播注册 / 注销三处全部配对；`PinyinEngine` 用 `@Volatile` + `synchronized` 保护共享词库；
`candidates[0]` 有 `isNotEmpty()` 守卫、`EXT_DICT_URLS.first()` 是常量非空列表；仅 1 处空 catch
（`BackgroundIo` 的 `RejectedExecutionException`），有意设计且有注释。

**登记未修**

- ① `MicButton.kt:98`（P2，属语音部分受禁改保护）：每次 `onDraw` 新建 `RadialGradient`，重绘频繁时
  持续 GC 压力（`DrawAllocation`）。建议用 `Matrix` + `setLocalMatrix()` 移动已有 shader，或在
  `onSizeChanged` 缓存 shader —— 不动语音代码
- ② `onTouch` 未调 `performClick`（`JinnIme:495/1078`、`PinyinKeyboardView:726/896`）：影响 TalkBack。
  标准修法是判为点击时补 `performClick()`，但键盘手势判定敏感、改动有触碰长按 / 滑动手势风险，待确认
- ③ 49 处 `UnusedResources`：纯体积优化，无功能影响

验证：Lint 2 error → **0 error**（88 warnings 保留）；单测 10 类 / 166 例；APK 6.2MB、
签名 V1=false / V2=V3=true；4 字节对齐通过；真机安装 Success（PACM00）。

累计：已修 84 项；Lint 0 error。

---

# 无障碍 performClick、语音入口连通性、保活文案清理

- **无障碍：onTouch 补 performClick（Lint 5 → 2）**：Lint `ClickableViewAccessibility` 要求自定义触摸
  处理在识别到点击时通知无障碍服务。原则：只在确认是「点击」时补，且不改动原手势返回值
  - `PinyinKeyboardView` 字母键：仅 `ACTION_UP` 且水平位移 < `touchSlop` 时补（该键有「符号层左右滑动
    翻页」手势，滑动结束时发点击事件会误导 TalkBack）
  - `PinyinKeyboardView` 退格键：`ACTION_UP` 时补（只有点击 / 长按连删，无滑动）
  - `JinnIme` 退格键：`ACTION_UP` 时补（`CANCEL` 不补）
  - `JinnIme` 麦克风按钮：`ACTION_UP` 时补（该按钮无 OnClickListener，`performClick()` 只向无障碍服务
    补发一次事件，不影响手势判定与录音）
  - 保留 1 项：Lint 另要求 `MicButton` 自身重写 `performClick()`，属语音部分受禁改保护，未动
- **语音入口增加服务连通性判断（核心）**：`JinnIme.switchToVoiceKeyboard()` 在
  `asr.state != LinkState.ONLINE` 时记日志 + Toast `voice_offline_blocked` 并 return（不切换键盘模式）。
  拦在入口的价值：否则用户进了语音面板才发现连不上，会白等一次 WebSocket 握手并连带触发录音权限检查、
  录音器初始化等整条链路负担
  - 新增文案「语音服务未连接（%1$s:%2$d），暂不能使用语音输入」。属入口控制，未改 `AsrClient` /
    录音链路，符合禁改约束
- **清理保活遗留文案 19 条**：`keepalive_*`、`accessibility_*`、`settings_keepalive*`、
  `switch_keepalive / root / notify_high`、`btn_accessibility / battery / root`、`battery_open`、
  `root_done / fail / need`
  - 其余 30 项 `UnusedResources` 不动：已启用 `isShrinkResources = true`，release 构建已自动裁掉
    （APK 里本就没有），清源码定义不减小 APK；且存在被动态引用（如 `getIdentifier`）的可能。
    收益 0、风险不为 0

验证：Lint 0 error / 88 → 64 warnings（`ClickableViewAccessibility` 5 → 2）；单测 10 类 / 166 例；
APK 6.2MB、签名与对齐均通过；真机 —— 字母键 `qie` → 且 / 切 / 窃（点击未被 performClick 破坏）、
符号层滑动翻页未被误判为点击、长按空格在服务在线时正常进入语音键盘未被误拦。

累计：已修 90 项；Lint 0 error / 64 warnings。

---

# Binder 超限崩溃修复 + 词库压缩调优（APK 6.2 → 6.1MB）

- **缺陷：粘贴广播携带全文 → `TransactionTooLargeException` 崩溃**
  （`ClipboardHistoryActivity.requestPaste()` 把 `item.content` 整条正文塞进广播）
  - 必然出事：广播经 Binder 传递、事务上限约 1MB，而剪贴板单条正文无长度上限
    （`ClipboardPrefs.maxItems` 只限条数）→ 复制长文章 / 日志 / 大段代码后点「粘贴」即崩溃。
    这正是「文本类应用 + Android 14+」的典型闪退源
  - 修法：广播只传 id，正文由接收方按 id 读库 —— `ClipboardDb` 新增 `contentById(id)`（含读库 +
    AES 解密，必须走 `BackgroundIo`）；`ClipboardHistoryActivity` 只带
    `EXTRA_CLIPBOARD_PASTE_ITEM_ID`；`JinnIme` 接收器按 id 读库后回主线程 `pasteClipboardText`；
    删除已无引用的 `EXTRA_CLIPBOARD_PASTE_TEXT`
  - 三处剪贴板入口核对（隐私 / 安全改动必须同步）：只有 `ClipboardHistoryActivity` 走广播；
    `ClipboardPanelView`（键盘内嵌）与 `SearchPanelView`（顶部搜索）是直接回调、不经过 Binder
  - 注：崩溃需 >1MB 剪贴板内容才能复现，无法用 adb 便捷构造 → 只有代码逻辑与编译保证，无真机复现证据
- **APK 体积分析**：`pinyin_phrases.txt.xz` 5.74MB 占 93.4%，其余（`classes.dex` 0.26MB、
  `pinyin_chars.txt` 0.04MB、okhttp `publicsuffixes.gz` 0.04MB）合计 <7% → **APK 就是词库**，
  其他已优化到位（R8 + `shrinkResources` 生效）
  - 试过的方案：xz `preset=7 + lc=4 + pb=0` 5.743 → 5.652MB（−1.59%，解压字典 8→16MiB）✅ 采用；
    xz `9e`（64MiB 字典）再省 0.14% 但要 +48MiB 解压内存 ❌；brotli `-q11 -w24` 5.653MB ❌
    （Android 无内置解码，引库反增体积）；二进制词库格式预估 −5~10% ❌（工程量大、风险高）；
    删低频词 / 更多词移到扩展包可达 −10~40% ❌（有损、牺牲输入体验，需用户决定）；
    扩展包同步重压只省 5KB ❌（要重传两个仓库附件）
  - 关键洞察：词库是「用体积换输入体验」的资产，无损压缩已到极限（再挤只剩 1.5%）；
    要显著变小只能砍词，那是体验取舍，不该替用户决定

**验证**：重压基础包并逐字节解压校验（无损）✓；`PhraseDictIntegrityTest`（直接校验 asset 词库）通过 ✓；
真机词库加载 8550ms（优化前 9514ms，反而快约 1 秒）、输入 `qie` → 且 / 切 / 窃 正常 ✓；
APK 6.2 → 6.1MB，签名与对齐通过。

累计：已修 92 项；APK 6.1MB；Lint 0 error；单测 166 例全绿。

---

# 词库整体替换为 rime-ice（APK 6.1 → 4.5MB）

- **为什么换**：原词库三个结构性问题 —— 无词频字段（候选无法按常用度排序、"低频"无从定义）、
  16.2% 的行有 GBK 误解码残留（`瀵勮繙` / `浣犲ソ` 永远打不出来，纯废条目）、连写歧义词需手工维护
  （企鹅 / 西安 / 饥饿靠人肉补，易漏）。rime-ice（雾凇拼音）三个都解决：100% 带词频、已校对无损坏、
  原生含歧义词
- **转换**：新脚本 `tools/dict_builder/convert_rime_ice.py`（可重跑，纯读取 + 统计，不碰 docs）——
  读 `docs/rime-ice/cn_dicts/{base,ext}.dict.yaml`，解析 `词<TAB>拼音(空格分隔无调)<TAB>词频`，
  拼音去空格即本项目的键，按词频降序聚合输出 `键<TAB>词1|词2|…`，再按 ≤4 字 / >4 字切分为基础包与扩展包

| 指标 | 原词库 | rime-ice | 变化 |
|---|---|---|---|
| APK | 6.1MB | 4.5MB | −26% |
| 基础包 xz | 5.65MB | 4.10MB | −27.4% |
| 扩展包 xz | 2.19MB | 1.80MB | −17.8% |
| 词库加载 | 8550ms | 6602ms | −1.9s |
| 拼音键 | 826,897 | 787,991 | −4.7% |
| 词条 | 1,006,326 | 882,180 | −12.3%（多为损坏词） |

真机：`qie` → [企鹅, 且, 切]、`xian` → [西安, 西岸, 锡安]、`jie` → [饥饿, 极恶, 极饿] —— 原先手工维护的
歧义词 rime-ice 全都原生支持，且靠词频自然排在首位。

---

# 清洗游戏词库并合并进扩展包

- **来源**：`docs/games_ime_dict.txt`（1361 条）+ `docs/klbq_ime_dict.txt`（329 条），格式与 rime-ice
  一致（`词 拼音(空格分隔无调)`）；docs/ 只读
- **清洗：删 145 条垃圾词，保留 1545 条**：含数字或字母 139（`CS2` `CSGO` `DOTA2` `DNF` `刀塔2`
  `奥卡努斯2` `2号仓库2`）、超长 >8 字 5（`前面的区域以后再来探索吧`、`行踪不定,暂无固定的居所`，是句子）、
  含标点 1（`千代(ちよ)`）；保留纯中文，允许间隔号 `·`（`奥黛丽·格罗夫`）
  - 字母数字必须删：拼音引擎按合法音节切分，字母 / 数字切不开（单字母合法音节只有 `a e m n o`），
    当键永远打不出来，与网络词 `yyds` 同理
- **合并（merge 模式）**：扩展包 183,919 → 185,408 键（新增 1,489 键、追加 1,506 词）；词条总数
  186,073；体积 1.80 → 1.81MB。同键已有词保留在前、游戏词追加在后，不覆盖基础候选

验证：词条入库（解压校验）—— `阿拉德/alade`、`崩坏星穹铁道/benghuaixingqiongtiedao`、
`艾泽拉斯/aizelasi`、`奥黛丽·格罗夫/aodailigeluofu`、`艾卡/aika`、`白墨/baimo` 全部在位；
真机日志确认「扩展词库: 已加载（长词可用）」；Release 附件同步（关键，否则用户下载到旧包）：
GitHub 覆盖上传 1,901,048 字节 ✅、Gitee 删旧附件（id 3201963）→ 传新（id 3204221）1,901,048 字节 ✅，
两源实测 `Content-Length` 均 1,901,048 ✅。
方法记录：Gitee 的 `releases/tags/{tag}` 返回的 assets 没有 id，必须用
`releases/{release_id}/attach_files` 才能拿到附件 id 来删除。

**新增工具**：`convert_rime_ice.py`（rime-ice 转换 + 对比报告）、`merge_game_dicts.py`
（游戏词库清洗 + 合并，支持 `--dry-run`）、`词库重构-提示词与公开词库参考.md`（格式规范、拼音约束、
网络词收集提示词）。

累计：已修 96 项；APK 4.5MB（自 10.6MB 累计 −58%）；Lint 0 error；单测 166 例全绿。

---
---

# 深挖词库 BUG：修复扩展包合并产生重复候选

- **缺陷**：`PinyinEngine.loadPhrasesReader()` 的 merge 分支是
  `if (existing != null) existing + kept else kept.toTypedArray()`，直接拼接不去重 → 扩展包与基础包
  收录同一个词时候选栏出现两个完全相同的候选：`aersasi` [阿尔萨斯, 阿尔萨斯]、
  `aizelasi` [艾泽拉斯, 艾泽拉斯]、`airen` [爱人, 矮人, 矮人]、`aixi` [爱惜, 艾希, 艾希]。
  影响面实测：877 个词条会产生重复候选，其中 404 个键完全重复
- **根因两层，代码层才是根治**
  - 数据层：合并游戏词时只查扩展包、没查基础包 → `merge_game_dicts.py` 合并前载入基础包全部词并排除
    （本次跳过 415 条）
  - 代码层：rime-ice 自身的 base/ext 就有重叠（如 `aersasi` 两边都收）→ merge 时先建「已见」集合，
    扩展包只追加未出现过的词
  - 关键：数据层只能治「自己引入的重复」，第三方词库自带的重叠必须靠代码层兜住 —— 数据修完仍剩
    613 个重叠键 / 467 个重复词条，全部由代码层消除
- **修复后**：`aersasi` [阿尔萨斯]、`aizelasi` [艾泽拉斯]、`airen` [爱人, 矮人]、`aixi` [爱惜, 艾希]。
  去重开销只落在真有重叠的键上（现 613 个），对 60 万键的加载耗时可忽略

**顺带排查（确认无问题，留证）**：键能否被合法音节切分 —— 基础包 604,077 键 + 扩展包 185,408 键、
切分失败 0 ✅；常用词覆盖抽查 20/20 命中 ✅；单字表不需替换（`pinyin_chars.txt` 17,117 字符完全覆盖
rime-ice 的 8,182 且更全）✅；单字表编码损坏仅 1 处（`覅`），属检测方法误报 ✅。
另注：日志里 `音节=414`（非合法音节表的 422）不是 BUG —— 414 是单字表实际覆盖到的音节数，
缺的 8 个（`biang` / `fiao` / `junding` 等）本就没有对应单字。

**验证与同步**：`lintDebug` + `testDebugUnitTest` 通过；APK 4.5MB、签名与对齐通过；真机安装 Success；
扩展包已变更 → Release 附件同步（GitHub `--clobber` + Gitee 删旧 id 3204221 → 传新 id 3204300），
两源实测 `Content-Length` 均 1,897,940 ✓。

累计：已修 98 项；APK 4.5MB。

---
---

# 可选词库延迟加载 + 分类词库页（词库链路专项）

背景：基础包换 rime-ice 后，可选包（长词包 1.8MB / 腾讯大词库 6.4MB）与基础包在 `onCreate` 一起加载，
使「开机后首次输入的候选就绪」从 6.6s 拖到 25.4s。

- **可选词库改延迟加载**：`PinyinEngine.load()` 只加载基础包；新增 `loadOptionalAsync(delayMs = 5000)`
  在基础包就绪 5 秒后于后台补齐可选包。IME 加载时机实测：弹 / 收键盘走 `onStartInputView` /
  `onFinishInputView`，不碰词库 → 加载耗时只影响「IME 服务重建后的首次输入」，日常打字无影响
  - 实测：基础包 6.4s 就绪（与不装可选包一致），可选包 26s 后后台就绪（172 万键）
  - 关键细节：可选包加载后必须重新 `finalizeLoad()` —— `sortedPhraseKeys` 是加载时快照，
    不重建则新词不参与前缀补全
- **新增分类词库页**：`OptionalDicts.kt`（词库清单：文件名 / 说明 / 体积 / 启动耗时 / 下载源，新增词库
  只需加一条记录）+ `DictManagerActivity.kt`（列表 + 下载 / 删除 + 顶栏关闭；视觉沿用剪贴板页配色：
  背景 `#0B1020`、顶栏 `#141C33`、卡片 `#1C1F26`、强调 `#4C8DFF`）；设置页旧扩展词库卡片
  （URL 输入框 + 下载 / 删除按钮）换成单按钮「打开分类词库」
- **词库容器线程安全化（并发缺陷修复）**：延迟加载在后台线程 merge、而用户此刻正在打字、主线程
  `query()` 并发读同一批容器。原实现全是普通 `HashMap` / `HashSet` / `ArrayList`：扩容可能让桶链表成环
  （读方死循环、IME 卡死）或抛 `ConcurrentModificationException`；有序表用 `clear()+addAll()` 会让读方
  看到中间态 → Map 改 `ConcurrentHashMap`（容量在声明处预分配）、`validSyllables` / `syllablePrefixes`
  改 `ConcurrentHashMap.newKeySet()`、三个有序表改 `@Volatile` 不可变快照 + 整体替换引用、
  `loadOptionalAsync` 的「检查-置位」纳入同一把锁。真机：基础包就绪后立刻连续打字（触发原竞态窗口），
  全程 0 异常
- **其余修复**：候选可达 60 条且每次按键全量重建 View（`yi` 有 326 字、93 个音节超 `MAX_CHARS(60)`）
  → 截断渲染为 24 条（上屏逻辑不受影响，`lastCandidates` 仍存完整列表）；加载失败静默无痕（裸线程 +
  `load()` 无 try/catch，异常穿透后线程死亡）→ `JinnIme` 侧 `runCatching` + `Diagnostics.e(msg, tr)`
  保留堆栈；下载失败用户看不到提示（回调写进已重建 Activity 的旧 View）→ 额外弹 Toast；
  merge 去重无测试守护（测试入口 `loadFromTexts` 固定 `merge=false`、分支不可达）→ 补
  `loadPhrasesText(merge)` + `mergePhrasesForTest`，新增 `OptionalDictMergeTest` 7 用例

验证：`compileDebugKotlin` + `testDebugUnitTest`（183 用例 / 14 类）+ `lintDebug` 全通过；APK 4.5MB、
对齐与签名通过；真机 —— 基础包 6409ms 就绪、可选词库后台就绪、候选栏输入 `yi` 命中
「易 / 翼 / 一 / 以 / 已…」、0 崩溃。

---
---

# 剪贴板模块专项（死代码清理 + 语义修正 + 状态同步）

## 一、删除已下线的剪贴板历史页

实跑发现 `ClipboardHistoryActivity`（719 行完整实现）不可达：
`exported=false` + 全项目无任何 `startActivity` 调用点。用户确认为有意下线后清理：

- 删除 `ClipboardHistoryActivity.kt`（719 行）与 Manifest 声明
- 删除 22 个历史页专用字符串资源（保留 `clipboard_root_*` 等设置页仍在用的）
- `AGENTS.md`：「三处入口」修正为「两处入口」；删除 4 条描述 ListView /
  Activity 生命周期 / FLAG_SECURE 的失效约束

现状：剪贴板只有两个展示入口，`ClipboardPanelView`（键盘内嵌面板）+
`SearchPanelView`（顶部搜索面板），改动需两处同步。

## 二、容量裁剪改为三级优先级

原 `trimTo` 只豁免收藏（`WHERE is_favorite = 0`），隐私记录会随「最旧非收藏」被删除。
虽代码/注释/规则三者一致（属有意设计），但隐私是用户刻意主动标记的，语义上更该保留。

新增 `TRIM_PRIORITY` 按顺序分轮取证、凑够即停：

1. `is_favorite = 0 AND is_private = 0`（普通记录）
2. `is_favorite = 0 AND is_private = 1`（隐私，最后才动）
3. 收藏永不参与裁剪

每轮只取「还需删的条数」，因此普通记录够删时完全不触碰隐私，不会因「隐私太多」而无法裁剪。

## 三、修复拖选状态未同步到键盘侧

`selectionActive` 在 `JinnIme` 与 `PinyinKeyboardView` 各存一份，靠显式调用同步。
两处「复位」逻辑分别实现，其中 `onClipboardStateChanged` 漏了同步调用：

```
onSelectionModeChanged  复位后 **有** pinyinKeyboard?.setSelectionActive(active)
onClipboardStateChanged 复位后 **没有** 同步调用    ← 缺陷
```

根本原因：同一语义的复位逻辑在两处重复实现，各自演化，典型的「复制粘贴式重复」
导致的不一致。已让两处对齐。

## 四、剪辑板布局数据抽离（顺带的架构改进）

`PinyinKeyboardView` 内嵌 455 行静态配置数据（QWERTY 行定义 + 数字层映射 +
符号分组含日文假名表），与视图逻辑混杂。抽出为 `KeyboardLayouts.kt`：

视图类 2029 → 1574 行。验收采用逐项比对：提取全部 `'x' to "Y"` 键值对，
旧 1263 / 新 1263 序列完全一致，编译与常规测试都发现不了数据丢失/串位，
只有用户按到那个键才会暴露。

## 五、验证

- `compileDebugKotlin` + `testDebugUnitTest`（183 用例）+ `lintDebug` 全通过
- 真机：设置页剪贴板卡片与「历史数量上限」正常渲染、键盘面板可开、符号层正常、0 崩溃
- 新增 `ClipboardClassifierBoundaryTest`（6 用例）：空白包裹、长数字+换行（含 CRLF）、
  长度边界、含数字负例、空白输入、URL 优先级

## 六、累计

- 本次净减代码约 1800 行（含删除 719 行死代码、抽离 455 行数据）
- 测试用例 166 → 183（+17）

---

# 26 键区圆角与按键间隙改为设置页可调

日期：2026-09-16 ｜ 验证：`testDebugUnitTest`（184 用例）+ `lintDebug`（0 error）+ 真机

## 一、需求与现状

要求：把 26 键区（布局上整整三行、共 28 个按键，10 字母 + 9 字母 + 「大写 + 7 字母 + 删除」）
的按键圆角与按键左右/上下间隙做成设置页可调项，定义域合理。

现状有个隐藏的不一致：这 28 键虽然视觉上连成一片，绘制路径却是两条：

| 键 | 绘制方式 | 原参数 |
|---|---|---|
| 26 个字母键 | `PinyinKey.onDraw` 自绘圆角矩形 | `KEY_CORNER_DP = 0f` / `KEY_MARGIN_DP = 0f`（硬编码常量） |
| 大写键 / 删除键 | XML `ImageButton` + `key_bg.xml` 背景 | 圆角 10dp，且贴满单元格（无内缩） |

也就是说同一行里两种圆角，且大写/删除键比字母键高一圈。做成「统一参数」正好一并修掉。

## 二、实现

1. 新增 `KeyAppearance.kt`（纯 JVM，可单测）：定义域、默认值、进度换算收敛到一处。
   - 圆角 0 ~ 24dp（步进 1dp，0 为直角，>24 会削成胶囊失去按键辨识度）
   - 间隙 0 ~ 8dp（步进 0.5dp，0 为无缝；8dp 时最窄的字母键仍剩 23dp 可点）
   - 默认 0dp / 0dp，与加入参数之前的外观完全一致（增量修改，不偷偷改观感）
   - NaN / Infinity 一律退回默认值：`coerceIn` 对 NaN 的比较恒为 false，不拦住会一路传到
     `drawRoundRect`（整个键面画不出来）
2. `Prefs`：新增 `keyCornerDp` / `keyGapDp`，读写都钳位（历史配置或外部写入可能越界）。
3. `PinyinKey`：删掉 `KEY_CORNER_DP` / `KEY_MARGIN_DP` 两个硬编码常量，改为
   `setKeyAppearance(cornerPx, insetPx)`。四边等量内缩（上下也缩，原来只有左右缩 +
   顶部固定 8px），并加「内缩不超过键尺寸一半」的保护。
4. `PinyinKeyboardView`：新增 `applyKeyAppearance()`，把同一组参数推给三类键：
   - 26 个字母键 → `setKeyAppearance`
   - 大写/删除键 → 用 `GradientDrawable` + `RippleDrawable` 动态重建同圆角背景
     （保留按压水波纹；mask 必须单独造不透明同圆角矩形，否则波纹被裁没）
   - 并把内缩等效成四周 `LayoutParams` 外边距，否则普通 View 会贴满单元格、与字母键错位
   - 大写锁定切换改走 `refreshShiftBackground()`（原来直接 `setBackgroundResource`，圆角写死）
5. 生效时机：`applyKeyAppearance()` 挂在 `init` 与 `configure()`；而 `configure()` 由
   `onStartInputView` 每次聚焦时调用，所以设置页改完，收起键盘再弹出即生效，无需重启进程。
6. 设置页：新增「键盘外观」卡片（两个 SeekBar + 当前值 + 生效说明），拖动即时落盘。

## 三、间隙语义（写进 KDoc，避免后人误解）

「间隙」= 相邻两键之间的空隙：每个键四边各内缩 `gap / 2`，相邻两键相加正好等于 gap，
左右与上下一致。不是「每边留多少」（旧常量 `KEY_MARGIN_DP` 是每边内缩语义，已随常量删除）。

## 四、验证

- `testDebugUnitTest`：184 用例全绿（新增 `KeyAppearanceTest` 11 例：钳位 / NaN /
  步进 / 全进度往返 / 定义域 / 上界可点性）
- `lintDebug`：0 error / 34 warning（warning 为既有基线）
- 真机（PACM00，1080×2280）：`build_apk.py` 产物 `adb install -r` 覆盖安装 →
  - 设置页「键盘外观」卡片渲染正常，滑杆 → 数值联动（实测拖到 24dp / 4dp，标签同步显示）
  - 收起键盘再弹出：28 键（含大写/删除）圆角与间隙同步生效、行高完全对齐（截图放大复核）
  - 日志侧证：`PinyinKeyboard: 键盘外观: 圆角=24 dp 间隙=4 dp`
  - 复位 0dp / 0dp 后恢复「连成一片的直角键区」
- 测试用例数勘误：前述记的 183 与实际不符，实测本次前 173、本次后 184（以实测为准）。

## 五、明确未纳入

第四行（符号 / 数字 / 逗号 / 空格 / 中英文 / 回车）仍用 `key_bg.xml` 的 10dp 圆角，
不跟随本参数，需求只圈定了 28 键的三行区域。若要整块键盘统一，再加一次即可。

## 六、发布（覆盖式）

- 提交 `08a9f31`（10 文件，+678 / −15）推送到三处引用，全部为快进：
  GitHub `main`、GitHub `perf/paging-pinyin-rematch`、Gitee `master`（均 `d730404` → `08a9f31`）。
- APK 重新构建：`versionCode=20260916` / `versionName=jinn`，4,737,805 字节，
  `apksigner` 校验 V1=false / V2=true / V3=true，4 字节对齐通过。
- 两仓库 `v20260916` Release 的 `jinn-release.apk` 覆盖：
  - GitHub：`gh release upload v20260916 jinn-release.apk --clobber`
  - Gitee：`GET /releases/{id}/attach_files` 取附件 id → `DELETE` 旧附件 → `POST attach_files` 上传新包
- Release 正文各补一条本条功能说明（变更内容 + 验证各一行），其余文字逐字保持原样。
- 校验：两个下载 URL 均 HTTP 200，`size_download=4,737,805`，sha256 与本地 APK 完全一致。

---

# 独立复核后的 6 项修复（死代码 / 回显 / 无障碍 / 颜色收敛 / 绘制分配）

日期：2026-09-16 ｜ 验证：`testDebugUnitTest`（184 用例）+ `lintDebug`（0 error）+ 真机逐项实测

## 一、来源与范围

本次来源是自己的独立复核（`docs/代码审查-20260916-独立复核.md`），十七项候选里只采纳 6 项，
其余九项在取证后被否（外部清单的裁定见 `docs/chatGLM建20260916232359.md` 对照）。

- 采纳：死代码两处、设置页回显、设置页自动填充、颜色收敛、绘制分配与单位。
- 明确不做：第四行（符号 / 数字 / 逗号 / 空格 / 中英文 / 回车）圆角不跟随 28 键参数，
  用户要求保持现状，禁止改动，此后不得再动。

## 二、修复明细

1. 死代码 `onBackspacePressed()`（`PinyinKeyboardView`）：全文件只出现一次；删除键真实路径是
   `setOnTouchListener → handleBackspaceTouch → deleteOne()`，且 `deleteOne()` 是它的严格超集
   （多一个预测态分支 + V 日志）。删 19 行。
   > 顺带说明：它就是外部审查被带偏的源头，对方把这段死代码当活代码逐行分析。
2. 死代码 `formatSize()`（`SettingsActivity`）：本文件内无调用；`DictManagerActivity` 有独立副本且在用。删 6 行。
3. 剪贴板上限越界后界面不回显真值：输入 `0` / `99999` 会被 `Prefs` 钳到 `1` / `9999`，但原实现只在
   解析失败时才回写输入框 → 界面显示与生效值不一致。改为存完统一回写生效值（仅在显示不同时 `setText`）。
4. 设置页 5 个 `EditText` 关闭自动填充（`android:importantForAutofill="no"`）：避免密码管理器把
   host / port / 提示词 / 上限当账号字段弹「保存密码」。Lint `Autofill` 5 条清零。
5. 硬编码颜色收敛：`ClipboardPanelView` / `SearchPanelView` / `DictManagerActivity` 共 33 处
   `Color.parseColor` → `R.color.*`。`colors.xml` 新增 `danger` / `ok` / `warn` / `btn_disabled` 四个
   色值（其余复用既有同名同值项：`app_bg` / `card_bg` / `surface_hi` / `text_primary` /
   `text_secondary` / `accent` / `text_on_accent`）。逐值等价、零行为变化。
   `DictManagerActivity` 里 9 个 `COLOR_*` 常量位于 `companion object`（取不到 Context），整块删除，
   调用点改用 `getColor(R.color.x)`。
6. `PinyinKey` 绘制分配与字号下限单位：
   - `subLabel` / `subLabelRed` 的 `split + filter + trim` 从 `onDraw` 移到 setter 缓存
     （绘制路径零分配，行为不变：空行丢弃、去首尾空白）；
   - `MIN_LONG_TEXT_SIZE = 9f`（像素）→ `MIN_LONG_TEXT_RATIO = 0.06f`（占键高比例）。
     旧值在 mdpi 上等于 9dp，比按宽度算出的适配字号还大，会把 `synchronized` 这类长标签顶出按键
     左右边界；改比例后任何密度都自洽，本机 2.75x 上等效 ≈ 8.9px，与旧观感一致。

## 三、验证

门禁：`testDebugUnitTest` 184 用例全绿；`lintDebug` 0 error / 34 → 29 warning（Autofill 5 条清零）。

真机（PACM00，覆盖安装后逐项实测）：

| 项 | 证据 |
|---|---|
| 上限回显 | 输入 `0` → 失焦后日志 `剪贴板历史数量上限: 1`，输入框回显 1（改前恒显示 `0`）；测完已把用户原值 `9999` 复原并复核 |
| 删除键（死代码删除后） | 单击 → `退格删拼音: b`；按住 1.5s → `退格删拼音:`（连续删除生效），无崩溃 |
| 双拼键面提示 | 多行提示 `uai/ing`、`ong/iong`、`iang/uang` 全部正常渲染（验证缓存改造没丢内容） |
| 符号层长文本 | 编程分组 `self/None/elif/lambda/global/assert/__name__` 按宽度自适应，无溢出、无裁切 |
| 颜色 | 剪贴板面板空态、分类词库页（顶栏 / 卡片 / 次文字 / warn 橙 / ok 绿 / danger 红 / accent 蓝）与改前一致 |
| 28 键区外观 | 仍为默认 0dp / 0dp 连片直角块（本次未触碰外观参数） |

## 四、累计

- 测试用例 184（不变，本次无新增可测纯逻辑）；Lint 0 error / 29 warning

## 五、发布（覆盖式）

- 提交 `798b874`（9 文件，+157 / −91）推送到三处引用，全部快进：GitHub `main`、
  GitHub `perf/paging-pinyin-rematch`、Gitee `master`（均 `68e671c` → `798b874`）。
- APK：`versionCode=20260916` / `versionName=jinn`，4,737,805 字节，
  `sha256=f45bf09d…`，`apksigner` V1=false / V2=true / V3=true，4 字节对齐通过。
- 两仓库 `v20260916` Release 的 `jinn-release.apk` 覆盖：GitHub `gh release upload --clobber`；
  Gitee 取旧附件 id `3212831` → DELETE → 重新 `attach_files`（新 id `3212991`）。
  Release 正文各补本次条目（变更内容 + 验证各一条），其余文字逐字保留。
- 校验：两支下载 URL 均 HTTP 200、`size_download=4,737,805`、sha256 与本地 APK 完全一致。
- 过程记录：GitHub 侧当晚出现多次瞬时抖动（`git ls-remote` / `api.github.com` 报
  TLS unexpected eof），push 与 upload 重试一次即成功，非权限问题。

---

# 删除废弃词库源文件 + 文档全量对齐

日期：2026-09-17 ｜ 影响面：无代码变更，不重编 APK（`tools/` 与 `docs/` 不参与构建）

## 一、删除

按要求删除 `tools/dict_builder/source/pinyin_phrases.txt`，顺带移除删空后的
`tools/dict_builder/source/` 目录（约 28MB 磁盘回收）。

删除前指纹（留档备查，文件已不可恢复）

| 项 | 值 |
|---|---|
| 大小 / 行数 | 29,370,289 字节 / 1,048,342 行 |
| sha256 | `b3519a87176b22e45101fa418a9c859576834423e3efc7f77bd6c15a64500129` |
| 版本库状态 | 被 `.gitignore`（`tools/dict_builder/source/`）忽略，从未入库，删后无法从 git 恢复 |

删除前确认（三条都已核实）

1. 构建链（`build.gradle.kts` / `app/build.gradle.kts` / `build_apk.py` / `settings.gradle.kts`）
   与现行词库脚本（`convert_rime_ice.py`、`convert_rime_tencent.py`）均不读取它；
2. 全仓已无 `source/pinyin_phrases` 硬编码引用；
3. 现行资产完好（`assets/pinyin_phrases.txt.xz`、`release/dict_ext.txt.xz`、`release/opt_tencent.txt.xz`）。

删除理由（每条独立成立）：第 28 轮起词库整体换成 rime-ice，流水线改读 `docs/rime-ice/`；
文件含 573 行 GBK 乱码（如 `鍙樺姩涓嶅眳` 应为「变动不居」、`b煤d脿o` 应为 `bùdào`，连拼音键都坏了）；
留着它只会让人误以为「改词库＝改这个文件」。

## 二、文档对齐到最新版

| 文档 | 改动 |
|---|---|
| `tools/dict_builder/README.md` | 新增：现行流水线图（源 → 脚本 → 资产 → 发布要求）、脚本一览（在用 / 备用 / 评估 / 历史）、已删文件说明与指纹 |
| `AGENTS.md` | 新增「词库来源与生成（重要）」：现行源 `docs/rime-ice/`、改词库必做三件事、旧目录不得再放源文件 |
| `docs/DATA_SOURCES.md` | 「当前状态」整段按实测重写：基础包 604,077 行 / 4.3MB、扩展包 185,005 行、可选包 955,032 行、单字表 422 键、音节表 422 行、常用字 6,528 字；标注雾凇拼音已正式采用；补 GPL-3.0 说明与旧文件删除记录 |
| `docs/词库重构-提示词与公开词库参考.md` | 提示词模板里的源码路径改为 `docs/rime-ice/cn_dicts/`；第五节生效流程改写为现行流水线（旧步骤划掉留作历史） |
| `docs/计划任务.md` | #10.10 由「建议改名 `_DEPRECATED_` 或删除」→「✅ 已于 2026-09-17 直接删除」 |
| `docs/代码审查-20260916-独立复核.md` | 废料隔离项标记完成 |

顺带修正两处历史错数：`pinyin_chars.txt` 与 `pinyin_syllables.txt` 实测均为 422（旧文档写 416）。

## 三、刻意未动

- `docs/HANDOFF.md`、`docs/ONBOARDING.md` 等带日期的历史快照保持原样
  （按 `AGENTS.md` 规则 `docs/` 是本地历史资料目录、只读；且它们自带「文档生成时间 / 对应提交」标注）。
- 不重编 APK、不动 Release：本次无代码变更，第 34 轮产物（4,737,805 字节 / sha256 `f45bf09d…`）
  与当前代码完全对应，Release 附件保持有效。

---

# 按 rime-ice 标准复刻 7 套双拼方案（含「26 键键面随方案变化」）

日期：2026-09-17 ｜ 验证：`testDebugUnitTest`（200 用例）+ `lintDebug`（0 error）+ 真机逐方案实测

> 需求原文：按 rime-ice 的标准复刻；一次不只做小鹤（搜狗/微软/紫光/ABC/加加一并做）；
> 双拼时 26 键键面必须随方案变化（韵母提示是必须做的事）；「输入方案」必须在「显示生僻字」上方。

## 一、核心做法：键位不手抄，从 schema 生成

手抄 7 套方案（共约 3000 条键位）必然出错且无法自证，因此新增生成器
`tools/dict_builder/gen_shuangpin_tables.py`：

1. 解析 `docs/rime-ice/double_pinyin*.schema.yaml` 的 `speller/algebra`（权威定义）；
2. 按 librime 的代数语义（`xform` 替换 / `derive` 追加 / `erase` / `xlit` 逐字符映射）
   逐条施加到项目 `assets/pinyin_syllables.txt` 的 422 个合法音节上，算出每套方案的码；
3. 产出 `app/.../ShuangpinSchemes.kt`（生成文件，勿手改，730 行）：
   `initials`（声母键）+ `codes`（码 → 音节，扁平表）。

| 方案 | 声母特例 | 码数 | 需要分号键 |
|---|---|---|---|
| 自然码 | zh→v / ch→i / sh→u | 434 | 否 |
| 小鹤 | 同上 | 424 | 否 |
| 搜狗 | 同上 | 429 | 是（`ing` 在 `;`） |
| 微软 | 同上 | 435 | 是 |
| 紫光 | zh→u / ch→a / sh→i | 417 | 是 |
| 智能 ABC | zh→a / ch→e / sh→v | 413 | 否 |
| 拼音加加 | zh→v / ch→u / sh→i | 429 | 否 |

## 二、换表安全性：与旧实现逐码对拍

`tools/dict_builder/verify_shuangpin_migration.py` 把旧的手写自然码实现照抄成 Python，
在 676 个两键码上与新生成的表逐一对拍：

- 3 处差异，全部是旧实现的 BUG（新表正确）：`rw` 旧产出 `ria`（非音节）→ 应为 `rua`（挼）；
  `yo` 旧 `yuo` → 应为 `yo`（哟）；`hm` 旧 `hian` → 应为 `hm`（叹词）；
- 旧实现另有 242 条独有码，产物全是非音节（垃圾） → 换表零功能损失。

## 三、键面提示：由码表反推（这才是「随方案变化」的正确做法）

`ShuangpinTable.finalHint(key)` 在运行期从 `codes` 反推每个键的韵母，
`initialOf(key)` 给出 zh/ch/sh 红字。于是：

- 7 套方案的键面提示各不相同且永远与引擎一致（同一份数据，不可能漂移）；
- 顺带修掉一个历史缺陷：旧 `shuangpinHint('o')` 手写成 `"ou"`，而引擎里 `o` 是 `o/uo`（`ou` 在 `b` 键）
，键面在骗用户。现在自动派生为 `uo/o` ✓（`ShuangpinSchemesTest` 用回归用例钉死）；
- 特例自动跟随：加加的 `sh/ch/zh` 是 `i/u/v`、ABC 是 `v/e/a`、紫光是 `i/a/u`，红字提示自己就对了。

## 四、分号键：三套方案缺它就打不出 ing

键盘只有 26 个字母键，而搜狗 / 微软 / 紫光的 `ing` 落在分号上
（三个 schema 的 `alphabet` 都显式含 `;`）。没有它，应/听/明/定 这类音节完全打不出来。

做法：第三行 `m` 与删除键之间加一个 `PinyinKey`（`key_semicolon`），只在当前方案需要时显示，
其余方案与符号层/数字层一律 `GONE`（不参与测量 → 26 键布局与已调好的圆角/间隙零影响）。
键面与大写字母键同款：主文本 `;`、下方韵母提示 `ing`（提示同样取自方案表）。

## 五、状态与入口

- `Prefs.useShuangpin: Boolean` → `Prefs.shuangpinScheme: Int`（0 全拼 … 7 加加，取值只增不改），
  读取时带迁移：新键不存在而老键为 `true` → 自然码。真机实测：设备老配置 `shuangpin=true`
  → 升级后设置页显示「自然码双拼」，用户方案不跳变（老键保留不删，利于回滚）。
- `ShuangpinScheme` 枚举（`PinyinEngine.kt`）：方案身份 + 持久化取值 + 展示名/短名 + `cycle()`；
  设置页下拉与键盘循环共用同一份列表，不会脱节。
- 键盘功能面板：按钮改为方案循环，主文本＝当前方案名、副文本＝下一个方案（真机实测
  「自然码 / 换小鹤」→「小鹤 / 换搜狗」…）；空格键小字改为「中文+方案名」。
- 设置页新增「输入方案」下拉（8 项），按要求放在「显示生僻字」上方（首版放错了位置，已修正并复验）。

## 六、诚实的边界（写清不做的部分）

1. 4 个音节任何双拼都编不出：`hng` / `hm` / `m` / `n`（鼻音叹词，词库 0 条）与 `junding`
   （`pinyin_syllables.txt` 里的脏数据：7 个字母，不是音节，顺带登记，本次不动词库）。
2. 「两键一音节」模型下同码写法必须让位（一码只能映射一个音节）：
   `lo`→让给 `luo`（咯/啰 双拼打不出，全拼仍可）；`lve`/`nve`→让给 `lue`/`nue`
   （但词库里 lve/nve 的词条仍能命中，因为查询链路有 ue↔ve 变体回退，无功能损失）；
   `ng`→让给 `neng`/`nang`/`niang`（词库 0 条）。覆盖测试对「让位集合」做了显式豁免，其它任何缺口都算失败。
3. 不实现 rime 的多拼写容错（如 `aai`/`oan` 这类 3 字母派生形式）：键盘模型是两键一音节，
   与其冲突；好处是每个音节固定两次按键。

## 七、验证

门禁：`testDebugUnitTest` 200 用例全绿（新增 `ShuangpinSchemesTest` 16 例）；
`lintDebug` 0 error / 29 warning（新加的下拉补了 `paddingEnd`，未引入新告警）。

真机（PACM00，覆盖安装后逐方案实测）：

| 项 | 证据 |
|---|---|
| 设置页位置 | `spinner_shuangpin` y=121 在 `switch_show_rare_chars` y=440 上方 ✓（截图复验） |
| 显示生僻字下方 | 「输入方案」卡片与其说明完整渲染，位于生僻字卡片正上方 |
| 配置迁移 | 老键 `shuangpin=true` → 设置页显示「自然码双拼」；改选小鹤后写 `shuangpin_scheme=1`/`=2` 可复现 |
| 键面提示随方案 | 逐方案截图核对：小鹤 `d=ai / k=ing·uai / l=iang·uang`；搜狗/微软 `d=iang·uang / k=ao / l=ai`；紫光 `q=ao / w=en / b=iao / n=ui·ue / m=un`；ABC 红字 `a=zh / e=ch`；加加红字 `i=sh / u=ch / v=zh` ✓ 全部符合各自标准 |
| 分号键 | 仅搜狗/微软/紫光出现，键面显示 `;` + `ing`；切到小鹤/ABC/加加/全拼后自动消失 ✓ |
| 分号键输入 | 紫光方案下 `t` + `;` → 日志 `双拼[t;]→ting → [听, 停, 厅]` ✓ 端到端打通 |
| 小鹤输入 | `x n h e` → 日志 `双拼[xnhe]→xiaohe → [小河, 小盒, 小鹤]` ✓（「小鹤」在第 3 位，前两位是同音更高频词，属正常排序） |
| 自然码回归 | 键面 `o` 键显示 `uo/o`（修掉旧 `ou` 错误），其余键位与旧版一致 ✓ |
| 现场还原 | 实测后已把方案改回用户原值「自然码双拼」（`shuangpin_scheme=1`） |

## 八、新增文件

| 文件 | 说明 |
|---|---|
| `app/.../ShuangpinSchemes.kt` | 生成数据：7 套方案的声母键 + 码表 + 提示派生（730 行） |
| `app/src/test/.../ShuangpinSchemesTest.kt` | 16 例：各方案键位、分号键、提示、全表往返自洽、覆盖完整性 |
| `tools/dict_builder/gen_shuangpin_tables.py` | 生成器（解析 schema → 施加代数 → 出 Kotlin） |
| `tools/dict_builder/verify_shuangpin_migration.py` | 换表对拍脚本（676 码逐一对拍） |

## 九、发布（新建 v20260917，非覆盖）

- 提交 `9e9e8e3` 推送到三处引用（`bbe6cbf` → `9e9e8e3`）：GitHub `main`、
  GitHub `perf/paging-pinyin-rematch`、Gitee `master`。
- APK：`versionCode=20260917`（构建日期已跨天，故是新 tag，与 v20260916 并存）、
  `versionName=jinn`、4,774,669 字节、`sha256=5a172f74…`、`apksigner` V1=false / V2=true / V3=true、4 字节对齐。
- 两端新建 Release `v20260917`：
  - GitHub：`gh release create`（首次因 `api.github.com` EOF 失败，重试成功；`--latest` 另一次重试成功）
  - Gitee：`POST /releases`（id `1149106`，`target_commitish=master`）→ `POST /releases/{id}/attach_files`（附件 id `3215230`）
- 校验：两支下载 URL 均 HTTP 200、`size_download=4,774,669`、sha256 与本地 APK 完全一致。
  至此 `v20260917` 标签在两仓库都存在，应用内「检查更新」不会再误报网络异常。

---

# 面板按钮恢复原样 + 方案选择只在设置页（含两个真机发现的修复）

日期：2026-09-17 ｜ 验证：`testDebugUnitTest`（201 用例）+ `lintDebug`（0 error）+ 真机实测

> 需求原文：「面板导航栏的原来的 拼音和双拼 切换的按钮，禁止修改，必须改回原样（文本都不准改，用回原来的）。
> 因为输入方案在 APP 首页设置里改就行了，禁止放在输入法面板里修改。」

## 一、恢复原样（一字未改）

| 位置 | 现在 |
|---|---|
| 功能面板按钮主文本 | `全拼` / `双拼`（不再显示方案名） |
| 功能面板按钮副文本 | `换双拼` / `换全拼` |
| 空格键顶部小字 | `中文全拼` / `中文双拼` |

原来的二态交互也一并恢复：面板只切「用不用双拼」，`ShuangpinScheme.cycle()`（八态循环）
删除，改为 `ShuangpinScheme.toggle(current, configured)`。具体方案只在设置页「双拼的输入方案」
下拉里改，面板没有任何选择方案的入口。

## 二、状态拆分为三键（老配置零迁移）

| 键 | 类型 | 谁写 | 含义 |
|---|---|---|---|
| `Prefs.useShuangpin` | Boolean | 键盘面板按钮 | 用不用双拼（老版本即此键，语义不变 → 无需迁移） |
| `Prefs.shuangpinScheme` | Int 1..7 | 设置页下拉 | 选定哪套双拼（只增不改） |
| `Prefs.effectiveShuangpinScheme` | 派生 | IME 读取 | 生效方案：没启用双拼就是全拼 |

设置页下拉语义：选「XX 双拼」＝启用双拼并记住该方案；选「26 键全拼」＝关闭双拼（方案记忆保留，
下次面板切回双拼仍是这套）。

## 三、真机发现并修掉的两个缺陷（本次重点）

1. 面板切回双拼会丢用户选的方案：原实现读 `effectiveShuangpinScheme`，而把双拼关掉后它
   就等于全拼 → 兜底成自然码，用户在设置里选的小鹤被丢掉。改为读 `Prefs.shuangpinScheme`（恒定 1..7）。
   实测：设置选小鹤 → 面板切到全拼 → 再切回 小鹤 ✓，且 `shuangpin_scheme=2` 全程未被面板改动。
2. 设置页下拉会静默改写用户的方案：原「初始化完成」闸门挡不住 `onRestoreInstanceState` 触发的
   迟来 `onItemSelected`（实测设备上的 `shuangpin_scheme` 被改成 2）。改为
   只有用户触摸过 Spinner 才允许写配置（`setOnTouchListener` 置位）；「默认键盘模式」下拉
   同类隐患一并修掉（同一个坑，前述有记录）。

## 四、验证

- 门禁：`testDebugUnitTest` 201 用例全绿（新增面板二态切换用例）；`lintDebug` 0 error / 29 warning
  （给两个 Spinner 的触摸监听补 `performClick`，消掉新引入的 2 条 `ClickableViewAccessibility`）。
- 真机（PACM00）：面板按钮显示原文「双拼 / 换全拼」；小鹤方案下键面提示确为小鹤（`w=ei`、`y=un`…）；
  面板二态切换不动方案值；设置页下拉选自然码/小鹤分别写入 `1`/`2`；实测后已还原为自然码。
- 用户自行编辑的文案原样保留未动（「双拼的输入方案」、「26 键按钮 会自动显示韵母…」、「检查APP更新」、
  键盘外观两条说明）。

## 五、发布（同日覆盖 v20260917）

- 提交 `adf2429`（9 文件，+168 / −51）推送到三处引用（`9f532f0` → `adf2429`）：
  GitHub `main`、GitHub `perf/paging-pinyin-rematch`、Gitee `master`。
  （用户自行编辑的 `strings.xml` 文案随本次一并提交，内容未做改动。）
- APK：`versionCode=20260917`、4,774,669 字节、`sha256=12360062…`、
  `apksigner` V1=false / V2=true / V3=true、4 字节对齐。
- 两仓库 `v20260917` 同日覆盖：GitHub `gh release upload --clobber`；
  Gitee 旧附件 `3215230` → `DELETE` → 重新 `attach_files`（新 `3215384`）。
- Release 正文各补本次条目，并修正上一轮正文中已过时的「面板按钮改为方案循环」描述
，同一个 tag 的正文必须与 APK 的实际行为一致。
- 校验：两支下载 URL 均 HTTP 200、`size_download=4,774,669`、sha256 与本地 APK 完全一致。

---

# 双拼方案下拉去掉「全拼」

日期：2026-09-17 ｜ 验证：`testDebugUnitTest`（202 用例）+ `lintDebug`（0 error）+ 真机实测

> 需求原文：「输入方案的下拉列表，只是双拼的而已，不要放全拼的进来。」

## 一、改动

- 下拉内容改用 `ShuangpinScheme.SHUANGPIN_ONLY`：只列 7 套双拼
  （自然码 / 小鹤 / 搜狗 / 微软 / 紫光 / 智能 ABC / 加加），不含「26 键全拼」。
- 语义随之简化：选中即「记住该方案 + 启用双拼」（原来「选全拼＝关闭双拼」的职责交回键盘面板按钮）；
  关闭双拼请用面板上原来那个「全拼 / 双拼」按钮。
- 初始选中显示已记住的那套双拼（而非「生效方案」）：列表里没有全拼，用生效方案会出现「双拼关着时
  不知该选谁」的错位。
- 说明文案保留用户原句，仅追加一句「此处只选『哪套双拼』；全拼/双拼切换用键盘面板的按钮」。

## 二、验证

- 门禁：`testDebugUnitTest` 202 用例全绿（新增「双拼方案列表_共七套且不含全拼」，同时校验
  每套都有键位表）；`lintDebug` 0 error / 29 warning。
- 真机（PACM00）：
  | 项 | 证据 |
  |---|---|
  | 下拉内容 | 实测 7 项，「全拼」项不存在（逐项打印选项文本核对） |
  | 选中即启用 | 选小鹤 → `shuangpin_scheme=2` 且 `shuangpin=true` |
  | 面板不动方案 | 面板切到全拼 → `shuangpin=false`，而 `shuangpin_scheme` 仍为 `2` |
  | 关闭状态下拉显示 | 双拼关闭时下拉仍显示「小鹤双拼」（= 已记住的方案） |
  | 现场还原 | 最后选自然码，恢复用户原值 `shuangpin_scheme=1` + `shuangpin=true` |

## 三、发布（同日覆盖 v20260917）

- 提交 `505f755`（5 文件，+71 / −18）推送到三处引用（`949f600` → `505f755`）。
- APK：`versionCode=20260917`、4,774,669 字节、`sha256=34979c13…`、V1=false / V2=true / V3=true、4 字节对齐。
- 两仓库 `v20260917` 同日覆盖（GitHub `--clobber`；Gitee 删旧附件重传），正文补本次条目。

---

# 双拼性能/健壮性审查 + 冷启动卡顿诊断（含 3 处低风险优化）

日期：2026-09-17 ｜ 验证：`testDebugUnitTest`（202 用例）+ `lintDebug`（0 error）+ 真机日志/内存取证

> 两条需求：①「彻底审查双拼方案相关代码，排查潜在 BUG，重点评估输入响应、高频输入性能、
> 内存占用、异步阻塞」；② 用户反馈「系统冷启动 / 刚开机后输入法很卡、无法正常打字，怀疑是
> 骁龙 8 至尊版性能不够」。

## 一、双拼代码审查（详见 `docs/双拼-性能与健壮性审查.md`）

实测结论：热路径没有问题，`toQuanpin` 每键 133~368ns（12 键 445ns、22 键 978ns），
26 键键面提示派生 3.2µs，表常驻内存 0.254MB（进程 PSS 的 0.08%）。全同步、无阻塞调用、
无每键注册监听、表数据不可变无竞态。

真实开销只有一处：7 套键位表首次访问要 57~79ms（JVM 实测；ART 同量级），
而键盘视图恰好是在 `JinnIme.onCreateInputView()`（键盘首次弹出）里构造的 → 拖慢首屏。
探针进一步定位：大头不是建表数学（单套仅 1.7ms），而是那个 730 行 / 3,000 字面量生成类的
类加载与静态初始化，所以优化要针对「何时做」而非「少做」。

已修（行为不变）：
1. `ShuangpinSchemes.kt` 改为按方案惰性构建（`Map<String, Lazy<ShuangpinTable>>`，生成器同步改）；
2. 新增 `Shuangpin.warmUpAll()`，`JinnIme.onCreate` 用后台优先级守护线程预热
   （日志 `双拼键位表预热完成: 7 套 / Xms`）；最坏情况退化为只建当前方案一张（1/7）。

被证伪的猜想（写进文档，避免后续重复排查）：怀疑「在需要分号键的方案里输入 `;` 后切到
不需要它的方案，composing 残留 `;` 会导致后续打不出字」，实测不可达：面板按钮只在
`composing` 为空时显示，设置页切换又会清空 composing。无需加防护。

判定「不划算不改」（附实测依据）：`finalHint` 每次排序（26 键仅 3.2µs）、残码日志里
重复调 `toQuanpin`（~0.5µs）、每键日志字符串、`Prefs(context)` 复用、`ShuangpinScheme.of()`
线性扫描。

## 二、冷启动卡顿诊断（详见 `docs/冷启动卡顿-诊断与优化方案.md`）

真机时间线（PACM00）：

| 时刻 | 事件 | 关键数字 |
|---|---|---|
| 15:57:55.719 | 内存采样[词库加载前] | 已用 14MB |
| 15:58:06.387 | 基础词库加载完成 | 词语键 601,139 / 反向索引 692,183 / 耗时 10,672ms |
| 15:58:06.389 | 内存采样[词库加载后] | 已用 134MB |
| 15:58:45.832 | 可选包加载完成 | 词语键 1,721,350 / 耗时 34,435ms |

（同日另一次：基础 5,962ms、可选 20,953ms，波动近 2 倍 ⇒ 不是算力不足，是启动期争抢。）

根因：① `query()` 在 `loaded == false` 时静默返回空（`PinyinEngine.kt:575`）→ 基础就绪前
6~10.7s 一个字都打不出来；② 加载期无任何提示，与「坏了」外观一致；③ 可选包固定
5s 延迟开跑（`JinnIme.kt:183`）正好撞上用户开始打字，21~34s 的重活抢 CPU/内存带宽；
④ 进程 PSS 305MB（`smaps_rollup`：Private_Dirty 290MB）⇒ 冷启动易被 LMK 杀，杀了又得重来。
骁龙 8 至尊版不是瓶颈。

耗时构成（JVM 拆解）：xz 解压 732ms（41%）／解析 + 建表 971ms（54%），真机 6~10.7s ≈ 其 4~6 倍。

优化方案（按收益）：P0-1 高频子集先行（首次可用 < 1s，随后后台 merge 全量）；
P0-2 加载期候选栏提示「词库加载中…」；P1 持久化二进制索引（6~10.7s → 0.3~1s，内存 290MB → 60~100MB）；
P1-2 可选包推迟到空闲/息屏（或纳入紧凑索引）；P2 反向索引懒建；P3 启动期任务错峰。

## 三、本次代码改动（3 处，均为低风险）

| 改动 | 位置 |
|---|---|
| 键位表改惰性 + 新增 `warmUpAll()` | `ShuangpinSchemes.kt`（生成器同步）、`PinyinEngine.kt` |
| 服务创建时后台预热（后台优先级） | `JinnIme.kt:156` |
| 可选包加载线程降为后台优先级 | `PinyinEngine.kt:349` |

## 四、验证

- `testDebugUnitTest` 202 用例全绿；`lintDebug` 0 error / 29 warning。
- 一次性性能探针（已删除）实测数据见两份文档。
- 真机冷启动复验（force-stop 后重开）：
  `16:18:43.686 [jinn-shuangpin-warmup] 双拼键位表预热完成: 7 套 / 34ms`，
  预热在独立后台线程 34ms 完成，不再占用键盘首次弹出路径；
  同一次冷启动基础词库加载 6,504ms（与此前 5,962 / 10,672ms 同量级，印证瓶颈在词库而非双拼）。
- 审查覆盖：逐码对拍（676 码）、全表往返自洽、7 套覆盖同一音节集合、边界场景逐项验证（见文档第三节）。

---

# P0-1 高频子集先行 + P0-2 加载期提示（冷启动可输入 243~314ms）

日期：2026-09-17 ｜ 验证：`testDebugUnitTest`（208 用例）+ `lintDebug`（0 error）+ 真机冷启动实测

> 承接前述的冷启动诊断，用户拍板「P0-1 + P0-2 立刻做」。

## 一、P0-1 高频子集先行（两段式加载）

- 新增生成器 `tools/dict_builder/gen_hot_dict.py`：从与全量资产同源同序的
  `base.dict.yaml` + `ext.dict.yaml` 里按词频取一批词，产出 `assets/hot_phrases.txt.xz`
，4 万词 / 34,515 键 / 220KB（词长 ≤4，与基础包一致；同键同词取最大词频）。
- `PinyinEngine.load()` 改两段：
  1. 先加载高频子集 + 单字表 + 音节表 → `loaded = true`（可输入）；
  2. 再加载全量基础包并 merge（走已有的并入语义：已有在前 + 去重追加）。
- 一致性保证（关键）：子集必须在每个键下都是全量词表的前缀，否则并入后的候选顺序
  会与「一次性全量加载」不一致。为此生成器改为「取权重 ≥ 阈值 W 的全部词（含并列）」，
  并自校验前缀性质；Kotlin 侧再加两道守卫：
  - `HotDictMergeTest`：子集先行 + 全量并入 vs 全量单载 → 候选完全一致；
  - `HotDictAssetTest`：抽样 346 键确认子集是全量前缀（并校验体积/键数/必含词）。
  > 该测试在实现期真的抓到了 BUG：第一版子集只从 `base` 取词，而全量资产是 `base+ext`
  > 合并的，抽样键的前缀性质被破坏 → 按同源同序重写后通过。

## 二、P0-2 加载期提示

- 候选栏在未就绪时显示「词库加载中…」，在有输入但候选为空且全量未并入时显示
  「词库补全中…」（均为候选栏内联文本，IME 内不放弹窗；文案进 `strings.xml`）。
- 说明：因为单字表随子集先行加载，任何合法音节至少能出单字候选，所以第二条实际很少触发，
  属安全网。

## 三、真机实测（PACM00）

| 项 | 结果 |
|---|---|
| 首次可用 | 243ms / 288ms / 314ms（三次冷启动），此前 6~10.7s，约 25~40× 提升 |
| 全量并入 | 日志 `全量基础包已并入（高频子集 288ms + 全量补全 6885ms）` |
| 输入验证 | 冷启动后 1.1s 输入「自然码 nihk」→ `候选: 双拼[nihk]→nihao → [你好, 拟好, 你]` ✓ |
| 键面/预热 | `双拼键位表预热完成: 7 套 / 33ms`（后台线程，见前述） |

## 四、代价与回归规则

- APK 体积 +220KB（4.8MB）。
- 改词库必须同时重跑 `gen_hot_dict.py`（否则子集与全量脱节，`HotDictAssetTest` 直接失败）；
  已写入 `AGENTS.md` 与 `tools/dict_builder/README.md`。

---

# P1-2 可选包空闲加载（+ P1 持久化索引设计）

日期：2026-09-17 ｜ 验证：`testDebugUnitTest`（208 用例）+ `lintDebug`（0 error）+ 真机触发实测

## 一、P1-2 可选词库包改为「空闲时加载」

- 问题：可选包（ext + 腾讯，1.14M 词条，真机解析 21~34s）原来在「基础包就绪后固定 5s」
  开始加载，而那正是用户开始打字的时间点，后台重活与前台输入抢 CPU/内存带宽。
- 改法（`JinnIme.maybeLoadOptionalDict`，单次触发，先到先得）：
  | 信号 | 触发条件 | 常量 |
  |---|---|---|
  | 息屏 | `ACTION_SCREEN_OFF` 广播（锁屏＝必然空闲） | — |
  | 键盘收起后闲置 | `onFinishInputView` 后再等 20s | `OPTIONAL_IDLE_DELAY_MS = 20_000` |
  | 兜底 | 服务创建后 180s 仍无上述信号 | `OPTIONAL_FALLBACK_DELAY_MS = 180_000` |
  加载线程本身仍是 `THREAD_PRIORITY_BACKGROUND`（前述已设）；`loadOptionalAsync` 内部会先
  `load()`（幂等、持锁等待），因此与基础包加载天然串行，不会重复解析同一批容器。
- 真机实测：`可选词库: 开始后台加载（触发: 息屏）`、`（触发: 键盘收起后闲置）`；
  且重活确实在息屏期间完成，`可选词库已加载 2 个包，共 8365KB`。

## 二、P1 持久化二进制索引：设计完成，待开工

产出 `docs/词库持久化索引-设计与分期.md`：格式（keysBlob + keyOffsets + wordsBlob + wordOffsets，
带 version + 源摘要）、运行时二分查找、可选包各自独立索引并缓存到 `filesDir/index/`、
回退安全阀（索引异常自动退回现有文本加载）、以及最重要的护栏
`IndexParityTest`：文本加载 vs 索引加载逐条比对候选完全一致。

改动半径已清点：`phrasesByPinyin` 6 个读取点 + 3 个写入点（含运行时 merge）、
`wordToPinyin` 7 处、`charsBySyllable` 小表不动 ⇒ 判定为独立一轮的中等重构，
分三期（Stage 1 索引+只读查询 / Stage 2 可选包独立索引+设备端缓存 / Stage 3 反向索引懒建）。

预期收益（依据实测拆分）：基础包 6~10.7s → 0.3~1s；常驻内存 290MB → 20~30MB（基础部分）。
开工前需确认两点（见文档第六节）：① 生僻字过滤从加载期移到查询期是否接受；
② 是否只保留索引资产（会牵动读文本资产的既有测试）。

## 三、验证

- `testDebugUnitTest` 208 用例全绿；`lintDebug` 0 error / 29 warning。
- 真机触发日志见上（两种信号各自实测命中）。

---

# P1 Stage 1 落地，词库改二进制索引（加载 6~10.7s → 2.9s，基础包内存 −70%）

日期：2026-09-17 ｜ 验证：`testDebugUnitTest`（213 用例）+ `lintDebug`（0 error）+ 真机实测

> 用户确认：接受「生僻字过滤移到查询期」+「APK 只留索引」。

## 一、改动

1. 构建期索引：新增 `tools/dict_builder/build_dict_index.py`，把基础包写成
   `assets/pinyin_index.bin.xz`（magic/version/摘要 + keysBlob + keyOffsets + wordsBlob + wordOffsets，
   与文本资产同源同序）。文本资产不再进 APK（改留档到 `out/rime_ice/`）。
2. 运行时只读索引：新增 `PhraseIndex`（二分查找 + 按需解码，共享一份字节数组，不复制数据）；
   `PinyinEngine.load()` 第二段由「读文本 + 建表」改为「解压索引 + 读偏移数组」。
3. 统一读取入口 `phrasesFor(key)`：运行时并入的词（高频子集/可选包）优先，否则查索引；
   生僻字过滤移到这里（查询期），索引因此与开关无关、可原样复用。
4. 移除 69 万条反向索引：`consumption`（残码保留）与 `predict`（智能预测）改为依赖
   `candidatePinyin`（查询时记录「候选 → 产出它的键」，上限 4096 条即清空），省约 50MB，
   语义还更准（记录真正产出该候选的键）。
5. 索引就绪后自动摘除「与索引内容完全重复」的高频子集条目（子集是全量前缀）。

## 二、真机实测（PACM00）

| 项 | 改前 | 改后 |
|---|---|---|
| 首次可用（可打字） | 243~314ms | 238ms |
| 全量基础包就绪 | 6~10.7s | 2.9s（索引本身 2.29s） |
| 进程内存（基础包 only） | ~145MB | PSS 90MB / Private_Dirty 77MB（−70%） |
| 进程内存（含可选包） | 305MB / 290MB | PSS 245MB / Private 232MB |
| 可选包加载 | 21~34s（定时触发） | 17s（空闲触发） |
| APK | 4.30MB 文本 | 5.04MB 索引（净 +0.74MB） |

与预估的差距（如实记录）：加载没到 0.3~1s，瓶颈从「解析+建表」变成解压
（索引原始 17.3MB，其中偏移表 4.8MB）；含可选包的内存只降 20%，因为可选包仍是 HashMap（Stage 2）。

## 三、护栏与实现期抓到的两个问题

- 新增 `IndexParityTest`（文本 vs 索引：300 键候选、80 个部分输入、逐键词表，全部一致）
  与 `IndexFeatureRegressionTest`（残码消费长度 == 键长；预测与文本路径逐条一致）；
  `PhraseDictIntegrityTest` / `HotDictAssetTest` 改读索引。
- 问题 1（对拍抓到）：索引头部解析按 24 字节、构建写 30 字节 → 偏移全错。已统一为 `HEADER_SIZE = 30`。
- 问题 2（预测对拍抓到）：删掉反向索引后 `predict` 直接失效 → 改用「候选→键」短期映射；
  测试还暴露 fixture 缺少「键是另一个键前缀」的组合（对拍退化成两边都空）→ fixture 生成器补上扩展键。

## 四、下一步（Stage 2）

可选包索引化（下载后在设备端构建索引并缓存到 `filesDir/index/`）→ 内存 245MB → 约 40~60MB、
可选包加载 17s → 约 1s。另有两条可选优化：索引偏移表改长度数组（省 3.6MB 原始体积）、
或换更快的 xz preset（体积换速度），见 `docs/词库持久化索引-设计与分期.md` 第八节。

---

# 键面韵母提示硬性规则（去自身字母 / ü 去重 / 行数 ≤2）

日期：2026-09-17 ｜ 验证：`testDebugUnitTest`（216 用例）+ `lintDebug`（0 error）+ 真机截图

> 用户明示四条规则（禁止违反）。

## 一、规则

| # | 规则 | 说明 |
|---|---|---|
| 1 | 绝不显示与该键字母相同的韵母 | e 键不显示 e、v 键不显示 v、a/i/u 同理，纯冗余 |
| 2 | ü 的两种写法只留 `v` | `u`（jqxy 后）与 `v`（l/n 后）同时出现时只留 v，避免同键出现两个同读音项 |
| 3 | 总行数强制 ≤ 2 | 含红色 zh/ch/sh 那一行：该键承担 zh/ch/sh 时韵母挤成一行、红色占第 2 行 |
| 4 | 同行内顺序确定 | 长者在前；同长按韵母表规范顺序（`ui` 在 `ue` 之前） |

用户点名项（已逐条断言）：自然码 / 小鹤 / 加加 `v` = `ui` + 红 `zh`；
微软 `v` = 一行 `ui ue` + 红 `zh`；搜狗 / 微软 `y` = `uai` / `v`。

## 二、实现与真机确认

- 规则写在生成器模板（`gen_shuangpin_tables.py` 的 `ShuangpinTable.finalHint`），
  重生成 `ShuangpinSchemes.kt`；不手改生成文件。
- 顺序踩坑（记录）：先去自身字母会让 v 键的 `{ui, u, v}` 变成 `{ui, u}`，
  去重条件（同时含 u 与 v）再也命中不了 → 必须先去重 ü 写法、再剔除自身字母。
- 另修：生成器的词典源原读 `assets/pinyin_phrases.txt.xz`（Stage 1 已删除）→ 改读流水线留档
  `tools/dict_builder/out/rime_ice/pinyin_phrases.txt`，否则生成器直接崩。
- 新增 `HintRuleTest`：7 套 × 26 键全量扫描（无自身字母、行数 ≤2）+ 用户点名项逐条核对。
- 真机截图确认（自然码）：`e`/`a` 键下方空白 ✓；`i` 键只有红色 `ch` ✓；`u` 键只有红色 `sh` ✓；
  `o` = `uo` ✓；`v` 键 = `ui` + 红色 `zh` ✓。

---

# P1 Stage 2，可选词库包索引化（复用 59ms，内存 −63%）

日期：2026-09-17 ｜ 验证：`testDebugUnitTest`（219 用例）+ `lintDebug`（0 error）+ 真机实测

> 承接前述（Stage 1：基础包索引化）。用户批准开工。

## 一、改动

1. 构建器（`PhraseIndex.build(Sequence<String>, stamp)`）：单趟流式构建同一套索引格式
，绝不把 114 万行读进内存（峰值≈索引体积）；用一个自增偏移数组 + 两个字节缓冲。
   键序异常即抛错，调用方回退旧的「运行时并入」路径，功能不丢。
2. 设备端缓存：可选包首次加载时构建索引并落盘 `filesDir/index/<源文件>.idx`；
   之后启动直接读缓存（源文件 `length:mtime` 摘要不符才重建）；源包被删则启动时清理缓存。
3. 查询改为逐段查找：「运行时（高频子集）→ 基础索引 → 各可选索引」按加载顺序查找，
   用 `LinkedHashSet` 去重合并，与旧实现「基础在前、可选追加」的结果完全一致；
   另加 `mergedCache`（上限 4096）避免同键重复合并。
4. `wordToPinyin` 不再因可选包加载而膨胀（候选→键短期映射已覆盖该需求）。

## 二、真机实测（PACM00）

| 指标 | 改前 | 改后 |
|---|---|---|
| 可选包首次加载 | 21~34s | 8.7s（ext 1.8s / 腾讯 6.9s） |
| 可选包后续加载 | 21~34s（每次重解析） | 59 ms（读缓存） |
| 进程内存（含可选包） | PSS 305MB / Private 290MB | PSS 110~113MB / Private ~100MB（−63%） |
| 磁盘 | — | 索引缓存 ~40MB（app 私有） |
| 输入功能 | — | 正常（`nihk → nihao → [你好, 拟好, 你]`） |

> 复用的 59ms 甚至优于设计预估（原预期 ~1s）：索引是未压缩的紧凑数组，
> 读盘 + 解析头部就是全部成本。

## 三、护栏与实现期修掉的三个问题

- 新增 `IndexBuilderParityTest`：① 设备端 Kotlin 构建器与构建脚本（Python）产出的索引
  逐字节一致；② 两端可互读；③ 可选索引按旧语义合并（基础在前 + 去重追加）。
- 问题 1：`mergedCache` 未在词库变更时失效 → `OptionalDictMergeTest` 两条失败
  → 在 `loadPhrasesReader`（所有写入路径的汇合点）里 `clear()`。
- 问题 2：新测试类用真实 asset 注入词库后污染单例状态，破坏
  `PinyinEngineTest.零声母双拼_安全` 的既有假设 → 新测试类统一加 `@After resetForTest()`。
- 问题 3：fixture 生成器补「前缀对」时打乱了键序 → 流式构建器（要求升序）直接报错
  → 生成器改为排序输出，两端构建器才能看到同一顺序（这也正是字节级对拍能通过的前提）。

---

# 索引格式 v2（偏移表 → 长度数组，APK −561KB、加载 −26%）

日期：2026-09-17 ｜ 验证：`testDebugUnitTest`（219 用例）+ `lintDebug`（0 error）+ 真机实测

## 一、改动

v1 的两张 u32 偏移表（各 604,078 × 4B = 2.42MB，共 4.83MB）替换为
`keyLengths`（u8，0.60MB）+ `wordLengths`（u16，1.21MB）；读取时前缀和还原为 IntArray，
运行时结构与 v1 完全一致。版本号升为 v2；构建脚本（Python）与设备端构建器（Kotlin）
同步升级，`IndexBuilderParityTest` 的逐字节对拍继续通过。

## 二、真机实测（PACM00）

| 指标 | v1 | v2 | 变化 |
|---|---|---|---|
| 索引原始体积 | 17.3MB | 14.4MB | −2.9MB |
| 索引 xz（APK 内） | 5.04MB | 4.48MB | −0.56MB |
| APK 体积 | 5,745,490 | 5,184,338 | −561KB |
| 基础索引加载 | 2289ms | 1700ms | −26% |
| 两段式总计 | 2942ms | 2314ms | −628ms |
| 设备端可选包 `.idx` | 39.6MB | 34MB | −5.5MB |
| 可选包复用 | 59ms | 44ms | −15ms |

## 三、兼容性（零用户操作）

格式版本升为 2 后被 `parse` 拒绝的旧缓存会自动重建：真机实测连续两次启动的日志为
`已构建词库包索引: ext.xz（…）` → `复用索引缓存: ext.xz（…）`，用户不需要做任何事。
另：构建脚本增加了长度数组的边界自检（键 ≤255B、词表 ≤64KB，超界直接失败而不是静默算错）。

---

# 今日改动专项审查（6 个缺陷，全部修复并回归）

日期：2026-09-17 ｜ 验证：`testDebugUnitTest`（221 用例全绿）+ `lintDebug`（0 error）+ 脚本冒烟

范围：今日 8 个提交（4452d2e..ddd6193）+ 工作区未提交补丁（12 文件）的全部差异。

| # | 缺陷 | 影响 | 处置 |
|---|---|---|---|
| 1 | `PhraseIndex.parse` 段长校验Int 溢出 | 声明「超大段长」的索引会绕过校验 → 切片时抛 AIOOBE，破坏 `of()` 「不自洽返回 null」的行为（设备端可选包索引是外部文件，坏了只应回退重建） | 改 Long 累加 + 精确总长比对（含回归测试） |
| 2 | `mergedCache` 未在基础索引到位时失效 | 高频子集窗口（0.24~2.3s）内查过的键，其「只有子集」的答案会粘住到可选包加载为止（20~180s）→ 候选截断 | 失效点收敛到 `finalizeLoad()`（所有加载路径的汇合处） |
| 3 | 💥 `mergedCache` 是裸 HashMap | 主线程查询读写 vs 加载线程 `clear()` → 丢更新/错值/桶链表成环卡死（与 `charsBySyllable` 当初同类） | 改 `ConcurrentHashMap` |
| 4 | 💥 `gen_hot_dict.py` 读已删除的文本资产 | 写完资产后抛 `FileNotFoundError` → 高频子集生成闸门静默失效 | 改为从源现算全量词表（不再依赖 APK 资产） |
| 5 | 💥 `verify_shuangpin_migration.py` ①找旧格式表（`ValueError`）②判据函数定义了却没调用 | 「换表不许改行为」这道闸门一直红着/崩着 = 形同虚设 | 适配新生成格式 + 判据真正接入 `valid_syllables()`；修正后结论：真回归 0（3 条差异均为「旧=非法音节 → 新=合法」的修正） |
| 6 | 引用腐化：`convert_rime_tencent.py` / `merge_game_dicts.py`（后者静默退化为空集、合并失去去重）/ `split_dict.py` / 流水线 README 仍指向已移除的文本资产 | 历史脚本一跑就崩或静默做错 | 全部改为读留档文本 + 兼容 xz + 显式警告；README 流程图补上索引步骤 |

额外核对：`Prefs.shuangpinScheme` 取值归一化（脏编号回写规范化值）✓ 正确；生成器幂等性 ✓；
`build_dict_index.py --fixture` 重跑输出逐字节一致 ✓；`convert_rime_ice.py` 对已删资产有 `isfile` 兜底 ✓ 不崩。

---

# 深度路径审查（predict 成本悬崖 + 拖选状态机发散）

日期：2026-09-17 ｜ 验证：`testDebugUnitTest`（221 用例）+ `lintDebug`（0 error）+ 探针量化

## 一、predict 的「成本悬崖」（本次唯一可控风险）

用探针实测（真实索引 604,077 键）：
| 调用 | 命中键数 | 耗时 |
|---|---|---|
| `keysWithPrefix("nihao")` | 39 | 0.05ms |
| `keysWithPrefix("de")` | 2,779 | 0.94ms |
| `keysWithPrefix("ni")` | 6,185 | 2.18ms（且实例化 6,185 个 String） |
| `predict("你好")`（真实路径） | — | 0.10ms/次 |

- 结论：当前不可达，`predict` 的前缀是「整词拼音」（≥4 字符），键数只剩几十个 ✓。
  只有当将来允许对单字候选做预测（前缀退化成单音节）时才会踩到 6,185 键 × 逐个
  `phrasesFor`（合并 + 写缓存，还会冲刷 4096 上限的 mergedCache）。
- 处置：`predict` 增加「收满 `MAX_PREDICTIONS` 立即停止扫描」的安全阀，
  结果与旧的「扫完再 take(N)」完全一致（旧实现同样按字典序累加后取前 N 个），已注释说明；
  并在 `PhraseIndex.keysWithPrefix` 上标注成本悬崖与将来的整改方向（区间回调 API，不建 String）。

## 二、单字候选没有预测（既有行为，非本次回归）

实测 `wordsFor("ni") = null`：单字不在词库里（单字源是 `pinyin_chars.txt`，与词库是两份数据），
因此旧实现的「词→拼音」全量反向索引同样没有单字，选单字后预测为空是既有行为，
新机制的 `candidatePinyin`（候选→键）只在出词路径记录，也不覆盖单字。
若要让「选『你』→ 预测『好吗/你们』」生效，前置条件是先解决第一节的成本悬崖。

## 三、拖选状态机发散（真实缺陷，已修）

`selectionActive` 在两个类各存一份（跨类同步是本项目标记过的坑）：

| 位置 | 触发条件 | 后果 | 处置 |
|---|---|---|---|
| `PinyinKeyboardView.showDirectionPanel()` | 显示方向面板时只重置视图侧，未回传 IME | 视图画 ●（未拖选）、IME 仍以为在拖选 → 点中心键先"取消"再"激活"，点一次没反应。当前不可达（面板只能经会回传的 `hideDirectionPanel` 隐藏），但两处不对称 | 补 `listener?.onSelectionModeChanged(false)`，与 hide 对称 |
| `JinnIme.onStartInputView`（会话切换） | 切到另一个输入框时 anchor/focus 未复位 | 旧字段的坐标残留 → `extendSelection` 拿旧下标 `setSelection`（被系统钳位，选区莫名跳动） | 会话边界调用 `clearSelectionState()` |

顺带把三处各自演化的重复逻辑（`onClipboardStateChanged` / `onSelectionModeChanged` / 新增会话复位）
收敛为一个 `clearSelectionState()`，避免再次出现"只改了一侧"。

## 四、核实为「无缺陷」的相邻项
- `phraseKeysOf`（ue↔ve 变体）在所有出候选路径都生效（查询 921、补全 1282、消费 1177）✓；
  且记录的「候选→键」总是命中的那个键，故预测前缀必然是规范写法，无变体缺口 ✓。
- 分词打分的两处查找用原始键（不含变体），既有行为，非本次引入，仅登记 ✓。

---

# 双拼代数语义与 librime C++ 逐条核对

日期：2026-09-17 ｜ 验证：重生成后键位表逐字节不变 + 迁移闸门「真回归 0」

核对源：`docs/librime-master/src/rime/algo/{calculus.h,calculus.cc,algebra.cc}`（librime 1.17.0，本地参考源码，不入库）。

## 一、逐条对照（C++ 权威语义 → 生成器）

| op | librime 实现 | 我们 | 结论 |
|---|---|---|---|
| `xform` | `Transformation` 默认 `addition=true`+`deletion=true` ⇒ 替换；`boost::regex_replace` 全局替换（`$1` 反向引用） | `re.sub` 替换 | 一致（补：结果为空则丢弃） |
| `derive` | `Derivation : Transformation` 且 `deletion=false` ⇒ 保留原 + 追加 | 保留 + 条件追加 | 一致（补：空结果不追加） |
| `erase` | `Erasion`：`addition=false` + `boost::regex_match`（整串匹配） ⇒ 整串命中才清除 | `re.fullmatch` | 一致 |
| `xlit` | 逐字符映射；Parse 要求两侧字符数相等，否则整个 algebra 加载失败 | `zip`（静默截断） | 本次修：不等长即报错 |
| `fuzz`/`abbrev`/`correction` | `Derivation` 子类，差异只在 credibility/type，字符串集合与 derive 相同 | 忽略 tag | 一致；实测 7 套 schema 未使用这些 op 名 |
| 集合语义 | `Projection::Apply`：每个 op 作用于上一轮整个拼写集合，派生结果进入下一轮（链式组合），map 键天然去重 | `cur→nxt→去重` | 一致 |
| 未匹配 | 原样保留 | `re.sub` 无匹配即原串 | 一致 |
| 正则语义 | `boost::regex`（Perl 语法），`\w \d \b` 默认按 ASCII | Python `re` 的 `\w` 为 Unicode | 本次修：统一 `re.ASCII`（实测当前 0 处受影响） |
| 解析 | 分隔符=首个非小写字母字符；多余参数（tag）忽略 | 严格 `op/…/…` | 本次加固：段结束按 YAML 缩进收紧 + 未知 op 响亮失败 |

## 二、修正的 4 处潜伏差异（+1 处解析加固）

1. `xform` 结果为空时 librime 丢弃该拼写（`!s.str.empty()`），旧实现会把空串留在集合中；
2. `derive` 同样不追加空结果；
3. `xlit` 两侧不等长：librime 直接拒绝，旧实现被 `zip` 静默截断；
4. `\w` 等的 ASCII/Unicode 语义差异（预防性对齐）；
5. `parse_algebra` 旧结束条件（`^\S`）在 algebra 不位于文件末尾时会吞掉后续段（当前恰好都在末尾 → 未暴露）
   → 改为按 YAML 缩进收紧，且遇到不认识的规则行报错而非跳过。

## 三、决定性验证

重生成 `ShuangpinSchemes.kt` 后逐字节不变（`git diff` 为空）→ 上述差异对现行 7 套方案是零语义变更，
即原有实现对真实 schema 本来就是正确的；本次改动的价值是「上游 schema 若调整顺序或新增 op，我们不会静默算错」。
迁移闸门 `verify_shuangpin_migration.py` 仍为真回归 0。

> 本次只改构建期脚本与文档，APK 内产物与应用代码均未变 → 无需重新发版（沿用 sha256 `f1f45f44…`）。

---

# 从 librime 可吸收的设计（取舍清单，含"不该吸收"的实测证据）

日期：2026-09-17 ｜ 参考源：`docs/librime-master/`（librime 1.17.0 源码，不入库，纯参考）

## 一、值得吸收

| 优先 | 吸收点（librime 出处） | 我们的现状 | 收益 | 状态 |
|---|---|---|---|---|
| P0-1 | `Prism : MappedFile`（内存映射索引） | 设备端 `.idx`（34MB）用 `readBytes()` 整块进堆 | 省 ~34MB 堆 + 免一次大拷贝 | 待办（可独立一轮，风险低） |
| P0-2 | `Prism::ExpandSearch` 的 `Match(value,length)` 不物化枚举 | `keysWithPrefix` 会实例化全部命中键（`"ni"` → 6,185 个 String / 2.18ms） | 有"单字候选预测"需求时才必须；现已由"收满即停"的安全阀兜住 | 按需 |

## 二、明确不该吸收（有实测数据）

音节图 + DP 分词（`algo/syllabifier.*`、`SyllableGraph`），看起来最该抄，实测会变坏：

1. `dictionarySegmentation` 里 `key = path.joinToString("")`，而 path 是输入的一种划分 ⇒ join 恒等于输入本身
   ⇒ `hit` 对所有路径相同 ⇒ 分数只差 `-path.size`，且 `phrasesFor(整串) != null` 门禁对所有路径一致
   ⇒ 实际只比「音节数最少」（DFS 最长音节优先，先枚举者胜）。
2. 按 librime 的 DP 形式实现「Σ 各前缀命中数」会退化（每个音节边界都加分 → 切得越碎分越高）。
   用真实词库实跑：`tianqi → ti-a-n-qi`、`beijingdaxue → bei-ji-ng-da-xue`、`wangshang → wang-sha-ng`
   （11 个语料 5 个变坏，还会切出 `ng` 这种鼻音碎片）。
3. 根因：librime 的音节图解决的是拼写歧义 + 可信度排序（同一音节多套拼写、模糊音），
   而我们 26 键全拼只有固定音节表，目标就是"最长音节优先"，用不着图与 DP。

同样不建议：双数组 Trie（我们二分查找已 ~0.5µs，加载耗时 100% 被 xz 解压支配）、
引擎流水线（XL 规模，且与"禁改语音链路"约束交错，列为长期方向）。

## 三、本次落地

1. 修正 `dictionarySegmentation` / `dfsSegment` 的 KDoc：原文声称"返回能拼出最多词库短语的切分"
   与实现不符；举的例子 `xuni → [xu, ni]` 也不成立（实测 `xuni` 只有 1 种合法切分，
   因为 `xun-i` 里的 `i` 不是合法音节）。新注释写明真实语义、反例数据，以及"别照那个思路改"的告诫。
2. 说明 `MAX_SEGMENT_PATHS = 16` 的真实作用：只是防御措施（实测 14 个常见语料仅 `jintiantianqi`
   （18 种切分）超限），不是"截断路径导致选错"的隐患所在。

> 本次为纯注释改动（零行为变更）→ 门禁 221 用例全绿、Lint 0 error，不发版（APK 行为未变）。
> 完整取舍清单（含功能类建议：用户词频学习 / 候选过滤器流水线 / 自动纠错）见
> `docs/从 librime 可吸收的设计.md`（按项目规则 `docs/` 不入库）。

---

# 吸收 librime 的两项设计（内存映射索引 + 不物化字符串枚举）

日期：2026-09-17 ｜ 验证：`testDebugUnitTest`（224 用例）+ `lintDebug`（0 error）+ 真机实测

承接前述的取舍清单，落地其中真正值得吸收的两类。

## 一、P0-1 `Prism : MappedFile`，索引全部改为内存映射

- `PhraseIndex` 读取路径由 `ByteArray` 改为 `java.nio.ByteBuffer`（堆内 = `wrap`，映射 = `map`），
  两种承载共用同一套偏移/二分/解码逻辑；新增 `ofMapped(file)`，失败返回 null 交给调用方回退。
- 基础索引新增磁盘缓存：资产是 xz，必须先解压，因此改为「解压一次 → 原子落盘 → 映射」，
  缓存文件名用 APK 文件 mtime 做有效性键（App 一更新必变），旧版本缓存启动时清理。
  （这正是 librime 的做法：编译产物 `.prism.bin` 落盘，deployer 只在需要时重建。）
- 可选包缓存改走 `ofMapped`；写入改为「临时文件 + 原子改名」，否则覆盖正在被 mmap 的文件会
  截断映射页（Linux 上 SIGBUS）。顺带解决了此前"`.idx` 非原子写"的遗留风险。
- 真机实测（PACM00）：

| 指标 | 改前 | 改后 |
|---|---|---|
| 基础索引加载 | 1798ms（每次重新解压） | 48ms（内存映射，37×） |
| 两段式加载总计 | 2689ms | 949ms |
| 进程 PSS | 110~113MB | 77.7MB（−33MB） |
| Private_Dirty | ~100MB | 51.5MB（−49MB） |
| 磁盘 | 34MB | 48.8MB（+14.8MB 基础索引缓存） |

## 二、P0-2 `Prism::ExpandSearch`，前缀枚举不再物化字符串

- 新增 `PhraseIndex.forEachRangeWithPrefix(prefix) { keyByteLen, wordsFrom, wordsTo -> ... }`：
  只回调键的字节长度与词区区间，不建 String（对齐 librime `Match(value, length)`）。
- 新增 `collectLongerSuffixes(...)`：字节级判断词是否以目标词开头（`regionEquals`），
  未命中零分配，命中者只解码后缀。
- `predict` 改用它；扫描顺序严格保持旧 `keysStartingWith` 的「基础索引 → 可选索引 → 运行时表」，
  因此预测结果集合与顺序完全不变（由新测试对拍）。死代码 `keysStartingWith` 已删。

## 三、护栏
新增 `IndexMappedParityTest`（3 例）：① 内存映射与堆内读取逐键逐词表完全等价；
② 区间枚举 + 字节级后缀收集与旧的"物化键 + startsWith"写法结果一致；
③ 文件缺失/截断/magic 不符时 `ofMapped` 返回 null 而不抛异常。
用例总数 221 → 224。

> 说明：本次改了生产代码 → 已重建并覆盖发布（APK 5,184,338 字节 / sha256 `051953d7…`），两端校验一致。

---

# 用户词频学习（吸收 librime `UserDictionary`/`UserDb`）

日期：2026-09-17 ｜ 验证：`testDebugUnitTest`（234 用例）+ `lintDebug`（0 error / 29 warning）+ 真机 A/B

## 一、要解决的问题
词库候选顺序是构建期按通用词频排的，但每个人常用词不同：你天天打「虚拟」，它在通用词频里可能排到几十名开外。
本次记住「你实际选过的候选」并提到前面。

## 二、语义（对齐 librime）
| 环节 | 做法 | 出处 |
|---|---|---|
| 记什么 | 只在明确选择时学：点候选栏 / 空格取首候选 / 点预测词（预测学完整词，如「你好」+「吗」→「你好吗」）。`commitComposing`（收起键盘自动上屏）、语音结果、剪贴板粘贴都不学 | librime `UserDictionary::UpdateEntry` |
| 累加与衰减 | `dee_new = commits + dee_old * exp((tick_old - tick_now) / 200)`，tick 为天计数（`now/86400000`），半衰期 ≈ 139 天；当天重复选择不衰减 | `algo/dynamics.h` `formula_d` |
| 排序 | 稳定排序：权重降序，同权保持词库原序；未学习时直接返回原数组 | 对齐 librime 的用户词典与系统词典权值比较 |
| 有界性 | 权重 < 0.15 丢弃、上限 3000 条（超出淘汰最低） | 防文件与内存无限增长 |

## 三、实现要点
- 新文件 `UserFrequency.kt`：内存态 `ConcurrentHashMap`（查询线程读 / 学习线程写），
  存储 `filesDir/user_freq.txt`（`词<TAB>权重<TAB>天`），原子写（临时文件 + 改名，与索引缓存同一套）、
  防抖 2s、退出时 `PinyinEngine.flushUserFrequency()` 兜底。
- 加载挂在词库加载同一后台线程，并显式降优先级（不抢词库 CPU，符合项目既有规则）。
- 排序注入点：`PinyinEngine.query` 收尾（对最终候选列表做一次稳定排序）。
- 设置页新增「用户词频学习」开关（`Prefs.userLearning`，默认开、立即生效、本地存储不上传）。

## 四、真机 A/B 决定性验证
| 步骤 | 结果 |
|---|---|
| 预置 `拟好 9.000`（当天 tick） | 日志 `用户词频: 已载入 1 条（含按天衰减）` |
| 输入 `nihk`（自然码 → nihao） | 候选由基线 `[你好, 拟好, 你]` 变为 `[拟好, 你好, 你]` ✓ |
| 按空格上屏首候选 | 文件变为 `拟好 10.000` ✓（累加 + 原子落盘） |
| 清理 | 测试数据已删除，未污染真实状态 |

## 五、护栏
新增 `UserFrequencyTest`（9 例）：排序稳定性（未学习零变化 / 高权重提前 / 同权保序）、
累加、按天衰减（半衰期 139 天 + 阈值丢弃）、序列化往返、脏数据容错、开关关闭后不学不改序、原子写不留 `.tmp`。
用例总数 224 → 234，全绿；Lint 0 error / 29 warning（新增布局告警已即时修掉）。

---

# 近期功能改动的全面缺陷排查（5 个问题，全部修复）

日期：2026-09-17 ｜ 验证：`testDebugUnitTest`（236 用例）+ `lintDebug`（0 error / 29 warning）+ 真机复验

排查范围：用户词频学习（新增）、内存映射索引、区间枚举式预测、拖选状态机修复及其相互作用。

| # | 问题 | 位置 | 触发条件 | 后果 | 依据与处置 |
|---|---|---|---|---|---|
| 1 | `file` 字段无 `@Volatile` | `UserFrequency.kt` | 加载线程在 `load()` 里赋值、主线程在 `remember()/flush()` 里读 | 主线程可能一直看到 `null` ⇒ 首批学习既不触发防抖落盘、退出时 `flush` 也直接 return，"学了但重启就没了"，且完全无声 | 相邻的 `enabled`/`dirty` 都有 `@Volatile`，唯独漏了它；已补 ✓ |
| 2 | 累加时未对"未来 day"钳位 | `UserFrequency.remember` | 用户把系统时间往回拨（`old.day > today`） | `exp(正数)` 放大权重（回拨 2000 天 ≈ 22 万倍），该词此后永久霸榜 | `parse()` 里本来就有 `.coerceAtMost(0)` 钳位，`remember()` 漏了，同一语义两处不一致；已对齐 ✓ + 新增回归用例 |
| 3 | `flush()` 走 BackgroundIo 异步 | `UserFrequency.flush` | `onDestroy` 后进程随即被系统回收 | 最后一次学习白记（安静丢失） | 该方法只在"一次性收尾"时调用，此刻异步最危险；改为同步写（文件 ≤3000 行、主线程 1~3ms 可接受）✓ |
| 4 | 学习词未做行格式防御 | `UserFrequency.remember` | 候选里出现制表符/换行（正常词库不会，属防御） | 写坏一行 ⇒ 下次 `parse` 整行跳过，静默丢失 | 与索引缓存的"原子写 + 容错解析"同一思路，入口直接拒收 ✓ + 新增用例 |
| 5 | 加载时序回归（本次新功能引入） | `PinyinEngine.load` | 每次冷启动 | `UserFrequency.load` 原本排在高频子集之前，那几毫秒文件 IO 直接计入"用户马上要打字"的延迟预算（实测 254~332ms → 337ms 的抖动即由此而来） | 已移到高频子集就绪之后 ✓：排序在查询期生效，第一键之前必定就绪，而首字延迟预算不被占用 ✓ |

## 真机复验（修复后）

```
高频子库就绪（可开始输入）: 词语数=34515 耗时=280ms        ← 首字可用
用户词频: 暂无历史（首次使用）                            ← 学习加载排在其后，不再占用首字预算
基础索引: 已解压并写入磁盘缓存（604077 键 / 14767KB / 1834ms）  ← 因本次重装 APK，缓存键变化 → 按设计重建
两段式加载完成（高频子集 280ms + 索引 1835ms，总计 2716ms）
```
> 注：重装 APK 会改变 `base.<APK mtime>.idx` 的缓存名，因此首次启动会重建一次；日常使用（不重装）走内存映射路径 = 48ms（上一轮实测）。

## 核实为「无缺陷」的相邻项
- 搜索面板隔离：`onCandidateSelected` 的搜索分支早于 `rememberChoice` 返回 ⇒ 在剪贴板搜索框里选词不会污染学习记录 ✓。
- 候选缓存交互：排序发生在 `PinyinEngine.query` 收尾（而 `mergedCache` 缓存的是未排序词表）⇒ 权重变化后无需失效缓存即可立刻生效，也不会读到过期顺序 ✓。
- `rank` 的稳定排序：Kotlin `sortedByDescending` 为稳定排序，同权保持词库原序 ✓（有专门用例）；未学习时直接返回原数组 ⇒ 零开销、零行为变化 ✓。
- `parse` 脏数据容错：坏权重/坏天数/负权重/字段不足/空行/表头全部跳过 ✓（有用例）。
- 开关语义：关闭后不学也不改排序，既有记录保留、重开即恢复 ✓；设置页开关立即生效 ✓。
- 文件与内存有界：上限 3000 条（超出淘汰最低）、权重 <0.15 丢弃 ⇒ 文件恒定几十 KB ✓。
- 原子写：与索引缓存同一套「临时文件 + 改名」，不留 `.tmp` ✓（有用例）。

## 已登记的已知边界（非缺陷）
- 学习只能在同一拼音键的候选列表内提升名次（`MAX_PHRASES=12` 截断之外提不上来），与 librime 结构一致。
- 预测行不参与用户词频排序（预测项是"后缀扩展"而非完整候选）；若将来要参与，应把学习键改为「基词 + 扩展」的组合。

---

# 索引缓存生命周期缺陷（内存映射在真实使用中失效）+ 修复

日期：2026-09-17 ｜ 验证：`testDebugUnitTest`（239 用例）+ `lintDebug`（0 error / 29 warning）+ 真机全流程

## 一、缺陷 D1（严重）：可选包加载会把基础索引缓存删掉

- 症状：真机 `files/index/` 里只有两个可选包缓存，`base.<mtime>.idx` 凭空消失；日志却说"已解压并写入磁盘缓存"。
- 根因：`loadExtensionDict` 里那道"清理源包已删除的索引缓存"的扫地逻辑，判据是
  「文件名去掉 `.idx` 后是否还在词库包列表里」。基础索引缓存叫 `base.<APK mtime>.idx`，
  它的"源"是 APK 内资产、永远不在包列表里 ⇒ 被当成失效缓存删掉。
- 日志实锤（三次独立发生）：
  ```
  22:08:40 清理失效索引缓存: base.1789653922000.idx
  22:29:03 清理失效索引缓存: base.1789654977000.idx
  22:36:30 清理失效索引缓存: base.1789655607000.idx
  ```
- 后果：可选包在息屏 / 键盘闲置 / 兜底超时时加载（即日常几乎每次都会触发），
  基础缓存随即被删 ⇒ 内存映射永远命中不了、每次冷启动都退回 1.8s 解压。
  上一轮报的"48ms"是真实的，但只存在于"启动后、下次可选包加载前"这个窗口，真实使用中基本不成立。
- 处置：把判据抽成纯函数 `PinyinEngine.staleOptionalCacheNames()`，显式排除 `base.` 前缀，
  并加回归测试 `IndexCacheLifecycleTest`（3 例）钉住。

真机全流程验证（修复后）
```
① 启动     → base.1789658229000.idx (15121760 字节) 写入 ✓
② 息屏     → 可选词库: 开始后台加载（触发: 息屏）→ 基础缓存【存活】✓（无"清理失效索引缓存"）
③ 重启     → 基础索引: 复用磁盘缓存并内存映射 → 索引 48ms / 两段式 924ms ✓✓
```

## 二、缺陷 D2（可观测性）：写缓存失败被静默吞掉，日志还反着说

- `writeIndexCacheAtomically` 有两处 `return false` 没有日志（删旧文件失败 / rename 失败）；
  调用方又无条件打「基础索引: 已解压并写入磁盘缓存」。
- 后果：缓存写不进去时用户与开发者都看不到任何异常，本次排查就被这条日志误导过。
- 处置：两处失败都打 W 日志；调用方按 `written && mapped != null` 区分，
  失败时明确打「磁盘缓存不可用（written=false），本次使用堆内索引」。

## 三、同轮核实（无缺陷）

- 拖选状态机复位确实生效：`clearSelectionState()` 位于 `flushPendingPaste()` 首行，
  而后者由 `onStartInputView` 无条件调用 ⇒ 每次输入会话切换都会复位 anchor/focus ✓。
- `candidatePinyin`（候选→键）不会因词库变更而过期：映射是「词→拼音」，与可选包内容无关；
  跨夹具的测试场景由 `resetForTest` 清理 ✓。
- 区间枚举预测的集合与顺序与旧写法一致（`IndexMappedParityTest` 用 `LinkedHashSet` 比较，
  对顺序敏感）✓；`collectLongerSuffixes` 只解码命中的后缀，等长/更短一律跳过 ✓。
- 索引缓存的原子写（临时文件 + 改名）使"覆盖正在被 mmap 的文件"不会截断映射 ✓。

---

# 近期改动验证矩阵（含 1 处用户文案过期缺陷）

日期：2026-09-17 ｜ 验证：`testDebugUnitTest`（239 用例）+ `lintDebug`（0 error / 29 warning）+ 真机逐点实测

## 一、验证矩阵（步骤 / 输入 / 预期 / 实测 / 是否一致）

| # | 操作步骤 | 输入条件 | 预期行为 | 实际结果 | 一致 |
|---|---|---|---|---|---|
| 1 | 全新安装后冷启动（用户已卸载过） | 空的 data 目录 | 高频子集先就绪可打字；基础索引解压并落盘缓存；无崩溃 | 高频子库 442ms → 基础索引走 mmap 97ms → 两段式 1481ms；`index/` 只有 `base.*.idx`；0 崩溃快照 / 0 FATAL | ✓ |
| 2 | 预置 `拟好 9.000` 后重启，输入 `nihk` | 学习记录 1 条 | 该词被提到前面，且在内存映射索引上同样生效 | `用户词频: 已载入 1 条` + `复用磁盘缓存并内存映射`；候选 `[拟好, 你好, 你]`（原 `[你好, 拟好, 你]`）；中间态 `nih` 也提前 | ✓ |
| 3 | 分类词库页点「下载」（长词包 1.8MB） | 全新安装、未安装任何包 | 下载成功 → 落盘 `dicts/ext.xz` → 提示重启 IME | `分类词库下载成功: ext.xz (1897940 B)`（Gitee 源）+ 文件存在 + `1.5s 后重启输入法进程` | ✓ |
| 4 | 制造失效缓存 `gone.xz.idx`，息屏触发可选包加载 | 同时存在合法基础缓存与 1 个失效可选缓存 | 只清失效的；构建 `ext.xz.idx`；基础缓存必须存活 | `清理失效索引缓存: gone.xz.idx`；`已构建词库包索引: ext.xz（184996 键 / 1764ms）`；`base.*.idx` 尺寸/mtime 未变 | ✓ |
| 5 | 词库页文案核对 | 打开分类词库页 | 文案应描述当前实现 | 发现过期文案（见下节），已修正 | ✗→已修 |

## 二、发现的问题

F1（用户文案过期，严重程度：低 / 影响认知）
- 位置：`strings.xml` 的 `dict_startup_cost_line1~3` + `OptionalDicts.startupSec`
- 原文案：「开机后首次使用输入法时，候选就绪需等待约 7 秒。之后不影响日常输入。」（腾讯包写 19 秒）
- 触发条件：任何用户打开「分类词库」页都会看到
- 与现状不符之处：① 现在加载只在空闲时（息屏 / 键盘闲置 20s / 兜底 180s），不影响打字；
  ② 首次是「构建索引」而非「候选就绪」；③ 数值早就不对（实测首次构建 ext 1.76s、腾讯 6.9s）；
  ④ 之后启动是索引缓存内存映射复用（实测约 0.05s），原文案完全没提。
- 判定依据：`PinyinEngine.loadOptionalAsync` / `maybeLoadOptionalDict` 的三信号实现 + 第 4 点的实测数字。
- 处置：文案改为「本包在空闲时才后台加载（息屏 / 键盘闲置），首次构建索引约需 N 秒，期间不影响打字。
  之后启动直接复用索引缓存（约 0.05 秒）。」，`startupSec` 按实测改为 2 / 7，并更新 `OptionalDicts` 的标准注释。

## 三、本次未被独立验证 / 存疑的功能点（明确列出）
1. 腾讯大词库（6.36MB）的下载与首次构建：与已验证的 ext 包走完全相同的代码路径（同一 `download()`、
   同一 `loadOptionalIndex()`），本次未再跑一遍；其 `startupSec` 取自更早的实测（6927ms）。
2. 设置页「用户词频学习」开关的真机点击：单测已覆盖开关语义（关闭后不学、不改排序），
   但开关控件本身的点击与立即生效未在真机验证（页面控件坐标需现场定位）。
3. 拖选状态机修复（`clearSelectionState`）：代码级已确认 `onStartInputView` 无条件调用；
   真机复现"锚点/焦点跨字段残留"需要构造特定中断序列，本次未做。
4. 剪贴板链路在全新安装下的表现：代码与单测覆盖，但本次未构造真实的"复制→入库→面板→粘贴"序列。
5. 语音链路：按项目必须做到只审不改，且依赖自建服务端在线，本次未做任何验证。

---

# 用户可见文案全量审计（纯源码核对，改 6 处）

日期：2026-09-17 ｜ 方法：从 `strings.xml` + 各页对 `R.string` 的引用按源码逐条核对（不看截图）
验证：`testDebugUnitTest`（239 用例）+ `lintDebug`（0 error / 29 warning）

## 一、首页（设置页，共引用 69 个字符串）

| 字符串 | 原文案 | 问题 | 现文案 |
|---|---|---|---|
| `settings_subtitle` | 语音识别 + 26键全拼 + 26键双拼(自然码) | 双拼已是 7 套方案，只写自然码是旧描述 | 语音识别 + 26 键全拼 + 7 套双拼方案 |
| `settings_usage` | …26 键全拼 / 自然码双拼，空格选词 | 同上 | …26 键全拼 / 双拼（7 套方案），空格选词 |
| `switch_show_rare_chars_desc` | 默认关闭,不加载生僻字.勾选后重启加载生僻字 | 索引化之后是查询期过滤（词库与索引不裁剪），"不加载"不成立 | 默认关闭，候选里不出现生僻字（过滤在查询期做，词库与索引不裁剪）。勾选后重启输入法生效 |
| `rare_chars_on` | 已开启：重启输入法后将加载生僻字 | 同上 | 已开启：重启输入法后候选会包含生僻字 |
| `rare_chars_off` | 已关闭：重启输入法后将不再加载生僻字（更省内存） | 同上；且"更省内存"已不成立（索引大小与开关无关） | 已关闭：重启输入法后候选不含生僻字 |
| `settings_clipboard_desc` | 在键盘的候选栏点击「剪贴板」可查看历史 | 面板确实渲染在候选栏（无输入时）✓，但按钮标签是「历史」，"剪贴板"只是其下小字提示 | 在键盘候选栏（未输入任何拼音时）点「历史」即可查看 |

> 说明：`settings_clipboard_desc` 最初我怀疑"候选栏"这个位置说法也错了，读源码后否掉了，
> `refreshCandidateBar()` 在"无候选、无拼音串、无预测"时 `renderFunctionPanel()`，面板就在候选栏里渲染。
> 真正错的只是按钮名。（这正是"看源码而非看截图"的价值：截图看不出渲染时机。）

## 二、分类词库页 + 检查更新（共 28 个字符串）
- 改 1 处：`dict_manager_hint_line2` 原写「下载后需重启输入法生效」，实际是自动重启
  （`DictManagerActivity` 下载完成后 `restartImeForDict()`，日志 `分类词库变更：1.5s 后重启输入法进程以加载`），
  → 改为「下载后会自动重启输入法以生效」。
- 数字类声明逐条核对为准确 ✓（用真实 release 资产实测）：
  · 长词包 `sizeMb = 1.81` ↔ 实际 `dict_ext.txt.xz` = 1,897,940 B ✓
  · 腾讯包 `sizeMb = 6.36` ↔ 实际 `opt_tencent.txt.xz` = 6,668,612 B ✓
  · 腾讯包描述「约 97 万词条」 ↔ 实测 971,257 词条 / 955,032 键 ✓
- 其余（下载/删除/状态/失败提示、`dict_startup_cost_line1~3`、检查更新五态文案）均与实现一致 ✓
  （`dict_startup_cost_*` 已在上两轮按实测校正为"空闲加载、首次建索引、之后复用缓存"）。

---

# 文档同步到最新内容 + 发布 v20260918

日期：2026-09-18 ｜ 验证：`testDebugUnitTest`（239 用例）+ `lintDebug`（0 error / 29 warning）+ 双端下载校验

## 一、文档同步（把当天所有改动落到文档）
| 文档 | 更新内容 |
|---|---|
| `AGENTS.md` | ① 修正过期性能数字：日志耗时段（基础包 6.4s → 高频子集 ~0.3s 可打字 / 全量索引日常 0.05s 映射复用、首次 1.8s）；索引段数字（加载 6~10.7s→2.9s / PSS 90MB → 0.05s / 77.7MB）。② 补入内存映射索引的设计与踩坑记录：读取一律 `ByteBuffer`、基础索引「解压一次→原子落盘→映射」、缓存写入必须临时文件+改名、以及「`index/` 下两类缓存的清扫判据必须分开（`base.` 前缀绝不能被可选包清扫删掉）」+ 指向 `IndexCacheLifecycleTest`。 |
| `README.md` | 首行与输入方式改为双拼（7 套方案）；分类词库改为「空闲时后台加载、不影响打字、首次建索引后复用约 0.05s、增删后自动重启」；新增「用户词频学习」特性；APK 体积 4.5MB → 4.9MB；词库清单补「映射复用」说明。 |
| `README_EN.md` | 同步首行与输入方式（7 schemes）。 |
| `tools/dict_builder/README.md` | 真机验证标准更新（高频子集先行 + 映射复用）。 |
| `docs/词库持久化索引-设计与分期.md` | 补「最终状态（2026-09-18 定稿）」：最终实测对照表 + 两条吸收自 librime 的设计 + `index/` 两类缓存的教训。 |

## 二、发布 v20260918（新日期 → 新版本号）
- 重新编译：`versionCode = 20260918`（`aapt dump badging` 实测）、`versionName = jinn`；
  APK 5,188,434 字节 / sha256 `f3337cd6…`。
- 两端新建 Release `v20260918`（不覆盖 v20260917，保留历史）：
  · GitHub：`gh release create v20260918` + 附件；
  · Gitee：`POST /releases`（id 1150500）+ 手写 multipart 传附件。
- 校验：两端 `.../download/v20260918/jinn-release.apk` 的 sha256 与本地完全一致 ✓
  （检查更新按 tag 判断，新 tag 已就位 → 装在旧版的设备可正常识别到新版本）。
- 代码同步：`main` / `perf/paging-pinyin-rematch` / Gitee `master` 三处快进到同一提交 ✓。

---

# 文档与注释措辞平实化（不改功能代码）

日期：2026-09-18 ｜ 验证：`testDebugUnitTest`（239 用例）+ `lintDebug`（0 error / 29 warning）

对齐的是本项目原本的写法：具体动词、真实数据、只给关键规则加粗；不堆 emoji、不写"写给评审看"的元话语、
不用 ①②③ 排句子。改动如下。

## 一、用户可见文案
| 位置 | 改前 | 改后 |
|---|---|---|
| 生僻字开关说明 | 默认关闭，候选里不出现生僻字（过滤在查询期做，词库与索引不裁剪）。勾选后重启输入法生效 | 默认关闭。勾选后重启输入法，候选里才会出现生僻字 |
| 用户词频说明 | 记住你实际选过的词并排到前面；只存在本机，不上传。可随时关闭（关闭后既有记录不再参与排序） | 记住你选过的词，下次排到前面。只存在本机、不上传；关掉后不再参与排序 |
| 词库加载代价 1~3 行 | 本包在空闲时才后台加载（息屏 / 键盘闲置），／首次构建索引约需 %d 秒，期间不影响打字。／之后启动直接复用索引缓存（约 0.05 秒）。 | 空闲时才在后台加载（息屏或收起键盘后），／第一次约 %d 秒，期间照样能打字。／之后启动几乎瞬间就绪。 |

改的原则：用户看到的句子里不出现"构建索引""复用索引缓存"这类内部实现词。

## 二、源码注释
- 圈号改回项目一直用的「 - 」列举：`PinyinEngine` 的 `load()` 装载说明、`candidatePinyin`、
  `loadOptionalIndex` 的三步；`JinnIme` 的空闲触发信号；`gen_shuangpin_tables.py` 的人工裁决说明。
- 删掉元话语：分词 KDoc 里"（2026-09-17 复核，原 KDoc 描述与代码不符，已改正）"这种写给评审看的话，
  直接说清代码在做什么。
- 顺带把几处 `` 收回单个，项目里一直是单 ``。

## 三、没有改的地方（避免为了改而改）
- `更新日志.md`：本项目的日志一直用 `## 一、` + 表格 + 加粗，第 30~35 轮就有 4 处 `` 等符号；
  我这几轮的用法与之一致，不动。
- `AGENTS.md` 里 `必须是并发容器` 那类：项目原本就用 `` + 加粗来标关键约束，保留。

---

# 源码注释全面审读，去除模板化措辞（只动注释）

日期：2026-09-18 ｜ 验证：`testDebugUnitTest`（239 用例）+ `lintDebug`（0 error / 29 warning）

做法是先机械扫特征、再逐处改，改完复查清零。共 12 处、10 个文件（含 4 个此前没碰过的测试文件）。

## 改掉的
- 设问式开头「`* 为什么 xxx：`」→ 去掉设问词，理由原样留着。涉及 `JinnIme`（不用固定延迟）、
  `KeyboardLayouts`（单独成文件）、`PinyinEngine`（用位图 / 单独一个测试入口）、
  `ClipboardClassifierBoundaryTest`、`OptionalDictMergeTest`。
- 文档式标签「`* 背景：`」→ 直接去掉，句子照读。涉及 `Diagnostics`、`CandidateCountBoundTest`、
  `PhraseDictIntegrityTest`。
- KDoc 里的下划线小标题：`PhraseIndex` 的格式段与读取承载段各删掉一条 `-----` 分隔线，标题文字保留。
- 理由列表：`ClipboardDb` 的「① 消除重复 ② 纯函数」改成顺口的两句。

## 判为误报、不动的
- `KeyboardLayouts` 里的 emoji 是表情键盘的数据，不是装饰。
- `ClipboardDb` 裁剪优先级里的 ①②③ 是真实的先后顺序，保留。
- 「不拦截本次复制」「放弃本次听写」是正常中文，不是元话语。

## 复查
按设问、文档标签、下划线、套话（值得注意的是/本质上/综上所述…）、元话语（本次仅/本次…）五类模式
复扫，命中数均为 0。

---

# 全部源码注释再扫一遍（补 4 处，只动注释）

日期：2026-09-18 ｜ 验证：`testDebugUnitTest`（239 用例）+ `lintDebug`（0 error / 29 warning）

这轮把 67 个源文件全部过了一遍，改的是上一轮正则漏掉的两类：加粗里的设问（`为什么与基础包分开：`，
行首正则匹配不到）和句中的文档标签（`原因：…`）。共 4 处：

| 文件 | 改前 | 改后 |
|---|---|---|
| `PinyinEngine` | `为什么与基础包分开：可选包可达 97 万词条…` | `与基础包分开加载：可选包可达 97 万词条…` |
| `PinyinEngine` | `原因：可选词库由后台线程延迟加载…` | `可选词库由后台线程延迟加载…` |
| `build_dict_index.py` | `# 原因：fixture 里若没有…` | `# fixture 里若没有…` |
| `PhraseIndex` | 上一轮删分隔线时留下一个悬空的"读取路径支持两种承载"标题，后面又重起一句 | 两句合成一句，该段 10 行压到 4 行 |

## 扫过但判定不用改的
- `注意：` 开头共 12 处：`TextSelection.kt`（项目原有文件）就是这么写的，属于本项目的写法，保留。
- `PinyinKeyboardView` 的"重建大写键背景"：这里的背景指按键底色，不是文档标签。
- `PinyinEngine` 的"「加载后过滤」更省内存的原因：…"：是一句正常的话，不是小标题。
- 加粗标记不成对：全部 67 个文件扫下来是 0 处。
- 套话（值得注意的是 / 换言之 / 综上所述…）与括号堆叠（一行 3 个以上括号）：均为 0 处。

## 覆盖范围说明
67 个文件都做了形态扫描（设问、文档标签、下划线小标题、套话、元话语、加粗失衡、括号堆叠），
注释块最厚的几个文件（`PinyinEngine`、`JinnIme`、`PinyinKeyboardView`、`ClipboardPanelView`、`PhraseIndex`）
逐块读过。没有做的是：67 个文件每一行注释逐个通读，形态扫描抓不到的个别措辞，只能靠以后再看到时顺手改。

---

# 全身审注释前述（按模块逐块读，改 8 处）

日期：2026-09-18 ｜ 验证：`testDebugUnitTest`（239 用例）+ `lintDebug`（0 error / 29 warning）

这轮换了读法：不再只靠正则，而是把每个文件里 >=2 行的注释块全部导出、按模块分批逐块读
（剪贴板 8 文件 43 块、语音与基础设施 9 文件 40 块、键盘两文件 107 块，其余 48 文件做模式过滤）。

## 一、删掉引用仓库外文档的章节号（7 处）
注释里写「（文档第十五 / 十六节）」「（方案第二 + 第四阶段核心）」「（方案第三阶段要求）」这类，
读者手上没有那些文档，等于没写。涉及 `ClipboardClassifier`（2 处）、`ClipboardController`（2 处）、
`ClipboardCrypto`、`ClipboardFirewall`、`ClipboardPrefs`。
`PinyinEngine` 里「详见 `docs/从 librime 可吸收的设计.md` 第二节」保留，那指向本机真实存在的文件。

## 二、修正过期注释（1 处）
`Prefs.showRareChars` 的说明仍写「关闭时不加载生僻字…直接降低词库常驻占用」，
实际早已改为查询期过滤（运行时日志就是「生僻字=隐藏(查询期过滤)」），词库与索引原样加载。
上次改了设置页文案，源码注释这处漏了，这轮补上。

## 三、读过但判定不用改的
- 「一行就是一句话」（`DictManagerActivity` / `OptionalDicts`）：描述的是页面渲染规则，正常。
- 「职责：」「注意：」开头：项目原有写法（见 `AGENTS.md` 的代码风格规则）。
- 剪贴板控制器「此前只有 copySelection 那条路径检查了 enabled…」、`TextSelection`「早前这里直接返回 0…」：
  这类"过去的坑"是真历史，保留。

## 四、覆盖范围
67 个源文件全部纳入本次：61 个文件按块逐块读过（含全部主源码与键盘/引擎），其余 48 个（测试与
构建脚本）做模式过滤（设问、文档标签、套话、章节引用）。仍未做的是逐字通读每个文件的每一行注释。

---

# 注释过期审查（机器可判的三类核查，改 19 处）

日期：2026-09-18 ｜ 验证：`testDebugUnitTest`（239 用例）+ `lintDebug`（0 error / 29 warning）

这轮不查文风，专查过期。做法是把注释里可机械核对的东西抽出来逐条验：
引用的符号是否存在、提到的功能是否还在、指的文件是否真的在。

## 一、已移除功能的残留词汇（17 处，主要问题）
隐私功能在 `ClipboardDb` 的 v5 迁移（2026-09-16）已整体移除，建表去掉了 `is_private` 列，
`TRIM_PRIORITY` 现在只剩一条 `is_favorite = 0`，`PSEUDO_PRIVATE` 常量也不存在了。
但一批注释还在按"隐私存在"写：

| 位置 | 原文 | 现文 |
|---|---|---|
| `ClipboardDb` 类头（3 处） | 元数据含"隐私标记"；"收藏永不删；隐私最后才删"；裁剪优先级"非收藏非隐私 → 非收藏隐私 → 收藏永不删" | 只提收藏标记；"只动非收藏记录，收藏永不删" |
| `ClipboardDb.trimTo` KDoc + 行内注释 | ①非收藏且非隐私 ②非收藏的隐私 ③收藏永不删除；"先最不敏感的，隐私留到最后" | 只取非收藏的最旧记录；"先删最旧的非收藏记录" |
| `ClipboardDb` 过滤/常量区（4 处） | "仅收藏 / 仅隐私"、"FAVORITE、PRIVATE 是伪分类"、TRIM_PRIORITY 里"隐私排在最后" | 只提收藏；伪分类只留 FAVORITE |
| `ClipboardPanelView`（3 处）+ `SearchPanelView`（1 处） | "隐私条目仍默认掩码，点击一次展开、再点才粘贴" | 代码里已无掩码逻辑，改为说明该结构只剩历史意义 |
| `ClipboardClassifier` | "隐私分类绝不自动判断（只能用户主动标记）" | "只做 URL / NUMBER / OTHER 三类，其余一律 OTHER" |

保留的是迁移历史那几条（v3 去掉 `is_sensitive`/`expire_at`、v4 去重、v5 去 `is_private`），
它们记录的是"当时为什么这么改"，是准确的，不该删。

## 二、数据源列表过期（1 处）
`PinyinEngine` 类头的数据源仍列 `pinyin_phrases.txt`（文本格式）。全量词库早已换成
`pinyin_index.bin.xz`（二进制索引），列表里反而没有它，也没有高频子集。按现在的
`app/src/main/assets/` 实际内容重写。

## 三、文档数字过期（1 处）
`AGENTS.md` 写"20 个测试类 / 221 个用例"，实际是 23 个测试类 / 239 个用例。
（第一次我随手填了"30 个"，随后数了一遍文件与类声明，改正为 23，写文档里的数字必须数过再写。）

## 四、核查过但没问题的
- 注释里引用的符号：扫出 12 处"疑似不存在"，逐条看后全是误报，`addPrimaryClipChangedListener`、
  `requestShowSelf`（Android API）、`coerceIn`、`drawRoundRect`（标准库）、以及 `[lastWord]`、`[minSize]`
  这类局部参数名。项目里没有坏掉的符号引用。
- 注释/文档提到的路径：28 处"找不到"，逐条看后只有上面那 2 处是真的，
  其余是 assets 路径拼接、测试资源、运行时路径（`filesDir/...`）、README 里带省略号的示意路径。

---

# 术语统一 + 文档事实核对（改 21 处，只动注释与文档）

日期：2026-09-18 ｜ 验证：`testDebugUnitTest`（239 用例）+ `lintDebug`（0 error / 29 warning）

这轮补的是之前没查过的两个维度：术语是否统一、文档里的事实是否还成立。

## 一、术语统一（19 处）
| 游移的两种叫法 | 处置 |
|---|---|
| 「剪贴板页」6 处 | → 剪贴板面板（它就是键盘里嵌的那个面板，项目里没有单独的"页"） |
| 「候选项」4 处 | → 候选（同一个东西） |
| 「写盘」3 处 | → 落盘 |
| 「mmap」2 处 | → 内存映射 |
| 「输入方案」5 处 | → 双拼方案（与设置页下拉的实际标签对齐；界面字符串不动） |
| 「长词包」/「扩展包」 | 同一个包（dict_ext）两种叫法，在 `OptionalDicts` 定义处加一句对照说明，其余不动，界面本来就叫「长词包」，代码与文档里习惯写「扩展包」 |

没动的：「子集 / 高频子集」（后者是前者的完整写法，上下文里不歧义）、
「拼音 / 拼音串」（含义不同：前者指某个词的拼音，后者指用户敲的那串）、
「单字 / 单字表」、「缓存 / 索引缓存」（都是含义确有区别的词）。

## 二、段落级重复：只有 2 条，且都该留
- 三个索引测试里的「收尾复位：PinyinEngine 是单例…」×3：每个测试文件都要能独立跑，这类自包含说明重复是必要的。
- `TextSelectionTest` 里同一段「行结构：…」×3：分别对应三个用例的输入布局，各说各的。

## 三、文档事实核对（2 处过期）
把 4 份 md 里的数字、命令、文件名逐条对着仓库验了一遍（实测标准：assets 5 个 / 主源码 29 个 /
测试类 23 个 / 用例 239 个）。查出两处：
- `README_EN.md` 的 APK 体积还写 4.5MB，中文 README 上次改成 4.9MB 时漏了英文这份；
- `AGENTS.md` 高频子集耗时写 0.25~0.31s，今天的真机数据里有全新安装后首次启动的 442ms，
  区间改为 0.28~0.44s 并注明原因。

其余声明（60.4 万键、4.48MB/14.4MB、可选包 8.7s→44ms、7 套双拼、圆角 0~24dp 等）与实际一致。

---

# 打字时按键响应专项取证（无卡死/无 ANR，1 处受限卡顿窗口）

日期：2026-09-18 ｜ 方法：静态扫描定位嫌疑 → 真机分层取证 → 定性。本次只取证，未改代码。

## 一、真机实测数据（PACM00，IME 进程）

| 指标 | 实测 | 判定 |
|---|---|---|
| 逐键渲染耗时 | 50th 7ms / 90th 12ms / 99th 48ms | 50/90 分位都在 16.6ms 帧预算内 ✓ |
| 掉帧 | 5 帧 janky（占 6.94%），单次最多 48ms | 属"小卡顿"，非卡死 ✓ |
| 高输入延迟帧 | 16 / 72 | 有优化空间，但未造成可感卡顿 ✓ |
| ANR / Davey | 0 / 0 | 无卡死、无无响应 ✓ |
| 按键往返（`input tap` 往返含 adb 开销） | 20 键全在 450~532ms，无尖峰 | 输入路径无阻塞 ✓ |
| 掉帧累计 | 4 次事件 / 5 帧 / ≈83ms | 可忽略 ✓ |

按键确实落到应用（候选日志 `候选: 双拼[ui]→shi → [是, 时, 事]` 逐键出现），
说明这轮测的是真实输入路径，不是空点。

## 二、排查过程中的两个"假 BUG"（记录下来避免以后重踩）
1. 第一次测出"键盘不弹、渲染 0 帧"，实际是屏幕处于息屏（`mWakefulness=Asleep`），
   所有点击落在黑屏上，`uiautomator` 也拿不到层级。叫醒后一切正常。
2. uiautomator 抓不到键盘按键文本，`PinyinKey` 是 Canvas 自绘控件，节点里没有文本，
   靠它取坐标会一直"抓不到"。键盘内的坐标只能用代码里的布局几何推算。

## 三、静态扫描的结论（含被证伪的怀疑）
- 曾怀疑"每按一键写一次日志文件"（`Diagnostics` 每条日志都 open→write→close）：
  查证后不成立，候选日志走的是 `Diagnostics.v`，而 V 级默认不落盘，只进 logcat。
  落在文件里的是空格上屏、面板开关这类非逐键动作，每次 1~2ms，可接受。
- 剪贴板面板 / 搜索面板的 DB 查询与解密：全部走 `BackgroundIo`（单线程队列），
  不在主线程；而且打字路径不依赖 BackgroundIo，面板在后台忙也不会挡住按键。
- 索引首次触达缺页：内存映射的索引在首次查询新键时会缺页，属一次性成本，
  5 帧 janky 里有它一份，量级在几十毫秒内。

## 四、唯一值得登记的受限卡顿窗口
开机后第一次把键盘切到「尚未预热」的那套双拼方案时，可能在主线程同步构建该方案键位表。
依据：`ShuangpinSchemes` 里 7 套表都是 `lazy { ShuangpinTable(...) }`，
而 `ShuangpinTable` 的构建按项目自己的注释是"几十毫秒量级"。
缓解：`JinnIme` 里有独立守护线程 `warmUpAll` 预热全部 7 套，所以窗口只在进程刚启动的约 1 秒内。
处置：不改代码（预热已覆盖绝大多数情况；若改成"首次弹出时同步建表"反而会把成本挪到弹键盘那一刻，更糟）。

---

# 修复「拼音打错/打太长时字母键卡住」（用户报告，已定位到根因）

日期：2026-09-18 ｜ 验证：`testDebugUnitTest`（241 用例）+ 真机 + JVM 实测

## 一、用户报告
> 26 个字母按键在某些情况下会卡住，可能是打错拼音了、打得太长了，总之出现在拼音打不对的情况下。

## 二、根因（读代码定位 + 实测确认）
`PinyinEngine.dfsSegment` 的「16 条完整路径」上限只对"找得到完整切分"的输入生效：

```kotlin
if (out.size >= MAX_SEGMENT_PATHS) return      // 只数已完成的路径
...
if (validSyllables.contains(candidate)) { dfsSegment(...) }   // 不判断剩余后缀还有没有救
```

拼音切不通时（打错、尾巴有问题），一条完整路径都找不到，`out` 永远是空的、上限永不触发，
DFS 会把所有前缀分支走到底。而前缀的合法切分是组合爆炸的：

| 输入长度（`banana…z` 这类：前缀能切、尾巴切不通） | 修复前耗时（JVM） |
|---|---|
| 13 字符 | 0.72 ms |
| 19 字符 | 1.48 ms |
| 25 字符 | 8.61 ms |
| 31 字符 | 36.80 ms |

每加 6 个字符约 ×4~5，真机上还要再乘 3~5 倍，30 多字符就是每按一键卡几百毫秒，
更长即秒级。这与用户描述的「打错/打太长就卡住」完全吻合。

## 三、修法（结果不变，只削代价）
新增 `suffixParseable()`：自后向前一趟 DP，算出「后缀能不能切通」；
`dfsSegment` 在进入分支之前就用它剪枝。走不通的分支本来就到不了终点，
所以切分结果与修复前完全一致（候选数 110/369 未变），代价从指数压回 O(n·6)。

| 用例 | 修复前 | 修复后 |
|---|---|---|
| 25 字符切不通 | 8.61 ms | 0.73 ms |
| 31 字符切不通 | 36.80 ms | 0.43 ms（85×） |
| 36 字符真实打错串 | 0.70 ms | 0.70 ms（候选仍 369 个） |

## 四、护栏
新增 `SegmentCliffTest`（2 例）：① 43 字符切不通的串，单次 `query` 必须远低于 200ms
（修复前是秒级，阈值取得很宽松，只守"别再退回指数级"）；② 剪枝不改变结果
（`zhongguorenmin` 仍召回「中国人民」、`nihao` 仍召回「你好」、长可切词仍有候选）。
用例数 239 → 241。

## 五、顺带记录一个编译器坑
KDoc 里写下标形式 `` `input[i..]` `` 会被 K2 当成 KDoc 链接语法，报
`Closing bracket expected`（指向注释行、看不出是注释的问题）。改成文字描述即可。

---

# 26 键打字模拟测试（复现并修复「冷启动窗口内打字卡顿」）

日期：2026-09-18 ｜ 方法：真机按键模拟电池 + 分层取证。用户反馈"暂时找不到问题"，于是做系统性模拟。

## 一、先测稳态：干净
按真实用键模式敲 79 次（长词、用户给的错串、同键连打 12 次、退格重打、全 26 键各一遍、空格回车）：

| 指标 | 结果 |
|---|---|
| 帧耗时 | p50 7ms / p90 10ms / p99 14ms（全在 16.6ms 预算内）|
| janky 帧 | 1 / 172（0.58%）|
| 高输入延迟帧 / 掉帧 / ANR | 0 / 0 / 0 |
| 逐键间隔 | 中位 457ms（= adb 自身开销）；>800ms 的 5 个样本全部落在脚本的 `sleep 0.4` 阶段边界，不是应用卡顿 |

→ 稳态打字没有问题。

## 二、再测非稳态：复现成功
你今天刚装过新 APK，"更新后首次启动要重建索引"正是非稳态。删掉 `base.*.idx` 造出该场景，
键盘一弹出来就狂打 15 键（不等索引就绪）：

| 指标 | 稳态 | 冷启动窗口内打字 |
|---|---|---|
| p99 帧 | 14 ms | 300 ms |
| janky 帧 | 1（0.58%） | 7（12.07%） |
| 高输入延迟帧 | 0 | 15 |
| 掉帧事件 | 0 | 3 |

日志同时显示 `基础索引: 已解压并写入磁盘缓存（604077 键 / 14767KB / 1846ms）` 与打字重叠。

## 三、根因
基础索引重建用的是 `XZInputStream(...).readBytes()`，一次 1.8 秒不给喘息的连续 CPU 冲击，
外加一个 14.7MB 大分配。线程优先级虽然已经是 BACKGROUND，但单核被占满时主线程照样被挤。

## 四、修法
- 分片解压（`readWithYields`）：每攒够 1MB `Thread.sleep(1)` 让出 CPU；
- 原子写缓存也分片：每 256KB 让出一次。
让出 CPU 后总耗时只多几十毫秒（都在后台），前台打字不再被挤。结果与功能不变，只是后台别再"霸屏"。

## 五、复现标准（留档）
`su -c 'rm -f filesDir/index/base.*.idx'` → force-stop → 启动 → 立刻点输入框弹键盘 → 不等就绪就连打
→ 看 `dumpsys gfxinfo com.jinn.inputmethod` 的 p99 与 janky 比例。稳态对照：直接连打即可。

---

# JVM 按键压力测试（不依赖真机，量化各类按键的触发耗时）

日期：2026-09-18 ｜ 验证：`testDebugUnitTest`（243 用例）+ `lintDebug`（0 error / 29 warning）

新增 `KeyboardStressTest`：在 JVM 里模拟一台键盘，只复刻按键触发逻辑（字母键走
`Shuangpin.toQuanpin` + `query`；空格/候选点选走 `consumption` 后按消费量裁 composing；全消费后走
`predict`），每键单独计时，并记录最慢几键的现场（当时的 composing 长度）。

## 一、实测数据（单位 ms，7 场景 / 1514 键）
| 场景 | n | p50 | p90 | p99 | max | 最慢键现场 |
|---|---|---|---|---|---|---|
| ① 连续快打（缓存涨到 112 字符） | 156 | 0.046 | 0.080 | 0.344 | 0.827 | 第 1 键 composing=0 |
| ② 短缓存对照 | 140 | 0.029 | 0.158 | 0.445 | 0.471 | composing=2 |
| ③ 字母+空格上屏 | 240 | 0.044 | 0.059 | 0.182 | 0.297 | composing=26 |
| ④ 字母+点候选+预测 | 280 | 0.009 | 0.024 | 0.104 | 0.182 | composing=102 |
| ⑤ 退格狂按 | 174 | 0.000 | 0.001 | 0.039 | 0.178 | — |
| ⑥ 同键 a×200（缓存 191 字符） | 200 | 0.156 | 0.331 | 0.532 | 0.562 | composing=191 |
| ⑦ 字母+回车清空 | 280 | 0.014 | 0.038 | 0.113 | 0.153 | — |
| 7 套方案首次使用（含建表） | — | — | — | — | 0.21~1.13 | 最慢 FLYPY |

## 二、结论
1. 按键触发逻辑本身不是瓶颈：7 类场景 p50 ≤ 0.156ms、max ≤ 0.83ms；即使输入缓存涨到
   112 / 191 字符（远超真机上出现问题的 78 字符），单键仍不到 1ms。
2. 因此真机上那个 1.2 秒主线程卡顿不来自引擎（`query`/`segment`/`consumption`/`predict` 全部排除），
   嫌疑集中在上屏/渲染那条 UI 路径（视图重建、宿主输入框、GC）。
3. 纠正两处旧判断：
   - `JinnIme` 里"7 套表一次全建有几十毫秒量级开销"是夸大，实测单套首次使用 0.21~1.13ms，
     已按实测改写注释；
   - 我在前述"登记"过的「第一次切到未预热方案可能卡一下」这个窗口，按实测基本不成立
     （建表 1ms 量级），故撤回该风险登记。

## 三、这支测试留作常驻护栏
用例数 241 → 243；阈值为宽松的 500ms/键，只守"别退回秒级卡顿"这类灾难性退化。

---

# 乱序 / 超长拼音的模糊测试（589 例，最慢 3.4ms）

日期：2026-09-18 ｜ 验证：`testDebugUnitTest`（244 用例）+ `lintDebug`（0 error / 29 warning）

给 `KeyboardStressTest` 加了模糊场景：固定种子（20260918，可复现），
覆盖纯随机字母（400 例 / 长度 1~40）、真实拼音打乱顺序（60 例）、合法音节拼接加坏尾（120 例，
专造"前缀能切、尾巴不通"）、以及 60 / 120 / 240 字符的超长输入（随机字母 / 真实音节流 / 同键重复）。

## 实测（589 例，单位 ms）
| 分位 | 耗时 |
|---|---|
| p50 | 0.037 |
| p99 | 0.453 |
| max | 3.437 |

最慢 5 例（说明超长输入也扛得住）：
```
3.437ms  超长同键 a×240    len=240  候选=9    残码=""
1.331ms  超长同键 a×120    len=120  候选=9    残码=""
0.962ms  随机字母 len=3    len=3    候选=81   残码="x"   ← 首次跑到该路径（JIT 预热）
0.786ms  超长音节流 len=240 len=240  候选=309  残码=""
0.540ms  超长同键 a×60     len=60   候选=9    残码=""
```

## 结论：代码是怎么处理这两类输入的
1. 先切音节：优先「词库约束分词」，不成立退回贪心最长匹配。切不动的尾巴原样留成残码
   （`partialSyllable`），不丢字符、不报错，退格/续打都正常。
2. 前缀能切多少就用多少：候选来自切得出的前缀（如 `nianjkelihshdhf` → 切出 `[nian]`，
   余下 `jkelihshdhf` 作残码，候选仍是「nian」的 15 个词）。
3. 补全接住"差一点"：最后一个不完整音节若是某合法音节的前缀就补全再查词
   （`nianj` → 年级 / 年纪 / 念及 / 年祭 / 年假）。
4. 完全切不通（首字母就不是任何音节开头）：候选为空，但拼音行照常显示所打字母、按键照常响应。
5. 超长不设硬上限：成本已压到 O(n·6)，240 字符也只有 3.4ms，所以不静默丢按键，
   宁可让它原样留在 composing 里等用户退格。

护栏：新增的模糊用例断言"最坏一例 < 200ms"，用例数 243 → 244。

---

# 打错的拼音怎么快速清掉（新增「长按退格清拼音串」）

日期：2026-09-18 ｜ 验证：`testDebugUnitTest`（244 用例）+ `lintDebug`（0 error / 29 warning）

## 一、先纠正我自己上一条结论
我说"现有清空手势不清 IME 侧拼音缓存"，读漏了：`onTripleBackspace()` 里本来就有
`composing.clear()`（注释写着"先清拼音串与预测，再通知 IME 删除已上屏文本"），是我只看了 IME 侧的
`deleteAllText` 就下了判断。此处更正，清空手势是干净的。

## 二、真正的缺口
现有手势「双击 → 长按退格」能清干净，但它连输入框里已上屏的文字一起清。
用户只是打错了一串拼音时（比如 `nianjkelihshdhf`，或压测里被连打到 78 字符那种），
用这条手势会把前面写好的正文一并删掉，过于危险。于是只有"逐字退格"这一条路：
78 个字符按 55ms/次连删要 6~8 秒。

## 三、新增：按住退格 ≥1.2 秒 → 整串清掉拼音（不动已上屏文本）
在连续删除的 runnable 里加了判定：按住够久 且 拼音串仍然 ≥12 字符 → 清空拼音串、停止连删。
- 门槛 12 字符：正常按着删几个字的拼音不受影响，不会被误清；
- 只清 IME 侧的 composing/候选/预测，不碰输入框里的文字；
- 与「双击+长按」形成两级：轻量清拼音 / 彻底清输入框；
- 顺带把 `onTripleBackspace` 改用现成的 `clearComposingState()`（原先漏清 `lastCommittedWord`，
  会让清空后紧接着的预测用到旧词）。

## 四、可选项（供选择，未实施）
若觉着 1.2 秒仍不够快，还有三条路：① 门槛降到 0.6 秒；② 门槛降到 8 字符；
③ 在候选栏加一个「清空拼音」按钮（需在拼音串非空时也渲染候选栏，改动更大）。
另外"给输入长度设硬上限"这条路不建议，会静默丢用户按键，且实测 240 字符也只要 3.4ms。

## 五、顺带
设置页用法说明补了一句手势提示（`settings_usage`），否则用户无从知道有这条快捷方式。

---

# 安装成功 + 三项待验全部通过（1.2 秒卡顿消失）

日期：2026-09-18 ｜ 验证：真机实测（PACM00 / ColorOS）

## 一、先订正一条我记错的结论
上一轮我写「ColorOS 一律拦截 adb install（-99）」，不对：这次 `adb install -r` 直接返回 Success，
`lastUpdateTime` 更新为 23:04:05。那个 `-99` 是可重试的（安全中心的校验状态会变），不是永久拒绝。
以后遇到先重试，再走 push + 安装界面那条路。

## 二、① 冷启动窗口（删 base 缓存 → 启动 → 立刻连打 15 键）
| 指标 | 修复前 | 修复后 |
|---|---|---|
| p99 | 300 ms | 101 ms |
| janky | 12.07% | 6.78% |
| 高输入延迟帧 | 15 | 8 |
| p50 / p90 | 11 / 20 ms | 9 / 11 ms |

分片让出 CPU 生效了：最坏分位掉到 1/3，中位与 90 分位也一并变好。剩下的 101ms 是解压那 1.8 秒
CPU 冲击的残余 + 14.7MB 分配，尚未完全消除（想再压需要改解压方式或延后到空闲）。

## 三、② 高频压测（160 键 / 34 次/秒 / 输入缓存涨到 78+ 字符）
| 指标 | 修复前（旧版，两轮） | 修复后 |
|---|---|---|
| Davey（主线程卡顿） | 1229 / 1198 ms | 0 |
| p99 | 30 ~ 300 ms | 32 ms |
| janky | 8.11 ~ 13.14 % | 1.82 % |
| 高输入延迟帧 | 4 ~ 20 | 4 |

注入速率与次序一致（4.83s / 4.65~4.70s），所以是一致对比。那 1.2 秒没了，
分词成本悬崖那个修复（后缀可执行性剪枝）在真机上确认有效。

## 四、③ 新增「长按退格清拼音串」真机验证
```
长按前拼音串长度=84
→ 退格长按 1260ms：清空拼音串（69 字符）
→ 之后候选栏回到「功能面板」（= 拼音串已空）
```
1.26 秒触发（阈值 1.2s + 55ms 轮询粒度），触发前 1.26 秒里已逐字删掉 15 个字符，与设计一致。

## 五、顺带记下真机坐标（下次省时间）
1080×2280 屏：行1 y=1680（qwertyuiop，x 65..1010）、行2 y=1840（asdfghjkl，x 115..955）、
行3 y=2020（大写+zxcvbnm+退格 x≈1000）、空格 (540,2200)、回车 (1015,2200)。
退格在第三行末尾（y=2020），不是第二行，我之前按第二行点，结果一直在打字。

---

# 新增「候选预测词」开关（设置页可关，关掉后不再出现预测候选）

日期：2026-09-18 ｜ 验证：`testDebugUnitTest`（244 用例）+ `lintDebug`（0 error / 29 warning）+ 真机

## 需求
> 候选的预测词应该允许用户自由开/关，放在 APP 设置页里。关闭了预测词，用户选完文字之后不再出现预测的候选文字。

## 实现
- `Prefs.predictEnabled`（默认开，保持现状）+ 设置页新增「候选预测词」开关（紧跟「用户词频学习」下方，同一套卡片样式）。
- 键盘侧在产出点加闸门：预测只从两个地方产生（空格取首候选后、点候选后），
  关掉时直接置空；「用预测」的两处（空格复用预测/预测词上屏）也加了开关校验。
- 另加一处自愈：`refreshCandidateBar()` 开头发现开关已关且还留着预测时，就地清空，
  否则刚在设置页关掉开关、回到键盘，上一次留下的预测还会挂在候选栏上。
- 开关读取走 `Prefs(context)`（SharedPreferences 内存缓存，微秒级），因此立即生效、不需要重启输入法。

## 真机验证
| 开关 | 选词上屏 | 「智能预测」日志 |
|---|---|---|
| 开（默认） | `空格取首候选: "您好"` ✓ | 1 条（照常有预测） |
| 关 | `空格取首候选: "您好"` ✓（打字不受影响） | 0 条（预测彻底不产生） |

开关切换即时落日志：`候选预测词: 关闭（立即生效）` / `开启（立即生效）`。

## 说明
- 默认开（与之前行为一致），需要清爽界面的用户可以自己关掉。
- 关掉只影响「选完词后的预测」，不影响候选查询、补全、用户词频排序，这些是各自独立的机制。

---

# 修复「双拼下按 g/h 候选栏拼音行不更新」

日期：2026-09-18 ｜ 验证：`testDebugUnitTest`（247 用例）+ 真机（双拼模式）截图

## 用户确认的根因
> 不是卡顿：候选栏完全不显示 g 之后的字母，所以以为程序卡住，实际是候选栏没更新。

## 根因
候选栏那行拼音显示的是 `queryInput`（转换后的全拼），而这段代码自己的注释写的是
「双拼：先转全拼再查询；显示仍保留双拼原文」，代码与它声明的意图不符。

双拼下两者经常不同：`jg` 转全拼会被吞成 `j`（日志实证：`双拼[j]→j`、`双拼[jg]→j`、`双拼[jgh]→j`），
于是按下 g/h 拼音行一个字都不动。这也正好解释了用户"底栏按钮倒是有反应"的观察：
底栏有可见反馈，只有拼音行没变化。

## 修法
两处 `viewCandidatePinyin.text` 由 `queryInput` 改为 `input`（用户实际按下的键）。
全拼模式下 `input == queryInput`，行为不变。

## 真机验证（双拼·自然码模式下，截图逐帧确认）
| 操作 | 拼音行显示 |
|---|---|
| 按 j | `j` |
| 按 g | `jg` |
| 按 h | `jgh` |

同时候选保持 `[级 集 几 及 机 …]`（转换后的全拼仍是 `j`），即"键看得到、候选不变"，符合预期。

## 过程中的一次自我纠错（值得记）
我第一次验证是在全拼模式下做的（自己之前的测试把方案切成了全拼），截图显示 `j → jg → jgh`，
但那不能证明双拼场景，被用户当场指出。切回自然码双拼重测才是有效证据。
教训：模式敏感的功能，验证前必须先确认当前模式（本次靠 `双拼[...]` 日志前缀确认）。

## 做法教训
排查"按键没反应"时我一直在看日志（模型层），日志每次都正确，于是一再得出"没问题"的结论；
而用户看到的是显示层。"看不见 / 不更新"这一类问题必须核对渲染结果（截图或 UiAutomator 文本），
只看日志会整类漏掉。

## 状态
用例 247 全绿、Lint 0 error / 29 warning；APK 已安装（`lastUpdateTime 23:46:14`）。
按用户要求，本次及此前三个提交只在本地，未推送。

## 附：这次修复对 7 套双拼的覆盖（实测转换对照）

疑问"是不是 7 种双拼都修了"，修复点在显示层（`viewCandidatePinyin.text = input`，其中
`input = composing.toString()`，与方案完全无关），所以 7 套走的是同一条路。
而根因（转换吞字母）是每套方案各不相同的，实测各方案的"按键 → 转换后全拼"：

| 方案 | 按 j | 按 jg | 按 jgh | 旧版是否看起来卡住 |
|---|---|---|---|---|
| 全拼 | j | jg | jgh | 不会（本来就不转换） |
| 自然码 | j | j | j | 会 |
| 小鹤 | j | j | j | 会 |
| 搜狗 | j | j | j | 会 |
| 微软 | j | j | j | 会 |
| 紫光 | j | jiang | jiangh | 这组键序不碰撞（别的键序仍可能） |
| 智能ABC | j | j | j | 会 |
| 加加 | j | j | j | 会（`zhong→z`，吞得最狠） |

结论：7 套里 6 套在这组键序上都会"看起来卡住"，紫光只是恰好不碰撞，所以这个 bug 不是
自然码独有；修复在显示层，对 8 种模式（含全拼）一并生效。
验证标准说明：真机截图验证做的是自然码；其余 6 套的覆盖依据是「显示代码与方案无关」+
上表的转换对照，未逐套上真机点验。

---

# 文档同步到最新 + 同步推送（2026-09-19）

日期：2026-09-19 ｜ 验证：`testDebugUnitTest`（247 用例）+ `lintDebug`（0 error / 29 warning）

## 一、文档更新（把当天后半程的改动全部落档）
- `AGENTS.md`：
  · 首行「自然码双拼」→「双拼（7 套方案，默认自然码）」；
  · 测试规模「23 个测试类 / 239 个用例」→ 26 / 247，并列出本次新增的三支测试；
  · 订正事实错误：原写「`is_private` 列与掩码逻辑保留」，实际 v5 迁移已删除该列、
    掩码逻辑随之失效（隐私功能整体移除）；目录说明与 `ClipboardFilter` 说明里的 `is_private` / `PRIVATE`
    一并清掉；
  · 新增「按键/模式排查方法」章节：日志形如 `拼音输入[双拼·自然码]: jgh`（自带模式）；
    切换全拼/双拼的正确步骤；界面默认值（词频学习开、预测关、剪贴板 500）。
- `README.md`：键盘特性补三条，预测默认关可开、按住退格清拼音、候选栏显示实际按下的键。
- `README_EN.md`：同步上述键盘特性。

## 二、同步推送（用户解除"审查前不推送"限制）
- 首次推送用 `git push … | tail -1 && break` 判成功是无效判据（管道退出码来自 `tail`，推失败也会 break），
  实际 `origin/main` 未更新、落后 9 个提交；2026-09-19 改用真实退出码重试后补齐，
  三处均为 `d3e0366`：`origin/main` / `origin/perf/paging-pinyin-rematch` / Gitee `master`（本地 `main` 一并快进对齐）。
- 新版本发布：跨日 → `versionCode = 20260919`，两端新建 Release `v20260919`（保留 v20260917/v20260918）；
  两端实际下载的 sha256 与本地一致：`30f288ec36caf9320ddb44db2aa86deb5559fceaae9bdfc92006fe695c56f827`。

---

# 补齐推送 + 补发 v20260919 + 订正入库文档（2026-09-19）

日期：2026-09-19 ｜ 验证：`testDebugUnitTest`（247 用例）+ `lintDebug`（0 error / 29 warning）

## 一、补齐上一轮没兑现的两件事
- 推送其实失败过：上一轮的 `git push … | tail -1 && break` 把管道退出码当成了 push 的结果，推失败也会 `break`，
  于是 `origin/main` 静默停在 `6380ae0`、落后 9 个提交（Gitee 侧反而成功，所以看起来「都推上去了」）。
  改用真实退出码重试后补齐三处，并把本地 `main` 快进对齐（它落后 130）。
- Release 确实没发：`v20260918` 之后累计 29 个提交未发布，其中含用户可见修复
  （双拼拼音行不更新、字母键失活隐患、分词卡顿、冷启动卡顿）与新增开关。已补发 `v20260919`：
  两端新建 Release、上传 APK、两端下载 sha256 与本地一致。

## 二、订正入库文档里与实际不符的地方
- `ClipboardPrefs.maxItems` 的 KDoc 仍写「默认 200」，而常量 `DEFAULT_MAX_ITEMS = 500`、设置页提示也是 500
，只有这处注释漏改（2c0dcbe 改常量时同步了 `strings.xml`，漏了 KDoc）。已订正。
- `AGENTS.md` 双拼段落写「7 套方案」却把全拼一起列进去（共 8 项），与「双拼方案下拉只列 7 套、不含全拼」
  的标准矛盾。改为「7 套双拼方案：自然码 / … / 加加（全拼不属于双拼方案）」。
- `AGENTS.md` 测试清单里点名的 `ClipboardDedupeTest` 已被删除（d0f8fe3「隐私功能彻底移除」时删的 75 行），
  属于「已移除功能的残留」，去掉；同时补上清单里一直没写的 `IndexMappedParityTest`（内存映射等价性护栏）。
- 另：前述里有一节记的是本机文件（不入库）的订正，且所记剪贴板上限已过期，从本日志移除。

---

# 入库文档措辞平实化 + 清掉与实际不符的表述（2026-09-19）

日期：2026-09-19 ｜ 验证：`testDebugUnitTest`（247 用例）+ `lintVitalRelease`（构建内）+ 重建 APK

## 一、去除模板化措辞（对外文档）
- 章节标题的 emoji 全部去掉：README 中英各 7 处（`## ✨ 核心特性` → `## 核心特性` 等）。
  每个标题配一个 emoji 是典型的生成痕迹，去掉不影响阅读。
- 删掉自问自答与过渡句：英文 README 的 `In other words:` → 结论直接说；
  中文 README 的「也就是说：」同样处理。
- 删重复：加密这条在剪贴板那一节说了两遍（加粗语态 + 子项），合并成一句。
- 修掉混排：英文 README 里夹了中文「双拼」→ `Shuangpin`。
- `tools/dict_builder/README.md`：「已按作者要求删除」这种写给作者看的元话语 → 「已于 2026-09-17 删除」。
- `注音规范-执行版.md`：「三个必须记住的事实」→「三条必须记住的事实」。

## 二、订正与实际不符的表述（数据都数过）
- 隐私功能残留（2026-09-16 整体移除，`is_private` 列已删）：
  `README.md` 分类列表里的「隐私」、长按里的「标记隐私」、`ClipboardPanelView` 模块职责里的「隐私」，
  英文 README 对应的三处，`AGENTS.md` 的「不动收藏/隐私标记」与 `ClipboardClassifier` 的「隐私绝不自动」。
  → 按现状改写：分类只有全部 / 网址 / 数字 / 收藏，长按只有收藏与删除，收藏由用户长按产生。
- 词条数：`xz -dc … | wc -l` 实测长词包 184,995、腾讯包 955,031；
  README 原写 18.6 万 / 97 万 → 改 18.5 万 / 95.5 万。应用内文案 `OptionalDicts.kt` 同样写着 97 万，一并改。
- APK 体积：实测 5,188,434B = 4.95MB，README 原写 4.9MB → 改。
- THUOCL：README 中英都写「THUOCL 自动注音」，而 `tools/dict_builder/README.md` 已把该脚本标为
  「评估·未采纳」，同一件事两处标准打架。按事实改写并说明未采用的原因。

## 三、补漏
- 两个 README 都没写 26 键区圆角 / 间隙可调（`KeyAppearance.kt` + `Prefs.keyCornerDp/keyGapDp`，2026-09-16 上线）。
- `AGENTS.md` 的「按键/模式排查方法」补 退格手势四级规则（单击 / 按住连删 / ≥1.2s 清拼音 / 双击+长按清输入框），
  含判据常量，避免下次再靠读代码猜。
- 源码注释同步一处：`ClipboardPanelView` 顶栏注释的「全部/网址/数字/收藏/隐私」→ 去掉隐私。

## 四、代码改动
- `OptionalDicts.kt`：腾讯包描述「约 97 万词条」→「约 95.5 万词条」。
- `ClipboardPanelView.kt`：顶栏注释清掉 `隐私` 残留。

## 五、发布
- 重新构建：versionCode `20260919`（同日），V1=false / V2=V3=true，4 字节对齐；
  APK sha256 `0df8637124e0ee227419cc3d42a4b1fa7ac7a6b31c5b4b965a8fcdca8291feb2`。
- GitHub + Gitee 两端覆盖 `v20260919`（Gitee 无 clobber：先删附件再传），下载 sha256 与本地一致。

---

# 入库文档模板化措辞二次清理（2026-09-19）

日期：2026-09-19 ｜ 范围：`README.md` / `README_EN.md` / `AGENTS.md` / `tools/dict_builder/*.md`
（均为入库文档）；`docs/`、`tools/dict_builder/THUOCL-master/README.md`、本地工作区目录 不入库，未处理。

## 改了什么

- 语音说明那段铺垫太厚：中文写「本项目还内置了一个可选的语音听写功能，但它不是开箱即用的：」、
  英文写「This project also ships an optional … — but it is not ready to use out of the box:」，
  都是先绕一圈再进正题。改成直接说「语音听写是可选功能，不开箱即用：识别完全在服务端完成」。
- 删除键的四条手势挤在一句话里（「双击 + 长按删除键清空全部；打错的拼音按住退格约 1 秒可整串清掉」），
  拆成四条并给英文版同样处理。
- 同义反复与重复修饰：
  - 用户词频那条连说两遍「记」（「记住你选过的词…只记你明确点过的候选」）→ 合并成一句。
  - 剪贴板的 AES-256-GCM 加密在主句和子项里各说一遍 → 合并。
  - 顶部搜索面板一句里出现三次「搜索」→ 去掉重复，改「输入关键词实时检索」。
- 自我指涉的引导句：「如果你只用拼音键盘 / 剪贴板，本节可以完全忽略」→「不用语音的话，这节可以直接跳过」。
- 模板词：`注音规范-执行版.md` 的「核心工作：」删掉标签，直接叙述。
- 过长的括号：`AGENTS.md` 诊断日志那行塞了三项耗时数据，拆成独立一句。
- 结论与事实打架：README 的「词库」段已写明 THUOCL 路线未采用，致谢和许可证段却还列着 THUOCL →
  两处一并去掉（`pypinyin` 保留，`extend_dict.py` 确实用过它）。

## 没改什么

- 章节标题与层级、章节顺序、表格结构：一处未动。
- `AGENTS.md` 正文（除上面那一句）：整篇是执行标准，事实密度高、无套路句式，强行润色只会稀释信息。
- `tools/dict_builder/README.md`：除上一轮的一处元话语外无可精简处。
- 历史的更新日志：属于团队记录，风格统一，不动。

## 验证

本次只改文档，未改 Kotlin，`testDebugUnitTest` 无需重跑；提交前 `git diff --stat` 复核仅 5 个 md 文件。

---

# 缺陷修复、剪贴板容量上限与文档标准同步（2026-09-19）

日期：2026-09-19 ｜ 范围：`ClipboardController` / `ClipboardDb` / `ClipboardPanelView` / `SearchPanelView` /
`ClipboardFirewall` / `TextSelection` / `PinyinEngine` / `UserFrequency` / `SettingsActivity` / `Prefs` 与入库文档。
`docs/`、本地工作区目录 不入库，未处理；语音链路（`AsrClient` / `Protocol` / `MicRecorder` / `MicButton`）按既定约束未动。

## 一、缺陷修复

| 提交 | 内容 |
|---|---|
| `e00fd37` | `su()` 先读流后 `waitFor` 导致超时形同虚设 → 改双路后台排空 + 先 `waitFor` 再取文本；host 校验 trim 不对称让主线程崩（`ws:// 1.2.3.4 :6016`）→ 取值侧统一 `normalizeHost`；剪贴板延迟重试堵 BackgroundIo 队列 → 改主线程 `Handler`；`TextSelection.lineStart` 行首边界；`PinyinEngine` 两处静态化与未用参数；`UserFrequency` 落盘时序（尾沿补写 + `setEnabled` 补载） |
| `ae5ab1c` | 复审自改代码：修 3 处新引入缺陷 + 1 处落盘时序窗口 |
| `0b4864d` | 复审（质量门禁轮）：host 校验 trim 不对称，补不变式用例（校验放行的取值必须能被 OkHttp 接受） |
| `8b08c80` | 复审（前述）：补符号表数据护栏 `SymbolLayoutTest`（分组非空 / 标签唯一 / 取值不含换行与制表符 + 标点组第 2 页 g 键录入回归） |

## 二、剪贴板容量两条上限（`c9c2eae`）

- 采集侧单条 256KB：超出直接不入库（不截断）。判定先按字符数快筛（UTF-8 字节数恒 ≥ 字符数，
  避免为超长串先分配字节副本），再按 UTF-8 精确定字节。
- 总量 100MB：超出按最旧非收藏淘汰（复用 `TRIM_PRIORITY` 的「收藏永不删」原则，改按字节累计）；
  体积标准取 `LENGTH(encrypted_content)`（base64 纯 ASCII，字符数即字节数），不需解密、不把正文读进内存。
- 淘汰挑选抽成纯函数 `overflowIdsForByteBudget`，批量删除抽成 `deleteByIds` 供两处复用；
  新增 `ClipboardLimitsTest`（15 例：单条判定 4 + 总量淘汰 6 + 窗口预算 5）。
- 标准：单条按明文字节、总量按库内密文体积（100MB ≈ 73MB 明文）；单条上限只在采集侧生效，
  库里既有超限行只由总量预算淘汰。

## 三、搜索解密窗口与发布节流（`5e16c76` / `6d0cfec` / `7899185`）

- 搜索分块 300 → 50（`SEARCH_WINDOW_ITEMS`，面板每页一致提为文件级 `PANEL_PAGE_ITEMS`）：
  一次查询会把整窗口逐条解密，原值下最坏单次明文峰值 ≈196MB，现在 ≈33MB。
- 新增纯函数 `ClipboardStore.decryptWindowPeakBytes()` 与预算 `DECRYPT_WINDOW_BUDGET_BYTES`，
  配「窗口 × 单条上限 × 放大系数 ≤ 预算」的护栏用例（含「300 条会被拦下」的反向验证）。
- 窗口细化后逐块发布会把大库搜索的主线程布局次数抬到数百次 → 补 `PUBLISH_MIN_INTERVAL_MS = 120ms`
  发布节流合并，并在循环结束后无条件补发（防止节流吞掉最后一块而「静默少给结果」）；命中数未变则跳过补发。
- 放大系数按实测修正：微基准（AES-GCM + base64 + String，与 `ClipboardCrypto` 等价）实测
  单条解密常驻 = 2.33 × 明文（密文串 4/3 + 明文串 1.0）；原先按「明文串在 UTF-16 下占 2×」估的
  3.5 高估 50%（JVM/ART 都用紧凑字符串）→ 取 2.5、预算 48MB。
- 附带修好：库为空或无命中时搜索面板不再「静默不更新」。

## 四、密钥与构建

- 仓库内不留密钥：`keystore/jinn-release.jks` 与 `keystore.properties` 移出（移动而非删除；
  两份 `.jks` 经 sha256 比对确认不是同一份，属旧遗留）→ 归档到
  `GLOBAL/credentials/JinnKeyStores/legacy-in-repo/` 并附说明；发布用现役密钥经 `JINN_KEYSTORE_ROOT` 传入。
- README / README_EN 的「在仓库内创建密钥」指引改为外部凭据目录 + 环境变量，并写明未提供时产出未签名包。

## 五、文档同步（本次）

- `AGENTS.md`：测试规模 283 → 285 例；解密窗口标准由 `×3.5` 订正为 `×2.5`（实测 2.33）；
  真机安装补上 ColorOS 拦新装的可用通道（`adb push → /data/local/tmp → su -c 'pm install -r'`；
  旧的「`-99` 可重试」与「`VIEW file://` 兜底」在本机实测均已失效），并记下「卸载即销毁 Keystore 密钥」。
- `README.md` / `README_EN.md`：`assembleRelease` 行内注释与「未提供配置则产出未签名包」的正文对齐。

## 验证

- `testDebugUnitTest` 29 类 / 285 例全绿；`lintDebug` 0 error / 29 warning；
  `--rerun-tasks compileDebugKotlin` 20 条告警全为既有 `Lateinit`。
- 真机（PACM00 / Android 10 / root，debug 包）：`su()` 审计 7/7 项返回真实值、无超时；
  选词后词频文件即时生成、`kill -9` 后重启 `已载入`；复制 3 次（含 1 条重复）库内 2 条（去重生效）；
  体积淘汰以 8KB 预算降尺度跑通（最旧非收藏被删、最新存活）；搜索发布节流的 A/B 对比
  （14 分块 → 节流 10 次发布 / 未节流 14 次，收尾发布含完整结果）。
- 三处未做并注明原因：host 校验 UI（服务端卡片随语音开关隐藏，属禁改域）、`lineStart` 的 UI 路径
  （方向面板为 Canvas 自绘不可自动化）、搜索节流在 debug 包上的注入（IME 自有搜索框无法从 adb 注入）。

## 剩余待办

- 发布 tag 纯数字化（远端 6 个 `v*` tag 重建 + 附着包重传）未执行，属发布动作。
- `ACTION_CLIPBOARD_PASTE` 动态广播在 Android ≤12 对外可达（任意应用可按自增 id 读到剪贴板历史并注入当前输入框），
  应用内已无发送方，建议删除该接收器。→ 本条已在前述关闭（已删除该接收器）。
- 日志内容最小化、`MANAGE_EXTERNAL_STORAGE` 去留、`Diagnostics.init` 移出主线程，前两项属用户决策。
  → 「日志内容最小化」中的正文落盘部分已在前述处理，其余仍待决策。

---

# 源码安全审查与修复（2026-09-19）

日期：2026-09-19 ｜ 范围：`JinnIme` / `ClipboardDb` / `PinyinKeyboardView` / `SettingsActivity` /
`PhraseIndex` / `DictManagerActivity`、备份规则 XML 与入库文档。
语音 / NAS 链路（`AsrClient` / `Protocol` / `MicRecorder` / `MicButton`）按既定约束未审查、未改动。

## 一、确认并修复的缺陷（按严重度排序）

### P1 · 剪贴板粘贴广播对外可达，已删除

- 位置：`JinnIme.clipboardPasteReceiver`（原 248 行）与 `registerReceiver(...)`（原 240 行）。
- 触发条件：Android 8~12（minSdk 26、targetSdk 34）。动态注册的接收器在没有广播权限时对外可达，
  任意应用执行 `am broadcast -a com.jinn.inputmethod.action.CLIPBOARD_PASTE --el clipboard_paste_item_id 1` 即可命中；
  Android 13+ 因 `RECEIVER_NOT_EXPORTED` 不受影响。
- 成因：早期「Activity → IME」的跨组件设计残留。面板改用同进程 `listener.onPaste(text)` 回调后，
  广播只剩接收方、没有发送方，却仍被注册，等于给外部留了一条可用的入口。
- 验证依据：全仓检索 `ACTION_CLIPBOARD_PASTE` / `clipboard_paste_item_id` 只有定义处与接收器自身引用，
  无任何发送点；`ClipboardPanelView.onPaste` 与 `SearchPanelView.onPaste` 走的是 `onPasteText` 回调。
  真机 `adb shell am broadcast` 实证可见接收器被触发，且 id 完全由外部指定。
- 影响：第三方应用可迫使输入法把任意一条剪贴板历史（解密后）注入用户当前正在编辑的输入框，
  若焦点在其自己的输入框内即可直接读走；重复发送还可刷屏并占满 `BackgroundIo` 单线程队列。
- 修复：删除接收器、`clipboardPasteFilter`、`notifyPasteResult` 与三个 action/extra 常量，
  一并删除仅供该广播使用的 `ClipboardDb.contentById`（无其它调用方，测试亦未引用）。

### P1 · 用户输入正文写入共享存储日志，已收敛

- 位置：`PinyinKeyboardView` 的「候选上屏」「空格取首候选」「搜索候选」「无候选丢弃拼音」，
  以及 `SettingsActivity.sendImeTest`。
- 触发条件：默认配置即触发。日志默认落在 `/storage/emulated/0/JinnIme/logs/jinn-YYYY-MM-dd.log`，
  `Diagnostics` 只过滤 `V` 级（`VERBOSE_TO_FILE = false`），`I` 级照常落盘。
- 成因：这几处用了 `Diagnostics.i` / `w`，与「候选类日志走 `v`」的既有规则（见「候选: …」那行）不一致。
- 验证依据：`log()` 中 `if (level == 'V' && !VERBOSE_TO_FILE) return` 是唯一的落盘过滤条件；
  「候选上屏」在每次点选候选时执行，日志正文就是用户刚上屏的词。
- 影响：输入法看到的全部内容（姓名、地址、口令、验证码）以明文写进共享存储，
  Android 10 以下持 `READ_EXTERNAL_STORAGE` 的应用可直接读取。这与项目「日志禁出正文」的既定规则相悖。
- 修复：上述五处统一降为 `Diagnostics.v`，仍可在 logcat 看到，需要落盘排查时把
  `VERBOSE_TO_FILE` 打开即可，调试能力不丢。

### P2 · 明文用户词频未排除备份，已补

- 位置：`res/xml/backup_rules.xml`、`res/xml/data_extraction_rules.xml`；`android:allowBackup="true"`。
- 成因：`UserFrequency` 把用户选过的词以明文存进 `filesDir/user_freq.txt`（最多 3000 条），
  而备份排除清单只列了剪贴板数据库，形成倒挂：有 Keystore 加密兜底的反倒排除了，
  真正可读的明文词表却跟着 `domain="file"` 的默认包含一起进了云端备份与设备迁移。
- 修复：两份规则文件补上 `user_freq.txt` 与 `user_freq.txt.tmp`（原子写的临时文件）排除项。

### P2 · 词库下载无体积上限，已加

- 位置：`DictManagerActivity.fetchToFile`。
- 成因：`body.byteStream().copyTo(out)` 无上限，且客户端开了 `followRedirects(true)`，
  实际落地路径交给目标主机指路；异常或恶意的响应会一直写满用户存储后才失败。
- 修复：新增 `copyCapped()`，超过 `MAX_DOWNLOAD_BYTES`（64MB，约为现役最大包 6.36MB 的 10 倍）即中止，
  临时文件仍走既有的失败清理逻辑。

### P3 · 索引偏移前缀和 Int 溢出，已修

- 位置：`PhraseIndex.expandLengths`。
- 成因：偏移累加用 `Int`。`parse` 的 `need == capacity` 只把条目数松散约束在「文件体积 / 3」量级
  （大索引可达千万级），累加值可越过 `Int.MAX_VALUE` 回绕成负数，进而让
  `keyOffsets[keyCount] != keysLen` 的自洽校验失效。
- 修复：改用 `Long` 累加，越界返回 `null`，`parse` 据此拒绝该文件（符合其「结构不自洽就返回 null、绝不抛异常」的规则）。

## 二、审查过但判定非缺陷的项（留档，避免重复排查）

- SQL 注入：`whereClause` 的分类走 `?` 占位；`LIMIT/OFFSET` 是 `Int` 字面量；`$where` 只由内部布尔分支拼装。搜索不进 SQL（在内存中对解密结果过滤），无 `LIKE` 拼接。
- 更新跳转 URL：`releasesUrl(latest: Int, source: String)` 的 `latest` 是整型、`source` 是内部字面量，URL 恒以固定 `https` 前缀起头，远程数据进不了 `Intent`。
- 剪贴板加密：Keystore AES-256-GCM、`setRandomizedEncryptionRequired(true)`、128-bit tag，IV 随密文存储，篡改即解密失败并返回 null，无静默降级。
- su 命令拼接：`ClipboardFirewall` 与 `Diagnostics` 拼进 shell 的路径均取自 `context.dataDir` / `Process.myUid()`（包名与 uid 固定），无外部可控片段。

## 三、验证

- `compileDebugKotlin`：通过，告警仅既有的 20 条 `Lateinit`，无新增。
- `testDebugUnitTest`：29 类 / 285 例全绿，与基线一致。
- `lintDebug`：0 error / 29 warning，与基线一致。
- `AGENTS.md` 的「点击粘贴」条目同步为实际的同进程回调标准，并写明不得改回全局广播。

## 四、遗留（本次不做）

- `JinnIme.handleResult` 仍把语音识别结果 `text.take(60)` 以 `i` 级落盘，属禁改的语音域，只登记不动。
- 词库下载仍无 sha256 校验：补齐需要把各包的哈希写进 `OptionalDicts`，属数据维护动作。
- `MANAGE_EXTERNAL_STORAGE` 去留、`Diagnostics.init` 移出主线程，属用户决策。

# 源码安全审查（前述）与修复（2026-09-20）

日期：2026-09-20 ｜ 范围：全量 Kotlin 源码 + `AndroidManifest.xml` + 备份规则 XML + 构建与仓库敏感文件扫描。
语音 / NAS 链路（`AsrClient` / `Protocol` / `MicRecorder` / `MicButton` 与 `JinnIme` 的录音·连接·识别分支）按既定必须做到未审查、未改动。
详细报告：`docs/security-review-2026-09-20-前述.md`。

## 一、确认并修复的缺陷（按严重度排序）

### P1 · 密码框内容被学进用户词频，已修

- 位置：全仓无 `inputType` 甄别；`PinyinKeyboardView` 的「空格取首候选」「候选上屏」「预测词」三处无条件 `PinyinEngine.rememberChoice`。
- 触发条件：在 `TYPE_TEXT_VARIATION_PASSWORD` / `VISIBLE_PASSWORD` / `WEB_PASSWORD` / 数字密码 / `TYPE_TEXT_FLAG_NO_SUGGESTIONS` / `TYPE_NULL` 的框里输入并选候选。
- 成因：IME 完全信任框架，从不读 `EditorInfo.inputType`；词频经 `UserFrequency` 以明文落 `filesDir/user_freq.txt` 并参与排序。
- 验证依据：全仓检索无 `TYPE_TEXT_VARIATION` / `NO_SUGGESTIONS` / `isEditable`；三处调用点无任何前置条件；与上一轮「候选词即用户刚敲进去的内容」同源。
- 影响：口令片段变成长期明文，且之后在普通输入框里被优先推荐出来。
- 修复：新增纯函数 `InputFieldPrivacy.suppressLearning(inputType: Int?)`；`onStartInputView` 每次聚焦按 EditorInfo 设置；键盘侧统一闸门 `learnChoice()`。输入行为与候选展示不变，只停词频落盘。
- 坑：`EditorInfo.TYPE_NULL` 的值就是 0，缺省值必须用 `null`，否则「取不到 EditorInfo」会被误判成 TYPE_NULL，首轮单测即在此翻车。

### P1 · 拼音串每键以 I 级落盘（隐私 + 主线程 IO），已修

- 位置：`PinyinEngine.query` 补全分支 `Diagnostics.i("query补全: input=$raw …")`。
- 成因：`Diagnostics.log` 里 `if (level == 'V' && !VERBOSE_TO_FILE) return` 是唯一落盘闸门，`i` 级必然同步写共享存储；`query` 由 `refreshCandidateBar` 在主线程调用。
- 影响：用户输入意图明文落盘，且每敲一键一次文件 IO，吃掉首字延迟预算。
- 修复：降为 `Diagnostics.v`；同批把 `takeRawComposing` 的「回车输出英文原文」降为 v、`SettingsActivity.saveAndRestart` 的 `prompt=…take(30)` 改为只记 `promptLen`。

### P2 · 搜索驻留内存预算按字符数计，中文下低估约 3 倍，已修

- 位置：`SearchPanelView.runSearch` 的 `retainedBytes += item.content.length.toLong()`。
- 成因：`String.length` 是 UTF-16 字符数，预算按 UTF-8 字节；中文 1 字符 = 3 字节，账上 17MB 时实际已 52MB。同项目其它标准（单条上限、总量预算）都用真实字节，唯独此处不一致，这条护栏是上一轮刚加的，判据标准错了等于没加。
- 修复：新增 `ClipboardStore.utf8ByteSize()`（纯函数）并改用字节；`ClipboardLimitsTest` 增加 3 条对拍用例。

### P2 · 备份未排除 SharedPreferences，已补

- 位置：`allowBackup="true"` 而两份规则 XML 只排除 db 与 `user_freq.txt`。
- 成因：`domain="sharedpref"` 默认全包含，`jinn_inputmethod.xml`（host/port/提示词/双拼方案）与 `jinn_clipboard.xml` 随云端备份与设备迁移出炉。
- 修复：两份 XML 的 `cloud-backup` / `device-transfer` 各补两条 `sharedpref` 排除。

### P3 · 防火墙的备份审计查的是已失效文件，已修

- 位置：`ClipboardFirewall.checkBackupConfig` 恒解析 `R.xml.backup_rules`。
- 成因：API ≥ 31 起生效的是 `dataExtractionRules`，`fullBackupContent` 被忽略 → 在 Android 12+ 上给出的是错误的安全保证。
- 修复：按 `Build.VERSION.SDK_INT` 挑生效文件；判据升级为「剪贴板库 + 用户词频」两者都排除才算合格，结论文案带上实际检查的文件名。

### P3 · 更新检查可被重定向降级到明文 HTTP，已修

- 位置：`UpdateChecker.httpGet`（`HttpURLConnection` 默认跟随跨协议重定向）。
- 修复：增加终态 URL 协议断言，非 https 即判失败并回退下一源（无 APK 自动下载，风险有限）。

### P3 · 后台 IO 队列静默吞掉任务异常，已修

- 位置：`BackgroundIo.run` 只捕 `RejectedExecutionException`。
- 影响：「剪贴板没保存」「搜索没结果」这类失败在日志里完全无痕。
- 修复：任务外层 `runCatching` + `Diagnostics.e`。

### P3 · 整窗解密失败会让分页 / 搜索提前终止，已修

- 位置：`SearchPanelView`（空 items 即 break）、`ClipboardPanelView.loadNextPage`（空页即 `hasMorePages=false`）。
- 成因：游标标准上一轮已统一为「原始行数」，判停却仍用「成功条数」，两者混用 → 后面的有效条目被永久跳过。两个平行实现犯的是同一个错。
- 修复：一律改为「游标没前进才停」，同时保留无限循环保护。

## 二、审查过但判定非缺陷（留档）

- SQL 注入：`category` 走 `?` 绑定，`LIMIT/OFFSET` 只插值已校验 `Int`，`$where` 由内部布尔拼装；搜索不进 SQL。
- 剪贴板加密：AES-256-GCM + `setRandomizedEncryptionRequired(true)` + 128-bit tag，失败统一返回 `null`，无「空串当成功」的静默降级。
- `su` 命令注入 / 路径穿越：拼进 shell 的片段仅来自 `dataDir`、`myUid()`、包名与固定命令。
- 导出组件：`JinnIme` 带 `BIND_INPUT_METHOD`，`DictManagerActivity` 不导出，`SettingsActivity` 仅 LAUNCHER 且不消费外部数据；无自定义权限。
- 词库下载 ZipSlip / zip bomb：`.xz` 单文件流式落地，且已有 `copyCapped` 64MB 上限。
- 仓库敏感文件：无 `.jks` / `.pem` / `.key` / `keystore.properties` 入库；签名走 `JINN_KEYSTORE_ROOT`。

## 三、验证

- `compileDebugKotlin`：通过，告警仍为既有的 `Lateinit` 一组，无新增。
- `testDebugUnitTest`：31 类 / 304 例全绿（新增 `InputFieldPrivacyTest` 5 例、`ClipboardLimitsTest` 3 例）。
- `lintDebug`：0 error / 29 warning，与基线一致。
- `AGENTS.md` 的测试覆盖数字与新增用例同步更新。

## 四、遗留（本次不做）

- 词库下载仍无 sha256 校验：需把各包哈希写进 `OptionalDicts`，属数据维护动作。
- 敏感输入框仍显示中文候选（本次只停了词频学习）：完全对齐系统 IME（密码框切英文层）属交互决策。
- 诊断导出包含 root 全量 logcat：需用户主动触发且仅 root 环境，裁剪会削弱排查能力，属用户决策。
- 语音链路的明文传输与识别正文日志：禁改域，只登记不动。
- 工程提示：同一文件的多处改动不要并行编辑（并发 Edit 会互相覆盖，本次 `PinyinKeyboardView.kt` 三处调用点因此丢失两次）。

---

# 源码安全审查（前述）与修复（2026-09-20）

日期：2026-09-20 ｜ 范围：全量 Kotlin 源码 + `AndroidManifest.xml` + 备份规则 XML。
语音 / NAS 链路按既定必须做到未审查、未改动（被动命中的两条见「三、确认但未修」）。
详细报告：`docs/security-review-2026-09-20-前述.md`。

## 一、确认并修复的缺陷（按严重度排序）

### P1 · 可选词库下载无完整性校验，可注入任意候选词，已修

- 位置：`OptionalDicts.ALL` 无摘要字段 → `DictManagerActivity.fetchToFile()` 写完 `.tmp` 直接 `renameTo` 成 `.xz` → `PinyinEngine.loadExtensionDict()` 只按后缀扫描。
- 触发条件：Release 附件被替换、`followRedirects(true)` 指向的主机返回别的内容、传输截断后仍拼出合法 xz，任一能改字节的环节即成立。
- 成因：整条链路没有「这份字节是不是我要的那份」的判据；`copyCapped` 的 64MB 上限只挡体积不挡内容。
- 验证依据：调用链追到加载侧确认无任何校验；实测 curl 从 GitHub 与 Gitee 各下一次，两端字节一致，故可硬编码单一摘要：
  `ext.xz` = `f831f41101b555d2f7a54648b3cd3507b55333a7637914a65882c678cce8e5b1`（1,897,940 B），
  `opt_tencent.xz` = `622b0ea87fb08afd87eadf72e055a8377e2f008da20e1c2a3d819599302146ea`（6,668,612 B）。
- 影响：词库内容直接变成上屏到任意输入框的候选词，攻击者无需任何权限即可长期替换用户常用拼音的候选；附带解压炸弹风险（`PhraseIndex.build` 无输出上限 → IME 进程 OOM）。
- 修复：`OptionalDict` 新增 `checksum`，新增 `OptionalDicts.sha256Of` / `matchesChecksum`（纯函数，空串与非法摘要一律拒绝），`fetchToFile` 在 rename 之前校验、不一致即丢弃。新增 `OptionalDictChecksumTest` 7 例。
- 关联：关闭上一轮「遗留」第一项。

### P2 · 暂存粘贴重试时丢失输入框身份，可跨字段注入，已修

- 位置：`JinnIme.flushPendingPaste()` 的「连接仍无效，继续暂存」分支只恢复 `pendingPasteText`、没恢复 `pendingPasteFieldKey`。
- 触发条件：无有效连接时点剪贴板条目 → 首次 flush 仍无连接 → 此后 10 秒 TTL 内任意输入框聚焦都会命中提交。
- 成因：`target == null` 会让 `if (target != null && current != null && target != current)` 判据短路，把 `RecentFixesRegressionTest` 钉住的「暂存粘贴只回原输入框」架空。
- 影响：剪贴板正文（常含口令、验证码）被静默提交到无关输入框。
- 修复：放回暂存时一并恢复 `pendingPasteFieldKey = target`。

### P2 · 词库「删除」与「下载」可并发，已修

- 位置：`DictManagerActivity.remove()` 缺下载中闸门（闸门只加在 `download()` 与按钮 `enabled` 上）。
- 触发条件：下载进行中点删除 → 下载线程完成后照样 rename 回来，界面提示「已删除」但包仍在。
- 修复：`remove()` 开头判 `downloading != null`，Toast 提示（新增 `dict_remove_busy`）后返回。

### P2 · `data_extraction_rules.xml` 漏 SQLite 侧车文件，防火墙审计给的是假 ✓，已修

- 位置：`cloud-backup` / `device-transfer` 两段只排除主库（`backup_rules.xml` 有 `-journal/-wal/-shm`）；`ClipboardFirewall.checkBackupConfig` 的判据是 `path.contains(DB_NAME)`，漏项一条都抓不到。
- 影响：WAL 模式下最近提交的页在 `-wal` 里，只排除主库等于让最新那批记录随备份/迁移走；审计页仍显示「已排除」。
- 修复：两段各补三条侧车 exclude；`checkBackupConfig` 改为按分段解析，必需清单升级为 `REQUIRED_EXCLUDES`（库 + 三侧车 + `user_freq.txt`），任一段缺任一条即判不合格并列出缺失路径。

## 二、审查过但判定非缺陷（留档）

SQL 注入（全参数绑定 / `LIMIT` 只插值 Int）、剪贴板加密（GCM + 失败返回 null 无静默降级）、`su` 命令注入与路径穿越（拼接片段仅来自 `dataDir`/uid/包名/固定命令）、导出组件（三个组件权限齐备、无 Provider）、`PhraseIndex.parse` 越界（Long 累加 + 精确总长校验）、并发可见性（跨线程字段均 `@Volatile`）、仓库敏感文件（无密钥入库）。

## 三、确认但未修（禁改域 / 用户决策）

- P1 语音识别正文以 I 级落盘：`JinnIme.handleResult` 的 `Diagnostics.i(... text.take(60) ...)` 与 `AsrClient` 解析失败分支的 `raw=${text.take(200)}`。全仓扫 `i/w/e` 级插值，其余命中项都只记长度/分类/计数，只有这两处是正文。落在识别链路禁改域，改法是一行降为 `Diagnostics.v`，需用户拍板。
- P3 诊断日志落共享存储：Android 10+ 分区存储下普通应用读不到，且是排查能力来源，维持不动。

## 四、验证

- `compileDebugKotlin` 通过，告警仍为既有 `Lateinit` 一组，无新增。
- `testDebugUnitTest`：32 类 / 311 例全绿（新增 `OptionalDictChecksumTest` 7 例）。
- `lintDebug`：0 error / 29 warning，与基线一致。
- `AGENTS.md` 的测试覆盖数字与新增用例同步更新。

## 五、遗留

- 语音识别正文日志待用户决策。
- 引擎加载期不校验已落盘词库（校验点在下载侧）：存量 `.xz` 仍按后缀加载，要收紧会在加载路径多一次全文件 IO，属性能取舍。

---

# 源码安全审查（前述）与修复（2026-09-20）

日期：2026-09-20 ｜ 范围：全量 Kotlin 源码 + `AndroidManifest.xml` + 备份规则 XML + 符号表数据。
语音 / NAS 链路按既定必须做到未审查、未改动（被动命中的两条见「三」）。
详细报告：`docs/security-review-2026-09-20-前述.md`。
本次方法：全量通读 + 每条结论追到真实消费者 + 数据表脚本全量扫描 + 两名独立审查员对候选结论二次核验（并发可达性一名、键盘视图一名）。

## 一、确认并修复的缺陷（按严重度排序）

### P2 · 长按退格「整串清拼音」的判据用错基准 → 12~27 字符会继续删已上屏正文 — 已修

- 位置：`PinyinKeyboardView.backspaceRepeatRunnable`（新增 `composingLenAtBackspaceDown`）。
- 触发条件：按住删除键不放，且按下时拼音串长度为 12~27。
- 成因：连删循环每 tick 先判门槛再删一位（380ms 起每 55ms）。判据 `shouldClearComposingOnHold(held, composing.length)` 取的是当前长度，而到 1200ms 门槛时循环已自己删掉约 15~16 位，「≥12 字符」永远晚一步。反推实际门槛是初始 ≥28 字符，与注释/常量写的 12 相反。
- 影响：12~27 字符的串在门槛到达前就被删空，分支永不命中，`deleteOne()` 落到 `listener.onBackspace()` → IME 持续发 `KEYCODE_DEL` → 连输入框里已上屏的正文一起被删，不可撤销。与「只整串清拼音、不动已上屏」的手势规则正好相反。
- 验证依据：tick 时序独立复算 + 独立审查员给出同一结论（实际门槛 ≈28 字符）；新增回归用例 `RecentChangesParityTest.长按清空必须以按下时的长度判据`（模拟 tick 序列，钉住「用当前长度判 → 12 字符串被删空仍为 false」）。
- 修复：`ACTION_DOWN` 时记录按下长度，循环内改用它判据；纯函数语义不变，既有边界矩阵用例全部保持有效。

### P3 · 连删延时任务无生命周期收尾（全仓无 `onDetachedFromWindow`）— 已修

- 位置：`PinyinKeyboardView`（新增 `onDetachedFromWindow`）。
- 成因：`backspaceRepeatRunnable` 是自我重排的 55ms 循环，唯一停止条件是 `ACTION_UP`/`ACTION_CANCEL` 复位 `backspaceHeld`；视图被 detach 时 Android 不保证补发 CANCEL。
- 影响：按住删除键期间视图被销毁 → 循环永久运行：持有已销毁视图 + 每 55ms 回调 `Listener.onBackspace()`（用户没碰键盘，字却在被删）。
- 修复：`onDetachedFromWindow` 内 `removeCallbacksAndMessages(null)` + 复位 `backspaceHeld` / `backspaceTapCount`。

### P3 · 音节前缀集合「就地 clear + 逐个 add」，与打字热路径并发 — 已修

- 位置：`PinyinEngine.syllablePrefixes` / `buildSyllablePrefixes()` / `resetForTest()`。
- 成因：写方是加载线程（`load()` 与 `loadOptionalAsync() → loadExtensionDict()` 都会走 `finalizeLoad()`），而 `loaded = true` 在第一段加载后随即发布 ⇒ 用户正在打字；读方 `isTruePrefixOfSyllable` 在热路径上（分词 / 伪完整音节判定 / 补全召回）读它且不加锁。就地清空重建会让读方看到「已清空但没填回」的中间态。
- 影响：窗口期内判据返回 false → 分词改走贪心分支、末尾补全召回整段跳过 → 偶发候选变少 / 切分不同（不崩溃，下一键自愈）。窗口真实出现两次，其中可选包加载那次是真机实测 21~34s 的后台重活。
- 验证依据：独立审查员逐条核实写方 / 读方 / 无互斥；同文件 KDoc 本就禁止「原地 clear+addAll」，本处是唯一漏网。
- 修复：先建 `HashSet` 再整体替换 `@Volatile` 引用（与同文件有序表同款写法）。

### P3 · 补全缓存用裸 `HashMap` — 已修（纵深防御）

- 位置：`PinyinEngine.completionCache`。
- 说明：调用链当前封闭在主线程，尚不可复现；但同文件其余 5 个缓存都是并发容器，注释还点名说它与它们同款。声明处对齐，避免将来被后台路径复用即成为「读 get 撞写扩容 → 桶成环卡死」那一类事故。

### P3 · `Diagnostics.dumpLogcat(suffix)`：suffix 同时进文件名与 shell 命令 — 已修

- 位置：`Diagnostics.dumpLogcat`。
- 两处成因：`sh -c "logcat … > '${dest.absolutePath}'"` 原样拼接（一个单引号即可改写命令）；`File(dir, "logcat-$suffix.log")` 带 `../` 可越出日志目录。
- 验证依据：全仓调用方只有一处字面量 ⇒ 当前不可达；上一轮清单核过 `su` 命令的拼接片段，但这条 `sh -c` + 文件名组合不在清单内。
- 修复：改为不经 shell 的 `ProcessBuilder(...).redirectOutput(dest)`（`redirectErrorStream(true)` 等价原 `2>&1`，补超时 `destroy()`），suffix 另做字符白名单过滤。

### P3 · `user_freq.txt` 的 NaN / Infinity 权重未被过滤 — 已修

- 位置：`UserFrequency.parse`。
- 成因：`"NaN".toDoubleOrNull()` / `"Infinity".toDoubleOrNull()` 都不是 null（Kotlin 的取值筛选用正则显式放行这两个字面量），而 NaN 与任何数比较恒为 false ⇒ `weight <= 0.0` 与 `decayed < MIN_WEIGHT` 同时失效。
- 影响：`Double.compare` 把 NaN 当最大值 → 该词永久霸占对应拼音的首候选（空格取首候选 = 一直上屏它），并被 `render()` 原样写回文件、不会自愈。
- 修复：判据补 `!weight.isFinite()`；新增 `UserFrequencyTest.非有限权重被当作脏数据丢弃`（NaN / +Inf / −Inf 三条）。

### 数据 · 「特殊」组天气页混入汉字「由」「白」— 已修

- 位置：`KeyboardLayouts` 的「特殊」组第 2 页 `k` / `l` 键。
- 验证依据：脚本按括号配平解析全部 10 组 48 页，只有这一页出现非本类字符（其余页面 26 键齐全、无重复、无越界键；唯一其他汉字页是「序号」组第 7 页的中文数字，属设计）。
- 修复：k → `♃`（木星）、l → `♅`（天王星），与同行 `☿` / `♄` 同属 U+263F~U+2647 行星符号块；新增护栏 `SymbolLayoutTest.特殊组天气页不得混入汉字`。
- 符号表字形需真机确认（本机无可用真机）。

## 二、审查过但判定非缺陷（留档）

`PhraseIndex.parse` 越界 / 溢出（Long 精确总长 + 段首尾对齐双向自洽）、`Prefs.isValidHost`（黑名单 + 真实消费者双判据、取值方用返回值）、`ClipboardDb` SQL（参数绑定 + `LIMIT/OFFSET` 只插值 Int）、剪贴板加密（GCM + 失败统一 null）、`deleteAllText`（系统钳位）、导出组件（`SettingsActivity` 新增确认不读 `intent` extra；无 Provider / WebView / DexClassLoader / PendingIntent）、`TextSelection` 边界（恒落 `[0, text.length]`）、`KeyAppearance` 换算、上一轮已修条目复核（仓库无密钥、备份规则两段齐备、下载摘要 rename 前校验、配置广播 `NOT_EXPORTED`）、`renameTo` 前先 `delete()`（Windows `renameTo` 不覆盖导致的历史写法，罕见路径，收益不足不改）、「单位」组第 2 页 ②~⑧ 补位（有意为之）。

## 三、确认但未修（禁改域 / 用户决策）

- P1 语音识别正文以 I 级落盘：`JinnIme.handleResult` 的 `text.take(60)`、`AsrClient` 的 `raw=${text.take(200)}`。本次重扫全仓 `i/w/e` 插值，其余命中项都只记长度 / 分类 / 计数，只有这两处是正文。禁改域，待用户拍板。
- P3 引擎加载期不校验已落盘词库：植入该文件本身需要 root（已越过本应用的安全边界），收紧还牵动「不匹配是否删除用户已装包」，属取舍，暂不动。
- 诊断日志落共享存储：维持不动。

## 四、验证

- `compileDebugKotlin` 通过，告警仅既有 `Lateinit` 一组（17 条）+ `TextSelectionTest` 未用参数（1 条），无新增。
- `testDebugUnitTest`：32 类 / 314 例全绿（311 → +3：词频非有限权重、符号表汉字回归、长按清空判据基准）。
- `lintDebug`：0 error / 29 warning，与基线逐条一致。
- 修复脚本（临时，已删）：9 处替换均要求原文「恰好出现 1 次」，`--check` 预检 + 应用后 grep 复核通过。

## 五、遗留

- 符号表 `♃` / `♅` 与长按清空新基准需各做一次真机复核。
- 上一轮两条（语音正文日志、引擎加载期校验）仍挂在「确认但未修」。
- 工程提示（沿用）：同一文件的多处改动不要并行编辑（并发 Edit 会互相覆盖）。本次 `PinyinKeyboardView.kt` 的 5 处、`PinyinEngine.kt` 的 4 处改为脚本文件内一次性应用 + 逐处唯一性断言，正是为规避这一点。

---

# 源码安全审查（前述）与修复（2026-09-20）

日期：2026-09-20 ｜ 范围：全量 Kotlin 源码（31 文件 / 12,712 行）+ Manifest + 备份规则 XML + 词库/索引资产 + 双拼码表 + 打包脚本。
语音 / NAS 链路按既定必须做到未审查、未改动。
详细报告：`docs/security-review-2026-09-20-前述.md`。
本次方法：全量通读 + 三名独立审查员分域并行（剪贴板持久化 / 词库配置 / 键盘 IME），
候选逐条回源码复核（剔除 1 项误判、降级 1 项） + 数据表脚本全量扫描。

## 一、确认并修复的缺陷（按严重度排序）

### P2 · 搜索态下五个入口仍把内容落到宿主输入框 — 已修

- 位置：`PinyinKeyboardView` 的回车 / 逗号 / 句号 / `onTripleBackspace` / `commitComposing`。
- 成因：搜索面板（`isPanelSearch()`）的设计前提是「26 键只作用于搜索框」，字母/符号/数字/空格/退格/候选/预测七处都做了路由，但五个入口漏了。
- 影响：回车 → 把搜索用的原始拼音串写进正文，或在宿主声明 `SEND` 时直接发出消息；逗号句号 → 全角标点写进在编辑的正文；「双击+长按清空」→ 调用 `onDeleteAll()` 清空宿主输入框全部文本；`commitComposing()`（语言键切换 / IME 收起）→ 把搜索词首候选上屏到宿主。
- 验证依据：`grep` 全量枚举 30 个 `listener?.on*` 回调点逐条核对守卫；`JinnIme.performEnter()` 实现；同文件既有反向注释（「漏掉这个分支会把文本直接提交到宿主」）。
- 修复：回车＝退出搜索（`hideSearchPanel()`）；逗号/句号＝`appendSearch()`；清空手势＝新增 `SearchPanelView.clearSearch()`；`commitComposing()` 搜索态改为丢弃。

### P2 · 长按连删在「拼音被自己删空」后仍继续删已上屏正文 — 已修（修复的残余）

- 位置：`PinyinKeyboardView.backspaceRepeatRunnable`（新增纯函数 `shouldStopRepeatOnExhaustedComposing`）。
- 触发条件：按住退格，按下时拼音串长度 12~15。
- 成因：门槛 tick 落在 1205ms（DOWN 1 位 + 380ms 起每 55ms），门槛前共 16 次删除；按长度 L 的串在第 L 次删除后即为空 ⇒ L ≤ 16 都在门槛前被删空，其中 12 ≤ L ≤ 15 正是「只清拼音」的区间，循环继续走 `deleteOne()` → `listener.onBackspace()` → 删掉已上屏正文（删掉字符数 = 16 − L）。
- 验证依据：时序独立复算 + 新用例 `RecentChangesParityTest.长按连删在拼音删空后不得继续删已上屏`（逐档断言，含 L=11 必须仍删已上屏的反向断言）。
- 修复：循环内补「删空即停」闸门（判据同样用按下时长度）。

### P2 · 自身剪贴板标记被三处「粘贴」误置 → 真实复制被静默丢弃 — 已修

- 位置：`ClipboardController.ownCommit` / `onOwnCommit()` / `OWN_COMMIT_WINDOW_MS` + `JinnIme` 三处粘贴路径。
- 成因：该标记的前提是「本 IME 刚写过系统剪贴板」，但全仓唯一的 `setPrimaryClip`（`copySelection`）不设标记；三处设标记的粘贴路径都只 `commitText` 到宿主、不写剪贴板 ⇒ 标记只能被监听侧当成「下一次变化是自身的」吞掉一次。
- 影响：粘贴后 3 秒内的真实复制被静默丢弃，历史里查不到（「静默少给」类）。
- 验证依据：`grep setPrimaryClip|onOwnCommit` 全仓 1:3 的分布 + 监听侧分支逐行确认。
- 修复：删掉三处误置调用与只被它消费的整套机制，原位留注释说明「本类是唯一入库入口，IME 的 setPrimaryClip 本就期望入库」。

### P2 · `imeOptions` 声明的「不要个性化学习」未参与敏感判定 — 已修

- 位置：`InputFieldPrivacy.suppressLearning`（新增 `imeOptions` 形参）+ `JinnIme.onStartInputView` 传入。
- 影响：用 `IME_FLAG_NO_PERSONALIZED_LEARNING` 声明「不要学」的输入框（密码管理器/银行类 App 拿不到 password 变体时的标准做法）仍会被学进 `user_freq.txt`。
- 验证依据：取到的 `imeOptions` 只用于回车动作与键面；本类 KDoc 自述职责包含「显式声明不联想的框」；新增用例两个方向钉住。
- 实测：imeOptions 侧没有公开的「不要联想」常量（`IME_FLAG_NO_SUGGESTIONS` 是 @hide，引用即 `Unresolved reference`），代码与注释已按此订正。

### P3 组 · 同批修复 8 项

1. 崩溃快照落 V 级正文（`Diagnostics.dumpLogcat`）：快照抓整个 logcat 缓冲区，V 行（搜索关键词/拼音串/候选词/测试框文本）在里面 ⇒ 「V 默认不落盘」被崩溃路径绕过、导出诊断包还带走。新增纯函数 `filterOwnVerboseLines` 剔本进程 V 行（续行跟随），落盘后再过滤（保持先落盘再处理，避免读写管道互相等死）。新增 `DiagnosticsLogcatFilterTest` 4 例。
2. `ClipboardDb.onCreate` 缺 `content_hash` 唯一索引：只在升级路径建 ⇒ 两条安装路径 schema 不一致、库层去重缺位、入库查找退化全表扫描。补建。
3. `ClipboardFirewall` 审计假 ✓：`bySection` 为空（规则里一条 exclude 都没有）时 `any{}` 恒 false ⇒ 把「什么都没排除」判成全部排除。空集合改判为「全部缺失」。
4. 面板点击按下标取条目（`ClipboardPanelView` + `SearchPanelView`）：类头声明「用稳定 itemId、不依赖 position」，实现却是 `currentItems.getOrNull(pos)`，`Holder.itemId` 写而不读 ⇒ 刷新与点击同帧时可能粘错条目。两处改为「取被点中那行渲染时写入的稳定 id → 按 id 找 → 才退回下标」。
5. `ClipboardPrefs.maxItems` 读侧不钳位：写侧钳、读侧不钳，而 `trimByCount` 在 `maxItems <= 0` 时直接 return ⇒ 条数上限形同虚设。读侧 `coerceIn(1, 9999)`。
6. `Prefs.defaultKeyboardMode` 读侧不兜底：越界值让 `when` 落到 `else -> VOICE` ⇒ 默认键盘变语音键盘。读侧兜底 `PINYIN_CN`，写侧一并钳位。
7. 数字分类下界标准不一致：上下文规则要求 `\d{6,}`，单串验证码是 4-8 位 ⇒ 「验证码：1234」进 OTHER、不带标签的「1234」进数字。下界改 4 位 + 两侧用例（含「第 1234 章」→ OTHER 防误扩）。
8. `completeSyllablePrefix` KDoc 说「所有」实际按字典序截断 32：以 z/c/s 开头的音节有 35~37 个，尾部（zou/zuo/cuo/suo…）永不成为补全候选。KDoc 改为如实描述，不改上限（与候选数量测试相关，属用户决策）。

## 二、审查过但判定非缺陷（留档）

双拼码表 7 套脚本全量扫描（零重复键 / 零声母错位 / 零表外音节；不可达音节恰为既有固有边界集合）、键盘布局与 `DIGIT_MAP`、音节表 422 条、三处词库解析的 `tab > 0` 守卫、`Shuangpin.toQuanpin` 的 `substring(i, i+2)` 越界不可能、`consumption`/`consumePinyin` 的删除区间、备份规则三段齐备、全仓 `i/w/e` 插值重扫（无正文）、剪贴板加密/SQL/分页/搜索驻留、`build_apk.py` 的口令走 `env:`。

## 三、确认但未修（登记）

- `junding` 这类 7 字符串伪音节会短路它自己的前缀（键入 `jund`/`jundin` 候选栏空）：修法要改词库资产并重跑 `convert_rime_ice.py` → `gen_hot_dict.py` → `build_dict_index.py`，而 `pinyin_syllables.txt` 还是 7 套双拼表的生成源（要连 `ShuangpinSchemes.kt` 一起重生成）；该族已明示「固有边界（不修）…… 别再微优化」。
- `common_chars.txt` 三个非 BMP 字（𠳐/𥻗/𬁼）永不被放行：改资产不改变实际行为，改代码是功能变更，均不划算。
- 引擎加载期不校验存量词库 + 索引构建失败回退路径二次解压（峰值翻倍），既有取舍。
- `content_hash` 是明文 SHA-256（低熵内容可离线比对），需先拿到库文件（root），既有边界之外。
- 语音识别正文以 I 级落盘（禁改域，待用户拍板；本次复核仍存在）。

## 四、验证

- `compileDebugKotlin` 通过，`w:` 21 条 = 既有 `Lateinit` 20 条（`PinyinKeyboardView` 17 + `SearchPanelView` 3）+ `TextSelectionTest` 未用参数 1 条，无新增。
- `testDebugUnitTest`：33 类 / 320 例全绿（314 → +6，新增 1 个测试类）。
- `lintDebug`：0 error / 29 warning，与基线逐条一致。
- 改动：13 个 main + 4 个 test 文件，35 处替换，两个一次性脚本（临时文件，已删），每处先断言「原文恰好出现 1 次」再应用、应用后 grep 复核；同一文件的多处改动都在同一次脚本内完成。
- 本次新写的过滤函数首版被自己的用例当场抓住：`lineSequence()` 会给以换行结尾的输入多补一个换行，改用 `split('\n')` + `joinToString` 后全绿。

## 五、遗留

- 需真机复验三处（本机无可用真机）：12 字符串按住退格 1.2s 只清拼音；搜索态回车只退出搜索、不触发宿主「发送」；搜索态「双击+长按」只清搜索框。
- 沿用前述遗留：符号表 `♃`/`♅` 字形、长按清空基准复验。

---

# 源码安全审查（前述）与修复（2026-09-20）

日期：2026-09-20 ｜ 范围：全量 Kotlin 源码 + Manifest + 备份规则 XML + 词库资产 + 打包脚本。
语音 / NAS 链路按既定必须做到未审查、未改动。
详细报告：`docs/security-review-2026-09-20-前述.md`。
本次方法：三名独立审查员分域并行（进程/文件/网络边界、剪贴板面板与库层、引擎与渲染）+ 主审逐条回源码
复核（剔除 1 项误判、更正 2 项结论）+ 资产类结论用脚本重跑核对（不采信审查员的脚本结论）。

## 一、确认并修复的缺陷（按严重度排序）

### P2 · 长文档下光标移动与拖选按错坐标算 — 已修

- 位置：`JinnIme.currentSelectionRange` / `moveCursor` / `extendSelection`；新增 `TextSelection.toWindowOffset`。
- 成因：`ExtractedText.selectionStart/End` 是全文绝对下标，而 `text` 在长文档下可能只是光标附近的一段
  窗口（`startOffset` 才是窗口起点）。旧实现把两者当同一坐标系：拿绝对下标索引窗口文本，
  `moveLine`/`lineEnd` 越界后返回「窗口长度」那个绝对下标，光标表现为跳到毫不相关的位置；
  算出的位置又直接 `setSelection`，宿主按绝对下标解释。
- 与前几轮的关系：`copySelection` 早遇到过同一问题（注释自证）并只给复制路径加了钳位；
  平台为此提供的 `ExtractedText.startOffset` 全仓从未被读取。
- 验证依据：`grep getExtractedText`（仅 2 处调用）+ `startOffset` 零读取；新增三条用例（整篇返回时换算
  恒等 ⇒ 对常见路径零行为变化、窗口内映射、窗口外返回 null）；复核 `selectAllText`/`deleteAllText`
  走 Ctrl+A 键事件不受影响（排除同族第二处）。
- 修复：`currentSelectionRange` 读 `startOffset` 并校验端点落在窗口内（否则放弃本次操作），
  `moveCursor`/`extendSelection` 全程按窗口内下标算、输出前加回 `startOffset`。

### P2 · 拖选 Anchor/Focus 在宿主改动选区后不失效 — 已修

- 位置：`JinnIme` 新增 `onUpdateSelection` 覆写 + `lastSetSelection` 期望值 + `applySelection`；
  新增纯函数 `TextSelection.isExternalSelectionChange`。
- 成因：拖选进入后，同一输入框内点别处移动光标（键盘不收起），IME 完全不知道；旧 Anchor 仍在用，
  再按方向键就会 `setSelection(min(旧Anchor, Focus), …)` 把选区拉回旧 Anchor。
  平台为此提供的 `onUpdateSelection` 全仓未实现。
- 影响：复制到别处的内容；带选区继续输入会把错误区间替换掉。
- 修复：实现 `onUpdateSelection`，用一次性期望值区分自身/外部改动，外部改动即退出拖选（并记日志）；
  同时给 `extendSelection` 补 Anchor/Focus 有效性闸门（负下标会让宿主 `Editable` 抛异常，
  键盘侧存在一条「未记 Anchor 却置拖选态」的不可达分支，将来接线会踩到）。

### P3 · 崩溃日志快照的隐私过滤可被失败路径绕过 — 已修

前述加的「剔掉本进程 V 级行」只作用在成功路径：抓取失败时留下半截且未过滤的快照（里面有 V 级正文：
搜索关键词/拼音串/候选词/测试框文本），而 `cleanupOldLogs` 只按时间清 7 天、`exportBundle` 会整体打包。
失败分支改为先删文件再返回。

### P3 · root 授权超时未销毁 `su` 进程 — 已修

`waitFor(5s) && exitValue() == 0` 超时后不 destroy，也没排空三个管道。同文件 `dumpLogcat` 与
`ClipboardFirewall.su()` 都有显式收尾（后者还加了排空线程），本处是漏网。改为超时即 `destroy()` + 记日志。

### P3 · 诊断导出包非原子写 + 设备信息文件永久残留 — 已修

直接写最终文件（失败/被杀留下半截 zip，而导出包正是要拿去排查的）、时间戳只到秒（同秒两次导出撞同一路径）、
`device-info.txt` 写进日志目录后从不清理（`cleanupOldLogs` 只按 `jinn-`/`logcat-` 前缀清）。
改为临时文件（名带纳秒）+ 原子改名，`finally` 清理临时文件与设备信息文件。

### P3 · 备份规则审计在「整段缺失」时仍给 ✓ — 已修

判据只比对已存在的段：`<device-transfer>` 整段被删后，剩余 `cloud-backup` 仍齐备 ⇒ 给 ✓，
而 Android 的语义是「整段缺失 = 该模式不做任何排除」。与函数自己「每一段都要齐」的注释相反。
改为先判段齐备性（扁平结构的 `backup_rules.xml` 退化成单一空名段），缺段即报全部必需项缺失。

### P3 · 搜索面板与剪贴板面板可同屏（互斥不对称）— 已修

`showDirectionPanel` 里做了反向互斥（剪贴板），`showClipboardPanel` 却没处理顶部搜索面板；
两个面板结构上互不遮挡，状态能同时成立 ⇒ 同屏两个列表，且底栏按键仍被 `isPanelSearch()` 路由到搜索框
（作用对象与画面不符）。补 `hideSearchPanel()`。

### P3 · 剪贴板面板：确认条与操作条不互斥 / 切分类不清操作条 — 已修

`showClearConfirm` 会收起操作条，`showItemMenu` 却不收起确认条（两条按钮挨着）；
`selectCategory` 不收起操作条 ⇒ `longPressItem` 仍指向切分类前那条（可能已不在列表里），
点「删除/收藏」作用在看不见的条目上（删除后不知道丢的是哪一条）。两处各补一行。

### P3 · 合并结果缓存可被「变更前的旧结果」永久粘住 — 已修

`phrasesFor` 无条件回写 `mergedCache[key]`，而加载线程有五处 `clear()`；「读快照 → 清缓存 → 回写旧结果」
的交错成立后，该键一直返回变更前的候选（只有缓存超 4096 条或重启进程才能自愈）。
新增 `@Volatile dataGeneration` + 统一失效入口 `invalidateMergedCache()`（五处失效点全部改走它），
回写前比对世代号。

### P3 · 候选→拼音键映射「首写锁定」，一词多音时消费与预测按错的键算 — 已修

`noteCandidateKeys` 用 `putIfAbsent`：同一个词挂在多个拼音键下时第一次记录的键永久粘住。
换拼法打到同一个词时，`consumption` 落空 → 走「消费全部」兜底（残码被整段清掉）、`predict` 扫错键区。
独立脚本核对：仅高频子集就有 79 个一词多键（如「朝阳」= chaoyang/zhaoyang、「不了」= bule/buliao）。
改为最新写入者胜。

### P3 · 单字表的长度判据只在生僻字分支生效 — 已修

`if (commonChars == null) chars else chars.filter { it.length == 1 && … }`，长度判据写在过滤分支里，
显示生僻字时失效，资产里的非单字符 token 会进单字表被当成单字候选上屏。
独立脚本核对：`pinyin_chars.txt` 17864 个 token 中非单字符恰好 1 个（`junding` 行的「均订」）。
改为长度判据无分支生效。副产物：`CandidateCountBoundTest` 的假单字表用了 `字1`…`字70`（每个 2 字符），
与真实表不符，被本次改动挡下（`实际=0`），已把假数据改成真正的单字符（U+4E00..U+4E45），测试更严格。

### P3 · 键盘触摸：`ACTION_POINTER_UP` 复位「已消费」标记 → 翻页后仍上屏符号 — 已修

`keyTouchConsumed` 是手势级判据（符号层滑动翻页后不许上字），多指时第二根手指抬起就把它复位，
手势还没结束 ⇒ 剩下那根手指 `ACTION_UP` 时把新页的符号上屏。改为只在
`ACTION_DOWN`/`ACTION_UP`/`ACTION_CANCEL` 复位。

### 文档 · `UpdateChecker.readCapped` 引用了不存在的常量 — 已修

`[MAX_BODY_BYTES]` → `[MAX_BODY_CHARS]`，并补上「截断按行判定」的语义（审查员怀疑会误报「已最新」，
复核不成立：返回空串会让两源都判失败 → `NetworkError`）。

## 二、审查过但判定非缺陷（留档）

引擎跨线程标志全部已 `@Volatile`（逐条查声明）；`loadOptionalAsync` 的检查-置位在锁内且收尾调
`finalizeLoad()`；`JinnIme` 三处接收器在 `onDestroy` 全部注销；`selectAllText`/`deleteAllText` 走 Ctrl+A
不受窗口化影响；`executeDirection` 的 `when` 覆盖全部取值；`phrasesFor` 的 4096 上限与查询同线程；
前述五项修复本次复读仍在位。

## 三、确认但未修（登记）

- 收藏数 ≥ `maxItems` 时新复制的内容会被立刻裁掉、而 `save` 仍返回条目 id：与 KDoc「剩的全是收藏即停手」
  以及体积路径已被测试钉住的同一原则一致，需 ≥500 个收藏才可达，仅登记标准，不改行为。
- `Diagnostics.init` 在主线程探测目录 + `su`（既有取舍）；`BackgroundIo` 队列无界、
  `DictManagerActivity` 的 `.tmp` 残留、`ClipboardFirewall` 的 `-maxdepth 4`/`u0_a` 判据（不可达或低价值）。
- 前几轮遗留：`junding` 伪音节、`common_chars.txt` 非 BMP 字、引擎加载期不校验存量词库、
  `content_hash` 明文 SHA-256、语音正文日志（禁改域）。

## 四、验证

- `compileDebugKotlin` 通过，`w:` 21 条与基线一致，无新增。
- `testDebugUnitTest`：33 类 / 323 例全绿（+3 例）。
- `lintDebug`：0 error / 29 warning，与基线逐条一致。
- 资产核对脚本（临时，已删）：单字表 17864 token / 非单字符 1 个；高频子集 34515 键 / 39921 词 / 一词多键 79 个。
- 修复应用：三个一次性脚本（临时，已删），29 处替换，每处断言「原文恰好出现 1 次」。
- 工程提示（本次新增，务必沿用）：
  1. 修复脚本必须幂等：逐条写盘 + 中途计数不符中止会留下半应用状态（本次第一条已落盘、第二条中止），
     判据是「目标文本已存在即跳过」；只看 `old` 的计数对「插入型改动」无效（插入后 `old` 仍在文件里，
     第一次修订版就因此差点重复插入）。
  2. 改动判据后先跑门禁再写报告：本次门禁抓到测试假数据与真实资产规则不符（`字1` 这类 2 字符被当单字）。

## 五、遗留

- 需真机复验：长文档里方向键移动光标与拖选应落在预期位置；拖选中点别处应自动退出拖选；
  符号层多指翻页不应上屏符号。
- 前述四项真机项（12 字符串按住退格只清拼音、搜索态回车只退出搜索、搜索态清空手势只清搜索框、
  符号表 `♃`/`♅` 字形）仍未复验。

---

# 发版：20260920（2026-09-20）

产物：`jinn-release.apk`（5,196,626 B）｜ sha256 `4d1b886d8f3d10019e6e79aa98146ab6b0b8fc0e0bf1c9e3a08ec94b60ae43ca`
代码：`27621ae`（含第七十七~八十二轮全部修复；39 个文件、+2341 −340）
tag：纯数字 `20260920`（首次使用纯数字 tag；历史 6 个 `v*` tag 的重建与附着包重传仍挂「未执行」）

## 一、发布前验证（全部通过）

- `compileDebugKotlin` 0 error（21 条告警均为既有：`Lateinit` 20 + 测试未用参数 1，无新增）
- `testDebugUnitTest` 33 类 / 323 例，失败 0 / 错误 0 / 跳过 0（`clean` + `--no-build-cache` 强制重跑，
  排除构建缓存命中造成的「假绿」）
- `lintDebug` 0 error / 29 warning（与基线逐条一致）
- 打包：AGP → 去 META-INF → zipalign -p 4 → apksigner → 校验；V1=false / V2=true / V3=true；4 字节对齐通过
- 证书 `CN=Jinn, OU=Dev, O=yezijinn`（SHA-256 `818e8c9b…`）与设备上旧版同签名 → 可直接覆盖升级
- 依赖与配置：manifest 3 个组件与源码一一对应（无悬空引用）；`res/xml` 只有 method / backup_rules /
  data_extraction_rules 三个文件；`strings.xml` 125 条无原始换行；仓库内无密钥与口令文件、源码无硬编码凭据；
  可选词库的两端下载源 HTTP 200 且实测 sha256 与内置摘要逐字节一致
- 构建配置：移除 5 条指向已删类（`KeepAliveService` / `JinnAccessibilityService` / `BootReceiver` /
  `rikka.shizuku.`）的 R8 规则，指向不存在的类不会报错，但会让发布配置与实际组件清单脱节

## 二、真机验证（PACM00 / Android 10 / SDK 29 / KernelSU）

- 安装：`adb push → /data/local/tmp` + `su -c 'pm install -r'` 成功；versionCode 20260919 → 20260920（数据保留）
- 启动链路：IME 服务创建 → 键盘视图创建 → 打印 `拼音键盘坐标` → 词库加载完成
  （音节 414 / 基础键 604077 / 运行时反查 39921 / 合法音节 422 / 生僻字隐藏；两段式 3083ms）
- 输入链路：字母键（坐标由 app 自己打印的键盘坐标推算，不盲猜）→ 候选 `[你, 尼, 泥]` → 空格取首候选 →
  宿主输入框出现「你你」
- 方向面板与拖选：面板显示 → 中心 ● `拖选激活` → `拖选扩展` → 在宿主里点别处移动光标 →
  `宿主改动选区[0,0]，退出拖选（Anchor/Focus 已失效）`（修复的真机证据）→ 再按方向键走
  `光标移动: 窗口内 0 → 1（offset=0）`（拖选确已退出；整篇返回时换算恒等，常见路径零行为变化）
- 剪贴板：全选 + 复制 → `save: 已保存`；粘贴后 1.7 秒内的真实复制同样入库
  （前述移除「自身标记」的差分验证，旧实现会在这里静默吞掉这次复制）
- 剪贴板面板：打开 → `DB category=ALL total=1 page=1`（解密读取正常，R8 包无反射/加密回归）
- 全程无 FATAL / ANR / E 级日志

## 三、双端发布与校验

| 项 | GitHub | Gitee |
|---|---|---|
| tag | `20260920` | `20260920` |
| Release | `20260920`（Latest，未覆盖历史 Release） | id=1154658 |
| 附件 | `jinn-release.apk` 5,196,626 B | 同名同尺寸 |
| 下载实测 | HTTP 200，sha256 与本地一致 | HTTP 200，sha256 与本地一致 |
| Release 页 | `/releases/tag/20260920` → 200 | 同 → 200 |

- 代码推送：`origin/main`、`origin/perf/paging-pinyin-rematch`、`gitee/master` 三处 `git ls-remote` 均为 `27621ae`。

## 四、发布过程中修掉的工具缺陷

- `dual-remote-release` 的 `scripts/gitee_release.py`：tag 尚无 Release 时 Gitee 返回的是
  200 + 正文 `null`（不是 404），脚本按 dict 解引用 ⇒ 当天首次发版必崩。
  同日重发走的是「复用已有 Release」分支，所以这个缺陷一直没暴露。
  已修两处（`resolve_release` 与 `--dry-run` 分支），并实测 dry-run 与首次建 Release 均正常。

## 五、遗留

- 历史 6 个 `v*` tag 的纯数字化重建 + 附着包重传（仍未执行）。现在起新版本用纯数字 tag 已使客户端的
  Gitee「去更新」链接可用（实测 `v20260919` 对应的纯数字 URL 返回 404），重建只影响历史 Release 页。
- 真机未覆盖项：符号层与搜索框（Canvas 自绘 + IME 窗口不可 dump）、多指手势、语音链路（禁改域）、
  可选词库的实际下载安装流程。

---

# 审查 · 批次 A 修复（2026-09-20）

日期：2026-09-20 ｜ 范围：全量审查（前述，四域并行 + 主审逐条回源码复核）后的
第一批低风险修复：全部为纯逻辑项，可在 JVM 单测内跑通。
IME 交互类（批次 B）、诊断/隐私类（批次 C）、引擎/剪贴板状态机（批次 D）未在本次实施。

## 一、修复项（8 条）

1. L15 · 索引构建对同键多行「静默少词」（`PhraseIndex.build`）：
   旧实现按行写出，同键第二个词表会被二分查找永久跳过（无日志无异常）；文本路径
   （`loadPhrasesReader`）对同键是合并语义，两条路径标准不一致。改为逐键写出 + 同键连续行
   合并词表（`|` 拼接，受既有 64KB 上限约束）；无重复键的输入输出逐字节不变，
   由「设备端构建器与构建脚本产出完全一致」钉住。影响面用真实资产复核：
   hot 34515 行 / 索引 604077 键，零重复零乱序。
2. L10 · `UserFrequency.flush` 写失败仍清 dirty：写盘失败（磁盘满 / IO 错误）后这次学习
   再没有任何路径重试（下一次 flush 因 `!dirty` 直接返回），与 flush 的职责「保证最后一次学习
   不丢」相反。改为锁内 `if (writeAtomically(...)) dirty = false`，与 `saveNow` 判据对齐。
3. L12 · `optionalLoading` 卡死（`PinyinEngine.loadOptionalAsync`）：`setThreadPriority`
   在 `runCatching` 之外，一旦抛异常 `optionalLoading` 永远为 true，此后所有
   `loadOptionalAsync` 直接返回，可选词库静默地永不加载且无重试入口。加载线程套
   `try/finally`，复位与 `onReady` 回调移入 finally（回调异常只记 W）。
4. L27 · 「检查更新」防重入闸门 3s 失效（`SettingsActivity`）：旧的 `updateDimRunnable`
   3s 后无条件复位为 Idle 并重新 enable 按钮，而网络超时是 10s，3~10s 内可重复点击、
   并发发起第二次检查并重复弹窗。改为：删除 3s 熄灯，新增 15s（10s 超时 + 5s 余量）
   看门狗（仅在仍处于 Checking 时兜底解锁）；并移除
   `updateState = when(result) { UpToDate/Available/NetworkError }` 这段被下一行覆盖、
   无任何消费方的死代码，`UpdateState` 收敛为两态。
5. L26 · 崩溃快照过滤不认跨年时间戳（`Diagnostics.filterOwnVerboseLines`）：logcat 在条目
   与「当前年」不同年时输出 `yyyy-` 前缀，旧正则失配后沿用上一行的 drop 状态，本进程 V 行
   （正文通道）可能被原样保留进快照。正则加可选年份前缀（非捕获组，捕获组编号不变）。
   未采用「失配即复位 drop」的写法：那会破坏续行跟随语义（堆栈续行会把被剔的正文带回来）。
6. L29 · `SettingsActivity` 三个未使用 import（`Build` / `androidx.core.content.edit` / `File`）。
7. L30 · `build.gradle.kts` 配置阶段泄漏 `FileInputStream`：`Properties.load` 不接管流所有权，
   不关会在 Gradle 守护进程整个会话持有 fd（Windows 上锁文件）。改 `inputStream().use { ... }`。
8. L32 · Manifest 注释日志目录名 `JinnIME` → `JinnIme`（与 `Diagnostics.DIR_NAME` 一致）。

## 二、验证

- `testDebugUnitTest`：33 类 / 326 例全绿（基线 323 → +3：同键合并、写失败保留 dirty、
  跨年时间戳）；改动前后均以 `--rerun-tasks` 强制重跑取基线，排除构建缓存假绿。
- `lintDebug`：0 error / 29 warning，与基线逐条一致。
- 写新用例时的一次自我纠错：`同键的连续多行被合并进同一个词表` 首版测试数据自身违反升序
  （`nihao` 后跟 `ni`，字典序 `ni < nihao`），被实现的 `check` 正确拦下，数据修正后全绿。
- 资产实检（本地临时脚本，跑完已删）：`hot_phrases.txt.xz` 34515 行 / `pinyin_index.bin.xz`
  604077 键，相邻重复 0、乱序 0、UTF-8 解码失败 0。

## 三、遗留（批次 B/C/D，未实施）

- 批次 B（IME 交互，需真机）：M1 搜索态功能面板守卫、M2 清空输入框改用
  `setSelection(0,0) + deleteSurroundingText(0, MAX)`（已由 AOSP 源码核实旧写法删不掉选区）、
  M7 搜索态跨模式复位、L20 复制窗口换算、L21 分号键命中判定。
- 批次 C（诊断/隐私）：M5 快照失败路径删除、L23 su 管道排空、L24 event 双时间戳、L25 崩溃同步等待。
- 批次 D（引擎/剪贴板）：M3 主线程 `coerceToText`、M4 加载锁、L3 分页状态位、L4 收藏后刷新、
  L11 预测过滤。
- 真机验证命令须带 `-s`（本机现有两台 PACM00：USB `BIYLBAFQQSS8DA69` 与无线 `192.168.1.155:5555`）。

---

# 审查 · 批次 B 修复（2026-09-20）

日期：2026-09-20 ｜ 范围：批次 A 之后的 IME 交互类修复（含真机验证）。

## 一、修复项（5 条）

1. M1 · 搜索态功能面板会把动作落到宿主（`PinyinKeyboardView.renderFunctionPanel` +
   `showSearchPanel/hideSearchPanel` + `JinnIme` 四个回调）：搜索态下候选栏此前仍渲染
   全拼/双拼/历史/方向/全选/复制/粘贴/收起，其中 全选/复制/粘贴/方向作用于宿主输入框
   （「全选」后退出搜索再输入会替换整段正文，「粘贴」会把剪贴板正文注入宿主）。修复：
   ① 搜索态候选栏只渲染「退出搜索」；② 显示/隐藏搜索面板时与方向面板互斥，并在可见性变化
   之后重刷候选栏（旧时序里 `clearComposingState` 的刷新发生在显示之前、`isPanelSearch()`
   仍为 false，会残留宿主功能面板）；③ IME 侧 `rejectedBySearchPanel()` 对四个回调做纵深防御。
2. M2 · 「双击+长按退格清空输入框」在标准宿主上失效（`JinnIme.deleteAllText`）：
   旧实现 Ctrl+A + `deleteSurroundingText(MAX, 0)`，按 AOSP 规则与实现，该 API 只作用于
   选区前后，全选后删除量为 0（一个字符都删不掉），只在宿主不接受 Ctrl+A 时误删光标前
   半截文本。改为 `setSelection(0, 0)` + `deleteSurroundingText(0, Int.MAX_VALUE)`。
3. M7 · 搜索态跨模式/会话残留（`JinnIme.switchToVoiceKeyboard` / `onFinishInputView`）：
   语音键盘下搜索面板不可见却仍激活，切回后 26 键输入被「隐形路由」进搜索框。两处边界各补
   `hideSearchPanel()`。
4. L20 · `copySelection` 未做窗口换算：长文档（`startOffset > 0`）下会把绝对下标当窗口
   下标，复制到与选区无关的内容。改为复用 `currentSelectionRange`（已含窗口校验）+
   `toWindowOffset`，与 `moveCursor/extendSelection` 一致。
5. L21 · 分号键 `ACTION_UP` 缺命中判定：按下后滑到相邻键抬起仍会上 `;`。
   补 `isInsideKey(keySemicolon, event)`（与字母键一致）。

## 二、验证

- JVM 门禁：`testDebugUnitTest` 33 类 / 326 例全绿（`--rerun-tasks` 强制重跑）；
  `lintDebug` 0 error / 29 warning（与基线一致）。
- 正式签名构建 + 覆盖安装成功（V2+V3、4 字节对齐）。
- 真机验证（PACM00 / Android 10 / KernelSU，`-s BIYLBAFQQSS8DA69`）：
  - M1：搜索态日志出现 `功能面板(搜索态): 退出`（证明只渲染退出按钮）；退出后候选栏恢复
    `功能面板: 双拼/历史/方向/全选/复制/粘贴/收起`；搜索态回车退出、BACK 收起键盘两条出口
    实测可用（`顶部搜索面板: 隐藏` + 候选栏恢复）。
  - M2：`退格双击+长按：清空全部` → `deleteAllText: 已发送清空（光标归零 + 删至末尾）`，
    33 字符的输入框被完全清空（对照：双击不成立时只连删约 15 字符并残留）。
    adb 注入间隔 > 280ms 双击窗口，无法直接触发该手势，本次用「临时把
    `doubleTapWindowMs` 放宽到 3000ms 的验证包」完成验证，验证后已恢复 280L 并重装最终包
    （`com.jinn.inputmethod.20260920140706.APK`），临时验证版 APK 已删除。
  - M7：`onFinishInputView` → `顶部搜索面板: 隐藏`（修复前会残留到下一次输入）。
  - 基本输入链路回归：字母键 → 候选 `[级, 集, 几]` → 空格取首候选 → 宿主出现文本。
  - 未覆盖：L20（需长文档窗口化）、L21（需切到搜狗/微软/紫光方案），属逻辑等价改动，
    建议后续顺带复验。
- 工程提示（沿用）：`sendevent` 注入在本 ROM 上未被识别（event2/event4 + slot 协议均无效），
  如需自动化触摸注入优先用 `input tap`（但进程启动间隔 > 双击窗口，无法覆盖 280ms 级手势）。

## 三、遗留（批次 C/D，未实施）

- 批次 C（诊断/隐私）：M5 快照失败路径删除、L23 su 管道排空、L24 event 双时间戳、L25 崩溃同步等待。
- 批次 D（引擎/剪贴板）：M3 主线程 `coerceToText`、M4 加载锁、L3 分页状态位、L4 收藏后刷新、
  L11 预测过滤。

---

# 审查 · 批次 C/D 修复（2026-09-20）

日期：2026-09-20 ｜ 范围：批次 B 之后的诊断/隐私类（C）与引擎/剪贴板状态机类（D）修复。

## 一、修复项（9 条）

### 批次 C · 诊断与隐私

1. M5 · `dumpLogcat` 失败/异常路径残留「未过滤」快照（`Diagnostics.dumpLogcat`）：
   原实现只在「子进程失败」一种路径删文件；`readText`/过滤/`writeText` 阶段抛异常（或进程
   在落盘后被强杀）都会留下未经过滤的快照，里面有本进程 V 级正文（搜索关键词/拼音串/
   测试文本），滞留 7 天并被 `exportBundle` 打包。改为 `try/catch/finally` + `produced` 标志
   统一收口：只有「过滤后成功写回」才算产出，其余路径一律删除。
2. L23 · `su` 子进程三管道既不排空也不关闭（`Diagnostics.grantPublicDirViaRoot`）：
   命令输出写满管道（64KB）时子进程阻塞在写端、父进程在 `waitFor`，必然 5s 超时，
   root 直写共享目录的功能静默失效。套用 `ClipboardFirewall.su` 的既有写法：stdout/stderr
   各起 daemon 排空线程，超时分支 destroy 后 join 收尾。
3. L24 · 事件日志双时间戳 + 两个死方法（`Diagnostics.event`）：事件正文自带时间戳又被
   传入 `log()`（它自己也会补时间戳与 tag），落盘行出现双时间戳、模块名被挤进正文。
   改为正文不带时间戳；同时删除从未接线的 500 条环形缓冲与 `eventSnapshot`/`dumpEventRing`
   （KDoc 声称的「崩溃时落盘事件序列」从未实现；事件已随日常日志落盘、崩溃快照也会带走
   logcat 里的部分）。
4. L25 · 崩溃处理同步等 logcat 最长 10s（`Diagnostics.installCrashHandler`）：崩溃线程
   （常是主线程）在「已崩溃但进程未死」状态下长时间阻塞，会把 ANR 弹窗与 tombstone 上报
   一并推迟。改为：快照放独立线程、`dumpLogcat` 增加 `waitMs` 参数（崩溃路径 2s），
   崩溃线程最多等 2.5s。

### 批次 D · 引擎与剪贴板状态机

5. M3 · 剪贴板监听在主线程做 `coerceToText`（`ClipboardController.onClipboardChanged`）：
   URI 型剪贴板条目会同步读取整个 content:// 流（AOSP 实现，无长度上限），主线程文件读
   就是键盘卡顿/ANR 的来源。改为回调只取 ClipData 快照、文本提取统一进 `BackgroundIo`
   （新增 `extractAndSave`，正常与重试两条路径共用）。同时把 L2 一并修掉：重试路径缺的
   `itemCount == 0` 守卫集中进 `extractAndSave`（0 条目时 `getItemAt(0)` 会抛越界异常）。
6. M4 · `loadOptionalAsync` 与 `load()` 争用 `this` 锁（`PinyinEngine`）：`load()` 全程持
   `synchronized(this)`（含秒级索引解压），而 `loadOptionalAsync` 的调用点全在主线程
   （息屏广播 / 键盘收起后闲置 / 兜底超时），首次加载期间的空闲信号会把主线程阻塞数秒。
   新增 `optionalLock` 专用于「检查-置位」的原子性（两个标志仍 @Volatile），不再与 `load()`
   的数据锁互等。
7. L3 · 面板分页状态位异常后不复位（`ClipboardPanelView.refresh` / `loadNextPage`）：查询
   抛异常（数据库损坏/磁盘满）时 `loadingPage` 永远停在 true，`loadNextPage` 的第一道闸门把
   后续分页永久拦住（静默「没有更多了」）。两处查询包 `runCatching`，失败也复位并记 E。
8. L4 · 收藏切换后列表被截回第 1 页（`ClipboardPanelView.toggleFavorite`）：`refresh()`
   只取第一页，不重置滚动时已加载的多页被整体截断、ListView 的 firstPosition 又被钳到末尾
，用户既不在原位置也找不到那条。与删除一致改 `refresh(resetScroll = true)`。
9. L11 · 预测候选绕过生僻字过滤（`PinyinEngine.predict`）：索引路径
   （`collectLongerSuffixes`）直接解码后缀、不过滤，与运行时路径（`phrasesFor` 内的
   `filterRareChars`）及 `query` 不一致，开启「隐藏生僻字」（默认）时预测仍可能出隐藏词
   并可上屏。改为在 `predict` 出口统一过滤（`commonChars == null` 时原样返回，零开销）。

## 二、验证

- JVM 门禁：`testDebugUnitTest` 33 类 / 326 例全绿（`--rerun-tasks` 强制重跑）；
  `lintDebug` 0 error / 29 warning（与基线一致）。
- 正式签名构建 + 覆盖安装成功。
- 真机验证（PACM00 / Android 10 / KernelSU，`-s BIYLBAFQQSS8DA69`）：
  - L24：日志行已是单时间戳格式（`… I/EVENT [main] [EV#n] 模块:事件`），不再出现双时间戳。
  - M3：`功能面板: 复制` → `复制: 选中 8 字` →
    `I/ClipboardStore [jinn-clipboard-io] save: 已保存`，保存发生在 BackgroundIo 线程
    （改动点：取文本已不在主线程）；非搜索态的全选/复制路径未受 M1 守卫影响。
  - M4：息屏触发 → `可选词库: 开始后台加载（触发: 息屏）` → `可选词库延迟加载完成，耗时 3ms`
    → `可选词库已在后台就绪`（专用锁下检查-置位正常，无重复加载）。
  - 冷启动链路正常（两段式 3166ms、可选词库改为空闲加载）。
  - 未覆盖（需特定数据/构造）：M5（需构造失败快照）、L3/L4（需 >50 条历史）、L11（需生僻字
    预测）、L23（本机 root 授权本就失败，日志无超时即为改善）。

## 三、遗留（登记）

- 批次 B 遗留真机复验：L20（长文档窗口化复制）、L21（搜狗/微软/紫光方案的分号键滑动）。
- 历史登记项（多轮已登记不改）：`junding` 伪音节、`common_chars.txt` 非 BMP 字、引擎加载期
  不校验存量词库、`content_hash` 明文 SHA-256、语音正文 I 级日志（禁改域）、
  `Diagnostics.init` 主线程探测、备份规则漏 `external` 域 logs（L1，需 bmgr 实测）、
  面板 OFFSET 分页重复/跳行（L9，需 keyset 重构）、`MicRecorder.stop` join 超时路径（禁改域）。

---

# 审查 · 引入风险自审后的两项加固（2026-09-20）

日期：2026-09-20 ｜ 范围：对批次 A~D 全部改动做「引入风险自审」（完整 diff 逐行审阅 +
关键规则回读 + AOSP 边界核对）后落地的两项最小改进与 5 条新用例。
自审结论：未发现必然/高概率触发的新 Bug；以下两项为自审提出的改进与加固。

## 一、改动

1. R1（改进）· 预测过滤后回填（`PinyinEngine`）：生僻字过滤在收集之后统一执行，
   而收集上限原先就是 `MAX_PREDICTIONS(6)`，前几个后缀全被过滤时，剩余名额无法由后续
   候选补上（预测无故变少甚至为空）。新增 `PREDICT_COLLECT_LIMIT = MAX_PREDICTIONS * 2`
   作为过滤前收集上限（索引路径与运行时表路径同步替换），出口仍 `take(MAX_PREDICTIONS)`；
   未启用过滤时仅多收集 ≤6 个后缀再被裁掉，开销可忽略。
2. R3（加固）· 崩溃处理自身不再可能抛（`Diagnostics.installCrashHandler`）：
   `Thread.start()/join()` 在极端情况（OOM、线程受限）抛异常会顶替原始崩溃异常继续传播，
   还会搅乱 finally 的收尾语义。整段快照逻辑包 `runCatching`，失败用裸 `Log.e` 记一笔
   （此刻 `Diagnostics` 的写日志链路可能已不可靠）。
3. L21 可测性重构（`PinyinKeyboardView`）：`isInsideKey` 的几何判定抽为文件级纯函数
   `isInsideKeyBounds`（视图取坐标 + dp 换算留在原处），行为逐条等价；字母键与分号键共用。
   该重构不改变任何阈值与语义，只为让「滑出不得上字」这条约束可被单测钉住。

## 二、新增用例（5 条）

- `PredictRareCharsTest`（新测试类，2 例）：「6 个生僻后缀 + 1 个常用后缀」场景下
  ① 过滤后必须由后续候选回填出常用后缀（不是静默变空）；② 未启用过滤时行为与旧实现
  一致（取字典序前 6 个）。用 `PhraseIndex.build` + `loadFromIndexBytes` 走真实索引路径。
- `RecentFixesRegressionTest`（+3 例）：命中判定的「键内/外扩内命中」「滑出到相邻键不命中」
  「取不到布局信息保守命中」。

## 三、验证

- 门禁：`testDebugUnitTest --rerun-tasks` 34 类 / 331 例全绿（33/326 → +1 类 +5 例）；
  `lintDebug` 0 error / 29 warning（与基线一致）；`assembleDebug` 成功。
- 变异验证：把 `PREDICT_COLLECT_LIMIT` 临时收窄回 `MAX_PREDICTIONS` 后，
  `过滤后应由后续候选回填_而不是静默变空` 立即变红（回归可复现），恢复后全绿，
  证明该用例真的钉住了 R1 的修复，而非"看起来通过"。
- 真机：正式签名包重新构建并覆盖安装（保持设备与源码同步）。
- 已知边界：L21 用例覆盖的是几何判定本身，不覆盖「分号键的 UP 分支确实调用它」这层
  接线（View/MotionEvent 依赖，JVM 不可测），该层仍靠代码审查 + 建议的真机复验。

## 四、遗留（不变）

- 见批次 B/C/D 章节的「遗留」：L20/L21 真机复验、历史登记项（`junding`、非 BMP 字、
  `content_hash` 明文、语音链路、备份规则漏 `external` 域 logs、面板 OFFSET 分页、
  `MicRecorder.stop` join 超时路径）。

---

# 搜索面板布局精简（2026-09-20）

日期：2026-09-20 ｜ 诉求：搜索页「搜索剪贴板历史」同一行右侧的「退出/搜索」按钮属多余
（候选栏已有同功能按钮），要求输入框独占一行。

## 一、改动（`SearchPanelView`）

- 删除输入框右侧的 `btnExit`（"退出\n搜索" 两行按钮）与其外层 `searchRow` 包装，
  输入框改为独占一行（`MATCH_PARENT × 46dp`，高度与原来一致，IME 总高不变）。
- 删除随按钮失去引用的 `WRAP_EXIT_DP` 常量（避免死常量）。
- `Listener.onClose` 保留：它还有第二处调用，粘贴搜索结果成功后
  （`handleItemClick`）自动关闭面板；删除按钮不影响该链路。

## 二、退出搜索的入口（删除按钮后仍完备）

候选栏「退出」按钮 / 搜索态回车 / BACK 收起键盘 / 剪贴板面板互斥接管。

## 三、验证

- 门禁：`testDebugUnitTest` 331 例全绿；`lintDebug` 0 error / 28 warning
  （比基线少 1 条、无新增）；正式签名构建 + 覆盖安装成功。
- 真机截图比对（PACM00，`-s BIYLBAFQQSS8DA69`）：布局为
  `搜索剪贴板历史`（输入框，独占一行，右侧无按钮）→ 候选栏 `退出 / 搜索` 两行按钮，
  与诉求的目标布局一致。
- 退出按钮真机可点：进入搜索态后点击候选栏「退出」→ 日志
  `功能面板: 退出搜索` → `搜索面板: 隐藏` → `顶部搜索面板: 隐藏`（完整跑通）。
- 附带侧证：期间一次点击落在 IME 窗口外触发收起，日志显示 `onFinishInputView` →
  `顶部搜索面板: 隐藏`（M7 会话边界行为的又一次真机证据）。

---

# 输入方案全局化 + 面板按钮移除（2026-09-20）

日期：2026-09-20 ｜ 诉求（用户明示，覆盖既有条款）：
① 设置页「双拼的输入方案」改为全局输入方案选择（语音除外）= 全拼 + 7 套双拼，文案同步修改；
② 输入法面板移除第一个「双拼 / 全拼」按钮，功能面板 7 → 6 个按钮。

## 一、改动

- 枚举（`ShuangpinScheme`）：新增 `ALL = listOf(QUANPIN) + SHUANGPIN_ONLY`（共 8 项，全拼在最前）；
  删除 `toggle`（面板二态切换，已无调用方）。
- 设置页（`SettingsActivity`）：下拉数据源改为 `ShuangpinScheme.ALL`；选中即全局生效
  （选全拼 = `useShuangpin=false`；选双拼 = 记住 `shuangpinScheme` + 启用）；
  选全拼不清方案记忆（再选回双拼时回到上次那套）；初始选中改为按 `effectiveShuangpinScheme` 定位。
- 键盘（`PinyinKeyboardView`）：`renderFunctionPanel` 删除第一个按钮（含 `togglePinyinScheme` 死代码）；
  功能面板日志改为 `功能面板(6 按钮): 历史/方向/全选/复制/粘贴/收起（当前方案=…）`，
  原日志仍按 7 按钮格式打印（含已删除的方案切换按钮），在真机验证时发现并修正。
- 文案（`strings.xml`）：标题 `双拼的输入方案` → `输入方案`；
  说明改为 `全拼 + 7 套双拼；选中即全局生效（语音键盘除外）\n26 键按钮会自动显示韵母\n搜狗/微软/紫光的 ing 在分号键上`。
- Prefs KDoc：`useShuangpin` / `shuangpinScheme` 的写入方与语义同步更新。
- 测试（`ShuangpinSchemesTest`）：删除已废弃的 `toggle` 用例；
  `双拼方案列表_共七套且不含全拼` → `输入方案列表_全拼加七套双拼共八项`（钉住 `ALL` 的内容与顺序）。
- AGENTS.md：更新「输入方案」条款（面板按钮移除、下拉含全拼）、「方案持久化」（写入方）、
  「切换输入方案」排查方法、项目结构注释，旧的「面板按钮禁止改文案 / 下拉只列 7 套」条款作废。

## 二、验证

- 门禁：`testDebugUnitTest` 34 类 / 330 例全绿（331 → 330：删 1 例、改 1 例）；
  `lintDebug` 0 error / 28 warning（无新增）。
- 真机（PACM00，`-s BIYLBAFQQSS8DA69`）：
  - 设置页标题/说明为「输入方案」「全拼 + 7 套双拼；选中即全局生效（语音键盘除外）」；
  - 下拉展开确认 8 项：26 键全拼 / 自然码 / 小鹤 / 搜狗 / 微软 / 紫光 / ABC / 加加（全拼在最前）；
  - 选「26 键全拼」→ 下拉值变更 + 日志 `输入方案: 26 键全拼（双拼=false）`；
  - 键盘侧截图确认：功能面板 6 个按钮（历史/方向/全选/复制/粘贴/收起）、键面为全拼样式、
    空格键提示「中文全拼」。

---

# 审查 · 批次 A 修复（2026-09-20）

日期：2026-09-20 ｜ 范围：审查的第一批实施（纯逻辑、JVM 可跑通）。
批次 B（IME 交互）/ C（诊断隐私）/ D（引擎剪贴板）未实施。

## 一、修复项（8 条）

1. L10 · `UserFrequency.flush` 写失败仍清 dirty：写盘失败（磁盘满 / IO 错误）后这次学习
   再没有路径重试（下一次 flush 因 `!dirty` 直接返回），与 flush 职责「保证最后一次学习不丢」
   相反。改为锁内 `if (writeAtomically(...)) dirty = false`（与 `saveNow` 判据一致）。
2. L12 · `optionalLoading` 卡死（`PinyinEngine.loadOptionalAsync`）：`setThreadPriority` 在
   `runCatching` 之外，一旦抛异常 `optionalLoading` 永远为 true，此后所有 `loadOptionalAsync`
   直接返回，可选词库静默地永不加载。加载线程套 `try/finally`（复位与 `onReady` 移入 finally）。
3. L15 · 索引构建对同键多行「静默少词」（`PhraseIndex.build`）：旧实现按行写出，同键第二个
   词表会被二分查找永久跳过；文本路径对同键是合并语义，两条路径标准不一致。改为逐键写出 +
   同键连续行合并词表（受既有 64KB 上限约束）；无重复键的输入输出逐字节不变（parity 用例钉住）。
4. L26 · 崩溃快照过滤不认跨年时间戳（`Diagnostics.filterOwnVerboseLines`）：logcat 跨年时输出
   `yyyy-` 前缀，旧正则失配后沿用上一行状态，本进程 V 行可能被原样保留进快照。正则加可选年份前缀
   （非捕获组，捕获组编号不变）。
5. L27 · 「检查更新」防重入闸门 3s 失效（`SettingsActivity`）：旧的 3s 无条件熄灯复位会让按钮在
   请求在途时重新可用（网络超时 10s），3~10s 内可并发发起第二次检查并重复弹窗。改为 15s 看门狗
   （仅仍处于 Checking 时兜底解锁）+ 删除被下一行覆盖的死赋值，`UpdateState` 收敛为两态。
6. L29 · `SettingsActivity` 三个未使用 import（`Build` / `androidx.core.content.edit` / `File`）。
7. L30 · `build.gradle.kts` 配置阶段泄漏 `FileInputStream`：`Properties.load` 不接管流所有权，
   不关会在 Gradle 守护进程整个会话持有 fd（Windows 上锁文件）。改 `inputStream().use { ... }`。
8. L32 · Manifest 注释日志目录名 `JinnIME` → `JinnIme`（与 `Diagnostics.DIR_NAME` 一致）。

## 二、验证

- 门禁：`testDebugUnitTest --rerun-tasks` 34 类 / 330 例全绿（+3 例：同键合并、写失败保留
  dirty、跨年时间戳，均已确认真实执行）；`lintDebug` 0 error / 28 warning（与基线一致）。
- 既有对拍/边界测试（`IndexBuilderParityTest` / `UserFrequencyTest` / `DiagnosticsLogcatFilterTest`）
  全部保持绿色：L15 的「无重复键逐字节不变」由 parity 用例守住。

---

# 候选栏 ✕ 清空 + 移除长按清空手势（2026-09-20）

日期：2026-09-20 ｜ 诉求（用户设计）：
① 输入方案 8 种、默认 26 键全拼（核实结论：现状已满足，无需改动）；
② 候选栏最右侧在有候选时出现 ✕ 按钮，点击完全清空候选词、回到 6 按钮功能面板；
③ 移除「用删除键触发清空候选栏的词」的隐式手势。

## 一、改动

- 候选栏 ✕ 按钮（`keyboard_pinyin.xml` + `PinyinKeyboardView`）：`candidate_bar` 右端新增
  `btn_clear_candidates`（40dp，默认 GONE）；`refreshCandidateBar` 在候选/预测/拼音串展示时置
  VISIBLE，符号层与 `renderFunctionPanel`（功能面板/搜索态退出）置 GONE；点击 = 打日志 +
  `clearComposingState()`（完全清空拼音/候选/预测 → 候选栏回到 6 按钮面板）。
- 移除「长按退格整串清空拼音」（机制 B）：删除 `shouldClearComposingOnHold`、
  `shouldStopRepeatOnExhaustedComposing`、`HOLD_TO_CLEAR_COMPOSING_MS`、`LONG_COMPOSING_TO_CLEAR`、
  字段 `composingLenAtBackspaceDown` 及连删循环里的两处判据。删除键长按恢复标准语义：
  逐字连删，拼音删空后继续删已上屏正文。
- 保留「双击 + 长按 >800ms 清空输入框」（机制 A：`clearOnLongPressRunnable` / `onDeleteAll`），
  其作用对象是宿主文本，不在本次「清空候选栏」范围内；如也需移除请另行说明。
- 测试同步（`RecentChangesParityTest`）：删除随机制 B 一并失效的 3 个用例与专属常量
  （330 → 327 例），原位置保留一段说明注释。
- 顺带：`Prefs.KEY_SHUANGPIN` 注释不再提「面板按钮」；删除键三态注释修正为
  「单击删一个 / 按住连续删 / 双击+长按清空输入框」；AGENTS.md 的「退格手势」排查方法与
  测试覆盖描述同步更新。

## 二、需求 ① 的核实结论（默认 26 键全拼）

- 下拉 = `ShuangpinScheme.ALL`（全拼 + 7 套双拼 = 8 种，全拼在最前）；
- `Prefs.useShuangpin` 默认 `false`、`effectiveShuangpinScheme` 未启用双拼时恒为 `QUANPIN`
  → 全新安装默认即「26 键全拼」；设置页初始选中按当前生效方案定位（真机显示「26 键全拼」）。

## 三、验证

- 门禁：`testDebugUnitTest --rerun-tasks` 34 类 / 327 例全绿（-3 为随功能移除的用例）；
  `lintDebug` 0 error / 28 warning（无新增）。
- 真机（PACM00，`-s BIYLBAFQQSS8DA69`）：
  - 输入拼音「jk」→ 截图确认候选栏最右侧出现 ✕（左侧为拼音串，布局不挤压）；
  - 点击 ✕ → 日志 `候选栏 ✕：清空候选（拼音=2 候选=0 预测=0）` → `功能面板(6 按钮): …`，
    截图确认候选栏回到 6 按钮面板且 ✕ 消失；
  - 输入 12 字符拼音后长按退格 1.5s（超过旧的 1.2s 阈值）→ 日志无「清空拼音串」，
    改为「退格删拼音 → 退格删已上屏文本」的逐字连删（捷径确已移除）。

## 四、遗留与订正

- ~~批次 B（IME 交互：M1/M2/M7/L20/L21）、批次 C（诊断隐私）、批次 D（引擎剪贴板状态机）未实施~~
  订正（2026-09-20 晚）：批次 B / C / D 已随 `a5b9d8c` 实施（更新日志上方对应章节 +
  提交信息可查），本行原记载有误。

---

# 当日改动全量审查与修复（2026-09-20 晚）

范围：`a5b9d8c`（批次 A~D + 输入方案 + 搜索面板，22 文件 / +974−323）+ 未提交工作树
（候选栏 ✕、移除长按清空、README/注释）。方法：三路独立审查（输入方案 / 搜索面板剪贴板 /
引擎诊断）+ 主审逐条回源码复核 + AOSP 源码级判定，全部发现按「是否本次引入」分档。

## 一、修复（8 项，均落到源码 + 全量门禁）

1. `PhraseIndex.build` 合并期 O(n²) 重建（L15 引入）：同键多行改为 `LinkedHashSet`
   保序累积（原实现每行重建整个 ByteArray，词多时是二次方拷贝）。
2. `PhraseIndex.build` 合并未去重（L15 引入）：与文本路径
   `existing + kept.filter { seen.add(it) }` 语义对齐，重复词只保留首个；同时丢弃脏空词
   （`a||b`）。「无重复键输入逐字节不变」由 parity 对拍继续钉住（新增用例
   `同键合并按出现顺序去重`）。
3. `loadOptionalAsync`：`Thread.start()` 失败不复位（L12 的 finally 够不到 start）：
   OOM / 线程受限时 `optionalLoading` 永久为 true（可选词库静默永不加载）→ 包 try/catch 复位。
4. `onReady` 在失败路径同样回调（L12 引入）：调用方会打出「可选词库已在后台就绪」，
   与同批 E 级失败日志矛盾 → 仅在加载成功（`runCatching.isSuccess`）时回调。
5. `dumpLogcat` 可能误删同名旧快照（M5 引入）：`dest` 是固定名，若「启动子进程」阶段就
   失败，文件里仍是上一次的可用产物却被 finally 清掉 → 进 try 先 `dest.delete()`，
   让「存在 ⇔ 本次产物」成为不变量。
6. `settings_usage` 用户文案过期（本次手势移除遗漏，三路中两路独立发现）：
   「按住退格约 1 秒可整串清掉」→ 改为「点候选栏右侧的 ✕ 可整串清掉」。
7. `UserFrequency.saveNow` 陈旧任务防御：写盘前校验 `file !== f`（排程时的文件已变则
   放弃），关闭「测试/未来换文件后旧任务写旧路径 + 清掉新文件 dirty」的污染窗口。
8. `resetForTest` 补齐 `extensionLoaded` 复位（L18 登记项）+ 删除随方案按钮失去调用方的
   `labelColorRes` 死参数、「7 个按钮」过期注释、`deleteAllText` 日志术语
   （"三击"→"双击+长按退格"）。

## 二、复核后撤销的误判（1 项）

- `deleteAllText` 的 `Int.MAX_VALUE` 溢出：审查提出「宿主未落实 setSelection 时
  `b + MAX_VALUE` 溢出 → 负区间抛异常」。回 AOSP `BaseInputConnection.deleteSurroundingText`
  源码核对：`if (numDeleteAfter > 0)` 守卫挡住负区间（不会调用 `content.delete`），
  且 `b=0` 时 `0+MAX_VALUE` 不溢出 → 无崩溃风险，M2 修复正确，撤销该条。

## 三、登记不改（非本次引入 / 既有行为）

- 搜索面板：结果列表新查询后不回顶（与剪贴板面板 `scrollToken` 写法不对称）；空查询也显示
  「未找到匹配内容」；粘贴失败无 Toast。均属既有实现，本次 diff 只删了搜索面板内的
  「退出」按钮（退出改由候选栏「退出」/ 搜索态回车 / 收起键盘），未触及列表/空态/粘贴逻辑。
- 剪贴板面板：切分类只收起长按操作条、不收起「清空确认条」（既有行为，待产品定夺）。
- 输入方案：切换后键盘侧无即时同步路径（`onStartInputView` 才 `configure()`；
  `ACTION_CONFIG_UPDATED` 无发送方），与设置页「保存配置并立即重启生效」的既有设计一致，
  但「选中即全局生效」的措辞偏乐观；下拉写入的「仅触摸过」闸门对硬件键盘/遥控器不生效（原 L33）。
- 引擎：`dirty` 置位在锁外（毫秒级丢更新窗口，非本次引入）；`isOptionalReady` /
  `isExtensionLoaded` 当前无生产调用方（预留 API）。

## 四、验证

- 门禁：`testDebugUnitTest --rerun-tasks` 34 类 / 328 例全绿（+1 去重用例，parity 对拍过）；
  `lintDebug` 0 error / 28 warning（无新增）。
- 真机（PACM00，`-s BIYLBAFQQSS8DA69`）：词库加载正常（`音节=414 基础键=604077`，与改动前
  逐项一致 → 索引产物未受影响）；输入候选正常；候选栏 ✕ 正常（截图：`jk` + 12 候选 + 最右 ✕）。
- 说明：设备当前方案为「自然码双拼」（用户选择，截图/日志可见），与「新装默认 26 键全拼」
  的核实结论不冲突（默认值仅对全新安装生效）。

---

# 发版记录：同日覆盖 20260920（2026-09-20 晚）

- 提交 `ec6f705`（16 文件，+296 / −222）推送到 main 主分支：GitHub `main` + Gitee `main`
  （按用户新规则，不再推 Gitee `master`；Gitee 现有 `master` 已同步至同一提交，内容无差异）。
- 发布包：`jinn-release.apk` 5,196,626 字节 / `sha256=3914cd8e…` / `versionCode=20260920` /
  V1=false V2=true V3=true / 4 字节对齐；三次独立构建 sha256 一致（确定性构建）。
- 覆盖两端 `20260920` Release 附件（新文件覆盖旧文件）：
  - GitHub：`gh release upload 20260920 jinn-release.apk --clobber`（旧 `4d1b886d…` → 新 `3914cd8e…`）；
  - Gitee：旧附件 `3229178` → DELETE → 新附件 `3235668`（5,196,626 字节）。
- Release 正文双端同步更新：新增「候选栏 ✕ 一键清空 + 移除长按退格整串清空」条目、补当日前述
  审查批次修复、修正已过时描述（原「按住退格丢长拼音…判据/闸门」已随手势移除）、
  校验数字更新为 34 类 / 328 例、sha256 更新。
- 校验：双端下载 URL 均 HTTP 200，`size=5,196,626`、`sha256` 与本地发布包三方完全一致 ✓
- 发布前验证：强制全量 `testDebugUnitTest --rerun-tasks`（34 类 / 328 例全绿）+ `lintDebug`
  （0 error / 28 warning）+ 真机（PACM00）覆盖安装 → 冷启动 / 词库加载（604077 键）/ 输入 / 崩溃检查
  — 全部通过。
- 发布规范已写进 `AGENTS.md`「发布与同步」章节（main 主分支 + 覆盖式 + 双端三方校验）。

---

# 半透明键盘审查修复（D1~D7，2026-09-22）

背景：透明度 WIP 经三轮严格审查（三路独立审查 + 主审复核 + 真机实测定性）收敛出 6 条真缺陷
（D1/D2/D3/D4 中等 + D6/D7 轻微；D5「窗口级透明无条件」与 D8「shift 双淡」经复核判为
「待确认/设计意图」移出真缺陷列表），本次全部修复。

## 一、修复内容

| # | 缺陷 | 修复 |
|---|---|---|
| D1 | 剪贴板/搜索面板整块不参与透明（识别规则只认 `kb_bg` 一种色） | `alphaFaces` 扫描升级为双档色集：背板类（`kb_bg`/`app_bg`）走 plate，内容面类（`card_bg`/`surface_hi`）走 surface；面板分类按钮（`key_bg` drawable）由 `ClipboardPanelView.applySurfaceAlpha` 按档重建（新增共享构建器 `buildKeyFaceBackground`，`SearchPanelView` 留空实现对齐接口） |
| D2 | 候选栏内已构建的面不跟随即时刷新 | `refreshAppearance()` 末尾补 `refreshCandidateBar()`（方向面板展开态跳过，避免被收回默认布局） |
| D3 | 语音面板覆盖不全 + 不即时 | (a) `onKeyAppearanceChanged` 补 `applyTransparencyToVoicePanel()`（即时生效）；(b) 面板扩展：`kb_divider` 走 plate、麦克风底盘与 6 个编辑键按 surface 重建（`mic_area` 补 id；MicButton 属禁改域不动） |
| D4 | 候选栏底档位与设计文档矛盾（有候选时全透 → 文字直接叠宿主） | `updateCandidateBarBackground()` 按内容状态选档：有拼音/候选/预测 → surface，空白 → plate；带缓存避免每次按键白重绘 |
| D6 | `lastTransparencyDesc` 死代码 + 诊断无条件执行 | 日志与 `logBackgrounds` 统一移入「档位变化」分支（每次弹键盘不再刷日志、不再 500ms 后扫全树） |
| D7 | 「绝不整键 setAlpha」与实现矛盾 | 措辞限定为「本功能的绘制路径」+ 标注符号层置灰的既有例外 |

## 二、验证（真机 PACM00，`-s BIYLBAFQQSS8DA69`）

- 门禁：`testDebugUnitTest` 40 类 / 381 例全绿；`lintDebug` 0 error / 23 warning（无新增）。
- D1 实测：100% 下打开剪贴板面板，面板主体全透（背景透出）、分类按钮与候选栏按钮均半透明
  （修复前为「整块纯实心 + 一排实心按钮」，截图对比）。
- D2 实测：面板态下拖滑杆 0%↔100%，候选栏 6 个功能按钮同会话内跟随（0% 实心 → 100% 半透明；
  修复前会保持 0% 时的实心）。日志确认两次档位变化各刷新一次。
- D4 实测：100% 下输入拼音，候选栏底为 surface 档半透明面，候选词清晰可读（不再直接叠在宿主内容上）。
- D6 实测：`键盘透明度: 100%` 全程仅打印一次（修复前每次弹键盘都打）。
- D3：设备语音功能为禁用态，未实测（代码级完成）。

## 三、维护标准（已同步 `AGENTS.md`）

- 面的档位靠色令牌相等识别（`kb_bg`/`app_bg` → plate；`card_bg`/`surface_hi` → surface）；
  `PinyinKeyboardView` 与 `JinnIme` 各有一份 `alphaFaces`，改一处必须同步另一处；
- drawable 键面（面板按钮 / 语音编辑键）带不了动态 alpha，需按档重建（`buildKeyFaceBackground`）；
- `key_bg.xml` 圆角若变，需同步 `KEY_BG_CORNER_DP` 与 `buildKeyFaceBackground` 的默认值；
- 候选栏底档位跟随内容状态（`updateCandidateBarBackground`），勿改回固定 plate 档。

---

# 产品改名：Jinn安卓输入法 → Jinn拼音输入法（2026-09-22）

诉求：手机桌面名称、APP 主页顶部名称等显示文本统一改为「Jinn拼音输入法」。

## 一、改动（5 处显示名）

| 位置 | 改动 |
|---|---|
| `strings.xml` `app_name` | Jinn安卓输入法 → Jinn拼音输入法（桌面图标名） |
| `strings.xml` `settings_title` | 同上（设置页顶部标题） |
| `README.md` 标题 | `# Jinn 拼音输入法` |
| `README_EN.md` 标题 | `# Jinn Android IME` → `# Jinn Pinyin IME`（与中文名对齐） |
| `JinnIme.kt` 类 KDoc | 「Jinn 安卓输入法」→「Jinn 拼音输入法」 |

刻意不改（已在 `AGENTS.md`「命名规则」登记）：`Jinn_AndroidInputMethod`（GitHub 仓库标识，
被 `UpdateChecker.REPO` / `OptionalDicts` 下载 URL / `gh release --repo` 引用）、
`JinnIme`（日志 TAG 与目录名 `Diagnostics.DIR_NAME`，改了会丢历史日志与 Root 授权标准）
，两者都不是显示文本。

## 二、验证

- APK 级：`aapt dump badging` → `application-label:'Jinn拼音输入法'` ✓（包名不变，
  `versionCode=20260922`）；
- 真机（PACM00）：设置页顶部标题截图确认「Jinn拼音输入法」（副标题不变）；
  冷启动词库加载正常（`音节=414 基础键=604077`，逐项与改动前一致）；无 FATAL/ANR；
- 回归门禁：`testDebugUnitTest` 40 类 / 382 例全绿；`lintDebug` 0 error / 23 warning（无新增）。

## 三、兼容性

包名 / 签名 / 数据 / Prefs / DB / 远程 URL 全部不变 → 无兼容性影响；
桌面图标名与设置页标题变化即为本次预期（Launcher 可能需一次刷新）。

---

# A16（Android 16）实测与「透明度功能消失」根因（2026-09-22 晚）

现象：用户在 Android 16 手机（Redmi K60 Pro，`5a91ac60`，API 36）上实测，反馈
「键盘透明度这个功能消失了」。

## 一、根因：设备装的是当日早期构建，不是 A16 兼容问题

- 代码侧核查：透明度区块在 `activity_key_appearance.xml` / `KeyAppearanceActivity` /
  `Prefs.keyTransparencyPercent` 全链路没有任何系统版本条件分支（布局无 visibility 条件、
  外层是 ScrollView 不会被裁剪）→ 不存在「A16 上不显示」的代码路径。
- 实测证据：装包前 UI dump 显示界面文案还是旧版，标题「Jinn安卓输入法」、
  入口「按钮圆角间隙」（新版应为「Jinn拼音输入法」/「键盘按钮调整」）→ 该包构建于
  「透明度功能 + 新文案」合入之前。
- 教训：`versionCode` 只到「日期」粒度（20260922），同一天多次构建无法从版本号区分；
  设备上「看起来是最新版」却可能是当日早期构建。给设备装包请以最新构建的 APK 文件为准。

## 二、解决与实测（覆盖安装 `com.jinn.inputmethod.20260922180459.APK` 后）

| 验证项 | 结果 |
|---|---|
| 设置页标题 / 入口 | 「Jinn拼音输入法」/「键盘按钮调整」✓ |
| 外观页三滑杆 | 按键圆角 1dp、按键间隙 0.5dp、键盘透明度 0%（`seek_key_transparency`） 全在 ✓ |
| 拖动滑杆 | 拖至 61% → 日志 `键盘透明度: 61%` ✓（落盘 + 即时生效链路通） |
| 半透明效果 | 键盘以 61% 显示，背后极光背景明显透出、文字清晰 ✓（截图留档后已恢复 0%） |
| 崩溃检查 | 无 FATAL / ANR ✓ |

结论：半透明键盘 / 改名 / 候选栏 ✕ 在 Android 16（API 36） 上全部正常；
此前「功能消失」为设备安装了当日早期构建所致，覆盖安装最新包即恢复。

## 三、排查附注（供后续真机操作参考）

- A16 收紧了 shell 权限：`am start` 无法启动 `exported=false` 的页面（如 `KeyAppearanceActivity`）
  → 走设置页入口点击；
- 该机 `wm size` 为 Override 1080x2400（Physical 1440x3200）→ `uiautomator dump` 坐标与
  截图均为 1080x2400 空间，以 dump 的 bounds 为准（目视估算会偏约一行高）；
- 输入框首次聚焦会弹系统 Autofill 面板（Google 服务），再点一次输入框即出键盘。

---

# 发版 · 20260922 前述覆盖发布（全量重编译）

- 全量重编译：`gradlew clean` + `build_apk.py` → `com.jinn.inputmethod.20260922185834.APK`
  （5.0 MB；V1=false / V2=V3=true；4 字节对齐通过）；复制为 `jinn-release.apk` 覆盖双端
  Release `20260922` 附件（旧附件删除后上传新包）。
- 三方 sha256 一致（双端下载实测）：本地 = GitHub = Gitee = `39e08ff2…b7645aa`。
- 双端 Release 正文已更新：补本次条目（半透明键盘 / 候选栏 ✕ / 移除长按清空 / 产品改名 /
  A16 实测），修正过时描述（外观页三滑杆 + 松手即时生效、入口名「键盘按钮调整」、测试数
  40 类 / 382 例、lint 0/23），其余逐字保留。
- 代码提交：`999d5d8`（副标题去「语音识别 +」+ A16 记录）→ 双端 main 已同步。
- Gitee API 两个新坑（已登记进 AGENTS.md 发布流程）：① 更新正文的 `access_token`
  必须放 body（放 query 报「登录失效」，而同 token 的附件接口 query 正常）；
  ② 正文 JSON 必须手工构造，PS 的 `ConvertTo-Json` 把中文转成 `\uXXXX` 会让 payload
  膨胀 ~8 倍并被拒（`body is invalid`），手工写 4.4KB 即通过。
- 排查附注：`Invoke-RestMethod` 读 Gitee 返回的中文会乱码（PS 5.1 解码问题，非数据问题），
  校验正文是否更新应以 `curl.exe` 拉原始 JSON 为准或在 PATCH 响应体里核对。

---

# 符号面板：「常用」拆为「全角 / 半角」（2026-09-21）

日期：2026-09-21 ｜ 诉求（用户）：「把『常用』改为『全角』，只放全角符号；在右侧新增『半角』，
只放半角符号」，顺序为「全角 半角 编程 ……」。

## 一、改动

- `KeyboardLayouts.kt`：原「常用」组拆成两组（分组总数 10 → 11，`全角` 仍是第 1 组，进入符号层的默认组）：
  - `全角`（1 页，26 键满）：中文全角标点原样，8 个 ASCII 符号换成全角变体
    （`~ @ # $ % ^ & *` → `～ ＠ ＃ ＄ ％ ＾ ＆ ＊`）；
  - `半角`（2 页）：第 1 页 = 原「常用」第 2 页（数字 + `- / : ; ( ) ' " \ = + _ | `` ` `` [ ]`，布局未动），
    第 2 页 = 补齐常用半角标点与从全角组移出的半角符号
    （`. , ? ! ~ @ # $ % ^ & * < > { }`，16 键，余下键位留空）。
- 符号层渲染与手势逻辑零改动（分组标签、组内翻页均按数据驱动）。

## 二、测试护栏（`SymbolLayoutTest`，5 → 8 例）

- `全角半角紧随其后于编程之前`：钉住用户要求的组顺序（`全角 / 半角 / 编程`）。
- `全角组不得混入半角字符` / `半角组不得混入全角字符`：按「是否 ASCII（`< 0x80`）」判定宽度归属
，旧「常用」组正是把两种宽度混装；拆开后混装会让「按宽度选组」失去意义。
  `·—…` 这类通用标点非 ASCII，按设计计入全角组。

## 三、验证

- 门禁：`testDebugUnitTest` 35 类 / 336 例全绿（+3 例）；`lintDebug` 0 error / 28 warning（无新增）。
- 真机（PACM00，`-s BIYLBAFQQSS8DA69`，release 覆盖安装）：
  - 候选栏分组标签实测为 全角(1/11) / 半角 / 编程 / 标点 / 特殊 / 序号 / 数学 / …；
  - 全角页 26 键全为全角符号（截图：`！？。，；：""（）` / `【】《》·—…、～` / `＠＃＄％＾＆＊`）；
  - 半角页 `1/2` 为数字 + 半角符号、`2/2` 为补齐标点（日志 `符号组内翻页: 半角 第 2/2 页`）
    该状态已被同日修订覆盖：半角组的数字已删除、两页按纯符号重排（见下节）；
  - 上屏端到端：q 键上屏半角 `.`、切回全角后 z 键上屏全角 `＠`，
    `uiautomator dump` 读到输入框文本为 `.＠` ✓；操作中误触的 shift 亦已复位（`大写锁定: false`）。
- APK：`com.jinn.inputmethod.20260921151413.APK`，5.0 MB（< 8 MB 上限），`adb install -r` 覆盖成功。
- 说明：本次尚未提交／发版（工作区同时含上一轮「设置页自动检查更新」的未提交改动）。

---

# 符号面板修订：全角/半角纯度与编程组去符号页（2026-09-21）

日期：2026-09-21 ｜ 诉求（用户）：
① 全角组每个字符须为全角符号、只含全角符号、不得混入任何其它类型，严格禁止数字 0-9；
② 半角组同理（只含半角符号、禁数字）；
③ 删除编程组第 1 页的全部符号，内容直接从关键字开始依次排列。

## 一、改动

- 「半角」组删数字（上一节引入时第 1 页带 `1~0` 十个数字）：32 个可打印 ASCII 符号重排为两页各 16 键
，第 1 页 `- / : ; ( ) ' " \ = + _ | `` ` `` [ ]`，第 2 页 `. , ? ! ~ @ # $ % ^ & * < > { }`，
  两页都排满两行（10 + 6），无空行；数字一律走数字层/数字键盘。
- 「全角」组核对为纯全角（`！？。，；：""（）【】《》·—…、～＠＃＄％＾＆＊`，26 键）：
  无 ASCII、无数字、无字母/汉字，数据未动，由新增护栏钉住。
- 「编程」组删除第 1 页（原「编程常用英文符号」页：`( ) { } [ ] < > = ! ; : ' " `` ` `` ~ & | \ + - * / % ^ _`）：
  页数 7 → 6，第 1 页直接是关键字页（return / print / main / if / else …），页码注释同步重排；
  需要打符号请用「半角」组（该组已含全部 32 个 ASCII 符号，两组刚好互补）。

## 二、测试护栏（`SymbolLayoutTest`，8 → 11 例）

- `全角半角两组不得含数字`：两组全域禁用数字（`isDigit`，含全角数字 ０-９）。
- `全角半角两组不得混入字母或汉字`：`isLetter` 判据（全角字母 Ａ 与汉字都算混入）。
- `编程组第一页从关键字开始`：第 1 页每条取值必须含字母，钉住「纯符号页不得加回来」。

## 三、验证

- 门禁：`testDebugUnitTest` 35 类 / 339 例全绿（+3 例）；`lintDebug` 0 error / 28 warning（无新增）。
- 真机（PACM00，`-s BIYLBAFQQSS8DA69`，release 覆盖安装）：
  - 全角页 26 键全为全角符号（截图中无数字）；
  - 半角页 `1/2`、`2/2` 各 16 键、均无数字（截图）；
  - 编程组标签显示 1/6，第 1 页全为关键字、无纯符号（截图）；
  - 上屏端到端：编程组点 `if` → 输入框 `if`；切半角翻第 2 页点 `!` → 输入框 `if!`
    （`uiautomator` 读取，日志 `符号分组切换: 编程 (3/11)` / `符号组内翻页: 半角 第 2/2 页`）。
- APK：`com.jinn.inputmethod.20260921152006.APK`，5.0 MB，`adb install -r` 覆盖成功。
- 说明：连同上一节，本次改动仍未提交／未发版。

## 四、排版修订（同日）：一页 26 键，先放满再翻页

- 问题（用户指出）：上节把半角组排成「16 + 16」两页，第 1 页只放 16 个就翻页，留下 10 个空键，
  违反「一页 26 个符号、先放满再翻页」的排版规范。
- 修正：32 个可打印 ASCII 符号改按码点升序铺排，第 1 页 26 键满
  （`! " # $ % & ' ( ) * + , - . / : ; < = > ? @ [ \ ] ^`），第 2 页只放余下 6 个（`` _ ` { | } ~ ``）。
- 全表核对：其余 10 个分组（含日本 10 页、序号 7 页）本来就是「非末页满 26 键」，只有半角组违反；
  护栏因此升级为全表规则，非末页恒 26 键、任何页不得超 26 键
  （`SymbolLayoutTest.符号页必须先放满26键再翻页`，该类 11 → 12 例）。
- 真机（PACM00，`-s BIYLBAFQQSS8DA69`）：半角 `1/2` 为 26 键满、`2/2` 为 6 键（截图 +
  日志 `符号组内翻页: 半角 第 2/2 页`）；点第 2 页 `|` → 输入框 `|` ✓。
- 门禁：`testDebugUnitTest` 35 类 / 340 例全绿；`lintDebug` 0 error / 28 warning（无新增）。
- APK：`com.jinn.inputmethod.20260921154028.APK`，5.0 MB，`adb install -r` 覆盖成功。

---

# 符号层：返回键红色粗体 + 大写键无效（2026-09-21）

日期：2026-09-21 ｜ 诉求（用户）：进入符号页面时，左下角「返回」用红色粗体；
此时大写键不可点击（不触发大写功能）。

## 一、改动（`PinyinKeyboardView.kt`）

- `btnSymbol`：符号层的文本改用 `SpannableString` 上 StyleSpan(BOLD) + ForegroundColorSpan(kb_key_hint_red)
，用 span 而不是 `setTextColor`/`setTypeface`：退出符号层时文本换回普通字符串，样式自动还原，无状态残留。
- `btnShift`：符号层三层兜底「不可用」，`isEnabled=false`（无障碍服务也会报 disabled）
  + `alpha=0.4`（视觉置灰）+ 点击守卫（`layer == LAYER_SYMBOL` 直接 return）；字母层完全不受影响。

## 二、验证（UI 行为，无逻辑单测可抽）

- 门禁：`testDebugUnitTest` 35 类 / 340 例全绿；`lintDebug` 0 error / 28 warning（无新增）。
- 真机（PACM00，`-s BIYLBAFQQSS8DA69`）：
  - 符号层截图：「返回」为红色粗体（与旁边「数字」「中文/英文」白字对比明显），shift 明显置灰；
  - 在符号层点 shift：无任何反应，shift 不高亮、空格提示仍「中文双拼」、logcat 无 `大写锁定`
    （`isEnabled=false` 不派发点击，连守卫日志都不会有，属预期）；
  - 点「返回」回字母层：键面恢复「符号」白字常规样式，shift 恢复正常亮度；
  - 字母层点 shift：正常锁定（截图全大写 + 空格提示「大写英文」+ 日志 `大写锁定: true`），随后已复位为 `false`。
- APK：`com.jinn.inputmethod.20260921154631.APK`，5.0 MB，`adb install -r` 覆盖成功。
- 说明：本次改动仍未提交／未发版。

---

# 全角组按用户清单扩充为 68 项三页（2026-09-21）

日期：2026-09-21 ｜ 诉求（用户）：按给定清单重排「全角符号」；半角组经确认为「只留 ASCII」。

## 一、清单核对（先核对、后实施）

用户清单逐字符核对（临时脚本解析码点，跑完即删）：

| 清单 | 项数 | 核对结论 |
|---|---|---|
| 全角 | 69 | 仅 1 处重复（`，` 同时在第 1、34 项）→ 去重后 68 项；其余全部为非 ASCII，符合「全角」定义 |
| 半角 | 70 | 10 组重复（`,`×3、`"`×3、`'`×3、`<`×3、`>`×3、`[`×4、`]`×4、`.`×2、`「`×2、`」`×2）+ 21 项非 ASCII 混入（`· ¥ £ € ¢ ° ± × ÷ ℃ ‰ § ¶ © ® 「 」 『 』`）→ 按此排会直接违反既有护栏 |

两清单另有 17 项重合（`¢ § © ® ° ± ¶ · × ÷ ‰ € ℃ 「 」 『 』`）。经用户确认：半角组只留 ASCII，
无全/半角之分的通用符号统一放「全角」组，两组零重合。

## 二、改动

- 「全角」组：26 项 → 68 项三页（26 + 26 + 16），顺序即用户清单（去重后），
  第 1 页 `，。、；：？！""'（）《》【】……，·～＋－＝＜＞`；
  第 2 页 `％＆＊＠＃￥．／＼｜｛｝［］＂＇｀＿＾￡€¢°±×÷`；
  第 3 页 `℃‰§¶©®〈〉『』「」〖〗〔〕`（末页 16 项）。
- 「半角」组：不变（32 个 ASCII 符号，26 + 6 两页）。
- 对拍：临时脚本把 Kotlin 里的最终取值序列与清单逐项比对，68 项、页 26/26/16、逐项顺序一致 ✓。

## 三、测试护栏（`SymbolLayoutTest`，12 → 13 例）

- 新增 `全角与半角两组取值零重合`：钉住「半角只留 ASCII、通用符号归全角」的方案。

## 四、验证

- 门禁：`testDebugUnitTest` 35 类 / 341 例全绿；`lintDebug` 0 error / 28 warning（无新增）。
- 真机（PACM00，`-s BIYLBAFQQSS8DA69`）：全角组三页截图核对（`1/3` 26 键、`2/3` 26 键、`3/3` 16 键，
  顺序与清单一致；日志 `符号组内翻页: 全角 第 2/3 页` / `第 3/3 页`）；点第 3 页 `℃` → 输入框 `℃` ✓。
- APK：`com.jinn.inputmethod.20260921155712.APK`，5.0 MB，`adb install -r` 覆盖成功。
- 说明：本次改动仍未提交／未发版。

---

# 半角组顺序改为「与全角逐项对应」（2026-09-21）

日期：2026-09-21 ｜ 诉求（用户）：半角符号排序尽量按与全角对应的顺序，全角第 1 个是逗号，
半角第 1 个也必须是逗号。

## 一、改动

- 「半角」组由「ASCII 码点升序」改为按全角对应序排列：取 32 个可打印 ASCII 符号中
  有全角对应的 31 个，按其全角对应在「全角」组中的出现次序排列；没有全角对应的 `$` 放末尾。
  页次不变（26 + 6）：
  - 第 1 页：`,` `.` `;` `:` `?` `!` `"` `'` `(` `)` / `<` `>` `[` `]` `~` `+` `-` `=` `%` /
    `&` `*` `@` `#` `/` `\` `|`（依全角 `，。；：？！“”‘’（）《》【】～＋－＝％＆＊＠＃／＼｜` 的次序）；
  - 第 2 页：`{` `}` `` ` `` `_` `^` `$`。

## 二、测试护栏（`SymbolLayoutTest`，13 → 14 例）

- 新增 `半角组顺序与全角组逐项对应`：半角每项的「全角对应」（取全角组首次出现者）
  在全角序列里的下标必须单调递增；无全角对应者只允许排在末尾。

## 三、验证

- 门禁：`testDebugUnitTest` 35 类 / 342 例全绿；`lintDebug` 0 error / 28 warning（无新增）。
- 真机（PACM00，`-s BIYLBAFQQSS8DA69`）：半角 `1/2` 首位为 `,`（与全角首位 `，` 对应）、末位 `|`；
  `2/2` 为 `{` `}` `` ` `` `_` `^` `$`（截图）；点第 2 页 `{` → 输入框 `{` ✓。
- APK：`com.jinn.inputmethod.20260921160541.APK`，5.0 MB，`adb install -r` 覆盖成功。
- 说明：本次改动仍未提交／未发版。

---

# 默认值调整：词频/预测/生僻字关、圆角 1dp、间隙 0.5dp（2026-09-21）

日期：2026-09-21 ｜ 诉求（用户）：「以下作为默认值」，生僻字关、词频学习关、预测词关、
按键圆角 1、按键间隙 0.5、剪贴板历史数量上限 500。

## 一、改动（只改「未写入过」时的取值，不动任何已保存的配置）

| 项 | 原默认 | 新默认 | 落点 |
|---|---|---|---|
| 生僻字 | 关 | 关（不变） | `Prefs.showRareChars` |
| 用户词频学习 | 开 | 关 | `Prefs.userLearning` |
| 候选预测词 | 关 | 关（不变） | `Prefs.predictEnabled` |
| 按键圆角 | 0dp | 1dp | `KeyAppearance.DEFAULT_CORNER_DP` |
| 按键间隙 | 0dp | 0.5dp | `KeyAppearance.DEFAULT_GAP_DP` |
| 剪贴板上限 | 500 | 500（不变） | `ClipboardPrefs.DEFAULT_MAX_ITEMS` |

- 圆角/间隙的默认值同时是 NaN/Infinity 的兜底值（`snap` 的 fallback），因此仍落在定义域与步进网格上。

## 二、测试（`KeyAppearanceTest`）

- `defaults_areInsideDomainAndKeepHistoricalLook` → `defaults_areInsideDomainAndOnStepGrid`：
  断言 1dp / 0.5dp 落在定义域内、且能被钳位原样保留（否则设置页显示与实际值不符）。

## 三、验证

- 门禁：`testDebugUnitTest` 35 类 / 342 例全绿；`lintDebug` 0 error / 28 warning（无新增）。
- 真机（PACM00，`-s BIYLBAFQQSS8DA69`）：设置页读数与设备已存配置一致（生僻字关 / 词频关 / 预测关 /
  圆角 3dp / 间隙 0.5dp / 剪贴板上限 500）。
  默认值只对从未写入过的键生效：设备上 `key_corner_dp=3.0` 是用户此前手动调过并已保存的值，
  不会被新默认覆盖（如需改为 1dp，请在设置页拖动该滑块）。
- APK：`com.jinn.inputmethod.20260921162814.APK`，5.0 MB，`adb install -r` 覆盖成功。
- 说明：本次改动仍未提交／未发版。

---

# 亮白/暗黑主题 · 批次 1：双色板落地（2026-09-21）

日期：2026-09-21 ｜ 范围：主题方案的第 1 批（色板基础设施）；批次 2（手动/定时切换 + 设置页入口）未实施。

## 一、改动

- `values/colors.xml` 重写为亮白色板（默认目录 = 跟随系统浅色），新增 `values-night/colors.xml`（暗黑 = 原值）。
  两套逐名对齐（40 个令牌 + 5 个新收敛令牌）；缺名会静默退回默认值，已在注释中警示。
- 5 处 drawable 硬编码色收敛为令牌（两套主题各给值）：
  `btn_aurora_primary`（涟漪 `#33FFFFFF`→`ripple_on_accent`）、`btn_aurora_secondary`（`#26FFFFFF`/`#1E2A4A`→
  `ripple_on_surface`/`btn_secondary_bg`）、`field_aurora`（`#101830`×2→`field_bg`）、`grip`（`#3A3F48`→`grip`）。
- `themes.xml` 两套：默认目录父主题改 `Theme.Material.Light.NoActionBar` + `windowLightStatusBar=true`
  （浅底深图标）；新增 `values-night/themes.xml`（`Theme.Material` + `Light*=false`，整条 style 完整重写）。
  这同时也解决了亮色下 AlertDialog 仍是深色框架配色的问题。
- 两处按实测微调（原值对比度不足，WCAG 2.1）：
  `danger` 暗色 `#E5484D`(4.31:1) → `#FF5A5F`(5.53:1)；
  `text_on_accent` 暗色 `#FFFFFF`（渐变上仅 1.77~4.23:1）→ `#0B1020`（4.47~10.68:1，按钮 bold 15sp 属大字，门槛 3:1）。
- 其它令牌保留原语义：浅色键面涟漪改用黑（`#1A000000`），麦克风/状态点/候选选中字等逐令牌给亮色值。

## 二、验证（对比度用脚本实测，跑完即删）

- 亮色实测：正文/次文字 15.36 / 5.58:1，accent 4.88、danger 5.62、ok 5.87、warn 6.39，
  键盘正文 16.47、候选选中字 4.63、渐变按钮白字 5.00~6.11，全部 ≥4.5:1；
  `card_stroke` 1.31:1 属装饰性边界（WCAG 1.4.11 豁免）已在文档标注。
- 门禁：`testDebugUnitTest` 35 类 / 342 例全绿；`lintDebug` 0 error / 28 warning（无新增）。
- 真机（PACM00，系统深浅色经 root 通道切换：`su -c "cmd uimode night no|yes"`，普通 adb 改不动）：
  - 浅色：设置页浅底白卡 + 深色文字、状态栏图标转深、主按钮深渐变白字（截图）；
    键盘同步转浅（`kb_bg #EDF1F9` / 白键面 / 深字 / 灰红双拼提示）（截图）；
  - 深色：设置页恢复极光深色、主按钮亮渐变 + 深墨字（新值可读性明显改善）、键盘恢复深色（截图）；
  - 切换均无白屏/黑屏闪烁（Activity 随系统配置重建，启动窗口即主题色）。
- APK：`com.jinn.inputmethod.20260921165859.APK`，5.0 MB，`adb install -r` 覆盖成功。
- 说明：批次 1 后 App 的主题等于跟随系统；「手动/定时切换」需批次 2（`Prefs` 主题键 + `ThemeManager` +
  `attachBaseContext`/IME 的 `uiMode` 覆盖），已在 `AGENTS.md`「主题」章节登记为待办。

---

# 主题 · 批次 2：手动 / 定时切换（2026-09-21）

- 新增 `ThemeManager`：`isDarkNow` / `minutesUntilSwitch` 纯函数 + `wrapContext`（只覆盖 uiMode 的 night 两位）。
- `Prefs` 新增 `theme_mode`（默认 2 = 暗黑）/ `theme_light_at` / `theme_dark_at`（默认 07:00 / 19:00）。
- 设置页「主题」卡片：模式下拉 + 定时的两个 TimePicker 按钮 + 说明；改任一值即 `recreate()` 立即换色；
  停在页面时按 `minutesUntilSwitch` 排一次到点刷新（结果变了才重建）。
- 应用点：两个 Activity 的 `attachBaseContext`、IME 的 `onCreateInputView`（`themedContext` 建键盘）
  与 `onStartInputView`（决策结果变了才 `setInputView` 重建）。

## 二、验证（真机 PACM00；验证期间系统始终为亮色，用于区分「强制」与「跟随」）

- 默认（暗黑）：系统亮色下设置页仍为暗色 → 强制的 uiMode 覆盖生效；
- 选「亮白」（日志 `主题模式: 1`）：设置页与键盘立即变亮；
- 选「定时」：默认窗口内保持亮色，把「亮起」改为 18:00 后立即变暗（日志 `定时切换: 亮起 18:00`），
  键盘同步重建（`主题变更: 重建键盘（暗黑）`）；TimePicker 对话框本身也随主题变色；
- `ThemeManagerTest` 11 例（模式 / 跨零点窗口 / 无效配置 / 脏时刻 / 下一次切换分钟数）；
- 现场已还原：`theme_mode=2`（默认暗黑）、`theme_light_at=420`，系统 uiMode 复原为 yes（暗色）；
- APK：`com.jinn.inputmethod.20260921171056.APK`，5.0 MB，覆盖安装成功。仍未提交／未发版。

## 三、默认值改为跟随系统（同日晚）

- `Prefs.themeMode` 默认由 2（暗黑）改为 0（跟随系统）（越界兜底同步改为跟随）；设置页下拉同步。
- 真机（PACM00，`-s BIYLBAFQQSS8DA69`）：设置页把模式选为「跟随系统」（`theme_mode=0`）后，
  系统切亮色 → 设置页立即变亮白（截图）；系统切回暗色已复原（`Night mode: yes`）。
- 门禁：`testDebugUnitTest` 35 类 / 353 例全绿；`lintDebug` 0 error / 28 warning。
- APK：`com.jinn.inputmethod.20260921173523.APK`，5.0 MB，覆盖安装成功。

---

# 当日改动全量审查与修复（2026-09-21）

范围：`ea40239`（符号面板 / 主题批次 1-2 / 默认值 / 自动检查更新）+ 审查后新增修复。

## 一、修复的缺陷（2 项）

1. 键盘可见时改主题不换肤（`JinnIme`）：原实现只在 `onStartInputView` 判定，设置页改主题要等下次弹出。
   新增同进程通知 `JinnIme.notifyThemeChanged()`（设置页三处改动点调用），判定逻辑抽成
   `applyThemeIfNeeded()` 两条路径共用；`instance` 为 `@Volatile` 可空、`onDestroy` 置空。
2. 定时模式跨切换点不换肤：键盘可见期间跨过 19:00 等切换点没有任何触发源。
   新增 `scheduleThemeTick()`（`postDelayed` 到下一切换点 +2s，`onFinishInputView` 取消），非定时无操作。

## 二、新增护栏（`ThemeColorParityTest`，2 例）

- `亮白与暗黑色板逐名对齐`：两边 `<color name>` 集合必须相等，缺名不报错，只在真机上表现为
  「某部件颜色不对」；`两套色板都非空` 防路径写错导致假绿。

## 三、逐项审查结论

| 维度 | 结论 |
|---|---|
| 功能正确性 | 修复 2 项；其余路径真机验证通过（强制暗黑 / 亮白 / 定时窗口内、键盘同步、TimePicker 随主题） |
| 边界条件 | 跨零点、`light==dark` 无效配置、脏时刻 `floorMod` 归一均已单测；`minutesUntilSwitch` 恰在切换点返回 1440（避免 0 被当「立即切换」） |
| 异常处理 | `wrapContext` 只改 uiMode 两位、其余配置原样；`notifyThemeChanged` 无实例静默返回；tick 在键盘收起 / 服务销毁时取消 |
| 并发安全 | `instance` 为 `@Volatile` 可空；tick 走主线程 Handler；无跨线程共享可变状态 |
| 性能影响 | 只在「决策变化」时重建（判重）；`wrapContext` 每次 UI 创建仅一次；自绘取色在构建/刷新期，无每帧取色 |
| 代码规范 | 判定逻辑全为纯函数（可 JVM 单测）；drawable 已无硬编码色（5 处收敛为令牌） |
| 兼容性 | `windowLightNavigationBar` 为 API 27+：API 26 设备亮色下导航栏图标仍浅色（不崩，已知限制；目标机 Android 10） |
| 文档一致性 | `AGENTS.md`「主题」章节与本节同步本次修复、默认「跟随系统」 |

## 四、验证

- 门禁：`testDebugUnitTest` 35 类 / 355 例全绿；`lintDebug` 0 error / 28 warning（无新增）。
- APK：`com.jinn.inputmethod.20260921175813.APK`（5.0 MB）已覆盖安装；现场已还原
  （`theme_mode=0`、系统暗色、定时时刻仍为 07:00 / 19:00）。

## 九、用户实测反馈修复：长按拖拽「无反应」的根因与交互强化（同日）

用户实测：长按分组约 2 秒后"没有能拖拽的功能"。

根因（真机日志 + 代码审查定位）：
1. 进入拖拽的视觉反馈过弱（alpha 0.85 + elevation 6dp，浅色候选栏上几乎不可见），
   2 秒到点后用户察觉不到已进入拖拽；
2. 拖动中其它分组不让位、也无落点指示，被拖项跟手的位移观感像"没反应"；
3. 真手指长按期间的超阈值抖动（>24px）会取消长按计时（与"横向滚动标签栏"的前提冲突，
   保留取消逻辑）；`input swipe` 为匀速手势、无法注入「先按住 2s 再移动」，自动化难以覆盖。

修复（交互强化，触发阈值保持 2 秒）：
- 进入拖拽：被拖项 `scale 1.15 + elevation 8dp`，其余全部分组变暗（alpha 0.45），一眼可见；
- 拖动中：被拖项跟手，落点槽位相对提亮（0.85），给明确的落点预期；
- 松手：全部复位，顺序即时生效并落盘（`SymbolOrder.move`，单测覆盖）；
- 落点换算：`位移 / 被拖项宽度` 四舍五入到相邻槽位，越界钳到两端。

验证：门禁 35 类 / 357 例全绿、lint 0 error / 28 warning；真机日志确认
`符号分组: 长按 2000ms 进入拖拽排序（起点 2）`；拖动落位与持久化由 `SymbolOrderTest` 覆盖
（APK：`com.jinn.inputmethod.20260921205628.APK`，5.0 MB）。

## 十、排序改版：设置主页单按钮 + 独立排序页（同日）

用户反馈：① 面板长按拖拽实测不可用；② 排序列表放在设置主页太占空间 → 改为「一个按钮 + 独立页面」。

改动：
- 移除输入法面板上的长按拖拽整套交互（`bindGroupTouch` / `dropGroup` / `highlightDropSlot` 等），
  面板分组按钮恢复为纯点击切组；
- 设置主页该区域改为一个按钮（`SettingsButton` 样式「符号分组顺序」，主题卡片下方），点击打开排序页；
- 新增 `SymbolOrderActivity`（独立排序页，`ThemeManager.themedContext` 跟随亮暗主题）：
  每行一个分组 + ↑↓ 调整（首/末行相应箭头自动禁用）+ 「恢复默认顺序」按钮；改动即时落盘；
- 生效时机：离开排序页（`onPause`）→ `JinnIme.onSymbolOrderChanged()`（companion 弱引用转发）→
  键盘视图已创建则重建（日志 `符号分组顺序变更: 重建键盘视图`），顺序立即生效；未创建则等下次弹出。

验证：门禁 35 类 / 357 例全绿、lint 0 error / 27 warning（较基线少 1：删除内嵌列表顺带消了
`DisableBaselineAlignment`，无新增）；真机 root 拉起排序页截图核对（11 行 + ↑↓ + 恢复默认按钮，
首/末行箭头正确禁用）；设置主页该区域已收缩为单按钮。↑↓ 点击与键盘生效由用户实测。

## 十一、排序页视觉微调（同日）

- ↑↓ 按钮换主题配色（用户反馈系统默认灰底与极光主题不符）：
  `btn_aurora_secondary` 半透明蓝底圆角 + 白字，与「恢复默认顺序」一致；禁用态仍半透明；
- 右上角新增 ✕ 关闭按钮（同款样式，点击 `finish()` 关页）。
- 验证：lint 0 error / 27 warning；真机截图核对（✕ 与全部箭头均为主题底色、禁用态正确）。

---

# 新增「收藏」分组：用户自由 DIY 符号（2026-09-21）

日期：2026-09-21 ｜ 诉求（用户）：符号面板新增「收藏」分组，默认第三位（全角/半角/收藏），
同样可排序；组内符号由用户自由增删，满 26 个自动新增页。三个决策点经确认：出厂预置 D I Y、
添加仅输入框、禁止重复。

## 一、改动

- `FavoriteSymbols.kt`（新）：收藏数据纯函数，存 `Prefs.favoriteSymbols`
  （JSON 二维数组：页 → 符号）；出厂预置 `D I Y`；`null`（从未编辑）→ 预置、`"[]"`（删光）→
  空组不回退预置、损坏 JSON → 回退预置；追加全局去重（≤ 8 字符）、末页满 26 自动开新页；
  删除「扁平化 → chunked 重排」= 前移补位 + 空页收起（天然满足「非末页恒 26 键」全表规范）。
- `KeyboardLayouts.kt`：`SYMBOL_GROUPS` 常量不含收藏（10 组不变，全部既有护栏照旧）；
  新增 `favoriteGroup(pages)`，页内符号按序铺 26 键位（QWERTY 行序）。
- `SymbolOrder.kt`：`DEFAULT` 插入「收藏」到第 3；`normalize` 兼容旧顺序串
  （无「收藏」→ 插回「半角」之后，不打乱用户已调顺序）；`groupsInOrder` 接收动态收藏组。
- `PinyinKeyboardView.kt`：`symbolGroups` 初始化改为读排序 + 收藏内容组装，
  候选栏 / 分组切换 / 翻页 / 上屏逻辑零改动（收藏就是普通分组）。
- `FavoriteSymbolsActivity`（新）+ 布局：按页分节网格，每格右上角红色「移除」角标
  （删除即时落盘重渲染）；底部「＋ 添加符号」弹输入框对话框（自由键入/粘贴，剥换行制表符；
  空/超长提示、重复提示「该符号已在收藏中」）；右上 ✕ 关页。
- 设置页：「编辑收藏符号」按钮（「符号分组顺序」上方，同款样式）；Manifest 注册；
  排序页/编辑页共用 `JinnIme.onSymbolLayoutChanged()`（离开页面重建键盘视图，立即生效）。

## 二、测试护栏（370 例，+13）

- `FavoriteSymbolsTest`（7 例）：预置/空态语义、序列化往返、去重与长度校验、满 26 翻页、
  删除补位/收页/越界、默认顺序第三位 + 旧串兼容、页内按序铺键位。
- `SymbolOrderTest` 3 例适配（收藏进默认序 + 插位兼容 + `groupsInOrder` 带入收藏）；
  `SymbolLayoutTest` 全部照旧（收藏不在常量里，内置 10 组护栏零影响）。

## 三、验证

- 门禁：`testDebugUnitTest` 370 例全绿；`lintDebug` 0 error / 27 warning（无新增）。
- 真机（PACM00，`-s BIYLBAFQQSS8DA69`，release 覆盖安装）：编辑页 root 拉起截图核对，
  标题 + ✕、说明、「第 1 页（3 个）」+ `D I Y` 三格（红色「移除」角标）、底部「＋ 添加符号」均正常。
- APK：`com.jinn.inputmethod.20260921221613.APK`，5.0 MB，`adb install -r` 覆盖成功。
- 待用户实测：设置页按钮进入编辑 → 添加/移除符号 → 返回键盘看「收藏」组（第三位）键面与上屏；
  排序页对「收藏」的 ↑↓ 调整。
- 说明：本次改动未提交／未发版（发版待用户实测确认）。
- 定时「到点自动换肤」未做真机跨点观测（设备时钟无法快进）：由 `ThemeManagerTest` 的
  `minutesUntilSwitch` 边界用例 + 代码审查覆盖，下次真机跨点时可直接看日志 `主题变更: 重建键盘`。

## 五、审查（同日）与 3 项修复

1. 【中】服务层取色用错 Context（`JinnIme.renderLink`）：状态点 `getColor()` 走的是系统配置，
   强制亮白/暗黑时拿到另一套色板（亮色键盘上用亮色的绿/黄/红，对比度下降）。新增 `keyboardThemeCtx`
   （键盘创建时的主题覆盖 Context），服务层取色一律走它，未创建时退回自身。
2. 【低】主题时刻 setter 未归一（`Prefs`）：写入端不钳位，负数/超 24h 的脏值会落盘；读取端虽已兜底，
   但绕过 Prefs 的读方会踩。改为 `Math.floorMod` 归一。
3. 【低】`StaticFieldLeak`：`instance` 静态强引用 Service 触发 lint 新警告（29 条）→ 改
   `WeakReference`，`onDestroy` 置空；lint 回到 0 error / 28 warning（无新增）。
4. 护栏补强：`ThemeColorParityTest` 增 `两套主题定义的item逐名对齐`（`values-night/themes.xml`
   少写 item 会退回框架默认值且不报错，与缺色板名同类）。
5. 验证：`testDebugUnitTest` 35 类 / 356 例全绿；真机覆盖安装后设置页 + 键盘（跟随系统=暗色）无回归；
   状态点仅在语音键盘显示（本机语音关闭），该修复以代码路径复核 + 与键盘同源 Context 的方式验证。

## 六、审查（同日）与修复

1. 【低】`Prefs.themeMode` 写入与读取标准不一致：setter 用 `coerceIn`，非法值（如 9）会落成
   「定时」（最接近的合法值），而读取端与 `isDarkNow` 的 else 分支都按「跟随系统」，
   同一非法值两条路径解释不同。改为与读取端一致：非法一律落「跟随系统」。
2. 【护栏】主题下拉 values 与常量对应：`theme_mode_values` 是 XML 裸数字，改错一位就会
   「选亮白存成暗黑」且编译/lint 都不报错 → 新增 `主题下拉取值与常量一一对应`
   （解析 `strings.xml` 与 `ThemeManager.MODE_*` 逐一比对）。
3. 【规范】测试中 `assertEquals(true, cond)` → `assertTrue(cond, msg)`（失败时能给出原因）。

核对后判定「非缺陷」的两处（记录标准，避免下次重复怀疑）：

- 全角组 68 项经码点逐项核对无误（`《` U+300A 与 `〈` U+3008 各 1 个）：PowerShell 的 `Group-Object`
  会把这两个字符折叠成同一键，从而产生「组内重复」的假象，字符级查重必须走码点（Python），
  不能用 PowerShell 分组。
- `SettingsActivity` 的定时 tick：`onDestroy` 已 `removeCallbacks(themeTickRunnable)`，无泄漏。

验证：`testDebugUnitTest` 35 类 / 357 例全绿；`lintDebug` 0 error / 28 warning；
真机覆盖安装后设置页正常（跟随系统＝暗色，生僻字/词频学习开关均为关，与默认值一致）。

## 七、审查（同日）：复核「是否误判」+ 2 项修复

前几轮结论的复核（如实标注成色）：

| 前轮结论 | 复核 |
|---|---|
| 键盘可见时改主题不换肤 | 真缺陷（原实现无同进程通知路径）→ 保留 |
| 定时跨点不换肤 | 真缺陷；但该修复引入了新问题，见下 #1 |
| 状态点取色用系统配置 | 真缺陷（强制模式下色板错位）→ 保留 |
| 时刻 setter 未归一 | 非用户可达（读取端已兜底）：防御性一致，保留无害 |
| `StaticFieldLeak` | 真告警（我引入的第 29 条 lint）→ 修复必要 |
| `themeMode` setter 标准 | 非用户可达（写入方恒 0..3）：防御性一致，保留无害 |
| 三组护栏（色板/主题 item/下拉 values） | 非 bug，防未来改错 |

本次新发现并修复：

1. 【中】换肤会静默丢弃未上屏内容（上一轮「定时到点 tick」引入）：到点时若键盘可见且正在打拼音，
   `setInputView` 重建会丢掉拼音串/预测词，宿主输入框毫无变化，用户以为输入被吞。
   修复：新增 `PinyinKeyboardView.hasPendingInput`，`applyThemeIfNeeded` 先判，有未上屏内容就
   延后到下次弹出换肤（日志 `主题变更: 键盘有未上屏内容，延后到下次弹出换肤`），不打断输入。
2. 【文档】`minutesUntilSwitch` 注释不准确：原写「恰好站在切换点给 1440 分钟」，实际是
   「到另一个端点的距离」（站在 07:00 得 720），测试期望本正确，只是注释错，已订正。

验证：`testDebugUnitTest` 35 类 / 357 例全绿；`lintDebug` 0 error / 28 warning；
真机覆盖安装后设置页 + 键盘日志正常（`onCreateInputView: 键盘视图创建` / `PinyinKeyboardView 初始化完成`，
无 FATAL、无异常重建）。「延后换肤」分支需「定时到点 + 键盘可见 + 有拼音串」三者同时成立，
真机时序不可构造，以代码路径复核。

## 八、审查（同日）：多角度核实，未发现新的真实缺陷（零代码改动）

逐项核查与结论（凡判定"非缺陷"的一并写出理由，避免下次重复怀疑）：

| 核查项 | 结论 |
|---|---|
| 14 组配色对比度（双拼红字提示 / 次级按钮文字 / 输入框提示 / 按压键面 / 状态点三色 × 亮暗两套） | 全部达标：最差 4.84:1（暗色红字提示，门槛 4.5）、状态点最差 4.97:1（门槛 3.0） |
| `DictManagerActivity` 的主题应用 | 已有 `attachBaseContext` → `themedContext`，无遗漏 |
| `Prefs` 构造开销（每键 `predictionsEnabled()` 会 new 一个） | 仅 `applicationContext.getSharedPreferences`（系统按名缓存同一实例），可忽略 |
| `ThemeManager.wrapContext` 的异常风险（`createConfigurationContext`） | 判定非缺陷：无既定异常路径；为假想问题加 try/catch 属增码不增健壮性，按「只修真 bug」原则不改 |
| `applyThemeIfNeeded` 的语音键盘路径（`pinyinKeyboard == null`） | 守卫为 `?.hasPendingInput == true` → 语音键盘走重建，行为正确 |
| 安全面（今日新增代码） | 无新增权限/导出组件/外部输入；日志只记模式与时刻，无敏感信息 |

真机补验（此前缺失的方向）：系统暗色下强制「亮白」→ 设置页与键盘同时变亮
（白键面 / 深字 / 浅候选栏 / 红字提示可见），证明 IME 侧覆盖 Context 在"系统暗 → 强制亮"方向同样生效
（此前只验过反向）；随后已还原 `theme_mode=0` + 系统暗色。

## 四、发版：`20260921`（新 tag）

- 提交 `ea40239` 推送 main：GitHub + Gitee 双端 `0f658bb..ea40239`。
- 产物：`jinn-release.apk` 5,200,722 字节 / `sha256=c1db99af…` / `versionCode=20260921`。
- GitHub：`gh release create 20260921`（含附件）；Gitee：新建 Release（id `1157859`）+ 附件（id `3240334`）。
- 校验：双端下载 URL 实测 `size=5,200,722`、`sha256` 与本地发布包三方完全一致 ✓

---

# 审查修复轮：parse 归一 + 重建守卫扩展 + dirty 标记 + 注释/死代码订正（2026-09-21）

背景：「收藏」分组功能（`2f3b297`/`0728ee5`）经三轮审查后，只实施低成本、正向的修复；
「多点触控并发删除」（未实证）与 `renderPages` 卡片化等过度修复项明确不做（记录在案）。

## 一、收藏符号数据归一（`FavoriteSymbols.parse`）
- `parse` 输出恒为规范结构：全局去重（`LinkedHashSet` 保序）+ `chunked(PER_PAGE)` 重新分页，
  空页自然消解。原缺陷：`favoriteGroup` 只铺 26 键位、超出部分静默丢弃 →
  「编辑页看得到第 27 个、键盘上看不到」（外部脏数据或被改写时才可达）。
- 页边界属实现细节：非规范结构会被合并（`[[3],[1]]` → `[[4]]`），符号集合与顺序不变；
  规范结构（非末页恒 26）下重新分页是恒等变换，正常使用无感。
- 测试：往返用例改为「内容/顺序稳定 + 页结构归一」；新增「超页重排不丢符号 + 去重/空页清理」
  与「满页 26 项恰好铺满 26 键位」（`PER_PAGE` 与键盘键位数的耦合护栏）。

## 二、重建守卫扩展（换肤 + 符号布局）
- 新增 `PinyinKeyboardView.hasActiveOverlay`（剪贴板面板 / 搜索面板）；`hasPendingInput` 保持
  「未上屏拼音/预测」原语义；换肤延后判据改为两者合并，修复「定时到点换肤会关掉用户正开着的
  剪贴板面板/搜索面板」（此前只挡未上屏输入）。
- `rebuildInputViewForSymbolLayout` 补 `hasPendingInput` 守卫（此前无条件重建，同款缺陷的另一路径）。

## 三、dirty 标记（不做无谓重建）
- `FavoriteSymbolsActivity` / `SymbolOrderActivity`：仅实际写入后（增删符号 / ↑↓ 调整 / 恢复默认）
  才在 `onPause` 通知 IME 重建；进来看看就退出不再整块重建。标记不复位（守卫可能延后重建，
  下次 `onPause` 再通知一次；重建幂等）。

## 四、注释与死代码订正
- 删除两个 0 调用的 `companion object { fun start }`（`SymbolOrderActivity` / `FavoriteSymbolsActivity`；
  均 `exported=false`，原注释「外部组件拉起」的理由不成立）。
- `Prefs`：`themeLightAtMinutes` 的孤儿 KDoc 归位；`symbolGroupOrder` 改「排序页写入」并注明
  「写入即归一（落盘恒为完整序列串）」；`clipboardPanelOpen` 注释改「仅供诊断日志」（恢复逻辑已移除）。
- `SymbolOrderTest` 类注释去掉「长按拖拽排序」过期表述。

## 五、验证
- 门禁：`testDebugUnitTest` 39 类 / 373 例全绿（+3）；`lintDebug` 0 error / 27 warning（与基线持平）。
- 真机（PACM00，root 通道 + 截图逐项核对）：
  1. 编辑页预置 `D I Y` 正常；添加 `Z` → 保存即时生效（第 1 页 4 个）；
  2. 关闭页面 → 日志 `符号分组顺序/收藏变更: 重建键盘视图`（计数 +1）；
  3. 未改动进出编辑页 → 计数不变（dirty 生效）；
  4. 键盘符号层 →「收藏」组（第 3 位，旧顺序串自动插位）键面 `D I Y Z`，点 `D` 正常上屏；
  5. 删除 `Z` → 页面回到 3 个 → 关闭 → 计数再 +1（删除同样触发重建）；
     prefs 落盘 `[["D","I","Y"]]` 正确。
- APK：`com.jinn.inputmethod.20260921231707.APK`，5.0 MB，ROOT 通道安装（`adb push /data/local/tmp`
  + `su -c 'pm install -r'`）`Success`。
- 发布前多角度复审：`git diff` 逐文件复核 + 临时探针实证（100 项正确分页 `[26,26,26,22]`、
  「实际累计长度」与「页序号×26」两种删除下标逐项相等、非规范/非字符串/深层嵌套 JSON 一律回退预置不崩、
  归一幂等），探针已删除；结论：未发现缺陷。

## 六、发版：`20260921`（同日覆盖式，tag 不变）

- 版本号 `20260921`（= `versionCode`；同日多次发布覆盖同一 tag 的 Release）。
- 提交 `6b1abd8` 推送 main：GitHub + Gitee 双端 `0728ee5..6b1abd8`。
- 产物：`jinn-release.apk` 5,217,218 字节 / `sha256=caadaae9…`；ROOT 通道安装 `Success`。
- 覆盖附件：GitHub `gh release upload --clobber`；Gitee 删旧附件（id `3240334`）→ 传新附件（id `3242216`）。
- 正文：双端追加本次条目（原文逐字保留）；Gitee `PATCH` 按规范同时带 `tag_name` + `name`。
- 校验：双端下载 URL 实测 `size=5,217,218`、`sha256` 与本地发布包三方完全一致 ✓

---

# 设置页「符号布局」入口改版：分组顺序 + 收藏编辑并排（2026-09-21）

- 原来两个各占整行的按钮（「编辑收藏符号」在上、「符号分组顺序」在下）改为同一张圆角卡片内并排：
  外层圆框与「启用输入法 / 切换输入法」组完全同款（`card_aurora` + `padding=12dp` + 两个
  `layout_width=0dp / weight=1` 按钮，中间 `marginStart=8dp`）。
- 顺序按用户要求：左「符号分组顺序」、右「编辑收藏符号」；按钮文案沿用既有字符串资源
  （与编辑页标题一致），两个按钮 `id` 未变，`SettingsActivity` 绑定逻辑零改动。
- 验证：`testDebugUnitTest` 39 类 / 373 例全绿；`com.jinn.inputmethod.20260921232129.APK`（5.0 MB）
  ROOT 通道覆盖安装 `Success`；设置页截图核对（同一行两按钮 + 圆角外框，与相邻组样式一致）。
- 说明：本次未发版（如需发布可覆盖 `20260921`）。

---

# 设置页布局与文案改版：按用户指定顺序改为两列网格（2026-09-21）

需求（用户）：主页面的排序与文本照指定内容改，能同排的两两一行，「没提到的保持原样」。

## 新顺序（自上而下）
1. 标题 / 保存配置并立即重启生效（原样）
2. 点这里唤起键盘（输入法测试，当时卡片里还有「发送」按钮与已收文本；同日稍后简化为纯编辑框），由第 3 位提到最前
3. 检查软件更新 ｜ 打开下载页面
4. 启用本输入法 ｜ 改默认输入法
5. 符号分组顺序 ｜ 收藏的自定义
6. 拼音输入方案 ｜ 键盘主题配色（两个下拉并排；「定时」行的两个时刻按钮与其说明保留在同卡片下方，
   非定时模式下随原逻辑整体隐藏）
7. 自动唤起键盘 ｜ 加更多生僻字（两个开关并排，各自带说明小字）
8. 用户词频学习 ｜ 追加候选词语（同上）
9. 键盘外观 / 分类词库 / 剪贴板 / 授权麦克风 / NAS 语音 / 语音开关 / 导出诊断 / 使用说明（原样）

## 文案改动（strings.xml）
- 检查APP更新 → 检查软件更新；启用输入法 → 启用本输入法；切换输入法 → 改默认输入法
- 显示生僻字 → 加更多生僻字；候选预测词 → 追加候选词语；输入方案 → 拼音输入方案；主题 → 键盘主题配色
- 输入法测试提示「点这里 唤起键盘」→「点这里唤起键盘」
- 用户手改的 `favorite_title`（符号收藏编辑）保留（编辑页标题 / 最近任务 label 用）；
  设置页按钮改用新增字符串 `favorite_entry_title` = 收藏的自定义
- `symbol_order_title` 按本次要求为「符号分组顺序」（排序页标题随之一致）
- 各开关的说明小字未改（不在清单内，保持原文）

## 验证
- 布局 44 个控件 id 逐一核对齐全（无新增 / 遗漏）；`testDebugUnitTest` 39 类 / 373 例全绿
- `com.jinn.inputmethod.20260921233800.APK`（5.0 MB）ROOT 通道覆盖安装 `Success`
- 真机截图逐项核对：1-8 行的顺序、文案、两列并排与圆角外框均与要求一致；键盘外观及之后各卡片原样
- 说明：本次未发版（如需发布可覆盖 `20260921`）。

---

# 新增「补充短语词库 / 按钮圆角间隙」入口行 + 键盘外观独立页（2026-09-22）

需求（用户）：在「符号分组顺序 ｜ 收藏的自定义」的下一行放置两个按钮（补充短语词库 / 按钮圆角间隙），
两者都打开独立页面设置。

## 改动
- 新入口行（符号布局行正下方，两按钮并排）：
  - 「补充短语词库」→ 打开既有的 `DictManagerActivity`（长词包 / 腾讯大词库按需下载）
  - 「按钮圆角间隙」→ 打开新建的 `KeyAppearanceActivity`
- 新增页面：`KeyAppearanceActivity` + `activity_key_appearance.xml`，标题「键盘外观」+ ✕ 关闭 +
  圆角 / 间隙两个滑杆（定义域与换算一律走 `KeyAppearance`，与绘制逻辑共用一套边界）；
  拖动即落盘，键盘下次弹出套用，不重启进程
- 设置页移除两个旧卡片（内容已迁出，避免重复入口）：「键盘外观」卡片（滑杆迁入新页）、
  「分类词库」卡片（入口改为上方按钮）
- 清理：`SettingsActivity` 删除 `btnDictManager` 与四个滑杆的字段 / 绑定 / 两个 init 方法及 `SeekBar` 导入；
  删除不再引用的字符串 `dict_entry_title` / `dict_entry_desc` / `dict_entry_open`；
  Manifest 注册新页（`exported=false`）
- 新增字符串：`dict_entry_button` =「补充短语词库」、`key_appearance_entry` =「按钮圆角间隙」
- 注：本次同时入库用户手改的 6 处说明文案（自动唤起键盘 / 加更多生僻字 / 用户词频学习 /
  追加候选词语 / 拼音输入方案 / 键盘主题配色 的说明小字）

## 验证
- `testDebugUnitTest` 39 类 / 373 例全绿；编译 + 签名 + 对齐 → `com.jinn.inputmethod.20260922000933.APK`（5.0 MB）
- ROOT 通道（`adb push` + `su -c 'pm install -r'`）覆盖安装 `Success`
- 真机逐项核对：设置页新增行两按钮就位；点「按钮圆角间隙」→ 新页正常（标题 / 说明 / 两滑杆 / 数值）；
  滑杆 tap → 落盘 `key_corner_dp=23.0` + 日志 `KeyAppearance: 键盘圆角: 23 dp`，
  再点回 → 精确恢复 1.0 dp（用户原值）；点「补充短语词库」→ 日志 `DictManagerActivity: 打开分类词库页`，页面正常
- 说明：本次未发版（如需发布可覆盖 `20260921`）。

---

# 输入法测试简化为纯编辑框 + 键盘外观页新增测试编辑框（2026-09-22）

需求（用户）：设置页首行的「点这里唤起键盘 + 发送 +（尚未收到任何文本）」太复杂，只保留编辑框；
键盘外观新页面同样加一个（复刻）编辑框，方便随时唤起 / 收起键盘对照调参。

## 改动
- 设置页：`edit_ime_test` 卡片简化为单一编辑框（`match_parent`；`imeOptions` 由 `actionSend` 改为 `actionDone`）；
  删除 `btn_ime_send` / `text_ime_received` 两个控件
- `SettingsActivity`：删除 `editImeTest` / `btnImeSend` / `textImeReceived` 字段与绑定、`sendImeTest()` 方法
  （含本地回显逻辑）、按钮监听与编辑框回车发送监听（`IME_ACTION_SEND`）
- 字符串清理：删除不再引用的 `settings_ime_send` / `settings_ime_received` / `settings_ime_received_empty`
- 键盘外观页：说明下方新增测试编辑框 `edit_key_appearance_test`（样式复刻设置页编辑框），
  点击即唤起 / 收起键盘；键盘弹出时滑杆仍同屏可见，可以对着键盘调
- 注：本次同时入库用户手改的 `favorite_entry_title`（收藏的自定义 → 符号的自定义）

## 验证
- `testDebugUnitTest` 39 类 / 373 例全绿；编译 + 签名 + 对齐 → `com.jinn.inputmethod.20260922001803.APK`（5.0 MB）
- ROOT 通道（`adb push` + `su -c 'pm install -r'`）覆盖安装 `Success`
- 真机截图核对：设置页首行只剩一个编辑框；键盘外观页编辑框就位，点它 → JinnIme 键盘弹出且滑杆同屏可见 ✅
- 过程记录：首次编译失败（漏删编辑框的 `IME_ACTION_SEND` 回车监听 → `Unresolved reference: editImeTest / sendImeTest`），
  已补删并复编译通过
- 说明：本次未发版（如需发布可覆盖 `20260921`）。

---

# 今日改动复查：lint 收敛 + 过期注释/文档订正（2026-09-22）

范围：`5ee6c82`（设置页两列网格）、`21ed71d`（词库/外观入口行 + 键盘外观独立页）、
`a117724`（输入法测试简化 + 新页编辑框）的累计影响。

## 一、功能与回归核查（未发现功能缺陷）
- 没有残留引用：`imeTest` / `edit_ime_test` / `settings_ime_send` / `settings_ime_received` /
  `btn_ime_send` / `text_ime_received` / `dict_entry_open` 全项目搜不到一处（发送链路清理干净）
- 入口只有一处：`DictManagerActivity` 只从新按钮「补充短语词库」进；
  `seek_key_corner` / `seek_key_gap` 现在只属于 `KeyAppearanceActivity`
- 与旧实现逐行对照一致：新页滑杆逻辑取自 `SettingsActivity.initKeyAppearanceCard`，搬迁未改动
  （定义域 / 换算 / 落盘 / 日志，连「先写初值再挂监听」的顺序也照旧）
- 真机实测：日志里能看到「新页调参 → 键盘下次弹出读到新值」（圆角 10dp → 3dp 两次变更都被键盘读到）
- 边界：钳位与换算仍只在 `KeyAppearance` 一处；新页不注册监听、不用 Handler，没有需要清理的状态

## 二、lint：30 → 22（优于 27 旧基线，净 −5）
- 修掉今天引入的 7 项：3×`DisableBaselineAlignment`（属性加错层级 → 移到父横排容器上）、
  3×`RtlSymmetry`（说明文字补 `paddingEnd`）、1×`UnusedResources`（删除已无引用的 `theme_mode_label`）
- 顺带清掉历史 1 项（NAS 卡片权重列的基线对齐）

## 三、注释与文档订正（全部为过期描述，非功能改动）
- `Prefs`：`keyCornerDp`「默认 0dp 直角」→ 1dp；`keyGapDp`「默认 0dp 无缝」→ 0.5dp
- `KeyAppearance`（3 处）与 `PinyinKeyboardView`（1 处）：「设置页」→ 键盘外观页（独立页标准）
- `AGENTS.md`：项目结构补登 5 个文件（`KeyAppearanceActivity` / `SymbolOrder(Activity)` /
  `FavoriteSymbols(Activity)`）；键盘外观入口段落改为「设置页 →『按钮圆角间隙』」；词库入口改「补充短语词库」

## 四、验证
- `testDebugUnitTest` 39 类 / 373 例全绿；`lintDebug` 0 error / 22 warning（较 27 基线 −5）
- `com.jinn.inputmethod.20260922002901.APK`（5.0 MB）ROOT 通道覆盖安装 `Success`
- 真机截图复核两列下拉 / 两组开关的排版与说明对齐，无视觉回归
- 说明：本次未发版（如需发布可覆盖 `20260921`）。

---

# 文档措辞平实化 + 发版 `20260922`（新 tag）

## 一、文档修订（只动措辞与过期事实）
- `README.md`：入口名同步（「输入方案」→「拼音输入方案」、圆角/间隙 → 设置页「按钮圆角间隙」、
  分类词库 → 「补充短语词库」）；APK 体积改为约 5.0MB；模块表补 `KeyAppearanceActivity` /
  `SymbolOrderActivity` / `FavoriteSymbolsActivity` 三行
- `README_EN.md`：同上（英文表述一并同步）
- `更新日志.md`：术语腔改直述（「零残留 / 逐行等价 / 端到端 / 无泄漏面」等）；
  「点这里唤起键盘」条目补注（该卡片同日稍后已简化为纯编辑框）；标题去掉「彻底审查轮」式口号
- 说明：历史此前已多次做措辞平实化，本次只处理新写段落与事实过期处，未大改历史文字

## 二、发版：`20260922`（跨天新建 tag）
- 提交 `0750ca2` 推送 main：GitHub + Gitee 双端。
- 产物：`jinn-release.apk` 5,217,274 字节 / `sha256=64169420…` / `versionCode=20260922`；
  ROOT 通道（`adb push` + `su -c 'pm install -r'`）覆盖安装 `Success`。
- 双端：GitHub `gh release create 20260922`（含附件）；Gitee 新建 Release（id `1158697`）
  + 附件（id `3242417`）；正文取自 `.relnotes.md`（本地草稿，不入库）。
- 校验：双端下载 URL 实测 `size=5,217,274`、`sha256` 与本地发布包三方完全一致 ✓

---

# 符号布局重建「延后」补重试 + 键盘外观页加 ScrollView（2026-09-22）

背景：上一轮多维审查发现两项低危缺陷（符号重建延后无重试、键盘外观页无滚动）；
本次先实机确证（排除误判）再修复。

## 一、修复前实机确证
- D1 延后无重试：在收藏编辑页「添加符号」对话框里打了一半拼音（候选栏有 `ni`）时按 Home，
  日志出现「延后到下次创建视图」；随后在设置页拉起键盘，收藏组键面仍是旧的 `D I Y`，
  而 prefs 已是 `[["D","I","Y","咋"]]`（新符号只在数据里，键盘上看不到）。
  同日日志还证实：框架跨会话复用同一个键盘视图（`onCreateInputView` 2 次 vs 键盘弹出 9 次），
  所以「延后到视图下次创建时自然生效」并不会自然发生。
- D2 键盘外观页无滚动：横屏下打开该页并唤起键盘，滑杆卡片被键盘完全遮住；
  `uiautomator` 实测 `scrollable=0`（无任何可滚动容器），滑杆不可达。

## 二、修复
- `JinnIme`：
  - 新增 `pendingSymbolLayoutRebuild` 标记：`rebuildInputViewForSymbolLayout` 因未上屏输入延后时置位；
  - 抽出 `recreateKeyboardView()`（换肤 / 符号重建共用，重建即清标记）；
  - `onStartInputView` 在 `applyThemeIfNeeded()` 之后补一次重建（仍看一眼 `hasPendingInput`），
    与换肤的「下次弹出再判」标准对齐；
  - 注释订正：触发场景改为「对话框里打了一半就按 Home / 锁屏」（真机实证），删掉「窗口极窄」的旧判断。
- `activity_key_appearance.xml`：外层包 `ScrollView`（与设置页等表单页同款），控件与 id 不变。

## 三、修复后验证（同场景重放）
- 延后日志变为「延后到下次弹键盘重建」；下次拉起键盘即出现「补重建（上次延后）」；
  收藏组键面 = `D I Y`（与 prefs 一致，删除即时生效）✓
- 横屏 + 键盘弹出：`scrollable=1`；在可视区内快速滑动，滑杆区（「按键圆角（0 ~ 24dp，0 为直角）」）
  滚入键盘上方可视区，可正常调节 ✓
- 门禁：`testDebugUnitTest` 39 类 / 373 例全绿；`lintDebug` 0 error / 22 warning（持平）
- 产物：`com.jinn.inputmethod.20260922101030.APK`（5.0 MB），ROOT 通道覆盖安装 `Success`
- 设备已复原：旋转设置回默认；测试用临时符号已删除（`favorite_symbols` 还原 `[["D","I","Y"]]`）；
  `theme_mode` / 圆角等用户数据未动
- 说明：本次未发版（如需发布可覆盖 `20260922`）。
---

# Release 旧 APK 清理（2026-09-22）

按用户要求「Release 只保留最新一版 APK」，删除双端 6 个历史 Release 的 APK 附件：
tag = 20260921 / 20260920 / v20260919 / v20260918 / v20260917 / v20260916。

- 清理后双端一致：APK 仅 20260922（`jinn-release.apk`，5225466 B）；
  词库包 3 个保留（`dict_ext.txt.xz` ×2、`opt_tencent.txt.xz`），它们是「分类词库」页的下载源，
  被 `OptionalDicts` 引用，不属于清理范围。
- 只删附件，Release 本体与 tag 保留（历史正文与版本记录仍可查）。
- 操作：GitHub `gh release delete-asset <tag> jinn-release.apk --yes`；
  Gitee `DELETE /repos/{owner}/{repo}/releases/{id}/attach_files/{file_id}?access_token=…`。
- 坑：Gitee 批量连续 DELETE 会被拦成 HTML 404 页（"你所访问的页面不存在"），
  单条重试即返回 204；逐个删、间隔 ~0.8s 稳定。已写入 `AGENTS.md` 发布规范。
---

# 界面文案平实化（2026-09-22）

逐条审查面向用户的中文文本（`strings.xml` 全部条目 + 各 Activity 内硬编码文案 + README），
共改写 8 处、修 3 处标点/格式问题：

- `strings.xml`：
  - 词库页：「按需下载词库，扩充输入覆盖范围」→「需要哪个下哪个，能打的词更多」；
    「下载后会自动重启输入法以生效」→「下载完会自动重启输入法」
  - Root 增强：「可选扩展：使用 Root 权限审计 JinnIme 数据目录安全」→「用 Root 权限检查数据目录是否安全（可选）」
  - 用户词频：「记住常选词,自动放前排」→「记住常选的词，下次放前排」（顺带修半角逗号）
  - 双拼说明：「双拼26键显示韵母」→「双拼时键面显示韵母」
  - 收藏编辑：「自由增删…满页自动新增页。改动即时保存，返回键盘即生效」→ 口语化改写
  - `settings_usage`：统一引号（"删除键" → 「删除键」）与全角逗号（用户手写内容，只动标点）
- `OptionalDicts`：词库描述去「含…等」模板句式；「覆盖大量专业术语与短语搭配」→「专业术语与短语搭配较多」；
  「建议按需安装」→「按需安装」
- `ClipboardFirewall`：去掉误入界面文本的 Markdown 加粗标记（原会在 UI 上显示成星号）
- README：语音说明长句断句；修复去装饰符时留下的引用块双空格（3 处）

真机验证（PACM00）：设置页、分类词库页（4 条新文案）、符号自定义页均正常显示、排版无溢出。
门禁 40 类 / 382 例全绿，lint 0 error。提交 `6a268d1`。

## 2026-09-22 编译警告 / lint 全量审查修复（Kotlin 编译警告清零）

- 审查范围：全量编译（`clean` + `--rerun-tasks --no-build-cache`）产出的 **51 条 Kotlin 警告**（去重 26 个源位置）与 **23 条 lint warning**，
  逐条回源码核实性质与影响后再决定修或不修
- 已修：
  - `PinyinKeyboardView` 20 处 + `SearchPanelView` 3 处 `lateinit` → `val`（字段都只在 `init` 内赋值一次，编译器已证明构造期必初始化）；
    `btnSpaceHint` 的 `isInitialized` 恒真守卫随之删除
  - 两处对非空面板的多余 `?.`（`clipboardPanel?.applySurfaceAlpha` / `searchPanel?.…`）去掉（lateinit 的 `?.` 本就不提供保护）
  - `TextSelectionTest.range(focus)` 删除未使用参数，12 处调用同步
  - `ConstantLocale`：`ClipboardPanelView` / `SearchPanelView` 的 `SimpleDateFormat` 静态缓存改为 `formatTime()` 即时构造
    （系统语言切换后不再沿用旧 Locale）
  - 删除无引用 drawable `key_bg_rect.xml`（半透明改造后只剩注释提及），同步订正 3 处注释
- 实测否决（保留 lint 项）：`ObsoleteSdkInt` 建议把 `mipmap-anydpi-v26` 并入 `mipmap-anydpi` —— AAPT 直接报
  `resource mipmap/ic_launcher not found`，编译失败，已回退
- 判定不改（有意设计或规则约束）：`Overdraw`（半透明背板）、`DrawAllocation`（MicButton 禁改域）、
  `GradleDependency`（锁版本守 <5MB）、`SetTextI18n` / `TypographyDashes`（strings.xml 默认禁改）、
  `ScrollViewSize` / `SmallSp` / `UselessParent` / `InflateParams` / `MonochromeLauncherIcon`（既定布局与图标）
- 门禁：`clean assembleDebug assembleRelease testDebugUnitTest lintDebug --rerun-tasks --no-build-cache` → **100/100 任务全执行、BUILD SUCCESSFUL**；
  Kotlin 警告 **51 → 0**；单测 **40 类 / 382 例全绿**；lint **23 → 20 warning / 0 error**；release 包 5,216,866 B（较前 -1,192 B）
- 真机（PACM00）：签名包 `install -r` Success；键盘（双拼键面 + 候选栏 + 符号标签）与剪贴板面板（分类栏 + 空态）截图正常；
  当日日志 `FATAL` 计数 0
- 教训：`lint` 建议不等于可以照做（`mipmap-anydpi-v26` 改名会直接破坏资源链接）；`strings.xml` 默认禁改时，
  文案类 lint（SetTextI18n / TypographyDashes）应保留而非改造

## 2026-09-23 全量编译警告逐条复核（回溯源码 + 三类判定）

- 复核基线：`clean assembleDebug assembleRelease testDebugUnitTest lintDebug --rerun-tasks --no-build-cache`
  → **100/100 任务全执行、BUILD SUCCESSFUL**；Kotlin 警告 0、javac/R8 警告 0、Gradle deprecation 0、错误 0
- lint：**20 → 19 warning / 0 error**；本次唯一修复（无风险、可清理）：
  `mipmap-anydpi-v26/ic_launcher.xml` 补 `<monochrome>` 层 —— 前景 `ic_launcher_foreground` 是单 path 的
  麦克风剪影 vector（`fillColor #FF4C8DFF`），Android 13+ 主题化图标只取 alpha 通道着色，可直接复用；
  低版本忽略该标签；真机桌面图标渲染无回归
- 复核确认 19 条「有意取舍」及依据：
  - `InflateParams`：`JinnIme.kt:667` 的 `inflate(R.layout.keyboard, null)` 对应 `:1091 setInputView(...)`，
    IME 场景无 parent 可用，布局参数由窗口决定
  - `UselessParent`：`keyboard_pinyin.xml:79` 的 `contentArea` 是剪贴板面板挂载（`:351`）与高度改写（`:2131`）容器
  - `ObsoleteSdkInt`：按建议改名实测 AAPT 报 `resource mipmap/ic_launcher not found`、编译失败（已回退）
  - `Overdraw`：根背板是半透明键盘「plate 档」载体（日志「铺底诊断」可见其 alpha）
  - `DrawAllocation`：`MicButton` 属语音禁改域自绘；`GradleDependency`：锁版本为守住 APK < 5MB（当前 4.98 MiB）
  - `SetTextI18n`×6：`ClipboardPanelView:245`、`SearchPanelView:366` 为**纯字面量**（lint 误报）；
    `PinyinKeyboardView:1505`（页码）、`SymbolOrderActivity:64`（序号）为中文单语言拼接，改用资源占位符会触碰
    `strings.xml` 默认禁改
  - `TypographyDashes`：同为 strings.xml 禁改；`SmallSp`：空格键 9sp 小字（真机显示正常、不溢出）
  - `ScrollViewSize`：候选栏 `match_parent` + `fillViewport` 语义所需；`ClickableViewAccessibility`：
    误报（`JinnIme.kt:672-678` 已在 `ACTION_UP` 补 `performClick()`）
- 门禁：单测 40 类 / **382 例全绿**；release **5,216,890 B**（4.98 MiB，+24 B）；签名包装机 Success、桌面图标无回归

## 2026-09-23（续）编译输出深度审查：清除残留的非必要信息

- 手段：`--warning-mode all` 全量编译（Gradle/AGP 层 0 警告）；R8 报告无 `missing_rules.txt`（无 missing-class 警告）；
  manifest 合并报告 0 WARNING；临时开启 lint `checkAllWarnings` 暴露 informational 项（81 条）后回退
- 发现并清理（均属冗余/过时信息，零行为影响）：
  - 未使用 id ×2：`activity_settings.xml` 的 `edit_ime_test`、`activity_key_appearance.xml` 的 `edit_key_appearance_test`
    ——编辑框只作「点击唤起键盘」用，代码 0 引用；移除 id 后 informational 模式 `UnusedIds` 2 → 0，
    真机实测点击仍正常唤起键盘（键盘坐标/候选栏日志齐全）
  - 历史模块名：`settings.gradle.kts` 的 `rootProject.name`（`JinnVoiceIME` → `com.jinn.inputmethod`）、`.idea/.name` 同步
  - 过时路径注释：`app/build.gradle.kts` 里 `<密钥目录>` → 现行 `JINN_KEYSTORE_ROOT`
    （`GLOBAL/credentials/JinnKeyStores`）标准
- 判定保留（informational 79 条中）：38×`SyntheticAccessor`（Kotlin 匿名对象访问私有成员，Java 时代性能建议、
  ART 无实际开销）、10×`LogConditional`（`Log.i` 在语音链路/引擎，排障依赖 logcat）、7×`SelectableText`
  （键盘与设置说明文字无需可选中）、2×`DuplicateStrings`（strings.xml 禁改）、2×`NewerVersionAvailable`
  （锁版本守 <5MB）、1×`NegativeMargin`（-2dp 视觉微调）
- ⚠ 结论：`lint { checkAllWarnings = true }` 只作审查手段，**不写入仓库**（默认基线维持 19 warning）
- 门禁：全量 100/100、Kotlin 警告 0、单测 40 类 / 382 例全绿、lint 19 warning / 0 error；
  当日日志 `FATAL` 计数 0（日志目录两个 crash 快照为 09-21 旧文件）；签名包装机 Success

## 2026-09-23（其三）清除「编译器与 lint 都不报」的沉默冗余

- 手段：脚本扫描全部 `.kt` 与 `*.gradle.kts` 的「import 后正文未引用」；复核 R8 报告（资源侧项目自有条目被移除 0 条、
  无 `missing_rules.txt`）；逐条核对 `.gitignore` 规则与实际路径；审读 `proguard-rules.pro`
- 已清理（均确认冗余/过时，零行为影响）：
  - **5 处未使用 import**：`SettingsActivity.kt` 的 `android.view.ViewGroup`、`android.widget.LinearLayout`；
    `HotDictAssetTest.kt` 的 `assertEquals`；`RecentChangesParityTest.kt` 的 `assertFalse`；
    `app/build.gradle.kts` 的 `java.io.FileInputStream`（早已改用 `use {}` + `inputStream()`）
  - **`proguard-rules.pro`**：删除与 `-keep class com.jinn.inputmethod.JinnIme`（保留类与全部成员）重复的
    `-keep class ...JinnIme { <init>(); }`，最终保留集合不变（R8 仍无 `missing_rules.txt`）
  - **`.gitignore`**：删除 6 行已失效规则 —— 根 `/CapsWriter-Offline-master/`、`docs/*.zip`、`HANDOFF.md`、
    `ONBOARDING.md`（前两者对应路径不存在，后两者已迁入被整目录忽略的 `docs/`）；
    `tools/dict_builder/source/`（历史保护）与 `artifacts/`（真机 QA 规则）保留
  - **`gradle.properties`**：`org.gradle.tooling.parallel` 的英文注释改为中文，并注明「Gradle 9.4+ 才识别，
    本仓库锁 8.9、当前不生效」（保留该行作升级预留，不删）
- 判定不改（附核实依据）：informational 项 `SyntheticAccessor` 38 条 / `LogConditional` 10 条 / `SelectableText` 7 条
  等（理由见上一条记录）；依赖 `core-ktx`（`Prefs`/`ClipboardPrefs` 各 1 处）、`activity-ktx`
  （`ComponentActivity` + `registerForActivityResult`）、`okhttp`（AsrClient/DictManagerActivity/Prefs）、
  `xz`（PinyinEngine + 9 个测试）均有实际引用点；仓库内无 lint baseline 文件（无需豁免清单可清理）
- 门禁：全量 100/100 任务、Kotlin 警告 0 / 错误 0、单测 40 类 / **382 例全绿**、lint 19 warning / 0 error、
  R8 无 `missing_rules.txt`；真机装机 Success、编辑框唤起键盘正常、当日 `FATAL`=0

## 发版 `20260923`（跨天新建 tag）

- 产物：`jinn-release.apk` 5,225,466 字节（4.98 MiB）/ `sha256=39e08ff2…` / `versionCode=20260923`（`versionName=jinn`）
- 门禁：`clean assembleDebug assembleRelease testDebugUnitTest lintDebug --rerun-tasks --no-build-cache`
  → **100/100 任务全执行**；Kotlin 编译警告 0 / 错误 0；单测 40 类 / 382 例全绿；lint 0 error / 19 warning
- 真机（PACM00）：ROOT 通道覆盖安装成功（`versionCode=20260923`、`lastUpdateTime=2026-09-23 00:32`）；
  设置页编辑框唤起键盘、符号层与剪贴板面板显示正常；当日日志 `FATAL`=0
- 双端发布：GitHub Release `20260923`（附件 `jinn-release.apk`）；Gitee Release id `1160874`（附件 id `3249560`）
- 附件清理（只留最新一版）：GitHub 删除 `20260922` 的 APK 附件；Gitee 删除 `20260922` 的
  `jinn-release.apk`（attach id `3247655`，HTTP 204）
- 校验：双端下载 URL 实测 `size=5,225,466`、`sha256=39e08ff2…`，与本地发布包**三方完全一致** ✓
- 提交：`e863516`（告警清理与订正，23 文件）；双端 main 已推送（`0c7ca31..e863516`）
- Release 正文：新增「本版变更 / 本版验证」两段，上一版（20260922）记录逐字保留

### 发布修正（同日覆盖重发）

- **事故**：首次上传误用了根目录遗留的历史 `jinn-release.apk`（5,225,466 B / `sha256=39e08ff2…`），
  并非本次构建产物（5,221,370 B / `sha256=71a5e12b…`）
- **根因**：`build_apk.py` 只产出时间戳包（`com.jinn.inputmethod.<ts>.APK`），不生成固定发布名
  `jinn-release.apk`，发布依赖手工复制 → 本次漏做，误用历史文件
- **修正**：`build_apk.py` 改为「时间戳包 + `jinn-release.apk` 同源双写」；重新构建
  （`com.jinn.inputmethod.20260923095511.APK`，ROOT 通道装机 Success）并覆盖重发双端
  （GitHub `--clobber`；Gitee 删旧附件 `3249560` → 新附件 `3252256`）
- **复核**：双端下载实测 `sha256=71a5e12b…`、`size=5,221,370 B`，与本地发布包**三方一致** ✓
  （GitHub 下载走 CDN，首次拉取可能命中缓存副本，需加 query 参数复核）

## 2026-09-23（其四）键盘皮肤第一期（默认 / 磨砂 / 彩虹 / 极光 / 复古机械）

- 需求：键盘支持用户可选的美化配色皮肤（磨砂 / 磨砂玻璃 / 彩虹 / 极光 / 复古仿机械…），APK 必须 < 5MB。
  第一期落地 4 套 + 默认：**磨砂、彩虹、极光、复古机械**；磨砂玻璃（需 API 31+ 真模糊分支）与霓虹留第二期
- 实现：
  - 新增 `KeyboardSkin.kt`：皮肤参数 + 清单 + `byId` / `visualFor` / `issues` 纯函数（自实现 HSV→RGB，便于 JVM 单测）
  - `PinyinKey` 增加皮肤绘制路径：渐变（shader 带缓存）/ 描边 / 底边「键帽厚度」/ 字色与提示色；默认皮肤仍走历史纯色路径
  - `PinyinKeyboardView` 固定顺序：`syncKeyboardSkin`（换肤复位各面缓存）→ `applyKeyTransparency`（按皮肤色重建面）
    → `applySkinToKeys`（逐键下发，彩虹按字母序取色相）→ `applyKeyAppearance`；功能键 / 大写键 / 删除键 /
    候选栏 / 背板统一走 `skinToken()`，皮肤色并入 `alphaFaces` 识别色集
  - `Prefs.keyboardSkinId`（未知值归一默认）；键盘外观页新增横向皮肤选择器，预览块由 `PinyinKey` 自绘（零图片资源）
- 兼容性：默认皮肤是**全空覆盖** ⇒ 与半透明键盘的历史观感逐像素一致（真机切回默认截图核对通过）
- 体积：release **5,223,046 B**（较上版 +1,676 B），距 5MiB 上限余量约 19.8 KB；零位图、零新增依赖
- 门禁：`clean … --rerun-tasks --no-build-cache` 100/100 任务；Kotlin 警告 **0**；单测 **41 类 / 395 例全绿**
  （新增 `KeyboardSkinTest` 13 例）；lint **保持 0 error / 19 warning**（未引入新警告）
- 真机（PACM00）：ROOT 通道装机 Success；外观页选择器渲染正常 —— 彩虹（26 键按字母序成谱）、
  复古机械（米白键帽 + 深棕描边 + 2dp 厚度）、默认（回归历史观感）三态截图逐一核对；
  `keyboard_skin` 落盘后弹键盘即时生效
- 交互坑（已登记到代码注释与 `AGENTS.md`）：`PinyinKey.onTouchEvent` 恒返回 true，
  预览块会吃掉点击，必须在预览上显式转回选项容器

### 皮肤实测缺陷修复（同日）

- 用户真机实测反馈：**磨砂 / 极光**两套皮肤下，候选栏功能按钮、大写键、删除键、空格、回车的**文字与图标不可读**
  —— 背景已被皮肤改成深色，而文字仍走亮白主题的 `text_primary #1A1F2B` / `text_secondary #5A6474`
  （暗黑主题下 `text_primary` 是浅色，因此该主题下不暴露）
- 修复：
  - `KeyboardSkin` 新增 `functionGlyph`（功能键与候选区主文字 / 图标色）与 `functionHint`
    （次要文字：空格键小字、页码、候选提示）；4 套非默认皮肤补齐色值；`issues()` 把 `functionGlyph`
    纳入「填充类必须不透明」校验，`functionHint` 允许带 alpha；`isDefault` 纳入这两个字段
  - `PinyinKeyboardView` 新增 `applySkinToTexts()`：统一套用空格键主 / 小字、回车 / 大写 / 删除的
    `imageTintList`、方向按钮；并在 8 处动态构建点改用皮肤色（候选词、预测词、符号分组标签、分组页码、
    候选提示、候选栏 6 按钮 label/hint、方向键、方向按钮非激活态）
  - 默认皮肤两项覆盖为 null ⇒ 回落 `text_primary` / `text_secondary`，历史观感不变
- 单测：新增「非默认皮肤必须给出功能键文字色」（glyph 非空且不透明、hint 非空、默认两项为 null），
  **41 类 / 396 例全绿**
- 门禁：全量 100/100 任务、Kotlin 警告 0、lint 维持 **0 error / 19 warning**；
  release 5,223,382 B（较上版 +332 B，仍远低于 5 MiB）
- 真机复核（PACM00）：磨砂、极光皮肤下候选栏按钮 / 大写 / 删除 / 空格 / 回车文字与图标**均清晰可读** ✓；
  默认皮肤回归观感不变；验证期间误碰透明度滑杆（100% → 0%）已通过滑杆 UI 精确还原为 100%

### 皮肤覆盖面补齐：剪贴板历史面板 + 搜索面板 + 底部次级功能键（同日）

- 用户实测指出：换皮肤后**剪贴板历史面板没跟着变**（面板底 / 条目卡 / 分类栏仍是主题亮色，与深色键盘割裂）；
  底部次级功能键（符号 / 数字 / 逗号 / 句号 / 中英）在深色皮肤下仍是白底、对比刺眼
- 修复：
  - `ClipboardPanelView` / `SearchPanelView` 新增 `applySkin()`：面板底与列表底取 `skin.plate`、条目卡取
    `skin.functionFill`、主 / 次文字取 `skin.functionGlyph` / `skin.functionHint`、序号与分类选中态取 `skin.accent`；
    条目卡与条目文字改成在 adapter 的 `getView` 里逐次读皮肤色（否则换皮肤后已渲染的行不变色）；
    搜索框的文字 / 提示 / 底色一并跟随
  - 分类栏按钮的「面」由 `key_bg_active` / `key_bg` 静态 drawable 改为运行时构建（选中 = accent 实心，
    其余 = 皮肤面色 × 透明度档）；`applySurfaceAlpha()` 顺带修复「拖过透明度滑杆后选中分类标记丢失」的老问题
  - `rebuildFunctionKeyBackgrounds()` 里次级功能键的填充色改用 `skin.functionFill`（默认皮肤仍回落
    `btn_secondary_bg`，历史观感不变）；`applySkinToTexts()` 同步这 5 个键的文字色
  - 下发时机固定在 `syncKeyboardSkin()`（早于 `applyKeyTransparency()`），面板的纯色面才会被透明度套档识别
  - `buildKeyFaceBackground()` 加 `fillColor` 参数（面板按皮肤色重建按钮面）；新增文件级 `skinColor()` 供面板复用
  - 删除失去引用的 `drawable/key_bg_active.xml`（分类选中态已改为运行时构建），lint 回到基线 19
- 门禁：全量 100/100 任务、Kotlin 警告 0、lint **0 error / 19 warning**、单测 **41 类 / 396 例全绿**、
  release 5,223,570 B
- 真机复核（PACM00）：极光、磨砂下键盘（含次级功能键深色面）与剪贴板面板（深底 / 深卡 / accent 选中 /
  红字清空）观感统一；默认皮肤回归浅底白卡深字，与历史一致

### 键盘外观页改为极简紧凑版式 + 字符串资源审计（同日）

- 用户要求：只留大标题「键盘外观」（不带描述）、测试输入框、三行滑杆（标签「圆角 / 间隙 / 透明」与拖动条同行）、
  「皮肤」标题 + 皮肤选项一行五个；并审查删除过时 / 废弃 / 0 调用的字符串
- 版式改动：删副标题与全部说明文案（含「只改键盘配色与质感…」「松手即时生效 不用重启」）；
  三行由「标题独占一行 + 滑杆行 + 数值」改为「两字标签 + 拖动条 + 数值」同一行；
  皮肤行去掉横向滚动改为按权重等分一行（预览块 56×44 → 42×34dp、名称 11→10sp、内边距收紧）；
  `KeyAppearanceActivity` 皮肤标题文案改「皮肤」
- 字符串审计：150 条 `string` + 6 个 `string-array` **全部有引用**（含 `R.array.*`，审计脚本初版漏查该模式，
  复核后确认无其它 0 调用项）；本次顺带删除因改版失去引用的 `key_appearance_desc`、`key_transparency_desc`、
  `key_appearance_apply_hint` 三条字符串，以及两套色板中的 `key_appearance_fg_dim`
- 测试：`ThemeColorParityTest` 中原「透明度标题文案与定义域一致」的前提（标题写着 `0% ~ 100%`）随版式作废，
  改为守住新的版式要求「三个滑杆标题只两字、不带范围描述」；定义域守卫仍在 `KeyTransparencyTest` / `KeyAppearanceTest`
- 门禁：全量 100/100、Kotlin 警告 0、lint **0 error / 19 warning**（维持基线）、单测 41 类 / **396 例全绿**、
  release 5,222,790 B（较上版 -780 B）
- 真机（PACM00）：外观页截图确认版式生效 —— 无描述文字、三行「两字标签 + 拖动条 + 数值」、皮肤一行五等分

### 皮肤标签统一两字

- 「复古机械」→「复古」，五套皮肤标签（默认 / 磨砂 / 彩虹 / 极光 / 复古）全部两字，等分一行不再挤压
- 新增守卫 `KeyboardSkinTest.皮肤标签一律两字`：以后新增皮肤若用三字以上标签会直接失败（外观页每行五等分，
  长标签会挤压或换行）
- 门禁：全量 100/100、Kotlin 警告 0、lint 19、单测 41 类 / **397 例全绿**

### 新增五套皮肤（共十套，全部两字标签）

- 用户要求标签统一两字后，追加五套纯参数皮肤（零图片资源，体积 +524 B）：
  - **暗黑**：近黑键面 + 冷白字（夜间低亮度，不用纯黑以免键面与背板糊成一片）
  - **霓虹**：黑底 + 洋红发光描边、功能键青色描边 + 洋红底边厚度
  - **樱花**：浅粉渐变 + 深枣字（浅色系）
  - **海洋**：深青蓝渐变 + 浅水蓝字（低饱和冷色）
  - **薄荷**：浅薄荷渐变 + 墨绿字（浅色系）
- 外观页皮肤选择器改为**多行**：每行五个（`SKINS_PER_ROW`），十个皮肤排两行，末行不足自动补空占位；
  容器由横向改为竖向，不再横向滚动
- 守卫：`KeyboardSkinTest` 既有的定义域校验（填充不透明、渐变不退化、描边与宽度成对、功能键字色齐备）
  在接入时即拦下一处配置错误（霓虹的底边厚度色带了 alpha）→ 修正为不透明
- 门禁：全量 100/100、Kotlin 警告 0、lint **0 error / 19 warning**、单测 41 类 / 397 例全绿、
  release 5,223,314 B
- 真机（PACM00）：外观页十套两行五列正常；霓虹（黑底洋红发光 + 青描边）与樱花（浅粉 + 深枣字，
  双拼提示 / 卷舌提示分色）切换后的键盘观感与可读性均通过

### 再增五套皮肤（共十五套，外观页排三行五个）

- 新增：
  - **石墨**：中性深灰 + 银灰蓝强调（商务风，低饱和、不挑背景）
  - **日落**：深紫 → 暖橙渐变 + 暖白字（黄昏色调）
  - **岩浆**：深红渐变 + 暖黄字 + 橙色底边厚度；卷舌提示用亮黄（红底上红色提示不可辨）
  - **墨玉**：墨绿渐变 + 玉白字 + 玉色微光描边（低亮度绿，久看不刺眼）
  - **皮革**：棕调键帽 + 暖色缝线描边 + 2dp 底边厚度 + 米白字（仿皮面按键）
- 清单顺序按「深色 / 中性系 → 深色彩色系 → 浅色 / 暖色系」重排为三行五个
  （默认 / 磨砂 / 暗黑 / 石墨 / 海洋 · 霓虹 / 岩浆 / 日落 / 墨玉 / 极光 · 彩虹 / 樱花 / 薄荷 / 皮革 / 复古）
- 门禁：全量 100/100、Kotlin 警告 0、lint **0 error / 19 warning**、单测 41 类 / 397 例全绿、
  release 5,223,794 B（较上版 +480 B，十五套皮肤仍远低于 5MB 上限）
- 真机（PACM00）：外观页十五套三行五列正常；日落（紫→橙渐变 + 暖橙描边）与皮革（棕调 + 缝线 + 底边厚度）
  切换后的键盘观感与可读性通过

### 皮肤增至二十四套，选择器改一行八个、名称入按钮（同日）

- 用户要求：按钮再缩小、一行放 8 个、行间距尽可能小、皮肤名从按钮下方移进按钮内部（替换占位「A」）、
  做成三行共 24 个
- 新增九套皮肤（纯色值，零资源）：
  - **钛金**（冷银灰 + 高光描边 + 银色底边厚度）、**夜蓝**（深靛蓝 + 淡蓝字）、
    **夜空**（深蓝紫渐变 + 星白字 + 紫色微光）、**紫晶**（紫渐变 + 浅紫字）、
    **雪原**（极冷白 + 深蓝字）、**青瓷**（浅青白 + 墨青字）、**蜜桃**（浅粉橙 + 深棕字）、
    **抹茶**（浅抹茶绿 + 深茶字）、**珊瑚**（珊瑚粉橙 + 深红棕字）
- 外观页选择器：
  - `SKINS_PER_ROW` 5 → **8**（屏宽 ÷ 8 等分，不再横向滚动）；行距 6dp → **2dp**（`SKIN_ROW_GAP_DP`）
  - 预览键 42×34dp → **等分宽 × 30dp**（`SKIN_CELL_HEIGHT_DP`）；圆角 8 → 6dp
  - **去掉「占位 A + 下方名称行」**：皮肤名（两字）直接写进预览键，由 `PinyinKey.centeredStyle` +
    `fitTextSize` 按宽度自适应字号；点击处理随之从外层容器移到预览键自身
- 门禁：全量 100/100、Kotlin 警告 0、lint **0 error / 19 warning**、单测 41 类 / 397 例全绿、
  release 5,224,394 B（二十四套皮肤共 +600 B）
- 真机（PACM00）：三行八列 24 个按钮排布正常、名称在按钮内清晰可读；夜空与钛金切换后的键盘观感通过

### 皮肤选择器按键面亮度排序（从亮到暗）

- 用户要求：皮肤按钮排序改为从亮色排到暗色，不允许按分组直觉乱排
- 实现：`KeyboardSkins.orderedForDisplay(fallbackKeyFill)` 按 `faceBrightness()`（BT.601 感知亮度，
  取键面首 / 次色的均值）降序排列；默认皮肤不覆盖键面色，由调用方传当前主题的 `R.color.kb_key` 兜底；
  彩虹皮肤按起始色相的实际生成色计算（否则会被当作「未覆盖」而拿主题色、排到最亮处）
- 排序是纯函数 + 稳定排序（同亮度保持定义顺序）：**新增皮肤自动落位，不需要手工找位置**
- 守卫：`KeyboardSkinTest.展示顺序按键面亮度从亮到暗`（逐个比较相邻两项、断言非递增）
- 实际顺序（三行八列）：
  - 行 1：默认 · 雪原 · 樱花 · 蜜桃 · 青瓷 · 抹茶 · 薄荷 · 珊瑚
  - 行 2：复古 · 彩虹 · 钛金 · 日落 · 皮革 · 磨砂 · 石墨 · 紫晶
  - 行 3：海洋 · 岩浆 · 夜蓝 · 墨玉 · 极光 · 夜空 · 暗黑 · 霓虹
- 门禁：全量 100/100、Kotlin 警告 0、lint **0 error / 19 warning**、单测 41 类 / **398 例全绿**、
  release 5,224,726 B

### 皮肤代码审查与修复（D1–D4 复核 + 6 项新缺陷，2026-09-23）

- 范围：今日皮肤系列提交（`313d442..HEAD`）+ 工作区未提交的 D1–D4 修复
- **D1–D4 修复复核通过**：`ClipboardPanelView.surfaceAlpha` 上移至 `init` 之前、`applyTabFaces()` 改遍历全部 `tabButtons`；
  候选栏「拼音回显 / ✕」走 `skin.functionHint`；三处红色语义走 `skin.hintRed`；大写锁定 shift 图标用 `accentOnColor`
- **新发现并修复 6 项**：
  1. 彩虹皮肤按压态闪成主题深块 —— `KeyboardSkin.visualFor` 判据由 `keyFill == null` 改为 `isDefault`（非默认皮肤由首色压暗）
  2. 皮肤 + 透明度时候选栏被压成背板档（100% 全透）—— `plateRgb` 移除 `skin.candidateBar`，档位交由
     `updateCandidateBarBackground` 专管
  3. 方向面板 ◉ 激活态字色未随 accent 亮度 —— `setSelectionActive` 激活用 `accentOnColor`，非激活恢复 `functionGlyph`
  4. 中英切换键高亮写死 `aurora_purple` —— 改 `skinToken(skin.accent, R.color.aurora_purple)`
  5. 皮肤绘制路径每帧分配 —— `PinyinKey` 复用 `faceRect` 字段；shader 判据由拼接字符串改 5 个数值字段比较
  6. `plateFaceAlpha` 声明在 `init` 之后（赋值被属性初始化器重置）—— 声明上移至 `keyFaceAlpha` 同区
- 新增守卫：`KeyboardSkinTest.按压色分支不因缺keyFill而回落主题色`（默认令牌色 / 彩虹首色压暗 / 显式 `keyPressed` 三路）
- 门禁：全量 100/100、Kotlin 警告 0 / 错误 0、单测 41 类 / **400 例全绿**、lint **19 warning / 0 error**（持平）、
  release 5,224,730 B（4.98 MiB）
- 真机（PACM00，ROOT 通道）：极光 + 透明度 100% —— 无内容候选栏走背板档（`a=0`）、**有候选时走内容面档且
  拼音串与候选词清晰**（修复前会被压成全透）；方向面板正常打开（UI 节点 20 → 61）；设备已复原 `amethyst` / 0
- 判定不改：方向面板展开期间换皮肤/拖透明度不即时刷新（既有设计限制，`refreshAppearance` 注释已声明）

### 皮肤刷新链二次复核与补齐（同日其三）

- 复核结论：上一轮 6 项修复（F1–F6）逐条证伪后**全部维持真实缺陷**，无一条为误报：
  - F1 彩虹按压态 —— `visualFor` 的 `pressed` 分支用 `keyFill == null` 判据，彩虹正属此类（源码事实）
  - F2 候选栏档位 —— `plateRgb` 含 `skin.candidateBar`，被 `alphaFaces` 统一压档；真机截图实证修复
  - F3 ◉ 字色 / F4 中英高亮 —— 语义色绕过皮肤，浅 accent / 深键面下对比不足
  - F5 绘制分配 —— 原文即 `new RectF(...)` + 拼接字符串（与「零分配」注释相悖）
  - F6 字段声明顺序 —— `plateFaceAlpha` 声明晚于 `init`，赋值被属性初始化器重置（靠 `configure()` 掩盖）
- 新发现并修复 2 项（同属「换肤 / 改透明度后未重刷」，低危观感）：
  - **中英键高亮 span 不更新**：`applySkinToTexts` 只改 base color，`buildLangLabel()` 的
    `ForegroundColorSpan` 仍是旧皮肤强调色 → 在该方法末尾重建 `btnLang.text`
  - **方向面板 9 键不更新**：`ensureDirectionPanel` 复用面板对象，键面 / 字色停留在构建期皮肤色
    （含 ◉ 激活态）→ 新增 `applySkinToDirectionPanel()`，`showDirectionPanel()` 每次打开时重刷
- 排除的非缺陷（误报候选）：`gradientShader` 判据不含位置（left/top）—— 26 键在会话内位置固定，无触发路径，属既有行为
- 门禁：全量 100/100、Kotlin 警告 0 / 错误 0、单测 41 类 / **400 例全绿**、lint 19 warning（持平）、
  release 5,224,890 B（4.98 MiB）
- 真机（PACM00）：方向面板回归通过 —— 9 键键面与字色随紫晶皮肤、中心键 ● 可见、
  候选栏「方向」按钮显示红色「返回」；装机 Success

### 自身改动自查（同日其四）

复核对象：本次全部 8 处改动（F1–F6 + 两处换肤刷新补齐）及其调用链。结论：**F1–F6 与中英键 span 重建均正确，无一条误判**；
发现 1 处刷新时机不当并已修正：

- `applySkinToDirectionPanel` 原先插在 `panel.tag` 之后（`selectionActive = false` 之前）：进入面板时会先按**进入前**的拖选状态
  刷中心键，随后 IME 的 `onSelectionModeChanged(false) → clearSelectionState() → setSelectionActive(false)` 才把它改回 ——
  结果正确但多一次反向重绘；已把调用移到状态归零之后（紧邻 `refreshDirectionButton()`），刷新与最终状态一致。
- 复核后判定**非缺陷**的两项疑似问题：
  - `gradientShader` 判据不含键位置：`faceRect` 基于键**本地**坐标（每键原点一致），尺寸判据即完备，不存在复用错位；
  - 进入方向面板直赋 `selectionActive = false`（绕过 `setSelectionActive`）：IME 侧 `clearSelectionState()` 已回传
    `setSelectionActive(false)`，中心键字符与视觉的状态跑通存在。
- 另核实：`refreshKeyLabels` 不设 base color（与 span 重建无冲突）；`refreshAppearance` 换肤链不含 `refreshKeyLabels`
  （故该重建必要且不会被覆盖）；24 套皮肤的 `candidateBar` 与 `plate` / `functionFill` / 默认令牌**无撞色**
  （候选栏档位修复在全皮肤下均有效）。
- 验证：单测 41 类 / 400 例全绿、Kotlin 警告 0、装机 Success；真机方向面板回归通过（中心键 ● 与状态字段一致）

### 改动自查（其二）：测试补齐与风险清单（同日其五）

按「逻辑错误 / 边界 / 异常 / 兼容性 / 数据流 / 测试 / 回滚」七维复核今日 8 处改动，结论与动作：

- **确认遗漏测试并补齐 2 条回归守卫**（`KeyboardSkinTest`，单测 402 例全绿）：
  - `候选栏色不得与背板或功能键面撞色` —— 遍历全皮肤断言 `candidateBar ∉ {plate, functionFill}`，
    防未来新增皮肤撞色让候选栏档位修复失效（100% 时候选词叠在宿主内容上）
  - `未声明按压色时默认皮肤走令牌_皮肤按首色推导` —— 断言默认皮肤用令牌按压色、彩虹皮肤走 `darken(首色, 0.88)`
- 边界与异常核对（结论：无需改动）：
  - `darken` 内部 `factor.coerceIn(0,1)` 且逐通道钳位，透明度由 `withAlpha` 统一套 → 无越界路径
  - `plateAlpha = 1 − p/100`、`surfaceAlpha = 1 − p/100·(1 − MIN)`：0% 时两档同为 1f（缓存差异无影响），
    100% 时 0 与 MIN 有区分 → 档位切换有意义
  - 按压色推导的变更只影响「`keyFill == null` 且非默认」的皮肤 = **仅 rainbow 一套**，其余皮肤本就走 darken 或显式声明
  - `applySkinToTexts` 调用点唯一（`applySkinToKeys` 内），不在 `init` 路径 → 重建 `btnLang.text` 无空指针窗口
  - `applySkinToDirectionPanel` 全程 `as?` + `?: continue`，面板构建失败时上层已早退
- 兼容性：默认皮肤是全空覆盖（含 `accent`）→ 中英键 span 回退 `R.color.aurora_purple`，历史观感不变
- 回滚方案：三个代码提交均未推送 → `git reset --hard 066d466~1` 可整体回到今日改动前；
  单独回退某一项需手工（`066d466` 内含 6 项同文件修复）；回滚后必须重编译并装机验证

### 主题与皮肤解耦 + 涟漪随皮肤（同日其六）

按「重叠 / 冲突 / 不搭配 / 冗余」审查结论落地四项改动（用户指定）：

- 文案：`theme_card_title`「键盘主题配色」→「界面明暗切换」；`key_appearance_entry`「键盘主题皮肤」→「键盘配色皮肤」
  （原文案与作用域不符：主题同时决定全 App 界面，皮肤只管键盘；两条都以「键盘主题」开头造成重叠）
- 涟漪随皮肤明暗：新增 `KeyboardSkins.rippleOn(base)`（亮底 10% 黑 / 暗底 30% 白，与两套主题令牌同值）
  - 键盘侧 `buildKeyBackground` / `buildButtonBackground` 与剪贴板面板 `buildKeyFaceBackground` 全部改走推导；
    默认皮肤仍走令牌（随主题），语音面板（禁改域）不传参保持原样
  - 消除审查确认的唯一可见不搭配：亮白主题 + 深色皮肤时深键面压 10% 黑（按压反馈几乎不可见）、
    暗黑主题 + 浅色皮肤时浅键面泛白
- 皮肤排序：`orderedForDisplay` 改为**默认固定首位且不参与排序**，其余仍按键面亮度从亮到暗
  （默认皮肤不覆盖键面色，亮度随主题漂移，参与排序会导致名次变化）
- 守卫单测：`展示顺序默认固定首位_其余按键面亮度从亮到暗`（跑两套主题令牌证明默认之外名次不受主题影响）、
  `涟漪色按底面明暗取相反侧`；单测 **403 例全绿**、lint 19 持平
- 真机（PACM00）：设置页显示「界面明暗切换」「键盘配色皮肤」；外观页「默认」在首位、其余从亮到暗；装机 Success

### 设置页移除「Root 增强模式」

- 背景：该开关只做数据目录安全审计（Root 权限体检），与用户日常使用与体验无关 → 移除
- 移除内容：设置页剪贴板卡片的开关与状态文本（布局 + 逻辑）、`ClipboardFirewall.kt`（整文件）、
  `ClipboardPrefs.rootEnhanceEnabled` 与 `root_enhance` 键、5 条相关文案、`Diagnostics` 里对已删代码的注释引用
- 保留：`MANAGE_EXTERNAL_STORAGE`（诊断日志直写共享目录仍在用）；剪贴板加密、历史上限等其余功能不变；
  旧版本写入的 `root_enhance` 偏好键留在本机，不再被读取
- 门禁：Kotlin 警告 0 / 错误 0、单测 41 类 / **403 例全绿**、lint **19**（持平）
- 真机（PACM00）：设置页剪贴板卡片现只余「历史数量上限 500」；装机 Success

### 存储权限清理：日志不落共享存储，诊断包改走系统文件选择器

- 背景：`MANAGE_EXTERNAL_STORAGE`（所有文件访问）属受管控的高危权限，与输入法职责不匹配；
  原用途只是「把日志写到共享存储方便取出」，存在零权限的等价方案
- 移除：Manifest 的 `MANAGE_EXTERNAL_STORAGE` 声明与 `xmlns:tools`；`Diagnostics` 的共享存储直写分支
  与 root `appops set … allow` 授权（`makeWritable` / `grantPublicDirViaRoot` 一并删除）；
  按钮文案「导出诊断包到内部存储」→「导出诊断包」
- 改造：日志统一写应用专属目录 `Android/data/<pkg>/files/logs/`（零权限）；导出改为
  「应用在缓存目录打包 → 系统文件选择器（SAF，`ACTION_CREATE_DOCUMENT`）由用户自选位置 → 写完删临时包」；
  提示改为显示生成的文件名（部分 provider 的 `lastPathSegment` 是文档 ID，对用户无意义）
- 结果：权限清单由 4 条降至 **3 条**（INTERNET / ACCESS_NETWORK_STATE / RECORD_AUDIO），不申请任何存储权限
- 门禁：Kotlin 警告 0 / 错误 0、单测 41 类 / **403 例全绿**、lint **19**（持平）
- 真机（PACM00）：点「导出诊断包」→ 系统「保存到」页 → 存入下载目录
  `jinn-diagnostics-20260923-143247.zip`（112,630 B）；应用内提示「已导出：<完整文件名>」

### 修复导出中断与排障脚本路径

- **导出在进程回收后静默中断（已实测复现）**：系统选择器打开期间应用进程被回收 → 回到选择器保存时内存里的
  临时包引用已丢、回调提前返回 → 目标位置留下 **0 字节空文件**、应用内无提示、缓存包残留。
  修复：新增 `Diagnostics.latestBundle()`（按最近生成时间在缓存目录兜底定位临时包）；SAF 回调改为
  「重建后仍继续写入；取消则删包并恢复提示；包不存在则明确提示『导出被中断，请重试』」
- **排障脚本路径失效（存量问题）**：`tools/bug_capture.ps1` / `bug_clear.ps1` 写死旧日志目录
  `/storage/emulated/0/JinnIme/logs`（实测为空，日志早已写在应用专属目录）→ 抓包得到空日志、清理清不到真实文件。
  修复：两脚本改为**多候选探测**（应用专属目录优先，旧共享存储兜底）
- 验证：
  - 时间线（杀进程后保存）：`开始打包 → 导出完成`，目标文件 **113,789 B**（修复前同场景 0 B）；缓存 `total 0`（修复前残留 113,301 B）
  - 取消分支：退出选择器 → 日志「用户取消导出（清理临时包 …）」+ 缓存 `total 0`
  - 脚本：路径探测实测命中新目录；两脚本 UTF-8 解析语法通过（`ParseFile` 按 ANSI 读中文注释会误报，已用 `ParseInput` 复核）
- 门禁：Kotlin 警告 0 / 错误 0、单测 41 类 / **403 例全绿**、lint **19**（持平）；装机 Success

### 发版记录：20260923 覆盖轮

- 推送：双端 `main` 同步至 `59a5a73`（`e2dc888..59a5a73`），三方一致
- 发布包：`jinn-release.apk` 5,229,506 B（4.99 MiB）/
  `sha256=2a2b239efb7412f5996fbe017ed345e5338033805ca618c297b5cfa6e71b520b`；
  签名 V1=false / V2=V3=true、4 字节对齐
- Release 附件覆盖：GitHub 同名附件 `--clobber`；Gitee 删旧附件 `3253629` → 新附件 `3254175`
- 校验：本地 / GitHub 下载 / Gitee 下载 **三方 sha256 一致**
- Release 正文：改为「本版（覆盖发布）→ 覆盖轮变更 → 覆盖轮验证 → 首轮记录（逐字保留）」

## 2026-09-23：用户配置导出与导入（跨设备迁移）

### 新增：配置备份包

- 背景：系统云备份与设备迁移已在 `backup_rules.xml` / `data_extraction_rules.xml` 中整体排除
  （Keystore 密钥不可备份，恢复也无密钥可解），此前没有任何迁移通道
- 包格式（`ConfigBackup`）：zip 容器 = `manifest.json`（格式标识 / 格式版本 / 来源版本 / 时间 / 设备 +
  各节 sha256）+ `prefs.json`（两个 SharedPreferences 的白名单键，带类型标记）+ `user_freq.txt`（词频原文）
  + 可选 `clipboard.jsonl`（剪贴板明文）+ 可选 `dicts/*.xz`（已下载词库）
- 剪贴板为何明文：密文由本机 Keystore 保护、跨设备无法解密，只能明文迁移；故导出默认不勾、
  导入需显式勾选。单节读入与导出预算均 16MB（按 UTF-8 明文字节算）
  ——后经加固：**导出预算收窄为 12MB、读入仍 16MB**（见本文件后续同名条目）
- 导入两种模式：**覆盖还原**（全量写回设置）/ **仅并入数据**（只合并词频与剪贴板，不动设置）
- 冲突与兼容：词频「同词取较大权重与较新天、不叠加」（两侧都先按天衰减到当天再合并，
  避免导入即失真或权重翻倍）；剪贴板按 `content_hash` 跳过本机已有、批内去重、按原时间落库；
  包格式版本高于本版拒绝导入；未知键忽略并计数、类型不符跳过、越界值走各级 setter 既有归一
- 完整性：导入前逐节 sha256 校验（词库用「文件名:sha256」聚合摘要），任何一节不符即整体拒绝；
  词库先全部提取到临时名、校验通过才改名，不留「一半新一半旧」的混合状态
  ——后经多轮加固：词库聚合摘要改为**仅勾选「导入词库」时校验**，白名单内的单文件内容不符按**跳过**处理
  （条目名含分隔符 / 条目数 / 总量 / 读取异常这类结构性问题仍判整包失败），见本文件后续同名条目
- UI：设置页底部新增「导出配置 / 导入配置」（SAF 文件选择器，零存储权限；文案在代码里下发，
  未改 `values/strings.xml`）

### 体积

- 新功能落地后包体逼近上限（5,241,794 B，仅剩 1KB 余量）→ 排除未被使用的 okhttp
  `publicsuffixes.gz`（41KB，Cookie 域判断用，本应用只走 WebSocket 与几个 GET、从不解析 Cookie）
  → 5,200,741 B（4.96 MiB，余量 41KB）

### 验证（真机 PACM00）

- 导出：`jinn-config-20260923-191707.zip`（966 B，节=[prefs]）保存成功；本地解包核对
  `manifest.json`（format / 版本 / 设备 / sha256）与 `prefs.json`（27 项、类型标记正确）
- 导入·仅并入：清单显示「设置 27 项、词频 0 条、剪贴板 0 条、词库 0 个」→ 日志
  `导入完成 mode=MERGE_DATA 设置=0 忽略=0`
- 导入·覆盖还原：日志 `mode=RESTORE 设置=27 忽略=0`，结果框提示「设置项需重启输入法后生效」
- 资源排除回归：点「检查软件更新」→「在线最新版本 20260923，已是最新」正常
- 门禁：全量 `testDebugUnitTest --rerun-tasks lintDebug` 通过；单测 42 类 / **421 例全绿**；
  Kotlin 警告 0；lint **0 error / 24 warning**（新增 5 条 `SetTextI18n`，`strings.xml` 禁改故保留）

### 加固：备份包整包加密 + 中文密码审核

- 背景：备份含剪贴板明文（身份证、手机号、地址、银行卡、账号口令都可能出现），明文包一旦落到
  网盘或聊天记录即等于泄露 —— 改为**无密码不可用**
- 算法（`ConfigCrypto`）：PBKDF2-HMAC-SHA256 **600,000 次迭代**派生 AES-256-GCM 密钥，
  每包独立 16 字节 salt + 12 字节 IV、认证标签 128 位；头部（magic `JINNENC1` / 版本 / 迭代数 / salt / IV）
  只含公开参数，迭代数入头部 ⇒ 将来调高强度仍能解旧包
- 密码规则：**只能用中文汉字**（Unicode HAN），**4~64 字**（界面建议 6 字以上），不许同字重复；本机不保存密码
- 密码输入框为**明文输入框**（普通文本 + `NO_SUGGESTIONS` + `IME_FLAG_NO_PERSONALIZED_LEARNING`）：
  用户要求不要用密码类型 —— 那类输入框在部分 ROM 会唤起系统「安全键盘」，安全键盘打不出拼音汉字；
  后两个标志确保密码不被本输入法联想/学习、也不被其它输入法个性化学习
- 防破解与防误判：错误提示恒为「密码错误或文件已损坏」（不区分，避免给攻击者密码验证判据）；
  解密用 `Cipher.update/doFinal` 手写循环而**不用 `CipherInputStream`**（后者可能吞掉认证失败异常，
  放出一份「短一截但看着正常」的备份）；明文中间件（导出 zip、导入临时 zip）用完即删
- 真机（PACM00）核对：焦点在密码框时 `mCurMethodId=com.jinn.inputmethod/.JinnIme`（未切安全键盘）、
  uiautomator dump 里输入内容出现在 `text` 属性（明文）；输入 ASCII 密码点「开始导出」→ 无打包日志、对话框留在原地
  （规则拦截生效）
- 门禁：单测 **43 类 / 436 例全绿**（新增 `ConfigCryptoTest` 15 例：密码规则边界、往返、改一字节即失败、
  截断/冒充/头参数篡改全拒、参数不得被削弱）、Kotlin 警告 0、lint 24（持平）；
  包 5,204,837 B（4.96 MiB，余量 38KB）；装机 Success

### 深度审查与修复（配置保存完整性 + 备份/导入健壮性）

- 手段：独立视角的子代理对抗式审查（逐文件读实现 + 与 `Prefs` 全部键逐条比对）+ 全量门禁 + 真机模拟用户操作
- **配置完整性结论：无漏项**。25 个可迁移键 + 剪贴板 2 键（`update_last_check_at` 属设备运行态、有意排除）
  ——后被用户澄清推翻：该键也要迁移，现为**全 26 键**（见 `Prefs.exportForBackup`）；
  词频文件、可选词库、剪贴板库都在导出范围内；索引缓存是派生数据、诊断日志非用户配置，均不该迁移
- 修复真缺陷 4 条：
  1. **返回键取消导入对话框 → 明文临时 zip 残留**：原来只挂了「取消」按钮回调，返回键/点外部都不触发；
     补 `setOnCancelListener` 与 `onDestroy` 清理
  2. **导入词频被运行期写盘覆盖**：新增 `UserFrequency.replaceFromBackup`，在写盘锁内完成「落盘 + 换内存态」；
     `load(enabled=false)` 同时清空内存态与脏标记（否则 `flush` 会把旧快照写回）
  3. **设置页三处后台线程无兜底**：导出/解密/导入链路含 SQLite 与文件 IO，漏网异常会连带杀死同进程的输入法
     → 整体 `runCatching`
  4. **剪贴板导出分页漏导**：结束判据「本页条数 < 步长」在存在解密失败行时会把「没取满」误判成「到底了」
     → 改为按游标 `nextOffset` 是否前进判断
- 另有 6 项加固：导出预算 16MB → 12MB 并对序列化结果再过闸（避免「刚导出就导不回来」）；
  解锁前先认 magic 并限包体 256MB（不再先把误选的大文件拷进缓存）；词库节限 16 个文件 / 单文件 64MB；
  头部迭代数上限 1000 万 → 200 万；非有限 Float 不进包；导入校验收紧为「节存在即必须通过摘要校验」
- 真机（PACM00）模拟用户操作：
  - 伪造两个明文残留（`jinn-import-*.zip` / `*.zip.plain`）→ 重启设置页 → 日志两条「清理残留中间文件」+ 目录清空 ✓
  - 选一个非加密文件导入 → **第一道门是密码审核**（弹出「输入备份密码」）→ 输入非汉字被规则拦下
    （无任何解密动作、对话框留在原地）✓
  - 截图确认密码框调起的是本输入法（不是系统安全键盘）✓
- 门禁：**43 类 / 441 例全绿**（新增 5 例：词频替换 3、非有限 Float 1、迭代数上限 1）、Kotlin 警告 0、lint 24 持平；
  包 5,204,837 B；装机 Success
- 已知受限：ADB 无法向设备输入中文（`input text` 遇非 ASCII 报 NPE），故「中文密码导出→导入」的完整跑通
  未能在 ADB 下跑通；该链路由 JVM 真实算法单测（真实 PBKDF2/AES-GCM）覆盖

### 全量覆盖确认 + 界面默认值调整

- 用户要求澄清：导出/导入必须覆盖「所有配置与用户设置的参数」。据此把 `Prefs` 的**全部 26 个键**纳入白名单
  （补上此前有意排除的 `update_last_check_at`：它只是「检查更新」的节流时间戳，照搬只让新机首次自动检查晚一点）；
  连同剪贴板 2 键、用户词频、剪贴板历史、可选词库，设置页每一项都在包内（逐项对照表见 `AGENTS.md`）
- 新增 `PrefsBackupCoverageTest`（源码对拍）：`Prefs` 里每个 `KEY_*` 常量都必须出现在导出与导入白名单方法体里，
  键名不得重复；`ClipboardPrefs` 的两个键同样纳入 —— 以后加键忘了进备份会直接测试失败
- 界面默认值调整（用户 2026-09-23 指定）：按键圆角 1dp → **5dp**、按键间隙 0.5dp → **2dp**、透明 0%（不变）、
  皮肤 → **紫晶**。实现上新增 `KeyboardSkins.INITIAL_ID`（新用户缺省值），与 `DEFAULT_ID`（全空覆盖的历史令牌基线、
  同时也是 `byId` 对脏值的回退点）刻意分开，避免「坏值回退成紫晶」与皮肤清单 id 冲突；主题默认仍为跟随系统
- 真机（PACM00）实测：键盘外观页显示「圆角 5 dp / 间隙 2 dp / 透明 0% / 皮肤 紫晶（选中）」✓
- 门禁：**44 类 / 445 例全绿**、Kotlin 警告 0、lint 24 持平；包 5,204,837 B；装机 Success

### 交互反馈补齐（用户反馈：输密码后没有明显反馈、输错也没弹框）

- 新增模态**进度框**（`showBusy` / `dismissBusy`）：打包并加密、解密校验、写入文件、导入四段耗时操作期间
  显示不确定进度条与说明（"密钥派生按安全强度计算，约需 1~2 秒，请勿退出"），结束（成功 / 失败 / 异常）
  统一关闭；不可取消，页面销毁时兜底关闭防 WindowLeaked —— 消除了「手机上处理慢导致的空窗期」
- 结果改为**弹框反馈**：导出成功（含「妥善保管文件与密码，忘记无法解密」提示）、导出失败、写入失败、
  导入失败、解密失败 —— 其中解密失败框带「**重新输入密码**」直达入口，不必再从文件选择器走一遍
- 密码规则错误改为**双重提示**：输入框红字气泡 + Toast（气泡几秒就消失，慢机 + 亮屏下容易被忽略）
- 取消也有反馈：导出 / 导入取消后提示行显示「已取消导出 / 已取消导入」
- 真机（PACM00）截图验证：密码框输入字母后点「开始导出」→ 输入框红字「备份密码只能用中文汉字…」
  与底部 Toast 同时出现，对话框留在原地可继续输入 ✓
  （进度框需中文密码才能走到，ADB 无法输入中文，该段由代码审查覆盖）
- 门禁：44 类 / 445 例全绿、Kotlin 警告 0、lint 24 持平；包 5,204,837 B；装机 Success

### 全面审查与修复（数据面 + 流程面双视角）

审查方式：两个独立视角的对抗式审查（数据层/加密层、UI 流程/并发/生命周期）＋自查，逐条给证据行号。
修复项（按严重度）：

**静默失败（最坏一类，全部消除）**
- 超上限的节被判成「不存在」→ 词频/剪贴板静默不导入而界面报成功：`readEntry` 改三态 `SectionRead`，
  超限一律拒绝整包；清单里单独标出「过大的节」并提前禁用导入
- 导入结果不展示忽略/跳过数：现在显示「忽略 N 项 / 剪贴板跳过 N 条 / 词库跳过 N 个」
- 剪贴板节超限时静默省略（`dropped=0`、界面无提示）：改为**中止导出并给出可读原因**
  （`ConfigBackupManager.lastError`：如「剪贴板历史过大，请取消勾选或先清理」）
- 词频/prefs 节此前没有字节闸：补齐，且导出包体必须低于导入端 256MB 上限
- 勾选词库但 manifest 未登记摘要时静默跳过：改为与其它节一致判包不完整

**数据正确性**
- 词频按天衰减用 Int 相减会在 `Int.MIN_VALUE` 附近回绕 → 脏行永不衰减、永久霸榜：
  改 Long 相减（`dayDiff`），并补测试
- 词频写盘「先删目标再改名」存在文件消失窗口：改为 `renameTo` 直接覆盖（失败才退回删+改名）
- `dirty` 清理竞态（渲染与清标记之间有并发学习 → 那次学习永不落盘）：加内存态版本号，仅版本未变才清
- 导入写词频前先 `Prefs.flush()`（`replaceFromBackup` 走写盘锁），避免与运行期防抖写盘互相覆盖

**流程、生命周期与安全**
- 启动清扫 / 解锁前清理与进行中的导出导入抢同一批文件名 → 加了 10 分钟时间窗
  （真机验证：旧残留被删、新文件保留）
  ——前述起改为「`tempInFlight` 登记集合为主 + 60 秒最小年龄兜底」，见本文件前述章节
- 解锁结果三态：把「文件移走/授权失效」误报成「密码错误」会让用户反复重输密码
- 杀进程重启前先 `Prefs.flush()`（`commit` 等排队中的 `apply`），不再只靠 800ms 延时赌落盘
- 「需要重启」遗漏词库（词库只在 IME 启动时加载）→ 判据含 `dictsWritten`
- 导入入口未防重入（连点会开两个选择器、覆盖明文临时包引用）→ 先禁用再 launch，各取消路径复位
- 页面重建期间完成的后台结果被丢弃（导入已落盘却零提示）→ 进程级 `pendingNotice` 承接并补提示
- 密码框取消不写提示（提示行停留旧失败文案）、`imeOptions` 设置顺序抹掉 `NO_SUGGESTIONS`
- 文案修正：覆盖写失败提示「原文件可能已被清空」、「覆盖还原」注明词频也按备份写回、
  「导入失败」不再只说包损坏（也含写入失败）

验证：门禁 **44 类 / 447 例全绿**、Kotlin 警告 0、lint 24 持平；真机（PACM00）验证清扫时间窗
（`jinn-import-old.zip` 被清、同批次新文件保留）；包 5,208,933 B（4.97 MiB）；装机 Success

仍属有意取舍（未改）：对话框不用 `DialogFragment`（旋转丢输入内容）；RESTORE 模式下备份里缺失的键
保持本机现值（旧版本包不含新键时不是「逐字还原」）；壳版本不匹配与密码错误共用同一提示；
剪贴板批量导入非事务（重导幂等可自愈）。

### 全面审查与修复（回归审查 + 盲区审查）

审查方式：两路新视角（①对上一轮修复逐条做回归验证；②此前未深入的盲区：加密数学、SAF 权限、
性能与内存、测试覆盖、文档一致性、反向泄漏检查、部分成功可恢复性）。**并发现上一轮有两处编辑未落地**。

**静默失败 / 报告标准（本次重点）**
- 导出**包体闸与词库闸缺失**（上一轮以为已加）：补齐词库 ≤16 个 / ≤64MB、包体 ≤256MB，
  与导入侧一致 —— 此前的超限包会「自己导不回来」，且用户看到的是「无法读取所选文件」这种错误方向
- `SectionRead.Failed`：节**读失败**不再被当成「没有这一节」（否则词频静默不导入、界面还报成功）
- 剪贴板新增数改为**库内净差**：导入条目带旧设备时间戳，本机库满时会被 `trimTo` 立刻裁掉，
  按「插入成功数」报 `+N` 是虚高的；被裁掉的差额计入跳过数（也修正了「重导幂等自愈」的错误说法）
- 导入按阶段包 `runCatching` 并返回 `failedStage`：**部分完成如实报告**（顺序写入不回滚，重试幂等），
  不再把「已写入一半」说成「导入失败」；设置项写入后 `Prefs.flush()` 校验（`apply()` 无失败信号）
- 导出并发保护（`exportInFlight` CAS）：页面重建后旧线程仍在跑时，两次导出会互相删中间件
- 剪贴板单条上限**两侧统一**（导出也排除超限条目）；导入新增条数上限（10 000，防畸形包造成假死）

**安全与隐私**
- `ConfigCrypto` 迭代数补**下限**（10 万）：密文强度不该由文件头自己声明（`iterations=1` 的包此前可解）
- SAF 写入改 `"wt"` 优先（默认 `"w"` 在部分 provider 不截断，覆盖大文件会留残骸）
- 日志里的 uri 只记 `scheme://authority`（完整 uri 可能含账号/目录名，而日志与诊断包会被用户上传求助）

**生命周期与状态**
- 密码框补 `setOnCancelListener`：返回键/点外面取消不会触发 negative，漏了会让「导入配置」永久禁用
- 「已解密但 manifest 不可识别」与「解密失败」分开提示：前者密码是对的，引导重输只会让人怀疑自己
- 过大节的包提前禁用「开始导入」（不再等输完密码才失败）

**数据正确性（词频并发）**
- `remember` 的内存更新移入 `saveLock`：与导入替换（clear + 重填 + 清脏）串行，避免「刚学到的词被抹掉」
- `replaceFromBackup` 按 `version` 核对脏标记，并在有并发学习时补排一次落盘（此前会永久跳过）
- 「仅并入」模式先 `UserFrequency.flush()`，防抖窗口内的学习不再随替换丢失；`version` 加 `@Volatile`

**临时件清理（隐私）**
- 新增 `tempInFlight` 登记集合（导出/解密自己的文件不删）+ 最小年龄 60 秒（原 10 分钟窗会让崩溃残留的
  **明文 zip** 活过整个会话）；清单补 `.jinn.tmp`；词库 `*.restore` 纳入清理，且**删除失败留日志**
  （真机验证：旧残留被清、新文件保留、`*.restore` 被清；目录不可写时明确告警）

**测试与守卫**
- 新增 7 例：双 prefs 文件往返、Int/Long 极值往返、含 `"`/`\`/换行/emoji 的字符串往返、
  畸形输入（半截 JSON/类型不符/空键/多字符标记）、剪贴板条数上限、迭代数下限拒绝、原子写覆盖
- `PrefsBackupCoverageTest` 加固：键名正则放宽（防大小写/数字键静默漏检）、数量断言改**精确 26**、
  新增「类型键与取值工具必须配对」（把 `KEY_PORT` 写成 `asString(v)` 此前照样绿）
- 测试 `FAST_ITERATIONS` 提到 10 万（与迭代数下限对齐）

验证：门禁 **44 类 / 454 例全绿**、Kotlin 警告 0、lint 0 error / 23 warning（少 1）；
真机（PACM00）验证 60 秒清理窗、词库残留清理与失败告警；包 5,208,933 B（4.97 MiB）；装机 Success

新增取舍（未改）：`pendingNotice` 在「弹框随实例销毁」的窄窗口内仍可能丢结论；`inspect` + `import`
对 zip 各扫一遍（约 10 趟），大包可再优化为复用解析结果；导入仍非事务（但已如实报告阶段）。

### 全面审查与修复（红队攻击面 + 端到端可测性）

审查方式：两路新视角（①红队：给定一个恶意/损坏/超大的备份包，能对用户环境做什么；
②往返等价性、可测性缺口、文档一致性）。本次有两条属「一包就能把输入法搞挂」的量级。

**攻击面（最高优先）**
- **词库白名单**：导入只接受 `OptionalDicts` 清单内的文件名，清单外的条目不落盘（仍参与摘要校验
  与跳过计数）。此前任意 `.xz` 都能落进 `filesDir/dicts/`，而引擎启动会无条件全量解压 ——
  xz 压缩比 >100:1，几百 KB 的包即可让输入法启动 OOM 或写满磁盘，且重启后继续炸
- **词库节改总量闸**（此前 16×64MB=1GB 先落盘再校验 → 现在总量 64MB，与导出侧一致）
- **`zipEntryNames` 加 512 条上限**（zip 本地头仅几十字节，256MB 包可含数百万条 → OOM）；
  「包内是否有词库条目」改用短路的 `hasDictEntry`
- **`favorite_symbols` 导入归一 + 限长**：它是唯一零归一落盘的键，解析在**主线程**（键盘视图构造），
  十几 MB 的 JSON 会让每次重建键盘都做百万级解析（ANR/OOM），且已持久化、重启输入法无效
- **`category` 归一到白名单** + 剪贴板面板/搜索的 meta 加单行省略（外部字段直接进主线程布局）
- **`manifest.device` 截断压平**（「设备名」是外部输入，会原样进导入对话框）
- **解锁读流加 90 秒预算** + `readFully` 防 `read` 返回 0 的空转：进度框不可取消，
  恶意 provider 的极慢流可让设置页（与输入法同进程）长时间无响应

**数据一致性**
- 导出前 `UserFrequency.flush()`：此前会漏掉 2 秒防抖窗口内的最后一次学习
- 导出的剪贴板同样受 `MAX_CLIP_IMPORT_ITEMS` 约束（此前只在导入侧限，超出部分会在对面被静默截断）
- 导入把 `maxItems` 改小后立即 `trimTo`（此前要等下一次复制入库才收敛）
- 往返等价性复核（无新增缺陷）：导出全部走 getter，26 键往返恒等；`symbol_group_order` 的
  「从未编辑 → 显式默认序列」是唯一一处缺省写进（低危，已记录）

**可测性（本次重点补上）**
- zip 层主路径此前**没有 JVM 守卫**（只能靠真机人工验证，而 ADB 打不出中文密码）：
  把 `readSection`/`readEntry`/`SectionRead`/`verify`/`sectionOf`/`zipEntryNames`/`hasDictEntry`
  与各上限常量改为 `internal`，新增 `ConfigBackupZipTest` 8 例：打包→读回、Missing/TooLarge/Failed
  三态边界、损坏条目、摘要绑定、条目上限、分类归一、预算链
- 顺带钉住一条真实语义：**非 zip 文件 → Missing**（不是 Failed），由 manifest 缺失兜住拒绝

**文档与 UI 一致性**
- 修正 AGENTS 三处自相矛盾/不准确：清理窗口 10 分钟 → `tempInFlight` + 60 秒、
  缓存路径补 `JinnIme/`、词库标准改「总量」
- UI 文案补全：导出说明写明包内含连接设置/语音提示词/设备型号，分享前请注意；剪贴板勾选项
  写明「在包内以明文保存」；密码提示补 4~64 字与「别用同一个字重复」

验证：门禁 **45 类 / 462 例全绿**（+8 例）、Kotlin 警告 0、lint 0 error / 23 warning；
包 5,213,029 B（4.97 MiB）；装机 Success

仍属取舍：进度框不可取消（已用时间预算兜底；真正的「取消」需把中断贯通导出/解密/写入三处状态机）；
导入仍非事务（但已按阶段如实报告）；`inspect`+`import` 对 zip 各扫一遍；剪贴板「本机已有同内容」时
收藏/分类不合并（只跳过）；词库同名不覆盖（内容不同也只跳过，仅计入跳过数）。

### 全面审查与修复（红队回归 + 全旅程 + 资源/性能）

审查方式：对上一轮改动做逐项回归验证，另两路新视角（全旅程 UX 与资源生命周期、性能与测试缺口）。
本次还发现一个**交付级工程陷阱**（见末尾）。

**安全 / 健壮性**
- `unlockLocked` 的 8 字节 magic 头此前既无时间预算也无空读保护（上一轮只给了正文循环）：
  恶意/故障 provider 每次只给 1 字节就能把握手拖成无限长 → 头部与正文共用同一预算，并加
  `MAX_EMPTY_READS` 防 `read` 返回 0 的空转；超时单独返回 `UnlockResult.ReadTimeout`
  （文案「读取超时，请把文件保存到本机后重试」，不再误报成「文件已移走/授权失效」）
- 词库落盘新增**内容校验**：白名单只保证文件名合法，内容仍须与 `OptionalDicts.checksum` 一致，
  否则「白名单名 + 攻击者内容」照样会被引擎加载（压缩炸弹）；不符按**跳过**处理（不整包拒绝），
  同名词库改版后旧备份仍能导入其余部分
- `.restore` 中间件登记进 `tempInFlight`，清理时先查集合（慢设备上曾被清扫线程删掉正在写的文件）
- `decodeClipboard` 加 `maxItems` 硬闸（16MB 可含近百万极短行 → 每行一个对象 = OOM 通道）

**流程 / 生命周期**
- 导入进行中置 `importInFlight`：旋转/回收触发 `onDestroy` 时不再删正在读的明文 zip
  （此前会让导入以「包不完整/已损坏」这种完全不沾边的原因失败）
- 三个配置流程对话框（导出选项 / 密码 / 导入清单）存字段并在 `onDestroy` dismiss：此前旋转会 WindowLeaked
- 清单框「返回键/点外」取消补提示行复位（此前永久停在「正在解密…」）
- 重启输入法前除 `Prefs.flush()` 外补 `ClipboardPrefs.flush()`（两个 prefs 文件，此前剪贴板侧可能回退）

**性能**
- manifest 提前为 zip 的**第一个**条目：读取端不能跳转，放最后意味着每次「扫 manifest」都要把全部节
  解压丢弃一遍（一次导入两趟，最坏近百 MB）→ 现在几乎为零
- 导出三节各只做一次 `toByteArray`（`sectionOfBytes` + `putBytes` 复用）：12MB 剪贴板节从约 48MB
  瞬时垃圾降到 12MB
- 导出完成日志不再对词频做全量解析、不再逐字符数 12MB 字符串（改用惰性行数统计）

**可测性**
- `restoreDicts` 改签名（吃 `dir: File`）并保持 `internal`，新增 5 条 JVM 测试：清单外不落盘、
  内容与官方校验值不符跳过、摘要不符整包失败、条目名带路径分隔符拒绝、条目数超限拒绝
- 「条目名收集有上限」测试改为精确断言（等于上限、且被截掉的正是词库条目）；修正损坏条目测试的机制注释

验证：门禁 **45 类 / 467 例全绿**（+5 例）、Kotlin 警告 0、lint 0 error / 23 warning；
包 5,213,029 B（4.97 MiB）；装机 Success；冒烟无崩溃

**⚠️ 交付级工程陷阱（已写入 AGENTS）**：改完代码后 `assembleRelease` 可能仍报
`compileReleaseKotlin UP-TO-DATE`（Gradle 文件监控漏检），于是交付的包是旧的 —— 本次遇到一次。
**判据不要只看字节数**：实测连续三轮 APK 大小完全相同（R8 优化吸收了改动），而内容确在更新；
可靠判据是直接读 APK 内 `classes.dex` 的文本，搜本次新增的**字符串常量**（方法名会被 R8 混淆，
搜方法名会假阴性）；确认 UP-TO-DATE 而源码已改时 `gradlew clean` 后重建。

仍属取舍：进度框不可取消（时间预算兜底；真正「取消」需贯通三处状态机）；导入仍非事务；
`inspect`+`import` 仍各扫一遍 zip（manifest 已提前，剩余成本主要是同一节被解析两次）；
剪贴板「本机已有同内容」时收藏/分类不合并；词库同名不覆盖（内容不同也只跳过）。

### 审查与修复（可维护性 + 测试有效性）

审查方式：两路新视角（可维护性/死代码/注释漂移、测试有效性/覆盖缺口），并对词库节测试做**变异验证**。

**测试有效性（本次最重要的发现）**
- 上一轮新增的 5 条词库测试里 **2 条是假测试**：「路径分隔符」与「条目数上限」都传了 `"0".repeat(64)`
  作为期望摘要，而摘要闸单独就能拒绝整包 → 删掉被测逻辑测试仍然绿
- 修法：期望摘要按「该条目若被正常记录」的值复算，并让清单认得它 → 删掉被测逻辑时会得到 `(0,N)`
  而不是 null，断言才真正生效
- **变异验证**：临时把 `restoreDicts` 改成「直接返回 (0,0)」→ **6 条词库测试全部失败** ✓
  证明它们确实能发现实现被破坏（验证后已还原并复跑全绿）
- 新增 3 条真测试，补齐此前**完全无法触发**的路径：成功落盘、同名不覆盖、摘要不符时清掉已提取的临时件
  （为此给 `restoreDicts` 加了注入点 `checksumOf`，让测试能提供合成清单）
- 另修 1 条永真断言：`UserFrequencyTest` 的「关闭开关不参与排序」此前用了不含已学词的入参数组
- 冻结摘要标准：新增 golden（空串 SHA-256 公开值）+ 聚合摘要与**独立实现**比对
  （此前 `dictsDigest` 既当实现又当 oracle，标准写错时测试跟着一起错）

**可维护性**
- 删掉 4 个无人读取的字段（`ExportOutcome.sections/bytes`、`ImportOutcome.mode/sections`）
- `MAX_TEXT_SECTION_BYTES * 9 / 10` 提为 `EXPORT_SECTION_LIMIT_BYTES`（此前测试复刻同一算式，
  改比例会出现「代码改了测试仍绿」）
- 修正 4 处与实现相反的注释：import 的「任何一节缺失即拒绝」（词频/剪贴板其实是可选节）、
  `ImportMode` 漏了词库、`restoreDicts` 的 `@return` 漏两类跳过原因、测试头注释「manifest 放最后」
- 合并 2 处孤立注释块；`restartImeProcess` 的两处 flush 失败改为留日志（此前返回值被丢弃）
- **包格式规则集中写进 `ConfigBackup` 类注释**（外壳 41 字节布局、内层条目与节名对应、manifest 字段、
  sha256 标准、manifest 必须第一个、版本规则）—— 此前同一件事实一半在代码一半在文档

验证：门禁 **45 类 / 469 例全绿**、Kotlin 警告 0、lint 0 error / 23 warning；装机 Success；
APK 内 dex 字符串确认含本次改动

### 核实（对最近改动的自审，确认无漏改与误判）

核实方式：`git diff` 与针对性检索做客观核对 + 独立视角逐项核实「意图 vs 实现」+ **细粒度变异验证**。

**细粒度变异验证（本次新增的证据类型）**
- 临时破坏两处**具体逻辑**（去掉词库路径穿越检查、去掉内容与官方校验值比对）→ 跑测试：
  **只有对应的 2 条测试失败**（`词库条目名带路径分隔符时拒绝整包`、`清单外与内容不符的词库都只跳过不落盘`），
  其余 13 条全过 → 证明每条测试对**其对应逻辑**敏感、测试之间相互独立
  （上一轮的整体变异只能证明"全绿/全红"，给不出对应关系）→ 验证后还原并复跑全绿

**逐项核实结论**
- `checksumOf` 注入点：默认值与改动前**逐情形等价**（清单有/无、checksum 空串、含分隔符的文件名都一致）；
  聚合摘要在判断前无条件记录 → 注入合成清单不改变摘要标准；调用方兼容
- `EXPORT_SECTION_LIMIT_BYTES`：Long 运算与截断值一致（15,099,494），全仓无第二处就地算式，测试已改断言常量
- 删除 4 个字段：全仓（含测试）确认无读取点；导出/导入日志用的是局部变量，信息未丢；
  `ImportOutcome` 的命名实参与属性逐一对应
- 注释修正 4 处：3 处与实现完全一致；**发现 1 条真问题**（见下）
- `ConfigBackup` 的包格式规则：字段/字节序/节名映射/摘要公式/hex 全部核对无误；两处措辞偏硬已改
  （「manifest 第一个」是**生产者规则**而非读取端校验项；sha256 标准限定为三个**文本节**——词库文件算压缩字节）
- `restartImeProcess` 的 flush 检查：日志在 kill 之前同步落盘（进程被杀不影响）；此刻不提示用户是合理处置
- 新测试逐条敏感度：8 条全部为真测试（含"删掉对应实现即红"的逐行定位）
- `UserFrequencyTest` 的 rank 断言：反事实推演确认删掉 `!enabled` 分支即会红（旧写法无论如何都绿）
- 文档订正：三处「构建判据」表述互相一致；再清掉一处过期体积数字（并写明体积不可作判据）

**修掉的 2 处问题**
- `restoreDicts` 的 `@return` 说「跳过的三种情形」，实现有**四种**（多出「改名落盘失败」，它会计入
  用户可见的「词库跳过 N 个」）→ 已补全（零行为影响）
- **核实过程中自查出的新缺陷**：给包格式规则写 KDoc 时用了 `dicts/*.xz` 这种写法，而 Kotlin 的块注释
  **支持嵌套** → 多出的 `/*` 让注释吞掉后续代码（`ConfigBackup` 定义被注释掉）→ 编译失败
  （`Unresolved reference: ConfigBackup`）。已改为「`dicts/` 目录下的 `*.xz`」，并全仓检索
  `/*` 紧邻字母的序列确认无同类残留。**教训：注释里避免 `/*` 序列；改完必须跑门禁**

验证：门禁 **45 类 / 469 例全绿**、Kotlin 警告 0、lint 0 error / 23 warning；装机 ✓

**提交提示**：备份功能的四个核心文件（`ConfigBackup.kt` / `ConfigBackupManager.kt` / `ConfigCrypto.kt` /
`ConfigBackupZipTest.kt`）目前是**未跟踪的新文件**，提交时必须 `git add`（只提交已跟踪文件的改动会漏掉它们）。

### 审查与修复（风格一致性 + 注释/文档最终对账）

审查方式：独立视角逐条核实上一轮注释改动 + 与项目其它代码做**风格一致性**对比 + 文档与代码的最终对账。

**修掉的问题**
- **真问题（低危）**：`restoreDicts` 在解压**中途**抛异常（deflate 损坏 / 磁盘满）时，当前条目的半截
  `*.restore` 既不在 `pending` 也未登记 `inFlight`（两者都在循环之后赋值）→ 只能等下次清扫回收。
  现在读取失败分支按后缀清空该目录的 `*.restore`（该目录只放词库，不会误伤）
- **风格收口**：`TAG` 从 `"ConfigBackup"` 改回类名 `"ConfigBackupManager"`（全项目另外 21 处 TAG 都等于
  类名，此前两个模块共用一个日志标签）；`MAX_CLIP_IMPORT_ITEMS` 移入顶部常量区；解析层的 `* 4`
  提成命名常量 `CLIP_PARSE_ITEM_FACTOR`（消除"文档说 1 万、实现是 4 万"的反复疑问）；导出完成日志
  句式统一
- **注释补全**：`Section.bytes` 注明「仅登记/诊断用，导入只校验 sha256」与两类字节标准；
  `sectionOf(text)` 注明「文本重载主要供测试，导出走 `sectionOfBytes`」
- **文档对账（5 处）**：AGENTS 的「节存在即必须通过摘要校验」补上**词库例外**、可见性表述订正为
  「`internal object` 成员」、过期的 Release 体积改为最新实测并写明体积不可作判据；
  更新日志两处首版标准（10 分钟时间窗、逐节校验）加注现行机制

**测试强度提升（三轮来一直挂在"建议级"的项）**
- `PrefsBackupCoverageTest` 的类型配对从 **10 项扩到全部 25 个普通键** + 收藏符号的归一分支单列钉住：
  此前「把 Int 键写成 `asString`」这类错误只有少数键会被发现
- 新增「导出与导入两侧都必须覆盖全部键」：新增键时只加一侧会被立刻抓住

验证：门禁 **45 类 / 470 例全绿**（+1 例）、Kotlin 警告 0、lint 0 error / 23 warning；
装机 Success；真机用旧残留触发清理，日志标签已显示 `ConfigBackupManager`（同时确认清理逻辑仍正常）

### 核实（逐项核实前述改动 + 测试有效性变异验证）

审查方式：逐项核实上一轮改动是否符合预期、是否存在误判，并用**变异验证**证明守卫测试真的有效。

**三条疑点核查（全部排除，非缺陷）**
- 「重启前两个 prefs 都要 flush」：`SettingsActivity.restartImeProcess` 已同时调 `Prefs(this).flush()` 与
  `ClipboardPrefs.of(this).flush()`，且各自失败都写日志（磁盘满不再表现为「重启后设置回退」且无迹可查）
- 「临时件注销会不会交叉误清」：`FILE_PREFIX = "jinn-config-"` 与 `DECRYPT_PREFIX = "jinn-import-"` 不重叠，
  两个 `removeIf` 的作用域互不干扰
- 「前述的清理改动有没有测试覆盖」：**没有**（"解压中途失败"路径全仓 0 测试）→ 本次补齐

**补齐测试（1 例）**
- `zip 读取中途失败时半截临时件也会被清掉`：造一个「条目头完整、内容被截断」的包（256KB 随机字节，
  不可压缩以保证截断点落在数据区），断言读取失败必须整包拒绝**且**目录里不留半截 `.restore`

**变异验证（测试有效性，两次）**
- 正向：删掉 import 侧 `KEY_VOICE_INPUT` 分支 + export 侧 `put(KEY_THEME_DARK_AT, …)` →
  「类型键与取值工具必须配对」「导出与导入两侧都必须覆盖全部键」「每个持久化键都必须同时在导出与导入
  白名单里」**三条守卫全部 FAILED** → 证明三个守卫都能真的抓到漏键/错类型，不是假测试
- 反向：删掉前述加的读取失败清理 → 新测试在 `ConfigBackupZipTest:277` **FAILED** → 证明半截件确实
  会被创建、清理代码确实必要（不是防御性冗余）

验证：门禁 **45 类 / 471 例全绿**（+1 例）、Kotlin 警告 0、lint 0 error / 23 warning；
**产品代码零净变化**（两处变异均已还原并逐项核对），故无需重新装机

**尚未做（建议级，供后续决定）**：`Prefs` 行为测试（需注入假 `SharedPreferences` 实现，目前只有源码对拍）、
`export` 四道闸抽纯函数后逐条断言、诊断包与配置包两套 SAF 流程合并、`ConfigBackupManager` 拆出 zip 读取层

### 审查与修复（诊断日志/诊断包专项 + 跨进程核实）

审查方式：`Diagnostics` 与「导出诊断包」的专项对抗审查（并发 / 资源 / 隐私 / 与配置导出流程的交互），
外加跨进程与缓存一致性核实。

**修掉的 6 处真问题**
- **半截诊断包被当成成品导出**：`latestBundle` 兜底定位只按前缀匹配，打包中断留下的 `*.zip.tmp`
  mtime 最新 → 进程被回收后恢复时会把半截件原样写给用户，提示还是「已导出」→ 过滤掉 `.tmp`
- **并发导出互相删源**：`exportBundle` 一进来就清同前缀旧包，而复制线程在 SAF 目标为云盘时能跑好几秒
  → 连点两次会让第一次复制失败且目标已被截断成 0 字节 → 清理加 60 秒年龄闸（`EXPORT_BUNDLE_KEEP_MS`）
- **导入的两个静默早退**：`manifest` 条目读不到 / 清单无法解析时直接 `return null`，而同函数其它拒绝
  都有 W 级日志 → 用户拿截断包求助时，诊断包里分不清原因 → 各补一条 W
- **配置侧清扫命中诊断包临时件**：`cleanupStaleCache` 的裸 `.zip.tmp` 规则会清掉诊断包正在写的 tmp
  → 清扫范围限定到本流程前缀（`jinn-config-*` / `jinn-import-*`）
- **隐私：三处用户正文以 I/W 级落盘**（日志文件在外部存储，且会随「导出诊断包」整体外发）：
  `JinnIme` 的上屏最终结果、`AsrClient` 的识别文本与原始报文 → 一律改为只记长度/字数

**核实为无问题（非缺陷）**
- 跨进程：`AndroidManifest` 未声明 `android:process`，IME 与设置页同进程，`SharedPreferences` 无多进程缓存问题
- 两个流程的临时件前缀隔离（`jinn-config-` / `jinn-import-` / `jinn-diagnostics-`），
  两个 SAF 回调各有独立 `pendingXxxZip` 字段，不会互相覆盖
- `Diagnostics` 单进程写路径线程安全（每行一次 `synchronized` open→write→close，无常驻 fd）；
  磁盘满/目录被删只 `Log.w` 不抛，不会把业务带崩；密码侧全局无泄漏（只记异常类名）

**未修（记录理由，供后续决定）**
- 日志目录无大小上限（长跑可能写大）：给 IME 主线程每次写日志加 stat 闸有性能代价，需更谨慎的设计
- `resolveLogDir` 失败后永久写进（进程内不再重试）；`AsrClient` 把内网 `wsUrl` 写进明文日志；
  `device-info.txt` 在打包中被杀时残留（无隐私内容，每次导出会重写）

验证：门禁 **45 类 / 471 例全绿**、Kotlin 警告 0、lint 0 error / 23 warning；装机 Success；
dex 判据两条命中（`textLen=`、`manifest 条目读不到`）；启动设置页无崩溃

**前述复核与补修（用户质疑「6 处是否误判」后逐条回验）**

- 逐条独立回验结论：**4 条真缺陷、1 条判定夸大、1 处计数表述不严谨**；并发现上次修复**不完整**
- **夸大的一条**：`cleanupStaleCache` 的裸 `.zip.tmp` 规则 —— 60 秒年龄闸已经挡住「正在写的 tmp」，
  只有打包耗时 >60 秒（需日志目录达到 GB 级）才可达 → 应判「需评估（极低概率）」而非真缺陷
  （限定前缀的改动无害且更精确，保留）
- **补修的两处遗漏**：
  - 配置侧 `exportLocked` 的清旧包是**无条件删同前缀**（与诊断包侧同型缺陷），上次只修了诊断包侧
    → 同样加 60 秒年龄闸（复用 `STALE_MIN_AGE_MS`）
  - 三处包名时间戳是**秒级精度**：同一秒内两次导出会算出同名文件，`dest.exists()` 的前置删除
    会删掉前一次正在复制的包 → 统一改为毫秒（`yyyyMMdd-HHmmss-SSS`）
- **判定标准（后续沿用）**：有可触发路径 + 有真实后果 = 真缺陷；需要极端前提才可达 = 需评估；
  纯理论无路径 = 不报
- 验证：门禁 45 类 / 471 例全绿、Kotlin 警告 0、lint 0 error / 23 warning；装机 Success；
  dex 判据命中（`HHmmss-SSS`）；启动无崩溃

### 审查（越界值归一逐条验证 + 守卫写进）

- 逐条验证更新日志里「导入的越界值走各级 setter/消费点既有归一」这一**此前从未被验证**的断言：
  26 键全部有防护 —— host `normalizeHost`、port getter 钳 `1..65535`、外观三项 getter 钳位、
  主题时刻 setter `Math.floorMod` + getter `takeIf`、皮肤 `KeyboardSkins.byId`、符号顺序 `SymbolOrder.serialize`、
  双拼方案 `ShuangpinScheme.of`、键盘模式 `coerceIn`/`takeIf`；其余键无越界面（Boolean / 长度不敏感 / 交服务端权威）
- 把归一规则写进为守卫测试（12 项断言 + 属性块提取辅助），**变异验证**：删掉 port 的 `takeIf` → 守卫失败 → 已还原
- 三类「看似缺陷、实为正常」的排查结论（**故意不改**）：port 的 setter 存原值 + getter 钳位是有意分工；
  themeMode 用 `takeIf` 而非 `coerceIn` 是语义正确性要求（coerceIn 会把 9 钳成「定时」）；
  language 只挡空白串（语言白名单属服务端权威，客户端不重复维护）
- 门禁：45 类 / **472 例**全绿、Kotlin 警告 0、lint 0 error / 23 warning；**产品代码零改动**

## 发布记录（20260924，跨天新建 tag）

- 提交 `311aecb`（31 文件，+4987 / −88）推送到双端 main（GitHub + Gitee），均为快进（`0c617e3` → `311aecb`）；
  过程记录：首次提交误把临时提交信息文件一并加入（`git add -A` 时它还在工作区），因**尚未推送**，
  用 `git reset --soft` 修正后重新提交，最终提交不含该文件
- APK：`versionCode=20260924`、5,213,029 字节、`sha256=05CF1476…2A7F`、`apksigner` V1=false / V2=true / V3=true、
  4 字节对齐通过；**全量编译**（`build_apk.py --clean`）+ dex 判据复核（`HHmmss-SSS` 与 `textLen=` 均命中，
  确认构建确实拾取了本次改动，规避「UP-TO-DATE 交付旧包」陷阱）
- 两端**新建** Release `20260924`（跨天，非覆盖）：
  - GitHub：`gh release create 20260924 --latest`
  - Gitee：`POST /releases`（id `1163187`，`target_commitish=main`）→ `POST attach_files`（id `3258954`，5,213,029 字节）
  - 正文规则（用户 2026-09-24 明确）：**≤10 行**，详细变更指向本文件。初版误写成 130+ 行（11KB），
    已重写为 **5 行** 精简版（新增 / 修复 / 验证 + 指路），双端一致；原文已不保留历史累积
- 旧版 APK 清理（双端只保留最新一版）：GitHub 删除 `20260923` 的 `jinn-release.apk`；
  Gitee 删除 `20260923` 的附件（id `3254175`，返回 204）；更早的 tag 两端本就无 APK 附件；词库包附件不受影响
- 校验：双端下载实测 `HTTP=200` / `size=5213029`，sha256 与本地发布包**三方一致**（`05CF1476…2A7F`）；
  GitHub 侧另用 `HEAD` 复核服务端 `Content-Length=5213029`
- 过程记录：GitHub API 当晚多次瞬时抖动（`api.github.com: EOF`、首次下载中断在 2,784,531 字节），
  重试 + 断点续传后通过；**判定以「服务端 Content-Length + 重下 sha256」为准**（不轻信一次下载的 size）

## 文案清理与 Release 正文精简（20260924）

- 全仓文案去掉描述自己工作过程的话：章节标题不再带次序编号，正文改直接叙述
  （涉及 35 个文件，纯文字替换，删改行数对称，代码只动注释）
- Release 正文统一为「本版新增 / 修复 / 验证 + 详细变更见更新日志」，双端 12 个 Release 全部 ≤7 行
  （此前是累积式长文，最长 179 行）
- `AGENTS.md` 补上「正文不超过 10 行」这条要求；与新规则冲突的旧表述一并改正
- 保留「提示词」：它是设置页的功能名（`strings.xml` 的 `settings_prompt`），不是自指措辞
- 门禁：45 类 / 472 例全绿；lint 0 error / 23 warning
