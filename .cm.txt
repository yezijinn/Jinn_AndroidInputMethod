按用户要求调整：hint 改回「控制」+ 隐私功能彻底移除（不兼容旧数据）

【调整 1】方向按钮的副文字改回「控制」
按钮宽度不够，最多 3 个字。进入方向面板时只有主文字变红色粗体「返回」，副文字保持「控制」：
  未进入：[ 方向 / 控制 ]
  进入后：[ 返回 / 控制 ]   ← 红色 #FF5A5F + 粗体

【调整 2】隐私功能彻底删除（用户明确：不再处理任何隐私、不兼容旧代码）
此前只删了 UI 入口，保留了 is_private 列与掩码逻辑。现按用户要求全部移除：

ClipboardDb：
- 建表去掉 is_private 列；Item 去掉 isPrivate 字段
- upsert 去掉 isPrivate 参数与写入；selectCols / readItem 同步
- setPrivate() 删除
- whereClause / recentPage / count 去掉 privateOnly 参数与条件
- deleteAll 条件简化为 "is_favorite = 0"
- TRIM_PRIORITY 简化为单项 "is_favorite = 0"
- mergeDuplicates 去掉隐私合并逻辑
- ClipboardFilter 去掉 privateOnly 字段与 PSEUDO_PRIVATE 常量
- **删除 deduplicate() 方法与 mergeDedupeFlags() 纯函数** —— 二者已无调用者
  （入库去重早已改由 upsert 的 content_hash 唯一约束承担），属废弃代码

UI：
- ClipboardPanelView / SearchPanelView：去掉内容掩码（改为直接显示）、
  去掉「· 隐私」标签、去掉点击展开分支、删除 revealedIds 集合

数据库迁移：
- DB_VERSION 4 -> 5，新增 v5 迁移：重建表去掉 is_private 列，其余内容原样保留
  （隐私标记本身丢弃，不做兼容）。同时重建 created_at 与 content_hash 两个索引

测试：
- 删除 ClipboardDedupeTest（测的是已删的 mergeDedupeFlags）
- 重写 ClipboardFilterTest：去掉 privateOnly 断言与两个隐私专属用例

【验证】
- compileDebugKotlin + testDebugUnitTest 通过
- 真机：v4 -> v5 数据库升级正常，无 SQLiteException / FATAL EXCEPTION
- 真机截图确认：
  · 功能面板显示「方向 / 控制」（hint 已修正）
  · 分类栏为 返回|全部|网址|数字|收藏|搜索|清空，无「隐私」
  · 列表条目分类正常（32174→NUMBER、747540.com→URL），无「· 隐私」标签
  · 旧数据完整保留（13 条）
