可以。下面这版不是普通“实现需求”的提示词，而是给 **AGENT 自主检索资料、阅读源码、提取算法、再结合当前 CapsWriterIME 实现**用的。

# AGENT 资料检索与技术研究提示词
## 不完整拼音解析（Incomplete Pinyin Parsing）+ 候选补全（Pinyin Completion）

```text
你是 CapsWriterIME 项目的高级输入法算法工程师。

当前任务：

研究并设计：
「不完整拼音解析（Incomplete Pinyin Parsing）」
+
「候选补全（Pinyin Completion）」

目标不是简单实现字符串补全，而是参考成熟开源输入法的拼音解析架构，研究其源码、数据结构、算法和候选召回机制，然后在当前 Kotlin Android IME 中实现轻量化版本。

==================================================
一、必须优先查询的官方/开源资料
==================================================

开始工作前必须主动查询并阅读以下资料。

--------------------------------------------------
1. libpinyin
--------------------------------------------------

项目：

[libpinyin GitHub](https://github.com/libpinyin/libpinyin?utm_source=chatgpt.com)

重点研究：

- 拼音解析
- 拼音分词
- incomplete pinyin
- pinyin segmentation
- 拼音候选生成
- 拼音纠错
- 模糊拼音
- 词库查询
- 用户词频
- 候选排序

重点不要只看 README。

必须进入源码目录搜索：

pinyin
segment
parse
fuzzy
prefix
dictionary
lookup
candidate
sentence
phrase

重点寻找：

「输入字符串 → 拼音音节 → 词库候选」

这一完整数据流。

--------------------------------------------------
2. ibus-libpinyin
--------------------------------------------------

项目：

[ibus-libpinyin GitHub](https://github.com/libpinyin/ibus-libpinyin?utm_source=chatgpt.com)

功能说明：

[ibus-libpinyin 功能说明](https://github.com/libpinyin/ibus-libpinyin/wiki/ibus-libpinyin-features?utm_source=chatgpt.com)

重点研究：

- pinyin correction
- fuzzy pinyin
- fuzzy pinyin segment
- 拼音 segment
- 输入错误修正
- 拼音边界推断

官方功能说明明确包含：

pinyin correction
fuzzy pinyin
fuzzy pinyin segment

这些机制必须作为本项目后续扩展的参考，但第一阶段不要直接全部实现。

--------------------------------------------------
3. RIME / librime
--------------------------------------------------

核心：

[librime GitHub](https://github.com/rime/librime?utm_source=chatgpt.com)

RIME 官方资料：

[RIME UserGuide](https://github.com/rime/home/wiki/UserGuide?utm_source=chatgpt.com)

重点研究：

- Speller
- Translator
- Segment
- Dictionary
- Candidate
- Algebra
- Schema
- 拼音输入方案
- 输入码解析
- segmentation
- candidate generation

特别研究：

「输入编码没有完全命中时，RIME 如何继续产生候选」。

不要只研究 UI。

必须关注：

raw input
→ segmentation
→ translator
→ dictionary
→ candidate

这一数据流。

--------------------------------------------------
4. RIME 朙月拼音
--------------------------------------------------

重点查找：

[rime-luna-pinyin](https://github.com/rime/rime-luna-pinyin?utm_source=chatgpt.com)

研究：

- 拼音 scheme
- 拼音字典
- speller
- 拼音规则
- segmentation
- candidate

--------------------------------------------------
5. 雾凇拼音
--------------------------------------------------

项目：

[雾凇拼音 Rime 配置](https://github.com/miantiao-me/Rime?utm_source=chatgpt.com)

重点研究其：

- 全拼
- 双拼
- 拼音词库
- speller
- algebra
- 拼音输入优化

但不要直接复制其配置到 CapsWriterIME。

只提取算法设计思想。

==================================================
二、Android 官方资料
==================================================

必须阅读：

[Android InputMethodService API](https://developer.android.com/reference/android/inputmethodservice/InputMethodService.html?utm_source=chatgpt.com)

以及：

[Android 创建输入法](https://developer.android.com/develop/ui/views/touch-and-input/creating-input-method?utm_source=chatgpt.com)

重点确认：

- InputMethodService 生命周期
- onStartInput
- onStartInputView
- currentInputConnection
- composing text
- candidate view
- 输入延迟
- 主线程约束

拼音算法必须避免阻塞 IME UI 线程。

==================================================
三、资料检索要求
==================================================

不要只搜索：

"pinyin completion"

必须主动检索：

pinyin segmentation
incomplete pinyin
partial pinyin
pinyin prefix matching
pinyin prefix completion
pinyin parser
pinyin syllable segmentation
pinyin fuzzy segmentation
pinyin correction
pinyin dictionary lookup
pinyin candidate generation
pinyin input method algorithm
Chinese IME pinyin parser
RIME pinyin speller
libpinyin pinyin segmentation

中文资料可以搜索：

不完整拼音
拼音补全
拼音分词
拼音前缀匹配
拼音输入法算法
拼音解析
拼音纠错
拼音模糊音
输入法候选生成
拼音 Trie
拼音词库索引

==================================================
四、必须回答的核心问题
==================================================

研究完成后必须回答：

1.
成熟输入法如何处理：

ni m

这种输入？

2.
如何区分：

完整音节
不完整音节
非法音节
拼音前缀？

3.
如何识别：

nim

可能是：

ni + m...

而不是非法拼音？

4.
如何识别：

nimen

优先解析成：

ni + men

5.
如何避免：

nim

产生大量无意义分词？

6.
如何利用词库反向约束拼音分词？

7.
Trie 是否适合当前项目？

8.
应该使用：

Trie
DFS
DP
Beam Search
还是其他算法？

必须根据实际源码研究后给出结论，不允许凭经验直接决定。

==================================================
五、核心目标示例
==================================================

必须重点解决：

输入：

ni m

理论上：

m

是一个不完整拼音前缀。

合法补全包括：

ma
mai
man
mang
mao
me
mei
men
meng
mi
mian
mie
min
ming
mo
mou
mu

然后组合：

ni + ma
ni + mai
ni + man
ni + mang
ni + mao
ni + me
ni + mei
ni + men
...

但是：

不要把这些拼音直接显示给用户。

必须继续进入词库：

拼音
↓
词库查询
↓
中文词
↓
候选排序

例如词库存在：

你妈 ni ma
你没 ni mei
你们 ni men

则最终候选应该优先出现：

你妈
你没
你们

==================================================
六、必须研究“词库约束分词”
==================================================

这是本任务最重要的研究点之一。

不要采用：

raw input
↓
暴力枚举所有拼音组合
↓
生成大量结果

应该研究成熟输入法是否采用：

raw input
↓
possible segmentation
↓
dictionary lookup
↓
score
↓
candidate

例如：

nimen

可能解析：

ni + men

也可能存在：

ni + me + n...

但如果：

ni + men

能够命中：

你们

则：

ni + men

应该获得更高分。

要求 Agent 找到成熟项目中的类似机制，并引用具体源码文件/函数。

==================================================
七、第一阶段功能边界
==================================================

第一阶段只实现：

【不完整拼音前缀补全】

例如：

m
→
ma / mai / man / mang / mao / me / mei / men / ...

暂时禁止加入：

模糊音
自动纠错
错拼修复
声母替换
方言音
语音相似音

例如：

z → zh
c → ch
s → sh

这些属于后续功能。

必须保持架构可扩展。

==================================================
八、推荐研究架构
==================================================

研究成熟项目后，如果没有更好的方案，优先评估：

PinyinSyllableTrie

负责：

合法完整音节
+
合法前缀

例如：

m
├── a
├── ai
├── an
├── ang
├── ao
├── e
├── ei
├── en
├── eng
├── i
├── ia
├── ...
└── u

然后：

PinyinSegmenter

负责：

nim
→
ni + m...

nimen
→
ni + men

PinyinCompletion

负责：

m
→
ma / mai / man / ...

DictionaryLookup

负责：

拼音序列
→
中文词

CandidateRanker

负责：

候选排序。

但是：

最终架构必须根据检索到的 libpinyin / RIME 源码重新评估。

不能机械采用上述设计。

==================================================
九、必须检查现有项目
==================================================

首先阅读：

app/src/main/java/com/jinn/inputmethod/PinyinEngine.kt

重点确认：

1. 当前词库格式
2. key 是什么
3. value 是什么
4. 拼音如何映射中文词
5. 当前候选查询流程
6. 当前是否已有前缀查询
7. 当前是否已有分词
8. 当前是否已有缓存
9. 当前候选排序算法
10. 自然码双拼如何实现

然后阅读：

PinyinKeyboardView.kt
PinyinKey.kt

确认：

输入：

n
ni
nim
nimen

之后：

PinyinEngine 接收到什么字符串？

候选栏如何刷新？

候选点击后如何清理 composing 状态？

==================================================
十、必须保护现有功能
==================================================

绝对不能破坏：

全拼正常输入。

自然码双拼。

英文输入。

候选选择。

删除。

空格。

回车。

候选翻页。

预测。

剪贴板。

语音输入。

输入法生命周期。

==================================================
十一、双拼必须完全隔离
==================================================

如果：

prefs.useShuangpin == true

必须继续走：

现有自然码双拼解析。

新增：

Incomplete Pinyin Parsing

只能作用于：

全拼模式。

不能让：

双拼输入

进入：

PinyinCompletion。

必须新增回归测试。

==================================================
十二、性能研究
==================================================

必须研究成熟项目如何避免：

每次按键扫描整个词库。

尤其关注：

Trie
HashMap
DAWG
MARISA Trie
索引表
prefix index
cache

librime 明确使用了 marisa-trie 等相关组件，其仓库说明可以作为数据结构研究入口。

但 CapsWriterIME 不应该为了这个功能直接引入大型 native 依赖。

优先考虑：

Kotlin HashMap
+
Trie
+
已有词库索引。

==================================================
十三、不要直接复制 GPL 代码
==================================================

特别注意：

libpinyin / ibus-libpinyin 等项目存在 GPL 许可。

研究其：

算法思想
数据结构
处理流程

可以。

但：

禁止直接复制 GPL 源代码进入 CapsWriterIME。

如果准备复用任何代码：

必须先检查 LICENSE。

必须报告：

项目
许可证
准备复用的内容
是否允许当前项目使用。

优先采用：

Android 官方资料
BSD / MIT / Apache 等兼容许可项目
以及自行实现算法。

==================================================
十四、最终研究报告格式
==================================================

完成资料检索后，先不要修改代码。

先输出：

# 1. 资料清单

每个资料：

项目：
链接：
许可证：
相关源码：
相关功能：

# 2. 成熟输入法实现分析

分别分析：

libpinyin
ibus-libpinyin
RIME

重点：

拼音解析
分词
补全
候选召回
候选排序

# 3. CapsWriterIME 当前实现

分析：

PinyinEngine.kt

当前数据结构：

当前查询流程：

当前瓶颈：

# 4. 推荐算法

必须明确：

数据结构：

解析算法：

补全算法：

词库召回：

排序：

缓存：

时间复杂度：

空间复杂度：

# 5. 示例

至少完整分析：

ni m

nim

nima

nimen

women

shanghai

# 6. 风险

包括：

候选爆炸
错误分词
性能
内存
双拼冲突
英文冲突
许可证
线程安全

# 7. 实施计划

分阶段：

Phase 1：
Trie / prefix

Phase 2：
Incomplete parsing

Phase 3：
Dictionary candidate recall

Phase 4：
Ranking

Phase 5：
Unit tests

Phase 6：
真机测试

==================================================
十五、实施阶段
==================================================

只有完成资料研究并确认方案后，才开始修改代码。

修改时：

优先小范围改动。

不要重写 PinyinEngine。

不要修改现有词库格式，除非证明现有格式无法支持。

不要引入大型第三方框架。

新增类必须符合项目现有 Kotlin 风格。

每个类添加 KDoc。

线程模型必须明确。

候选查询不能阻塞主线程。

==================================================
十六、必须新增的测试
==================================================

至少：

m
me
men

prefix test。

以及：

ni m
nim
nima
nimen
women

分词测试。

必须测试：

完整输入：

ni men

应该优先：

你们

不完整：

ni m

应该能够召回：

你们

同时：

xyz

不能崩溃。

超长输入：

不能 ANR。

双拼：

必须保持原有行为。

英文：

不得进入中文拼音补全。

==================================================
十七、最终验收
==================================================

最终必须证明：

ni m

可以得到合理中文候选。

nim

可以合理分词。

nimen

优先：

ni + men。

women

优先：

wo + men。

完整拼音：

不受影响。

自然码双拼：

不受影响。

英文：

不受影响。

候选查询：

无明显卡顿。

没有：

ANR
崩溃
候选爆炸
无限递归
内存泄漏。

最终报告必须包含：

源码资料链接
具体源码位置
许可证
算法依据
当前项目分析
实现方案
修改文件
测试结果
性能测试结果
真机测试结果

所有重要算法结论必须能追溯到：

【开源源码】
或
【官方文档】

禁止仅凭模型记忆声称“某输入法就是这样实现的”。
```

## 建议 Agent 重点看的资料

| 资料 | 用途 |
|---|---|
| [libpinyin](https://github.com/libpinyin/libpinyin?utm_source=chatgpt.com) | **重点研究拼音解析、分词、词库算法** |
| [ibus-libpinyin](https://github.com/libpinyin/ibus-libpinyin?utm_source=chatgpt.com) | 研究 correction / fuzzy / segment |
| [ibus-libpinyin 功能说明](https://github.com/libpinyin/ibus-libpinyin/wiki/ibus-libpinyin-features?utm_source=chatgpt.com) | 明确看到成熟项目已经实现的拼音纠错、模糊音、模糊分词能力  |
| [librime](https://github.com/rime/librime?utm_source=chatgpt.com) | **重点研究 Speller → Segment → Translator → Dictionary → Candidate 架构** |
| [RIME UserGuide](https://github.com/rime/home/wiki/UserGuide?utm_source=chatgpt.com) | 输入方案、拼音方案、候选机制  |
| [朙月拼音](https://github.com/rime/rime-luna-pinyin?utm_source=chatgpt.com) | 研究实际 RIME 全拼方案 |
| [雾凇拼音](https://github.com/miantiao-me/Rime?utm_source=chatgpt.com) | 研究现代 RIME 拼音配置、词库及全拼/双拼方案  |
| [Android InputMethodService](https://developer.android.com/reference/android/inputmethodservice/InputMethodService.html?utm_source=chatgpt.com) | IME 生命周期与输入连接约束  |
| [Android 创建输入法](https://developer.android.com/develop/ui/views/touch-and-input/creating-input-method?utm_source=chatgpt.com) | Android IME 官方开发规范  |

**特别建议：**让 Agent 不要一开始就写代码。先执行「资料检索 → 源码定位 → 当前 `PinyinEngine.kt` 分析 → 算法对比 → 方案评审」，最后才进入实现。这样更容易避免把 `ni m` 简单做成暴力字符串补全。