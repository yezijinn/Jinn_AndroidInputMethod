# 计划任务

> 面向接手后的第一批修复工作。所有问题按**严重程度从高到低**排序，优先修严重的。
> 生成时间：2026-09-15 ｜ 基线提交：`941ca68`

## 一、本次任务的约束

| 约束 | 说明 |
|---|---|
| **禁止修改语音部分** | 语音功能为自用，`MicRecorder` / `AsrClient` / `Protocol` / `MicButton` 以及 `JinnIme` 中的录音、WebSocket、识别结果处理链路一律不动。凡修复方案会触碰到这些代码的问题，一律**不修**，只登记 |
| 只改非语音 | 拼音键盘、剪贴板、保活、设置页、构建配置属于可改范围 |
| 无法真机验证 | 当前环境没有可用 Gradle 与真机，所有改动仅保证**编译通过 + 单测通过**，UI 手感类改动需要真机复验 |

## 二、严重程度分级标准

| 级别 | 判定标准 |
|---|---|
| **P0** | 功能缺失或缺陷，用户每天都会遇到，或涉及数据安全 |
| **P1** | 性能/稳定性隐患，或明显的代码正确性缺陷，影响部分场景 |
| **P2** | 轻微的体验问题、代码整洁度、文档与实现不符 |

---

## 三、任务清单

### P0 — 严重（优先修复）

| # | 问题 | 状态 | 涉及文件 |
|---|---|---|---|
| 1 | **拼音键盘按键按压反馈失效**：字母键 OnTouchListener 全部 `return true`，`PinyinKey.onTouchEvent` 永不执行，`pressed` 恒 false，按压高亮是死代码。用户按字母键看不到任何反应 | ✅ 已修复 | `PinyinKey.kt`、`PinyinKeyboardView.kt` |
| 2 | **隐私分类功能只有数据库字段没有 UI**：`is_private` 在 `ClipboardDb` 里完整实现（建表/upsert/去重合并/deleteAll 保护），但全项目无隐私 Tab、无标记入口、无明文掩码——`AGENTS.md` 承诺的功能实际不可用 | ✅ 已修复 | `ClipboardDb.kt`、`ClipboardHistoryActivity.kt`、`ClipboardPanelView.kt`、`strings.xml` |
| 3 | **粘贴状态机卡死**：IME 不回广播时 `pasting` 永久为 true，粘贴功能彻底失效；`pendingPasteText` 无时效，用户换输入框后旧文本会被粘到无关位置 | ✅ 已修复 | `ClipboardHistoryActivity.kt`、`JinnIme.kt` |
| 3.1 | **搜索面板不处理隐私条目**（剪贴板第三个展示入口，前两轮补齐掩码时漏掉）：隐私内容在此**明文直显**，且点一次就粘贴出去，隐私标记在该入口形同虚设 | ✅ 已修复 | `SearchPanelView.kt` |
| 3.2 | **退格「双击态」永不超时**：快速点两下退格后，此后任意时刻长按退格都会清空整个输入框（不可撤销）。形成双击态时未更新时间戳、且 DOWN 分支不校验时间 | ✅ 已修复 | `PinyinKeyboardView.kt` |

### P1 — 中等

| # | 问题 | 状态 | 涉及文件 |
|---|---|---|---|
| 4 | **每次按键双倍查询**：`queryWithCompletion` 在 `PinyinEngine.query()` 内部已跑过一次，候选刷新时又跑一遍，结果只用于打日志 | ✅ 已修复 | `PinyinKeyboardView.kt` |
| 5 | **Root 审计命令可能挂死**：`su()` 只读 stdout 不排空 stderr，管道写满即阻塞；且无超时。另 Backup 检查硬编码返回 ✓，无论规则怎么改都显示安全（假检查） | ✅ 已修复 | `ClipboardFirewall.kt` |
| 6 | **删除用了只读库句柄**：`ClipboardDb.delete` / `deleteAll` 用 `readableDatabase` 执行写操作，磁盘异常时会静默失败 | ✅ 已修复 | `ClipboardDb.kt` |
| 7 | **词库内存**：真机实测净占 **156MB**（堆上限 384MB 的 41%），加载 8424ms；预分配经计算**无调整空间**；唯一有效方向（省 `wordToPinyin` ≈45MB）需改核心候选链路，风险收益不匹配 | 📊 已实测结案（非缺陷，暂不优化） | `PinyinEngine.kt`、assets |
| 8 | **剪贴板搜索无谓解密过多**：解密全库再内存过滤，被分类筛掉的条目也照解密（解密是这条路径最贵的操作） | ✅ 已修复（分类下推 SQL，先过滤再解密） | `ClipboardHistoryActivity.kt` |
| 9 | **三份重复的列表 UI**：Activity / Panel / Search 的 adapter+分页+Holder 逐行雷同，改一处必漏两处 | 🟡 部分完成（**筛选逻辑已抽取**并补单测；渲染层重复仍需真机回归时再做） | `ClipboardDb.kt`、`ClipboardHistoryActivity.kt`、`ClipboardPanelView.kt` |
| 10 | **粘贴广播携带明文正文**：靠 `setPackage` + `RECEIVER_NOT_EXPORTED` 保护，任一处被改成都导出即泄露 | ⏸ 待办 | `ClipboardHistoryActivity.kt`、`JinnIme.kt` |
| 10.1 | **设置页「保存并重启」未保存剪贴板数量上限**：该项平时靠输入框失焦回调保存，时序不保证早于保存动作，用户改完点保存可能不生效 | ✅ 已修复 | `SettingsActivity.kt` |
| 10.2 | **剪贴板面板显示异常后不回滚视图**：26 键被隐藏后 `hideClipboardPanel` 因标志位为 false 直接 return，键盘永久损坏至重启 IME | ✅ 已修复 | `PinyinKeyboardView.kt` |
| 10.3 | **切换搜索模式不清拼音缓冲**：残留拼音不可见但仍生效，退格删错对象、空格把旧候选塞进搜索框 | ✅ 已修复 | `PinyinKeyboardView.kt` |
| 10.4 | **V 级日志全量落盘**：音频包每秒最多 10 条，每条都是一次 open/write/close 小文件 IO，纯浪费 | ✅ 已优化（V 级改走 logcat） | `Diagnostics.kt` |
| 10.5 | **音节前缀判断 O(音节数)**：`isTruePrefixOfSyllable` 每次遍历 400+ 音节做 startsWith，位于按键热路径 | ✅ 已优化（预建前缀集合，O(1)） | `PinyinEngine.kt` |
| 10.6 | **保活开关缓存单向失效**：缓存为 false 后不再重读，用户重新开启保活后前台服务被杀即无法拉起（互保失效） | ✅ 已修复 | `JinnAccessibilityService.kt` |
| 10.7 | **白名单命令短路**：`commands.all {}` 首条失败则后续三条不执行，部分成功本可生效 | ✅ 已修复 | `RootShizuku.kt` |
| 10.8 | **词库漏收「连写歧义」词**：拼音连写吞掉音节边界——「企鹅」(qi e) 连写成 qie 与单音节「切」同键，源词表漏收后用户输入 qie / 双拼 qiee **完全打不出该词**；同类经核实共 11 个词缺失（西安、饥饿、提案、彼岸、立案…） | ✅ 已修复（补词 + 检测脚本 + 防回归测试） | `assets/pinyin_phrases.txt`、`tools/dict_builder/` |
| 10.9 | **词库生成器存在覆盖隐患**：`write_outputs` 对每个带空格拼音各写一行，不同音节切分连写成同一键时产生重复键，app 加载 `phrasesByPinyin[key] = ...` 后写覆盖先写，词条静默丢失 | ✅ 已修复（先按 compact 聚合再输出） | `tools/dict_builder/dict_builder.py` |
| 10.10 | **词库疑存编码损坏词条**：字符串 `涓嶅`（UTF-8 被按 GBK 解码的典型产物，原文疑为「不」）出现在 603 个词的拼音键下 | ⏸ 待确认（属源数据编码问题，建议生成流程加编码校验） | `assets/pinyin_phrases.txt` |
| 24 | **新增「显示生僻字」开关（默认关闭）**：判定依据《通用规范汉字表》一级+二级 6500 字，三级及表外字为生僻；加载时过滤（跳过的不进内存），单字不进候选、含生僻字的词整条丢弃。开关只放设置页，键盘无入口 | ✅ 已实现 | `PinyinEngine.kt`、`Prefs.kt`、`SettingsActivity.kt`、`assets/common_chars.txt` |
| 25 | **APK 体积过大（10.6MB，95% 是词库）**：词库改用 xz 存储 + 流式解压，并配 `noCompress` 避免二次压缩 | ✅ 已优化（10.6MB → **8.4MB**，−20.8%；代价：加载 +2.2s，在后台线程） | `assets/pinyin_phrases.txt.xz`、`app/build.gradle.kts`、`PinyinEngine.kt` |

### P2 — 轻微

| # | 问题 | 状态 | 涉及文件 |
|---|---|---|---|
| 11 | **注释与实现完全相反**：`showClipboardPanel` 的 KDoc 写「INVISIBLE + 零 layoutParams 修改」，实际是「GONE + 固定 162dp×2 高度」 | ✅ 已修复 | `PinyinKeyboardView.kt` |
| 12 | **根目录 `ses_*.json`（3.6MB，疑似云凭证）未被 .gitignore 忽略**，有误提交风险 | ✅ 已修复 | `.gitignore` |
| 13 | 字母键抬起无命中判定：手指从 Q 滑到 W 抬起仍上屏 Q（用户看到的是自己按了 W） | ✅ 已修复 | `PinyinKeyboardView.kt` |
| 14 | 候选栏每次按键 `removeAllViews()` 重建全部 TextView | ❌ 不修（收益不敌风险，见下） | `PinyinKeyboardView.kt` |
| 15 | 搜索防抖缺少 scrollToken，快速输入时可能出现滚动残留 | ✅ 已修复 | `ClipboardHistoryActivity.kt` |
| 16 | Root 增强开关递归触发：程序化拨回关闭会再次触发监听器，把「Root 不可用」提示覆盖成功能描述 | ✅ 已修复 | `SettingsActivity.kt` |
| 17 | 导出诊断包的后台回调未检查 Activity 已销毁，可能操作已 detach 的 view | ✅ 已修复 | `SettingsActivity.kt` |
| 18 | Spinner 初始化时的 `setSelection` 回调会把「默认键盘模式」静默改写成第 0 项 | ✅ 已修复 | `SettingsActivity.kt` |
| 19 | 搜索态点「预测词」会把文本提交到宿主输入框而非搜索框（`onPredictionSelected` 漏搜索分支） | ✅ 已修复 | `PinyinKeyboardView.kt` |
| 20 | 切换大写锁定不清拼音串，切回小写后退格删错对象 | ✅ 已修复 | `PinyinKeyboardView.kt` |
| 21 | 方向面板与剪贴板面板互斥只做一半，剪贴板打开时点「方向」无反应 | ✅ 已修复 | `PinyinKeyboardView.kt` |
| 22 | 拖选态「行首 / 行末」跳到全文首尾，与光标态的按行行为不一致 | ✅ 已修复 | `TextSelection.kt` |
| 23 | **AGENTS.md 与代码不符**：测试命令写死本机 gradle 路径、项目结构缺 3 个模块、称设置页有「测试连接」（实际没有）、未记录语音禁改约束 | ✅ 已修正 | `AGENTS.md` |

### 待核实项已处理完毕（第八轮逐条读码核实，非盲修）

| 项 | 结论 |
|---|---|
| `onPredictionSelected` 漏搜索模式判断 | ✅ 已修复：搜索态点预测词会串进宿主输入框 |
| 切大写 / 切层不清拼音串 | ✅ 部分修复：大写锁定已清；符号层 / 数字层**保持不清**（常用于输中文标点后继续拼音，属设计权衡） |
| 方向面板与剪贴板面板互斥只做一半 | ✅ 已修复 |
| 拖选态行首 / 行末跳全文首尾 | ✅ 已修复：与光标态统一为按行计算，并补了多行测试 |
| `capsMode` 生命周期不复位 | ⏸ 维持现状：大写锁定是用户显式设置的持久态，每次聚焦都重置会让「锁定」失去意义；点一下 shift 即可退出 |

### 不修（受语音约束限制）

| 问题 | 不修原因 |
|---|---|
| `refreshConfig()` / `ACTION_CONFIG_UPDATED` 是死代码（无人发送该广播，实际靠 SettingsActivity 杀进程重启 IME） | 唯一的调用目标 `refreshConfig()` 内容是 `asr.connect()` 重连，属于语音链路。要让它生效或删除都必然改动语音代码，本次不动。**已知影响**：设置页改服务端地址后 IME 不会热更新，需等进程重启 |
| `AsrClient` / `MicRecorder` 零测试覆盖 | 属于语音模块。若后续解禁，建议优先补 WebSocket 重连与 VAD 的单测 |
| VAD 静音阈值（120s）偏长 | 属于语音模块 |

---

## 四、待办项说明与建议

**#7 词库内存 —— 已真机实测结案（结论：暂不优化）**

实测（真机 logcat，2026-09-15）：

```
词库加载前 9MB → 加载后 165MB（堆上限 384MB）  ⇒ 词库净占 156MB，41%
加载耗时 8424ms｜词语键 1,048,342｜反向索引 1,162,486｜音节 422
```

**逐条核对原先设想的优化方向，结论与预判不同**：

1. ❌ **降低 HashMap 预分配**——没有空间。`HashMap(1_500_000)` 经 `tableSizeFor`
   得容量 2^21 = 2,097,152，扩容阈值 1,572,864 已高于实际 1,162,486 条，
   本来就不会扩容；`HashMap(2_000_000)` 同理。
2. ⚠️ **省掉 `wordToPinyin`（≈45MB，29%）**——唯一真正有效的方向，但它是功能必需的：
   `consumption()` 靠它反查候选词拼音以计算残码消费区间，`predict()` 靠它做联想。
   要省必须让候选在生成时就携带来源拼音（改 `query()` 返回类型 + `consumption()` 入参），
   **改动覆盖候选生成/消费/预测/测试全链路**。
3. ❌ 共享 char[] / 按需加载——工程量与风险更高，收益不确定。

**判定：不做。** 当前设备堆上限 384MB、占用 165MB 有充足余量，无 OOM 压力；
为此重构核心候选链路不划算。**若将来换到堆上限 192/256MB 的设备出现 OOM，
再按方向 2（候选携带拼音）重构**，届时用 `PinyinEngineTest` / `PinyinCompletionTest` 兜底。

**#8 搜索性能 —— 已完成（本轮修复）**
分类 / 收藏 / 隐私已下推到 SQL，先过滤再解密，不再解密大量本就被筛掉的条目
（解密是这条路径上最贵的操作）。
**连带修正**：SQL 过滤后 `globalIndex` 变成「分类内偏移」，若序号基准仍用全库总数会把
序号整体算大——已改为分类内总数，与非搜索态（`categoryTotal`）语义一致。
剩余可选优化：对近期 N 条建内存明文缓存，进一步减少解密次数。

**#9 三份重复 UI —— 已完成一半（本轮）**
先把风险最低、收益最确定的一块抽掉了：**伪分类 → SQL 参数的翻译**。
原先这段三行代码在 5 处各写一遍（Activity 的首页/下一页/搜索、Panel 的刷新/下一页），
且是本项目最容易写错的地方——把 `"FAVORITE"` 当 category 传给 SQL 会让列表永远为空。
现已收敛为 `ClipboardFilter.of(raw)`，并补了 `ClipboardFilterTest`（7 个用例，纯 JVM），
把这个规则钉死。同时 UI 里的 `CATEGORY_FAVORITE` / `CATEGORY_PRIVATE` 常量改为引用
`ClipboardFilter.PSEUDO_*`，不再各定义一份。

剩余的 adapter / 分页 / Holder 渲染层重复仍建议有真机回归时再做：
届时可抽一个共享的 `ClipboardListController`（分页 + 解密 + 令牌 + 空态），
三个界面只保留各自的容器与样式。

**#10 广播明文 —— 决定不修**
现有保护（`setPackage` + `RECEIVER_NOT_EXPORTED`）已能阻止跨应用接收，明文只在同进程内传递。
改为「只传 itemId、IME 侧查库解密」虽能彻底消除明文出进程，但会把同步粘贴改成异步查库，
触碰的是**粘贴这条只有真机才能验证的核心链路**，当前无法回归验证，风险与收益不对等。
建议具备真机回归条件后再做。

**#14 候选栏重建 —— 决定不修**
`removeAllViews()` 后重建的是纯 `new TextView`（无 inflate），省不掉后续 measure/layout
（内容变了必然重排），而候选数量每次波动很大、复用命中率有限。
收益不大却要处理「不同渲染分支（普通/预测/符号组）样式不同」的复用污染问题，
风险不划算。体积/卡顿若真成问题，应改用 RecyclerView 整体重写，而非打补丁。

---

## 五、验证方式

```bash
# 编译 + 单元测试（7 个测试类，~100 用例，纯 JVM 无需设备）
.\gradlew.bat testDebugUnitTest
```

本轮所有已修复项均已通过 `testDebugUnitTest`（BUILD SUCCESSFUL）。

> 注意：本机 Gradle Wrapper 原本无法下载发行版，已通过腾讯云镜像手动安装 gradle-8.9；
> 同时 `settings.gradle.kts` 启用了项目注释里预留的阿里云镜像（国内直连
> dl.google.com / repo.maven.apache.org 会解析失败）。依赖缓存已建立，后续构建会快很多。

**必须真机复验的改动**（编译通过不等于体验正确）：
- 按键按压反馈（#1）：按字母键应有高亮
- 隐私 Tab 与掩码（#2）：标记隐私 → 列表显示掩码 → 点一次展开 → 再点粘贴
- 粘贴超时（#3）：正常粘贴仍应成功关闭页面
